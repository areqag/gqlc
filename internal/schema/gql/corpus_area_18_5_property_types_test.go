package gql

// corpusArea185PropertyTypes holds the corpus entries for test/data/schema/gql/corpus/18.5-property-types/
// — ISO GQL clause 18.5 <property types specification>. One area variable per clause so authors never share a file;
// corpusAreas requires every entry here to live under that directory.
var corpusArea185PropertyTypes = []corpusEntry{}
