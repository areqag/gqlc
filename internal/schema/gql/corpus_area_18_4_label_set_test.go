package gql

// corpusArea184LabelSet holds the corpus entries for test/data/schema/gql/corpus/18.4-label-set/
// — ISO GQL clause 18.4 <label set phrase> and <label set specification>. One area variable per clause so authors never share a file;
// corpusAreas requires every entry here to live under that directory.
var corpusArea184LabelSet = []corpusEntry{}
