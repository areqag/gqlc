package gql

// corpusArea187PropertyValueType holds the corpus entries for test/data/schema/gql/corpus/18.7-property-value-type/
// — ISO GQL clause 18.7 <property value type>. One area variable per clause so authors never share a file;
// corpusAreas requires every entry here to live under that directory.
var corpusArea187PropertyValueType = []corpusEntry{}
