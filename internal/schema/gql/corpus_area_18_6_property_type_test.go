package gql

// corpusArea186PropertyType holds the corpus entries for test/data/schema/gql/corpus/18.6-property-type/
// — ISO GQL clause 18.6 <property type>. One area variable per clause so authors never share a file;
// corpusAreas requires every entry here to live under that directory.
var corpusArea186PropertyType = []corpusEntry{}
