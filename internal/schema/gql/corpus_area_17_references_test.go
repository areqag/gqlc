package gql

// corpusArea17References holds the corpus entries for test/data/schema/gql/corpus/17-references/
// — ISO GQL clause 17 <references>. One area variable per clause so authors never share a file;
// corpusAreas requires every entry here to live under that directory.
var corpusArea17References = []corpusEntry{}
