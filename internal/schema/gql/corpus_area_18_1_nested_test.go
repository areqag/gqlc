package gql

// corpusArea181NestedGraphType holds the corpus entries for test/data/schema/gql/corpus/18.1-nested-graph-type/
// — ISO GQL clause 18.1 <nested graph type specification>. One area variable per clause so authors never share a file;
// corpusAreas requires every entry here to live under that directory.
var corpusArea181NestedGraphType = []corpusEntry{}
