// Code generated by gqlc dev. DO NOT EDIT.

package uniononlyuuidcarrierdecodeonly

import (
	"context"
	"fmt"

	"github.com/neo4j/neo4j-go-driver/v5/neo4j"
	"github.com/neo4j/neo4j-go-driver/v5/neo4j/dbtype"
)

const accountWholeQueryText = `MATCH (a:Account) WHERE a.id = $id RETURN a`

// AccountWhole executes the AccountWhole query.
//
//	MATCH (a:Account) WHERE a.id = $id RETURN a
func (q *queries) AccountWhole(ctx context.Context, arg int64) (Account, error) {
	records, err := q.db.run(ctx, accountWholeQueryText, map[string]any{"id": arg}, neo4j.AccessModeRead)
	if err != nil {
		return Account{}, err
	}
	if len(records) == 0 {
		return Account{}, ErrNoRows
	}
	if len(records) > 1 {
		return Account{}, ErrMultipleResults
	}
	node, isNil, err := neo4j.GetRecordValue[dbtype.Node](records[0], "a")
	if err != nil {
		return Account{}, fmt.Errorf("AccountWhole: decode column %q: %w", "a", err)
	}
	if isNil {
		return Account{}, fmt.Errorf("AccountWhole: column %q is non-nullable but arrived null", "a")
	}
	value, err := decodeAccount(node)
	if err != nil {
		return Account{}, fmt.Errorf("AccountWhole: decode column %q: %w", "a", err)
	}
	return value, nil
}

const accountEithersQueryText = `MATCH (a:Account) RETURN collect(a.either) AS eithers`

// AccountEithers executes the AccountEithers query.
//
//	MATCH (a:Account) RETURN collect(a.either) AS eithers
func (q *queries) AccountEithers(ctx context.Context) ([][]any, error) {
	records, err := q.db.run(ctx, accountEithersQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]any, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "eithers")
		if err != nil {
			return nil, fmt.Errorf("AccountEithers: decode column %q: %w", "eithers", err)
		}
		if isNil {
			return nil, fmt.Errorf("AccountEithers: column %q is non-nullable but arrived null", "eithers")
		}
		acc := make([]any, 0, len(value))
		for i, elem := range value {
			v, err := decodeUnion26f53c9e(elem)
			if err != nil {
				return nil, fmt.Errorf("AccountEithers: decode column %q element %d: %w", "eithers", i, err)
			}
			acc = append(acc, v)
		}
		out = append(out, acc)
	}
	return out, nil
}
