// Code generated by gqlc dev. DO NOT EDIT.

package nestedlistelementprojection

import (
	"context"
	"fmt"

	"github.com/neo4j/neo4j-go-driver/v5/neo4j"
)

const tagsPairQueryText = `MATCH (g:Grid) RETURN [g.tags, g.tags] AS tagss`

// TagsPair executes the TagsPair query.
//
//	MATCH (g:Grid) RETURN [g.tags, g.tags] AS tagss
func (q *queries) TagsPair(ctx context.Context) ([][]*[]*string, error) {
	records, err := q.db.run(ctx, tagsPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]*[]*string, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "tagss")
		if err != nil {
			return nil, fmt.Errorf("TagsPair: decode column %q: %w", "tagss", err)
		}
		if isNil {
			return nil, fmt.Errorf("TagsPair: column %q is non-nullable but arrived null", "tagss")
		}
		acc := make([]*[]*string, 0, len(value))
		for i, elem := range value {
			if elem == nil {
				acc = append(acc, nil)
				continue
			}
			inner1, ok := elem.([]any)
			if !ok {
				return nil, fmt.Errorf("TagsPair: decode column %q element %d: expected []any, got %T", "tagss", i, elem)
			}
			innerAcc1 := make([]*string, 0, len(inner1))
			for i, elem1 := range inner1 {
				if elem1 == nil {
					innerAcc1 = append(innerAcc1, nil)
					continue
				}
				v, ok := elem1.(string)
				if !ok {
					return nil, fmt.Errorf("TagsPair: decode column %q element %d: expected string, got %T", "tagss", i, elem1)
				}
				innerAcc1 = append(innerAcc1, &v)
			}
			acc = append(acc, &innerAcc1)
		}
		out = append(out, acc)
	}
	return out, nil
}

const ranksPairQueryText = `MATCH (g:Grid) RETURN [g.ranks, g.ranks] AS rankss`

// RanksPair executes the RanksPair query.
//
//	MATCH (g:Grid) RETURN [g.ranks, g.ranks] AS rankss
func (q *queries) RanksPair(ctx context.Context) ([][]*[]int16, error) {
	records, err := q.db.run(ctx, ranksPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]*[]int16, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "rankss")
		if err != nil {
			return nil, fmt.Errorf("RanksPair: decode column %q: %w", "rankss", err)
		}
		if isNil {
			return nil, fmt.Errorf("RanksPair: column %q is non-nullable but arrived null", "rankss")
		}
		acc := make([]*[]int16, 0, len(value))
		for i, elem := range value {
			if elem == nil {
				acc = append(acc, nil)
				continue
			}
			inner1, ok := elem.([]any)
			if !ok {
				return nil, fmt.Errorf("RanksPair: decode column %q element %d: expected []any, got %T", "rankss", i, elem)
			}
			innerAcc1 := make([]int16, 0, len(inner1))
			for i, elem1 := range inner1 {
				v, ok := elem1.(int64)
				if !ok {
					return nil, fmt.Errorf("RanksPair: decode column %q element %d: expected int64, got %T", "rankss", i, elem1)
				}
				vn, err := narrowInt[int16](v)
				if err != nil {
					return nil, fmt.Errorf("RanksPair: decode column %q element %d: %w", "rankss", i, err)
				}
				innerAcc1 = append(innerAcc1, vn)
			}
			acc = append(acc, &innerAcc1)
		}
		out = append(out, acc)
	}
	return out, nil
}

const labelsPairQueryText = `MATCH (g:Grid) RETURN [g.labels, g.labels] AS labelss`

// LabelsPair executes the LabelsPair query.
//
//	MATCH (g:Grid) RETURN [g.labels, g.labels] AS labelss
func (q *queries) LabelsPair(ctx context.Context) ([][][]*string, error) {
	records, err := q.db.run(ctx, labelsPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][][]*string, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "labelss")
		if err != nil {
			return nil, fmt.Errorf("LabelsPair: decode column %q: %w", "labelss", err)
		}
		if isNil {
			return nil, fmt.Errorf("LabelsPair: column %q is non-nullable but arrived null", "labelss")
		}
		acc := make([][]*string, 0, len(value))
		for i, elem := range value {
			inner1, ok := elem.([]any)
			if !ok {
				return nil, fmt.Errorf("LabelsPair: decode column %q element %d: expected []any, got %T", "labelss", i, elem)
			}
			innerAcc1 := make([]*string, 0, len(inner1))
			for i, elem1 := range inner1 {
				if elem1 == nil {
					innerAcc1 = append(innerAcc1, nil)
					continue
				}
				v, ok := elem1.(string)
				if !ok {
					return nil, fmt.Errorf("LabelsPair: decode column %q element %d: expected string, got %T", "labelss", i, elem1)
				}
				innerAcc1 = append(innerAcc1, &v)
			}
			acc = append(acc, innerAcc1)
		}
		out = append(out, acc)
	}
	return out, nil
}
