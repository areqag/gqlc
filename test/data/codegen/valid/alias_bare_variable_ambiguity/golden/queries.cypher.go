// Code generated by gqlc dev. DO NOT EDIT.

package aliasbarevariableambiguity

import (
	"context"
	"fmt"

	"github.com/neo4j/neo4j-go-driver/v5/neo4j"
)

const peopleBareQueryText = `MATCH (p:Person) RETURN p.name, p.age`

type PeopleBareRow struct {
	Name string
	Age  int64
}

// PeopleBare executes the PeopleBare query.
//
//	MATCH (p:Person) RETURN p.name, p.age
func (q *Queries) PeopleBare(ctx context.Context) ([]PeopleBareRow, error) {
	records, err := q.db.run(ctx, peopleBareQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([]PeopleBareRow, 0, len(records))
	for _, record := range records {
		var row PeopleBareRow
		value0, isNil, err := neo4j.GetRecordValue[string](record, "p.name")
		if err != nil {
			return nil, fmt.Errorf("PeopleBare: decode column %q: %w", "p.name", err)
		}
		if isNil {
			return nil, fmt.Errorf("PeopleBare: column %q is non-nullable but arrived null", "p.name")
		}
		row.Name = value0
		value1, isNil, err := neo4j.GetRecordValue[int64](record, "p.age")
		if err != nil {
			return nil, fmt.Errorf("PeopleBare: decode column %q: %w", "p.age", err)
		}
		if isNil {
			return nil, fmt.Errorf("PeopleBare: column %q is non-nullable but arrived null", "p.age")
		}
		row.Age = value1
		out = append(out, row)
	}
	return out, nil
}

const peopleAliasedQueryText = `MATCH (p:Person) RETURN p.name AS name, p.age`

type PeopleAliasedRow struct {
	Name string
	Age  int64
}

// PeopleAliased executes the PeopleAliased query.
//
//	MATCH (p:Person) RETURN p.name AS name, p.age
func (q *Queries) PeopleAliased(ctx context.Context) ([]PeopleAliasedRow, error) {
	records, err := q.db.run(ctx, peopleAliasedQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([]PeopleAliasedRow, 0, len(records))
	for _, record := range records {
		var row PeopleAliasedRow
		value0, isNil, err := neo4j.GetRecordValue[string](record, "name")
		if err != nil {
			return nil, fmt.Errorf("PeopleAliased: decode column %q: %w", "name", err)
		}
		if isNil {
			return nil, fmt.Errorf("PeopleAliased: column %q is non-nullable but arrived null", "name")
		}
		row.Name = value0
		value1, isNil, err := neo4j.GetRecordValue[int64](record, "p.age")
		if err != nil {
			return nil, fmt.Errorf("PeopleAliased: decode column %q: %w", "p.age", err)
		}
		if isNil {
			return nil, fmt.Errorf("PeopleAliased: column %q is non-nullable but arrived null", "p.age")
		}
		row.Age = value1
		out = append(out, row)
	}
	return out, nil
}
