// Code generated by gqlc dev. DO NOT EDIT.

package certifiedlistelement

import (
	"context"
	"fmt"

	"github.com/neo4j/neo4j-go-driver/v5/neo4j"
)

const personIdQueryText = `MATCH (p:Person) RETURN p.id AS id`

// PersonId executes the PersonId query.
//
//	MATCH (p:Person) RETURN p.id AS id
func (q *queries) PersonId(ctx context.Context) ([]int64, error) {
	records, err := q.db.run(ctx, personIdQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([]int64, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[int64](record, "id")
		if err != nil {
			return nil, fmt.Errorf("PersonId: decode column %q: %w", "id", err)
		}
		if isNil {
			return nil, fmt.Errorf("PersonId: column %q is non-nullable but arrived null", "id")
		}
		out = append(out, value)
	}
	return out, nil
}

const personPairQueryText = `MATCH (p:Person) RETURN [p.id, p.age] AS pair`

// PersonPair executes the PersonPair query.
//
//	MATCH (p:Person) RETURN [p.id, p.age] AS pair
func (q *queries) PersonPair(ctx context.Context) ([][]int64, error) {
	records, err := q.db.run(ctx, personPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]int64, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "pair")
		if err != nil {
			return nil, fmt.Errorf("PersonPair: decode column %q: %w", "pair", err)
		}
		if isNil {
			return nil, fmt.Errorf("PersonPair: column %q is non-nullable but arrived null", "pair")
		}
		acc := make([]int64, 0, len(value))
		for i, elem := range value {
			v, ok := elem.(int64)
			if !ok {
				return nil, fmt.Errorf("PersonPair: decode column %q element %d: expected int64, got %T", "pair", i, elem)
			}
			acc = append(acc, v)
		}
		out = append(out, acc)
	}
	return out, nil
}

const personCollectIdQueryText = `MATCH (p:Person) RETURN collect(p.id) AS ids`

// PersonCollectId executes the PersonCollectId query.
//
//	MATCH (p:Person) RETURN collect(p.id) AS ids
func (q *queries) PersonCollectId(ctx context.Context) ([]int64, error) {
	records, err := q.db.run(ctx, personCollectIdQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	if len(records) == 0 {
		return nil, ErrNoRows
	}
	if len(records) > 1 {
		return nil, ErrMultipleResults
	}
	value, isNil, err := neo4j.GetRecordValue[[]any](records[0], "ids")
	if err != nil {
		return nil, fmt.Errorf("PersonCollectId: decode column %q: %w", "ids", err)
	}
	if isNil {
		return nil, fmt.Errorf("PersonCollectId: column %q is non-nullable but arrived null", "ids")
	}
	acc := make([]int64, 0, len(value))
	for i, elem := range value {
		v, ok := elem.(int64)
		if !ok {
			return nil, fmt.Errorf("PersonCollectId: decode column %q element %d: expected int64, got %T", "ids", i, elem)
		}
		acc = append(acc, v)
	}
	return acc, nil
}

const personCollectPairQueryText = `MATCH (p:Person) RETURN collect([p.id, p.age]) AS pairs`

// PersonCollectPair executes the PersonCollectPair query.
//
//	MATCH (p:Person) RETURN collect([p.id, p.age]) AS pairs
func (q *queries) PersonCollectPair(ctx context.Context) ([][]int64, error) {
	records, err := q.db.run(ctx, personCollectPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	if len(records) == 0 {
		return nil, ErrNoRows
	}
	if len(records) > 1 {
		return nil, ErrMultipleResults
	}
	value, isNil, err := neo4j.GetRecordValue[[]any](records[0], "pairs")
	if err != nil {
		return nil, fmt.Errorf("PersonCollectPair: decode column %q: %w", "pairs", err)
	}
	if isNil {
		return nil, fmt.Errorf("PersonCollectPair: column %q is non-nullable but arrived null", "pairs")
	}
	acc := make([][]int64, 0, len(value))
	for i, elem := range value {
		inner1, ok := elem.([]any)
		if !ok {
			return nil, fmt.Errorf("PersonCollectPair: decode column %q element %d: expected []any, got %T", "pairs", i, elem)
		}
		innerAcc1 := make([]int64, 0, len(inner1))
		for i, elem1 := range inner1 {
			v, ok := elem1.(int64)
			if !ok {
				return nil, fmt.Errorf("PersonCollectPair: decode column %q element %d: expected int64, got %T", "pairs", i, elem1)
			}
			innerAcc1 = append(innerAcc1, v)
		}
		acc = append(acc, innerAcc1)
	}
	return acc, nil
}

const personNullablePairQueryText = `MATCH (p:Person) RETURN [p.score, p.score] AS scores`

// PersonNullablePair executes the PersonNullablePair query.
//
//	MATCH (p:Person) RETURN [p.score, p.score] AS scores
func (q *queries) PersonNullablePair(ctx context.Context) ([][]int64, error) {
	records, err := q.db.run(ctx, personNullablePairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]int64, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "scores")
		if err != nil {
			return nil, fmt.Errorf("PersonNullablePair: decode column %q: %w", "scores", err)
		}
		if isNil {
			return nil, fmt.Errorf("PersonNullablePair: column %q is non-nullable but arrived null", "scores")
		}
		acc := make([]int64, 0, len(value))
		for i, elem := range value {
			v, ok := elem.(int64)
			if !ok {
				return nil, fmt.Errorf("PersonNullablePair: decode column %q element %d: expected int64, got %T", "scores", i, elem)
			}
			acc = append(acc, v)
		}
		out = append(out, acc)
	}
	return out, nil
}

const personNarrowPairQueryText = `MATCH (p:Person) RETURN [p.rank, p.rank] AS ranks`

// PersonNarrowPair executes the PersonNarrowPair query.
//
//	MATCH (p:Person) RETURN [p.rank, p.rank] AS ranks
func (q *queries) PersonNarrowPair(ctx context.Context) ([][]int32, error) {
	records, err := q.db.run(ctx, personNarrowPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]int32, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "ranks")
		if err != nil {
			return nil, fmt.Errorf("PersonNarrowPair: decode column %q: %w", "ranks", err)
		}
		if isNil {
			return nil, fmt.Errorf("PersonNarrowPair: column %q is non-nullable but arrived null", "ranks")
		}
		acc := make([]int32, 0, len(value))
		for i, elem := range value {
			v, ok := elem.(int64)
			if !ok {
				return nil, fmt.Errorf("PersonNarrowPair: decode column %q element %d: expected int64, got %T", "ranks", i, elem)
			}
			vn, err := narrowInt[int32](v)
			if err != nil {
				return nil, fmt.Errorf("PersonNarrowPair: decode column %q element %d: %w", "ranks", i, err)
			}
			acc = append(acc, vn)
		}
		out = append(out, acc)
	}
	return out, nil
}

const personFoldPairQueryText = `MATCH (p:Person) RETURN [p.id + p.age] AS folded`

// PersonFoldPair executes the PersonFoldPair query.
//
//	MATCH (p:Person) RETURN [p.id + p.age] AS folded
func (q *queries) PersonFoldPair(ctx context.Context) ([][]any, error) {
	records, err := q.db.run(ctx, personFoldPairQueryText, nil, neo4j.AccessModeRead)
	if err != nil {
		return nil, err
	}
	out := make([][]any, 0, len(records))
	for _, record := range records {
		value, isNil, err := neo4j.GetRecordValue[[]any](record, "folded")
		if err != nil {
			return nil, fmt.Errorf("PersonFoldPair: decode column %q: %w", "folded", err)
		}
		if isNil {
			return nil, fmt.Errorf("PersonFoldPair: column %q is non-nullable but arrived null", "folded")
		}
		acc := make([]any, 0, len(value))
		for _, elem := range value {
			acc = append(acc, elem)
		}
		out = append(out, acc)
	}
	return out, nil
}
