-- elementTypeSpecification#2, the edge branch. Area C's edge files reach it too,
-- but the alternative is assigned to area A, so without a carrier here the tag
-- would rest on spellings another area is free to rewrite. One graph type holding
-- both branches, and nothing else.
CREATE GRAPH TYPE Seed { (a:A), (b:B), (a)-[:R]->(b) }
