-- elementTypeSpecification#2 (the edge branch). check.sh reports this covered
-- because area C's edge files also reach it, but the tag is assigned to area A
-- and would go red if C dropped or rewrote a spelling — so it needs a carrier
-- in this worktree too.
CREATE GRAPH TYPE Seed { (a:A), (b:B), (a)-[:R]->(b) }
