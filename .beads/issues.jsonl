{"_type":"issue","id":"gqlc-u91z","title":"vuln: on a non-release Go toolchain the scan is silently blind to every stdlib advisory","description":"`just vuln` reports success over the entire standard-library half of the scan\nwhen the Go toolchain is a custom build rather than a release. No warning, no\ndiagnostic, rc=0 — the same fail-open shape bd gqlc-s3lt closed for an empty\nwalk, one layer further out.\n\n## Single-variable A/B (first-party, 2026-08-17)\n\nSame tree (gqlc at 15e7a365), same tags, same `govulncheck@latest`, same\nvulnerability database, minutes apart. Only GOTOOLCHAIN differs:\n\n    GOTOOLCHAIN=local      go1.26.5-X:nodwarf5   -\u003e  \"No vulnerabilities found.\"\n    GOTOOLCHAIN=go1.26.5   go1.26.5 (release)    -\u003e  \"Your code is affected by 3\n                                                      vulnerabilities from the Go\n                                                      standard library.\"\n\nArm B names them: GO-2026-6218 (net/url), GO-2026-6089 (net/http), GO-2026-6088\n(encoding/xml), plus GO-2026-5942 and GO-2026-5026, with a concrete trace —\n\n    internal/grammar/gql/gen/gql_parser.go:2620:21: gen.GQLParserInit calls\n    sync.Once.Do, which eventually calls asn1.Unmarshal\n\nArm A reports none of them and exits 0.\n\n## Why this matters beyond one machine\n\nThis is how the failure was met: `just vuln` was rc=0 locally on both master and\nthe branch under review, and govulncheck was RED in CI on both. The local gate\nagreed with nothing. A developer running it before pushing gets a green that\ncarries no information about the largest attack surface in the binary, and the\nfirst place the truth appears is a required check on a PR.\n\nIt also inverts the usual local/CI story. The normal complaint is that CI is\nstricter; here the local gate is not stricter or looser, it is BLIND — the\nfinding is absent rather than downgraded, so no amount of reading the output\nreveals it.\n\n## Mechanism\n\ngovulncheck attributes standard-library findings by resolving the toolchain's\nversion to a `go1.N.P` release the vulnerability database has entries for. A\nversion string like `go1.26.5-X:nodwarf5` does not resolve, so the stdlib is\ntreated as having no known advisories at all rather than as unknown. Third-party\nmodule findings are unaffected — both arms report the same module-level results\n— which is why the output still looks like a real scan.\n\n## Fix\n\nThe recipe should refuse a toolchain whose version it cannot place, by the\nbranch's own rule that an unmeasurable input is the measurement broken and not a\nclean bill of health. Concretely, before scanning:\n\n  - read `go env GOVERSION`;\n  - require it to match `^go1\\.[0-9]+(\\.[0-9]+)?$` (the same shape\n    `internal/tools/modscope`'s `releaseTag` already recognises);\n  - if it does not, refuse and say that stdlib advisories will not be reported\n    under this toolchain, naming GOTOOLCHAIN as the lever.\n\nA pin belongs with it: assert that the scan output contains a standard-library\nsection at all, so the day the attribution silently changes shape the gate\nnotices.\n","notes":"PR #846 open (NOT merged; merge held by coordinator). Branch fix/vuln-toolchain-blindness, head 9c4892b2.\n\nFIX SHAPE: reads govulncheck's OWN SBOM header ('...modules and the \u003cver\u003e standard library:') per module rather than matching GOVERSION here. An empty version formats back to a bare 'go', which IS the unplaced rendering.\n\nREJECTED the regex the bead proposed (^go1\\.[0-9]+(\\.[0-9]+)?$). x/vuln@v1.7.0 internal/semver/semver.go:62 is the real pattern: ^go(\\d+\\.\\d+)(\\.\\d+|)((beta|rc|-pre)(\\d+))?$ over strings.Fields(tag)[0]. Drove it: that regex falsely refuses go1, go1.27rc1, go1.27beta1, go1.27-pre1 and 'go1.26.5 X:nodwarf5' — 5 of 11 rows, including EVERY Go release candidate. Would have been an outage.\n\nALSO REJECTED the bead's proposed pin ('assert the output contains a standard-library section'). Measured arm C (release go1.26.6, current pin): zero stdlib findings, zero 'Standard library' lines, rc=0 — textually identical to blind arm A. That pin fails the GOOD state.\n\nMEASURED: armA go1.26.5-X:nodwarf5 -\u003e rc=0, 0 ids. armB go1.26.5 release -\u003e rc=1, 8 ids, 3 CALLED (GO-2026-6091/6090/5972). End-to-end 'just vuln' on master's justfile under arm A: rc=0, fully green.\n\nNote: arm A no longer reproducible on master verbatim — go.mod pins 1.26.6 so GOTOOLCHAIN=local hard-errors. Reproduced in a scratch copy with the go directive lowered. Becomes live again when a distro ships a custom build \u003e= the pin.","status":"open","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:08:45Z","created_by":"areqag","updated_at":"2026-08-17T06:07:12Z","external_ref":"https://github.com/areqag/gqlc/issues/816","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-f1yf","title":"ci: govulncheck is red on master — the tree calls 3 stdlib advisories fixed in go1.26.6","description":"`govulncheck` is a required check and it is currently RED on master itself, so\nevery open PR is blocked regardless of content.\n\n## Evidence (first-party, 2026-08-17)\n\nRe-ran master's own last vuln workflow run — run 31368862709, head f4fb1a19,\nwhich had passed on 2026-08-10 — against today's vulnerability database. Same\ntree, same workflow, same commit:\n\n    2026-08-10  run 31368862709  f4fb1a19  SUCCESS\n    2026-08-17  re-run of same   f4fb1a19  FAILURE\n\n    Your code is affected by 3 vulnerabilities from the Go standard library.\n\nNothing in the tree changed. The advisories are new.\n\n## Cause\n\nAdvisories published against the standard library since 2026-08-10, every one of\nthem `Fixed in: \u003cpkg\u003e@go1.26.6`:\n\n    GO-2026-6218  net/url        quadratic complexity in resolvePath\n    GO-2026-6089  net/http       ReadHeaderTimeout not applied on the h2c check\n    GO-2026-6088  encoding/xml   missing recursion depth guard in decode\n    GO-2026-5942  net            panic parsing an invalid SVCB/HTTPS RR\n    GO-2026-5026  net/http       ASCII-only Punycode labels not rejected (idna)\n\nCalled, not merely required — e.g.\n\n    internal/grammar/gql/gen/gql_parser.go:2620:21: gen.GQLParserInit calls\n    sync.Once.Do, which eventually calls asn1.Unmarshal\n\nso the accepted-and-uncalled register (bd gqlc-k22l) is the wrong instrument:\n`just vuln` refuses called advisories by design and should keep doing so.\n\n## Fix\n\n`go.mod` and `test/data/codegen/go.mod` both pin `go 1.26.5`, and both CI\nworkflows resolve the toolchain with `go-version-file: go.mod`. Bump both to\n`go 1.26.6`, which is released.\n\n## Not reproducible locally\n\n`just vuln` is rc=0 on this machine for BOTH master and every branch, reporting\nno stdlib advisory at all. The local toolchain identifies as\n`go1.26.5-X:nodwarf5`; govulncheck appears not to attribute stdlib findings to a\nnon-release version string. So the local gate is silently blind to the entire\nstandard-library half of the scan, which is worth a bead of its own — a green\n`just vuln` on a developer machine does not mean a green `govulncheck` in CI.\n","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:06:33Z","created_by":"areqag","updated_at":"2026-08-17T05:17:32Z","closed_at":"2026-08-17T05:06:51Z","close_reason":"Fixed independently and already merged as #810 (f1f23cb6, 'build: patch the Go toolchain the scan found vulnerable'), which bumps go.mod and test/data/codegen/go.mod from go 1.26.5 to go 1.26.6 — the same fix this bead specifies. Filed and closed within the same hour; kept for the record because the evidence here (re-running master's own 2026-08-10 vuln run at f4fb1a19 and watching it flip SUCCESS -\u003e FAILURE with the tree unchanged) is the cleanest attribution proof that the failure was advisory drift rather than any open branch.","external_ref":"https://github.com/areqag/gqlc/issues/818","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-9322","title":"build: patch the Go toolchain to 1.26.6 — three called stdlib vulnerabilities","description":"govulncheck went red on every PR from 2026-08-17: both modules pin `go 1.26.5` and CI installs exactly that via go-version-file.\n\nCalled (exit-status-bearing):\n- GO-2026-5972 encoding/asn1 recursion depth — reached from gen.GQLParserInit -\u003e sync.Once.Do -\u003e asn1.Unmarshal\n- plus two more called stdlib findings in the same scan\n\nUncalled, package-level: GO-2026-6218 (net/url resolvePath quadratic), GO-2026-6089 (net/http ReadHeaderTimeout on h2c check), GO-2026-6088 (encoding/xml decode recursion), GO-2026-5942 (x/net dnsmessage SVCB panic), GO-2026-5026 (x/net idna punycode).\n\nAll fixed in go1.26.6. Same shape as 1e26f868. Blocks every open PR because govulncheck is a required context.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:52:36Z","created_by":"areqag","updated_at":"2026-08-17T06:15:29Z","started_at":"2026-08-17T04:58:24Z","closed_at":"2026-08-17T06:15:29Z","close_reason":"Merged to master as f1f23cb6 via PR #810; verified ancestor of origin/master","external_ref":"https://github.com/areqag/gqlc/issues/805","comments":[{"id":"01a00e15-cc77-75a4-a759-10d09622c0a7","issue_id":"gqlc-9322","author":"areqag","text":"PR #810 (branch build/go-1266-toolchain, worktree /home/antranig/Developer/gqlc/gqlc-9322). Squash auto-merge armed. Verified locally on go1.26.6: just vuln exit 0, symbol results empty, residual set exactly the three register entries (GO-2026-5158, GO-2026-5841, GO-2026-5932). tidy-check, lint, test, test-codegen-fence, actionlint all green. Blocks PR #804 and every other open branch until merged.","created_at":"2026-08-17T04:58:25Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-rrtl","title":"mergeLabels unions a re-bound relationship variable's types; should intersect","description":"`listener.mergeBinding` (`internal/query/cypher/pattern.go:413`) calls\n`rb.mergeLabels(labels)` (`:444`) when a relationship variable is re-bound, and\nthat merge **unions** the label sets. So two occurrences of `r` carrying\ndifferent single relationship types produce exactly the label set that\n`-[r:A|B]-\u003e` would.\n\n    MATCH (:Person)-[r:AUTHORED]-\u003e(:Post), (:Person)-[r:LIKES]-\u003e(:Post) RETURN r\n\nemits a sealed sum over `AUTHORED` and `LIKES` on the neo4j targets today.\n\n**That is backwards.** Re-binding the same relationship variable is\n*conjunctive*: the one edge `r` denotes must satisfy every occurrence. A\nrelationship has exactly one type, so requiring both `AUTHORED` and `LIKES` is\nunsatisfiable — the query returns no rows, always. Alternation is the\n*disjunctive* construct, and it is the only one that should widen a candidate\nset. gqlc currently models the conjunctive case as if it were the disjunctive\none, so it emits a multi-candidate decoder for a query that can never produce a\nrow of either candidate.\n\nThe correct treatment is intersection: `KeyLabels` for a re-bound variable is\nthe intersection of the occurrences' label sets, and an empty intersection is a\nrefusal (the query is unsatisfiable as written) rather than a union.\n\nFound by the `gqlc-35yu.14` adversarial reviewer, who was hunting for a\ncounterexample to that bead's claim that a multi-candidate edge column is\nreachable only through `'|'`. Four shapes reach it with no `'|'` anywhere —\ncomma-joined pattern parts, `OPTIONAL MATCH` extending an already-bound `r`,\ntwo sibling `MATCH` clauses, and `MATCH` + `MERGE`. Each was driven end-to-end\nthrough `gqlc generate`; each emits the real sealed sum on `neo4j-go-v5`.\n\nPre-existing: `a912e3f` behaves identically, so this is not a regression from\n`.14` or `.9`.\n\nAlready correctly guarded, and worth preserving: the **cross-part** case\n(`MATCH ... WITH r MATCH ...`) is refused with `part binding type conflict`.\nOnly the within-part path is open. Whatever fix lands should reach the same\nverdict for both, by the same reasoning.\n\n**This blocks `gqlc-35yu.14`.** That bead wants to delete AGE's edge-union\nemission on the grounds that AGE 1.7.0's parser rejects `'|'` and therefore no\nAGE query can reach a multi-candidate edge column. The premise is true about\nAGE's parser and false about gqlc: these four shapes reach the column with no\nalternation for AGE to reject. Once labels intersect rather than union, the\nclaim becomes true and the deletion becomes sound — so this is the enabling\nfix, not merely an adjacent one.\n\nAcceptance: a re-bound relationship variable whose occurrences name different\ntypes is refused as unsatisfiable rather than emitted as an edge union; the\nfour shapes above are fixtured in the invalid corpus; the cross-part refusal\nstill fires; and a genuine `-[r:A|B]-\u003e` alternation still emits its sealed sum\nunchanged.\n","notes":"WORKTREE STATE, 2026-08-02. Agent stalled on a watchdog timeout (no progress 600s), NOT a failure and NOT a rejection of the work. Substantial uncommitted work survives in /home/antranig/Developer/gqlc/gqlc-rrtl-mergelabels on branch fix/relvar-label-intersection. Do not discard; read the diff before resuming.\n\nModified: internal/query/cypher/pattern.go (+67), internal/query/cypher/errors.go (+42), internal/query/cypher/parser_test.go, internal/resolver/resolver_test.go (+37), internal/codegen/conformance/conformance_test.go (+104).\nUntracked: internal/query/cypher/pattern_test.go, and four invalid fixtures — unsatisfiable_relationship_type_{comma_parts,merge,optional_match,sibling_match}.\n\nNothing committed. The branch is based on 1bc5a84 and is now behind origin/master (c198b3d merged gqlc-9n6t), so a raw 'git diff origin/master' misleadingly shows the 9n6t changes as deletions. Diff against 1bc5a84 to see this bead's actual work.\n\nThe agent's last recorded thought: 'Confirmed — M1 was a build break, not a behavioural kill. Let me redefine it as a compile-valid union revert and add a build guard to the harness.' So its mutation table was incomplete at the stall: M1 (presumably reverting intersection to union) did not compile, which kills a mutant for the wrong reason. Whoever resumes must redefine M1 as a compile-valid union revert and confirm it reddens a behavioural assertion.\n\nScrutinise the +104 in conformance_test.go — that is large for this bead and may be the 'build guard' the agent was mid-way through adding. Judge whether it belongs here or is scope creep.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T12:20:13Z","created_by":"areqag","updated_at":"2026-08-03T10:11:22Z","closed_at":"2026-08-03T10:11:22Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/709","dependencies":[{"issue_id":"gqlc-rrtl","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-02T08:20:12Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-ak39","title":"codegen/neo4j: a property named 'out' or 'err' emits a decoder that does not compile","description":"The neo4j backend derives each per-property local in decode\u003cName\u003e from the\nschema property name (codegen.LowerFirstRune of the field name). The decoder\nbody already holds locals named `out` and `err`, so a schema declaring a\nproperty named `out` or `err` emits Go that does not compile, and generation\nstill exits 0 — the format gate only parses.\n\nReproduced on master (bd29f01), so this predates the AGE entity-columns work\non gqlc-35yu.9. With a fixture node declaring `err :: STRING NOT NULL` and\n`out :: STRING NOT NULL`:\n\n  valid/\u003cfixture\u003e/golden/neo4j-go-v5/models.go:26:7: err repeated on left side of :=\n  valid/\u003cfixture\u003e/golden/neo4j-go-v5/models.go:41:11: no new variables on left side of :=\n\nThe AGE arm had the same defect over a wider reserved set (`raw`, `label`,\n`props`, `out`, `err`) and gqlc-35yu.9 fixed it by emitting positional\ngenerator-owned locals — value0, value1, ... — the vocabulary render_queries.go\nalready uses for projected columns. No user identifier enters the decoder's\nscope at all, so the whole class goes away rather than being special-cased.\nThe same change applies to internal/codegen/neo4j/render_models.go.\n\nNot done under gqlc-35yu.9 because it churns every neo4j golden that carries\nan entity decoder (~77 files) on a branch that owns the AGE backend. The\ncollision fixture that bead enrols, valid/entity_property_shadows_decoder_local,\nis deliberately targeted at apache-age-pgx-v5 only: enrolling it for neo4j\nwould red the compile gate on this pre-existing defect.\n\nFix: emit positional locals in the neo4j decoder, then enrol\nvalid/entity_property_shadows_decoder_local for neo4j-go-v5 and neo4j-go-v6.\nThe fixture already carries err, label, narrowed, out, props and raw. Once\nboth backends emit it, TestBackendInvariantSurface compares it across targets\nfor free.","notes":"HANDOFF 2026-08-01. Branch fix/neo4j-decoder-locals clean at 1b3ddd0. Adversarial review dispatched and DIED ON SESSION QUOTA before a verdict — NO VERDICT EXISTS, do not merge. Re-dispatch.\n\nThe dead reviewer's brute-force compile probe was preserved out of the worktree at /tmp/gqlc-handoff/ak39_reviewer_probe_test.go.txt (package neo4j_test; emits models.go per candidate property name and type-checks it inside the golden module, over the names out/err/node/rel/v/ok/s/narrowed). Worth reusing.\n\nHighest-value unreviewed attacks: (a) the claim that 78 pre-existing models.go goldens changed by the positional rename ALONE, 374 in / 374 out — 88 files and 924/401 lines actually moved, so account for the difference exactly; a golden that changed by more than the rename is a behaviour change smuggled inside churn; (b) whether the new TestNoDecoderLocalTakesAPropertyName is structurally blind to package-level identifiers the body REFERENCES rather than DECLARES — the sibling bead gqlc-35yu.10 failed review for exactly that hole (an emitted 'const \u003cBare\u003eQueryText' referenced by name, invisible to a declared-idents walker); (c) whether the value/record/records skip-via-parse-failure is genuinely 'GQL reserved word' or a silent swallow of unrelated parse errors.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T18:14:25Z","created_by":"areqag","updated_at":"2026-08-02T04:07:58Z","started_at":"2026-08-01T20:52:46Z","closed_at":"2026-08-02T04:07:58Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/663","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.14","title":"codegen/age: the emitted edge-union dispatch has no live coverage — AGE 1.7.0 has no relationship-type alternation","description":"gqlc-35yu.9 emitted the multi-candidate edge column's dispatch for AGE: the\nlabel is read off the wire value and chooses which candidate decoder reads the\nwhole of it. Eight edge_union_* fixtures are enrolled and pass the golden and\ncompile gates, so the emission is pinned as text and as something that builds.\nNothing runs it.\n\nVerified against apache/age 1.7.0 (image digest pinned in\ntest/data/codegen/live_age_test.go): the relationship-type alternation\nopenCypher spells `-[r:AUTHORED|LIKES]-\u003e` is a syntax error. Every existing\nedge_union_* fixture writes its column that way, so none of them can seed or\nrun against a live AGE server, and the live battery gained node- and\nedge-entity scenarios only.\n\nThat leaves the dispatch's live behaviour unknown on two axes the goldens\ncannot reach: whether an AGE edge's label text matches the schema's KeyLabels\nbyte for byte, and what the default arm does with a label outside the\ncandidate set.","acceptance_criteria":"- A fixture whose edge-union column is written in a form AGE 1.7.0 parses (an\n  untyped relationship pattern, or a union reached some other way), enrolled\n  for apache-age-pgx-v5.\n- The shared live battery gains an edge-union read scenario that runs green on\n  every arm, covering both a matching label and a label outside the candidate\n  set.\n- If no such query shape exists, the bead closes by recording that, and the\n  AGE backend's edge-union admission is reconsidered rather than left emitting\n  code no author can reach.","notes":"Raised P2 -\u003e P1 during gqlc-35yu.9 round 3. The bead .9 review found a live defect in exactly this dispatch — an edge-union column whose candidates share a label emitted a switch with a duplicate case, generate exit 0, non-compiling Go — and it went undetected through two review rounds precisely because the switch is exercised by goldens only and never by a running program. The gap is not theoretical; it cost a round.\nRULING (round 1 review FAIL, B1). The branch's load-bearing claim — that a multi-candidate edge column is reachable in openCypher only through relationship-type alternation '|' — is false. listener.mergeBinding (internal/query/cypher/pattern.go:413) calls rb.mergeLabels(labels) (:444) on re-binding, which UNIONS label sets across occurrences within a query part. Four shapes reach ResolvedEdgeUnion with no '|' anywhere: comma-joined pattern parts; OPTIONAL MATCH extending an already-bound r; two sibling MATCH clauses; MATCH + MERGE. The reviewer built cmd/gqlc from parent commit a912e3f via git archive and drove shape C through AGE: exit 0, emitting a complete compiling AGE package containing the full label dispatch that 9de56dd deletes. So the deleted code was NOT dead. The cc43d123 identical-golden-hash argument is circular: a8f7437 had already un-enrolled every fixture that could reach the emission.\n\nDECISION: this bead is now BLOCKED on gqlc-rrtl, which makes the premise true by intersecting rather than unioning a re-bound relationship variable's labels. Deleting working functionality — 45 goldens, 10 manifest enrolments, and production emission that demonstrably compiles and serves — on a false premise is precisely the irreversible mistake the merge protocol exists to prevent. Once rrtl lands, rebase this branch, re-verify the four shapes are refused, and the deletion becomes sound.\n\nVerified sound by the same reviewer and worth keeping through the rebase: the un-enrolment is honest (all 9 valid + 1 invalid manifests contain a literal '|'); the gate is load-bearing (loosening it gives exit 0 + 'undefined: GetActionR'); the arms table asserts both directions; TestAGERefusesRelationshipTypeAlternation and the neo4j edge_union_undeclared_relationship_type fixture are pure gains.\n\nAlso fix on the rebase: the refusal message is a lie about the author's query — it says the column 'which openCypher expresses only as a relationship-type alternation' when no alternation is present. Non-blocking findings to fold in: N1 the claimed compile-gate safety net is gone; N2 TestBackendInvariantSurface has no teeth for this column kind; N3 stale mutation numbers (actual 29/12 and 14/12, not 25/10 and 12/10); N4 premise re-measured only nightly; N5 '|' occurs in three grammar productions; N6 the shared-label fixture's AGE message is wrong in a second way.\nQUOTA KILL 2026-08-03 ~19:00 EDT — state preserved, NOT discarded. Worktree gqlc-35yu14-edgeunion, branch feat/age-edge-union-live, HEAD b3e33ce, with 270 uncommitted insertions implementing the round-3 fix: internal/query/cypher/alternation.go (76 lines, NEW) + alternation_test.go (158 lines, NEW), plus age/errors.go +76, age/age_test.go +129, cli/pipeline/pipeline_test.go +38, age/generate.go +10, and the invalid/unrepresentable_edge_union_shared_label fixture. That is the parse-level relationship-type-alternation guard the coordinator ruled for: refuse on len(labels)\u003e1 at the parsed edge binding, downstream of the parse so list-comprehension | is a non-issue, upstream of the resolver's edgeCandidates subset drop so a narrowed-to-one-declared-candidate alternation is still caught. Resume from this working tree; do not reset or restore.\nQUOTA KILL RESCUE (2026-08-04). Agent carmack-35yu14-r7 was killed mid-round by a quota limit (resets 12:30am ET).\n\nState preserved, NOT lost:\n- Worktree: /home/antranig/Developer/gqlc/gqlc-35yu14-edgeunion\n- HEAD: ba20561177527af21b2de83fda5a10e208d37554\n- internal/codegen/age/age_test.go modified, uncommitted (85171 bytes)\n- Backup: /tmp/quota-rescue/35yu14/uncommitted.patch (2750 bytes) plus the file copy.\n\nRound 7 must fix linus-35yu14-r6's blocker B1 plus four folded-in non-blocking findings:\nB1: internal/codegen/age/errors.go:244 -- unservedReason's parameter arm hardcodes\n    edgeUnion=false on a separate return statement with no witness. Flipping that\n    literal to true leaves the ENTIRE root suite green, and a probe showed the wrong\n    message wins. Needs a test row that pins which message wins for a parameter whose\n    type is unserved on a query that also has an edge union.\nN1-N4 folded in rather than filed: each is a single test row at an existing seam, so\n    filing separately would cost a worktree/branch/PR/review apiece.\nExplicitly NOT to be done: no row for strings.TrimSpace (a genuine no-op) -- do not let\n    the sweep push toward asserting something vacuous.\n\nr6 verified all round-6 fixes genuinely hold: round-5's distinct-name escape killed at\nall three seams; first-vs-last substitution killed; yield-removal killed; yield-always\nkilled; ResolvedEdge substitution killed; len(u.EdgeKeys)\u003c2 guard reachable and\nload-bearing; M19/M20 (zero-column and write queries bypassing the gate) killed;\nspace-stripping killed. He also independently confirmed the golden-trap claim:\ninternal/cli/pipeline/*_test.go has zero golden hits.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T17:25:22Z","created_by":"areqag","updated_at":"2026-08-04T21:50:42Z","closed_at":"2026-08-04T21:50:42Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/664","dependencies":[{"issue_id":"gqlc-35yu.14","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-08-01T13:25:22Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.14","depends_on_id":"gqlc-rrtl","type":"blocks","created_at":"2026-08-02T08:20:36Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-w318","title":"bd-gh-sync push leaves a permanently-open GH mirror for a bead that is closed and unmirrored","description":"`.githooks/bd-gh-sync push` creates a GitHub mirror for a bead that is already\nclosed locally, then fails to close it. The issue stays open forever.\n\nObserved 2026-07-31: `gqlc-w29n` was closed, had no `external_ref`, and the\nfirst push after `just init` re-activated the hooks minted #616 for it. #616\ncame out OPEN and nothing closed it. Closed by hand.\n\n## Mechanism: the snapshot is taken too early\n\n    _beads=$(bd list --status all --limit 0 --json ...)      # (1) snapshot\n\n    _new=$(... beads with no external_ref ...)\n    bd github push $_new                                     # (2) creates mirrors\n\n    _open_gh=$(gh issue list --state open ...)                # (3) fresh\n    printf '%s' \"$_beads\" | python3 -c '                      # (4) reads (1)\n        for b in beads:\n            if b.get(\"status\") != \"closed\": continue\n            m = pat.search(b.get(\"external_ref\") or \"\")\n            if not m: continue                                # \u003c-- skipped here\n    ' \"$_open_gh\"\n\n`_beads` is captured once, at (1), before the push. Step (4) re-reads that\nstale snapshot. Every bead mirrored in step (2) still has an empty\n`external_ref` there, so `pat.search(\"\")` returns None and the bead is\n`continue`d past — including the closed ones the phase exists to handle.\n\nNote (3) IS refreshed after the push, so the newly created issue is present in\n`_open_gh`. The defect is purely that the bead side is stale. The two halves\ndisagree about when \"now\" is.\n\n## Blast radius\n\nAny bead closed before its first successful push. That is the normal shape for\nshort-lived work: create bead, do the work, close it, and push once at the end\nof the session. It is also silent — the push exits 0 and the issue count grows.\n\nLatency in this repo was masked while `core.hooksPath` was misconfigured, so\nnothing pushed at all. Re-activating hooks makes it live.\n\n## Fix direction\n\nRe-read the bead list after `bd github push` rather than reusing the pre-push\nsnapshot. One extra `bd list` call, no new dependency. Alternatively intersect\non bead id rather than on `external_ref`, resolving the number from\n`_open_gh` by title — worse, titles are not unique.\n\nGuard: the fix needs a test that pushes a bead which is closed and unmirrored\nin the same run, and asserts the GH mirror ends CLOSED. Asserting only that\n\"a closed bead with a mirror gets closed\" passes today and misses this.","notes":"=== FIX 2026-08-01 (branch fix/bd-gh-sync-state-fidelity) ===\n\nMechanism in the description is exactly right and reproduces. Confirmed by\nreading the two halves: `_beads` was captured once before `bd github push`,\nand the close pass re-read that same snapshot, where every just-mirrored bead\nstill carries an empty external_ref, so `pat.search(\"\")` returns None and the\nclosed ones are continued past. `_open_gh` was already refreshed after the\npush, so only the bead side was stale.\n\nTook the fix direction the bead prescribes: re-read `bd list` after the push\nrather than intersecting on bead id. One extra call, no new dependency, and it\navoids resolving numbers by title, which the bead correctly notes are not\nunique.\n\nThe guard the bead asks for is in place and it is the load-bearing part. The\ntest pushes a bead that is closed AND unmirrored in the same run, stubbing bd\nand gh on PATH so the run is offline, and asserts the fresh mirror #616 ends\nCLOSED. Verified it is the fix that makes it pass: run against the pre-fix\nscript the case fails with \"mirror #616 left open\". The weaker case the bead\nwarns about -- an already-mirrored closed bead -- is kept alongside it as a\ncontrol and passes on BOTH versions, which is precisely why it could never\nhave caught this.\n\nAlso chunked `bd github push` through xargs. It previously relied on unquoted\nword-splitting of every unmirrored bead id onto one argv, which is the same\n128KB MAX_ARG_STRLEN failure class as gqlc-63y, just not yet triggered.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T01:14:12Z","created_by":"areqag","updated_at":"2026-08-02T04:29:26Z","closed_at":"2026-08-02T04:29:26Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/619","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-onji","title":"bd pull re-mints a ghost bead for any orphaned GH issue; deletion is not durable","description":"Deleting a sync-manufactured ghost bead does not hold. The `--prefer-github`\npull creates a bead for any GH issue in its update window that has no bead\ncarrying a matching `external_ref`, and bd exposes no ignore or suppression\nlist (`bd github sync` has only `--dry-run`, `--pull-only`, `--push-only`,\n`--prefer-*`).\n\n## Proven, not inferred\n\nWhile closing gqlc-w29n:\n\n1. `#597` closed as a DUPLICATE of `#598` (GraphQL `closeIssue`).\n2. `bd delete gqlc-1785373131289-1-b6686b0c --force` -\u003e gone.\n3. Next session-start `bd-gh-sync pull`, 14 minutes later -\u003e a ghost is back\n   under a NEW id, `gqlc-1785456331014-1-e662488b`. Same title, same\n   `external_ref` -\u003e #597, status closed.\n\nThe close in step 1 is what caused step 3: it bumped `#597`'s `updated_at`\ninto the pull's window. The cleanup action is itself the trigger.\n\nThis also corrects an earlier reading. A sample of 60 recent closed GH issues\nfound 12 with no bead and no ghost; that was taken as evidence that closed\nissues are exempt from bead creation. It is not -- those issues are quiescent\nand sit behind the watermark. Status is irrelevant; recency is what matters.\n\n## Why `--dry-run` cannot be used to check this\n\nIt prints only \"Dry run mode - no changes will be made\" and a trailing line,\nwith no per-issue summary, both when a create is pending and when it is not.\nIts silence is not evidence either way. Verified both directions.\n\n## Durable options\n\n- **Annotate in place.** Keep the ghost, retitle/describe it as a known\n  duplicate mirror of #598. Survives every pull, costs one permanent junk\n  bead, keeps the forensic trail.\n- **Delete GH #597 outright.** Removes the source, so nothing can be minted.\n  Destructive to shared state, 404s the URL, and loses the duplicate link\n  that currently records the relationship.\n- **Upstream.** Ask bd for an ignore list keyed on external_ref.\n\nNot deciding here -- both local options are the owner's call, and the current\nstate (ghost deleted, #597 closed as duplicate) is stable until something\ntouches #597 again.\n\nMemory: `bd-ghost-bead-resurrects-on-touch`.","notes":"=== FIX 2026-08-01 (branch fix/bd-gh-sync-state-fidelity) ===\n\nFixed, and by the same change as gqlc-63y rather than by any of the three\noptions listed under \"Durable options\". None of annotate-in-place, delete-GH,\nor upstream-ignore-list was needed, because they all accept an unscoped pull\nas a given. The pull is now scoped: eligible bead ids are passed as --issues,\nand a GH issue with no bead carrying a matching external_ref is by\nconstruction not in that list, so there is nothing for the pull to mint. The\nsuppression list the bead asks bd for turns out to be expressible as the\ncomplement of an allowlist bd already supports.\n\nRecency-not-status is confirmed as the right reading and is now irrelevant:\nscoping does not depend on the watermark at all, so a touched-and-therefore-\nrecent orphan like #597 cannot come back regardless of when it was last\nmodified.\n\nOrphans are reported instead of created -- one stderr line naming the issue\nand the `bd github pull \u003cn\u003e` command to adopt it deliberately. Only OPEN\norphans are reported; a closed orphan is the #597 ghost shape itself, and\nreporting it would mean every cleanup regenerates its own noise. Both arms are\npinned by tests. Against the live corpus this surfaces exactly two, #452 and\n#505, which previously would have been minted silently.\n\nCORRECTION, measured. The section \"Why --dry-run cannot be used to check this\"\nis stale for bd 1.0.4. It does print a per-issue summary:\n\n  $ bd github sync --pull-only --prefer-github --dry-run --issues gqlc-w29n\n    [dry-run] Would update local issue: 616 - gqlc-fex has two GH mirrors ...\n\nand an unscoped dry-run distinguishes creates from updates (\"Would create\" vs\n\"Would update\"), which is what made it possible to measure gqlc-63y blast\nradius without mutating anything. Its silence IS evidence in this version.\nWhoever revisits this should re-check the claim before trusting it.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T00:13:25Z","created_by":"areqag","updated_at":"2026-08-02T04:29:26Z","closed_at":"2026-08-02T04:29:26Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/613","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-63y","title":"bd-gh-sync divergence guard never runs — ARG_MAX overflow fails open into an unguarded --prefer-github pull","description":"`.githooks/bd-gh-sync` builds a divergence guard that is supposed to abort the\npull when a bead description has diverged from its GH issue body — because\n`bd github sync --pull-only --prefer-github` makes the GH body authoritative\nand silently reverts local bd amendments.\n\nThat guard is dead. Observed at session start on 2026-07-30:\n\n    .githooks/bd-gh-sync: line 78: /usr/bin/python3: Argument list too long\n\n## Mechanism\n\nThe guard passes its inputs through the environment:\n\n    _bd_beads=$(bd list --status all --limit 0 --json ...)\n    _gh_bodies=$(gh issue list --state all --limit 1000 --json number,body ...)\n    _diverged=$(BD_BEADS=\"$_bd_beads\" GH_BODIES=\"$_gh_bodies\" python3 - \u003c\u003c'PYEOF'\n\n`execve` counts the environment block toward `ARG_MAX`. With 232 beads carrying\nmulti-KB descriptions plus up to 1000 GH issue bodies, the combined env exceeds\nthe limit and `python3` is never executed.\n\n## Why it fails open\n\nThe script has no `set -e` and its header states the design intent: \"Never\nfails: exits 0 on any error so the git/session operation continues.\" So the\nfailed command substitution yields an empty `_diverged`, `[ -n \"$_diverged\" ]`\nis false, and control falls straight through to:\n\n    GITHUB_TOKEN=\"$_token\" bd github sync --pull-only --prefer-github || true\n\nThe unguarded pull runs. The guard produces no signal on the path where it\nmatters, which is the shape recorded in memory `pr-blocking-two-halves`: a\ndetector that exits 0 is not a gate.\n\n## Fix direction\n\nPass the payloads on stdin or via temp files rather than the environment, and\ndistinguish \"guard found no divergence\" from \"guard did not run\". The second\nhalf matters more than the first — any future breakage of the guard must fail\nloud rather than silently re-enabling the overwrite path.\n\n## Acceptance\n\n- The guard executes with the current bead/issue volume.\n- A deliberately diverged bead is detected and the pull is skipped.\n- A forced failure of the guard itself blocks the pull instead of falling\n  through to it.\n\n## Amendment 2026-07-31: the guard is also over-broad, not just dead\n\nFixing the ARG_MAX fault ALONE will wedge the repo. Moving the payload off the\nenvironment turns a permanent no-op into a gate that blocks every pull, on\nevery session start, until someone hand-reconciles the corpus.\n\nMeasured against the live corpus (234 beads, 250 GH issues) by running the\nguard's own logic with the payloads in temp files: exactly one divergence,\n`gqlc-23e` / GH#171. Inspecting it shows why \"block on any difference\" is the\nwrong predicate -- the two texts are COMPLEMENTARY, not one stale copy:\n\n- bd description = the 2026-07-18 tagpr + GoReleaser DESIGN (scope, why not\n  release-please, acceptance criteria).\n- GH#171 body    = the 2026-07-19 DEFERRED status (GH013 attempts, what\n  failed, revisit-when).\n\nNeither is a superset of the other. `--prefer-github` loses the design\nrationale; the guard's own prescribed remedy (mirror bd -\u003e GH) loses the\ndeferral record. Both directions destroy information.\n\nSo the fix has three parts, and shipping only the first is worse than\nshipping nothing:\n\n1. Move both payloads off the environment (stdin or temp files), and\n   distinguish \"python did not run\" from \"python ran and found nothing\" --\n   the current code cannot tell those apart, which is the whole defect.\n2. Replace the equality predicate. \"GH ahead of bd\" is the normal case that\n   `--prefer-github` exists to serve and must not block. The data-loss shape\n   is specifically \"bd holds content GH lacks\". A length or equality test\n   cannot separate them.\n3. Reconcile gqlc-23e / GH#171 into a merged body first, or the gate fails\n   closed on its first real run. Fix the corpus in the same change.\n\nThe remedy text printed to the developer should offer a merge, not a\none-directional mirror.\n\nSee memory `bd-gh-divergence-guard-is-overbroad` for the reproduction.","notes":"=== LIVE REPRO 2026-08-01, and it is wider than the description says ===\n\nObserved while sequencing epic gqlc-35yu. Sequence:\n\n  1. bd update gqlc-35yu.4 --claim   -\u003e IN_PROGRESS  (confirmed via bd show)\n  2. bd update gqlc-35yu.6 --claim   -\u003e IN_PROGRESS  (confirmed via bd show)\n  3. export committed with both in_progress, merged as PR #643\n  4. gh pr merge + git checkout master (post-merge/post-checkout bd hooks fire)\n  5. bd show both beads -\u003e OPEN, with started_at still set to 2026-08-01\n\nThe passive export then showed in_progress -\u003e open for both, i.e. the\nunguarded --prefer-github pull took the GH mirrors' OPEN state (issues\n#629 / #627 — GitHub has no in_progress, so every claimed bead's mirror\nreads OPEN) and clobbered the local status.\n\nWIDER THAN RECORDED: this bead's description scopes the damage to \"local\nbd amendments\" to DESCRIPTIONS. It also silently reverts STATUS. That is\nworse in one specific way — GitHub cannot represent in_progress at all,\nso the GH body is not merely stale for status, it is structurally\nincapable of holding the local value. Every claimed bead is guaranteed to\nbe reverted by any pull, not just ones that happen to have diverged. A\ndivergence guard that compares descriptions would not have caught this\neven if it ran.\n\nstarted_at survives while status does not, so the DB ends in a state no\nbd command produces: OPEN with a started_at. That inconsistency is a\ncheap detector — a post-pull assertion that (started_at != null AND\nclosed_at == null) implies in_progress would flag it without needing the\nARG_MAX-heavy body diff at all.\n\nInteracts with gqlc-jwuw, which reports the re-derivation running the\nOTHER way (started_at -\u003e in_progress, reverting a deliberate 'open'). Both\ncannot be right; whichever fix lands must pick a direction and state it,\nbecause today the two beads describe mutually contradictory behaviour and\nat least one of them is a stale observation.\n\nWorkaround in use: re-issue `bd update \u003cid\u003e --status in_progress` after\nany merge/checkout, and check `bd show` rather than trusting the export.\n=== THIRD REPRO, 2026-08-01, with a sharper mechanism than 'statuses revert' ===\nTrigger: agent claimed gqlc-3eyw (open -\u003e in_progress in the local Dolt DB)\nAFTER I had run 'bd export -o .beads/issues.jsonl' for PR #649 but BEFORE\n'git pull' brought #649's merge commit back down. Post-pull, gqlc-3eyw was\n'open' again.\n\nThe mechanism is JSONL-import-on-pull clobbering NEWER local Dolt state with\nthe committed snapshot. Verified by reading the merged commit directly:\n  git show c6381cc:.beads/issues.jsonl\n    gqlc-3eyw     -\u003e open        | started_at: None\n    gqlc-35yu.6   -\u003e in_progress | started_at: 2026-08-01T03:22:47Z\ngqlc-35yu.6 was in_progress in the committed export and SURVIVED the pull.\ngqlc-3eyw was open in it and was reverted. So the import is not preferring\nGitHub and is not merging by recency — it simply replays the file over the DB.\n\nTHE PART THAT MATTERS MOST: the clobber is PARTIAL and produces a state that\nexisted in NEITHER source. The committed record had status=open AND\nstarted_at=None. After the import the bead reads:\n  status:  open        (taken from the file)\n  Started: 2026-08-01  (kept from the DB)\nSo 'status' was overwritten from the file while 'started_at' was not cleared\nto match. This is neither record-level replace nor field-level merge — it can\nsynthesize an impossible bead: unstarted-by-status, started-by-timestamp.\nAny consumer trusting started_at to imply in_progress (or the inverse) is\nreading a state no writer ever wrote.\n\nThat impossible-state synthesis is the bug worth chasing; the visible\n'my claim disappeared' symptom is downstream of it. Whether this shares a root\ncause with the ARG_MAX divergence-guard failure this bead was filed for is\nNOT established — the guard never running would explain why nobody is warned,\nbut not why the import merges fields inconsistently. Treat as possibly two\nbugs until someone reads the import path.\n\nPractical mitigation until fixed: re-assert claims with 'bd update \u003cid\u003e\n--status in_progress' after every pull that carries a bd export commit.\n=== FIX 2026-08-01 (branch fix/bd-gh-sync-state-fidelity) ===\n\nMECHANISM CORRECTION, measured. The description blames ARG_MAX (\"the combined\nenv exceeds the limit\"). That is wrong. Measured on this box: ARG_MAX is\n4194304; the two payloads combined are 1599151 bytes, comfortably under it.\nWhat fires is MAX_ARG_STRLEN, the per-string cap execve applies to any single\nargument or environment variable, empirically bracketed here between 130000\n(ok) and 200000 (E2BIG) -\u003e 131072. BD_BEADS alone is 1089638 bytes, 8x over.\n\nThis matters for the fix: the limit is per-variable, not aggregate, so no\nulimit raises it and shrinking the corpus 8x would not have helped. It would\nhave started failing at roughly 30 beads, not 232.\n\nBLAST RADIUS, measured. `bd github sync --pull-only --prefer-github --dry-run`\nunscoped proposes exactly four updates: GH #671, #650, #623, #667 = gqlc-kph4,\ngqlc-rohp, gqlc-35yu.10, gqlc-h4ug -- precisely the beads that were claimed,\nand nothing else. Zero creates. So the status reversion is not a side effect\nof the pull, it is currently the ONLY effect. The amendment is right that a\ndescription-comparing guard would never have caught it.\n\nDESIGN FORK: gate the pull, or scope it. Rejected gating. `bd github sync`\n1.0.4 exposes no field-level control (--dry-run, --pull-only, --push-only,\n--prefer-github/local/newer, --issues, --parent), so status cannot be excluded\nfrom a pull. --prefer-local would keep the claim but discard the GH close that\nis the whole point of pulling. A gate can therefore only choose between\n\"revert claims\" and \"never pull\".\n\nChose scope. --issues is the one lever that expresses per-bead intent\n(`bd github pull [refs]` is documented as `sync --pull-only --issues \u003crefs\u003e`).\nThe guard became an allowlist: a bead is eligible only when GitHub is CAPABLE\nof holding its value and is not BEHIND it --\n  * status in {open, closed}: GitHub state is a two-value domain, so\n    in_progress/blocked/deferred have no representable counterpart;\n  * not (closed locally AND open on GH): pulling would reopen it;\n  * no description line present in bd and absent from GH (the amendment part 2\n    predicate -- \"GH ahead of bd\" stays eligible, which equality cannot say);\n  * not already identical (see churn note below).\n\nFAILS CLOSED. The selector emits a DONE sentinel as its last line; the caller\nrequires rc==0 AND that sentinel, so \"ran and chose nothing\" is distinguishable\nfrom \"did not run\". Either failure yields an empty allowlist, and an empty\nallowlist skips the bd invocation entirely rather than passing an empty\n--issues (which would widen the pull back to everything). Payloads travel in\ntemp files, never argv or env. python3 absence is now an explicit skip rather\nthan a silent empty result.\n\nAMENDMENT PART 3 IS MOOT. It required reconciling gqlc-23e/GH#171 before the\ngate could run, or the gate would wedge on its first pass. Per-bead holds mean\none divergent bead excludes only itself, so no corpus reconciliation is needed.\ngqlc-23e is in fact held on status grounds (blocked) before the body check.\n\nCHURN, found by measurement and nearly shipped as a regression. Naming a bead\nin --issues syncs it regardless of GitHubs update watermark, so the first\nscoped dry-run proposed 129 updates where the unscoped one proposed 4. The\nallowlist now also drops beads already identical to their mirror. Against the\nlive corpus the allowlist is 0: nine held, two orphan issues left alone, no\nsync invoked -- i.e. nothing legitimate is pending, and the only thing the old\npull would have done was revert the six claims.\n\nCORRECTION to the started_at detector proposed in these notes. \"(started_at\n!= null AND closed_at == null) implies in_progress\" would false-positive:\n`bd update \u003cid\u003e --status open` on a previously-claimed bead yields open with\nstarted_at retained, verified in an isolated scratch DB, and four live beads\nsit in that shape (gqlc-do1 open since 2026-07-12, gqlc-h9n.33, gqlc-h9n.1,\ngqlc-23e). It is a reachable state, not a torn one. Replaced with a\npostcondition that is actually invariant: a bead held out of the pull must\ncome back unchanged, checked by re-reading bd list after the sync and warning\nper bead with the command to re-assert it.\nHANDOFF 2026-08-01. Adversarial review of 0e05690 returned FAIL. Refix was dispatched but DIED ON SESSION QUOTA before making any change — branch is clean at 0e05690, nothing started. Blockers, all reproduced by the reviewer against a read-only snapshot of the live corpus (273 beads / 289 GH issues) with stubbed bd/gh on PATH:\n\nB1 (gqlc-jwuw undelivered): .githooks/bd-gh-sync:163-166 exits before the postcondition at :176-215. ALLOW=0 is the STEADY STATE on the live corpus (270 mirrored, 261 dropped as in-sync, 9 HOLD, ALLOW=0), so every merge takes that exit and the revert detector never runs. Measured with a jwuw-shaped revert injected: bd list invocations 1 (2 would mean the postcondition ran), 0 WARNING lines. Both jwuw tests hard-code an eligible bead beside the claimed one (tests :282-287, :294) so CLAIMED is never tested alone — the fixtures are shaped so the bug cannot appear, which is the very thing the commit header indicts. FIX: hoist the postcondition above the eligibility bail, and add the CLAIMED-alone case.\n\nB2 (eligibility rule does not implement its stated property): :119-124 is a subset test over .strip()ed line SETS, so four GH-is-behind-bd shapes are handed to --prefer-github anyway — deletion (local subset of remote), indent change (per-line strip), reordering (sets are order-blind), de-dup (sets collapse duplicates). Deletion is the ordinary way a description gets edited. MY RULING: adopt the append-only rule — ALLOW only when gh_body.startswith(desc); everything else HOLDs and warns. One test per row.\n\nPROMOTED BY ME TO BLOCKING: (3) the summary line at :215 reports intent not outcome — :172-173 ends || true, so with a failing bd the run prints 'pulled 2 bead(s)' and the SessionStart consumer (.claude/settings.json:29, 'bd-gh-sync pull 2\u003e\u00261 | tail -1') shows ONLY that line; same fail-open-in-reporting class the commit exists to end. (4) postcondition WARNINGs print at :183-211, before the summary at :215, so tail-1 always drops them — a detector nobody can see. (5) HOLD notices are structurally unconditional: 9 per git pull on the live corpus, 7 of them status-unrepresentable on beads whose GH body is BYTE-IDENTICAL, and they can never be silenced by the in-sync drop at :130 because status==gh_state is false by construction for any status GitHub cannot represent. Signal 2:9 forever is how 2\u003e/dev/null gets re-added.\n\nFiled as separate beads, NOT to be fixed here: gqlc-fc1v (no circuit breaker on the ALLOW count), gqlc-mdhr (--prefer-github write-back fidelity unverified; needs a scratch repo, never the real corpus).\n\nVERIFIED GOOD, leave alone: fail-closed held under every probe (malformed JSON either side, gh/bd returning [], empty and hostile bead ids); MAX_ARG_STRLEN is structurally closed (_batch=100 constant bounds --issues; 1249 bytes at 5000 synthetic beads vs a 131072 limit); gqlc-w318 and gqlc-onji fixed as filed; the '14 of 20' re-derivation is exact and swapping the binary changed results; post-merge letting stderr survive is right; no CRLF in the current corpus; no title drift (270/270).\nREFIX LANDED d25d947 (2026-08-01). All five blockers addressed; suite 20 -\u003e 31 tests, all 9 new assertions red against 0e05690 first.\n\nMANAGER VERIFICATION of the two structural claims (read the script myself, not the report): B1 is genuinely fixed — the eligibility bail at the old :163-166 is gone, replaced by an else-branch at :198-200 that prints 'no pull issued' WITHOUT exiting, so the postcondition at :202+ now runs on every path. The remaining 'exit 0' at :171 is the fail-closed bail for when the selection Python itself fails, which is correct and must stay. B2 is implemented at :142-143 as 'if not gh_body.startswith(desc): HOLD \u003cid\u003e \u003cn\u003e bd-description-not-a-prefix-of-gh-body'. Commit scope clean: 2 files, zero .beads/ churn.\n\nDISAGREEMENT RULED IN THE IMPLEMENTER'S FAVOUR. I ruled for append-only; they implemented it verbatim and then pushed back that row 1 of my table (bd 'line one' / GH 'line one\\nline two DELETED IN BD') is still admitted, because startswith is true and the shape is byte-identical to the legitimate GH-append case the b-ahead fixture requires to pull. They are right: no function of the two bodies separates an addition on one side from a deletion on the other. They pinned the residual with a passing test and a comment naming the collision rather than assert a HOLD they would have to weaken later. Split out as gqlc-6mx3 (add a timestamp discriminator — updated_at is already present in bd list --json).\n\nMeasured effect on the live corpus: HOLD notices 9 -\u003e 2, both actionable (gqlc-3eyw, gqlc-23e); the gh_state == 'open' term is what preserves gqlc-23e, which is blocked/GH-closed. ALLOW remains 0 under both the old and new rule, and no bead is newly held, so the narrowing costs nothing today. Cost noted: the hoist adds a second 'bd list' per pull/session start (~0.84s).\n\nSTILL NEEDS A FRESH ADVERSARIAL REVIEW before merge.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-30T23:33:10Z","created_by":"areqag","updated_at":"2026-08-02T04:29:26Z","closed_at":"2026-08-02T04:29:26Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/614","dependency_count":0,"dependent_count":6,"comment_count":0}
{"_type":"issue","id":"gqlc-fex","title":"codegen: unused dbtype/time import in querier.go when multi-param method has a temporal param","description":"## Summary\n\n`renderQuerier` in `internal/codegen/render_querier.go` emits an unused import in `querier.go` whenever a query has 2+ parameters and at least one of them is a temporal type (`dbtype.X` or `time.Time`).\n\n## Root cause\n\n`querierImports` scans every entry in `p.ParamFields` for `dbtype.`/`time.Time`:\n\n```go\nfor _, param := range p.ParamFields {\n    scan(param.GoType)\n}\n```\n\nBut `writeMethodSignature` only surfaces the raw Go type in the signature when there is **exactly one parameter**. With 2+ parameters it emits `arg MethodNameParams` — a struct name with no import requirement. So the import is declared needed but never referenced in the interface body.\n\n## Observed failure\n\nUser-generated `querier.go` contained:\n\n```go\nimport (\n    \"context\"\n    \"github.com/neo4j/neo4j-go-driver/v6/neo4j/dbtype\"\n)\n```\n\nwhere `dbtype` appeared in no method signature — Go rejected the file as invalid.\n\n## Fix\n\nIn `querierImports`, mirror the same guard that exists for `RowFields` (single-column only):\n\n```go\nif len(p.ParamFields) == 1 {\n    scan(p.ParamFields[0].GoType)\n}\n```\n\n## TDD plan\n\n1. Add `TestGoldenBuild` to `internal/codegen/codegen_test.go` — runs `go build ./...` on the nested `test/data/codegen` module; makes compile errors visible under `just test` (not just `test-codegen-fence`).\n2. Add a golden fixture `write_exec_two_date_params` — a query with two `DATE`-typed parameters triggering a `Params` struct (and, before the fix, a spurious `dbtype` import).\n3. Confirm RED: `TestGoldenBuild` fails because the generated `querier.go` has an unused import.\n4. Fix `querierImports`.\n5. Regenerate golden with `-update`.\n6. Confirm GREEN.\n\n## Acceptance criteria\n\n- `go test ./internal/codegen/...` catches any future golden-package compile error (via `TestGoldenBuild`).\n- `write_exec_two_date_params` golden `querier.go` imports only `\"context\"` (no `dbtype`, no `time`).\n- All existing golden tests pass.\n- `just test` and `just test-codegen-fence` both green.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-30T00:45:25Z","created_by":"areqag","updated_at":"2026-07-30T00:59:35Z","started_at":"2026-07-30T00:45:30Z","closed_at":"2026-07-30T00:59:35Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/598","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-cb0","title":"schema/gql: SIGNED/UNSIGNED INTEGER(w) silently drops its declared width and resolves to the machine word","description":"`SIGNED INTEGER(8)` resolves to graph.TypeInt — a machine word — instead of\nTypeInt8. Same for every width, in both signednesses:\n\n    SIGNED INTEGER(8|16|32|64|128|256)    -\u003e TypeInt    (want TypeInt8 … TypeInt256)\n    UNSIGNED INTEGER(8|16|32|64|128|256)  -\u003e TypeUint   (want TypeUint8 … TypeUint256)\n\nTwelve spellings. Not a rejection — the schema parses, and codegen emits a 64-bit\nfield where the author declared eight bits. Silent wrong output for valid ISO GQL.\n\nEvery neighbouring spelling is correct, which is what kept it hidden:\n\n    INT(8)             -\u003e TypeInt8    ok    explicit typeSpellings row\n    INTEGER(8)         -\u003e TypeInt8    ok    explicit row\n    UINT(8)            -\u003e TypeUint8   ok    explicit row\n    SIGNED INTEGER8    -\u003e TypeInt8    ok    alias row\n    UNSIGNED INTEGER8  -\u003e TypeUint8   ok    alias row\n    SIGNED INTEGER(8)  -\u003e TypeInt     BUG   no row; falls back to \"SIGNED INTEGER\"\n\nGRAMMAR. verboseBinaryExactNumericType admits `INTEGER (LEFT_PAREN precision\nRIGHT_PAREN)?` (GQL.g4:1827), and both signedBinaryExactNumericType\n(`SIGNED? verboseBinaryExactNumericType`, :1803) and unsignedBinaryExactNumericType\n(`UNSIGNED verboseBinaryExactNumericType`, :1816) wrap it. So the prefix and the\nparenthetical compose, and all twelve spellings are well-formed. Confirmed by\nprobe: they parse, and land on the wrong constant.\n\n`SIGNED INT(8)` is NOT grammatical — `SIGNED?` guards only the verbose rule, and\n`INT (…)` is a sibling alternative — so the gap is exactly these twelve and no\nmore.\n\nWHY THE GAP. typeSpellings' own doc states both rules. The parenthesised widths\n\"appear as explicit rows because their bit-width identifies a model constant\",\nand the SIGNED/UNSIGNED prefixes are \"one alias row per bare verbose spelling\".\nEach rule was applied across its whole range. Their COMPOSITION was not, and\nnothing in the file or its tests ranges over the product.\n\nThe same hole is in the test tables, which is why the suite was green:\n\n    widthSpellingEquivalences        every row has a suffix width, none a prefix\n    verboseSignednessEquivalences    every row has a prefix, none a parenthetical\n\nTwo properties, each pinned across all six widths, whose conjunction was pinned\nnowhere. The completeness gate could not see it either: TestTypeSpellingsEveryRowPinned\nchecks that every EXISTING row is grounded, and these rows did not exist.\n\nFIX. Twelve typeSpellings rows plus twelve verboseSignednessEquivalences pairs,\nwhich is the shape the file already argues for (\"alias rows over a pre-lookup\nnormalisation ... alternative-additions to the grammar are grep-visible here\").\nNo ADR: ADR 0017 already decided that a width WITH an exact model counterpart\ngets a row and only widths without one take the lossy fallback. INT(7) and\nSIGNED INTEGER(7) still land on TypeInt, unchanged and correct.\n\nVERIFIED 1:1 IN BOTH DIRECTIONS.\n  - Pairs added without the rows: exactly 12 subtests fail, one per spelling.\n  - Each row deleted in turn: exactly 1 subtest fails, and it names that spelling.\n  - Rows kept, pairs removed: TestTypeSpellingsEveryRowPinned reports exactly the\n    12 as ungrounded, so the pins are what grounds them and not something else.\n\nFOUND BY the 36-case mutation sweep of propertytype.go (2026-07-28), 33 caught.\nThe bug was not a survivor — it surfaced from probing the three survivors, which\nneeded a matrix of real spellings to decide whether they were reachable.\n\nThe three survivors are all inert, and are recorded in bd memory rather than\nfiled. Instrumenting each branch and running the whole suite gave zero hits:\n\n  :289  `end \u003c 0` (unmatched paren) — parens in the grammar are balanced and a\n        parse error aborts before property() runs, so a `(` with no `)` cannot\n        reach here. Without the guard it panics on a slice bound.\n  :297  the `s[end+1:]` tail — reachable in principle (`LIST\u003cINT(8)\u003e` has a `\u003e`\n        after the paren) but not observable: truncateParenthetical keeps only\n        what precedes `(`, and no typeSpellings row has text after a\n        parenthetical, so both spellings miss and land identically.\n  :302  truncateParenthetical's TrimRight — redundant given collapseParenthetical\n        already trimmed the space before the same `(`. It is reachable only via\n        the `end \u003c 0` early return above, so it is dead exactly while that is.","status":"closed","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T16:24:09Z","created_by":"areqag","updated_at":"2026-07-28T16:30:10Z","started_at":"2026-07-28T16:24:16Z","closed_at":"2026-07-28T16:30:10Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/583","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4np","title":"schema/gql: a repeated property name silently keeps the last declaration and discards the first","description":"`{ id :: INT NOT NULL, id :: STRING }` parses, resolves, and produces a node type with ONE property: `{Name:id Type:STRING Nullable:true}`. The first declaration is gone -- its type and its NOT NULL with it -- and nothing anywhere says so.\n\nMeasured at 61a6cd1 with a probe against New().Parse:\n\n    CREATE PROPERTY GRAPH TYPE T AS {\n        (:Person { id :: INT NOT NULL, id :: STRING })\n    }\n    err=\u003cnil\u003e\n    node Person props=1\n      id -\u003e {Name:id Type:STRING Nullable:true}\n\nThe mechanism is one line: listener.properties builds `out := make(map[string]schema.Property)` and writes `out[p.Name] = p` for every element of propertyTypeList, so a repeat overwrites in walk order (listener.go:498-516).\n\nThis is the class-2 shape -- a schema that says one thing resolving to a model that says another, with no diagnostic -- and it is the one duplicate gqlc does NOT reject. resolve() rejects a duplicate node type (ErrDuplicateNodeType) and a duplicate edge type (ErrDuplicateEdgeType) on exactly the reasoning that a map keyed by identity cannot hold two things with one key. A property map is keyed by name and has the same problem.\n\nDownstream this is not cosmetic: codegen emits a struct field per property, so the generated Go silently carries the last spelling. An author who edits a property type and leaves the old line behind gets no error from the tool whose job is to catch that.\n\nDECIDING IT NEEDS AN ADR, WHICH IS WHY THIS BEAD DOES NOT SHIP A FIX.\n\nThe ISO BNF admits the syntax: \u003cproperty type list\u003e ::= \u003cproperty type\u003e [{ \u003ccomma\u003e \u003cproperty type\u003e }...] with no uniqueness constraint stated in the grammar. Whether a repeat is an error is Syntax Rules prose, which lives in the paid PDF gqlc-lir declined to buy. So this is the same bind as ErrEdgeKindArcMismatch and ErrImpliedLabelIsKeyLabel: two defensible readings, and choosing one silently is picking a dialect. The options are\n\n  (a) reject with a new sentinel, consistent with ErrDuplicateNodeType and ErrDuplicateEdgeType and with the no-dialect principle -- rejecting rather than silently choosing is the move ADR 0015 and ADR 0016 both make; or\n  (b) define last-wins (or first-wins) precedence explicitly and pin it with a corpus semantic case, which is the honest form of the status quo.\n\nNote (b) is not free either: the status quo is last-wins by accident of map assignment, not by decision, and nothing pins it, so it can flip to first-wins under a refactor with no test going red.\n\nREGARDLESS OF WHICH IS CHOSEN, the current behaviour is unrepresented in the tree today: no corpus entry, no semantic case, no invalid fixture. That is the part this bead measures.\n\nFound by mutation sweep of internal/schema/gql while auditing the harness for fail-open defects (see gqlc-q5z, gqlc-fon, gqlc-d9b).","notes":"This loop (h9n-loop, 2026-07-28) cannot proceed with this bead. The description says the choice between (a) reject with a new sentinel or (b) define last-wins precedence needs an ADR before any change. The delegated ADR set for this loop is only h9n.6, h9n.9, h9n.12, h9n.25 — gqlc-4np is not in it. Additionally, even the 'measurement' deliverable (adding a corpus entry or semantic case) requires choosing an outcome, which is the same decision. Releasing the claim so an owner decision can unblock it.","status":"open","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:54:57Z","created_by":"areqag","updated_at":"2026-08-16T23:31:00Z","started_at":"2026-07-28T09:55:09Z","external_ref":"https://github.com/areqag/gqlc/issues/503","metadata":null,"dependencies":[{"issue_id":"gqlc-4np","depends_on_id":"gqlc-fon","type":"discovered-from","created_at":"2026-07-28T05:55:09Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019fa8a6-521c-722f-8ab3-4949b1c707fd","issue_id":"gqlc-4np","author":"areqag","text":"Partially addressed by PR #540 (bead gqlc-v1t, now closed): the corpus now carries 18.2-node-type/property_name_repeated.gql as an area-B semanticCase, so the last-wins behaviour is pinned and can no longer flip to first-wins silently. Verified by making listener.properties skip a name already present — before #540 that flip left the whole suite green; after it, TestSemanticCaseCollisions goes red and nothing else does.\n\nTHIS BEAD STAYS OPEN. A semanticCase asserts the model is WRONG, not that the behaviour is wanted, so it deliberately does not prejudge the choice between (a) a new sentinel and (b) an explicitly decided precedence. That is still an ADR outside the autonomously-delegated set (h9n.6/.9/.12/.25), and it needs the user's call.\n\nWhen the ADR lands, the semanticCase row becomes either an unsupported entry with the new sentinel, or a plain corpus entry recording the decided precedence.","created_at":"2026-07-28T12:15:00Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-fon","title":"schema/gql: two invariants resolve.go states in prose have no witness in the corpus","description":"resolve.go argues for two design choices in comments, and neither claim is pinned by anything. A mutation sweep of the resolver (13 semantically meaningful mutations, run against `go test ./...` at 61a6cd1) caught 11 and left these two SURVIVED:\n\n1. ORDER-INDEPENDENCE OF THE INHERITANCE CHECK. resolve.go:80-82 says the rejectInheritance call is \"Deferred past the loop because a collision is order-independent: a later declaration's key label can collide with an earlier one's implied label.\" Moving the check inside the node loop -- so each declaration is only checked against the key labels seen so far -- passes the whole suite.\n\n   The corpus has one file for this rule, 18.2-node-type/implied_key_label.gql, and it is written in the order that does not discriminate: `(:Person ...)` is declared FIRST and `(:Engineer =\u003e :Person ...)` second, so the key label is already in the set by the time the implying declaration is read. The deferral is load-bearing only in the other order, and no file uses it.\n\n2. NODE AND EDGE LABELS ARE SEPARATE NAMESPACES. resolve.go:166-167 says \"Node and edge labels are checked against their own kind only -- they are separate namespaces, keying separate maps, and an edge type labelled KNOWS says nothing about a node type's Person.\" Merging nodeKeyLabels into edgeKeyLabels before the edge-side rejectInheritance call passes the whole suite.\n\n   Every GG21 file today keeps the two vocabularies disjoint by accident rather than by design: corpus 18.3-edge-type/key_label_set.gql implies WROTE over nodes keyed Person and Post; valid/key_label_set.gql implies Author over nodes keyed Engineer, Manager\u0026Staff and Article. No file has an edge imply a label some node type holds as a key label, which is the only shape that can tell one namespace from two.\n\nBoth are the same failure as gqlc-q5z and gqlc-d9b: a claim stated in prose, believed on review, and unrepresented in the tree. ADR 0015 is the decision behind both -- the inheritance decline is the one part of GG21 gqlc refuses, and how far the refusal reaches is exactly what these two claims say.\n\nFIX: two corpus files, one per claim.\n\n  18.2-node-type: `(:Engineer =\u003e :Person { level :: INT }), (:Person { name :: STRING })` -- the implying declaration first. Rejects with ErrImpliedLabelIsKeyLabel under the deferral and resolves without it.\n\n  18.3-edge-type: an edge whose implied label is spelled the same as a declared node type's key label. Resolves under separate namespaces and is rejected under one. The spelling looks odd on purpose: reusing a name across the two vocabularies IS the invariant.","status":"closed","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:47:02Z","created_by":"areqag","updated_at":"2026-07-28T09:54:18Z","started_at":"2026-07-28T09:47:06Z","closed_at":"2026-07-28T09:54:18Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/500","dependencies":[{"issue_id":"gqlc-fon","depends_on_id":"gqlc-q5z","type":"discovered-from","created_at":"2026-07-28T05:47:06Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-q5z","title":"schema/gql: both carrier gates go green if the file-to-area partition collapses","description":"TestCorpusAreaParseCarriers and tier 2 of TestCorpusResolvingCarriers exist to answer one question -- does the area that OWNS a grammar name carry a file for it, rather than some unrelated area reaching it in passing. Both take that ownership from a file-to-area partition, and neither partition has a positive control. Break either one so that every area holds every file and the whole suite stays green.\n\nMeasured against origin/master at 9ddf10a, two independent mutations, both SURVIVED:\n\n  corpusFilesByArea (corpus_carriers_test.go:492) rewritten to append every corpus file to every area -- go test ./internal/schema/gql/ ok\n  resolvingFilesByArea (corpus_resolving_test.go:88) rewritten the same way -- ok\n\nThe mechanism is that both gates are silent registers: they assert an empty orphan list, and today that list is empty. Over-generous carriage can only remove orphans, so a partition that credits every area with every file reads exactly like a corpus with no gaps. TestUnmetGroups pins the orphan LOGIC with synthetic input, and nothing pins the input it is given.\n\nThis is the failure the two files were written to prevent, undetectable in the instrument that prevents it. The parse gate's own header cites PR #392 (localdatetimeType#2 carried only by five 18.3-edge-type files spelling TIMESTAMP incidentally) and says \"no test stopped it from being reverted\"; the resolving gate's header cites 7217b180 (no resolving path exercised an explicit node synonym). Both of those defects are area-attribution defects, and both gates would report clean under either mutation above.\n\nA synthetic witness is not needed and would be weaker than what the tree already supports. The two partitions are independently derived: corpusFilesByArea reads directory prefixes off disk, resolvingFilesByArea reads corpusAreas[name].entries out of the manifest, and requireOwnedByArea (corpus_test.go:288) already pins each manifest entry's file to its declaring area's prefixes. So restricted to resolving entries the two must agree exactly, and a probe confirms they do over all 108 files today. Cross-checking them is a real invariant rather than a mock, and it kills both mutations: each rewrite makes one derivation disagree with the other.\n\nWorth adding alongside it that the prefix partition is a function -- each file in exactly one area -- which TestCorpusAreasAreDisjoint asserts of the prefixes but nothing asserts of the mapping they produce.","status":"closed","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:34:44Z","created_by":"areqag","updated_at":"2026-07-28T09:43:34Z","started_at":"2026-07-28T09:37:21Z","closed_at":"2026-07-28T09:43:34Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/497","dependencies":[{"issue_id":"gqlc-q5z","depends_on_id":"gqlc-dk2","type":"discovered-from","created_at":"2026-07-28T05:37:21Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-dk2","title":"schema/gql: optionality coverage credits a branch inside an optional the file never entered","description":"forceOptional (corpus_branch_test.go:108) pins the nth optional node to one branch and leaves every other optional alone -- including the ones enclosing it. So for an optionality point nested inside another optional, both withElement and withoutElement still admit parses in which the enclosing group is absent, and any corpus shape that elides the whole group matches both. The point is credited on both branches by a file that never reached it.\n\nThis is the mistake the withElement/withoutElement design was written to avoid, one level up. The doc comment already argues that \"x? x puts the same symbol on both branches, and a substring test would call it exercised\" -- the same is true of a point under an elided parent, and matching a whole child sequence does not catch it because the sequence really does match.\n\n8 of the 137 points are nested (depth 1, all of them), spread over 6 classes: 18.2::TYPE, 18.3::TYPE, 18.3::edgeKind, 18.9::COMMA scale, 18.9::minLength COMMA, 18.9::notNull.\n\nFive of the six are covered legitimately by a sibling non-nested point in the same class, so the mis-credit changes nothing there. One is live today:\n\n  18.3 :: edgeKind -- shallow{present,absent} vs correct{present only}\n\nedgeTypePattern is `(edgeKind? edgeSynonym TYPE? edgeTypeName)? (edgeTypePatternDirected | edgeTypePatternUndirected)` and the corpus has exactly three shapes for it:\n\n  edgeKind edgeSynonym edgeTypeName edgeTypePatternDirected\n  edgeTypePatternDirected\n  edgeTypePatternUndirected\n\nThe second and third elide the whole naming group, and are what currently satisfy \"a file elides edgeKind\". Nothing writes the pattern form that names the edge type without saying DIRECTED. That is not a cosmetic gap: an absent edgeKind is the input to the kind-vs-arc reconciliation that ErrEdgeKindArcMismatch and ErrUndirectedEdge guard, and gqlc-h9n.26 already found a real defect in the neighbouring nodeTypeFiller position under this clause.\n\nMeasured against origin/master at 941b746, with a probe that reimplements forceOptional forcing the optional ancestors on the path to the target present. Under that forcing exactly one class flips, and it flips to one-sided.\n\nFix has two halves. Force the enclosing optionals present so a point is only credited on a parse that reached it. Then discharge 18.3::edgeKind -- preferably with a corpus file spelling the pattern form with a named edge type and no kind, since a file is writable here and owedOptionality is for classes that are owed one, not for ones nobody got round to.","status":"closed","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:24:54Z","created_by":"areqag","updated_at":"2026-07-28T09:37:20Z","started_at":"2026-07-28T09:25:09Z","closed_at":"2026-07-28T09:37:20Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/494","dependencies":[{"issue_id":"gqlc-dk2","depends_on_id":"gqlc-d9b","type":"discovered-from","created_at":"2026-07-28T05:25:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-d9b","title":"schema/gql: allSentinels is hand-maintained and one sentinel already escaped it","description":"TestSentinelReachability calls itself the bidirectional sweep over Parse's sentinels, and allSentinels calls itself the single source of truth. Neither is checked against the errors the package actually declares, and the drift has already happened: ErrEndpointFillerHasProperties (errors.go:17) is a leaf sentinel returned by fillerLabels (edgetype.go:108) and is absent from allSentinels.\n\nSo the sweep never demanded a file for it. Adding it to allSentinels and re-running proves that: 'sentinel ... has no negative fixture or unsupported corpus entry'. The behaviour is covered by TestEndpointFillerRejectsProperties, but that is a direct unit test the sweep does not count, so the registry's own promise -- a declared sentinel with no file is an orphan -- is false for this one. Deleting that unit test today would be silent.\n\nIt is the only one missing: 21 leaves are declared, allSentinels lists 20, and ErrUnsupportedSource is the documented class-not-leaf omission.\n\nFix has two halves. Pin the escaped sentinel with a file. Then close the mechanism, so that a sentinel added to errors.go and forgotten here cannot pass again -- the doc comment already states the rule ('A new sentinel must be added here'), and a rule with no mechanism is what just failed.","status":"closed","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:11:27Z","created_by":"areqag","updated_at":"2026-07-28T09:24:28Z","started_at":"2026-07-28T09:11:34Z","closed_at":"2026-07-28T09:24:28Z","close_reason":"Shipped in PR #493 (merged): TestSentinelRegistryIsComplete parses errors.go and requires every declared Err* to be pinned by allSentinels or excused in sentinelsWithoutAFile; ErrEndpointFillerHasProperties pinned by test/data/schema/gql/invalid/endpoint_filler_properties.gql. 9/9 mutations caught.","external_ref":"https://github.com/areqag/gqlc/issues/491","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-so6","title":"tooling: bd-gh-sync pull silently reverts bead description amendments","description":"`.claude/settings.json` (SessionStart, including on compaction) and\n`.githooks/post-merge` (every `git pull`) both run `.githooks/bd-gh-sync pull`,\nwhich is:\n\n```\nbd github sync --pull-only --prefer-github\n```\n\n`--prefer-github` makes the **GitHub issue body authoritative for the bead\ndescription**. A `bd update --body-file` that is not also mirrored to the GH\nissue is overwritten the next time either trigger fires — in practice the very\nnext session. No warning, no diff, no record in `bd comments`.\n\n## Cost so far\n\nFour occurrences on 2026-07-26 during the gqlc-h9n.4 close-out, two\nmisdiagnoses (first a teammate accused of fabricating an amendment, then the\n`.beads/hooks/post-checkout` JSONL import blamed), and one regression shipped to\nmaster: PR #442 (`26d0879`) put `gqlc-h9n.21` on master at 3923 chars instead of\n7659. Restored by PR #443.\n\n## Why it is hard to see\n\n1. The revert is **selective**. Status, labels and dependency edges survive,\n   because none of them has a GH-body representation. A partial revert reads as\n   a partial edit, i.e. as an agent lying, not as a store event.\n2. The passive export is written **asynchronously**, so a revert followed by a\n   re-export silently rewrites `.beads/issues.jsonl` with the older text. A `cp`\n   of that file into a worktree then captures the reverted revision.\n3. A **diffstat cannot catch it**: the amended and the reverted record are both\n   one JSONL line, so `2 insertions, 2 deletions` is identical either way.\n\n## Current manual workaround\n\nEdit GitHub first, then bd, then prove it:\n\n```bash\ngh issue edit \u003cN\u003e --body-file /tmp/new.md   # N from `bd show \u003cid\u003e` -\u003e External\nbd update \u003cid\u003e --body-file /tmp/new.md\n.githooks/bd-gh-sync pull                   # description must survive\nBD_GIT_HOOK=1 bd hooks run pre-commit       # deterministic export flush\n```\n\nThat is a workaround, not a fix — it depends on every agent remembering it, and\nthe failure is silent when they do not.\n\n## Candidate fixes\n\n- **Detect and refuse.** Before the pull overwrites a description, compare it\n  with the local one; on divergence, print the bead id and both lengths and skip\n  that field rather than clobbering it. Preserves `--prefer-github` for the\n  create/close/comment mirroring that it is actually there for.\n- **Push descriptions the other way.** If bd is to be authoritative for prose,\n  the pull should not touch `description` at all, and the push side should\n  mirror bd -\u003e GH.\n- **Guard at commit time.** A pre-commit check that every bead whose description\n  changed in the staged export matches its GH issue body; fail with the `gh\n  issue edit` command to run.\n\nThe first is the smallest change and closes the silent-loss path; the third\ncatches the case where someone edits bd without the sync running at all.\n\n## Acceptance\n\nA test or scripted repro that: amends a bead description via bd only, runs\n`.githooks/bd-gh-sync pull`, and asserts the amendment survives (or that the\nsync failed loudly naming the bead). Today that repro reverts silently.\n","status":"closed","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-27T00:35:20Z","created_by":"areqag","updated_at":"2026-07-28T03:47:13Z","started_at":"2026-07-28T03:31:40Z","closed_at":"2026-07-28T03:47:13Z","close_reason":"Shipped in PR #454. Pre-flight divergence check + bulk gh issue list + 11-case test suite. Linus 2-round review.","external_ref":"https://github.com/areqag/gqlc/issues/444","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lir","title":"decision (RESOLVED: no buy): ISO/IEC 39075 CHF 227 — semantic beads held indefinitely","description":"**This is a spending decision for the repo owner, not an engineering task. No\nagent should resolve it autonomously.**\n\n## The situation\n\n\"Consult the standard\" is two acts, and only one of them is free.\n\n- **Syntactic half — free and normative.** ISO publishes the COMPLETE BNF (814\n  productions) and the full Annex D feature list (228 codes) at\n  https://standards.iso.org/iso-iec/39075/ed-1/en/ under the ISO Customer\n  Licence. Does this production exist, what does it produce, is this feature\n  code real, what is it called — all answerable today, at no cost, citably.\n  This has already unblocked real work (gqlc-cfj, gqlc-h9n.3, gqlc-h9n.30).\n- **Semantic half — CHF 227.** What the standard says a production *means*.\n  The General Rules and Syntax Rules exist only in the paid PDF\n  (https://www.iso.org/standard/76120.html).\n\n## What is actually blocked on the paid half\n\n- **gqlc-h9n.28** — does `FLOAT(p)` precision count bits or decimal digits?\n  The BNF *proves the ambiguity is real and proves it is unresolvable\n  syntactically*: `FLOAT [ (precision [, scale]) ]` (bnf.txt:1423) sits as a\n  sibling of `FLOAT16..FLOAT256` while carrying the *decimal* type's\n  scale-bearing shape. Nothing in the grammar decides it.\n- **gqlc-h9n.29** — retire or confirm `ErrEndpointFillerHasProperties` against\n  ISO 18.3. Same shape: `\u003csource node type reference\u003e` (bnf.txt:1250) does not\n  distinguish declaration from reference in the grammar.\n- **Parts of gqlc-0ri** — a deviation table cannot honestly record \"we deviate\n  from X\" without knowing what X requires.\n\n## Why these must NOT be resolved by inference\n\nA deviation record is honest when *we decided something*. It is not honest when\nthe standard is the only possible arbiter and we skipped it — that launders a\ncoin-flip into documentation. A web-search paraphrase of what SQL's `FLOAT(p)`\nmeans is inference dressed as evidence and must not be promoted to a citation.\nVendor conformance tables (Neo4j, Ultipa) match iso.org byte-for-byte on the\nfree artefacts, which makes them useful corroboration but never the citation.\n\nSo the honest states are: **buy it**, or **hold the beads at \"blocked on ISO\npurchase\"**. Not \"decide by inference and write it down confidently\".\n\n## Cost/benefit\n\n- **Cost:** CHF 227, one-off. Single-user licence.\n- **Buys:** unblocks h9n.28, h9n.29 and the semantic half of 0ri; and every\n  future question of this shape, of which there will be more — the epic is\n  explicitly aiming at full graph-schema DDL conformance, and each remaining\n  type-system and cardinality question has the same free-syntax/paid-semantics\n  split.\n- **Cost of not buying:** those beads stay open indefinitely. It does NOT block\n  the epic's bulk — coverage, corpus, grammar and rejection work all proceed on\n  the free BNF. Two named beads and one section of a third are the whole\n  exposure today.\n\n## Also unresolved, and cheaper to settle\n\nWhether the ISO Customer Licence permits *vendoring* the free files into a\npublic repo. The grant is \"use in their original format without any\nmodifications for the purposes specified in their respective ISO standard(s)\",\nwhich is plausibly a use grant rather than a redistribution grant. Current\nworking ruling (team-lead, applied on gqlc-cfj): vendor **bare identifier\ncodes only, never ISO's descriptions** — bare alphanumeric identifiers are\nfacts rather than expression. That shipped and is defensible, but it is a\njudgement call, not an answer. See gqlc-4jm.\n\n## Recommendation\n\nBuy it. The epic's stated goal is conformance with a specific published\nstandard; owning that standard is a normal cost of that goal, and the\nalternative is a permanent floor of un-answerable questions that will keep\nrecurring. But it is the owner's money and the owner's call — this bead exists\nto make the decision explicit rather than let it be made by default.\n## Resolved 2026-07-27: NOT buying. Closed.\n\nRepo owner's call, unambiguous: the CHF 227 is not being spent, now or later.\nThis is the \"hold\" branch of the decision, taken deliberately rather than by\ndefault — which is what this bead existed to force.\n\n**Consequences, stated plainly so nobody re-litigates them by inference:**\n\n- `gqlc-h9n.28` (does `FLOAT(p)` count bits or decimal digits?) and\n  `gqlc-h9n.29` (retire or confirm `ErrEndpointFillerHasProperties` against ISO\n  18.3) are now **permanently blocked on an arbiter we will not have**. They\n  stay open as honest unknowns. They are not \"hard\"; they are unanswerable from\n  the free artefacts, and the BNF positively proves that — see the citations\n  above.\n- The semantic half of `gqlc-0ri`'s deviation table cannot be written. A\n  deviation record needs to know what is being deviated from.\n\n**The rule that survives this close, and matters more than the bead did:** a\nweb-search paraphrase, a vendor conformance table, or an analogy to SQL is\n**not** a citation and must never be promoted to one. Closing this bead removes\nthe option to buy the answer; it does not license inventing it. Anything\nresolved by inference gets recorded as a gqlc decision with gqlc's name on it,\nnever as \"ISO says\".\n\nThe free half is unaffected and remains the workhorse: the complete BNF (814\nproductions) and Annex D (228 codes) at\nhttps://standards.iso.org/iso-iec/39075/ed-1/en/ are normative, citable, and\nalready carry the epic's coverage, corpus, grammar and rejection work. Nothing\nabout the epic's bulk depended on the purchase.\n\nThe separate, cheaper licence question — whether the ISO Customer Licence\npermits vendoring the free files — is untouched by this and stays with\n`gqlc-4jm`. The working ruling (bare identifier codes only, never ISO's\ndescriptive prose) still stands.\n","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:58:47Z","created_by":"areqag","updated_at":"2026-07-27T13:05:38Z","closed_at":"2026-07-27T13:05:27Z","close_reason":"Repo owner decided against the CHF 227 purchase. h9n.28, h9n.29 and the semantic half of 0ri stay open as permanently-blocked honest unknowns; they must not be resolved by inference.","external_ref":"https://github.com/areqag/gqlc/issues/436","dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-r41","title":"githooks: reject commits authored by a test-fixture identity","description":"On 2026-07-26 a real feature commit landed on a real branch and was pushed to\norigin authored as a test fixture:\n\n    875fa4e schema/gql: reject edge kind that contradicts arc direction (gqlc-h9n.3)\n    author=test \u003ctest@example.invalid\u003e\n    committer=test \u003ctest@example.invalid\u003e\n\nNothing caught it. The commit succeeded, the hooks ran and passed, the test\nsuite was green, and it pushed cleanly. It was found only because a reviewer\nread `git log --format=%ae` by hand while investigating an unrelated symptom.\n\n## Cause\n\n`internal/tools/bdguard`'s tests (gqlc-v2p, in flight) chdir'd into a\n`t.TempDir()` fixture repo and ran `git config user.email test@example.invalid`\nwithout pinning the command's directory. Process cwd is global to the test\nbinary, not per-test, so `t.TempDir()` cannot isolate it. During the window\nwhere cwd was the real worktree, the config write landed on the real\nrepository.\n\n`.git/config` is shared by every linked worktree — it is not per-worktree\nstate. The write therefore hit all seven worktrees at once, and any agent\ncommitting during that window inherited the fixture identity. The same runs\nalso set `core.bare = true`, which broke `git rev-parse --show-toplevel` in the\nmain checkout, which is where bd writes its passive export. It recurred three\ntimes before containment.\n\nThe cwd bug itself is fixed on gqlc-v2p (`cmd.Dir` on the fixture command\nhelper, plus `t.Chdir` for the run under test). This bead is the *other* half.\nFixing the one test removes the one known source; it does not make the failure\ndetectable, and the class is broader than this test — any fixture, script, or\nmistyped `git config` produces the same silent result. The defect is not that a\nbad identity was written. It is that a commit with a bogus author passed every\ngate we have.\n\nCommit time is also the only cheap point of repair. Once pushed, the fix is\n`--amend --reset-author` plus a force-push, and if a PR is open the rewrite\nchurns the review.\n\n## What to build\n\nExtend `.githooks` to reject a commit whose author or committer email is\nimplausible. `.githooks/commit-msg` already rejects `Co-Authored-By` values\nmentioning claude/@anthropic.com — this is the same shape of check on a\ndifferent field, so extend the existing hook rather than adding a new one.\n\nDecisions to settle in the PR:\n\n- **Denylist vs allowlist.** A denylist of known fixture spellings\n  (`test@example.invalid`, `@example.com`, `@example.org`, `root@localhost`,\n  empty) is low-friction and catches this incident. An allowlist is stronger\n  but needs maintenance and misfires on new contributors. Recommend denylist,\n  plus a shape check that the address contains `@` and a dot in the domain.\n- **Author AND committer.** Check both. This incident set both, but rebase and\n  cherry-pick can split them.\n- **Which hook fires.** `commit-msg` receives the message, not the identity, so\n  read the identity via `git var GIT_AUTHOR_IDENT` rather than a hook argument.\n  Confirm the chosen hook fires for `--amend` and `cherry-pick`, since those\n  are exactly the paths a repair takes.\n\n## Acceptance — the gate must be shown to FAIL\n\nPer the standing rule that a detector exiting 0 is not a gate: showing the hook\nruns is not enough. Demonstrate that\n`git -c user.email=test@example.invalid commit` is **rejected**, with exit\nstatus and message, and that a normal commit still succeeds. Then mutation-test\nthe guard — change the denylist entry, confirm the rejection stops — so we know\nthe rejection is caused by the rule and not by something incidental.\n\n## Related\n\n- gqlc-v2p — the cwd fix that removes this instance's source.\n- gqlc-5fm — `core.hooksPath` drift in this same shared `.git/config` silently\n  killed the hooks in every worktree at once. Same file, same shared-state\n  property, same invisibility. Confirm this hook cannot itself be disabled\n  without notice.","status":"closed","priority":1,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:48:58Z","created_by":"areqag","updated_at":"2026-07-28T03:14:07Z","started_at":"2026-07-28T03:05:27Z","closed_at":"2026-07-28T03:14:07Z","close_reason":"Shipped in PR #449. Extended commit-msg hook with _check_identity() denylist+shape guard for author and committer.","external_ref":"https://github.com/areqag/gqlc/issues/433","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.30","title":"test(schema): measure grammar coverage against ISO's own BNF production list, not GQL.g4","description":"Our grammar-coverage gate measures coverage against `internal/grammar/gql/GQL.g4`.\nThat denominator is wrong for this epic's stated goal.\n\nA gate built on our own grammar can only report **alternatives we implemented and\ndid not exercise**. It is structurally incapable of reporting a production that\nexists in ISO/IEC 39075 and that we never implemented at all — such a production\nis not in the denominator, so 100% coverage is reachable while arbitrarily much\nof the standard is missing. \"Have we covered our grammar\" is not the question;\n\"have we covered the standard\" is.\n\n## This is now measurable, for free\n\nVerified 2026-07-26 (team-lead, by fetch, not by report):\n\n    https://standards.iso.org/iso-iec/39075/ed-1/en/           -\u003e HTTP 200\n    ISO_IEC_39075(en).bnf.txt                                   3502 lines\n    productions matching ^\u003c...\u003e ::=                              814\n    ISO_IEC_39075(en)-features.xml                               228 \u003cfeature\u003e elements\n\nPublished by ISO themselves under the ISO Customer Licence, labelled as\ncontaining the grammar specified by ISO/IEC 39075. This is a **normative,\nmachine-readable, complete** list of every production in the standard. It is\nnot a draft, not a vendor rendering, and not the community ANTLR adaptation\nthat `gqlc-h9n.11` exists to correct the provenance of.\n\n## What to build\n\n1. Derive the **graph-schema-relevant subset** of the 814 productions: the\n   transitive closure from `\u003cgraph type specification\u003e` / `\u003cnested graph type\n   specification\u003e`. Derive it mechanically from the BNF file, do not hand-pick\n   — the whole point is that a human-chosen denominator reintroduces the bias\n   this bead exists to remove.\n2. For each production in that subset, record one of: implemented and\n   exercised by the corpus / implemented and unexercised / **not implemented**.\n   The third bucket is the one no current gate can produce.\n3. Gate on the third bucket not growing. Whether it must be empty is a\n   separate decision — see the sequencing note.\n\n## Sequencing note, and the reason this is P1 rather than P0\n\nThe third bucket is almost certainly large today, so this cannot start life as\n\"must be empty\" without blocking every PR. Land it first as a **recorded\ninventory with a ratchet** (the count may not increase), then drive the count\ndown bead by bead. Landing it as a hard zero-gate on day one would force either\na mass of stub implementations or an exemption list long enough to be\nmeaningless — both of which destroy the measurement.\n\nNote the ratchet must gate on the count of *unimplemented productions*, not on\na percentage: a percentage moves when the denominator moves, so adding corpus\nfiles could show improvement while nothing was implemented.\n\n## Relationship to the existing coverage gate\n\nThis does not replace the g4-derived gate. That one answers \"is our grammar\nexercised\", which is still worth knowing and catches a different defect\n(dead alternatives). The two denominators measure different things and both\nshould exist. Do not fold them together.\n\nAlso relevant: `bd memories antlr-alt-order-unreachable-alternatives` — ANTLR\nalternative ordering makes some GQL.g4 alternatives unreachable, so any\nobligation set over grammar alternatives needs an exemption mechanism. That\napplies to this gate too, and the exemption design in `gqlc-h9n.2` amendment\nA9 should be reused rather than reinvented.\n\n## What this does NOT do\n\nThe BNF is syntax only. It cannot resolve any question of the form \"what does\nthe standard say this production *means*\" — that prose is in the paid PDF.\n`gqlc-h9n.28` and `gqlc-h9n.29` are both blocked on exactly that and are\n**not** unblocked by this bead. Do not let a green coverage number be read as\na conformance claim; coverage of the production list is a necessary condition\nfor conformance, not a sufficient one, and the ADR should say so in those\nwords.","status":"closed","priority":1,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:32:35Z","created_by":"areqag","updated_at":"2026-07-28T03:46:10Z","started_at":"2026-07-28T03:14:16Z","closed_at":"2026-07-28T03:46:10Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/430","dependencies":[{"issue_id":"gqlc-h9n.30","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T13:32:35Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f9f8c-8fdb-7c54-aa92-53da0cdb7476","issue_id":"gqlc-h9n.30","author":"areqag","text":"Denominator derived (iso-access, 2026-07-26). Artefacts preserved at\n/tmp/lead/iso/ — iso-schema-productions.md, extract_productions.py,\niso39075.bnf.txt. **Commit the script with the implementation**; /tmp is\nvolatile and the closure must be reproducible, not a number someone once got.\n\nRoots used: \u003ccreate graph type statement\u003e (247), \u003cdrop graph type statement\u003e\n(262), \u003cgraph type source\u003e (253), \u003cnested graph type specification\u003e (1144),\n\u003cgraph type reference\u003e (1112), \u003ccatalog graph type parent and name\u003e (1116).\n\nTwo closures:\n- **DDL-only: 200 productions** (25% of the standard's 814), treating\n  \u003cgraph expression\u003e as an opaque leaf.\n- Full/unpruned: 738 (91%).\n\nThe 538 difference is entirely the query-language subtree reachable through\n\u003cgraph type like graph\u003e (line 238) and the \u003ccopy of graph type\u003e form. Those are\nDDL *references* into a query-language value, not DDL structure. Taking\nDDL-only, per iso-access's recommendation, which I agree with: the compliance\nquestion at that frontier is \"do we accept the reference\", not \"do we implement\nthe entire query grammar beneath it\".\n\n## Refinement the extraction does not yet make — do this before the gate lands\n\n**Split the 200 into structural vs lexical before reporting any coverage\nnumber.** Roughly 70 of the 200 are lexical terminals and identifier machinery:\n\u003ccomma\u003e (3422), \u003cleft paren\u003e (3452), \u003cdigit\u003e (3353), \u003chex digit\u003e (3338),\n\u003cunderscore\u003e (3494), the whole 3413-3497 punctuation block, and the 2334-2725\nname/identifier/quoted-sequence block.\n\nThose are satisfied for free by any lexer. Counting them in the denominator\nmeans the inventory opens at a high number that reflects having a tokeniser,\nnot having implemented graph-type DDL, and it will barely move as real features\nland. The signal we want lives in the ~130 structural productions — the\n1144-1573 graph-type-body range plus the 178-262 statement forms.\n\nThis does not change the ratchet design, which already gates on the **count of\nunimplemented productions** rather than a percentage precisely so a moving\ndenominator cannot fake progress. It changes what a human reads off the\ninventory. Report both buckets separately; let the ratchet run on structural.\n\n## Unchanged from the original scope\n\nLand as recorded inventory + ratchet, not as a hard zero-gate — the\nnot-implemented bucket is certainly large today and a day-one zero-gate forces\neither stub implementations or an exemption list long enough to be meaningless.\nReuse the exemption mechanism from gqlc-h9n.2 amendment A9 rather than\nreinventing it; ANTLR alternative ordering makes some alternatives unreachable\n(see `bd memories antlr-alt-order-unreachable-alternatives`) and that applies\nhere too.\n\nAnd the standing caveat, which the ADR must state in these words: coverage of\nthe production list is a **necessary** condition for conformance, not a\nsufficient one. The BNF is syntax only. A green number must not be read as a\nconformance claim — the prose that would justify that claim is in the paid PDF.","created_at":"2026-07-26T17:50:17Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-0qu","title":"ci: codegen-fence runs on every PR but is not a required status check","description":"`codegen-fence` runs on every PR and cannot block a single one.\n\nFound 2026-07-26 while checking why PR #421's CI had been computed against a\nstale base. `gh api repos/areqag/gqlc/branches/master/protection` returns:\n\n    required_status_checks.contexts:\n      [\"lint\", \"test\", \"tidy\", \"actionlint\", \"govulncheck\", \"live-smoke\"]\n\nSix. But `gh pr checks 421` and `gh pr checks 422` each list **seven**:\n`actionlint`, `codegen-fence`, `govulncheck`, `lint`, `live-smoke`, `test`,\n`tidy`. `codegen-fence` is defined at `.github/workflows/ci.yml:63`, triggers\non pull_request like every sibling job, runs, reports — and is absent from the\ncontexts list, so a red `codegen-fence` leaves the merge button green.\n\nThis is the `workflow-pr-blocking-two-halves` class exactly: `on: pull_request`\nmakes a workflow **run**; `required_status_checks.contexts` is what makes it\n**block**. Someone landed the YAML half and never landed the protection half.\nEvery PR since has been merging with this gate advisory.\n\n## What is actually unguarded\n\n`just test-codegen-fence` is not a nicety. It is the only thing standing\nbetween us and shipping generated code that does not compile:\n\n    cd test/data/codegen \u0026\u0026 go build ./... \u0026\u0026 go vet ./...\n    cd test/data/codegen \u0026\u0026 go mod tidy -diff\n    cd test/data/codegen \u0026\u0026 golangci-lint run\n\n`test/data/codegen` is a **separate Go module** — it is not reached by the\nroot `just test`, so no other required check compiles it. A codegen change that\nemits uncompilable Go, leaves the fixture module's `go.mod` untidy, or trips a\nlinter currently produces a red advisory check and merges anyway.\n\nThe reason nobody has noticed is that it has never actually failed on a PR we\nwanted to merge. That is luck, not evidence. Same shape as `gqlc-5fm`: ask\n\"does the check FAIL and does the failure stop anything?\", never \"is there a\ncheck?\".\n\n## Fix\n\n    gh api -X PATCH repos/areqag/gqlc/branches/master/protection/required_status_checks \\\n      -f 'contexts[]=lint' -f 'contexts[]=test' -f 'contexts[]=tidy' \\\n      -f 'contexts[]=actionlint' -f 'contexts[]=govulncheck' \\\n      -f 'contexts[]=live-smoke' -f 'contexts[]=codegen-fence'\n\n**This is a shared-infrastructure change and needs the repo owner to make it or\nexplicitly authorise it.** It is deliberately not applied by the autonomous\nloop: merge authority was delegated, branch-protection authority was not.\n\n**Verify the fix the same way the defect was found** — re-read the protection\nAPI and confirm seven contexts, then open a throwaway PR that deliberately\nbreaks the fixture module (e.g. a syntax error in a generated file) and confirm\nthe merge button is actually blocked. A protection change that has only been\nverified by reading the API response is the same mistake one level up.\n\n## Second, separate question: `strict` is false\n\nThe same response shows `required_status_checks.strict: false` — \"require\nbranches to be up to date before merging\" is OFF. That is why #421's green CI\nwas computed against a base that no longer existed once #422 landed, and both\nPRs touch `internal/schema/gql`. I checked the combined tree by hand (clean\nmerge, 12 packages green) and it was fine, but nothing forced that check.\n\nDo **not** bundle this with the fix above. Turning `strict` on serialises every\nmerge behind a rebase and a full CI re-run, which cuts directly against the\nstanding CI-wall-time priority, and this epic merges several PRs into one\npackage per session. The cheaper mitigation is a merge-time habit — test-merge\nonto current master and run the affected package — which costs seconds and only\nwhen the file sets actually overlap. Price both before choosing; if `strict`\nstays off, record *that* as the decision so the next person does not rediscover\nit as a bug.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:21:00Z","created_by":"areqag","updated_at":"2026-07-28T03:23:02Z","closed_at":"2026-07-28T03:23:02Z","close_reason":"Applied 2026-07-27 with owner's explicit authorisation (branch-protection authority delegated for this one change).\n\nrequired_status_checks.contexts is now 7, was 6: lint, test, tidy, actionlint, govulncheck, live-smoke, codegen-fence. strict deliberately left false per this bead's 'do not bundle' instruction; filed separately.\n\nVERIFIED THE WAY THE DEFECT WAS FOUND, not by reading the API response. Throwaway PR #451 added a syntax error in test/data/codegen/broken_probe.go, isolated to the separate fixture module so the root suite could not see it:\n  - codegen-fence  FAIL\n  - test           PASS  (confirms the break is invisible to the root suite)\n  - merge state    BLOCKED\nPR #451 closed unmerged, branch deleted.\n\nCORRECTION TO THIS BEAD'S PREMISE, found by that test: the claim 'test/data/codegen is not reached by the root just test, so no other required check compiles it' is FALSE. live-smoke also failed, because 'just test-codegen-live' runs go test inside test/data/codegen and therefore compiles it — and live-smoke was already a required context. So the compile half was already guarded.\n\nWhat codegen-fence uniquely adds is go vet, go mod tidy -diff and golangci-lint run over the fixture module, none of which live-smoke performs, plus it does not depend on the docker-gated job that GQLC_SKIP_LIVE=1 can short-circuit. The gate was still worth making required, for narrower reasons than the bead argued.","external_ref":"https://github.com/areqag/gqlc/issues/425","comments":[{"id":"019f9f96-3f77-75bf-af40-359b37d1ba80","issue_id":"gqlc-0qu","author":"areqag","text":"Re-verified 2026-07-26 17:5x UTC, still true. Read-only check:\n\n    $ gh api repos/areqag/gqlc/branches/master/protection \\\n        --jq '{contexts: .required_status_checks.contexts, strict: .required_status_checks.strict}'\n    {\"contexts\":[\"lint\",\"test\",\"tidy\",\"actionlint\",\"govulncheck\",\"live-smoke\"],\"strict\":false}\n\nSix contexts; `codegen-fence` still absent. Every PR merged since the YAML\nlanded — including four merged today — had this gate advisory.\n\n## Second half, same object: `strict: false`\n\nThe `strict` flag is off. That is \"require branches to be up to date before\nmerging\" disabled, and it is why #421's CI had been computed against a base\nthat no longer existed. Two distinct holes in one API object:\n\n- **`contexts`** decides which checks can block. `codegen-fence` cannot.\n- **`strict`** decides whether a green check is even *about* the merge result.\n  With it false, \"CI passed\" can mean \"CI passed against a tree that is not\n  what will land\".\n\nFixing only the first leaves a required `codegen-fence` that can still go green\non a stale base. Decide both together.\n\n## This needs the repo owner — not an agent\n\nChanging branch protection is repo-admin configuration. Merge authority was\ndelegated to the agent team; repo configuration was not. **No agent should run\nthis PATCH.** Filed for the owner, deliberately not executed.\n\nThe fix, for whoever has admin:\n\n    gh api -X PATCH repos/areqag/gqlc/branches/master/protection/required_status_checks \\\n      -f 'contexts[]=lint' -f 'contexts[]=test' -f 'contexts[]=tidy' \\\n      -f 'contexts[]=actionlint' -f 'contexts[]=govulncheck' \\\n      -f 'contexts[]=live-smoke' -f 'contexts[]=codegen-fence'\n\nAdd `-F strict=true` if the stale-base half is taken at the same time. Note\n`strict=true` has a real cost — it forces a rebase whenever master moves during\nreview, which on a repo with several agents landing PRs in parallel means\nfrequent re-pushes. That is a genuine trade-off, not an oversight to correct\nreflexively; the parallel-agent workflow here is exactly the case where it\nbites hardest. Weigh it rather than defaulting.\n\n## Verify it actually blocks\n\nPer the standing rule that a detector exiting 0 is not a gate: after the PATCH,\nconfirm the contexts list contains all seven, then confirm a PR with a\ndeliberately red `codegen-fence` shows a blocked merge button. A required check\nthat is misspelled relative to the job name silently never matches and is\nindistinguishable from not being required at all — so read the name back from\n`gh pr checks` rather than trusting the string that was sent.","created_at":"2026-07-26T18:00:52Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-v2p","title":"ci: nothing detects a PR that reverts the beads passive export","description":"Found 2026-07-26 on PR #422, caught by hand during a routine rebase check. Had\nthe lead not been checking that branch for an unrelated squash-duplicate\nproblem, it would have merged green and silently reverted master.\n\n## What happened\n\n`.beads/issues.jsonl` is a passive export: bd rewrites it as a side effect of\nordinary commands. A teammate worktree therefore accumulates a copy reflecting\nwhatever bd state existed when that worktree last ran a bd command. Staged with\na blanket `git add -A` and committed, it travels with an unrelated code PR.\n\nPR #422 (h9n.18, endpoint fillers) carried an export snapshot taken before PR\n#423 landed. Content comparison:\n\n    branch:  gqlc-h9n.19 open     gqlc-h9n.17 open     gqlc-h9n.28 absent\n    master:  gqlc-h9n.19 closed   gqlc-h9n.17 closed   gqlc-h9n.28 open\n\nMerging it would have reopened two beads closed that morning and deleted a bead\nfrom the export outright.\n\n## Why nothing caught it\n\n- **CI cannot.** A stale export is still syntactically valid JSONL. Every\n  existing gate — lint, test, tidy, actionlint, govulncheck, codegen-fence,\n  live-smoke — passes on it. There is no gate that looks at this file at all.\n- **`git diff --stat` actively misleads.** It renders as\n  `.beads/issues.jsonl | 34 ++++----` sitting among the real files, which reads\n  as incidental churn. The revert is only visible by diffing *content* —\n  parsing both copies and comparing per-issue status.\n- **Review would not catch it either.** A reviewer told to focus on\n  edgetype.go/listener.go has no reason to parse a 166-line JSONL, and the\n  line-level diff is mostly reordering noise.\n\nSo this is a class with zero detectors, not a weak one. It is the inverse of\ngqlc-5fm: there the detector existed and exited 0; here nothing looks.\n\n## Proposed gate\n\nA CI job that fails when a PR's `.beads/issues.jsonl` regresses against the\nmerge base. Cheapest sound form:\n\n1. Parse `.beads/issues.jsonl` at the PR head and at `origin/master`.\n2. FAIL if any issue id present on master is absent at head.\n3. FAIL if any issue closed on master is open at head.\n4. Additions and new closures are fine — that is what a sync PR does.\n\nThat is monotonicity, deliberately one-directional so `chore(bd)` sync PRs pass\nunmodified and no exemption mechanism is needed. Reopening a bead legitimately\nwould trip it; that is rare enough to warrant a label escape hatch, and it is\nbetter that it be loud.\n\nCheaper alternative worth pricing first: make the export path lead-only by\nconvention and add a pre-commit hook rejecting `.beads/issues.jsonl` in the\nindex unless the commit subject starts `chore(bd)`. Catches it earlier and\nlocally. But it is a local hook, and gqlc-5fm is the standing proof that local\nhooks die silently and repo-wide — so the hook is a nicety and the CI job is\nthe gate. Do not ship only the hook.\n\n## Notes for whoever takes this\n\n- Mutation-test it against the real artefact: replay PR #422's `eb4e7f0` export\n  against the master of that moment and confirm the gate FAILS naming both\n  reopened beads and the dropped one. A gate for this class that has never been\n  run against the incident that motivated it is not evidence of anything.\n- Check `ci.yml` before adding a `just` recipe that exits non-zero — `just test`\n  runs in CI, and gqlc-5fm is the incident where a fatal recipe would have\n  failed every run. If the check belongs in `just`, guard it on `$CI` semantics\n  deliberately rather than by reflex.\n- Watch the cost: parsing two JSONL files is milliseconds, but a whole extra\n  job has fixed runner overhead. Prefer folding it into an existing job unless\n  it needs the merge base checked out.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:04:48Z","created_by":"areqag","updated_at":"2026-07-26T23:14:19Z","closed_at":"2026-07-26T23:14:19Z","close_reason":"PR #429 merged as e067cb1; GH #424 auto-closed. bdguard monotonicity gate wired into required tidy job.","external_ref":"https://github.com/areqag/gqlc/issues/424","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.28","title":"BLOCKED (no ISO arbiter, gqlc-lir): does FLOAT(p) precision count bits or decimal digits","description":"\u003e **BLOCKED — permanently, as of 2026-07-27. Do not resolve this by inference.**\n\u003e\n\u003e This question is answerable only from ISO/IEC 39075's General Rules, which are\n\u003e in the paid PDF. `gqlc-lir` decided against the CHF 227 purchase, so there is\n\u003e no arbiter and there will not be one. The free BNF positively proves the\n\u003e ambiguity is not resolvable syntactically.\n\u003e\n\u003e A web-search paraphrase, a vendor conformance table, or an analogy to SQL is\n\u003e **not** a citation. If gqlc must pick a behaviour to ship, record it as a gqlc\n\u003e decision under gqlc's own name — never as \"ISO says\" — and leave this bead\n\u003e open, because the standards question stays unanswered either way.\n\nFound 2026-07-26 by team-lead while reviewing the uncommitted gqlc-h9n.16 diff,\nbefore it reached adversarial review. The implementer had extended the bit-width\nfold from `binaryExactNumericType` to `approximateNumericType`, folding\n`FLOAT(16)` -\u003e `TypeFloat16`. That half was rejected; this bead records the\nquestion it exposed.\n\n## The question\n\nDoes the `precision` in `FLOAT(p)` count **bits of mantissa** or **decimal\ndigits**?\n\nThe grammar does not say, and its structure points the wrong way for a\nbit-width reading:\n\n```\nGQL.g4:1801   INT              (LEFT_PAREN precision RIGHT_PAREN)?               precision only\nGQL.g4:1814   UINT             (LEFT_PAREN precision RIGHT_PAREN)?               precision only\nGQL.g4:1827   INTEGER          (LEFT_PAREN precision RIGHT_PAREN)?               precision only\nGQL.g4:1832   DECIMAL | DEC    (LEFT_PAREN precision (COMMA scale)? RIGHT_PAREN notNull?)?\nGQL.g4:1849   FLOAT            (LEFT_PAREN precision (COMMA scale)? RIGHT_PAREN)?\n```\n\n`FLOAT`'s parenthetical is identical in shape to `DECIMAL`'s — it carries the\noptional `scale`. The criterion that justifies the INT fold (a binary type takes\nprecision alone; a decimal type pairs precision with scale, which is what\nseparates `binaryExactNumericType` from `decimalExactNumericType`) therefore\nsorts `FLOAT(p)` with the decimal types, not the binary ones.\n\nAgainst that: `FLOAT16|FLOAT32|FLOAT64|FLOAT128|FLOAT256` are siblings of\n`FLOAT (...)` under `approximateNumericType` (GQL.g4:1844-1849), the same\nsibling relationship that makes the INT reading work. So the grammar supplies\none argument each way and adjudicates neither.\n\n## Why it matters more than INT(7)\n\n`gqlc-h9n.25` asks what to do about widths with no exact model counterpart.\nThis is worse, because the plausible-looking fix is **strictly narrowing**:\n\n- Today `FLOAT(16)` -\u003e `TypeFloat` (64-bit). Whatever the author meant, the\n  resolved type is a safe superset.\n- Folded, `FLOAT(16)` -\u003e `TypeFloat16`. Half precision holds ~3.3 decimal\n  digits. If `p` is digits, a schema asking for 16 digits silently resolves to a\n  type that holds three.\n\nStrictly-widening is the property demanded of any resolution-rule change on this\nepic. A digits reading makes the fold violate it, and the failure is silent —\naccepted input, nil error, wrong model, which is this epic's signature defect.\nWe would be introducing one while fixing another.\n\n## What this bead must produce\n\nAn **ISO/IEC 39075 citation**, not an inference. `gqlc-cfj` is open precisely\nbecause this repo has a habit of asserting what the standard says without\nsourcing it, and SQL tradition (where `FLOAT(p)` is binary mantissa precision\nand has no scale) is not evidence about GQL — note that this grammar gives FLOAT\na scale slot, which SQL's does not, so GQL's FLOAT is demonstrably not SQL's.\n\nDeliverable is one of:\n\n1. Citation says bits -\u003e fold `FLOAT(16..256)` the way INT is folded, in a\n   follow-up PR, with the same equivalence test shape.\n2. Citation says digits -\u003e record in the `gqlc-0ri` deviation register that\n   `FLOAT(p)` is lossy-but-safe, and comment the fallback in\n   `internal/schema/gql/propertytype.go` naming it.\n3. Standard does not specify -\u003e same as (2), plus say so out loud, because\n   silence is itself the finding.\n\nUntil then the status quo stands and is pinned: `FLOAT(16)` is a row in\n`TestPropertyBinaryWidthNonFolds`, so the decision is asserted by a test rather\nthan implied by absence.\n\nScope note: `DECIMAL(p,s)` is not in scope (gqlc-5md owns the length/digit\nqualifier question). `INT(7)` and friends are gqlc-h9n.25. This bead is only\nabout what `FLOAT(p)`'s precision denotes.","status":"open","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T16:51:37Z","created_by":"areqag","updated_at":"2026-07-27T13:08:46Z","external_ref":"https://github.com/areqag/gqlc/issues/420","dependencies":[{"issue_id":"gqlc-h9n.28","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T12:51:37Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-h9n.28","depends_on_id":"gqlc-lir","type":"blocks","created_at":"2026-07-26T13:58:56Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f9f7c-b19c-7f01-adda-56ac1add58b4","issue_id":"gqlc-h9n.28","author":"areqag","text":"2026-07-26, team-lead. Research (agent iso-access) plus my own verification settles what CAN and CANNOT resolve this bead.\n\nISO publishes the complete normative BNF free at https://standards.iso.org/iso-iec/39075/ed-1/en/ (verified by fetch: HTTP 200, ISO_IEC_39075(en).bnf.txt, 3502 lines, 814 productions, ISO Customer Licence). The relevant production, verbatim at line 1423:\n\n    \u003capproximate numeric type\u003e ::=\n        FLOAT16 [ \u003cnot null\u003e ]\n      | FLOAT32 [ \u003cnot null\u003e ]\n      | FLOAT64 [ \u003cnot null\u003e ]\n      | FLOAT128 [ \u003cnot null\u003e ]\n      | FLOAT256 [ \u003cnot null\u003e ]\n      | FLOAT [ \u003cleft paren\u003e \u003cprecision\u003e [ \u003ccomma\u003e \u003cscale\u003e ] \u003cright paren\u003e ] [ \u003cnot null\u003e ]\n      | REAL [ \u003cnot null\u003e ]\n      | DOUBLE [ PRECISION ] [ \u003cnot null\u003e ]\n\nThis CONFIRMS the ambiguity and PROVES it cannot be resolved syntactically. FLOAT(p) carries the decimal-type shape (precision, optional scale) while sitting as a sibling of the bit-width spellings. Both readings remain grammatically viable. The free artefact is syntax only; the General Rules and Syntax Rules that adjudicate meaning are in the paid PDF (CHF 227, https://www.iso.org/standard/76120.html).\n\nStatus: BLOCKED ON ISO PURCHASE. Not blocked on research — the research is done and the answer is that no free normative source exists for this question.\n\nExplicitly REJECTED resolution: 'record it as a stated deviation with our reasoning and note the standard was not consulted'. Choosing the wrong reading here produces silent strict NARROWING — a schema author writing FLOAT(16) meaning 16 decimal digits of precision would get a 16-bit float. Accepted input, nil error, wrong model: this epic's signature defect, wearing a citation. A deviation record is honest when we have decided something; it launders a coin-flip when the standard is the only possible arbiter and we did not consult it.\n\nCurrent state is correct and should be held: the parenthesised-width fold landed in PR #421 for INT/INTEGER/UINT only, with FLOAT deliberately excluded, so FLOAT(p) resolves to the widest type. That is a provable superset — safe under either reading.\n\nNote for whoever buys: ISO/IEC 39075:2024/CD Cor 1 is at Committee Draft (https://www.iso.org/standard/93701.html), so the published text has known errata in process. Does not change the buy decision but the corrigendum should be re-checked against this answer when it publishes.\n\nNot purchasing on my own authority — that is the user's money and their call.","created_at":"2026-07-26T17:32:57Z"}],"dependency_count":1,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-5fm","title":"just: check-hooks warns and exits 0, so dead git hooks read as a green doctor","description":"Found 2026-07-26. Two agents independently hit this within an hour; the second\nreport is what made the scope clear.\n\n## What was wrong\n\n`core.hooksPath` was set in `.git/config` to the absolute path\n`/home/antranig/Developer/gqlc/gqlc/.git/hooks`. That directory contains **only\n`.sample` files**, which git ignores. So `commit-msg`, `pre-commit`, `pre-push`\nand `post-merge` were all inactive — repo-wide, in every worktree, since\n`.git/config` is shared by all of them.\n\nAmong the casualties: `.githooks/commit-msg`, which CLAUDE.md names as the\nenforcement mechanism for the no-AI-attribution rule. CLAUDE.md's claim that the\nrule is \"enforced at commit time\" was simply false for the duration.\n\nRepaired by running `just init` (the project's own recipe:\n`git config core.hooksPath .githooks`). Audited the last 40 commits for\n`Co-Authored-By:` naming claude/anthropic and for the \"Generated with Claude\nCode\" footer: **zero violations**. Discipline held where the gate did not, but\nthat is luck, not a control.\n\n## The actual defect — the gate does not gate\n\n`justfile:19` `check-hooks` detects the drift correctly and then does this:\n\n```\necho \"warning: core.hooksPath is '${got:-\u003cunset\u003e}', expected '.githooks' — local hooks are inactive. Run 'just init' to fix.\" \u003e\u00262\n```\n\nIt warns and exits 0. Consequences:\n\n- `just doctor` runs `check-hooks` and then prints `ok`. It printed `ok` the\n  entire time hooks were dead.\n- `check-hooks` is wired into `just test`, where a single stderr line is buried\n  in test output nobody reads line-by-line, and the recipe stays green.\n\nSo the detector is present, correct, and inert. This is the same shape as\n`gqlc-h9n.15` (inert measurement guards) and the CI bead about a workflow that\nruns but is not in `required_status_checks` — the mechanism executes and\ninfluences nothing.\n\nThe comment above the recipe (justfile:15-17) makes it sharper: it says drift is\n\"the only way local pre-commit/pre-push gates can silently die (CI cannot see\nlocal git config)\". The author identified the exact silent-death mode and then\nmade the only detector for it non-fatal.\n\n## Fix\n\nMake `check-hooks` exit non-zero on drift. It is already wired into `just test`,\nso this converts a buried warning into a hard stop with an actionable message\nthat already names the remedy (`just init`).\n\nPoints to weigh while doing it:\n\n- **`just test` must still be runnable in a fresh clone.** A new contributor who\n  has not run `just init` will now hit a hard failure. That is the intent, but\n  the message must be unmissable and must lead with the fix. Confirm the failure\n  text renders well when `just test` fails on it.\n- **Does anything in CI run `check-hooks`?** CI has no local hooks and does not\n  need them. If `just test` is invoked in CI, a hard failure there would be a\n  self-inflicted outage. Check `ci.yml` before changing the exit status, and if\n  CI does call it, make the check conditional on not-CI rather than weakening it.\n  This is the load-bearing question — get it wrong and the fix breaks the build.\n- **Mutation-test the fix**: unset `core.hooksPath`, set it to a wrong path, and\n  set it to a path of `.sample`-only files. The third is the case that actually\n  occurred and the one a naive \"is it set?\" check would miss. The current\n  string-equality check against `.githooks` happens to catch it; do not\n  regress that into an existence test.\n\n## Follow-up worth considering, not required here\n\n`check-hooks` compares the config value to the literal string `.githooks`. That\nis right, but it does not verify the hooks are executable or that the directory\nis non-empty. A `core.hooksPath = .githooks` pointing at a directory whose files\nlost their executable bit would pass. Cheap to add while in there; do not let it\nexpand the change.\n","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T16:37:23Z","created_by":"areqag","updated_at":"2026-07-26T16:42:56Z","closed_at":"2026-07-26T16:42:56Z","close_reason":"Landed in PR #417. check-hooks now exits 1 on core.hooksPath drift, skipped under CI (ci.yml:51 runs 'just test' in a fresh clone where the value is unset, so a naive fatal check would have broken every CI run). Kept the value comparison rather than a directory-existence test — .git/hooks exists and holds only .sample files, so an existence test cannot see this failure; a justfile comment records that. Mutation-verified across four states: correct value, CI=true, drift to the absolute .git/hooks path that actually occurred, and unset. 'just doctor' now exits 1 on drift instead of printing ok. Audited the 40 commits made while hooks were dead for AI-attribution trailers and footers: zero violations.","external_ref":"https://github.com/areqag/gqlc/issues/399","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.19","title":"schema/gql: EnterNodeTypePattern has no nesting-depth guard","description":"`closedGraphReferenceValueType` (`internal/grammar/gql/GQL.g4:1926`) is\n`PROPERTY? GRAPH nestedGraphTypeSpecification notNull?` — a whole graph type body\ncan appear as a **property value type**. `EnterNodeTypePattern`\n(`internal/schema/gql/listener.go:109`) appends to `l.raw.nodes` unconditionally,\nwith no check on how deeply nested the pattern is. Element types declared inside\na nested specification are therefore collected as element types of the **outer**\ngraph type.\n\nThe test harness already models the correct rule: `coverage.nestingDepth`\n(`internal/schema/gql/corpus_coverage_test.go:128`) counts enclosing\n`nestedGraphTypeSpecification` contexts precisely so it can tell \"an element of\nthis graph type\" from \"an element of one nested inside a property\". The listener\nhas no equivalent.\n\n## Status: latent, not live — and the failure shape is worse than a leak\n\nMeasured at `7217b18` by walking the listener directly and reading `l.raw`:\n\n```\nCREATE GRAPH TYPE Seed {\n    (:Outer { g :: PROPERTY GRAPH { (:Inner), (:Inner2) } })\n}\n```\n\n```\nwalk err = unsupported property value type\nraw.nodes collected = 2\n  [0] name=\"\" alias=\"\" labels=[Inner]  props=0\n  [1] name=\"\" alias=\"\" labels=[Inner2] props=0\n```\n\nNote what that is. It is not \"outer plus two extra\". `Outer` is **absent** and the\ntwo nested nodes have been **promoted to top level** — the collection is inverted.\n`EnterNodeTypePattern` returns early without appending when `nodeContent` fails, so\nthe one real node is dropped, while the two nodes that should not exist at this\nlevel are appended by their own (successful) `EnterNodeTypePattern` calls.\n\nNothing of this is observable today: `walk` returns\n`ErrUnsupportedPropertyValueType`, so `resolve()` never runs and `Parse` returns a\nzero schema with a non-nil error. The behaviour is correct by accident, via a\nrejection that has nothing to do with nesting.\n\n## Why this needs fixing before the value type is supported, not after\n\nThe masking error is exactly what a later bead will remove. The moment\n`closedGraphReferenceValueType` is accepted, this becomes a live silent wrong\nanswer of the worst shape in this epic — a *partial and wrong* schema with a `nil`\nerror, where the declared node is missing and two undeclared ones are present.\n\nSo: add the depth guard now, while it is cheap and provable in isolation, rather\nthan discovering it as a regression inside the value-type work.\n\n## Fix\n\nGive the listener the same notion of depth the coverage harness has, and ignore\nelement types at depth \u003e 0 in `EnterNodeTypePattern` and its edge counterpart.\nWhether nested element types should eventually be *modelled* is a separate\nquestion; the immediate requirement is that they must not silently become\nelements of the enclosing graph type.\n\nGuard it with a test that walks the listener and asserts on `raw`, not only on\n`Parse` — a `Parse`-level test cannot see this while the value type is rejected,\nand a test that passes for the wrong reason is how this survived in the first\nplace.\n\nDiscovered while reviewing PR #392.\n","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T05:35:24Z","created_by":"areqag","updated_at":"2026-07-26T16:59:51Z","closed_at":"2026-07-26T16:59:51Z","close_reason":"Landed in #418 (squashed to master). Four element-type collectors (EnterNodeTypePattern, EnterNodeTypePhrase, EnterEdgeTypePattern, EnterEdgeTypePhrase) now skip contexts whose nestedGraphTypeSpecification depth exceeds 1, so a nested graph-type body no longer promotes its elements into the outer schema.\n\nReviewer rev-h9n19 verified by execution, not by reading: applied ONLY the new parser_test.go on top of parent e62c6fd and confirmed all five subtests FAIL there, with the actual leaked node sets observed ([Inner1 Inner2], [A B], [Inner], [A B], [C]). Each of the four guards was then deleted individually and every one reddened at least one subtest. The '\u003e 1' threshold was mutated to '\u003e 0' (all five fail) and '== 2' (only the twice-nested subtest fails), confirming the twice-nested case is the sole witness that turns the threshold choice.\n\nThe ErrUnsupportedType pin was itself mutation-tested: patching property() to fake-lift the property-type rejection makes all five subtests fail. That is the mechanism that forces gqlc-h9n.6 to update this test rather than silently re-expose the leak, so the guard cannot rot quietly.\n\n'Nothing observable changes today' was verified rather than assumed: parser.go:39 is the only reader of l.raw and only runs after l.walk returns nil, which no current input reaches because PROPERTY GRAPH is absent from typeSpellings.\n\nThe duplicated nestedDepth helper was compared line-by-line against coverage.nestingDepth; they count the same thing by independent mechanisms (interface assertion vs rule-name string), which is the property the author cited for keeping them separate.\n\nOpen follow-up, non-blocking: the nestedDepth doc comment says the single enclosing spec is 'the one after AS', but AS is optional in graphTypeSource (GQL.g4:350), so a top-level CREATE GRAPH TYPE with no AS also has depth 1. Being corrected in the stacked gqlc-h9n.18 branch, which edits the same file, rather than spending a separate CI cycle.","external_ref":"https://github.com/areqag/gqlc/issues/400","labels":["gql","schema"],"dependencies":[{"issue_id":"gqlc-h9n.19","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T01:35:23Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.18","title":"schema/gql: properties on an inline endpoint node reference are parsed and discarded","description":"`sourceNodeTypeReference` and `destinationNodeTypeReference` each have an\nalternative 2 of the form `LEFT_PAREN nodeTypeFiller? RIGHT_PAREN`\n(`internal/grammar/gql/GQL.g4:1640-1646`). That filler admits a full property\ntype set. The listener parses it and throws it away.\n\nMeasured at `7217b18` with a scratch probe:\n\n```\n(:Person) -[:AUTHORED]-\u003e (:Post { extra :: STRING })\n```\n\n```\nendpoint_props             err=nil nodes=2 edges=1 | Person:props=1 Post:props=1\nbaseline_plain_endpoints   err=nil nodes=2 edges=1 | Person:props=1 Post:props=1\n```\n\nThe two spellings resolve **byte-identically**: `extra` is discarded and the error\nis `nil`. This is the same silent-wrong-answer shape the corpus work was built to\neliminate, surviving inside a construct the gate reads as covered.\n\n## Why the gate does not catch it\n\nCoverage is measured on the **parse tree**. ANTLR entering the subtree discharges\nthe obligation whether or not the listener then reads anything from it. So\n`sourceNodeTypeReference#2` is green at `alternatives 46/46` while its contents are\ndropped. `assertNothingDropped` does not help either: it compares *element* counts —\nthree declarations, three model entries — and never looks at properties.\n\nThis is a live witness that \"133/133/46\" and \"no silent drops\" are different\nstatements. Worth keeping that framing when fixing it.\n\n## Fix\n\nDecide the semantics first. An inline endpoint filler either\n\n- (a) declares properties on the endpoint node type, in which case it must merge\n  with, or conflict-check against, the node type declared elsewhere in the same\n  graph type; or\n- (b) is a constraint the model has no room for, in which case it is a\n  `semanticCase` and must be **rejected** with a sentinel rather than accepted and\n  dropped.\n\nEither way the current behaviour — accept, discard, return nil — is not one of the\noptions. Reject-with-sentinel is the cheap correct step if (a) is not wanted yet;\nthat converts a silent wrong answer into a named gap, which is the sequencing the\nepic already uses everywhere else.\n\nAdd corpus coverage for the property-bearing endpoint spelling; the corpus today\nreaches the alternative only with an empty filler.\n\nDiscovered while reviewing PR #392.\n","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T05:35:22Z","created_by":"areqag","updated_at":"2026-07-26T17:15:41Z","closed_at":"2026-07-26T17:15:41Z","close_reason":"Fixed in PR #422 (squash bb2db4b), reviewer PASS verified by execution.\n\nAn inline endpoint filler carrying properties — (:Person)-[:AUTHORED]-\u003e(:Post { extra :: STRING }) — now fails with ErrEndpointFillerHasProperties instead of parsing, returning err=nil, and producing a model byte-identical to the label-only spelling. That was the epic's signature defect: accepted input, nil error, wrong model.\n\nVerified by rev-h9n18 in detached worktrees at ba67304:\n- Non-vacuity: all four rejection subtests FAIL at parent 7fd4118 with 'got nil' — the silent accept is reproduced, not asserted.\n- Mutation 1 (neuter the guard): all four redden; the empty-brace pin and TestEdgeInlineEndpoints stay green.\n- Mutation 2 (widen to pts != nil, the ANTLR non-nil-accessor trap): rejection subtests still pass but TestEndpointFillerEmptyBraceAllowed FAILS. The (:Post {}) ALLOW pin is load-bearing, not decorative — it is what stops the guard over-rejecting empty braces.\n- Symmetry: source-side, destination-side, both-sides, and left-pointing-arc permutations all reject. Under edgeTypePatternPointingLeft the second paren is syntactically the sourceNodeTypeReference, so the rejection reaches it through sourceRef; source→target canonicalisation is preserved.\n- Corpus: zero endpoint-filler-with-properties rows, re-derived with two independent regexes because the author's grep missed lowercase and no-space spellings. 46/46 grammar alternatives still covered.\n\nWhat this did NOT settle: whether the spelling is a property *declaration* on the endpoint node type (propertyType is 'propertyName typed? propertyValueType' — declaration syntax) or a *constraint* over a type declared elsewhere (alt 2 of sourceNodeTypeReference/destinationNodeTypeReference at GQL.g4:1620/1625 strips the nodeSynonym TYPE? nodeTypeName prefix and localNodeTypeAlias?; CONTEXT.md:50 already resolves the endpoint to the referenced type's label set). The grammar does not adjudicate. #422 could land regardless because accept-and-discard is wrong under BOTH readings, so the sentinel is correct today either way.\n\nSettling it needs the ISO/IEC 39075:2024 subclause 18.3 semantic sentence — a citation, not an inference. Tracked as gqlc-h9n.29, which also carries the guardrail that the retirement must stay a local change; the reviewer confirmed the plumbing (fillerLabels returning error, refErr threading) survives a move to either reading and only the guard body would be replaced.\n\nAlso carried in this PR: the nestedDepth doc comment from gqlc-h9n.19 no longer implies AS is required. GQL.g4:350 is 'AS? nestedGraphTypeSpecification' — the citation is exact (347 is the rule header, 349 is the graphTypeLikeGraph alternative).\n\nCloses GH #401 (auto-closed on merge).","external_ref":"https://github.com/areqag/gqlc/issues/401","labels":["gql","schema"],"dependencies":[{"issue_id":"gqlc-h9n.18","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T01:35:22Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.17","title":"schema/gql: gate the incidental-carrier class in the corpus coverage test","description":"The corpus-wide coverage gate asks whether *some* corpus file reaches a grammar\nname, never *which* file. So a name can read green because a file in an unrelated\nclause area reached it in passing. That is the **incidental-carrier** class, and it\nis invisible to `TestCorpusGrammarCoverage` by construction.\n\nTwo instances were found and fixed by hand on the `gqlc-h9n.2` branch (PR #392):\n\n1. `localdatetimeType#2` (`TIMESTAMP (WITHOUT TIME ZONE)? notNull?`) was reached\n   only by five `18.3-edge-type` files that used `TIMESTAMP` as a property type in\n   examples that are not about type grammar at all. Fixed by adding\n   `18.9-value-type/scalar_timestamp.gql` and\n   `scalar_timestamp_without_time_zone.gql` so the value-type area carries its own\n   spellings.\n2. Every corpus file spelling the `NODE` keyword was an `unsupported` entry — both\n   18.2 phrase forms and `constructed_node_open` — so no *resolving* path exercised\n   an explicit node synonym in `nodeTypePattern`, even though the `VERTEX` spelling\n   of the same production had resolved all along. Fixed by\n   `12.6-graph-type-statement/synonyms_node_edge.gql`.\n\n## The actual defect\n\n**Nothing in the tree gates any of this.** Delete either fix, repin\n`wantCorpusEntries` / `wantCorpusResolving`, and the suite is green again at\n`rules 133/133, tokens 133/133, alternatives 46/46` — carriage silently reverts to\nthe incidental files. The three `.gql` files that close the two instances are held\nin place by a header comment, not by a test.\n\nThe measurement itself was also by hand, at one SHA, and is not reproducible from\nthe tree: the detector lives in `/tmp/lead/orphans.sh` and was never checked in.\n\n## What to build\n\nMove the per-area detector into the package as a test. The method that worked:\n\n- Derive the full obligation set once by running the closure with an empty corpus.\n- For each area, keep only that area's files and run the gate; `carried(area)` is\n  the obligation set minus the red set.\n- A name whose owning area is not among its carriers is an orphan.\n\nGuard rails that mattered while building it out of tree, and that a real test gets\nfor free: the copy step must not nest (`cp -r src dst` nests when `dst` exists —\nthis produced two byte-identical runs at two different SHAs and was caught only by\nvalidating against a SHA with a known orphan), and `go test -count=1` is mandatory\nbecause the corpus files are out-of-module inputs the build cache does not see.\n\nValidation requirement: the new test must be shown non-vacuous by pointing it at\nthe commit before either fix and confirming it independently reproduces that\norphan.\n\n## Scope note\n\nDeciding *which* area owns a name is the hard part and is a judgement call for\ncross-cutting productions (`notNull` is reached by every area). The out-of-tree\ndetector resolved this by directory prefix on the corpus path. That is good enough\nand should be kept; where it is wrong, an explicit exemption list with a reason\nper entry is preferable to loosening the rule.\n\nDiscovered while reviewing PR #392.\n","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T05:35:16Z","created_by":"areqag","updated_at":"2026-07-26T17:00:06Z","started_at":"2026-07-26T16:42:06Z","closed_at":"2026-07-26T17:00:06Z","close_reason":"Landed in #419 (squashed to master). Adds internal/schema/gql/corpus_carriers_test.go: TestCorpusAreaParseCarriers gates parse-time incidental carriage — a grammar name the whole corpus reaches but that no file in the OWNING area reaches.\n\nReviewer rev-h9n17 verified by execution. Non-vacuity was replayed at both historical SHAs in detached worktrees with the test file copied in unadapted: FAILS at 0b000c60 with exactly 'localdatetimeType#2 (alt) owned by D1, D2' (the pre-TIMESTAMP-fix orphan), PASSES at 22e02c02. All 35 exemptions were deleted one at a time and every single deletion failed with 'unowned' naming the removed entry — no dead exemptions. All six metadata guards were mutated and all six killed with the exact claimed message (owned-name exemption, not-in-obligation, stale token, stale alt, empty bead/why/name, invalid kind, duplicate identifier).\n\nThe blind spot I was most worried about is closed: remapping area C's prefix to a nonsense section surfaces 54 formerly-owned names as unowned rather than silently passing, because findOrphans early-returns on an empty owner set but findUnowned then requires each such name to be exempt. The two halves close the loop. Prefix-boundary probes confirm no lexical false-match (18.10 does not match 18.1).\n\nRuntime 0.32s standalone, 0.43s for the package — no CI-speed cost.\n\nScope limit is disclosed in the file rather than left implicit: the gate measures the parse tree only, so a name carried exclusively by files the listener rejects after a successful parse still reads as covered. That is why it does NOT reproduce the NODE-synonym orphan at 22e02c02, and it is filed as gqlc-h9n.26.\n\nFollow-up noted, not blocking: three exemptions (isOrColon, IS, COLON) are attributed to gqlc-h9n.27 but are clause 16.7, whereas h9n.27's title names only the 12.4-declared graphTypeLikeGraph/LIKE. h9n.27 is being rescoped to the general section-heading-misattribution class so those references do not go stale when it closes.","external_ref":"https://github.com/areqag/gqlc/issues/402","metadata":null,"labels":["gql","schema","test"],"dependencies":[{"issue_id":"gqlc-h9n.17","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T01:35:15Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.16","title":"schema/gql: parenthesised numeric width spellings lose their width — INTEGER(8) resolves to INT, not INT8","description":"RE-SCOPED 2026-07-26 to Problem A only. The length/precision/scale half moved to\ngqlc-5md; see \"What moved out and why\" at the bottom.\n\n## Problem A: the width fold\n\nMeasured 2026-07-25 (probe /tmp/h9n2probe/leadstrlen) and re-measured 2026-07-26\n(probe /tmp/h9n16-probe, complete files, Parse + model compare).\n\nAll six numeric width pairs diverge. The suffix spelling keeps the width; the\nparenthesised spelling of the SAME construct loses it:\n\n  INT8    -\u003e INT8     but  INTEGER(8)  -\u003e INT\n  INT16   -\u003e INT16    but  INTEGER(16) -\u003e INT\n  INT32   -\u003e INT32    but  INTEGER(32) -\u003e INT\n  INT64   -\u003e INT64    but  INTEGER(64) -\u003e INT\n  FLOAT32 -\u003e FLOAT32  but  FLOAT(32)   -\u003e FLOAT\n  FLOAT64 -\u003e FLOAT64  but  FLOAT(64)   -\u003e FLOAT\n\n`INTEGER(8)` emits Go `int` where the schema said 8 bits. ADR 0002's own\nrationale condemns exactly this:\n\n  \"Losing signedness and width would force codegen to emit int/uint everywhere\n   and discard information the schema author explicitly stated.\"\n\nSo Problem A is a defect *against a recorded decision*, not a change *to* one.\nThat is the whole reason it separates cleanly from gqlc-5md.\n\n## Root cause\n\n`internal/schema/gql/propertytype.go:canonicalSpelling` truncates the spelling at\nthe first `(` before the `typeSpellings` lookup runs. `INTEGER(8)` is looked up as\n`INTEGER`, which is a real row, so the lookup succeeds and returns `TypeInt`. The\nparameter is not \"dropped for lack of a field\" — it is discarded before anything\ncould read it.\n\n## Measured equivalence classes (2026-07-26)\n\n  TYPE=STRING \u003c- STRING | STRING(10) | STRING(2, 5) | STRING(0xFF) | CHAR |\n                 CHAR(4) | CHAR(8) | VARCHAR | VARCHAR(5)\n  TYPE=INT    \u003c- INT | INT(8) | INT(10) | INT(16) | INT(32) | INT(64) | INT(7) |\n                 INTEGER | INTEGER(8) | INTEGER(10)\n  TYPE=INT8   \u003c- INT8 | INTEGER8\n\nOnly the width-bearing members of these classes move under Problem A. The\nSTRING class does not change at all — it is gqlc-5md's problem.\n\n## Mechanism (no new field, no new code path)\n\n- `internal/schema/gql/propertytype.go`: 23 new `typeSpellings` rows; make\n  `canonicalSpelling` normalise whitespace *inside* the parenthetical; make\n  `normaliseType` try the full canonical spelling first and fall back to today's\n  truncated lookup.\n- `internal/schema/gql/propertytype_test.go`: a table pinning the 23 folds AND\n  the non-folds — INT(7), INT(10), FLOAT(24), FLOAT(10, 2), DEC(8), STRING(5) —\n  because the fallback is the half that can silently over-reach.\n- `docs/adr/0002-bit-width-preserving-value-type-model.md`: amend the\n  normalisation sentence so the width-bearing parenthetical is named as folding,\n  distinct from the length/precision qualifier that still drops.\n- Re-point the five corpus rows citing `bead: \"gqlc-h9n.16\"` to `gqlc-5md`.\n  Nothing checks that a cited bead is open, so this must land in THIS PR.\n\n## NOT strictly widening — the PR body must say so\n\n- Codegen: 13 emissions change text; 9 spellings go from generating to\n  `ErrUnrepresentableWidth`, which aborts the WHOLE schema generation at\n  `internal/codegen/prepare.go:489`; 1 no-op. `INT(128)` emits `Amount int`\n  today and will abort generation after the fold.\n- Resolver: `INT8` vs `INT(8)` unions start passing (widening). `INT(8)` vs\n  `INT(16)` unions that pass today start FAILING. That is the one genuine\n  regression and it is intended — the two are different widths.\n\n## Do not sub-split\n\nDo not stage this as \"fold 8/16/32/64 now, 128/256 later\". Leaving 128/256\nunfolded preserves `INT(128)` != `INT128`, which is the divergence this bead\nexists to close, and buys only a deferral of a regression that has to be taken\neventually.\n\n## Ordering\n\ngqlc-h9n.21 (assert the model collision a semanticCase describes, not just its\nspelling) must land before gqlc-5md. It does NOT block Problem A: zero corpus\nentries and zero semantic rows change under Problem A.\n\n## What moved out and why\n\nThe length/precision/scale field on PropertyType is now gqlc-5md at P3.\nADR 0002 states verbatim that \"length and precision qualifiers (VARCHAR(255),\nDECIMAL(p,s)) are dropped\", and `TestPropertyLengthQualifiersDropped`\n(internal/schema/gql/propertytype_test.go:108) pins that with five rows and a\ndoc comment citing the ADR. Adding the field is therefore a REVERSAL of a\nrecorded decision, not a bug fix, and it needs a named consumer to justify it.\nProblem A needs neither.\n\n## Why this bead matters beyond itself\n\nStructurally the same defect family as gqlc-uhb (phrase forms silently dropped)\n-- accepted input, nil error, wrong model -- which is the family the h9n corpus\nepic exists to find. The corpus manifest cannot express it: such a file's outcome\nis `resolves` and the coverage gate is satisfied, because coverage is measured\nfrom the parse tree and the parse tree is correct. Detecting this class needs a\nmodel assertion, not a coverage one. See gqlc-h9n.13.\n","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T21:07:15Z","created_by":"areqag","updated_at":"2026-07-26T17:22:42Z","closed_at":"2026-07-26T17:22:42Z","close_reason":"Fixed in PR #421 (squash 5748e7d), reviewer PASS verified by execution.\n\nINT(8)/INTEGER(8)/UINT(8) and their 16/32/64/128/256 siblings now resolve to the same PropertyType as the suffix spellings INT8/INTEGER8/UINT8. canonicalSpelling used to strip the parenthetical before the typeSpellings lookup, so a width-carrying spelling silently landed on the machine-word constant — accepted input, nil error, wrong model.\n\nVerified by rev-h9n16 at 4ae21a8:\n- Non-vacuity at parent f0e8a77: folds 18/18 FAIL, equivalence 18/18 FAIL, with the stated message 'expected INT8, actual INT' — not a compile error.\n- All 18 fold rows deleted one at a time; every deletion reddens. Zero dead rows.\n- normaliseType's full-then-truncated lookup ORDER is itself pinned — reversing it reddens.\n- 19 adversarial spellings through canonicalSpelling (nested parens, hex and string literals with commas, whitespace-only parens, the decimal-branch NOT NULL): every one either hits an existing fold key or falls through correctly. No new full-canonical key shadows any other typeSpellings entry.\n- I separately confirmed INT(n) and INTEGER(n) map to byte-identical constants at all six widths, so the fold does not introduce a distinction the language lacks. Synonymy is preserved.\n- Merged-state check: master had moved to bb2db4b (#422, same Go package) after this PR's CI ran, so green CI was computed against a base that no longer existed. Test-merged and ran the full suite on the combined tree — 12 packages green, no failures.\n\nFLOAT(p) is deliberately NOT folded, and this is the part worth remembering. I caught an earlier revision of this branch folding FLOAT(16)-\u003eTypeFloat16 and required its removal. FLOAT's parenthetical at GQL.g4:1849 is (precision (COMMA scale)?) — byte-identical in shape to DECIMAL(p,s) at :1832 and structurally unlike the scale-free binary-integer parentheticals at :1801/:1814/:1827 that warrant the fold. Nothing establishes FLOAT's precision counts bits; under a digits reading 16 digits is ~53 bits, so the fold would have been strictly NARROWING — the exact failure mode this epic exists to eliminate. FLOAT(16/24/32/64) and FLOAT(10,2) are now pinned as non-folds resolving to TypeFloat, so the decision is asserted rather than implied by absence. Deferred to gqlc-h9n.28.\n\nThis change is deliberately NOT strictly widening, and the PR body enumerates the cost measured against master: 12 codegen emissions change text, and 6 spellings ({INT,INTEGER,UINT}(128|256)) move from emitting Go 'int' to aborting the whole schema with ErrUnrepresentableWidth. That is correct — the suffix spellings INT128 etc. already abort, so the fold makes the two spellings consistent, and emitting a machine int for a 128-bit column is data loss dressed as success. Reviewer confirmed the abort path by reading codegen/types.go:193 and prepare.go:489.\n\nTen corpusEntry/semanticCase rows in corpus_area_d1_test.go re-pointed from this bead to gqlc-5md, which actually tracks the length-qualifier drop; reviewer confirmed 5md's scope matches the five spellings those rows cite. Zero corpus outcomes change. INT(7) has no counterpart constant and falls back, cited to gqlc-h9n.25.\n\nTwo non-blocking nits recorded rather than fixed: the equivalence-exclusion comment cross-references the NonFolds table instead of citing gqlc-h9n.28 inline (both referenced sites do cite it), and the PR body omits INT(7) vs INT(8) from the regression enumeration though it is the same class as INT(8) vs INT(16). Fold into a future edit if anyone touches the file.\n\nCloses GH #403 (auto-closed on merge).","external_ref":"https://github.com/areqag/gqlc/issues/403","dependencies":[{"issue_id":"gqlc-h9n.16","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-25T17:07:15Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-qpy","title":"schema/gql: corpus size pins block every corpus author","description":"TestCorpusManifest ends with two count pins:\n\n    require.Len(t, entries, wantCorpusEntries, \"corpus size changed; repin wantCorpusEntries\")\n    require.Equal(t, wantCorpusResolving, resolving, \"resolving count changed; repin wantCorpusResolving\")\n\nwantCorpusEntries / wantCorpusResolving are consts in corpus_test.go -- a SHARED file. The\ncorpus authors own only their own corpus_area_\u003cX\u003e_test.go plus .gql files, and the author\nbrief tells all five that \"TestCorpusManifest and TestCorpusOutcomes must pass outright\" as\ntheir done-signal.\n\nThose two facts contradict each other. An author cannot add a single corpus file without\nfailing TestCorpusManifest, and cannot fix it without editing a file they are forbidden to\ntouch. VERIFIED, not reasoned: simulated one author adding one .gql plus one area-file entry\nand touching no shared file, in a scratch copy of the worktree --\n\n    --- FAIL: TestCorpusManifest (0.00s)\n        corpus_test.go:274: corpus: 10 entries, 5 resolving\n        Messages: corpus size changed; repin wantCorpusEntries\n\nTestCorpusOutcomes passed, so it is specifically the count pins.\n\nWHY IT MATTERS MORE THAN THE NOISE. The failure message says \"repin wantCorpusEntries\",\nwhich instructs the author to do the one forbidden thing. Five authors each bumping 9 to\n9+their own count is a five-way conflict on a single line whose correct value none of them\ncan know, in the one file the area split exists to keep uncontended. The alternative\noutcomes are five duplicate messages to team-lead, or an author concluding the harness is\nbroken and stopping.\n\nFIX, prototyped and verified in /tmp/goldencheck, diff at /tmp/lead/patch/corpus_test.go.diff:\nmove both pins out of TestCorpusManifest into a new TestCorpusSize. The two tests answer\nquestions with different lifetimes -- the manifest checks are invariants (every file has an\nentry, every unsupported entry names a real sentinel), while the counts are regression pins\nthat are stale by design the moment anyone adds a file. Verified both directions:\n\n    author adds a file, runs their done-signal:\n        go test -run 'TestCorpusManifest|TestCorpusOutcomes'   -\u003e ok\n    and the regression pin still fires, carrying the repin to team-lead:\n        go test -run TestCorpusSize   -\u003e FAIL  corpus size changed; repin wantCorpusEntries\n\nDo NOT delete the pins instead. wantCorpusResolving is a real guard: an author converting a\nresolves entry to unsupported to make their life easier is a regression the ElementsMatch\nchecks cannot see, and \"a drop is a regression\" is the note that catches it.\n\nMust land BEFORE the five author worktrees are cut, since they branch from that commit.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T13:34:30Z","created_by":"areqag","updated_at":"2026-07-26T05:53:33Z","closed_at":"2026-07-26T05:53:33Z","close_reason":"Landed in PR #392 (squash 6d4ac8f). Corpus size pins no longer block parallel authors.","external_ref":"https://github.com/areqag/gqlc/issues/375","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.7","title":"resolver: label expression must resolve by satisfaction and union, not exact label-set lookup","description":"`internal/resolver/resolve.go:184-187` resolves a query's node label to a schema\nnode type by **exact map lookup** on `LabelSetKey` — the sorted, deduped,\n`\u0026`-joined canonicalisation of the type's *complete* label set. So a pattern can\nonly ever match a node type whose full label set is spelled identically.\n\nThat is wrong. A label expression is satisfied by an element whose label set is a\n**superset**, and it resolves to the **union** of every node type that satisfies\nit.\n\nVerified 2026-07-25:\n\n    CREATE GRAPH TYPE G AS {\n      NODE TYPE Person (p :Person\u0026Employee {id STRING, name STRING, joined_date DATE}),\n      DIRECTED EDGE TYPE K (p)-[:KNOWS {since INT}]-\u003e(p)\n    }\n\n    match (p1:Person)-[r:KNOWS]-\u003e(p2:Person) ... \n\n    internal/user/db/user.cypher: query T: unknown label: Person\n\nA node of type `Person\u0026Employee` **does** carry the label `Person`, so the\npattern must match it. It does not.\n\n## Why this is the most serious defect in the epic\n\nSubclause 16.8 `\u003clabel expression\u003e` carries **no Annex D feature ID**, which per\nNeo4j's conformance appendix means it is **mandatory**, not optional. Every other\nitem in this epic is an optional feature that gqlc may legitimately decline. This\none it may not.\n\nCITED: *Graph Pattern Matching in GQL and SQL/PGQ*, SIGMOD 2022 (arXiv\n2112.06217), authored by the GQL editors — \"`MATCH (x:Account)` … only nodes with\nthe label Account will now be bound to x\"; `\u0026`, `|`, `!`, `%` supported. The\nexpression is evaluated over an element's **data** label set, never against\ndeclared type identities.\n\nIt is also wrong under plain openCypher semantics, where `(p:Person)` likewise\nmeans \"has label Person\", so this is not a GQL-vs-Cypher nuance. The bug is\nlanguage-agnostic.\n\n## Two things are conflated\n\nPer the key-label-set research (see the `gqlc-iso-node-type-identity` bd\nmemory), ISO GQL separates the **key label set** (identity, the part before `=\u003e`)\nfrom the **complete label set** (what conforming data must carry). `LabelSet.Key`\nis a fine canonicalisation of the latter and an illegitimate stand-in for the\nformer. Fixing satisfaction and fixing identity are related but separable: this\nbead is satisfaction only, and is deliberately scoped to leave identity keyed as\nit is today so it can land on its own.\n\n## Resolution must be a union, and a union can be empty or plural\n\n- zero satisfying node types -\u003e the existing unknown-label error, but phrased as\n  \"no node type satisfies\" rather than \"unknown label\"\n- exactly one -\u003e today's behaviour\n- more than one -\u003e a new decision. The properties available on the binding are\n  those common to all members, and a property present in only some members must\n  not type-check. This is the part that needs design, not just a lookup change.\n\nThe plural case is reachable today without any `=\u003e` support: two node types that\nmerely share a label (`(:Person\u0026Employee)` and `(:Person\u0026Contractor)`) are enough.\n","design":"# gqlc-h9n.7 — label satisfaction and the plural case\n\nDesign note. Read-only research at `origin/master` `7518581`. Probes:\n`/tmp/h9n7/scratch/probe{,2,3,4,6}/main.go`. Full output:\n`/tmp/h9n7/probe-output.txt` (every claim below is a line in it).\n\n---\n\n## 0. Three corrections to the brief\n\n**(a) The bug is broader than the bead says.** It is not \"`:Person` misses\n`(:Person\u0026Employee)`\" — *no* proper subset of a declared label set resolves.\n`match (p:Employee) return p.salary` also fails with `unknown label: Employee`.\nSo writing any `NODE TYPE X (:A\u0026B)` makes **both** `A` and `B` unusable as\nquery labels.\n\n**(b) The query spelling is `:A:B`; `\u0026` does not parse.** `match\n(p:Person\u0026Employee)` is a *parse* error (`token recognition error at: '\u0026'`), as\nis `|`. Only openCypher conjunction `:A:B` works. So the query parser has no\nlabel-expression surface at all — just a label *set* — and satisfaction reduces\nto `declared ⊇ queried`, a subset test, not an expression evaluator. The bead\ncites SIGMOD 2022 for `\u0026`/`|`/`!`/`%`; those are Cypher-grammar work, not\nresolver work. **The bead should say so** — \"implement §16.8\" and \"make subset\nmatching work\" are very different sizes, and only the latter is in its\nacceptance criteria.\n\n**(c) The plural design already exists in the tree, on the edge side.** R3\nshipped `edgeCands` / `ResolvedEdgeUnion` / `unionProperty`, and\n`unionProperty` (`resolve.go:731`) implements *exactly* the rule the bead\nproposes as new. Verified: `r.w` (on both, same type) resolves; `r.only` (on\none) → `unknown property: ... missing on union member A-[S]-\u003eB`; `r.skew`\n(`INT` vs `STRING`) → `unknown property: ... type differs across union\nmembers`. The owner's position is therefore not a new commitment; it is the\nalready-shipped edge semantics, and the node side should match it.\n\n---\n\n## 1. What intersection means for generated code (Q1)\n\nSchema: `Employee (e :Person\u0026Employee {id, name, salary INT})` and\n`Contractor (c :Person\u0026Contractor {id, name, agency STRING})`.\n\n**1a. `RETURN p.name` — free.** Intersection collapses to one\n`ResolvedProperty{STRING}` before codegen sees anything. `phaseAAdmit`\n(`prepare.go:571`) and `phaseBDerive` (`:690`) map it to a `*string` row field.\n**Zero codegen change.** The whole implementation is `unionProperty` with\n`[]schema.NodeType` in place of `[]schema.EdgeKey`.\n\n**1b. Same name, different type → error.** Precedent, not preference: the edge\nlane already errors (`resolve.go:744`, `r.skew` above). Narrowing is\nunavailable — `graph.PropertyType` is a bit-width-preserving enum (ADR 0002)\nwith no join. Note the existing check compares `Type` **and** `Nullable`, so\n`STRING` vs `STRING NOT NULL` also errors. Copy it verbatim; do not invent a\nnullability lattice.\n\n**1c. `RETURN p` — the real problem.** `ResolvedNode` carries exactly one\n`LabelSetKey` (`validated.go:89`), and codegen resolves it via\n`entityIndex[{entityNode, t.Labels}]` (`prepare.go:576, :698`) into one struct\nper declared node type. A plural binding has no single key.\n\nThe edge side solves this with a synthesised sealed interface — verified\nemission for `RETURN r` on `[r:R|S]` is `type TR interface{ isTR() }`, marker\nmethods `func (R) isTR() {}`, and a `switch rel.Type` in the query method.\n**Copying that for nodes does not work, and this is the finding I most want\nyou to see.** The edge dispatch is total because `dbtype.Relationship.Type` is\na `string` and a relationship has exactly one type. The node discriminator is\n`dbtype.Node.Labels []string` — a **set** — and under the very\nsuperset-satisfaction rule this bead introduces, one runtime node can satisfy\ntwo declared types at once: a node labelled `{Person, Employee, Contractor}`\nconforms to both `Person\u0026Employee` and `Person\u0026Contractor`. No principled\n`switch` arm exists; \"first match wins\" makes the emitted struct depend on\nslice order.\n\nThat rules out one design, not the plural case. Three options remain:\n\n| | emits | works? | cost |\n|---|---|---|---|\n| **(a) sealed interface** | interface + label dispatch | **no** — ambiguous discriminator | — |\n| **(b) intersection struct** | per-query struct with only common properties | yes, and total — no dispatch needed | a **new codegen entity kind**: naming rule, decode fn, collision check vs schema entity names |\n| **(c) refuse** | a resolver diagnostic | yes | one sentinel + one fixture |\n\nTo be accurate: (b) is the literal reading of your stated rule and it is *not*\nblocked, only expensive. It also silently drops `salary`/`agency` from the row\n— defensible for a property projection, surprising for a whole-entity one.\n\n---\n\n## 2. Intersection or error? (Q2) — recommendation\n\n**Neither as posed. Split on projection shape, not on plurality:** plural +\nproperty → **intersection**; plural + whole-entity → **refuse** (option c),\nleaving (b) as a wideable follow-up.\n\n1. **The brief's \"reject anything plural\" alternative fails its own goal.**\n   `MATCH (p:Person) RETURN p.name` is the most ordinary query in the language.\n   Under blanket rejection it compiles or not depending on whether some *other*\n   type the query never mentions shares a label — a colleague adding `NODE TYPE\n   Contractor` breaks your build. The property path must work.\n2. **The property path costs zero downstream** (§1a).\n3. **Refusing is wideable; shipping (b) is not.** A rejected query later being\n   accepted breaks nobody. Emitting an intersection struct and later preferring\n   a discriminated shape *is* breaking — user code has named the type.\n4. **Shape-specific refusal inside `refProjectionType` is an existing\n   pattern.** `scope.go:698` already refuses *property* projection on a\n   var-length edge while permitting the whole-entity one, with a message naming\n   the alternative route. Same function, same posture, opposite axis.\n5. The bead's acceptance criteria only bind the property case; (c) satisfies\n   the bead as written, (b) exceeds it.\n\n---\n\n## 3. Edges (Q3) — your suspicion is right, for a sharper reason\n\n**3a. The `Label` component: same shape, currently worse than nodes.**\n`EdgeKey.Label` is a full `LabelSetKey` and edge types really can be\nmulti-label (`schema/gql/resolve.go:65`). But **no query spelling reaches\none**: `:KNOWS` misses `FOLLOWS\u0026KNOWS`; `:KNOWS:FOLLOWS` does not parse (Cypher\nedges take `|`, not `:`); `:KNOWS|FOLLOWS` is alternation of two singletons,\nboth missing. `edgeCandidates` (`resolve.go:471`) hardcodes the singleton by\nbuilding `graph.LabelSet{L}.Key()` per label. A multi-label edge type is\n**dead** — a stricter statement than the bead makes, and worth its own bead.\nThe fix itself is the same subset compare as nodes.\n\n**3b. The endpoints: harder, and *forced* by the node fix.** This is worse than\n\"materially harder\" — **you cannot fix nodes and defer edges.**\n`endpointLabels` (`resolve.go:503`) returns exactly one `LabelSetKey` from\n`resolved[v].Labels`. The moment a node binding is plural it has no single key\nto return, and `closeEdge` / `edgeCandidates` / `candidateTypes` /\n`describeTriedEdges` all consume that single-key output. So `scope.nodeTypes`\nmust become a candidate lane mirroring `edgeCands` in the same change.\n\nCounted ripple: 20 `nodeTypes[...]` sites in non-test code. 13 are\npresence-only (`_, ok :=`) and survive untouched. **7 read the value and must\neach decide what plural means**: `scope.go:144` (BindNode's identity compare),\n`:654` (`refProjectionType`), `:807` (`Export`), `:918`\n(`PropertyUseWitness`), `resolve.go:972`/`:1057`/`:1130` (SET/REMOVE/DELETE\nvalidators) — plus `endpointLabels:503`.\n\nConceptually it composes cleanly once nodes expose a set (\"is `Person\u0026Employee`\nin the satisfying set of `{Person}`?\" is the same predicate reused); the cost\nis a cross-product in `edgeCandidates`, which is fine — schemas are small and\nthe code is already naive-iteration by choice (`resolve.go:1166`).\n\n---\n\n## 4. Blast radius (Q4)\n\n**Small, and nothing pins the bug.**\n\n- **No resolver fixture schema uses a multi-label node type** — `grep -l \"\u0026\"\n  test/data/resolver/*/schemas/*.gql` is empty across all 8 schemas. No valid\n  golden changes; no invalid fixture flips.\n- **All six `ErrUnknownLabel` fixtures stay red for the right reason** —\n  `unknown_label`, `create_unknown_label`, `merge_endpoint_unknown_label`,\n  `union_unknown_label_branch`, `set_labels_undeclared`,\n  `remove_labels_undeclared` all name a label declared nowhere, which satisfies\n  nothing under the new rule too. **None pins the bug.** Only their *messages*\n  move, and the string-level assertions are\n  `internal/cli/pipeline/pipeline_test.go:308,442,444,445,478` and\n  `internal/cli/generate_test.go:321` (`\"unknown label: Ghost\"`).\n- **`TestSentinelReachability`** (`resolver_test.go:285`) plus `TestInvalid`'s\n  two totality assertions (`:262-263`) mean a new sentinel needs a fixture *and*\n  entries in `invalidFixtures` and `schema.mapping.json`. That is the gate.\n- **Codegen already refuses part of the space:** `ErrUnnamedMultiLabelType`\n  (`prepare.go:408`) rejects *unnamed* multi-label node types, so every plural\n  member is guaranteed an explicit name — which is what makes (b) namable.\n- **A ready-made plural fixture already exists**:\n  `test/data/schema/gql/valid/node_types_only.gql` declares both `(:Person …)`\n  and `(:Person\u0026Employee …)`. Schema-parse-only today; the obvious donor.\n\n**Anything asserting the wrong thing?** No test pins the bug. The closest wrong\nassertion is documentation: `errors.go:12-15` describes `ErrUnknownLabel` as\nfiring when a label set \"does not resolve to a declared node type\" — exact\nlookup written up as if intended. That comment must change with the behaviour.\n\n---\n\n## 5. One error or two? (Q5)\n\n**One sentinel for the two label diagnostics; a new one for the plural\nrefusal.**\n\n- `ErrUnknownLabel` is *already* overloaded across five fail-sites (its own\n  docstring enumerates them). The house discipline is clearly not one sentinel\n  per message.\n- The in-file precedent is `ErrUnknownProperty`, which distinguishes `\"missing\n  on union member\"` from `\"type differs across union members\"`\n  (`resolve.go:737, :745`) — two faults, one sentinel, message carries it.\n- `TestSentinelReachability` prices each new sentinel at a fixture plus two map\n  entries. Worth it when a caller branches; none does here — the CLI formats\n  the message (`pipeline_test.go` asserts strings, not `errors.Is`).\n- The branch predicate **already exists**: `labelDeclared` (`resolve.go:1168`),\n  R6's per-label-component check. Note the latent inconsistency it exposes —\n  R6's SET/REMOVE label validation is already satisfaction-shaped while Phase\n  A1's is exact-key-shaped. They should agree after this bead.\n\n```\nsome label undeclared → \"unknown label: %s is not declared on any node type\"\nall declared, none satisfies → \"no node type satisfies %s; declared types carrying these labels: \u003clist\u003e\"\n```\n\nThe plural refusal is a different fault with a different fix, needs a fixture\nanyway, and overloading it onto `ErrUnknownLabel` would make that sentinel mean\nboth \"too few\" and \"too many\". `ErrAmbiguousBinding` is tempting but its\ndocumented meaning is *inference* failure at Phase B; this is a\n*representation* failure at the projection walk. New sentinel.\n\n---\n\n## 6. Does this need an ADR? (Q6)\n\n**Yes — for the plural decision, not the lookup fix.** The lookup fix is a bug\nfix; bead plus spec note suffices. The plural decision picks a permanent shape\nfor the type model that, per ADR 0010, leaks into generated code users name.\nADR 0006's core argument — \"the conservative default is safe because relaxing\nlater is non-breaking and the reverse is not\" — decides this one too, which is\na good sign it belongs in the same series. 0006 is 64 lines; this needn't be\nlong.\n\n\u003e A query label expression resolves to the **set** of declared node types whose\n\u003e label set is a superset of it. Where that set is plural, a **property**\n\u003e reference resolves to the intersection — identical name, type and nullability\n\u003e across every member, else `ErrUnknownProperty` — reusing the rule R3 already\n\u003e applies to multi-candidate edges; a **whole-entity** reference is\n\u003e **rejected**, because the generated decode would have to discriminate on\n\u003e `dbtype.Node.Labels`, a set, and one node can conform to two declared types at\n\u003e once, so no total dispatch exists.\n\u003e\n\u003e Rejection is chosen over synthesising a per-query intersection struct because\n\u003e widening a rejected query to an accepted one is non-breaking, while replacing\n\u003e an emitted struct later is not.\n\n---\n\n## 7. What I would do next\n\n1. **Bead 1 (small, lands now).** Subset matching, singular results only: 1 →\n   today's behaviour; 0 → reworded diagnostic; ≥2 → new sentinel, refuse.\n   **Fixes the bead's verbatim repro.** Touches `resolve.go:184-188`, errors,\n   fixtures. No representation change, no edge change, no codegen change.\n2. **Bead 2.** Plural property intersection. Pluralises `scope.nodeTypes`,\n   drags `endpointLabels` and the 7 value-reading sites. The ADR lands here.\n3. **Bead 3 (new, found here).** Multi-label edge types are unreachable from\n   any query spelling (§3a). Not covered by gqlc-h9n.7 as written.\n","acceptance_criteria":"A query label matches every node type whose label set contains it, so MATCH (p:Person) resolves against a node type declared (:Person\u0026Employee).\nZero satisfying node types produces an error phrased as 'no node type satisfies', naming the expression and the declared types.\nExactly one satisfying node type behaves as today.\nMore than one satisfying node type has a designed, documented and tested outcome; a property present on only some members does not type-check.\nEdge label resolution gets the same treatment as node label resolution.\nThe plural case is covered by a fixture using two node types that share a label, with no =\u003e support required.","notes":"\n## Design landed; scope narrowed to step 1 (team-lead decision, 2026-07-26)\n\nFull design note attached as the issue's design field. Read it before starting;\nit is dense and every claim in it was probed, not reasoned. Headlines:\n\n- **The bug is broader than this bead said.** It is not \"`:Person` cannot match\n  `(:Person\u0026Employee)`\". *No proper subset of any declared label set resolves.*\n  Writing `NODE TYPE X (:A\u0026B)` makes both `A` and `B` unusable as query labels.\n- **Scope correction: `|`, `!`, `%` are NOT in scope.** The query parser accepts\n  conjunction only, spelled openCypher-style `:A:B` — `:A\u0026B` is a lexer error.\n  So satisfaction reduces to a subset test, `declared ⊇ queried`, not a label\n  *expression* evaluator. \"Implement §16.8 label expressions\" and \"make subset\n  matching work\" are very different sizes and only the latter is wanted here.\n- **Intersection semantics are already shipped, on the edge side.**\n  `unionProperty` (`resolve.go:731`) implements the exact rule this bead\n  proposes as new. Reuse it verbatim; \"common\" means same name **and** type\n  **and** nullability (`resolve.go:744`), which is already enforced there. Do\n  not invent a nullability lattice.\n- **You cannot fix nodes and defer edges.** `endpointLabels` (`resolve.go:503`)\n  returns exactly one `LabelSetKey`; the moment a node binding can be plural it\n  has no single key to return, and `closeEdge` / `edgeCandidates` /\n  `candidateTypes` / `describeTriedEdges` all consume that. 20 `nodeTypes[...]`\n  sites, 13 presence-only and survive, **7 must decide what plural means**.\n  That ripple is why this bead splits.\n\n### This bead is now step 1 only\n\nSubset matching that **stays singular**. Compute the satisfying set at\n`resolve.go:184-188`; 1 → today's behaviour; 0 → reworded diagnostic; \u003e=2 →\nrefuse with a new sentinel. No representation change, no edge change, no\ncodegen change, no ADR. It fixes this bead's verbatim repro.\n\n### One correction to the design, and it is load-bearing\n\nThe design's step 1 as written **ships a regression**, and the fixture that\nproves it is already in the tree. `test/data/schema/gql/valid/node_types_only.gql`\ndeclares *both* `(:Person {...})` and `(:Person\u0026Employee {...})`. So for the\nquery `MATCH (p:Person)` the satisfying set is **{Person, Person\u0026Employee} —\nsize 2** — and \"≥2 → refuse\" would fail a query that resolves fine today via\nthe exact-key lookup at `s.Nodes[key]`.\n\n**Decision: an exact label-set match wins outright.** Resolution order is\n(1) exact match on the declared key → done, plural test never runs;\n(2) otherwise the proper-superset satisfying set, with the 1/0/\u003e=2 rule above.\nThat is the ordinary most-specific-declaration-wins rule and it makes step 1\nstrictly widening — no query that compiles today stops compiling.\n\nA regression test built on exactly this shape (a bare `(:Person)` alongside a\n`(:Person\u0026Employee)`, querying `:Person`) is **required**, not optional. It is\nthe one case where the naive reading of \"satisfaction\" breaks a working query.\n\nNoted for step 2, not step 1: a runtime node labelled `{Person, Employee}` does\nmatch the pattern `(:Person)` and will be decoded as the `Person` struct,\ndropping `employeeId`/`title`. That unsoundness exists today and step 1 neither\nadds nor removes it. It belongs in the step-2 ADR.\n\n## Correction to my own note above (team-lead, 2026-07-26)\n\nMy \"Decision: an exact label-set match wins outright\" stands, and the\nimplementation follows it. But **my stated justification was partly wrong** and\nshould not be carried forward.\n\nI wrote that \"\u003e=2 -\u003e refuse would fail a query that resolves fine today\" and\nthat \"the fixture proving it is already in the tree\". The behavioural claim is\nright; the *fixture* claim is not. `test/data/schema/gql/valid/node_types_only.gql`\nis **schema-parse-only** — no resolver query runs against it — and no schema\nunder `test/data/resolver/{valid,invalid}/schemas/` declares `\u0026` at all. So no\nexisting test would have gone red. The naive rule would have shipped a silent\nbehavioural regression with a green suite, which is worse than what I described,\nnot better.\n\nThe correct and stronger justification: exact precedence **preserves observable\nbehaviour exactly**, making step 1 strictly widening. Verified by probe against\nthat schema (declared keys `[Company Employee\u0026Person Person Tag]`):\n\n    match (p:Person) return p.email          -\u003e OK, STRING nullable\n    match (p:Person:Employee) return p.title -\u003e OK\n    match (p:Employee) return p.title        -\u003e unknown label: Employee   \u003c- the bug\n\nSo precedence is the status quo, not a new invention.\n\nRecorded because a tracker entry with a false reason is the same defect this\nepic exists to remove, and because the difference matters for step 2: see\ngqlc-h9n.22, where exact precedence must be treated as a provisional tie-break\nthat step 2 reopens, not as settled satisfaction semantics.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:36:24Z","created_by":"areqag","updated_at":"2026-07-26T06:44:16Z","closed_at":"2026-07-26T06:44:16Z","close_reason":"Step 1 landed in PR #394 (squash-merged 2026-07-26). resolveNodeLabels replaces exact LabelSetKey lookup with satisfaction: exact match wins, then proper-superset satisfying set of size 1 resolves, 0 -\u003e ErrUnknownLabel with split diagnostics, \u003e=2 -\u003e new ErrAmbiguousLabel sentinel. Strictly widening; no representation, edge, or codegen change. Reviewer mutation-tested every guard. Steps 2 and 3 continue as gqlc-h9n.22 (plural satisfaction + property intersection + ADR on exact-match precedence) and gqlc-h9n.23 (multi-label edge types, dead today because endpointLabels returns a single key).","external_ref":"https://github.com/areqag/gqlc/issues/356","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.7","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:36:24Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.2","title":"test: ISO GQL graph-type schema corpus + coverage gate (foundation for the epic)","description":"Owner direction (2026-07-25): *\"first build our own huge dataset of valid ISO GQL\nschema files and then merge that in and incrementally increase coverage to 100%\nover that as a test suite. this follows a /tdd flow.\"*\n\nThis bead lands the dataset and the gate. It is the **first** bead in the epic:\nevery sibling then becomes \"move these N corpus entries from unsupported to\nsupported\", which is a red-green cycle with a pre-written red.\n\n## Why a corpus rather than per-bug tests\n\nThree separate silent-drop/silent-reinterpret defects (gqlc-uhb, gqlc-h9n.1,\ngqlc-h9n.3) share one shape: a grammar alternative the listener neither handles\nnor rejects. Per-bug regression tests would have caught none of them, because\nnobody knew to write them. A corpus enumerated from the grammar rather than from\nknown bugs is what makes the unknown-unknowns visible, and a coverage number is\nwhat makes progress against them legible.\n\n## Build on the existing harness, do not replace it\n\n`internal/schema/gql/parser_test.go` already has the right skeleton:\n\n- `test/data/schema/gql/valid/*.gql` + paired `.golden.json`, regenerated with\n  `-update` (parser_test.go:41-65)\n- `test/data/schema/gql/invalid/*.gql` + the `invalidFixtures` map pinning each to\n  a sentinel (parser_test.go:234-272)\n- `TestSentinelReachability` (parser_test.go:295) — a bidirectional sweep proving\n  every sentinel has a fixture and every fixture maps to a canonical sentinel\n\nThe corpus is a scaled-up third category plus one new invariant, reusing all of\nthe above.\n\n## Corpus shape\n\nOne file per grammar alternative or semantic case, systematically enumerated from\nGQL.g4 §18.1-18.10 (`nestedGraphTypeSpecification` and everything reachable from\nit) and §12.6 (`graphTypeSource`). Minimal files — each isolates one construct, so\na failure names the construct.\n\nEvery entry carries a manifest record. Proposed fields:\n\n- **outcome** — `resolves` (a golden exists) or `unsupported` (a sentinel is\n  named). No third value, and no entry may be absent from the manifest.\n- **feature** — the Annex D feature ID where one applies (GG01-GG05, GG20-GG26,\n  GH02, GV50, GC01-GC05), or `mandatory` for constructs with no ID. See the\n  `iso-gql-annex-d-optional-features` bd memory.\n- **bead** — for `unsupported` entries, the bead that will make it resolve, or an\n  explicit `wontfix` pointing at the ADR section justifying the refusal.\n- **reason** — one line, for `unsupported` only.\n\nKeep the manifest in Go beside the test rather than in a data file, so a stale\nentry is a compile error and the sentinel is a real `error` value, not a string\nto be matched.\n\n## The invariant that closes the hole\n\nBeyond the existing sentinel sweep, add: **no corpus entry may parse successfully\ninto an empty or partial schema.** Concretely, for every entry the parse must\neither\n\n- return an error that `errors.Is` the manifest's named sentinel, or\n- return a schema equal to the golden,\n\nand **never** return a nil error with a schema that has fewer element types than\nthe source declares. That last clause is the direct guard against silent drop, and\nit is what all three known defects would have failed.\n\nAssert the manifest is exhaustive in both directions, mirroring\n`TestSentinelReachability`: a corpus file with no manifest entry fails, and a\nmanifest entry with no corpus file fails. Without that, adding a `.gql` file\nwithout classifying it re-opens the hole the corpus exists to close.\n\n## Coverage as a number\n\nReport supported / total, and pin the current count with a constant that must be\nupdated deliberately. A change that moves an entry from `unsupported` to\n`resolves` must bump it; a change that regresses one fails. That makes \"increase\ncoverage to 100%\" a mechanically checkable claim rather than a narrative one, and\ngives each sibling bead an objective done condition.\n\n## Derived from the grammar, not hand-listed\n\nA hand-maintained list drifts exactly the way the listener did. The enumeration\nmust be checkable against the `.g4` — either generated from it, or verified by a\ntest that walks the generated parser's rule/alternative metadata and asserts every\nalternative reachable from `nestedGraphTypeSpecification` and `graphTypeSource`\nappears in the manifest. The second is likely cheaper and is sufficient: it makes\na *new* grammar alternative fail the build until classified.\n\n## Scope boundary\n\nGraph-type DDL only. The query-side corpus is the vendored openCypher TCK\n(`test/data/query/cypher/tck`, `just fetch-tck`) and is out of scope here, with one\nexception already known to matter: label-expression satisfaction (gqlc-h9n.7) is\nwhere a schema corpus and a query corpus meet, so the corpus must include\nmulti-label node types even though nothing in DDL alone exercises them.\n","acceptance_criteria":"A corpus under test/data/schema/gql enumerates one minimal file per grammar alternative reachable from nestedGraphTypeSpecification and graphTypeSource, plus the semantic cases each alternative admits.\nEvery corpus file has a manifest record declaring outcome (resolves|unsupported), Annex D feature ID or 'mandatory', the bead that will fix it or an explicit wontfix, and a one-line reason.\nThe manifest is exhaustive in both directions: a corpus file without a manifest entry fails, and a manifest entry without a file fails.\nNo corpus entry may parse to a nil error with fewer element types than its source declares — the direct silent-drop guard.\nAll three known defects (gqlc-uhb, gqlc-h9n.1, gqlc-h9n.3) are represented and fail before their fixes land.\nA test derived from the generated parser's rule metadata fails when a grammar alternative reachable from the graph-type rules has no manifest entry.\nCoverage is reported as supported/total and pinned by a constant that must be updated deliberately; a regression fails.\nThe corpus lands green — everything unsupported is classified, nothing is silently skipped.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:33:16Z","created_by":"areqag","updated_at":"2026-07-26T05:53:32Z","started_at":"2026-07-25T03:47:42Z","closed_at":"2026-07-26T05:53:32Z","close_reason":"Landed in PR #392 (squash 6d4ac8f). 86-file ISO GQL graph-schema corpus plus the three-set coverage gate: rules 133/133, tokens 133/133, alternatives 46/46 with connectorUndirected#1 exempt as unreachable under ALL(*).","external_ref":"https://github.com/areqag/gqlc/issues/357","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.2","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:33:15Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f9768-e6a4-7e19-8c88-0930deb44b62","issue_id":"gqlc-h9n.2","author":"areqag","text":"# gqlc-h9n.2 — ISO GQL graph-type schema corpus + coverage gate\n\n## Why this exists\n\n`gqlc` generates typed Go from an ISO GQL graph-type schema. Triage on 2026-07-24/25\nfound three defects with one shape: a **grammar alternative the listener neither\nhandles nor rejects**. The file parses, the walk collects nothing, `resolve()`\nreturns an empty schema with a **nil error**, and the eventual diagnostic lands on\nthe user's *query* file (`unknown label: Person`). The user hit exactly that while\ndogfooding.\n\nKnown instances: phrase-form element type declarations (`gqlc-uhb`), `COPY OF`\ngraph type source (`gqlc-h9n.1`), and `UNDIRECTED` edge kind silently reinterpreted\nas directed (`gqlc-h9n.3`).\n\nPer-bug regression tests would have caught **none** of them, because nobody knew to\nwrite them. Owner direction, 2026-07-25:\n\n\u003e \"first build our own huge dataset of valid ISO GQL schema files and then merge\n\u003e that in and incrementally increase coverage to 100% over that as a test suite.\n\u003e this follows a /tdd flow.\"\n\nand, 2026-07-24:\n\n\u003e \"we should be able to accept any valid ISO GQL. we should not have our own GQLC\n\u003e flavour of ISO GQL.\"\n\nSo this bead lands **a corpus enumerated from the grammar, not from known bugs**,\nplus a gate that makes an unclassified grammar branch a build failure. Every sibling\nbead in the epic then reduces to \"move these N corpus entries from `unsupported` to\n`resolves`\" — a red-green cycle whose red is already written.\n\n## Working location\n\nWorktree: `/home/antranig/Developer/gqlc/gqlc-h9n2-corpus`\nBranch: `gqlc-h9n2-schema-corpus` (off `origin/master` at `e510f4b`)\n\nDo **all** work there. Never touch `/home/antranig/Developer/gqlc/gqlc` — that\ncheckout is shared and other sessions switch its branch without warning.\n\n## What already exists (build on it, do not replace it)\n\n`internal/schema/gql/parser_test.go`:\n\n- `fixtureDir = \"../../../test/data/schema/gql\"`\n- `TestValid` (41-65) globs `valid/*.gql`, compares `.golden.json`, `-update` regenerates\n- `TestInvalid` (234-253) + `invalidFixtures` (258-272) pin 13 files to sentinels\n- `TestSentinelReachability` (295-314) — bidirectional sweep: every sentinel has a\n  fixture, every fixture maps to a canonical sentinel\n\nThe corpus is a **third category** plus new invariants, reusing this style. Leave\n`valid/`, `invalid/`, and the existing tests alone.\n\n`internal/schema/gql/parser.go` shows the parse setup: `gen.NewGQLLexer` →\n`antlr.NewCommonTokenStream` → `gen.NewGQLParser` → `gp.GqlProgram()`. `Parse()`\ndoes **not** expose the tree; the corpus test does its own parse, which is\ndeliberate — the coverage measurement must be independent of the listener under test.\n\nSchema model (`internal/schema/schema.go`): `Schema{Name string, Nodes\nmap[graph.LabelSetKey]NodeType, Edges map[EdgeKey]EdgeType}`. Note both are **maps\nkeyed by identity**, so two identical declarations collapse.\n\n## Design — decided, not up for redesign\n\n### 1. Corpus layout\n\n```\ntest/data/schema/gql/corpus/\u003cclause\u003e/\u003cname\u003e.gql\n```\n\n`\u003cclause\u003e` from the section comments in `internal/grammar/gql/GQL.g4`:\n`12.6-graph-type-statement`, `17-references`, `18.1-nested-graph-type`,\n`18.2-node-type`, `18.3-edge-type`, `18.4-label-set`, `18.5-property-types`,\n`18.6-property-type`, `18.7-property-value-type`, `18.8-binding-table-type`,\n`18.9-value-type`, `18.10-field-type`.\n\n**Every corpus file is syntactically valid ISO GQL.** A corpus file that produces a\nsyntax error is a test failure, full stop — the corpus is \"valid ISO GQL we do or do\nnot yet support\", so the two outcomes are about the *semantic* stage only. Syntax\nnegatives stay in the existing `invalid/` dir.\n\nFiles are **minimal**: one construct each, so a failure names the construct.\nNo goldens by default (see §5).\n\n### 2. Manifest — in Go, beside the test\n\n`internal/schema/gql/corpus_test.go`, package `gql`, so sentinels are real `error`\nvalues and a stale entry is a compile error.\n\n```go\ntype corpusEntry struct {\n    file     string // path under corpus/, e.g. \"18.2-node-type/phrase_bare.gql\"\n    outcome  outcome // resolves | unsupported\n    sentinel error  // required iff outcome == unsupported, else nil\n    feature  string // Annex D ID (GG01.., GH02, GV50, GC01..) or \"mandatory\"\n    bead     string // unsupported only: the bead that will fix it, or \"wontfix\"\n    reason   string // unsupported only: one line\n}\n```\n\n`outcome` is a defined type with exactly two values. No third value, no default.\n\nThere is **no `covers` field**. Coverage is *measured* from the parse tree (§3), not\ndeclared — a declared list drifts exactly the way the listener did.\n\n**Bidirectional exhaustiveness**, mirroring `TestSentinelReachability`: a `.gql` file\nunder `corpus/` with no manifest entry fails; a manifest entry with no file fails.\nWithout this, dropping in a file without classifying it re-opens the hole.\n\n### 3. The coverage invariant\n\nTwo mechanically derived obligation sets, both required to be fully covered:\n\n**(a) Reachable parser rules.** Scan `internal/grammar/gql/GQL.g4` (relative path\nfrom the test, same as `fixtureDir`). Build rule → referenced-rules. Compute the\ntransitive closure from roots:\n\n```\ncreateGraphTypeStatement, graphTypeSource, nestedGraphTypeSpecification\n```\n\n**Frontier cuts** — stop descending into these, but the rule itself is still\nrequired to be covered:\n\n| Rule | Why cut |\n|---|---|\n| `graphExpression` | `graphTypeLikeGraph : LIKE graphExpression` drags in the entire query/expression grammar (545 of 574 rules). Query surface is out of scope; it has its own corpus, the vendored openCypher TCK. |\n| `nonReservedWords` | `regularIdentifier : REGULAR_IDENTIFIER \\| nonReservedWords` — ~200 keyword tokens usable as identifiers. Identifier lexis, not a schema construct. One corpus file using a non-reserved word as an identifier discharges it. |\n\nBecause a frontier rule must still be *entered* by some corpus file, a cut that\nstops being reachable becomes a coverage failure rather than dead weight. Do not add\na third cut without messaging team-lead with evidence.\n\n**Ground truth, already computed** (verify it, do not take it on faith): with those\ntwo roots plus `createGraphTypeStatement` and those two cuts, the closure is\n**133 rules** and **133 tokens**.\n\n**(b) Reachable tokens.** Every token name referenced in the body of a\n**non-frontier** reachable rule. 133 of them, all real schema keywords or\npunctuation — `DIRECTED`, `UNDIRECTED`, `IMPLIES`, `COPY`, `LIKE`, `BYTES`,\n`ZONED`, `VARBINARY`, `RECORD`, `NOTHING`, `SUBSTITUTED_PARAMETER_REFERENCE`, …\n\n**Measurement.** Walk each corpus file's parse tree with a custom\n`antlr.ParseTreeListener` recording `ctx.GetRuleIndex()` in `EnterEveryRule` and\n`node.GetSymbol().GetTokenType()` in `VisitTerminal`. Map indices to names via the\nparser's `RuleNames` / `SymbolicNames`. Union across all corpus files = covered set.\nThis is generic — no per-rule code, and it cannot drift from the grammar.\n\n**The gate.** `required \\ covered` must be empty, for both rules and tokens. The\nfailure message lists what is uncovered, so it doubles as the authoring worklist.\n\n**No exemptions in this landing.** If you believe a token or rule is genuinely\nunreachable inside a valid `CREATE GRAPH TYPE`, message team-lead with the grammar\npath that proves it. Do not invent an allowlist — that is the exact mechanism by\nwhich this gate would rot.\n\nScanner details that matter: strip `//` and `/* */` comments; strip single-quoted\nliterals; strip `# AltLabel` alternative labels; strip element labels (`name=rule`);\nlowercase-initial identifiers are rule refs, uppercase-initial are tokens. Rule\nblocks are `^name$` followed by body lines up to a lone `;`. A working Python\nprototype of exactly this lives at `/tmp/reach.py` — read it, it is 50 lines and it\nproduced the 133/133 numbers. Port it, do not re-derive it.\n\n### 4. Silent-drop guard\n\nThe direct assertion against the defect class that motivated the bead. For every\n`resolves` entry: count `elementTypeSpecification` rule contexts in the tree (same\nwalk, independent of the listener) and require\n\n```\nlen(s.Nodes) + len(s.Edges) == that count\n```\n\nExact equality, not `\u003e=`. Minimal one-construct files declare distinct identities so\nthey never collapse; if you find yourself wanting an exception, the file is wrong.\nNote an edge needs its endpoint node types declared too, so an edge file is normally\n3 element type specifications → 2 nodes + 1 edge.\n\nFor every `unsupported` entry: `Parse` must return the named sentinel with\n`errors.Is`. Silence is impossible in either direction.\n\n### 5. Coverage as a pinned number\n\n```go\nconst (\n    wantCorpusEntries   = N\n    wantCorpusResolving = M\n)\n```\n\nExact equality, both. A change that implements a construct must bump\n`wantCorpusResolving`; a regression fails. `t.Logf` the ratio so the number is\nvisible in CI output. That turns \"increase coverage to 100%\" into a mechanically\ncheckable claim and gives every sibling bead an objective done condition.\n\n### 6. Known limitation — state it, do not paper over it\n\nThe corpus catches **silent drop** (class 1) and **undocumented rejection**\n(class 3). It does **not** catch **silent reinterpretation** (class 2): `UNDIRECTED\nEDGE ... CONNECTING (a TO b)` resolves, the element count matches, and the edge is\nstill recorded as directed. Optional per-entry goldens are the mitigation — add a\n`golden bool` field, and for the handful of entries where the *mapping* is the point\n(the `gqlc-h9n.3` undirected entry especially) generate a golden that pins today's\n**wrong** output, with the `reason` line saying so explicitly and naming the bead\nthat must change it. A golden capturing a known defect is a good thing here: the\nfix's diff then shows the semantic change in review.\n\nDo not silently rely on goldens for correctness of constructs; that is per-bead work.\n\n## Phases (TDD — red before green)\n\n**Phase 1 — harness, red.** Everything in §2/§3/§4/§5 except the corpus itself. Seed\nthe corpus with a handful of files (you may copy the shapes from the existing\n`valid/` fixtures). The coverage test **must fail**, printing the full uncovered\nrule and token lists. Save that list to `/tmp/h9n2-uncovered.txt` — it is the\nauthoring worklist for phase 2. Everything else must pass.\n\n**Phase 2 — corpus, green.** Authored by parallel corpus-author agents, area by\narea, each adding `.gql` files plus a Go file declaring\n`var corpusArea\u003cX\u003e = []corpusEntry{...}` which the main manifest concatenates. Keep\nthe per-area split so parallel authors never touch the same Go file.\n\n**Phase 3 — integrate.** Pin the constants, final `just test`, `just lint`,\n`just fmt-check`. Note in `internal/schema/gql/corpus_test.go`'s package-level doc\ncomment what the corpus is and what the two coverage numbers mean.\n\nPhase 1 is yours (carmack). Do it now, get it reviewed, then team-lead fans out\nphase 2.\n\n## Non-negotiables\n\n- **Test-first.** Red, then green. The uncovered list *is* the red.\n- **Independently mergeable.** If only this PR landed and every sibling bead were\n  deferred, `just test` must still pass. So the corpus lands **green**: everything\n  not yet supported is classified `unsupported` with a sentinel and a bead.\n- **`bd` for task tracking**, never TodoWrite / markdown TODO lists. Do not run\n  `bd edit` (opens `$EDITOR`, blocks).\n- **Comments state non-obvious constraints only.** No aspirational or justificatory\n  commentary, no restating what the code does. Look at the existing comments in\n  `schema.go` and `parser.go` for the bar.\n- **No AI attribution.** No `Co-Authored-By: Claude` trailer (a commit-msg hook\n  rejects it), no \"Generated with Claude Code\" footer.\n- **Never skip hooks** (`--no-verify`, `--no-gpg-sign`).\n- Scratch probes go in `/tmp`, never a transient `_test.go` in the tree.\n- Quality gates: `just test`, `just lint`, `just fmt-check`, `just tidy-check`.\n","created_at":"2026-07-25T03:54:22Z"},{"id":"019f9778-883a-7343-9725-b5633961219d","issue_id":"gqlc-h9n.2","author":"areqag","text":"Phase 1 (harness) committed on gqlc-h9n2-schema-corpus @ 4acdfab. Obligation\nverified in Go: 133 reachable parser rules, 133 tokens, cross-checked against\nthe generated parser's tables. Seed corpus of 5 files covers 54/133 rules and\n23/133 tokens. TestCorpusGrammarCoverage is the only red; its message is the\nphase-2 worklist (snapshot: /tmp/h9n2-uncovered.txt).\n\nPhase-2 blocker found empirically, needs a scoping decision:\n\n- COPY OF (copyOfGraphType) parses as createGraphTypeStatement and Parse\n  returns an empty schema with a nil error (this bead's sibling gqlc-h9n.1).\n- Node/edge phrase forms (nodeTypePhrase, edgeTypePhrase) parse and are\n  dropped: 1 element type in the source, 0 in the model (gqlc-uhb).\n\nNeither can be classified green: not `resolves` (empty/short model), and not\n`unsupported` (no sentinel exists). So phase 2 is not purely additive test\ndata — landing the corpus green needs rejection sentinels for silently-dropped\nconstructs, or the sibling beads first. A new sentinel also needs an entry in\nallSentinels and a fixture in invalid/, since TestSentinelReachability sweeps\nboth directions.\n\nNote the harness cannot be pushed while red: .githooks/pre-push runs `just test`.","created_at":"2026-07-25T04:11:27Z"},{"id":"019f9783-0b3e-77af-9462-11486e28c2be","issue_id":"gqlc-h9n.2","author":"areqag","text":"Phase 1 review round 1 (linus audit) — items 2 and 4 implemented, commit d6a48a6.\n\nItem 2 (real bug in 4acdfab): the silent-drop guard counted every elementTypeSpecification context regardless of nesting depth. closedGraphReferenceValueType (GQL.g4:1926) admits a whole nestedGraphTypeSpecification as a property value type, so the rule recurs. Verified: '(:A { p :: GRAPH { (:B) } })' counted 2 for one node type, and 4 with an edge nested inside — the guard would have failed a correct implementation. Now counts only contexts one nested specification deep, with TestCoverageElementCount pinning the instrument (7 cases incl. twice-nested).\n\nItem 3 reconciled: rather than forbidding zero-element sources outright, the guard reads off the same walk whether the source declares elements inline. COPY OF and LIKE do not, so they must resolve to a NON-EMPTY model; inline sources get the exact count equality. This closes the vacuous 0 == 0 hole and still survives the day COPY OF is implemented.\n\nItem 4 (documented + made self-detecting): 'CREATE GRAPH TYPE t AS COPY OF other' has zero syntax errors but enters createGraphTypeStatement zero times — graphSource : AS COPY OF graphExpression (GQL.g4:331) matches it as createGraphStatement (GQL.g4:313). Rather than only documenting it, measureCoverage now requires every corpus file to enter createGraphTypeStatement, so a file that parses as some other statement fails loudly instead of passing every assertion while exercising nothing.\n\nCorrection to the audit: 'COPY OF $src' was listed as verified working; it is a SYNTAX ERROR. SUBSTITUTED_PARAMETER_REFERENCE is DOUBLE_DOLLAR_SIGN PARAMETER_NAME (GQL.g4:3604), so the spelling is $$src. '$src' lexes as GENERAL_PARAMETER_REFERENCE. Matters because that token is one of the 133 and referenceParameterSpecification (GQL.g4:1477) is its only path.\n\nItem 5 (alternative obligation set, 17 uncoverable grammar alternatives) routed to team-lead as a scope call — it expands the phase-1 contract the parallel phase-2 authors build against.\n\nState: obligation 133 rules / 133 tokens unchanged; coverage 54/133 rules, 23/133 tokens; TestCorpusGrammarCoverage still the only red; lint/fmt-check/tidy-check clean; verified under -shuffle=on.","created_at":"2026-07-25T04:22:56Z"},{"id":"019f9789-04bc-74c7-95ae-a0bec9ac7c69","issue_id":"gqlc-h9n.2","author":"areqag","text":"Phase 2 fan-out plan + mechanically derived ground truth (team-lead, 2026-07-25).\n\nThree obligation sets, all derived from internal/grammar/gql/GQL.g4 by scripts kept in /tmp so the numbers can be reproduced rather than trusted:\n\nRULES/TOKENS (/tmp/reach.py graphExpression nonReservedWords)\n  133 rules, 133 tokens reachable from roots createGraphTypeStatement,\n  graphTypeSource, nestedGraphTypeSpecification with two frontier cuts.\n\nFLAGGED ALTERNATIVES (/tmp/flagged.py graphExpression nonReservedWords)\n  wantFlaggedAlternatives = 17, over 131 reachable non-frontier rules.\n  Derived twice by independent routes (hand enumeration in Amendment A5, then this\n  script) and agreed, so the number is mechanical rather than tuned. An alternative\n  is flagged when its symbol set is a subset of the union of its siblings', i.e.\n  rule+token coverage cannot tell whether any file ever took it.\n  catalogObjectParentReference#2; nodeTypeFiller#2; nodeTypeImpliedContent#1,#2,#3;\n  nodeTypePhraseFiller#2; edgeTypeFiller#2; edgeTypeImpliedContent#1,#2,#3;\n  edgeTypePhraseFiller#2; valueType#3,#4,#5,#7,#9,#10.\n\nFRONTIER ELISION (/tmp/elided.py)\n  wantElidedRules = 412, wantElidedTokens = 191.\n  Marginals: graphExpression alone erases 412 rules / 186 tokens; nonReservedWords\n  alone erases 0 rules / 30 tokens. With no cuts the closure is 545 of the\n  grammar's 574 parser rules, which is what justifies the graphExpression cut.\n  The identifier-lexis cut is cheap: nonReservedWords references ~200 keyword\n  tokens but only 30 are reachable nowhere else in the schema grammar.\n\nAREA SPLIT (/tmp/h9n2-areas.md, author brief /tmp/h9n2-phase2-brief.md)\n  Five authors, thirteen Go files, each with exactly one owner:\n  A = 12.6 / 17 / 18.1 · B = 18.2 / 18.4 / 18.5 / 18.6 / 18.7 · C = 18.3 ·\n  D1 = 18.9 scalar · D2 = 18.9 constructed / 18.8 / 18.10.\n  18.9-value-type is split into corpusArea189Scalar + corpusArea189Constructed\n  because D1 and D2 both write that clause dir and must not share a Go file.\n  The 17 flagged alternatives partition across areas with none shared, none\n  orphaned: 1 to A, 5 to B, 5 to C, 6 to D2.\n\nSENTINEL REGISTRY RULING\n  invalid/ is not syntax-only — 12 of its 13 fixtures are valid ISO GQL that this\n  parser semantically rejects. Rather than adding a duplicate fixture for the new\n  ErrUnsupportedPhraseForm, TestSentinelReachability is extended so a sentinel\n  counts as covered by either an invalid/ fixture or an unsupported corpus entry.\n  This keeps one registry instead of two, and is forward-correct: sentinels here\n  are temporary by design, so when gqlc-uhb or gqlc-h9n.1 lands, its corpus\n  entries flip to resolves in the same commit and reachability stays honest with\n  no orphaned fixture to clean up.","created_at":"2026-07-25T04:29:27Z"},{"id":"019f978d-216f-7429-9e51-a661ea7a9d39","issue_id":"gqlc-h9n.2","author":"areqag","text":"Two corrections and one verification before Phase 2 fan-out (team-lead, 2026-07-25).\n\nAREA SPLIT VERIFIED COMPLETE AND DISJOINT (/tmp/areacheck.py)\n  Cross-checked /tmp/h9n2-areas.md against the derived obligation: 0 unassigned\n  rules, 0 unassigned tokens, 0 assigned-but-not-required rules. Rule counts sum\n  exactly: A 36 + B 17 + C 29 + D1 29 + D2 22 = 133. All 133 tokens assigned to\n  exactly one area. This mattered because an unassigned obligation would leave the\n  coverage gate red with no author responsible for it.\n  Load is uneven — D1 carries 64 of the 133 tokens (scalar type keywords), A carries\n  36 of the 133 rules.\n\nAMENDMENT A1.2 CORRECTED — it contradicted A2\n  A1.2 gave the AS-prefixed spelling 'CREATE GRAPH TYPE t AS COPY OF other' a corpus\n  entry with sentinel ErrNoGraphType. A2 requires every corpus file to enter\n  createGraphTypeStatement at least once. That file enters it zero times by\n  construction, and ErrNoGraphType is precisely the sentinel for zero times, so the\n  two items are mutually exclusive. Both were written in the same amendment.\n  Resolution: A2 stands with zero exceptions — no exemption field, no allowlist, no\n  per-entry flag, because an exception mechanism in the one invariant designed to\n  catch files that silently cover nothing is self-defeating. The AS-prefixed case\n  becomes invalid/as_copy_of_graph_type.gql mapped to ErrNoGraphType.\n  It belongs there on the merits, not for convenience: it discharges no corpus\n  obligation. Its tree holds createGraphStatement, graphSource and graphExpression,\n  none of which are in the required-133; the only required symbols it touches are\n  the tokens CREATE GRAPH TYPE AS COPY OF, all covered many times over by real\n  area-A files.\n  Consequence to remember: ErrNoGraphType is the one sentinel no corpus entry may\n  ever pin.\n\nAUTHORING GRANULARITY RULED\n  'One construct per file' is about isolating failures, not maximising file count.\n  Different outcomes must be different files, since outcome is per file. Within one\n  outcome, split where a failure stops being informative. For D1 specifically:\n  group supported scalar spellings by type family as several properties on one node\n  type, and give each unsupported spelling its own file so it is individually\n  flippable as the epic progresses. Safe for the silent-drop guard, which counts\n  element type declarations rather than properties — forty properties on one node\n  type is still one element type and one node.","created_at":"2026-07-25T04:33:57Z"},{"id":"019f9795-0f7d-7b8b-b660-39a2b11b30ce","issue_id":"gqlc-h9n.2","author":"areqag","text":"Phase 1 harness complete on branch gqlc-h9n2-schema-corpus (8b12b4a). One red gate, everything else green.\n\nThree obligation sets, all derived from GQL.g4, none hand-listed:\n  133 reachable parser rules, 133 reachable tokens, 17 flagged alternatives.\nCovered by the 6 seed files: 57 rules, 25 tokens, 0 alternatives.\n\nThe third set is amendment A5: an alternative whose (rules ∪ tokens) are all named by its siblings between them cannot be demanded by rule or token coverage — cover the siblings and both gates pass while the alternative is never parsed. nodeTypeImpliedContent#3 is the concatenation of #1 and #2. flaggedAlternatives() derives the 17; wantFlaggedAlternatives pins the count, so a grammar change that opens an 18th blind spot fails TestGrammarObligation.\n\nGolden files are gone (A6). A golden could not have pinned gqlc-h9n.3 at all: schema.EdgeType has no undirectedness field, so UNDIRECTED and DIRECTED resolve byte-identically. Replaced with semanticCases, one member, swept against the manifest in both directions.\n\nManifest is keyed by author area (A/B/C/D1/D2), not by ISO clause: areas D1 and D2 both write into 18.9-value-type/, so clause keys would have forced them to share one Go file.\n\nPhase 2 worklist at /tmp/h9n2-uncovered.txt — the clause-grouped uncovered list plus the flagged alternatives, the semantic-case mechanism, the area split, and four verified spelling traps.\n\nBranch is unpushable while red (.githooks/pre-push runs just test). No --no-verify.","created_at":"2026-07-25T04:42:36Z"},{"id":"019f993c-167a-76c5-852a-8e9e3611a8f9","issue_id":"gqlc-h9n.2","author":"areqag","text":"Phase 1 harness: third obligation set is now measured, not declared.\n\n- `covers` deleted. M1 (labelled context struct per `#Label`) + M2 (ordered direct-child sequence vs alternative EBNF, lowest match wins) attribute every alternative from the tree. Unattributable rule or unmatched shape = hard error.\n- Criterion is forbid-and-recompute: 47 candidates (sibling-subset found 17).\n- Ruling 9 applied: `coverageRoots` is the single `createGraphTypeStatement`. `graphTypeSource#3` drops out of the candidate set; deleting it loses 110/133 rules from the real entry point.\n- Obligation = 47 candidates minus 1 exemption (`connectorUndirected#1`, stolen by `connectorPointingRight#1`, gqlc-h9n.10). `stolenBy` joins the required set, so one red gate carries the demand.\n- Pins: rules 133, tokens 133, invisible alternatives 47.\n\nState: 78/133 rules, 32/133 tokens, 17/46 alternatives from the 9 seed files. `TestCorpusGrammarCoverage` is the only failing assertion in the repo under `-shuffle=on`; lint, fmt-check, tidy-check clean. Commit 4416558 (local; branch unpushable while red).\n\nFeasibility (ruling 6b) discharged with parse evidence, not static reading: all 29 uncovered alternatives have a verified spelling, tabulated in /tmp/h9n2-uncovered.txt.","created_at":"2026-07-25T12:24:40Z"},{"id":"019f994c-21a8-7876-ad87-892249bd61d8","issue_id":"gqlc-h9n.2","author":"areqag","text":"Phase 1 harness at f5c38c2 (local; branch unpushable while the coverage gate is red — .githooks/pre-push runs just test).\n\nGate state is the deliverable: TestCorpusGrammarCoverage is the only failure in the repo, at rules 78/133, tokens 32/133, alternatives 17/46. lint 0 issues, fmt-check and tidy-check clean.\n\nCount reconciliation settled, since the pins depend on it. The pre-Ruling-9 candidate set was 48 and included graphTypeSource#3; Ruling 9 cut coverageRoots to the single statement root, which drops it, giving 47 candidates and 46 required after the one connectorUndirected#1 exemption. graphTypeSource#3 is GQL.g4:350 'AS? nestedGraphTypeSpecification', the body form, so every body-form corpus file takes it and no probe for it can fail — which is why a 48-of-48 probe came back clean against the wrong set. It is also why it is not invisible: deleting it costs 110 of 133 rules. wantInvisibleAlternatives = 47 stands.\n\nTwo corpus-affecting spelling findings for Phase 2, both probed:\n- An arc endpoint is an alias OR a filler, never both. (a {p :: STRING})-[:R]-\u003e(b) is 'no viable alternative'. (a)-[:R]-\u003e(b) and (:A)-[:R]-\u003e(:B) discharge different alternatives of sourceNodeTypeReference; neither implies the other.\n- COPY OF $$gt discharges graphTypeReference#2 while COPY OF $$s/gt discharges schemaReference#3 — one character apart, neither implies the other. COPY OF $$s.gt and COPY OF /$$s/gt are syntax errors. Note ANTLR error recovery still reports graphTypeReference coverage for both errors, so a hand-run probe that prints rule hits looks like it worked; measureCoverage requires zero syntax errors so the gate is not fooled.\n\nAlso fixed: the edgeTypePhraseFiller#2 spelling in the worklist was UNDIRECTED ... CONNECTING (a TO b), which parses clean but yields connectorPointingRight#1 — the gqlc-h9n.10 misparse. Changed to DIRECTED so the phrase-filler file does not double as that golden.","created_at":"2026-07-25T12:42:11Z"}],"dependency_count":0,"dependent_count":2,"comment_count":8}
{"_type":"issue","id":"gqlc-h9n.1","title":"schema/gql: COPY OF graph type source silently yields an empty schema","description":"## STATUS 2026-07-28: the bug half is MOOT — discharged by #392, not by this bead\n\nVerified against `origin/master` at `fe79e3a`, by reading the code and running\nthe suite, not by report:\n\n- `listener.go` now rejects the AS-less `COPY OF` spelling. The test is\n  `src == nil || src.NestedGraphTypeSpecification() == nil`, i.e. it asserts the\n  *supported* alternative rather than enumerating the unsupported ones, so a\n  fourth source alternative added to the grammar later is rejected rather than\n  silently dropped.\n- **The false comment quoted below no longer exists.** It was rewritten in the\n  same change and is now correct, including the `graphTypeSource` (§12.6) vs\n  `graphSource` (§12.4) distinction this issue identified.\n- `COPY OF` is pinned by 11 corpus files (`12.6-graph-type-statement/copy_of_source.gql`\n  and ten under `17-references/`), every one carrying the `ErrUnsupportedSource`\n  sentinel; `AS COPY OF` is pinned separately by `TestCorpusSpellingTraps`\n  because it parses as a different statement and so cannot be a corpus file.\n\nThe fix landed in `6d4ac8f` — *test(schema): ISO GQL graph-schema corpus +\ngrammar coverage gate* (#392) — as a side effect of building the corpus, which\nis why no commit names this issue and why the issue text was never updated.\nThe deviation-table correction this issue asked for is therefore also moot: the\ntable credits `ErrUnsupportedSource` with covering `COPY OF`, and that is now\ntrue.\n\n## What remains, and why this is blocked rather than closed\n\nOnly the **support** half: making `COPY OF` resolve instead of erroring. That is\nnot a listener fix. `COPY OF` names a graph *type* in another file, so it needs\ncatalogue and multi-file scoping — which is sequenced behind `gqlc-h9n.8` and\nbehind the unmade one-graph-type-per-file decision. `copy_of_source.gql`'s\nmanifest entry already names this bead as the one that would flip it from\n`unsupported` to `resolves`.\n\nRetitling deferred: the title still describes the discharged half.\n\n---\n\n## Original report (bug half — no longer reproducible)\n\n`graphTypeSource` has three alternatives (GQL.g4:346-350):\n\n    graphTypeSource\n        : AS? copyOfGraphType      # COPY OF \u003cgraph type reference\u003e\n        | graphTypeLikeGraph       # LIKE \u003cgraph expression\u003e\n        | AS? nestedGraphTypeSpecification\n        ;\n\n`EnterCreateGraphTypeStatement` (listener.go:87-90) tests only\n`src.GraphTypeLikeGraph()`. The `COPY OF` alternative falls straight through,\nthe name is recorded, no element types are ever collected, and `resolve()`\nreturns an empty schema with a nil error — the same silent-drop failure as\nphrase form (gqlc-uhb).\n\nVerified 2026-07-25 against a binary built from `schema-phrase-form`:\n\n    CREATE GRAPH TYPE Foo COPY OF Bar\n\n    internal/user/db/user.cypher: query FindPersonTest: unknown label: Person\n    Error: generate: 1 error\n\nContrast the `LIKE` spelling, which is rejected correctly:\n\n    CREATE GRAPH TYPE Foo LIKE Bar\n\n    Error: schema schema.gql: unsupported graph type source\n\n## The code comment asserting otherwise is false\n\nlistener.go:84-86 reads:\n\n    // A LIKE source (graphTypeLikeGraph) reaches this rule; COPY OF is parsed\n    // as a different statement and never gets here. Only an inline AS { ... }\n    // body (nestedGraphTypeSpecification) is supported.\n\n`COPY OF` *does* get here. The comment appears to have confused\n`copyOfGraphType` under `graphTypeSource` (§12.6, `CREATE GRAPH TYPE x COPY OF\ny`) with `graphSource : AS COPY OF graphExpression` under `createGraphStatement`\n(§12.4, `CREATE GRAPH ...`) — a genuinely different statement that indeed never\nreaches this listener method. Both the comment and gqlc-0ri's deviation table\n(which credits `ErrUnsupportedSource` with covering `COPY OF`) must be corrected\nalong with the code.\n\n","acceptance_criteria":"CREATE GRAPH TYPE x COPY OF y fails with a named sentinel naming the unsupported source, not a downstream unknown-label error.\nThe false comment at listener.go:84-86 is corrected to say COPY OF does reach this rule.\ngqlc-0ri's deviation table no longer credits ErrUnsupportedSource with covering COPY OF.\nA test pins the COPY OF rejection alongside the existing LIKE rejection.","notes":"\n## The defect half landed in #392; scope narrows to support (team-lead, 2026-07-26)\n\nVerified on merged master `7518581`. Three of the four acceptance criteria are\ndischarged, and the fourth is **inverted** rather than outstanding.\n\n1. **Named sentinel, not a downstream unknown-label error — DONE.**\n   `EnterCreateGraphTypeStatement` (listener.go:92) now inverts the source test\n   from *enumerate the bad* to *require the good*:\n   `if src := c.GraphTypeSource(); src == nil || src.NestedGraphTypeSpecification() == nil`.\n   `COPY OF` therefore fails with `ErrUnsupportedSource` instead of falling\n   through to an empty schema with a nil error. That inversion is also what\n   makes a *future* fourth `graphTypeSource` alternative rejected rather than\n   silently dropped.\n\n2. **The false comment — DONE.** listener.go:84-91 now states the opposite of\n   what it did, and states more than the criterion asked: \"Both LIKE and the\n   AS-less `COPY OF` spelling reach this rule and are rejected here. Of the two\n   COPY OF spellings only the AS-less one gets this far: `AS COPY OF` matches\n   createGraphStatement instead, so it surfaces as ErrNoGraphType.\"\n\n3. **Tests pin it — DONE, in two independent places.** `TestCorpusSpellingTraps`\n   (corpus_test.go:465-484) pins all four spellings including the `AS COPY OF`\n   trap and both parameter sigils; five corpus entries pin the sentinel\n   (`12.6-graph-type-statement/copy_of_source.gql`, `17-references/copy_of_*`).\n\n4. **\"gqlc-0ri's deviation table no longer credits ErrUnsupportedSource with\n   covering COPY OF\" — MOOT, and worth understanding why.** That criterion was\n   written on the belief that the table's claim was false. #392 made the claim\n   **true**: `ErrUnsupportedSource` now genuinely does cover `COPY OF`. So the\n   table needs no correction — the code moved to meet it. The only residue is a\n   stale line reference (`listener.go:87-90`, now :92), which belongs to\n   gqlc-0ri's own pass, not here.\n\n**What remains is support, not the silent drop.** The five corpus rows above are\n`unsupported` and cite this bead as the one whose landing flips them to\n`resolves`. Do not close this bead on the strength of the rejection working —\nthat would orphan five rows pointing at a closed bead. Same shape as gqlc-uhb,\nwhere #392 fixed the contract-violation half and the support half stayed open.\n\nSupport means catalogue and multi-file scoping: `COPY OF` names a graph *type*\nin a schema namespace, so it is solvable in principle (unlike `LIKE`, which\nneeds a live graph *instance* and is declined permanently). Sequencing note:\nthat likely wants gqlc-h9n.8's key-label-set work and a decision on gqlc's\none-graph-type-per-file rule first.","status":"blocked","priority":1,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:33:15Z","created_by":"areqag","updated_at":"2026-07-28T03:46:42Z","started_at":"2026-07-28T03:45:04Z","external_ref":"https://github.com/areqag/gqlc/issues/358","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.1","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:33:14Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-h9n.1","depends_on_id":"gqlc-h9n.8","type":"blocks","created_at":"2026-07-27T23:46:47Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f977b-fb35-7259-a253-1adcaee6da73","issue_id":"gqlc-h9n.1","author":"areqag","text":"Grammar finding, verified 2026-07-25 with an out-of-tree ANTLR probe (linus, gqlc-h9n.2 design audit):\n\n`CREATE GRAPH TYPE t AS COPY OF other` parses with **zero syntax errors** and enters `createGraphTypeStatement` **zero** times. `createGraphStatement` (GQL.g4:313) wins the ALL(*) decision because it precedes `createGraphTypeStatement` in `primitiveCatalogModifyingStatement` (GQL.g4:293 vs 295): `TYPE` is consumed as the graph name, `t` as `ofGraphType`, and `AS COPY OF other` as `graphSource` (GQL.g4:331-333). So the listener's `EnterCreateGraphTypeStatement` never fires and the failure surfaces as `ErrNoGraphType` — a misleading diagnostic, not silence.\n\nDropping the `AS` works: `CREATE GRAPH TYPE t COPY OF other` gives createGraphTypeStatement=1, copyOfGraphType=1. This is why the corpus uses the AS-less spelling for `copyOfGraphType` coverage, and pins the AS-prefixed spelling as a separate entry with sentinel ErrNoGraphType.\n\nConsequence for this bead: implementing COPY OF is not sufficient. The AS-prefixed spelling — which is the natural ISO spelling, since \u003cgraph type source\u003e is 'AS? \u003ccopy of graph type\u003e' — will still not reach the handler. Fixing that requires disambiguating createGraphStatement vs createGraphTypeStatement in the grammar, which is gqlc-h9n.11 territory. Decide here whether this bead covers only the AS-less spelling or takes the grammar fix too.\n\nAlso out of corpus scope: `t AS COPY OF s/other` and `t COPY OF s/other` are genuine syntax errors today — a bare identifier is not a \u003cschemaReference\u003e (GQL.g4:1469-1472). Schema-qualified COPY OF sources are unreachable; routed to gqlc-h9n.11.","created_at":"2026-07-25T04:15:13Z"},{"id":"019f979e-5918-77bc-8708-ec921cea0b24","issue_id":"gqlc-h9n.1","author":"areqag","text":"Acceptance criterion is now mechanical, from the gqlc-h9n.2 corpus harness.\n\nUntil this bead lands, COPY OF is classified in the corpus as outcome=unsupported with sentinel ErrUnsupportedSource, shared with LIKE — both name a graph whose element types are not in the file and need a live catalogue to inspect. That widening is done by gqlc-h9n.2, not here. It also corrects the false comment at listener.go:84-86, which claims COPY OF 'is parsed as a different statement and never gets here'; that is true only of the AS-prefixed spelling.\n\nDONE for this bead means flipping the entry to resolves, at which point a currently DORMANT guard goes live and it is the one to watch. assertNothingDropped branches on declaresElements: for an inheriting source there is no in-file element count to compare, so the only assertion available is that the model is NON-EMPTY. Resolving COPY OF to an empty schema is precisely this bead's defect, and the equality check would pass it vacuously at 0 == 0. Once both COPY OF and LIKE are unsupported that branch has no live caller, so gqlc-h9n.2 pins its predicate via a declaresElements==false case in TestCoverageElementCount. Do not delete either as dead code.\n\nSpelling traps, all pinned by TestCorpusSpellingTraps:\n- 'CREATE GRAPH TYPE t AS COPY OF other' is NOT this construct. graphSource : AS COPY OF graphExpression (GQL.g4:331) makes it match createGraphStatement (GQL.g4:313) instead, so it enters createGraphTypeStatement zero times.\n- Working spellings omit AS: COPY OF CURRENT_SCHEMA/gt, HOME_SCHEMA/gt, ./gt, /a/b/gt, ../a/gt, $$gt.\n- A parameter reference needs both sigils: only SUBSTITUTED_PARAMETER_REFERENCE (GQL.g4:3604) is reachable from a schema reference.\n- 'COPY OF s.gt' parses and is the ONLY spelling reaching catalogObjectParentReference#2; 'COPY OF s/gt' is a syntax error, because an identifier is not a schemaReference (GQL.g4:1469).","created_at":"2026-07-25T04:52:45Z"}],"dependency_count":1,"dependent_count":0,"comment_count":2}
{"_type":"issue","id":"gqlc-h9n","title":"[EPIC] schema/gql: full ISO GQL graph type DDL conformance — no gqlc dialect","description":"Umbrella for closing the gap between what `internal/grammar/gql/GQL.g4` accepts\nand what `internal/schema/gql` actually collects, across ISO/IEC 39075:2024\nclause 18 (`\u003cnested graph type specification\u003e`) and the clause 12.6\n`\u003cgraph type statement\u003e` sources.\n\n## Owner principle and method\n\n2026-07-24: *\"we should be able to accept any valid ISO GQL. we should not have\nour own GQLC flavour of ISO GQL.\"* Reaffirmed 2026-07-25: *\"if it is valid ISO\nGQL, we should allow it.\"*\n\nMethod, set 2026-07-25: *\"first build our own huge dataset of valid ISO GQL schema\nfiles and then merge that in and incrementally increase coverage to 100% over that\nas a test suite. this follows a /tdd flow.\"*\n\nSo gqlc-h9n.2 lands **first** — the corpus and the coverage gate. Every other bead\nin the epic then reduces to moving corpus entries from `unsupported` to `resolves`,\nwhich is a red-green cycle whose red is already written.\n\n## Framing: optional features, not deviations\n\nResearch on 2026-07-25 established that ISO/IEC 39075:2024 Annex D assigns feature\nID codes **only to optional features**; mandatory features have no ID, and\nconformance is governed by subclause 24.2 *Minimum conformance*. Essentially all\ngraph-type DDL is optional in GQL 1.0 — CITED, PG-Schema (arXiv 2211.10962,\nauthors include Green, Plantikow, Voigt): *\"schema support is limited both in\nexisting systems and in the first version of the GQL Standard. It is anticipated\nthat the second version of the GQL Standard will include a rich DDL.\"*\n\nThat reframes most of this epic. Declining an optional feature is **conformant**,\nso the no-dialect principle bites hardest on two things:\n\n1. **Mandatory features.** Subclause 16.8 `\u003clabel expression\u003e` has no feature ID.\n   gqlc gets it wrong (gqlc-h9n.7). That is the one item here that is a genuine\n   conformance violation.\n2. **Anything silently dropped.** A construct that parses and then vanishes is a\n   defect whether or not the feature is optional, because the diagnostic lands on\n   innocent code.\n\nFeature-ID table and sources: `iso-gql-annex-d-optional-features` bd memory.\nNode-type identity findings: `gqlc-iso-node-type-identity` bd memory.\n\n## The three failure classes\n\n1. **Silent drop** — the branch is neither handled nor rejected, so the schema\n   resolves to nothing and the resolver blames the query file. Worst class: the\n   diagnostic points at correct code.\n2. **Silent reinterpretation** — the branch is partly read and a modifier is\n   discarded, so generated code disagrees with the schema as written.\n3. **Undocumented rejection** — refused with a sentinel, but no ADR says why, so\n   the accepted subset exists only as scattered `l.fail` calls in one file.\n\n## Inventory\n\nEnumerated from GQL.g4 §18.1-18.10 and §12.6. Every row verified empirically\nagainst a binary built on 2026-07-25, not inferred from reading the listener.\n\n| Construct | Grammar | Today | Class | Bead |\n|---|---|---|---|---|\n| label expression satisfaction (**mandatory**) | 16.8 | exact-key lookup, wrong | — | h9n.7 |\n| `nodeTypePhrase` / `edgeTypePhrase` | 1510, 1557 | silent drop | 1 | uhb |\n| `COPY OF` graph type source | 348 | silent drop | 1 | h9n.1 |\n| `UNDIRECTED` kind + directed arc | 1554 | silent reinterpretation | 2 | h9n.3 |\n| key label set as identity | 1534 | conflated with complete label set | — | h9n.8 |\n| `=\u003e` label implication (GG21) | 1534, 1577 | `ErrLabelImplication` | 3 | h9n.9 |\n| `edgeTypePatternUndirected` (GH02) | 1602 | `ErrUndirectedEdge` | 3 | h9n.12 |\n| `LIKE` graph type source (GG04) | 349 | `ErrUnsupportedSource` | 3 | h9n.12 |\n| `BYTES` / `BINARY` / `VARBINARY` | 1760-1763 | `ErrUnsupportedType` | 3 | h9n.4 |\n| `TIME` / `ZONED TIME` / `LOCAL TIME` | 1881-1889 | `ErrUnsupportedType` | 3 | h9n.4 |\n| `DURATION(...)` | 1891-1898 | `ErrUnsupportedType` | 3 | h9n.4 |\n| `SIGNED` / `UNSIGNED` verbose numerics | 1803, 1816 | `ErrUnsupportedType` | 3 | h9n.4 |\n| `LIST\u003cT\u003e` / `ARRAY` (GV50) | 1723-1725 | `ErrUnsupportedType` | 3 | h9n.5 |\n| `recordType` | 1977 | `ErrUnsupportedType` | 3 | h9n.6 |\n| `pathValueType` | 1964 | `ErrUnsupportedType` | 3 | h9n.6 |\n| dynamic union types | 1728-1732 | `ErrUnsupportedType` | 3 | h9n.6 |\n| reference value types | 1900 | `ErrUnsupportedType` | 3 | h9n.6 |\n| immaterial types (`NULL`, `NOTHING`) | 1907 | `ErrUnsupportedType` | 3 | h9n.6 |\n| node type with properties, no labels | 1528-1532 | `ErrUnnamedNodeType` | 3 | h9n.10 |\n| `CONNECTING (a TO b)` ambiguity | 1659-1667 | works by ANTLR rule order | — | h9n.10 |\n\nAlready decided and test-pinned, needing only an ADR line: qualified graph type\nname truncation, `IF NOT EXISTS`, `OR REPLACE` (all in `TestGraphTypeName`,\nparser_test.go:71-90).\n\nOut of scope because they are **not ISO**: `CONSTRAINT ... REQUIRE ... IS KEY`,\n`ABSTRACT` node types, `+=`, `(\u003c:Message)` endpoint subtyping. All are Microsoft\nFabric extensions or GQL-2 prereserved words; `CONSTRAINT` appears at GQL.g4:3504\nin the prereserved-word lexer block with zero parser uses. Recorded so a future\nreader does not file them as gaps — Fabric's documentation calls them \"the syntax\ndefined in the GQL standard\", which is wrong for those parts.\n\n## Sequencing\n\n1. **h9n.2** corpus + coverage gate — foundation, lands first, green.\n2. **uhb**, **h9n.1**, **h9n.3** — the silent-drop/reinterpret P1 bugs.\n3. **h9n.7** — the mandatory-feature violation.\n4. **h9n.4**, **h9n.5** — value types worth having.\n5. **h9n.8**, **h9n.9** — the identity model, then the `=\u003e` syntax that populates it.\n6. **h9n.6**, **h9n.10**, **h9n.12** — the declined families, grounded.\n7. **h9n.11**, **0ri** — the provenance correction and the ADR that records it all.\n\nEach is its own branch and its own PR, independently mergeable: if only that PR\nlanded with the rest deferred, `just test` still passes.\n","notes":"2026-07-28, h9n-loop tick (this session): stop condition remains in effect — no actionable h9n child. Cleaned up two stale worktrees: gqlc-bdsync (branch chore/bd-sync-h9n-tick, PR #591 already squash-merged) and gqlc-h9n33 (branch feat/h9n33-record-closed-union, at master SHA, no work done). All remaining open h9n children confirmed blocked: h9n.28/h9n.29 (ISO arbiter permanently blocked by gqlc-lir decision), h9n.22/h9n.23/h9n.5 (CLOSED since previous tick — PRs #592 #593 #594), h9n.33 (parameterised PropertyType model, non-delegated structural design), h9n.31/gqlc-5md (named consumer needed), h9n.1 (one-graph-type-per-file rule decision needed), h9n.27 (P4, do not do standalone), gqlc-0ri (must land last in epic, siblings h9n.28/h9n.29 permanently blocked).\n\n2026-07-28, h9n-loop tick (second this-session): stop condition re-confirmed. No actionable h9n children. No stale worktrees. 30 children closed, 1 blocked (h9n.1), 7 open-but-blocked (h9n.28, h9n.29, h9n.33, h9n.31, gqlc-5md, h9n.27, gqlc-0ri). All blocks require owner decisions outside the delegated ADR set. Epic remains OPEN.","status":"open","priority":1,"issue_type":"epic","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:32:31Z","created_by":"areqag","updated_at":"2026-07-28T21:33:24Z","external_ref":"https://github.com/areqag/gqlc/issues/359","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0gb.4","title":"cli: init --add appends a generation target","description":"Branch 4 of 4. `gqlc init --add` appends one generation target.\n\nAuthority: docs/specs/config-multi-target.md §8.2 (merged in #374). This bead\nrecords scope only.\n\nCode:\n- `init --add`: loads the existing config, prompts for one new target with the\n  distinguishing fields blank and the rest prefilled from the last entry,\n  validates the proposed `out` at the prompt via config.CheckOutAgainst (the\n  exported seam branch 2 adds — do NOT re-implement the overlap rule here),\n  previews every entry, appends on confirm.\n- Extends the §8.1 multi-target-edit refusal message, which branch 2 ships\n  without the `--add` hint because branch 2 has no such flag.\n\nWhy --add is not on branch 2: it would put the config rewrite and a new flag in\none PR and leave this branch empty. The cost is one string written twice;\nTestInitRefusalNamesAddFlag pins the second wording.\n\nDocuments (§9): cli-stage-2.md — the flag, and the refusal path in the flow\ntable. Branch 2 already fixed the same file's renamed wire keys.\n\nTests: §10, the branch-4 rows.\n","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:24:49Z","created_by":"areqag","updated_at":"2026-07-25T21:59:05Z","started_at":"2026-07-25T21:18:57Z","closed_at":"2026-07-25T21:59:05Z","close_reason":"Merged as 9b00d7c (PR #389). Linus PASS: 38 mutations, 35 caught; survivors triaged to gqlc-5yg (pre-existing blank-out gap) and gqlc-ayq (merged-spec contradiction).","external_ref":"https://github.com/areqag/gqlc/issues/360","metadata":null,"dependencies":[{"issue_id":"gqlc-0gb.4","depends_on_id":"gqlc-0gb","type":"parent-child","created_at":"2026-07-24T23:24:49Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-0gb.4","depends_on_id":"gqlc-0gb.3","type":"blocks","created_at":"2026-07-24T23:24:56Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0gb.3","title":"pipeline: run every generation target; two-phase write","description":"Branch 3 of 4. internal/cli/pipeline runs every config entry, and the write\nbecomes two-phase.\n\nAuthority: docs/specs/config-multi-target.md §6 (pipeline contract) and §7\n(CLI output protocol), merged in #374. This bead records scope only.\n\nCode:\n- pipeline.Run loops the entries: Result{Targets []TargetResult, Diagnostics},\n  TargetResult{Files, OutDir}. The existing invariant widens unchanged: Targets\n  is populated iff Diagnostics is empty and err is nil.\n- Setup failures fail fast at the offending entry, prefixed `graph[i]: `.\n  Query diagnostics accumulate across every target; any diagnostic anywhere\n  means nothing is written for any target (§6.1, §6.2).\n- The two-phase write (§7): inspectOutputs -\u003e []targetPlan (no mutation), then\n  commitOutputs (every step a mutation). Replaces today's interleaved\n  writeOutput at generate.go:113-172.\n- Deletes branch 2's temporary len(Targets) != 1 guard.\n\nThe N-target pipeline and the two-phase write are ONE branch on purpose (§9):\nan N-target Result driven through the single-target write path wipes and\nrewrites target 0 before target 2 is inspected — the half-generated abort ADR\n0013 forbids — and a two-phase write over a one-target Result is a rename with\nno second target to protect.\n\nDocuments (§9): cli-generate-pipeline.md, cli-stage-1.md. README.md does NOT\nbelong here — its sample is a config file, correct once branch 2 lands.\n\nTests: §10, the branch-3 rows. Two are load-bearing and easy to omit:\nTestRunStateIsPerTarget (two DIFFERENT schemas plus procsig asymmetry — fails\nif sch/reg/queryParser/res at pipeline.go:122-159 are hoisted out of the loop,\nwhich every other test passes with a shared resolver) and\nTestGenerateAbortLeavesAbsentDirAbsent.\n\nNote (§7.2 step 7): a wipe-list os.Remove returning fs.ErrNotExist is SUCCESS.\nThe §4.3 overlap check has an admitted abs-vs-relative blind spot that\nguarantees a path to it, and \"already gone\" satisfies the wipe.\n","notes":"AMENDMENT (from branch 2 review, 2026-07-25). Two corrections to the description above.\n\n1. README.md DOES belong to branch 3, contrary to the description's claim. Branch 2's MAJOR 2 fix added a temporary refusal clause to README.md (~:35-38: 'This gqlc runs one: a config declaring several entries loads but is refused at generation time, and generating from all of them lands in the next release') and to CONTEXT.md (~:479-481: 'generation currently refuses a config that declares more than one, and running every target in document order lands in the next release'). Branch 3 deletes the pipeline guard at pipeline.go:117, so it MUST delete both clauses in the same PR or ship docs saying multi-target is refused while the binary does it. Grep 'next release' to find all three sites.\n\n2. The §4.3 blind spot named in the note above is narrower than written. Branch 2 closed the relative-vs-relative class by anchoring both operands onto a synthetic absolute base before the bidirectional filepath.Rel (config.go anchorRelative). TWO classes survive, both genuinely undecidable without the working directory: (a) absolute vs relative, (b) re-entry through the working directory's own name, e.g. '../b/db' vs 'db' when cwd is itself named b. Either still guarantees a path to the fs.ErrNotExist wipe case, so step 7's reasoning is unaffected — only the description of why.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:24:48Z","created_by":"areqag","updated_at":"2026-07-25T21:11:15Z","started_at":"2026-07-25T19:55:23Z","closed_at":"2026-07-25T21:11:15Z","external_ref":"https://github.com/areqag/gqlc/issues/361","metadata":null,"dependencies":[{"issue_id":"gqlc-0gb.3","depends_on_id":"gqlc-0gb","type":"parent-child","created_at":"2026-07-24T23:24:47Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-0gb.3","depends_on_id":"gqlc-0gb.2","type":"blocks","created_at":"2026-07-24T23:24:55Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0gb.2","title":"config: load a list of generation targets","description":"Branch 2 of 4. Rewrite internal/config for the nested multi-target wire shape.\n\nAuthority: docs/specs/config-multi-target.md (merged in #374). Sections 2-5 are\nthe wire shape, in-memory shape, loader semantics with the full error\ncatalogue, and the canonical Save form. Do not re-derive the rules here; this\nbead records scope only, so the spec stays the single copy.\n\nCode:\n- internal/config: Config{Targets []Target}, the document scan (§4), the entry\n  prefix (§4.1), old-flat-shape detection (§4.2), output-directory overlap\n  (§4.3), null entries (§4.4), the entry-count invariant (§4).\n- Exported `CheckOutAgainst` (§4.3) — internal/cli borrows the overlap rule\n  rather than restating it, as it already borrows the XxxValues() vocabularies.\n- Mechanical consumer adaptation (§9.1): pipeline.Run gains a temporary\n  len(Targets) != 1 guard (branch 3 deletes it); init binds one target and\n  refuses a multi-target edit (§8.1); fixtures rewritten.\n- classifyTarget -\u003e classifyConfig (§9 Terminology).\n\nDocuments (§9): config-file-format.md, README.md, cli-stage-2.md, CONTEXT.md.\nBranch 2 carries more docs than its code footprint suggests because renaming a\nwire key invalidates every document that quotes one.\n\nTests: §10, the branch-2 rows.\n\nTwo traps the spec pins and review found the hard way:\n- The overlap rule is filepath.Rel run in BOTH directions, rejecting\n  containment as well as equality. The disjointness test is on a path\n  component (rel == \"..\" || HasPrefix(rel, \"..\"+sep)); a bare string-prefix\n  test wrongly accepts internal/db vs internal/db/..foo.\n- Every kind or tag test the document scan makes must resolve YAML aliases\n  first. An alias node's own Tag is \"\", so an unresolved !!null test passes\n  `- *none`, yaml.v3 drops it, and every later graph[i] index silently\n  renumbers. The count invariant catches any residual drop.\n","notes":"Round 4: Linus PASSed the bit-claim after independent test over 781 cwds spanning depth 0-4 including / (the root-clamping case that falsified the previous wording) — 1310 pairs overlapping under NO cwd, zero wrongly rejected; all 420 departures spell an anchor segment and all are rejections. Singular sweep clean, subtest rename agreed. One finding: b vs ../a was grouped as 'plainly disjoint' when ../a IS the working directory whenever that directory is named a, making it a class-B limit acceptance. Lead verified and fixed directly in 90f7c64 (one doc line, no implementer round). Round 5 confirmation dispatched.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:24:47Z","created_by":"areqag","updated_at":"2026-07-25T19:33:09Z","started_at":"2026-07-25T16:52:17Z","closed_at":"2026-07-25T19:33:09Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/362","metadata":null,"dependencies":[{"issue_id":"gqlc-0gb.2","depends_on_id":"gqlc-0gb","type":"parent-child","created_at":"2026-07-24T23:24:46Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-0gb.2","depends_on_id":"gqlc-0gb.1","type":"blocks","created_at":"2026-07-24T23:24:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0gb.1","title":"spec: ADR + spec rewrites for multi-target config","description":"Branch 1 of 4 in the multi-target stack. Docs only — ratify the design before any code exists.\n\nScope refined 2026-07-24: edits to config-file-format.md, cli-stage-1.md, cli-stage-2.md, cli-generate-pipeline.md, CONTEXT.md and README.md do NOT belong here. Those documents describe shipped behaviour (config-file-format.md and cli-stage-1.md each landed as one branch with their code), so rewriting them ahead of the loader would leave master carrying a spec that contradicts its own binary. They travel with the branches that make them true: config-file-format.md + CONTEXT.md with branch 2, cli-generate-pipeline.md with branch 3, cli-stage-1.md + cli-stage-2.md + README.md with branch 4.\n\nDeliverables:\n- docs/adr/0013-*.md — the config file declares a list of generation targets. Records the sqlc-mirroring shape, the per-entry `schema`, the `graph` key name, the version-1 breaking change, the all-or-nothing write posture, and the two-phase tripwire. Amends ADR 0012's single-output-directory framing. ADRs ahead of implementation are house style (cf. ADR 0009).\n- docs/specs/config-multi-target.md — the implementation brief for all four branches, in the style of cli-stage-1.md: wire format, loader semantics + full error catalogue, pipeline Result contract, CLI two-phase write, init changes, and which branch owns which downstream doc update.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:24:46Z","created_by":"areqag","updated_at":"2026-07-25T04:54:56Z","started_at":"2026-07-25T03:25:04Z","closed_at":"2026-07-25T04:54:56Z","external_ref":"https://github.com/areqag/gqlc/issues/363","metadata":null,"dependencies":[{"issue_id":"gqlc-0gb.1","depends_on_id":"gqlc-0gb","type":"parent-child","created_at":"2026-07-24T23:24:45Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0gb","title":"config: multi-target generation — one schema, many generated packages (sqlc-style)","description":"Widen the gqlc config file from a single generation target to a list of targets, mirroring sqlc's version-2 `sql:` list. One schema file, many query directories, each producing its own Go package in its own output directory.\n\nWire shape (breaking change to version 1 — no v2, no users yet; user ratified 2026-07-24):\n\n    version: 1\n    graph:\n      - schema: schema.gql\n        schema_language: gql\n        queries: internal/user/query\n        query_language: opencypher\n        procsig: procs.procsig.json   # optional\n        gen:\n          go:\n            package: userdb\n            out: internal/user/gen\n            driver: neo4j-go-v5\n\nDesign decisions ratified with the user before any code:\n- `schema` repeats per entry (sqlc-identical), not hoisted to the document root.\n- `gen: go:` nesting mirrored from sqlc; `output` renamed `out` and moved under it, `driver` alongside it (the analogue of sqlc's `sql_package`).\n- Top-level list key is `graph` (the analogue of sqlc's `sql`).\n- version stays 1; the old flat shape simply stops loading.\n- Setup failures (missing schema, empty query dir, bad procsig) fail fast, prefixed `graph[i]: `. Query-level diagnostics still accumulate across every target.\n- `gqlc init` authors one target; a new `--add` flag appends one more.\n\nLands as a 4-branch gt stack: spec → config → pipeline → cli.","status":"closed","priority":1,"issue_type":"epic","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:24:21Z","created_by":"areqag","updated_at":"2026-07-25T22:00:21Z","closed_at":"2026-07-25T22:00:21Z","close_reason":"All 4 branches merged (#374, #379, #386, #389). Acceptance criteria §12 verified end-to-end against a built binary on 2026-07-25: two-target generate produces two packages in two directories with a byte-identical second run; nested out rejected at load naming both entries; tripwire abort names the entry and the offending file and leaves target 0 byte-identical; old flat shape reports the shape change; a null entry reports graph[1], not a shifted index; init --add is wired and TTY-gated. Residual prose defects filed as gqlc-5ta and gqlc-ayq; test gap as gqlc-5yg.","external_ref":"https://github.com/areqag/gqlc/issues/364","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-uhb","title":"schema/gql: support ISO GQL phrase-form node and edge type declarations (currently silently dropped)","description":"`GQL.g4` implements both ISO GQL alternatives for element type declarations:\n\n    nodeTypeSpecification : nodeTypePattern | nodeTypePhrase    # GQL.g4:1501-1504\n    edgeTypeSpecification : edgeTypePattern | edgeTypePhrase    # GQL.g4:1549-1552\n\n`internal/schema/gql/listener.go` implements only the *pattern* branches —\n`EnterNodeTypePattern` (listener.go:106) and `EnterEdgeTypePattern`\n(listener.go:154). There is no `EnterNodeTypePhrase` or `EnterEdgeTypePhrase`.\n\nA phrase-form declaration therefore parses without error, the walk collects\nnothing, and `rawSchema.resolve()` (resolve.go:26) returns an **empty schema\nwith no error at all**.\n\n## Repro\n\nValid ISO GQL that silently yields an empty schema:\n\n    CREATE GRAPH TYPE SocialNetworkSchema AS {\n      NODE Person LABEL Person { id STRING, name STRING, joined_date DATE },\n      NODE Company LABEL Company { id STRING, name STRING, industry STRING },\n      DIRECTED EDGE KNOWS LABELS KNOWS { since INT }\n        CONNECTING (Person TO Person),\n      DIRECTED EDGE WORKS_FOR LABELS WORKS_FOR { role STRING, start_year INT }\n        CONNECTING (Person TO Company)\n    }\n\nwith `match (p1:Person)-[r:KNOWS]-\u003e(p2:Person) ... return p1, p2, r` gives:\n\n    internal/user/db/user.cypher: query FindPersonTest: unknown label: Person\n\nThe error blames the query file. The query is correct; the schema is the fault\nand is never mentioned.\n\nIsolated confirmation (2026-07-24):\n- pattern-form `Person` + phrase-form `Company` -\u003e `unknown label: Company`\n- pattern-form nodes + `DIRECTED EDGE ... CONNECTING (p TO c)` -\u003e\n  `unknown edge: Person-[WORKS_FOR]-\u003eCompany`\n- converting every declaration to pattern form generates all four files\n  correctly\n\n## Why this is a contract violation, not just a gap\n\nerrors.go:5-7 states these are \"Errors returned by Parse for GQL this parser\nrejects: constructs outside the supported subset\", and the listener *does*\nloudly reject label implication (`ErrLabelImplication`), undirected edges\n(`ErrUndirectedEdge`) and unsupported graph type sources\n(`ErrUnsupportedSource`). Phrase form is the sole unsupported construct that is\nneither supported nor rejected.\n\n## Explicitly NOT part of this bug\n\nAll three were suspected during triage and verified legal and working:\n\n- `CREATE GRAPH TYPE` without `PROPERTY` — `PROPERTY?` is optional\n- `LABEL \u003cname\u003e` / `LABELS \u003cspec\u003e` — GQL.g4:1679-1683; `labelSet()`\n  (nodetype.go:23-40) already handles both branches\n- untyped properties `id STRING` — `propertyType : propertyName typed?\n  propertyValueType` (GQL.g4:1701), `typed?` optional\n\nThe grammar is a faithful ISO GQL grammar and is not the problem.\n","design":"## 1. `EnterNodeTypePhrase` -\u003e `rawNode`\n\n    nodeTypePhrase       : nodeSynonym TYPE? nodeTypePhraseFiller (AS localNodeTypeAlias)?\n    nodeTypePhraseFiller : nodeTypeName nodeTypeFiller? | nodeTypeFiller\n\nName from `nodeTypeName`, alias from `AS localNodeTypeAlias`, labels and\nproperties by reusing the existing `nodeContent(...)` helper (listener.go:197).\nNo new label or property code is needed — `labelSet()` already handles the\n`LABEL` and `LABELS` branches.\n\n## 2. `EnterEdgeTypePhrase` -\u003e `rawEdge`\n\n    edgeTypePhrase       : edgeKind edgeSynonym TYPE? edgeTypePhraseFiller endpointPairPhrase\n    edgeTypePhraseFiller : edgeTypeName edgeTypeFiller? | edgeTypeFiller\n    endpointPairPhrase   : CONNECTING endpointPair\n\nReuse `edgeContent(...)` (listener.go:221) for labels and properties. Map\n`endpointPair` to `rawEndpoint`.\n\nTwo behaviours must match the pattern-form path:\n- `endpointPairUndirected` must be rejected with `ErrUndirectedEdge`, mirroring\n  `EnterEdgeTypePatternUndirected` (listener.go:150). Otherwise undirected edges\n  become silently droppable again through the phrase door.\n- `endpointPairPointingLeft` lists `destinationNodeTypeAlias` first; normalise to\n  canonical source-\u003etarget, as the pattern path already does (listener.go:177-180).\n\nNote `edgeKind` is **mandatory** in phrase form but optional in pattern form, and\n`connectorUndirected : TO | TILDE` overlaps `connectorPointingRight : TO |\nRIGHT_ARROW`. `CONNECTING (a TO b)` therefore resolves as pointing-right only\nbecause `endpointPair` lists the directed alternative first and ANTLR takes the\nfirst match. This bead depends on that behaviour, so add a test pinning it;\ngqlc-h9n.10 owns the wider ambiguity write-up.\n\n## 3. Endpoint references are ALIAS-ONLY — resolved, see notes\n\n    endpointPairPointingRight : LEFT_PAREN sourceNodeTypeAlias connectorPointingRight destinationNodeTypeAlias RIGHT_PAREN\n    sourceNodeTypeAlias       : regularIdentifier\n\n`CONNECTING (Person TO Person)` requires `Person` to be a declared **local\nalias**; a `\u003cnode type name\u003e` is not a valid endpoint reference. resolve.go:47-49\nalready populates the alias table only from `localNodeTypeAlias`, which is\ncorrect — implement alias-only resolution and do **not** add a type-name\nfallback, which would be a gqlc dialect.\n\nThe evidence and its tier are in the notes. It is INFERRED, not CITED: ISO/IEC\n39075:2024 is paywalled and no public source containing the Syntax Rules for\n`\u003cendpoint pair\u003e` could be located. Alias-only is also the conservative\ndirection — it rejects what the standard may reject, rather than accepting what\nit may forbid — so a later citation can only widen what is accepted.\n\nThe error for the alias-less spelling must be precise: it must name that an alias\nwas expected and that the referenced node type declares none, not surface a bare\n`ErrUnknownEndpoint`.\n\n## 4. Empty-graph-type guard (`ErrEmptyGraphType`)\n\n`resolve()` must reject a graph type that declares zero node types.\n`ErrNoGraphType` covers \"no graph type statement\"; nothing covers \"graph type\nwhose body declared no element types\".\n\n**This guard catches the total-silence case only.** A mixed schema — some\ndeclarations in pattern form, one in phrase form — leaves `r.nodes` non-empty,\npasses the guard, and still drops the phrase declaration silently (verified\n2026-07-25). It is a narrow backstop, not the structural fix. The structural fix\nis the branch-exhaustiveness test in gqlc-h9n.2, which this bead depends on.\n\nNote the guard is not reachable through an empty body: `elementTypeList` requires\nat least one element, so `CREATE GRAPH TYPE G AS { }` is a syntax error (verified\n2026-07-25). It is reachable only via an unhandled branch — which is exactly why\nthe exhaustiveness test is the real remedy.\n\n## 5. Fixtures for BOTH forms\n\nEvery existing fixture (`test/data/**`, README.md:35) uses pattern form\nexclusively — that is precisely why this shipped. Add phrase-form coverage, and\nprefer a paired-equivalence test: the same graph type spelled both ways must\nresolve to an equal `schema.Schema`.\n","acceptance_criteria":"EnterNodeTypePhrase and EnterEdgeTypePhrase collect name, alias, labels and properties, reusing the existing nodeContent/edgeContent/labelSet helpers.\nThe alias-bearing repro schema in the description generates without error and produces a schema.Schema equal to its pattern-form equivalent.\nThe alias-less spelling fails with an error naming that an endpoint alias was expected and that the referenced node type declares none — not a bare ErrUnknownEndpoint.\nUndirected endpoint pairs in phrase form are rejected with ErrUndirectedEdge, matching pattern-form behaviour.\nLeft-pointing endpoint pairs are normalised to canonical source-\u003etarget.\nA test pins CONNECTING (a TO b) as pointing-right, since the TO connector is grammatically ambiguous and this bead relies on ANTLR alternative order.\nresolve() rejects a graph type resolving to zero node types via a new ErrEmptyGraphType sentinel, documented as catching the total-silence case only.\nFixtures cover both forms; a paired-equivalence test asserts pattern and phrase spellings of the same graph type resolve identically.\nThe PR restates the alias-only decision and its INFERRED evidence tier.","notes":"## RESOLVED 2026-07-25 — endpoint references are ALIAS-ONLY\n\nVerdict: `CONNECTING (Person TO Person)` requires `Person` to be a **declared\nlocal alias**. A `\u003cnode type name\u003e` is NOT a valid endpoint reference. Implement\nalias-only resolution; do NOT add a type-name fallback.\n\nEvidence tier: **INFERRED, independently corroborated** — not a direct citation\nof the ISO Syntax Rules. ISO/IEC 39075:2024 is paywalled and no public draft\ncontaining the Syntax Rules for `\u003cendpoint pair\u003e` could be located. Flagging that\nhonestly so a future reader knows the strength of the claim.\n\n**One decisive argument, two supporting:**\n\n1. **Grammar, deliberate asymmetry (strong — this is the argument that carries\n   the conclusion).** The pattern form's `sourceNodeTypeReference`\n   (GQL.g4:1618-1621) accepts *either* an alias *or* an inline `nodeTypeFiller`\n   (i.e. a label reference). The phrase form's `endpointPairPointingRight`\n   (GQL.g4:1646-1648) accepts **only** a bare alias — no filler alternative. Had\n   the standard intended endpoints in a `CONNECTING` clause to be referenceable\n   by name or label, the filler alternative was right there in the adjacent rule\n   and was not used.\n\n2. **Grammar, reference positions (supporting only — weaker than first\n   assessed).** `nodeTypeName` occurs at exactly two sites in GQL.g4 — 1507\n   (`nodeTypePattern`) and 1515 (`nodeTypePhraseFiller`) — both *declaration*\n   positions, plus its own rule at 2860. It appears in no reference position\n   anywhere in the grammar.\n\n   Demoted from \"strong\" on review: `sourceNodeTypeAlias : regularIdentifier`\n   (GQL.g4:1669) means the grammar structurally *cannot* carry a distinct\n   type-name reference in that slot — a name and an alias are the same token\n   shape. So `nodeTypeName`'s absence from reference positions is what you would\n   observe whether the static semantics bound type names or not. It is\n   consistent with the conclusion but does not discriminate between the\n   hypotheses.\n\n3. **Independent expert corroboration (supporting).** Alex Miłowski's worked GQL\n   graph-type example writes the alias explicitly and then references it:\n\n       NODE :Person =\u003e :Thing { ... } AS Person\n       DIRECTED EDGE knows {} CONNECTING (Person -\u003e Person)\n\n   with the note: *\"In phrase version, you'll see that `AS Person` added to the\n   declaration of the `Person` node type. This seems necessary as the endpoint\n   pair phrase requires a local alias.\"*\n   https://www.milowski.com/journal/entry/2024-06-26T12:00:00-07:00/\n\n   Corroboration at the same tier, not a citation of the prose: his \"seems\n   necessary\" shows he is reading the same grammar.\n\nSupporting, non-decisive: Microsoft Fabric's GQL graph types reference (reviewed\nby Stefan Plantikow, a GQL standard editor) uses the pattern form exclusively —\n`(:Person)-[:knows { ... }]-\u003e(:Person)` — and states endpoint references resolve\nby key label: *\"You only need to specify the key labels ... for endpoint node\ntypes ... The system resolves these references to the full node type\ndefinitions.\"* That matches gqlc's existing pattern-form `fillerLabels`\nbehaviour. The page contains no `CONNECTING` example at all.\nhttps://learn.microsoft.com/en-us/fabric/graph/gql-graph-types\n\n**Why acting on an inference is acceptable here.** Alias-only is the conservative\ndirection: it refuses a spelling the standard may also refuse. If a citation\nlater shows type names *are* bound, the fix widens what gqlc accepts and breaks\nno existing schema. The opposite guess — inventing a type-name fallback — would\ncreate a gqlc dialect that later has to be taken away.\n\n## Consequence for the repro schema\n\nThe alias-less original from the user's sandbox is **not conformant** — its\n`CONNECTING` clauses reference node type *names*. The conformant spelling adds\n`AS \u003calias\u003e`, one clause per node type:\n\n    CREATE GRAPH TYPE SocialNetworkSchema AS {\n      NODE Person LABEL Person { id STRING, name STRING, joined_date DATE } AS Person,\n      NODE Company LABEL Company { id STRING, name STRING, industry STRING } AS Company,\n      DIRECTED EDGE KNOWS LABELS KNOWS { since INT }\n        CONNECTING (Person TO Person),\n      DIRECTED EDGE WORKS_FOR LABELS WORKS_FOR { role STRING, start_year INT }\n        CONNECTING (Person TO Company)\n    }\n\nAn alias may share a node type's spelling (`AS Person`), so this stays readable.\n\nVerified 2026-07-25: this spelling produces **no syntax error** from the current\ngrammar — it fails only with `unknown label: Person`, i.e. it parses and is then\nsilently dropped. So it is the correct headline repro and acceptance fixture for\nthis bead: it must generate successfully once phrase form is collected. The\ndescription has been corrected to use it.\n\nKeep the alias-less original as a second fixture asserting a *precise* error — it\nmust name that an alias was expected and that the referenced node type declares\nnone, rather than surfacing a bare `ErrUnknownEndpoint`.\n\n## Not in scope, filed separately\n\nResearch on 2026-07-25 established that essentially all graph-type DDL is\n*optional* in GQL 1.0 (Annex D feature IDs; see the\n`iso-gql-annex-d-optional-features` bd memory), and that node type identity\nshould be the key label set rather than the complete label set (see\n`gqlc-iso-node-type-identity`). Neither changes this bead: phrase form is a\n*silent drop*, which is a defect regardless of whether the construct is optional.\nThe identity and satisfaction work is gqlc-h9n.7 and gqlc-h9n.8.","status":"closed","priority":1,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:01:07Z","created_by":"areqag","updated_at":"2026-07-26T06:45:01Z","started_at":"2026-07-26T05:56:17Z","closed_at":"2026-07-26T06:45:01Z","close_reason":"Landed in PR #395 (squash-merged 2026-07-26). EnterNodeTypePhrase/EnterEdgeTypePhrase now consume the phrase-form alternatives that GQL.g4 always parsed and the listener silently dropped, including the LABEL/LABELS keyword form and alias binding. Corpus resolution 32 -\u003e 38 of 86; grammar coverage unchanged at 133/133/46 (the constructs were always reached on the parse tree — what was missing was consumption). ErrUnsupportedPhraseForm deleted with no danglers, TestSentinelReachability green. ErrUnknownEndpoint split with ErrEndpointNotAlias for a CONNECTING endpoint naming a node type rather than its alias.\n\nTwo declinations recorded in the PR body and verified by the reviewer as correct ISO readings, not gaps: no type-name fallback for CONNECTING endpoints (the grammar slot is sourceNodeTypeAlias, not the sourceNodeTypeReference the pattern form uses), and no ErrEmptyGraphType (AS { } is a parser syntax error, so the sentinel would be unreachable and unpinnable).\n\nOne re-sentinelling: phrase_form_undirected_connector.gql moves to ErrUndirectedEdge rather than resolves, because connectorUndirected: TO | TILDE means CONNECTING (a ~ b) has no canonical source-\u003etarget — same reason the pattern-form ~[ ]~ is rejected.\n\nReviewer verified all seven claims against the tree and caught six of seven mutations thrown at new and adjacent code; the seventh escapes only in a weak reordering form the code comment does not claim to forbid.","external_ref":"https://github.com/areqag/gqlc/issues/353","metadata":null,"dependencies":[{"issue_id":"gqlc-uhb","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:38:27Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-uhb","depends_on_id":"gqlc-h9n.2","type":"blocks","created_at":"2026-07-24T23:41:43Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f977f-64de-781a-82e7-dd70743ae0e4","issue_id":"gqlc-uhb","author":"areqag","text":"Scope narrowed by team-lead ruling, 2026-07-25 (gqlc-h9n.2 phase 1).\n\nThe corpus in gqlc-h9n.2 cannot land green while phrase form is *silently dropped*: such an entry is neither `resolves` (the model is empty or short) nor `unsupported` (no sentinel exists). Rather than add a third `silentlyDropped` outcome — which would ship nothing to the user, pin the bad behaviour as expected, and give future authors an escape hatch for making the gate green — gqlc-h9n.2 adds **`ErrUnsupportedPhraseForm`** and rejects phrase form loudly.\n\nConsequences for this bead:\n\n1. The class-3 half (undocumented rejection) is discharged by gqlc-h9n.2, not here. By the time this bead starts, phrase form already fails with a named sentinel and the diagnostic already lands on the schema file instead of the user's query file — which is the actual user-visible fix for the complaint that started the epic.\n2. This bead's scope narrows to **implementing** phrase form: `EnterNodeTypePhrase` / `EnterEdgeTypePhrase`, alias-only endpoint resolution per the RESOLVED note above, and removing the sentinel.\n3. **This bead's red is a reclassification**, not a new failing test: flip the phrase-form corpus entries from `unsupported` to `resolves` and bump `wantCorpusResolving`. The corpus already asserts every element type declaration reaches the model, so the silent-drop guard is what proves the implementation actually collected them.\n4. Removing `ErrUnsupportedPhraseForm` is not free: `TestSentinelReachability` sweeps bidirectionally, so its `allSentinels` entry and its `invalid/` fixture must be deleted in the same PR.\n\nSame pattern applies to gqlc-h9n.1 (COPY OF → ErrUnsupportedSource).","created_at":"2026-07-25T04:18:56Z"},{"id":"019f978a-fb92-7d5c-9f26-e45fff68d651","issue_id":"gqlc-uhb","author":"areqag","text":"Grammar fact that sharpens this bead's value (team-lead, 2026-07-25).\n\nendpointPairPhrase is referenced at exactly one site in GQL.g4 — line 1558, inside\nedgeTypePhrase. Nothing else in the grammar reaches it. So implementing the phrase\nform is what unlocks the entire CONNECTING endpoint syntax: every endpointPair*\nand connector* rule (endpointPairPhrase, endpointPair, endpointPairDirected,\nendpointPairPointingRight, endpointPairPointingLeft, endpointPairUndirected,\nconnectorPointingRight, connectorUndirected) plus the tokens CONNECTING, TO,\nTILDE, RIGHT_ARROW and LEFT_ARROW are unreachable without it.\n\nThat is roughly ten of the required-133 rules and five of the required-133 tokens\nriding on this one bead, which makes it a bigger slice of ISO surface than the\n'phrase form is an alternative spelling' framing suggests. Node side is much\nsmaller by comparison: nodeTypePhrase and nodeTypePhraseFiller only, because\nnodeTypeFiller is shared with nodeTypePattern and sourceNodeTypeReference.\n\nAlso note RIGHT_ARROW ('-\u003e', connectorPointingRight) is a different token from\nBRACKET_RIGHT_ARROW (']-\u003e', arcTypePointingRight). The pattern form uses the\nbracketed family exclusively; the bare arrows are phrase-form only.\n\nUntil this bead lands, gqlc-h9n.2's corpus classifies all of the above as\nunsupported with ErrUnsupportedPhraseForm — a sentinel added by h9n.2 purely to\nconvert today's silent drop into a loud rejection. This bead deletes that sentinel\nand flips those entries to resolves, which is its red-green cycle.","created_at":"2026-07-25T04:31:36Z"},{"id":"019f979e-2146-73bf-a64c-8e7eb1df01f2","issue_id":"gqlc-uhb","author":"areqag","text":"Acceptance criterion is now mechanical, from the gqlc-h9n.2 corpus harness.\n\nUntil this bead lands, the phrase forms are classified in the corpus as outcome=unsupported with sentinel ErrUnsupportedPhraseForm — an explicit rejection replacing today's silent drop (class 1 -\u003e class 3). That sentinel is added by gqlc-h9n.2, not here.\n\nDONE for this bead means: flip the phrase-form corpus entries from unsupported to resolves, bump wantCorpusResolving, and the silent-drop guard in assertNothingDropped must hold — depth-1 elementTypeSpecification count == len(Nodes)+len(Edges). The guard is what makes the flip meaningful: a listener that handles EnterNodeTypePhrase but drops the properties would count 1 declaration against 0 or 1 element types and fail.\n\nAlso required, and easy to miss: nodeTypePhraseFiller and edgeTypePhraseFiller each have a named and an unnamed alternative (NODE TYPE A LABEL A vs NODE TYPE LABEL A), and both are in the 48 invisible alternatives that rule+token coverage cannot compel. Both need to take their alternative, so both need a corpus file.\n\nNote endpointPairPhrase (GQL.g4:1633) has exactly ONE reference site, edgeTypePhrase at GQL.g4:1558. So the phrase-form work discharges roughly 10 rules and 5 tokens of obligation at once — this bead is disproportionately valuable to the coverage gate.","created_at":"2026-07-25T04:52:31Z"}],"dependency_count":1,"dependent_count":0,"comment_count":3}
{"_type":"issue","id":"gqlc-3w2","title":"internal/config: YAML config parser — Load/Save, v1 format, gqlc init interface","description":"Design locked in grill session 2026-07-11/12. CLI-agnostic package internal/config using gopkg.in/yaml.v3 (promote to direct dep).\n\nPUBLIC API:\n- const DefaultFilename = \"gqlc.yaml\" (canonical name only; no .yml/.json variants; no search logic)\n- Load(path string) (Config, error); Decode(r io.Reader) (Config, error) (procsig parity)\n- (Config) Save(path string) error — canonical YAML for future 'gqlc init', golden-fixture pinned, round-trips through Load\n- Typed enums, single source of truth for decode+errors+prompts: SchemaLang (SchemaLangGQLC=\"gqlc\"), QueryLang (QueryLangOpenCypher=\"opencypher\"), Driver (DriverNeo4jGoV5=\"neo4j-go-v5\") + XxxValues() accessors\n- Config struct = canonical LATEST shape, NO Version field (version is wire-only): SchemaPath, QueryDir, OutputDir, OutputPackage, ProcsigPath (optional, empty=absent), SchemaLang, QueryLang, Driver\n\nWIRE FORMAT v1 (snake_case keys, declaration order = canonical Save order): version(int, required, only 1), schema, queries, output, package (all required non-empty), schema_language, query_language, driver (required enums), procsig (optional, explicit \"\" rejected as ambiguous).\n\nLOAD SEMANTICS: version probe (lenient two-field decode, pointer int: nil=missing-field error, !=1 = 'declares version N; only version 1 is supported') then dispatch to strict v1 decoder — the seam for future v2 (per-version decoders normalizing into one Config; NO version interface). Strict decode: yaml.Decoder KnownFields(true), wireV1 with pointer fields for precise missing-field errors listing valid enum values. Enum UnmarshalYAML rejects invalid values with line info: 'line N: invalid query_language \"x\" (valid values: opencypher)'. package must pass go/token.IsIdentifier. NO filesystem checks (relative paths are relative to the config file's dir — documented semantics, future CLI resolves). Empty file gets actionable error (procsig precedent).\n\nERRORS: no custom sentinels; fmt.Errorf with 'config: \u003cpath\u003e: ...' prefix, %w wrapping (fs.ErrNotExist passes through for future gqlc init exists-check). Tests assert substrings, procsig table style.\n\nDELIVERABLES: internal/config + tests + testdata golden fixture; docs/specs/config-file-format.md (field table, error catalogue, rationale: CLI-agnostic, required single-value enums, seam-not-interface versioning); CONTEXT.md glossary entry (already drafted, committed on branch).\n\nPrecedent: internal/procsig/file.go + docs/specs/procsig-file-format.md. Refs gqlc-aqb (stays open — config carries the procsig path; threading into cypher.WithRegistry is aqb's scope once the CLI exists).","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T04:02:03Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:11Z","started_at":"2026-07-12T04:02:17Z","closed_at":"2026-07-12T04:40:23Z","close_reason":"Shipped in PR #258 (squash 8ce98ee). internal/config: Load/Decode/Save v1, tag-strict version probe, typed single-value enums with XxxValues() vocabulary, KnownFields strict decode, golden fixture + round-trip, spec doc docs/specs/config-file-format.md, CONTEXT.md glossary entry. Linus round 1 FAIL (1 BLOCKER: yaml float→int truncation in version probe; 5 NITs) → fixes in 1aa535c → round 2 verified PASS. Two non-blocking NITs deferred to follow-up bead.","external_ref":"https://github.com/areqag/gqlc/issues/283","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-9io","title":"Nested golden module: add golangci-lint to test-codegen-fence","description":"The C0 spec §7 pins 'go build \u0026\u0026 go vet' inside test/data/codegen as the compile fence. Owner clarified 2026-07-11 that generated code must uphold the same linting + formatting standards as gqlc's own CI. To enforce: add a .golangci.yml at test/data/codegen (matching or a subset of the root .golangci.yml), extend 'just test-codegen-fence' recipe to also run 'golangci-lint run' inside the nested module, and update the CI job codegen-fence to match. Filed as a follow-up in case C0 code cycle lands the recipe without the linter arm; the C0 review will grill for this and this bead can close if C0 already includes it.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T04:09:49Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T04:30:29Z","closed_at":"2026-07-11T04:30:29Z","close_reason":"Folded into PR #224 (master 4ff49ac). The 'just test-codegen-fence' recipe was extended in commit e3f0736 to run 'golangci-lint run' inside the nested module (discovers root .golangci.yml via upward walk — parity for free, no duplicate config to maintain) plus 'go mod tidy -diff' (catches the // indirect drift Linus flagged, nit #1). CI job codegen-fence inherits. Generated goldens verified: 0 lint issues at C0. No standalone bead needed.","external_ref":"https://github.com/areqag/gqlc/issues/223","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.8","title":"C6: polish — version stamp, session config, gqlc-0aa re-scope","description":"Version-stamp hardening in generated headers, session-config polish (access modes, WithTx edge cases), and re-scope gqlc-0aa against D4's no-runtime-package decision (at most session/tx helpers remain). Spec first: docs/specs/codegen-stage-c6.md.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:52Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T16:33:18Z","started_at":"2026-07-11T16:05:15Z","closed_at":"2026-07-11T16:33:18Z","close_reason":"Shipped in PR #244. C6 polish + version stamp + gqlc-0aa re-scope delivered per merged spec (PR #243, 3a69db9). Linus PASS. FINAL STAGE OF EPIC. version.go extracted (var not const, fixes latent -ldflags bug); TestGeneratedHeaderFormat regression test; ErrOutOfC5Scope→ErrOutOfC6Scope mechanical rename; New/WithTx/SessionConfig verify-only (byte-stable since C0). Epic gqlc-8i0 CLOSES with this bead.","external_ref":"https://github.com/areqag/gqlc/issues/209","dependencies":[{"issue_id":"gqlc-8i0.8","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:52Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.8","depends_on_id":"gqlc-8i0.7","type":"blocks","created_at":"2026-07-10T23:13:52Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.7","title":"C5: hard residue — edgeUnion + collision hardening","description":"edgeUnion as sealed interface per query column (type \u003cQueryName\u003e\u003cField\u003e interface with unexported marker, //sumtype:decl, marker methods on entity structs, nil interface for nullable); package-level collision sweep hardening. Distinct/GroupingKey confirmed to surface nothing. Spec first: docs/specs/codegen-stage-c5.md.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:51Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T16:03:13Z","started_at":"2026-07-11T15:11:16Z","closed_at":"2026-07-11T16:03:13Z","close_reason":"Shipped in PR #241. C5 edgeUnion + collision hardening delivered per merged spec (PR #240, e077e50). Linus PASS. ResolvedEdgeUnion admitted; sealed interfaces per query-column with //sumtype:decl + unexported markers; models.go marker methods beside entity structs; nullable→nil interface; 6-source collision sweep (decode helpers promoted); ErrOutOfC4Scope→ErrOutOfC5Scope. 9 valid + 3 invalid fixtures new. NEXT: C6 (gqlc-8i0.8) — LAST STAGE.","external_ref":"https://github.com/areqag/gqlc/issues/210","metadata":null,"dependencies":[{"issue_id":"gqlc-8i0.7","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:51Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.7","depends_on_id":"gqlc-8i0.6","type":"blocks","created_at":"2026-07-10T23:13:51Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.6","title":"C4: write queries","description":":exec, zero-column methods (len(Columns)==0 \u0026\u0026 StatementWrite), WriteQuerier population, ExecuteWrite path, cardinality-x-shape rejection (:exec on column-producing query, :one/:many on zero-column write). Spec first: docs/specs/codegen-stage-c4.md.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:50Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T15:44:18Z","started_at":"2026-07-11T14:19:10Z","closed_at":"2026-07-11T15:44:18Z","close_reason":"Shipped in PR #237. C4 write queries delivered per merged spec (PR #236, f661b2f). Linus PASS. :exec 3-line body via ExecuteWrite; WriteQuerier populated by Statement; driverDB.run write arm live; AccessMode per-method compile-time constant; ErrExecOnProjection + ErrCardinalityShapeMismatch sentinels; ErrOutOfC3Scope→ErrOutOfC4Scope. 12 valid + 4 invalid new fixtures. Two truth-table cells (write_exec_read, cardinality_shape_zero_column_read_one) unreachable via test pipeline — filed gqlc-2ud (P3, docs-only). NEXT: C5 (gqlc-8i0.7) — edgeUnion + collision hardening.","external_ref":"https://github.com/areqag/gqlc/issues/211","metadata":null,"dependencies":[{"issue_id":"gqlc-8i0.6","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:49Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.6","depends_on_id":"gqlc-8i0.5","type":"blocks","created_at":"2026-07-10T23:13:49Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.5","title":"C3: collections, temporals, widths","description":"list\u003cT\u003e recursive; six temporals via dbtype (Date/Time/LocalTime/LocalDateTime, time.Time for zoned datetime, dbtype.Duration never time.Duration); unrepresentable-width sentinels (INT128+/UINT128+/FLOAT16/FLOAT128+/DECIMAL); FLOAT32 schema-width contract (float32 field, widen on encode, plain-convert narrow on decode); unknown/null -\u003e any, map -\u003e map[string]any. Spec first: docs/specs/codegen-stage-c3.md. Verify dbtype cells against driver docs.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:48Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T14:16:47Z","started_at":"2026-07-11T06:39:42Z","closed_at":"2026-07-11T14:16:47Z","close_reason":"Shipped in PR #234. C3 collections+temporals+widths delivered per merged spec (PR #233, cf123ad). Six temporals via dbtype (DateTime uses time.Time), Duration never time.Duration; property DATE/TIMESTAMP mapped; list\u003cT\u003e recursive via GetRecordValue[[]any] + per-element assertion; unknown/scalar-null via record.Get, scalar-map via GetRecordValue[map[string]any]; FLOAT32 schema-width contract (encode-widen decode-narrow); ErrUnrepresentableWidth (permanent, distinct from ErrOutOfC3Scope); ErrOutOfC2Scope-\u003eErrOutOfC3Scope per-stage rename. 18 new valid + 4 new invalid fixtures. Two fixture families deferred as unreachable via current pipeline (list_entity/list_nullable — resolver rejects list\u003cnode\u003e; INT128-param/FLOAT128-list-column — Phase Z catches property widths first). CI lint caught unused groupHasEntityColumn in round-2 (fixed in commit 33be802). Linus subagent hit account rate limit mid-review (resets 4am ET); orchestrator did inline verification instead. NEXT: C4 (gqlc-8i0.6) — write queries.","external_ref":"https://github.com/areqag/gqlc/issues/212","dependencies":[{"issue_id":"gqlc-8i0.5","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:48Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.5","depends_on_id":"gqlc-8i0.4","type":"blocks","created_at":"2026-07-10T23:13:48Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.4","title":"C2: entity projections","description":"models.go structs from schema (explicit Name wins; single-label mangle; multi-label/ambiguous-edge REQUIRE explicit name, checked eagerly); schema-shaped-only posture (no ElementId — identity is a projection, gqlc-v5t types it later); entity decode helpers in models.go, per-query row assembly inline. Spec first: docs/specs/codegen-stage-c2.md.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:47Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T06:37:43Z","started_at":"2026-07-11T05:39:29Z","closed_at":"2026-07-11T06:37:43Z","close_reason":"Shipped in PR #231. C2 entity projections delivered per merged spec (PR #230, 4756b7c). Linus PASS. Phase Z eager schema-shape admission, models.go populated with entity structs + decode helpers, ErrOutOfC1Scope→ErrOutOfC2Scope per-stage rename, nullable via Props map + type assertion, non-nullable via GetProperty[T]. 10 valid + 7 invalid fixtures new. All gates green. NEXT: C3 (gqlc-8i0.5) — collections+temporals+widths.","external_ref":"https://github.com/areqag/gqlc/issues/213","dependencies":[{"issue_id":"gqlc-8i0.4","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:47Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.4","depends_on_id":"gqlc-8i0.3","type":"blocks","created_at":"2026-07-10T23:13:47Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.3","title":"C1: scalar/property read queries","description":"Params/Row structs; naming rules (method verbatim, param mangle, row fields via text-shape analysis of Column.Name: bare ident / ident.ident / else alias-required sentinel); native-width property mapping; nullable -\u003e pointers both directions (params too); :one/:many; ErrNoRows sentinel; read path via ExecuteRead. Spec first: docs/specs/codegen-stage-c1.md. Verify D3 dbtype cells against driver docs.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:46Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T05:36:31Z","started_at":"2026-07-11T04:38:21Z","closed_at":"2026-07-11T05:36:31Z","close_reason":"Shipped in PR #228 (master 21c3f4b). C1 scalar/property read queries delivered per merged spec (PR #226, 81f85de). Linus round-1 PASS on code (5 defensible deviations validated). Deliverables: Params/Row structs (sqlc ergonomics), naming rules with text-shape analysis, native-width property mapping (STRING/BOOL/INT8-64/UINT8-64/INT/UINT/FLOAT32-64), nullable=pointer uniform, :one/:many with generated ErrNoRows/ErrMultipleResults, ExecuteRead read path via package-level generic neo4j.ExecuteRead[T], driverOrTx.run signature revised to ([]*neo4j.Record, error) with .Collect(ctx) inside callback, ReadQuerier populated. 9 new valid + 8 new invalid fixtures; 47 subtests pass -shuffle=on; fence green (build+vet+tidy+lint). Testcontainers deferred as gqlc-73h (P2). NEXT: C2 (gqlc-8i0.4) — entity projections.","external_ref":"https://github.com/areqag/gqlc/issues/214","dependencies":[{"issue_id":"gqlc-8i0.3","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:45Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.3","depends_on_id":"gqlc-8i0.2","type":"blocks","created_at":"2026-07-10T23:13:45Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.3","depends_on_id":"gqlc-9io","type":"blocks","created_at":"2026-07-11T00:09:48Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.2","title":"C0: codegen harness + skeleton","description":"Nested golden module at test/data/codegen (own go.mod pinning neo4j-go-driver v5) + compile fence (go build \u0026\u0026 go vet as just recipe), internal/queryfile annotation front end (// name: X :one|:many|:exec, strict sentinels), double-run byte-identity determinism test, skeleton emission that compiles: derived package name + header, db.go (Queries, New, WithTx, unexported run indirection), querier.go (ReadQuerier/WriteQuerier/Querier + assertion, emitted even when empty), empty models.go. Spec first: docs/specs/codegen-stage-c0.md.","notes":"Context for fresh agents: the full design is docs/adr/0010-codegen-typed-repositories-from-validatedquery.md (merged via PR #219) — read it entirely before starting; your stage's scope is pinned in D7, cross-cutting decisions in D1-D6/D8. Vocabulary: CONTEXT.md 'Generation language' section (query file / named query / cardinality). Discipline (ADR 0009 precedent): write docs/specs/codegen-stage-c*.md spec FIRST, get it reviewed, then golden tests, then implementation; own branch + PR per stage; adversarial implement-\u003ereview loop. Codegen consumes internal/resolver.ValidatedQuery + internal/schema.Schema; generated code lives in the user's module and may only import stdlib + neo4j-go-driver v5 (never internal/*). Standing instruction: verify D3 dbtype cells against driver docs when your stage's spec is written.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:13:44Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T04:30:25Z","started_at":"2026-07-11T03:35:52Z","closed_at":"2026-07-11T04:30:25Z","close_reason":"Shipped in PR #224 (master 4ff49ac). C0 harness + skeleton delivered per merged spec (PR #222, 48e1c95). Linus PASS after round 2 (round 1 flagged missing lint parity per owner directive; folded inline in e3f0736). Deliverables: internal/queryfile annotation front end (byte-for-byte sqlc grammar adapted to Cypher line comments), internal/codegen batch Generate with skeleton emission (derived package name, header, db.go with driverOrTx/driverDB/txDB unexported run indirection, three unconditional Querier interfaces + compile-time assertion, empty models.go), nested Go module at test/data/codegen (neo4j-go-driver v5.28.4, own go.mod, include.go anchor), double-run byte-identity determinism test, per-package closed sentinel sets + bidirectional reachability sweep. Quality fence upgraded: go build + go vet + go mod tidy -diff + golangci-lint (root .golangci.yml discovered via upward walk from nested module). CI job codegen-fence enforces. Spec §10 out-of-scope table cleanly defers Params/Row/entity structs/writes/edgeUnion/version-stamp to C1-C6.","external_ref":"https://github.com/areqag/gqlc/issues/215","metadata":null,"dependencies":[{"issue_id":"gqlc-8i0.2","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:13:44Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-8i0.2","depends_on_id":"gqlc-8i0.1","type":"blocks","created_at":"2026-07-10T23:13:44Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0.1","title":"ADR 0010: codegen typed repositories from ValidatedQuery","description":"Write and grill ADR 0010: generated API shape (Queries/New/WithTx, ReadQuerier/WriteQuerier/Querier), D3 type-mapping table for neo4j-go-driver v5, []File Generator contract, no-config v1, nested golden module + compile fence, C0-C6 staging. Closes when the ADR PR merges. All C-stages are blocked on this bead (ADR 0009 precedent: no code before the owner grills the ADR).","notes":"ADR PR: https://github.com/areqag/gqlc/pull/219 (auto-merge armed pending CI). Close this bead when the PR merges.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T03:12:33Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T03:20:24Z","started_at":"2026-07-11T03:14:05Z","closed_at":"2026-07-11T03:20:24Z","close_reason":"ADR 0010 grilled to completion and merged in PR #219 (b6ca984). All D-sections resolved; C-stages gqlc-8i0.2–.8 unblocked.","external_ref":"https://github.com/areqag/gqlc/issues/216","dependencies":[{"issue_id":"gqlc-8i0.1","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T23:12:32Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0aa","title":"Driver wiring: runtime layer for generated repositories","description":"Post-freeze runtime integration for the code-generated repository layer. The generator's output (see gqlc-8i0) needs a driver/runtime layer — connection handling, query dispatch, result decoding into the typed Go values — that the generated code calls into. Per ADR 0004 §Decision, written once against the frozen model.\n\nSplit off from #14 / gqlc-0mx after the resolver portion completed (R0-R7 shipped 2026-07-05 through 2026-07-06). Driver wiring is a distinct concern from both the resolver and the codegen.","status":"closed","priority":1,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-10T05:38:49Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T13:50:02Z","closed_at":"2026-07-18T13:50:02Z","close_reason":"Closed outright per C6 spec §8: all five original scope items (session opening, per-method access dispatch, entity decode helpers, WithTx, driver-native routing) fully subsumed by C0-C5 inline emissions. No standalone runtime layer needed per ADR 0010 D4. Auto-closed via PR #244 'Closes #150'.","external_ref":"https://github.com/areqag/gqlc/issues/150","metadata":null,"dependencies":[{"issue_id":"gqlc-0aa","depends_on_id":"gqlc-0mx","type":"discovered-from","created_at":"2026-07-10T01:38:54Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0aa","depends_on_id":"gqlc-8i0","type":"blocks","created_at":"2026-07-10T01:38:55Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-8i0","title":"Codegen: type-safe repository generator from ValidatedQuery","description":"Post-freeze code generator that takes a ValidatedQuery (produced by internal/resolver) plus a schema and emits type-safe Go repository code. Per ADR 0004 §Decision, written once against the frozen query.Query / ValidatedQuery model.\n\nSplit off from #14 / gqlc-0mx after the resolver portion completed — R0-R7 all shipped 2026-07-05 through 2026-07-06 (see gqlc-0mx.2 through gqlc-0mx.9). Codegen is a separate concern from the resolver and gets its own tracking. The generated driver wiring is tracked separately again.","notes":"ORCHESTRATION (for any agent driving this epic, 2026-07-11): Work the dotted children gqlc-8i0.2 (C0) -\u003e .8 (C6) strictly in order — they are dependency-chained, so 'bd ready' surfaces exactly the one unblocked stage. Per stage: claim the bead, read docs/adr/0010-codegen-typed-repositories-from-validatedquery.md in full (stage scope = D7; cross-cutting = D1-D6/D8) + the bead's own notes, then spec-first (docs/specs/codegen-stage-c*.md, own PR) before any code, then golden tests, then implementation — each in its own worktree+branch, adversarial implement-\u003ereview loop, verified reviewer PASS authorizes squash-merge. One stage per fresh session/team (context budget). Close each bead with PR # in the reason and close its GH mirror. Post-v1 satellites (not in the chain): gqlc-1a5 (:iter), gqlc-v5t (resolver builtins); gqlc-0aa re-scoped at C6. Epic closes when .8 closes — final PR or manual gh close for GH #151 (umbrella: child PRs must NOT write 'Closes #151').","status":"closed","priority":1,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-10T05:38:44Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T16:33:19Z","started_at":"2026-07-11T00:48:52Z","closed_at":"2026-07-11T16:33:19Z","close_reason":"Codegen epic COMPLETE. All six stages C0-C6 delivered: PR #224 (C0 harness), #228 (C1 reads), #231 (C2 entities), #234 (C3 collections/temporals/widths), #237 (C4 writes), #241 (C5 edgeUnion), #244 (C6 polish + epic close). GH umbrella #151 auto-closed by PR #244. Post-v1 satellites: gqlc-1a5 (:iter), gqlc-v5t (resolver builtins), gqlc-73h (testcontainers). Docs-only: gqlc-2ud (C4 §4.9 amendment). Autonomous ultraloop 2026-07-10→2026-07-11: Carmack+Linus subagent pattern with Linus PASS = merge auth; owner asleep throughout; all decisions agent-gated.","external_ref":"https://github.com/areqag/gqlc/issues/151","metadata":null,"dependencies":[{"issue_id":"gqlc-8i0","depends_on_id":"gqlc-0mx","type":"discovered-from","created_at":"2026-07-10T01:38:53Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.2","title":"R0: resolver skeleton — package, API, minimal ValidatedQuery, golden harness","description":"internal/resolver package with the ADR-0008-pinned API (resolver.New(s schema.Schema, opts ...Option) + WithRegistry(procsig.Registry); pure (*Resolver).Resolve(q query.Query) (ValidatedQuery, error)). Minimal ValidatedQuery; labelled single-node MATCH/RETURN (whole-entity + property refs) resolves against the schema; golden-pair harness under test/data/resolver/ (hand-authored schema+query fixtures -\u003e validated.golden.json, -update flag, valid/invalid split, sentinel-reachability sweep). Blocked on ADR 0009 surviving owner grilling.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:15Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:12Z","started_at":"2026-07-04T13:26:26Z","closed_at":"2026-07-10T06:07:12Z","close_reason":"R0 skeleton merged to master via PR #87 (squash a08ec3c): internal/resolver package with pinned ADR 0008 API, QueryResolver interface + compile-time assertion, minimal ValidatedQuery with ResolvedType sealed sum (graph.PropertyType vocabulary), 6-step kernel for labelled single-node MATCH/RETURN, golden-pair harness (5 valid / 7 invalid fixtures), 3 sentinels with bidirectional reachability sweep. Spec merged earlier via PR #86. Linus PASS verified; prescribed nits landed.","external_ref":"https://github.com/areqag/gqlc/issues/154","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.2","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:54:14Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.2","depends_on_id":"gqlc-0mx.1","type":"blocks","created_at":"2026-07-03T16:54:14Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.1","title":"ADR 0009: resolver test-first staged build (approach + ValidatedQuery vocabulary)","description":"The ADR-0004 analogue for the resolver, drafted as Cycle 4 of the freeze effort and grilled by the owner BEFORE any resolver code (explicitly reserved decision). Contents: pure (query.Query, schema.Schema[, procsig.Registry]) -\u003e ValidatedQuery with sentinel-error posture mirroring internal/schema/gql/resolve.go; ValidatedQuery type vocabulary carrying the schema's bit-width families (graph.PropertyType, ADR 0002) — richer than the frozen query.Type sum, mapping table is a stage-0 design item; golden-pair test strategy (schema fixture + query fixture -\u003e validated.golden.json) under test/data/resolver/ with -update flag, valid/invalid split, sentinel-reachability sweep — TCK has no GQL schemas so fixtures are hand-authored; staging skeleton R0-R7 (one capability per slice). API surface itself already pinned by ADR 0008.","status":"closed","priority":1,"issue_type":"decision","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:03Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:12Z","started_at":"2026-07-03T20:58:07Z","closed_at":"2026-07-10T06:07:12Z","close_reason":"ADR 0009 merged to master as PR #84 (69b225a); adversarial review PASS (zero blockers). R0 (gqlc-0mx.2) unblocks pending owner grilling of the ADR.","external_ref":"https://github.com/areqag/gqlc/issues/155","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.1","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:54:02Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-33k","title":"Cypher parser: full openCypher surface before the query.Query freeze","description":"Umbrella for the pre-freeze scope expansion (owner decision 2026-07-01). gqlc generates type-safe repository implementations for practically any openCypher query, including writes, so the freeze (gqlc-cta) must wait for: expression core + typing (Stage 6), temporal (7), full patterns (8), read-clause completion (9), aggregations (10), predicates (11), writes (12), MERGE (13), CALL + procedure registry (14), and the TCK corpus sweep. Sibling of gqlc-ude (read surface, Stages 1-5, complete). gqlc-cta dependencies extended to each piece.","status":"closed","priority":1,"issue_type":"epic","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:39Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T20:53:34Z","closed_at":"2026-07-03T20:53:34Z","close_reason":"Epic complete: all pre-freeze scope delivered. Stages 6-14 (gqlc-97z, 9b4, 9nl, ydk, 2av, 665, mkv, cqh, im1) + scope ADR gqlc-39s + TCK sweep gqlc-x6u + pre-freeze fixes gqlc-33k.1/.2 + gqlc-3r0 all closed; freeze gqlc-cta landed as PR #81 (6bdb86c, ADR 0008). Remaining open children re-homed: 33k.3/.4 -\u003e gqlc-0mx (post-freeze, consumer-driven); gqlc-c5f re-scoped standalone (premise corrected: Neo4j dialect extension, not a grammar bug); gqlc-7ur detached (standalone P3 polish).","external_ref":"https://github.com/areqag/gqlc/issues/40","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-im1","title":"Stage 14: CALL + procedure signature registry","description":"New compile-time input artifact: procedure signatures, declared alongside the schema (TCK precedent step: there exists a procedure test.my.proc(in :: INTEGER?) :: (out :: INTEGER?)). Type CALL...YIELD, YIELD *, and standalone CALL - the latter two are untypeable without the signature, so an unreferenced/unknown procedure is a generation-time compile error (a feature: fails at codegen instead of runtime). Needs godog step defs that build the per-scenario registry. Wire TCK dir: clauses/call (~41 scenario outlines).","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:18Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T12:25:42Z","started_at":"2026-07-03T09:29:15Z","closed_at":"2026-07-03T12:25:42Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/41","dependencies":[{"issue_id":"gqlc-im1","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:11Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-im1","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:54:17Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-cqh","title":"Stage 13: MERGE + ON CREATE / ON MATCH","description":"MERGE upsert semantics on top of the Stage 12 write model: match-or-create pattern binding plus conditional SET branches (ON CREATE SET / ON MATCH SET). Both branches contribute to parameter typing. Wire TCK dir: clauses/merge (~75 scenario outlines). Depends on Stage 12.","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:17Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T09:24:48Z","started_at":"2026-07-03T05:04:14Z","closed_at":"2026-07-03T09:24:48Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/42","dependencies":[{"issue_id":"gqlc-cqh","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:11Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cqh","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:54:16Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cqh","depends_on_id":"gqlc-mkv","type":"blocks","created_at":"2026-07-01T23:54:16Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-mkv","title":"Stage 12: write clauses - CREATE, DELETE, SET, REMOVE","description":"The headline scope expansion: query.Query gains a read/write statement distinction (drivers need transaction mode), a write-effect shape, and projection-less statements (no RETURN). SET value typing against the schema yields parameter type inference for writes - the core of the repository-pattern goal (typed Create/Update/Delete methods). DETACH DELETE included. TCK side-effect assertion steps (the side effects should be) are reinterpreted parse-level per gqlc-39s. Wire TCK dirs: clauses/{create,delete,set,remove} (~205 scenario outlines). Depends on Stage 6 (SET/CREATE property expressions).","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:16Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T05:04:13Z","started_at":"2026-07-03T00:23:34Z","closed_at":"2026-07-03T05:04:13Z","close_reason":"Merged as PR #67 (0971f42), GH #43 closed. StatementKind axis + Part.Effects (7 variants), 6th sentinel ErrNestedPropertyTarget, SF3 write-golden harness. Suite 3740/3307/433/0, goldens 3061, pins 88/15. Follow-ups filed: gqlc-33k.3, gqlc-33k.4, gqlc-mpn.","external_ref":"https://github.com/areqag/gqlc/issues/43","dependencies":[{"issue_id":"gqlc-mkv","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:10Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-mkv","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:54:16Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-mkv","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:54:16Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-665","title":"Stage 11: predicate expressions - quantifiers + existential subqueries","description":"ALL/ANY/NONE/SINGLE list quantifiers and EXISTS {} existential subqueries (patterns inside expressions - scoping rules matter: inner pattern variables must not leak into the outer scope). Result type is always BOOLEAN so model impact is thin; grammar and scoping are the work. Wire TCK dirs: expressions/{quantifier,existentialSubqueries} (~110 scenario outlines). Depends on Stage 6.","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:15Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T00:23:27Z","started_at":"2026-07-02T23:24:46Z","closed_at":"2026-07-03T00:23:27Z","close_reason":"Stage 11 merged: PR #65 (45045fe), GH #44 closed. Quantifiers ALL/ANY/NONE/SINGLE + EXISTS{} subqueries, TypeBool. Two review rounds (round-1 FAIL: mineComparisons descent leak → guard fix cffa32e + 4 red pins). Counts: suite 3535/3099/436/0; pins 75/13; sentinels 5. Follow-up gqlc-33k.3 filed (EXISTS LIMIT/SKIP param imprecision).","external_ref":"https://github.com/areqag/gqlc/issues/44","dependencies":[{"issue_id":"gqlc-665","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:10Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-665","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:54:15Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-665","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:54:15Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-2av","title":"Stage 10: aggregations","description":"count/sum/avg/min/max/collect + DISTINCT: detect aggregates in projections, model implicit grouping keys and the cardinality implications for generated row types. Parser-side counterpart of gqlc-gyw (resolver grouping-key semantics) - coordinate the model shape with that bead. Wire TCK dir: expressions/aggregation (~27 scenario outlines). Depends on Stage 6 expression typing.","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:14Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T23:24:39Z","started_at":"2026-07-02T22:39:22Z","closed_at":"2026-07-02T23:24:39Z","close_reason":"Stage 10 aggregations merged as PR #62 (2e9f427), GH #45 closed. Round-1 review PASS (first of run); docs-only fix round (SF-1..4: skiplist comment, spec wire-truth amendments §1.2/§1.4/§8). Suite 2886/2468/418/0 → 2921/2496/425/0; pins 66 mustParse / 12 mustReject; sentinels 4. Follow-ups filed: gqlc-33k.1 (aggregate-kind preservation), gqlc-33k.2 (projection-list DISTINCT).","external_ref":"https://github.com/areqag/gqlc/issues/45","dependencies":[{"issue_id":"gqlc-2av","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:09Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-2av","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:54:14Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-2av","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:54:14Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-ydk","title":"Stage 9: read-clause completion - ORDER BY, WITH...WHERE, UNWIND","description":"Close out the read-clause surface: ORDER BY on RETURN and WITH (ordering does not change row types but its expressions must be accepted and validated), WITH...WHERE, WITH skip/limit, and UNWIND (introduces a new binding whose element type derives from the list expression - needs Stage 6 typing). Wire TCK dirs: clauses/{return-orderby,with-orderBy,with-skip-limit,with-where,unwind} (~176 scenario outlines).","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:53:54Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T22:39:21Z","started_at":"2026-07-02T14:16:57Z","closed_at":"2026-07-02T22:39:21Z","close_reason":"Stage 9 landed: PR #60 squash-merged as 9c39a50, GH issue #46 auto-closed. Two review rounds: round-1 caught B1 UnwindBinding collision matrix holes + B2 unconditional pattern-reuse skip; fixed via symmetric kind-conflict checks + elemType gate. Suite 2886/2468/418/0, pins 59/12.","external_ref":"https://github.com/areqag/gqlc/issues/46","dependencies":[{"issue_id":"gqlc-ydk","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:09Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ydk","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:53:54Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ydk","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:53:54Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-9b4","title":"Stage 7: temporal types","description":"Add DATE/TIME/LOCALTIME/DATETIME/LOCALDATETIME/DURATION to the type vocabulary; accept and type temporal literals, accessors, and arithmetic (semantics pass through to the driver per ADR 0005 - only result types matter). Wire TCK dir: expressions/temporal (~89 scenario outlines). Depends on Stage 6 expression typing.","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:53:53Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T11:39:07Z","started_at":"2026-07-02T10:28:39Z","closed_at":"2026-07-02T11:39:07Z","close_reason":"Stage 7 temporal types merged via PR #56 (squash f3589e9); review PASS after one fix round (operator-aware promotion); suite 2399/1941/458/0; GH issue #47 closed","external_ref":"https://github.com/areqag/gqlc/issues/47","dependencies":[{"issue_id":"gqlc-9b4","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:07Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-9b4","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:53:52Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-9b4","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:53:52Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-9nl","title":"Stage 8: full pattern surface - named paths, variable-length, multi-type relationships","description":"Reverses the largest deliberate read-core exclusions (permanent ErrUnsupportedPattern): named paths (161 pending), variable-length relationships (59), multi-type relationships (2). Adds PATH to the type vocabulary; path variables become projectable bindings. Wire TCK dirs: expressions/{path,pattern,graph}. Also revisit the Stage-5 undirected model for var-length interaction.","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:53:53Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T14:16:56Z","started_at":"2026-07-02T11:39:16Z","closed_at":"2026-07-02T14:16:56Z","close_reason":"Stage 8 landed: PR #58 squash-merged as 53d20ea, GH issue #48 auto-closed. Adversarial review round 2 PASS (round 1 caught B1 synthetic-name collision — fixed via PathMember tagged sum). Suite 2517/2068/449/0, pins 52/6.","external_ref":"https://github.com/areqag/gqlc/issues/48","dependencies":[{"issue_id":"gqlc-9nl","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-02T09:04:01Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-9nl","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:53:53Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-97z","title":"Stage 6: expression core - full scalar expression surface + result typing","description":"Grammar-complete scalar expressions and compute result types for projections; retire the permanent ErrUnsupportedProjection category (53 pending scenarios of rich RETURN expressions). Completes the base type vocabulary (list, map, primitive coercions) - the type set is part of the interface to be frozen. Foundational: writes (SET values), aggregation, temporal, and predicates all build on this. Wire TCK dirs: expressions/{literals,boolean,comparison,mathematical,string,null,precedence,typeConversion,list,map,conditional}. Runtime-error scenarios in those dirs go to the documented out-of-scope skiplist category (see gqlc-39s).","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:53:52Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T10:27:18Z","started_at":"2026-07-02T05:29:18Z","closed_at":"2026-07-02T10:27:18Z","close_reason":"Stage 6 rich expressions merged via PR #54 (squash eb9db79); review PASS; suite 1395/955/440/0; GH issue #49 closed","external_ref":"https://github.com/areqag/gqlc/issues/49","dependencies":[{"issue_id":"gqlc-97z","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:07Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-97z","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:53:52Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":7,"comment_count":0}
{"_type":"issue","id":"gqlc-39s","title":"ADR: expand pre-freeze scope to full openCypher surface (writes, CALL, expressions)","description":"Product goal (owner, 2026-07-01): gqlc must generate type-safe, compile-safe repository implementations for practically ANY openCypher query - reads AND writes. ADR 0004 planned Stages 1-5 (read core) then freeze; that is now insufficient: query.Query must also model writes, MERGE, CALL, full patterns, and the full expression/type surface BEFORE the freeze (gqlc-cta), because retrofitting them after codegen exists would be far more expensive. Write a new ADR documenting: (a) the three TCK buckets - must-model (writes/merge/call/patterns/read clauses), must-parse-with-thin-model-impact (most expressions: only the result type matters, semantics pass through per ADR 0005), out-of-scope (runtime-error scenarios; result/side-effect assertions are reinterpreted at parse level); (b) explicit reversal of the permanent read-core skiplist exclusions (named paths x161, variable-length x59, multi-type x2, rich projections x53, writes x9) - those were deliberate Stage-0-5 scope cuts, now in scope; (c) procedure signatures as a new compile-time input artifact alongside the schema (TCK precedent: 'there exists a procedure test.my.proc(in :: INTEGER?) :: (out :: INTEGER?)'); (d) stage renumbering 6-14 with per-stage specs in docs/specs/ as before. Type vocabulary completeness (lists, maps, temporal, path) is part of the frozen interface and must be settled here.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:53:33Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T05:28:28Z","started_at":"2026-07-02T05:07:43Z","closed_at":"2026-07-02T05:28:28Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/50","dependencies":[{"issue_id":"gqlc-39s","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:06Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":10,"comment_count":0}
{"_type":"issue","id":"gqlc-nmg","title":"CI: golangci-lint + GitHub Actions + git hooks + branch protection","description":"Add static analysis and CI: .golangci.yml (v2, curated linter set incl. exhaustive/depguard/musttag), one GitHub Actions workflow with parallel jobs (lint/test/tidy), path-filtered vuln+actionlint workflows, just recipes as single command surface, .githooks (pre-commit: block-master+fmt+vet; pre-push: test+diff-lint) folding in the add-git-guardrails branch, and master branch protection with required checks. Agreed in session 2026-07-01: no grammar-sync job, no -race, golangci-lint binary pinned in justfile, CI runs just recipes with explicit caching instead of golangci-lint-action.","status":"closed","priority":1,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T01:06:28Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:12Z","started_at":"2026-07-02T01:06:57Z","closed_at":"2026-07-10T06:07:12Z","close_reason":"PR opened: golangci config + fixes, CI workflows, hooks, branch protection. Closes via merge.","external_ref":"https://github.com/areqag/gqlc/issues/156","metadata":null,"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx","title":"Resolver / codegen / generated driver","description":"The downstream consumers ADR 0004 reserves for post-freeze: the schema-aware resolver, the type-safe code generator, and the generated driver wiring. Each is written once against the stable frozen model. Per ADR 0004 §Decision.","status":"closed","priority":1,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:09Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:40:27Z","closed_at":"2026-07-10T05:40:27Z","close_reason":"Split into codegen gqlc-8i0 (GH #151) and driver wiring gqlc-0aa (GH #150). Resolver R0-R7 shipped under gqlc-0mx.1 through gqlc-0mx.9 (2026-07-03 through 2026-07-06). Follow-ups gqlc-33k.3 and gqlc-33k.4 re-parented under codegen.","external_ref":"https://github.com/areqag/gqlc/issues/14","dependencies":[{"issue_id":"gqlc-0mx","depends_on_id":"gqlc-cta","type":"blocks","created_at":"2026-05-26T00:26:15Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-cta","title":"Freeze query.Query + revise ADR 0003 + CONTEXT.md","description":"When Stages 1-5 are complete, lock the query model. Revise ADR 0003 to declare the model frozen and document its final shape. Update CONTEXT.md glossary to the final terminology. Also specify the resolver's API shape in the freeze ADR: schema-bound constructor (`resolver.New(schema).Resolve(query)`) per the deferral agreed during Stage 2 grilling — see ADR 0006 §Consequences (\"Resolver API shape is deferred\"). ValidatedQuery lives in the resolver package. This is the gate ADR 0004 names: no consumer is built until this lands.","notes":"Pre-freeze items surfaced by the static-analysis PR (gqlc-nmg, 2026-07-01): (1) RESOLVED 2026-07-02 by PR #38 — query.QueryBranch/QueryPart renamed to query.Branch/query.Part (matches CONTEXT.md glossary); both nolint:revive markers removed. Decision taken by owner ahead of the freeze. (2) Parameter's JSON keys were fixed to lowercase name/uses (musttag finding) — a wire change already taken pre-freeze; goldens rebaselined. (3) Post-freeze follow-up: add an apidiff CI gate on internal/query so the frozen API is mechanically enforced.","status":"closed","priority":1,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:09Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T20:53:09Z","started_at":"2026-07-03T18:36:20Z","closed_at":"2026-07-03T20:53:09Z","close_reason":"Freeze landed: PR #81 merged as 6bdb86c (closes GH #15). ADR 0008 declares query.Query frozen, inventories the frozen shape (sums: Binding x5, PathMember x4, Endpoint x2, Projection x5, Type x17, Use x3, Effect x8, SetEffect x3 + all axes), pins the resolver API (internal/resolver; resolver.New(schema, opts...) + WithRegistry(procsig.Registry); pure Resolve(query.Query) -\u003e (ValidatedQuery, error)) discharging ADR 0006's deferral, and defines the additive-only revision protocol + apidiff gate. ADR 0003 header flipped to frozen note; CONTEXT.md Resolver/Validated-query entries updated. Linus adversarial review: round 1 FAIL (sentinel-retirement overreach, fixed in 79c50b0) -\u003e round 2 PASS zero blockers, all claims verified first-party incl. godog baseline 3897/3459/438/0.","external_ref":"https://github.com/areqag/gqlc/issues/15","dependencies":[{"issue_id":"gqlc-cta","depends_on_id":"gqlc-2av","type":"blocks","created_at":"2026-07-01T23:54:42Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-39s","type":"blocks","created_at":"2026-07-01T23:54:39Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-4n2","type":"blocks","created_at":"2026-05-26T00:26:12Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-665","type":"blocks","created_at":"2026-07-01T23:54:43Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-7z6","type":"blocks","created_at":"2026-05-26T00:26:14Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:54:40Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-9b4","type":"blocks","created_at":"2026-07-01T23:54:40Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-9nl","type":"blocks","created_at":"2026-07-01T23:54:41Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-cqh","type":"blocks","created_at":"2026-07-01T23:54:44Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-im1","type":"blocks","created_at":"2026-07-01T23:54:45Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-mkv","type":"blocks","created_at":"2026-07-01T23:54:44Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-npg","type":"blocks","created_at":"2026-05-26T00:26:13Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-s2t","type":"blocks","created_at":"2026-05-26T00:26:12Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-v23","type":"blocks","created_at":"2026-05-26T00:26:14Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-x6u","type":"blocks","created_at":"2026-07-01T23:54:46Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-cta","depends_on_id":"gqlc-ydk","type":"blocks","created_at":"2026-07-01T23:54:42Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":16,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"gqlc-4n2","title":"Stage 1 — non-property-typed parameters (SKIP/LIMIT $p)","description":"First model evolution after Stage 0: parameters whose type comes from their slot rather than a binding property. Specifically bare $p in SKIP and LIMIT. Introduces query.Use as a sealed sum and relaxes invariant D4.","design":"Unlock bare SKIP $p / LIMIT $p as non-property-typed parameter uses.\n\nModel change:\n- Introduce sealed sum `query.Use` = PropertyUse | PositionalUse (mirrors Binding/Endpoint discipline)\n- PropertyUse wraps a Ref (existing case)\n- PositionalUse carries a `Position` enum (PositionSkip | PositionLimit), int-backed, stringer (mirrors graph.EntityKind)\n- `Parameter.Uses` type changes from []Ref to []Use\n- Invariant D4 relaxes: every Use is exactly one of PropertyUse | PositionalUse\n- Smart constructors enforce invariants\n- Tagged-union MarshalJSON with lowercase \"kind\": PropertyUse flattens to {\"kind\":\"property\",\"variable\":...,\"property\":...}; PositionalUse to {\"kind\":\"positional\",\"position\":\"skip\"|\"limit\"}\n\nAccept/reject delta from Stage 0:\n- SKIP $p (bare): reject → accept as PositionalUse{Skip}\n- LIMIT $p (bare): reject → accept as PositionalUse{Limit}\n- SKIP $p + 1, LIMIT f($p), ORDER BY $p, every other deferred-parameter construct: stay rejected\n- Accept rule mirrors WHERE D1 directness discipline: bare positional only\n\nDedup unchanged: Parameter dedup by Name in first-appearance order. Mixed-kind uses on one Parameter are fine; the resolver judges type unification post-freeze (parser stays schema-agnostic per ADR 0003).\n\nJSON casing wart: Parameter struct stays uppercase Go-default while Uses items go lowercase tagged-union. Acknowledged scope-creep-avoidance — defer harmonization to post-freeze.","acceptance_criteria":"- TCK scenarios in clauses/return-skip-limit/ exercising SKIP $p / LIMIT $p flip red→green\n- Layer 2 targeted sentinel tests cover still-rejected cases (SKIP $p+1, ORDER BY $p)\n- No previously-passing scenario regresses; pending count only decreases\n- Both gated-swarm reviewers (Linus + Sandi) approve the diff\n- `just test` green","notes":"Plan agreed via /grill-with-docs on 2026-05-26.\n\nReference docs:\n- ADR 0004 — test-first, evolving-until-feature-complete (governs staged approach)\n- ADR 0003 — schema-agnostic parser; resolver handles type unification\n- ADR 0005 — generated code executes original text; model is type-interface only\n- docs/specs/cypher-query-parser-stage-0.md — Stage 0 spec (canonical baseline)\n\nDeliverables in the same PR as the code:\n- docs/specs/cypher-query-parser-stage-1.md — short delta-style spec doc (~80-120 lines); structure mirrors Stage 0\n- CONTEXT.md gains a \"Use\" entry; \"Parameter\" refined to reference it\n- Stage 0 spec §4 C2 erratum: one-line note clarifying that label-conflict against the schema is the resolver's responsibility (the conjunctive constraint interpretation is faithful to openCypher, not a parser limitation)\n\nTCK dir widening: add clauses/return-skip-limit/ to the godog dir list. Expect 1-2 skiplist additions to surface during implementation (runtime-error assertions we cannot model).\n\nImplementation methodology: /tdd inside /gated-swarm (Carmack implements; Linus + Sandi review).","status":"closed","priority":1,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:05Z","created_by":"antranig-yeretzian","updated_at":"2026-06-04T01:08:31Z","started_at":"2026-05-26T04:38:17Z","closed_at":"2026-05-26T06:31:13Z","close_reason":"Stage 1 feature-complete: model evolution (Use sum + ClauseSlotUse), parser change (mineClauseSlotParameter for SKIP+LIMIT bare $p), TCK corpus widening (return-skip-limit/ + 18-entry skiplist + TestSkiplistOrphans), Layer-2 rule update (directional rule + AUTHORED pin for non-bare $p in SKIP/LIMIT), documentation bundle (Stage 1 spec + CONTEXT.md Use entry + Stage 0 §4 C2 note). All five cycles approved by both gated-swarm reviewers (Linus + Metz). just test green at 509/315/0/194.","external_ref":"https://github.com/areqag/gqlc/issues/16","dependency_count":0,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"gqlc-ude","title":"Cypher query.Parser: feature-complete the read surface","description":"Umbrella for the staged build-out of internal/query/cypher. Per ADR 0004: evolve query.Query through Stages 1-5 (test-first against the openCypher TCK), then lock the model and build the resolver / codegen / generated driver. No consumer is built until after the freeze.","status":"closed","priority":1,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:25:21Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T20:53:47Z","closed_at":"2026-07-03T20:53:47Z","close_reason":"Stale umbrella, substance delivered: Stages 1-5 (read core) closed under it long ago; the expanded surface (Stages 6-14 + sweep + freeze) was tracked by sibling epic gqlc-33k, now closed with the freeze (PR #81 / ADR 0008). The dependency edge on gqlc-0mx (resolver/codegen) was removed at close: that is post-freeze work this umbrella was never scoped to contain — it proceeds under gqlc-0mx directly. GH #17 superseded by #15's close.","external_ref":"https://github.com/areqag/gqlc/issues/17","dependencies":[{"issue_id":"gqlc-ude","depends_on_id":"gqlc-4n2","type":"blocks","created_at":"2026-05-26T00:26:15Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ude","depends_on_id":"gqlc-7z6","type":"blocks","created_at":"2026-05-26T00:26:18Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ude","depends_on_id":"gqlc-cta","type":"blocks","created_at":"2026-05-26T00:26:18Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ude","depends_on_id":"gqlc-npg","type":"blocks","created_at":"2026-05-26T00:26:16Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ude","depends_on_id":"gqlc-s2t","type":"blocks","created_at":"2026-05-26T00:26:16Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ude","depends_on_id":"gqlc-v23","type":"blocks","created_at":"2026-05-26T00:26:17Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":6,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-zzz5","title":"check-pr-closes: a malformed Bead: id silently passes the gate","description":"Found by the independent review of PR #867 (/tmp/factory/verdict-867.md), filed as\na nit. PRE-EXISTING -- identical behaviour on master before that branch, so #867\nneither introduced nor widened it.\n\n.github/scripts/check-pr-closes.py resolves the bead id from a 'Bead:' line in the\nPR body, then demands a matching 'Closes #N' for the GH issue that bead mirrors.\nIf the id does not resolve to a real bead the gate has nothing to demand, and\npasses.\n\nTwo spellings that reach that state:\n\n  Bead: gqlc-x.        a trailing dot / non-existent suffix\n  Bead: `gqlc-w4al`    backticked, so the id carries the backticks\n\nBoth are plausible typos rather than deliberate evasion -- markdown-inlining the id\nis a natural thing for an author to do -- which is what makes it worth closing. The\nfailure direction is the wrong one: an unresolvable id should be a refusal that\nNAMES the id it could not resolve, not silence.\n\nThe gate already fails closed on an unreadable body file and an unreadable export\n(shipped in #867), so the fail-closed posture exists and this is a gap in it rather\nthan a new principle.\n\nACCEPTANCE. A 'Bead:' line whose id does not resolve against the export makes the\ngate exit non-zero with a message naming the unresolved id. A row in\n.githooks/tests/check-pr-closes-test.sh for each of the two spellings above, and\nthe suite still FAILs (not merely runs) when the refusal is removed -- 'just\ntest-hooks' rc=1 propagating to 'just test' rc=1.\n\nCARE. Do not make this reject a PR that legitimately has no bead. The distinction\nis 'a Bead: line is present and does not resolve' vs 'no Bead: line at all', and\nthe branch-name fallback path must keep working.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:59:31Z","created_by":"areqag","updated_at":"2026-08-17T08:01:04Z","external_ref":"https://github.com/areqag/gqlc/issues/883","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-xdd0","title":"ci: a selective tidy re-run can leave lint/test/codegen-fence 'skipped' and mergeable","description":"Witnessed while landing gqlc-w4al (PR #867), 2026-08-17.\n\nOBSERVED. On PR #867 at head e9119f75 I removed the 'Closes #617' line. Run 32005420996 fired on the edit; its tidy job failed. Because lint, test and codegen-fence all declare 'needs: [actionlint, tidy]', GitHub marked all three SKIPPED, and a skipped check run has conclusion 'skipped', which branch protection reads as a PASS. Rollup at that moment:\n\n  tidy    COMPLETED FAILURE   \u003c- newest, blocks\n  lint    COMPLETED SKIPPED\n  test    COMPLETED SKIPPED\n  codegen-fence COMPLETED SKIPPED\n\nThat state itself is safe: tidy is red and blocks the merge.\n\nTHE HOLE. gqlc-w4al made tidy re-passable at an UNCHANGED SHA (the gate now reads the live PR body over the API, so adding the Closes line back turns tidy green with no new commit). That creates a sequence that was previously unreachable:\n\n  1. head H, body missing Closes -\u003e run R1: actionlint ok, tidy RED, lint/test/codegen-fence SKIPPED.\n     Note lint/test have now never run at H.\n  2. Add the Closes line. Run R2 fires on 'edited' and would re-run everything -- CANCEL it.\n  3. Re-run ONLY the tidy job of R1 ('gh run rerun \u003cR1\u003e --job \u003ctidy\u003e'). It fetches the live body, passes.\n  4. Newest per context: tidy SUCCESS, actionlint SUCCESS, lint/test/codegen-fence SKIPPED (= pass).\n     Mergeable green, with lint and test never having executed at H.\n\nBefore gqlc-w4al this was not reachable: the only tidy failure fixable without a push was the body gate, and the body gate could not re-evaluate at all. Every other tidy failure (tidy-check, bd-export-monotonic) needs a commit, which moves the SHA and re-runs everything.\n\nSEVERITY. Requires deliberate action by someone with write access: cancelling a run and selectively re-running one job. It is a bypass vector, not an accidental green. Step 3 is exactly the command I ran as the stale-payload witness, so it is not hypothetical -- I ran 'gh run rerun 32004817300 --job 95312044713' and got a fresh tidy check run on the same SHA.\n\nSHAPE OF A FIX (pick one, all have costs):\n- Drop 'needs: [actionlint, tidy]' from lint/test/codegen-fence. Loses the cheap-gates-first saving that the ci.yml header comment justifies (~3.5 min of runner time on a trivial formatting miss).\n- Move the PR-body gate into its own job with no dependents. Needs a new required context added to BOTH protection configs (classic branch protection and the 'protect-master' ruleset), which strands every open PR on a pending context until they are updated -- the same objection recorded in gqlc-2rli.\n- Add 'if: always()' to the dependents so they run and report a real verdict even when a gate fails. Costs the runner time the needs: chain was there to save.\n\nACCEPTANCE. A test that reaches the state in step 4 and asserts it is not mergeable, or a workflow shape where a skipped dependent cannot be the newest verdict for a required context at a SHA where the job never ran.\n\nRELATED. gqlc-2rli (separate-workflow shape + protection-config steps). The 'skipped reads as a pass' fact is already documented in ci.yml's trigger comment and in vuln.yml.","notes":"CORRECTION 2026-08-17, from the independent review of PR #867 (/tmp/factory/verdict-867.md).\n\n1. A SECOND HYPOTHESIS I RELAYED IS FALSE. I had suggested to the reviewer that a\n   path filter on ci.yml could produce the same skipped triple without any re-run,\n   which would make this bug far wider (a routine PR-description edit would suffice).\n   It does not. Measured: the 'needs:' lines are byte-identical to master, tidy\n   failed at 07:21:35 and the dependents skipped at 07:21:36. The cause is only\n   ever the needs: chain with a failed gate. The bead's own mechanism paragraph\n   was right; my widening was wrong. Severity stays \"deliberate action required\".\n\n2. THE RE-RUN SEMANTICS IN STEP 3 NEED RE-MEASURING. The reviewer found that\n   attempt 2 of run 32004817300 shows lint/test/codegen-fence with NEW job ids and\n   NEW 07:22:30 timestamps, while actionlint carried over unchanged at 07:12:54.\n   So 'gh run rerun --job \u003cone\u003e' RE-CREATES the dependents and RE-EVALUATES needs:,\n   rather than leaving the old skipped runs standing. In that attempt they skipped\n   again only because tidy had failed. Step 4 of the sequence above therefore\n   assumes something not yet demonstrated: that after a SUCCESSFUL selective tidy\n   re-run, the re-created dependents are still skipped rather than executed. If\n   they execute, the bypass does not exist and this bead should be closed. That\n   case was NOT run end-to-end by anyone; the reviewer states its account of it is\n   inference from job records, not execution.\n\n3. THE LOAD-BEARING PREMISE IS UNVERIFIED. \"a skipped check run ... branch\n   protection reads as a PASS\" is asserted here as fact and is repeated in ci.yml's\n   trigger comment and in vuln.yml. Nobody has probed it live, and the reviewer\n   found ZERO recorded cases across 40 merged PRs of a required context whose\n   newest run was skipped. If GitHub in fact treats skipped as unsatisfied, this\n   bead is empty. Settle this BEFORE building any of the three fixes -- all three\n   cost real runner time or strand every open PR on a pending context.\n\nWHAT SURVIVES REGARDLESS. The merge-precondition rule is unaffected and is the\ndurable output: evaluate newest check run per context over the seven required\ncontexts and require SUCCESS, rejecting SKIPPED, NEUTRAL and CANCELLED. Under any\nresolution of (3) that query is correct, because a dependency-skip at a SHA where\nthe job never ran is exactly the state a coordinator must not read as green.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:24:58Z","created_by":"areqag","updated_at":"2026-08-17T07:59:03Z","external_ref":"https://github.com/areqag/gqlc/issues/869","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lj9s","title":"ci: sweep-discovery-probes only recognises mktemp -d as a probe site","description":"`sweep-discovery-probes` (justfile) holds three copies of the probe names to\neach other: the `*_probe` variables, the `discovery_probes` concatenation, and\nthe sites that create the probes. It discovers sites by grepping `just --dump`\nfor one idiom:\n\n    site_re=\"mktemp -d \"\"test/data/\"\n\nSo a probe directory created under test/data by anything other than\n`mktemp -d` — `mkdir -p`, `install -d`, a Go helper this justfile invokes, a\ntest using t.TempDir with a TMPDIR pointed at test/data — is not a site to any\ncheck in the recipe. The declared names are not held to it, the site-shape\nrefusal does not see it, and if its spelling is not one of the declared names\nthe sweep glob does not clear it either.\n\nNot currently reachable: at the time of filing the only three probe sites in\nthe justfile are `mktemp -d test/data/{{ var }}.XXXXXX` (justfile:721, :832,\n:1191, PR #833 head). This is a limit of the check, stated in the tree beside\n`site_re`, not a live defect.\n\nClosing it means either (a) discovering sites from the paths that actually\nappear under test/data at runtime rather than from the justfile text, or\n(b) moving the probe creation behind one recipe/helper that the other recipes\ncall, so there is one site to hold rather than a pattern to match.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:20:36Z","created_by":"areqag","updated_at":"2026-08-17T07:29:06Z","external_ref":"https://github.com/areqag/gqlc/issues/870","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-k4h7","title":"lint: the justfile's shell is not shellchecked by any gate","description":"`just lint` runs `lint-hooks`, whose default argument is `dir=\".githooks\"` (justfile: `lint-hooks dir=\".githooks\"`, and `lint: ensure-golangci lint-hooks check-golangci-build-tags`). So every line of bash in the justfile itself — including the whole `vuln` recipe, several hundred lines of it, which is the body of a required CI context — has no static analysis behind it.\n\nWitness: extract the `vuln` recipe body and run the repo's own `.bin/shellcheck` 0.10.0 over it. It parses and reports SC2015 (info) on `lines() { [ -n \"${1}\" ] \u0026\u0026 printf '%s\\\\n' \"${1}\" || true; }`. Nothing in CI does that run, so the finding has been sitting there unreported.\n\nThe fix is not simply widening `lint-hooks` to the justfile: a just recipe body is not a shell script on disk (interpolation braces, per-recipe shebangs, dedent), so it needs an extraction step first. Scope: extract each shebang recipe body, shellcheck it, fail the gate on new findings, and either clear or baseline the pre-existing SC2015.\n\nFound while reviewing PR #846 (bd gqlc-u91z), which added ~130 lines of unlinted bash to that recipe.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:54:02Z","created_by":"areqag","updated_at":"2026-08-17T06:55:04Z","external_ref":"https://github.com/areqag/gqlc/issues/865","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-agt0","title":"vuln: the unplaced-stdlib witness block has no witness of its own","description":"In `just vuln`, five assertions inside the unplaced-stdlib witness block survive deletion because each one passes on a good tree, so removing it changes nothing observable.\n\nMeasured first-party at 74b08e4e, each mutation run alone with `just --summary` + `bash -n` asserting the artifact stayed valid:\n\n- M9: `expect_refusal`'s marker check `*\"${marker}\"*)` -\u003e `*)` — SURVIVED rc=0. The clause that asserts WHICH refusal fired can be deleted; M8 and M11 are killed by it today, so it is live, but nothing regresses it.\n- M13: delete the first `expect_refusal` call (bare 'go') — SURVIVED rc=0.\n- M14: delete the second `expect_refusal` call (absent header) — SURVIVED rc=0.\n- M16: the witness-quotes-the-header `case` -\u003e `*) ;;` — SURVIVED rc=0.\n- M19: the per-module tally guard `if [ -n \"${ungraded}\" ]` -\u003e `if false` — SURVIVED rc=0 on a release toolchain (it IS killed under GOVERSION=go1.26.5-X:nodwarf5, row B19, by the grading's own exit).\n\nThis is a check-on-a-check regress that terminates. It is second-order — each clause is demonstrably load-bearing on the paths that do run — but a deleted assertion here is invisible, which is the shape this whole gate exists to catch.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:53:52Z","created_by":"areqag","updated_at":"2026-08-17T06:55:04Z","external_ref":"https://github.com/areqag/gqlc/issues/866","dependencies":[{"issue_id":"gqlc-agt0","depends_on_id":"gqlc-65sl","type":"relates-to","created_at":"2026-08-17T03:54:34Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-agt0","depends_on_id":"gqlc-eo46","type":"relates-to","created_at":"2026-08-17T03:54:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vs9i","title":"codegen/age: the live battery never writes an offset sidecar","description":"DEFECT: the AGE live smoke battery exercises the instant round trip but never puts an \u003cf\u003eOffset property in the graph, so the emitted agtypeZone is only ever live-witnessed on its absent-key branch. Neither the in-range zoned read nor the refusal added by gqlc-x5wx (PR #848) has a live witness — both are pinned only by the corpus harness, which runs the emitted helpers against hand-built props maps rather than against agtype coming back from Apache AGE.\n\nWITNESS: run 32000835738 (codegen-live, headSha 619c7186) is green with live-smoke-age success. The scenario it drives is readScenarios' \"timestamp_property_roundtrip: instant round trip + ordering\" (test/data/codegen/live_age_test.go), whose oneEvent CREATE binds the instant alone. grep for \"Offset\" across test/data/codegen returns no write. So the live arm proves the absent-sidecar path returns the instant unchanged in UTC, and says nothing about the two paths PR #848 changed.\n\nWHAT WOULD CLOSE IT: a live scenario whose CREATE sets both the instant property and its \u003cf\u003eOffset sidecar, asserting (a) an in-range offset comes back as the zone on the decoded time.Time, and (b) an out-of-range one (86400) fails the decode with an error naming the property. The second half needs the harness to admit an expected-error scenario, which readScenarios does not model today.","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:47:34Z","created_by":"areqag","updated_at":"2026-08-17T06:48:02Z","external_ref":"https://github.com/areqag/gqlc/issues/859","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-grbh","title":"codegen/age: agtypeInstant takes any int64 as epoch-micros","description":"DEFECT: the emitted agtypeInstant (internal/codegen/age/render_models.go) reads the stored int64 and calls time.UnixMicro on it with no bound, so a property holding math.MaxInt64 decodes to a year ~294000 instant and is returned without error. Sibling of gqlc-x5wx, which bounded the \u003cf\u003eOffset sidecar.\n\nWITNESS: the emitted helper is\n\n\tmicros, err := agtypeInt64(raw)\n\t...\n\treturn time.UnixMicro(micros).UTC(), nil\n\nwith no range test between the two. The corpus test TestAgtypeZoneRefusesAnOffsetOutsideADay (internal/codegen/age/testdata/corpus_test.go.txt) refuses math.MaxInt64 as an offset; the same value as micros is accepted by agtypeInstant.\n\nWHY IT WAS DEFERRED ON PR #848 rather than fixed there: the two are not the same shape.\n1. The micros count IS the instant. Any bound on it is a calendar-range policy decision, not a well-formedness one, and there is no equivalent of \"a zone no clock keeps\" to key it on. The live corpus deliberately carries year 9999 and pre-epoch instants (test/data/codegen/valid/timestamp_property_roundtrip), so any candidate bound has to be argued against rows that already exist.\n2. gqlc's own encode writes the micros. It is a value this encoding produced and read back, so it is not the untrusted external input the sidecar is — the sidecar is the interop affordance for graphs gqlc did not write.\n\nReachability is therefore lower and the fix needs a policy decision (what calendar range does this backend admit?) that belongs in its own bead, not smuggled into a bounds fix.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:47:22Z","created_by":"areqag","updated_at":"2026-08-17T06:48:03Z","external_ref":"https://github.com/areqag/gqlc/issues/860","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-qx5p","title":"codegen: the swept-file partition is complete but its two sides are unmeasured — moving a fixed file to inputDerivedFiles silences the sweep","description":"`internal/codegen/prepare_test.go` (PR #845, `TestEveryEmittedFixedDeclarationIsReserved`) partitions the golden corpus in two: `fixedDeclarationFiles` = `{db.go, graph.go, querier.go}`, whose exported declarations must each carry a `reservedIdentifiers` row, and `inputDerivedFiles` = `{models.go}` plus the `*.cypher.go` suffix, whose declarations follow from the batch and owe nothing. Every golden must fall in one side or the other, so an emitted file nobody classified fails.\n\nTHE HOLE IS THE SIDE A NAME IS PUT ON. Membership in `inputDerivedFiles` is asserted, never measured. Moving a fixed file to that side silences the sweep for it and passes the completeness check, because the file is still classified.\n\nWITNESS (first-party, detached worktree at `c4642404`, `go vet ./internal/codegen/` printing BUILD OK before the test ran):\n\n    fixedDeclarationFiles = {\"db.go\": true, \"querier.go\": true}\n    inputDerivedFiles     = {\"models.go\": true, \"graph.go\": true}\n\n    test exit=0 · kill-count(top-level FAIL)=0 · ok github.com/areqag/gqlc/internal/codegen 35.216s\n\n`graph.go` is the Apache AGE-only file: `SessionInit`, `EnsureGraph`, `DropGraph`. All three lose their obligation and the package stays green. This is the same fail-silent family as the aggregate `swept` counter #845 replaced — one edit narrower.\n\nFIX DIRECTION: derive the side rather than declaring it. The property that separates them is measurable in the corpus already: a fixed file's exported names are the emitter's own, so the set it declares is drawn from a fixed vocabulary across every fixture of a target, while an input-derived file's names track the fixture. `graph.go` declares the same three names in all 45 AGE goldens; `models.go` declares `Person` in one fixture and `Movie` in another. A file classified input-derived whose exported names never vary across the fixtures of a target is misclassified, and that is a check, not a claim.\n\nRELATED (nit N2, independent review of #832): there are now three hand-maintained emitted-file lists, two of them of opposite polarity — `conformance_test.go:638` `connectionSurface` (exclusion, `{db.go, graph.go}`), `prepare_test.go` `fixedDeclarationFiles` (inclusion) and `inputDerivedFiles` (exclusion). A derivation would retire two of the three.\n\nACCEPTANCE: the witness above turns a NAMED test red, with `go vet` clean before the test runs so the kill is not the compiler.\n\nPROVENANCE: found and measured while fixing blocker 1 of the independent review of PR #845, 2026-08-17. The blocker's own prescribed fix (count per basename) does not kill the mutation that motivated it; pinning the file set to the corpus does, and this is what that pinning still cannot see.\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:44:22Z","created_by":"areqag","updated_at":"2026-08-17T06:48:03Z","external_ref":"https://github.com/areqag/gqlc/issues/861","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-1ekq","title":"check-pr-closes forces a Closes on any PR whose branch or body names a bead, including PRs that only change its status","description":"`.github/scripts/check-pr-closes.py` derives the bead a PR is about from a\n`Bead:` line in the body, falling back to the branch name, and then requires\nthe body to carry `Closes #N` for that bead's GitHub mirror. The gate has no\nway to say \"this PR touches the bead but does not resolve it\".\n\n## Witness\n\nPR #609 reopened bead `gqlc-4np` (GH issue #503). Its branch happened not to\ncontain the id, so the gate stayed silent. Had the branch been named after the\nbead — the repo's own convention — the gate would have demanded `Closes #503`\non a PR whose whole purpose was that #503 should be open, and merging it would\nhave closed the issue it had just reopened.\n\nChoosing a branch name that dodges the id is not a control: it is invisible,\nunenforced, and it also disables the gate for PRs that genuinely should close\ntheir issue.\n\n## Why it was left out of gqlc-w4al\n\ngqlc-w4al fixed re-evaluation (`edited` in ci.yml's `pull_request` types) and\nthe frozen-payload read (the body is now fetched from the API). Both make the\ngate harder to get past. This one pulls the other way: any opt-out marker is\nby construction a one-line bypass, and now that the gate re-evaluates on every\nbody edit, an opt-out can be added to a body at any point up to merge. What\nthe marker should be, and what stops it being used to wave a PR through, is a\ndesign decision that deserves its own review rather than a fold-in.\n\n## Shape of a fix\n\nNeeds to decide, at minimum:\n- the marker's spelling and where it may appear (e.g. `Bead: gqlc-x (refs)` vs\n  a separate `Refs:` line vs a label);\n- whether an opt-out PR must still say something checkable — e.g. that the\n  bead is not `in_progress`/`closed` in `.beads/issues.jsonl`, or that the PR\n  demonstrably does not close it — so the marker is not simply \"trust me\";\n- whether a label is better than body text, since labels are events\n  (`pull_request: labeled/unlabeled`) and are attributable.\n\n## Acceptance\n\n- A PR that changes a bead's status without resolving it can name that bead\n  and merge without `Closes #N`, and does so via a declared marker rather than\n  by avoiding the id in the branch name.\n- The marker cannot be used to skip the gate on a PR that does resolve its\n  bead without that being visible in the diff or the check's output.\n- `.githooks/tests/check-pr-closes-test.sh` gains a row per new verdict.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:40:40Z","created_by":"areqag","updated_at":"2026-08-17T06:48:04Z","external_ref":"https://github.com/areqag/gqlc/issues/862","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nrao","title":"codegen/neo4j: no live scenario decodes a nested list, so the depth-suffixed decoder arms have never met a real Bolt server","description":"PR #822 (`0a139b74`, bd gqlc-ye6g) changed the emitted decoder for nested list\ncolumns: `walkListElemBody`'s `inner`/`innerAcc` and `walkListElemPlan`'s `elem`\nare now suffixed by nesting depth via `elemLocal`. The review of that PR recorded\n\"the emitted decoders were never exercised against a real Bolt server\" as an\nunverified gap, attributing it to Docker being down on the review box.\n\nDocker is not the binding constraint. I dispatched the live workflow on a branch\ncarrying that emitter (run 32001875182, `gh workflow run codegen-live`) and both\njobs went green with a REAL execution — 47.3s wall, zero `(cached)` lines, zero\n`no tests to run` lines. It still witnesses nothing about this change.\n\nWITNESS — the live battery has no nested-list shape in it:\n\n    grep -rn 'list_list_int|schema_list_property_widths|schema_list_any_property' \\\n      test/data/codegen/*.go\n    -\u003e no matches\n\n    grep -rn '\\[\\]\\[\\]' test/data/codegen/live_test.go\n    -\u003e no matches\n\n`TestLiveSmoke` (test/data/codegen/live_test.go:267) iterates `readScenarios`\n(:235-248) plus `edgeUnionScenarios` (:257). The full scenario list is:\n\n    one_col_one_param_one, many_col_many, entity_node_projected_one,\n    entity_edge_projected_one, mixed_read_write_batch,\n    timestamp_property_roundtrip, edge_union_undeclared_relationship_type\n\nNone projects a `LIST\u003cLIST\u003c...\u003e\u003e`. The goldens that DO contain `innerAcc` /\n`inner1` — `list_list_int`, `schema_list_property_widths`,\n`schema_list_any_property` under test/data/codegen/valid/ — are driven by the\nconformance golden sweep and by `just test-codegen-fence`, which COMPILE and\nLINT them against the real driver but never execute them against a server.\n\nSo the depth-suffix change is covered on three axes (golden text, compilation\nagainst the real neo4j driver, and the corpus driver-stub battery\n`TestEmittedDecodersRunOnDriverValues`) and on zero live ones. Nothing has ever\nhanded a real Bolt `[]any` of `[]any` to `decode...` and read the result back.\n\nThis is distinct from gqlc-df3d, which is about the `-run` allowlist mechanism\npreventing a NEW live test from being selected. Here the test does not exist at\nall; adding one would also have to clear df3d's allowlist to actually run.\n\nACCEPTANCE: a `readScenarios` entry that seeds a nested list property (depth \u003e= 2,\nso the emitted decoder declares `inner1`/`innerAcc1`), reads it back through a\ngenerated method, and asserts the round-tripped value; selected by the live\nrecipes' `-run` regex; witnessed executing in a `codegen-live` run with no\n`no tests to run` line and no `(cached)`. Reverting `elemLocal` to `return name`\nshould redden it.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:32:47Z","created_by":"areqag","updated_at":"2026-08-17T06:34:08Z","external_ref":"https://github.com/areqag/gqlc/issues/856","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-offa","title":"spec fence: a driver binding written without map[string]any{ is unswept","description":"The binding sweep's anchor is the literal `map[string]any{` — the opening brace is part of the anchor. A spec that states a binding as a bare code span, with no literal around it, is not read at all.\n\nWitness (measured 2026-08-17 on PR #804, head dbeabc3a, and still live): inserting into docs/specs/codegen-stage-c1.md §5.3 the bullet\n\n    - The driver binding is `\"minAge\": minAge` inside the map literal.\n\nleaves TestSpecParamsMapBindsGeneratorOwnedValue PASS. That line documents the capture vector gqlc-lhs3 removed from the emitter, in the section gqlc-rz0l exists to correct, and the fence says nothing.\n\nThe symmetric signature escape was closed on that branch by anchoring a second scanner on a backtick before the context parameter (ADR 0029 decision 10). The same move was declined for bindings after measurement: the swept documents hold 130+ bare `\"key\": value` code spans across 21 files, essentially all JSON model shapes with no relation to a driver binding, so a brace-less recogniser would fire on prose across the corpus. See ADR 0029 decision 10.\n\nClosing this needs something narrower than a bare key/value recogniser — e.g. grading only spans whose key matches a parameter name the surrounding section declares, or requiring specs to print bindings only inside the literal and fencing that requirement.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:25:15Z","created_by":"areqag","updated_at":"2026-08-17T06:28:25Z","external_ref":"https://github.com/areqag/gqlc/issues/849","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-wdo7","title":"codegen/neo4j: the decoder probe stops at two levels of list nesting","description":"`decoderProbeWidths` in `internal/codegen/neo4j/decoder_test.go` derives, for each scalar the type table admits, that scalar plus `LIST\u003cscalar\u003e` plus `LIST\u003cLIST\u003cscalar\u003e\u003e`. Two levels is the first depth that reaches `writeSliceNarrow`'s recursive arm at all: `nested0`, `acc0`, `elem1`, `i1` and `v1` enter the swept set there and are absent at one level.\n\nBut no finite depth closes this, because the emitted names are depth-indexed (`internal/codegen/neo4j/render_models.go:327-359`):\n\n    item := fmt.Sprintf(\"elem%d\", depth)\n    idx  := fmt.Sprintf(\"i%d\", depth)\n    inner := fmt.Sprintf(\"nested%d\", depth)\n    acc   := fmt.Sprintf(\"acc%d\", depth)\n    ... fmt.Fprintf(b, \"%sv%d, ok := ...\", body, depth)\n\nSo a schema declaring a three-deep list emits `elem2`, `i2`, `nested1`, `acc1` and `v2` — five identifiers the sweep has never seen, and a property named after any of them would collide undetected. Depth 4 adds five more. Extending the probe to depth 3 only moves the boundary.\n\nThe fix is different in kind from widening the probe: pin the *family* rather than the set. Either derive the guard from the format strings (no emitted local may be `fmt.Sprintf`-derived from a property name), or assert that the depth-indexed families are closed — that for every name in the swept set of the form `\u003cstem\u003e\u003cn\u003e`, no property may be named `\u003cstem\u003e\u003cm\u003e` for any m.\n\nFiled from gqlc-320w, which reaches depth 2 and states the limit in the `decoderProbeWidths` doc comment rather than hiding it.","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:24:30Z","created_by":"areqag","updated_at":"2026-08-17T06:28:26Z","external_ref":"https://github.com/areqag/gqlc/issues/850","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2l8v","title":"codegen/neo4j: TestTypeMapProperty leaves ANY VALUE and the bare LIST unswept","description":"`internal/codegen/neo4j/types_test.go`'s `TestTypeMapProperty` enumerates 29 of the 31 `graph.PropertyType` constants: 21 representable widths plus the 8 unrepresentable ones. `graph.TypeAnyPropertyValue` and `graph.TypeList` are named nowhere in that file, so the two arms of `typeMap.Property` that answer `any` and `[]any` have no unit row behind them.\n\nFound while doing gqlc-320w, which built a walk (`propertyArmNames` in `internal/codegen/neo4j/decoder_test.go`) that reads the case expressions of `typeMap.Property` off types.go. That walk already gives `TestTypeMapProperty` the derivation it needs: hold its case table to naming every constant the switch has an arm for, rather than to a hand list.\n\nMeasured 2026-08-17 on fix/neo4j-decoder-probe-derives-coverage:\n  grep -o 'graph.Type[A-Za-z0-9]*' internal/codegen/neo4j/types_test.go | sort -u | wc -l  -\u003e 29\n  the same over internal/graph/propertytype.go                                             -\u003e 31\n\nNot a defect in emitted code — both arms are exercised end to end by the decoder probe added in gqlc-320w and by the golden corpus. It is a hole in the unit-level table test.","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:24:20Z","created_by":"areqag","updated_at":"2026-08-17T06:28:26Z","external_ref":"https://github.com/areqag/gqlc/issues/851","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-sxzj","title":"resolver: Phase B commits an unlabelled binding to one type through a far end that does not cover","description":"On test/data/resolver/invalid/schemas/satisfy_plural_edges_inline_subtype.gql:\n\n  MATCH (p:Person)-[q:WORKS_AT]-\u003e(c)\n  OPTIONAL MATCH (c)-[h:HAS_DESK]-\u003e(d:Desk)\n  MATCH (c)\u003c-[w:WORKS_AT]-(x)\n  RETURN x.personOnly\n\naccepts {\"type\":\"STRING\",\"nullable\":false}. The Employee\u0026Person -[WORKS_AT]-\u003e Company\u0026Large row matches with h/d null, so c is Company\u0026Large, the only WORKS_AT into it comes from Employee\u0026Person, and that x has no personOnly. A NOT NULL column that is null.\n\ncandidateTypes computes c = {Company,Company\u0026Large} INTERSECT {Company} = {Company} with covered=false, because the HAS_DESK hop is OPTIONAL and witnessesItsEndpoints refuses it. inferUnlabelled then writes resolved[c]=Company anyway and merely withholds the resolvedCovers mark. The covering lane gates what OTHER bindings learn from c; it does not stop c itself being committed to a strict subset of the attainable types, and x is then inferred singularly from that wrong c and projected directly.\n\nSo the resolver has two different notions in one map: 'the type' and 'the types a row can have'. The narrowing respects the difference; the projection path does not.\n\nSame soundness class as gqlc-3uof but a different mechanism — 3uof was endpointLabels under-enumerating, this is Phase B committing on an admittedly-incomplete intersection. Closely related to the unlabelled-binding axis in gqlc-pv0u / gqlc-h6h7, and to gqlc-856f / gqlc-edze (ResolvedType is not a sealed sum), since the honest answer here is probably a plural node type rather than a refusal.\n\nWitness for any fix: the query above must not offer x.personOnly as NOT NULL. The one-token twin with the OPTIONAL made mandatory must stay accepted — every returned row then really does have a HAS_DESK out of c, HAS_DESK is declared from the bare Company only, and the chain is sound.","notes":"Found while fixing gqlc-3uof/gqlc-qlr2. PRE-EXISTING and identical on origin/master 5718a935 and on fix/resolver-inline-endpoint-satisfying-set; NOT introduced by that branch.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:19:26Z","created_by":"areqag","updated_at":"2026-08-17T06:28:27Z","external_ref":"https://github.com/areqag/gqlc/issues/852","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-yb0w","title":"resolver: a refusal-sentinel change inside a FAIL-\u003eFAIL cell is invisible to the corpus sweep, so a worse diagnostic can land green","description":"The strongest evidence for PR #806 (merged `aade7a40`, bead gqlc-0tft) was a corpus sweep: 310 queries x 28 schemas = 8680 cells, branch vs base, reporting 8 FAIL-\u003ePASS, 2616 PASS-\u003ePASS with every golden byte-identical, and zero accept-\u003erefuse / accept-\u003edifferent-answer. That is a strong result and it is why #806 was sound to land.\n\nIt has a hole, and the hole is a whole quadrant. The sweep compares VERDICTS (accept/refuse) and golden BYTES. It does not compare error strings. So of the 8680 cells, the **6056 that were FAIL before and FAIL after are compared on nothing but the fact of failing.** A change that swaps which sentinel a refusal returns — or degrades a precise diagnostic into a generic one — moves no cell between quadrants, changes no golden byte, and is therefore invisible.\n\nThis is not hypothetical. The reviewer found one by hand while spot-checking: a refusal that changed from `ErrAmbiguousLabel` to `ErrUnknownEdge`. That instance is an IMPROVEMENT, so nothing is broken today. But it was found by hand, and the reviewer stated plainly it 'cannot bound the rest' — nobody knows how many of the 6056 changed sentinel, or in which direction.\n\nWHY THIS MATTERS BEYOND #806: this is the sweep methodology the resolver work keeps using to prove a narrowing change is safe. Every future use inherits the same blind spot, and diagnostic quality is exactly what a narrowing change is most likely to perturb — narrowing changes which check fires first.\n\nSame family as gqlc-fd06, gqlc-x5dq, gqlc-320w: a guard that is structural on one axis and blind on another, so it defends the shapes it was written alongside.\n\nFIX: have the corpus sweep record the sentinel (or the full error string) per refusing cell and diff that too, so a FAIL-\u003eFAIL cell whose sentinel changed is reported as a third category rather than folded into 'no change'. Then the sweep's own output states which of the three it is: same verdict same sentinel, same verdict different sentinel, different verdict.\n\nACCEPTANCE: deliberately change one refusal's sentinel in the resolver, run the sweep, and confirm it REPORTS the change rather than printing an all-clear. Today it does not — that is the defect.\n\nDo the before/after drill in both directions, and watch for the fake RED: a sweep that errors out because the harness cannot parse a changed message is not the same as one that detects the change.\n\nPROVENANCE: the 'Not verified' section of the independent Linus review of PR #806, 2026-08-17, /tmp/factory/verdict-806.md. Filed rather than folded in; #806 had zero correctness blockers and this is a gap in the verification method, not in the shipped behaviour.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:01:15Z","created_by":"areqag","updated_at":"2026-08-17T06:02:00Z","external_ref":"https://github.com/areqag/gqlc/issues/841","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-fd06","title":"codegen: reservedIdentifiers is a hand-maintained list — a new emitter-fixed exported name is not reserved and the collision gate fails open","description":"PR #832 (merged as `5718a935`, bead gqlc-e6mh) added `reservedIdentifiers` in `internal/codegen/prepare.go:49` — 12 rows, 9 `scopePackage` + 3 `scopeMethod` — so a schema element named `Queries`/`DBTX`/`ErrNoRows`/... is refused instead of emitting a non-compiling package.\n\nThe set is CORRECT today. An independent `go/parser` sweep over all 153 golden target trees found exported package-level fixed decls == exactly the 9 `scopePackage` rows and exported `*Queries` methods == exactly the 3 `scopeMethod` rows. There is no under-broad hole at this commit.\n\nTHE DEFECT IS THE MAINTENANCE PROPERTY, NOT THE CURRENT CONTENTS. The list is hand-maintained and nothing holds it against the emitters. A future backend or emitter change that introduces a new exported package-level fixed declaration WITHOUT adding a `reservedIdentifiers` row leaves the gate green, and the collision it was built to refuse silently returns.\n\nWITNESS (independent reviewer on #832, reproduced first-party): appending `type Config = any` to every golden's `db.go` leaves the whole suite green. `Config` is now an emitter-fixed exported package-level name that no reserved row covers, so a schema entity named `Config` would once again emit a package that does not compile — exit 0, broken Go, which is exactly the gqlc-e6mh defect.\n\nThis is the fail-open shape this repo keeps getting bitten by: the gate keeps compiling, keeps passing its own tests, and stops covering the thing that was just added. Compare gqlc-x5dq (offset-sidecar gate keyed on the carrier text) and gqlc-eo46 — same family.\n\nFIX (reviewer estimated ~6 lines): derive the obligation instead of listing it. `db.go`/`graph.go`/`querier.go` hold exactly the emitter-fixed declarations, and `conformance_test.go:638` already names a file set (`connectionSurface = map[string]bool{\"db.go\": true, \"graph.go\": true}`) — note `querier.go` is NOT in that map today, so the fix must establish its own file set rather than reuse that one. Walk those files' ASTs across the golden targets, collect exported package-level decls and exported `*Queries` methods, and assert set equality against `reservedIdentifiers`. Then adding an emitter-fixed name without a reserved row goes RED naming the missing identifier.\n\nACCEPTANCE: appending `type Config = any` to the goldens' `db.go` (the witness above) turns a NAMED test red. Mutate it to confirm it is not a fake RED — the tree must still compile when the assertion fires, so the kill must carry a real test name, not a blank one.\n\nPROVENANCE: nit N1, independent Linus review of PR #832, 2026-08-17. Filed rather than folded in, per this repo's preference for a well-filed follow-up over a rushed fold-in.","notes":"READY PATCH, not pushed. gqlc-e6mh's session had already implemented this\nbefore the stand-down; the work is verified but deliberately unpushed.\n\n  /tmp/factory/e6mh-nits-fd06-xv62.patch  (git format-patch, applies to 5718a935)\n  also on local branch fix/codegen-reserved-set-completeness in worktree\n  /home/antranig/Developer/gqlc/gqlc-e6mh (commit 660fe68a)\n\nThe patch also covers gqlc-xv62 (one line of the spec table); split it if\nthe beads are worked separately.\n\nSHAPE: internal/codegen/prepare_test.go gains fixedDeclarationFiles =\n{db.go, graph.go, querier.go} — established locally, NOT reused from\nconformance's connectionSurface, which omits querier.go as this bead\nnotes — plus TestEveryEmittedFixedDeclarationIsReserved. It walks those\nfiles across the golden corpus and requires a reservedIdentifiers row for\nevery exported declaration, package-level or method.\n\nSet equality falls out across the two tests rather than inside one:\ndeclarations -\u003e rows is the new test, rows -\u003e declarations is check 3 of\nTestReservedScopeMatchesTheEmittedGoldens, which #832 already landed.\nMembership only in the new one; the scope column is held by the existing\nsweep for any name once it is a row, so re-asserting it here would only\nduplicate that fail.\n\nMUTATIONS, all in a detached worktree at the commit, all BUILD OK first\nso none is a fake RED, all killed by the NAMED test:\n  Q1 this bead's own witness — `type Config = any` appended to every\n     golden db.go                                      -\u003e KILLED\n  Q2 the DBTX row deleted from reservedIdentifiers      -\u003e KILLED\n  Q3 goldenCorpusGlob matches nothing (vacuity)         -\u003e KILLED\n  Q4 fixedDeclarationFiles names no emitted file        -\u003e KILLED\n\nGATES on 9dc1a8ee (same tree): just fmt-check 0, just lint 0 issues,\nfull just test green. Not run: test-codegen-live* (docker down locally).\n\nProbed before writing: exported decls in those three files across the\nwhole corpus are exactly the 12 reserved names, no more, and the scopes\nmatch the table (SessionInit is a func, not a method).","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:48:47Z","created_by":"areqag","updated_at":"2026-08-17T05:51:23Z","external_ref":"https://github.com/areqag/gqlc/issues/838","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4xto","title":"vuln gate: ADR 0026 overclaims the build-tags check, and a leaked discovery probe wedges four gates","description":"Two findings from the independent adversarial review of PR #813, raised after it\nhad already squash-merged as 7dfcf04e. Fixed in PR #833.\n\n## 1. ADR 0026 claims a coverage the recipe denies\n\ndocs/adr/0026:118-119 said `check-golangci-build-tags` holds `.golangci.yml`'s\n`run.build-tags` and the tree to each other \"in both directions\". justfile:172,\nfrom the same change, says it compares in ONE direction \"because the other one\ncannot report anything\".\n\nThe recipe is right. An undeclared term in the tree is refused by the derivation\n— `classify` returns `classUnknown`, the caller errors naming the file — so it\nnever survives to reach a comparison in the recipe. Flip the recipe's `comm` and\nthe remaining direction is empty by construction, which is why it injects a\nstale term of its own on every run.\n\n\"still holds\" was false as well: the recipe was new on that branch.\n\nThis is the two-halves defect (bd gqlc-eo46) expressed in prose, in the artefact\nsomeone reads right before deleting `return classUnknown`.\n\n## 2. `goDirs` asserted a universal the same branch falsifies\n\nmain.go said a Go module with no directory holding a Go file \"is not a thing\".\nThe branch creates exactly that three times per run — the discovery probes are a\ngo.mod and nothing else.\n\nMeasured: plant test/data/{vuln,fence,xtest}probe.LEAKED and\n\n    check-golangci-build-tags  rc=1      check-codegen-external-tests  rc=1\n    test-codegen-fence         rc=1      vuln                          rc=1\n\nall four blaming \"the walk broken rather than the module empty\" — a misdiagnosis\nof the repo's own artefact. .gitignore already anticipated the leak.\n\nFixed in two parts: the refusal stays but states the negative it can support and\nnames the leaked probe as the first cause with its remedy; and a shared\n`[private] sweep-discovery-probes` dependency clears residue before any of the\nfour recipes calls modscope. Shared and covering all three names because the\nrecipe that breaks is not the one that leaked — test-codegen-fence depends on\ncheck-codegen-external-tests, so a leftover fenceprobe takes out the dependency\nfirst. A per-recipe, per-name sweep was tried and measured still wedging the\npair.\n\n## Rejected during the fix\n\nExtending the probe traps to INT/TERM/HUP. A trap on INT runs the handler and\nRESUMES, so Ctrl-C would delete the live probe and let the recipe continue\nasserting things about it. Bash was measured running the EXIT trap on SIGTERM\nregardless. Traps stay EXIT; the sweep covers SIGKILL, which no trap can.\n","notes":"ROUND 2 (2026-08-17). The independent review of PR #833 at 880a825f returned\nREVISE with three blockers, all of them defects the FIX introduced rather than\nones it failed to fix. It confirmed the two originals are genuinely closed and\nthat the headline gqlc-s3lt empty-walk claim is a real RED (four named FAILs,\ntree still compiles, so not a fake RED).\n\n1. The sweep recipe was inserted between check-golangci-build-tags and its\n   45-line doc block, so `just --dump` showed that recipe's doc as None and the\n   build-tags prose as documentation for a three-line rm -rf. Moved below, blank\n   lines restored; verified by diffing the whole recipe-\u003edoc map against master.\n\n2. The sweep glob was a FOURTH hand-maintained copy of the probe names, held to\n   nothing. Renaming the fenceprobe mktemp literal left `just test-codegen-fence`\n   green with the sweep no longer covering it — this bead's own defect class\n   (gqlc-oxne), and refused by the standing rule in the comment block the sweep\n   had been parked under. Names now come from one variable block; .gitignore,\n   which cannot read them, is held by a `git check-ignore` witness inside the\n   sweep that reddens on a rename. The lookup counts its own calls (gqlc-eo46:\n   a working guard invoked zero times passes as quietly as a broken one).\n   Seven mutation rows KILLED, each naming the right cause.\n\n3. The ADR paragraph added to fix the \"both directions\" contradiction introduced\n   a universal of its own: \"a term the key does not declare is refused by the\n   derivation\". False — classify checks four vocabularies in order and the key\n   decides only the classCustom arm. Witnessed on a scratch root: hurd, linux,\n   go1.24 and unix all exit 0 undeclared; only zzmystery is refused.\n\nFixed in 2f75d284 (ADR) and d17b81c5 (justfile + main.go comment nits).\n\nReported honestly, not fixed: two mutation rows SURVIVE — deleting the\ninvocation counter itself (a second-order guard; guarding it starts a regress)\nand silencing the sweep's firing-case echo (observability, not a gate).","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:34:10Z","created_by":"areqag","updated_at":"2026-08-17T06:12:58Z","external_ref":"https://github.com/areqag/gqlc/issues/834","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-c7o7","title":"vuln/modscope: 'A Go module with no such directory is not a thing' is falsified by its own harness","description":"MERGED WITH THIS DEFECT KNOWN. PR #813 (7dfcf04e) landed on master while this was an open Linus blocker — auto-merge fired on green CI ahead of the review verdict. Filing so it is not lost.\n\nTHE UNIVERSAL, both on origin/master:\n  internal/tools/modscope/main.go:284 — '// is not a thing, and the postcondition this walk feeds — every directory'\n  internal/tools/modscope/main.go:324 — '\"file. A Go module with no such directory is not a thing, so this is the walk \"+'\n\nWHY IT IS FALSE: the branch that wrote it creates exactly that shape THREE TIMES PER RUN. Its own probe harness does mktemp -d plus a lone go.mod, producing a Go module whose directory holds no Go file. The code asserts the shape cannot exist while the test suite manufactures it on every invocation. This is the repo's recurring 'a branch breaks its own rule' pattern, and the standing rule is that a cannot/never/every in prose which the branch's own rows falsify is a blocker: state the negative and enumerate the limits instead.\n\nSECOND, OPERATIONAL DEFECT IN THE SAME AREA: a probe module that leaks — left behind by a killed or interrupted run — wedges 'just lint' across FOUR gates, because the stray go.mod is discovered as a module. That is a self-inflicted outage with no cleanup guarantee. The probe modules need to be created somewhere the walk does not reach, or removed under a defer/trap that survives a kill.\n\nWITNESS: adversarial review of PR #813, blocker 2. Re-verified first-party against origin/master after the merge with:\n  git grep -n 'is not a thing' origin/master -- internal/tools/modscope/\n\nFIX: replace the universal at :284 and :324 with the measured negative and its limits, and make the probe modules unable to leak into the walk. Note the :324 text is an ERROR MESSAGE a user can see, not just a comment — so this is user-facing prose asserting something the tool's own tests contradict.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:30:51Z","created_by":"areqag","updated_at":"2026-08-17T05:32:04Z","external_ref":"https://github.com/areqag/gqlc/issues/830","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-9kuf","title":"vuln: ADR 0026 says the register is compared 'in both directions', justfile:172 says ONE","description":"MERGED WITH THIS DEFECT KNOWN. PR #813 (7dfcf04e) landed on master while this was an open Linus blocker — auto-merge fired on green CI ahead of the review verdict. Filing so it is not lost.\n\nTHE CONTRADICTION, both on origin/master, shipped in the same commit:\n  docs/adr/0026-govulncheck-cannot-see-in-package-tests.md:119 — 'other in both directions.'\n  justfile:172 — '# This recipe therefore compares in ONE direction, because the other one cannot'\n\nOne of these is false and they ship together. ADR 0026:83 and :196 and :259 also say 'in both directions', so the ADR is self-consistent and the justfile is the odd one out — which makes it likely the ADR is the stale side, but that must be MEASURED, not assumed.\n\nSECOND DEFECT IN THE SAME PASSAGE: ADR 0026:118-119 uses the word 'still', which presupposes a prior state the recipe was already doing this in. The recipe is NEW on that branch. There is no earlier state for it to still be doing anything in, so 'still' is false independently of the direction question.\n\nWITNESS: adversarial review of PR #813, blocker 1. Re-verified first-party against origin/master after the merge with:\n  git grep -n 'both directions' origin/master -- docs/adr/0026-govulncheck-cannot-see-in-package-tests.md\n  git grep -n 'ONE direction' origin/master -- justfile\n\nFIX: establish by experiment which direction the recipe actually compares — delete a package from the register and confirm the gate fails, then add one and confirm it fails — then make the ADR and the justfile agree with what was measured. Drop 'still'.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:30:30Z","created_by":"areqag","updated_at":"2026-08-17T05:32:05Z","external_ref":"https://github.com/areqag/gqlc/issues/831","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-65sl","title":"vuln: every selftest CALL in the vuln recipe is deletable with the gate green","description":"The same two-halves defect gqlc-eo46 documents for `refuse_stale`, measured in\nthe `vuln` recipe. It is not one instance — it is every `selftest_*` invocation\nin that recipe, and it predates the branch that added the newest one.\n\n## Reproduction (lead, first-party, at 620cea66 / PR #813)\n\nEach row deletes ONE line from the justfile and runs `just vuln`:\n\n    delete the `selftest_platformtag` call      rc=0   SURVIVED\n    delete the `selftest_tagblind` call         rc=0   SURVIVED\n\nThe BODY of each selftest is well witnessed — mutate the assertion inside\n`selftest_platformtag` (`*\",windows,\"*` -\u003e `*\",zzneverwindows,\"*`) and the gate\ngoes rc=1 immediately; same for `selftest_tagblind`'s derived-tag assertion. So\nboth halves of gqlc-eo46's pattern are present here too: the guard works, and\nnothing proves it runs.\n\n`selftest_tagblind` predates `fix/vuln-gate-walk-and-fence`, so this is a\nstanding class the branch extends by one instance rather than one it introduces.\nThat is why it is filed rather than fixed there.\n\n## Why it does not close in-script\n\nSame regress gqlc-eo46 names: any counter or assertion wrapped around the call\nsite is itself a line someone can delete, and the recursion does not terminate\ninside the script. The close is the same out-of-band move eo46 proposes — run\nthe recipe against a fixture tree in which the selftest's precondition is\nviolated, and assert rc=1 with the term named.\n\nDoing it once for the whole family is probably cheaper than doing it per\nselftest: a single harness that, for each `selftest_*` function, deletes its\ncall site, runs the recipe, and requires rc!=0.\n\n## Scope note\n\nNot a defect in what the gates measure — every selftest passes on the real tree\ntoday, and the properties they assert are independently checked elsewhere in the\nrecipe. The defect is that their continued presence is unenforced.\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:02:24Z","created_by":"areqag","updated_at":"2026-08-17T05:17:32Z","external_ref":"https://github.com/areqag/gqlc/issues/819","dependencies":[{"issue_id":"gqlc-65sl","depends_on_id":"gqlc-agt0","type":"relates-to","created_at":"2026-08-17T03:54:34Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-65sl","depends_on_id":"gqlc-eo46","type":"discovered-from","created_at":"2026-08-17T01:02:23Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-wprl","title":"ci: justfile recipe bodies are outside shellcheck coverage, and lint-hooks:130 is a real ERROR","description":"`just lint` runs shellcheck over the `.githooks` tree only. The justfile's own\nrecipe bodies — which are `#!/usr/bin/env bash` scripts with `set -euo pipefail`,\nseveral of them 60+ lines and carrying the repo's gate logic — are outside that\ncoverage entirely.\n\nThis is not hypothetical. Two findings this week were plain shell bugs that\nshellcheck-class analysis is built to catch:\n\n- `fix/vuln-gate-walk-and-fence` shipped a collision guard that matched\n  newline-delimited `sort -u` scalars with the space-delimited\n  `case \" ${a} ${b} \" in *\" ${x} \"*)` idiom. It could not fire, and it shipped\n  in the commit whose job was to close the finding that the clause it guards had\n  no coverage. Found by review, three rounds in.\n- The same file uses that idiom correctly seven other times, on genuine arrays\n  where `${arr[*]}` joins on a space. Scalar-vs-array is exactly the distinction\n  a linter tracks and a reader does not.\n\nMeasured while reviewing that branch: shellcheck over the five recipe bodies the\nbranch owns reports 0 error and 0 warning at enforced severity, so enrolling\nthem is not a wall of pre-existing noise.\n\nOne real pre-existing hit does exist, on master:\n\n    justfile:130  (inside `lint-hooks`)  — shellcheck ERROR\n\nThat one needs fixing or an explicit inline disable before the gate can be\nturned on, and it is the argument for turning it on rather than against.\n\n## Shape of the work\n\nExtract each recipe body (just already knows them; `just --dump` or a small\nreader like the one in `internal/codegen/age/dialect_test.go` can enumerate\nthem), write each to a temp file with its shebang, and run shellcheck at the\nsame severity `lint-hooks` uses. Gate must FAIL on a finding, not warn — a\ndetector that exits 0 is not a gate.\n","notes":"\n## Measurement from the fix/vuln-gate-walk-and-fence review (PR #813, 2026-08-17)\n\nThat branch adds ~795 lines to the justfile. Shellcheck was pointed at the four\nshebang recipe bodies it owns, by hand, with just interpolation ({{x}}) replaced\nby a placeholder first:\n\n    check-golangci-build-tags     --severity=warning  rc=0, 0 findings\n    test-codegen-fence            --severity=warning  rc=0, 0 findings\n    check-codegen-external-tests  --severity=warning  rc=0, 0 findings\n    vuln                          --severity=warning  rc=0, 0 findings\n\nSo turning this bead's gate on would NOT have blocked that branch. At info\nseverity there are three findings, all judged intentional:\n\n  - SC2015 x2, both the same helper `lines() { [ -n \"${1}\" ] \u0026\u0026 printf ... || true; }`\n    where the `|| true` is the point (the function must always succeed).\n  - SC2086 x1, `go list -deps ... ./... ${xtest}` — deliberate word-splitting of\n    a newline-separated list into separate arguments.\n\nThis bead's headline finding is independently reproduced: extracting `lint-hooks`\nand running shellcheck over it gives SC1073/SC1124/SC1072/SC1085 at ERROR, all\nfrom the `# shellcheck only supports shell; ...` comment sitting BETWEEN case\nitems (justfile:130 on master). Shellcheck parses any comment beginning\n`# shellcheck` as a directive, and directives are only valid before a complete\ncommand. The fix is to reword the comment so it does not start with the word\n`shellcheck`, which is a one-line change that should land with the gate.\n\nNo prose on that branch claims the recipes are linted; verified by grep over its\ndiff of justfile, docs/ and .github/.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T02:07:07Z","created_by":"areqag","updated_at":"2026-08-17T05:02:37Z","external_ref":"https://github.com/areqag/gqlc/issues/786","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-eo46","title":"ci: check-golangci-build-tags proves refuse_stale works and never proves it is called","description":"`check-golangci-build-tags` carries a 60-line witness proving `refuse_stale`'s\nBODY refuses a stale entry, and nothing at all proving that body is APPLIED to\nthe real sets. The application is one line:\n\n    justfile:302   refuse_stale \"${derived}\" \"${configured}\" || exit 1\n\nDelete it and a genuinely stale entry passes in silence.\n\n## Reproduction (lead, first-party, at 620cea66)\n\nInject a real stale entry — `- zzstalereal` in `.golangci.yml` run.build-tags,\nconstraining no file in the tree — then run `just check-golangci-build-tags`:\n\n    A  pristine justfile + stale entry      rc=1  names zzstalereal, 5 stderr lines\n    B  justfile:302 deleted + same entry    rc=0  no stderr\n    C  pristine justfile + clean config     rc=0  no stderr\n\nB is byte-identical in observable behaviour to C. The gate reports \"clean\" on a\ntree it never checked.\n\n## Why the branch's own idiom does NOT close it\n\n620cea66 introduced a positive control for exactly this class: look up every\nterm the two sets contain and refuse if the lookup cannot find them. That works\nfor `uses_term` because the control can positively exercise the predicate. It\ndoes not transfer here — any counter or assertion added around line 302 is\nitself deletable, and the regress does not terminate inside the script. The\nreviewer's \"closable with `control`'s own idiom\" is optimistic; measured, it is\nnot.\n\nThe close is out-of-band: a test that runs the recipe against a fixture tree\nwith a known-stale tag injected and asserts rc=1 and that the term is named.\nThat is the same move `feat/age-dialect-gap-admission` made for its justfile\nrecipes (a Go test that parses the artefact and asserts properties of it), so\nthere is a worked precedent in-tree.\n\n## Related residue on the same branch\n\n- Deleting the whole `control` loop survives (`control=0` -\u003e `1`). Same\n  last-line-of-the-script shape, disclosed by the implementer as NB-2.\n- Both are \"the outermost assertion is unguarded\", NOT the vacuous-pass class\n  that gqlc-s3lt covered — that one is closed and stays closed.\n","notes":"PR #833 round 3 widens the surface this bead describes, and does not close it.\n\nsweep-discovery-probes now carries nine refusals (empty aggregate, empty probe\nvalue, no *_probe declared, odd-shaped site, no site at all, site variable off\nthe *_probe convention, declared-vs-swept in both directions, per-name coverage\nasking BOTH `git check-ignore -q` and `-v` with the source pinned to .gitignore,\nlookup-membership, uncovered-control, post-sweep verify). Every one has this\nbead's shape: the refusal is deletable and nothing inside the script observes\nthat it is gone.\n\nMeasured at a5f07491 (PR head), mutation harness /tmp/r3-mut-833.py, 42 rows,\n28 KILLED / 14 SURVIVED, per-row preconditions 'just --summary rc=0' AND the\nrendered recipe body passing 'bash -n', md5 restore verified on justfile and\n.gitignore per row. The guard-isolation rows -- each refusal deleted ON ITS OWN\nwith its own defect still applied, so only the deleted guard could have spoken:\n\n  membership refusal deleted, no other defect       rc=0 SURVIVED\n  zzuncoveredprobe control mkdir deleted, no defect rc=0 SURVIVED\n  final verify loop deleted whole, no defect        rc=0 SURVIVED\n  lookup `-v` source check deleted + rules moved\n    to .git/info/exclude                            rc=0 SURVIVED\n  lookup `-q` hide check deleted + negated\n    !/test/data/vulnprobe.*/ rule applied           rc=0 SURVIVED\n  site-shape refusal deleted + dotless site         rc=0 SURVIVED\n  literal-site refusal deleted + literal site       rc=0 SURVIVED\n  declared-vs-swept refusal deleted + its defect    rc=0 SURVIVED\n  per-name coverage refusal deleted + its drift     rc=0 SURVIVED\n\nSome refusals overlap, so deleting one is sometimes caught by another (deleting\nthe empty-aggregate refusal still trips the empty-value one; deleting the\nswept-not-declared accumulation still trips the coverage refusal unless\n.gitignore is also given a rule for the extra name). Nothing in the recipe makes\nthat so -- it is incidental, and the rows above are the ones where it does not\nhold.\n\nThe recipe states this limit in the tree beside the lookup-membership refusal\nrather than only in the PR body. The close is unchanged and still out-of-band: a\ntest that drives the recipe over a fixture tree from outside it and asserts rc\nand the named term, per this bead's original description.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T02:06:52Z","created_by":"areqag","updated_at":"2026-08-17T07:33:32Z","external_ref":"https://github.com/areqag/gqlc/issues/787","dependencies":[{"issue_id":"gqlc-eo46","depends_on_id":"gqlc-agt0","type":"relates-to","created_at":"2026-08-17T03:54:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-3uof","title":"resolver: an inline endpoint's type is read as a satisfying set, emitting NOT NULL on a nullable column (master too)","description":"MATCH (:Person)-[r:WORKS_AT]-\u003e(c) RETURN c.smallOnly emits {\"type\":\"STRING\",\"nullable\":false} on BOTH origin/master (f4fb1a19) and branch feat/resolver-narrow-plural-endpoints. The row may match a Company that is not Company\u0026Large, so smallOnly can be absent -- a NOT NULL column that is null.\n\nRoot cause is shared with the branch's F1/F2 findings: endpointLabels' InlineEndpoint arm (internal/resolver/resolve.go, ~:617-640) returns the exact SPELLED key rather than the set of declared types satisfying it, so callers reading it as a complete statement about the pattern's ends under-approximate.\n\nFound by adversarial review of that branch on 2026-08-16 (verdict /tmp/lead/verdict-0tft2.md, NB2). Recorded as PRE-EXISTING and therefore out of scope for that branch, which only had to stop ANSWERING where master refused.\n\nThe reviewer's assessment: fixing this is probably the same change that fixes F2 (gating candidateTypes at resolve.go:975, or restructuring so the superset/subset distinction is carried in the type rather than a comment convention). So check whether F2's fix already closed this before doing separate work -- it may be closeable for free, or already closed.\n\nRelated: gqlc-pv0u, gqlc-qlr2 (filed from round 1 of the same review).","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T00:15:32Z","created_by":"areqag","updated_at":"2026-08-17T07:15:45Z","closed_at":"2026-08-17T07:15:45Z","close_reason":"Fixed by PR #857, squash-merged to master as 26971c2ec3922d3b3210b26bbdbb7e7aa107d159 on 2026-08-17. endpointLabels' InlineEndpoint arm now returns satisfyingNodeTypes rather than the spelled label key, so the inline and var spellings of a pattern agree. Adversarial review returned PASS: reviewer built an independent correctness oracle over 258,864 generated patterns with no resolver code in the loop and measured 137 unsound answers on master vs 0 on the branch, audited all 134 accept-\u003erefuse transitions (all 134 already refused by master when written with a variable, byte-identical messages), and reproduced 8/8 author mutations plus 7 of its own under a go vet screen with zero blank-name kills. GH #788 and #790 both CLOSED by the merge.","external_ref":"https://github.com/areqag/gqlc/issues/788","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0rjn","title":"codegen spec fence: the census has a per-document floor of one graded site","description":"The spec fence's census reconciles per (document, method-name), so a document keeps its census entry as long as ONE graded site survives. A one-part invisible edit therefore passes green.\n\nReproduction (found by adversarial review of branch docs/c1-bare-arg-spec-drift, 2026-08-16): move docs/specs/codegen-stage-c4.md:404 -- the WriteQuerier member -- out of the anchor. The suite stays GREEN, exit 0. Deleting the whole document's entry is caught; losing all but one site is not. The binding half has the same floor: c1.md:1339 map[string]any{\"id\": arg} -\u003e mapOf{...} is GREEN.\n\nConfirmed by the reviewer to fall OUTSIDE all five existing deferrals (gqlc-vu7z, gqlc-jfwo, gqlc-173n and the two others cited in that review).\n\nWhy it was documented rather than closed on that branch -- three closures were tried and each measured to lose:\n- per-(document, method-name) census: does NOT close it. C4 grades RemovePerson at :363, :404 and :840, so the NAME survives :404 leaving.\n- per-site COUNT: closes it, but is exactly the shape specfence_test.go:82-85 argues against (a bare '9 != 10' with no name in the failure).\n- per-site VERBATIM census: closes it, but makes the test file a copy of the documents -- red on every honest edit to an example, which is how a census gets bulk-updated without being read.\n\nSo closing this likely needs a different mechanism, not a wider census. Note the related growth-direction gap gqlc-jfwo needs candidate roots from an independent source (the repo's own markdown); the same independent-source idea may close both.\n\nFiled at lead level because the implementing agent was instructed not to write .beads while sibling agents were live.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T00:10:45Z","created_by":"areqag","updated_at":"2026-08-17T04:46:44Z","external_ref":"https://github.com/areqag/gqlc/issues/789","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-qlr2","title":"resolver: closeEdge reads an inline endpoint's spelled key, not its satisfying set","description":"endpointLabels' InlineEndpoint arm (internal/resolver/resolve.go) returns the exact key the query spells, not the set of declared node types satisfying those labels. edgeCandidates is therefore a SUBSET of the keys a matching row can carry, and closeEdge commits an edge type on that subset.\n\nReproduction (test/data/resolver/invalid/schemas/satisfy_plural_edges_inline_subtype.gql: Person / Person\u0026Employee, Company / Company\u0026Large, WORKS_AT declared Person-\u003eCompany and Person\u0026Employee-\u003eCompany\u0026Large):\n\n  MATCH (p:Person)-[r:WORKS_AT]-\u003e(:Company) RETURN r\n    -\u003e ResolvedEdge{Person -[WORKS_AT]-\u003e Company}\n\nBoth declarations match the pattern, so r is genuinely plural and the answer names one of two. The var spelling MATCH (p:Person)-[r:WORKS_AT]-\u003e(c:Company) RETURN r closes over the full cross-product and is refused, so the two spellings of one pattern disagree.\n\nThis predates gqlc-0tft and is unchanged by it: identical on f4fb1a19 and on the branch. gqlc-0tft's NarrowPluralEndpoints stops reading the under-approximation via endpointKeysCoverEveryMatch, which contains the node-type half; this bead is the edge-type half that remains.\n\nFix is to make the InlineEndpoint arm return satisfyingNodeTypes(ls, sch). That widens closeEdge's candidate sets, so queries that resolve today become ambiguous refusals -- the ADR 0006 forbidden direction -- and it needs its own decision about whether a spelled label set should key exactly or satisfy. Not a drive-by.\n\nThe stale citation in endpointLabels' doc comment (it pointed at the CLOSED gqlc-h9n.23, which is about multi-label edge types) is repointed here.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-16T23:45:02Z","created_by":"areqag","updated_at":"2026-08-17T07:15:45Z","closed_at":"2026-08-17T07:15:45Z","close_reason":"Fixed by PR #857, squash-merged to master as 26971c2ec3922d3b3210b26bbdbb7e7aa107d159 on 2026-08-17. endpointLabels' InlineEndpoint arm now returns satisfyingNodeTypes rather than the spelled label key, so the inline and var spellings of a pattern agree. Adversarial review returned PASS: reviewer built an independent correctness oracle over 258,864 generated patterns with no resolver code in the loop and measured 137 unsound answers on master vs 0 on the branch, audited all 134 accept-\u003erefuse transitions (all 134 already refused by master when written with a variable, byte-identical messages), and reproduced 8/8 author mutations plus 7 of its own under a go vet screen with zero blank-name kills. GH #788 and #790 both CLOSED by the merge.","external_ref":"https://github.com/areqag/gqlc/issues/790","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-osf1","title":"codegen/age: the dialect gap table is short — four constructs are suspected and unwitnessed","description":"The Apache AGE backend refuses a query on its TEXT when it spells a construct\nthe pinned image cannot accept (`internal/codegen/age/dialect.go`). Every entry\nin that table carries a probe and the answer a live session gave, and a sweep\n(`TestEveryDialectGapCarriesItsWitness`) refuses an entry with no witness. That\nis the right shape, and it leaves the table SHORT: three openCypher constructors\nand one namespaced call are every bit as suspect as the five that are refused,\nand none of them has ever been run.\n\n## Unverified, and therefore not refused\n\nAgainst `apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb`\n(PostgreSQL 18.1, AGE 1.7.0 — the digest is a constant in\n`test/data/codegen/live_age_test.go`):\n\n1. `RETURN time()` — expected `function time does not exist`, never run.\n2. `RETURN localtime()` — same, never run. Load-bearing: it is what\n   `test/data/codegen/invalid/unrepresentable_temporal_localtime_column` and\n   `TestTemporalProjectionNamesThisBackend` rest on. If localtime() turns out to\n   be undefined, the dialect gate answers those two ahead of\n   `codegen.ErrUnrepresentableTemporal` and BOTH have to move to a constructor\n   that is still unwitnessed — or the fixture has to reach a temporal column\n   some other way.\n3. `RETURN point({x: 1, y: 2})` — expected `function point does not exist`,\n   never run. Not a temporal, so it would be a third gap rather than a name\n   added to this one, and the refusal prose would have to change: the present\n   message says \"defines no temporal constructor at all\".\n4. `RETURN duration.between(a, b)` — namespaced, so a different name from\n   `duration` (Cypher.g4 §oC_FunctionName is `oC_Namespace oC_SymbolicName`) and\n   not covered by the `duration` probe. `cypher.UnqualifiedFunctionCalls` drops\n   namespaced calls by design, so refusing it needs a second scanner as well as\n   a witness.\n5. The SQLSTATE for every refusal in the undefined-function gap. The spike\n   (gqlc-35yu.5) recorded the server's MESSAGE and not its code, so\n   `TestAGERefusesTheFunctionsItDoesNotDefine` asserts the message alone —\n   unlike the alternation gap, which pins 42601. PostgreSQL's\n   `undefined_function` is 42883 and AGE plausibly reports through the same\n   channel, but plausibly is not measured.\n6. The first run of `TestAGERefusesTheFunctionsItDoesNotDefine` itself. It is\n   nightly-and-manual only (`codegen-live.yml`, job `live-smoke-age`, skipped on\n   PRs), so the five refusals it re-measures ship having been verified by hand in\n   the spike and not yet by CI.\n\n## Why this is a separate bead\n\nAnswering any of the above needs Docker, and therefore needs CI: the\n`live-smoke-age` job is the only place in this project with a container. The\nwork is to add the probes, read the answers off the run, and then — for whichever\nturn out to be refused — add them to the gap table with their recorded answers,\nwhich the sweep will accept only once the live test carries them.\n\n## Done when\n\n- Each of (1)–(4) has been run against the pinned image and its answer recorded\n  in this bead.\n- Each one that AGE refuses is in `dialectGaps` with its probe and answer, and\n  the ones AGE accepts are in the gap's `served` list so the gate is pinned not\n  to refuse them.\n- The undefined-function rows assert a SQLSTATE, as the alternation rows do.\n- `just test` and one green `live-smoke-age` run.\n","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T22:23:09Z","created_by":"areqag","updated_at":"2026-08-17T04:46:45Z","external_ref":"https://github.com/areqag/gqlc/issues/791","dependencies":[{"issue_id":"gqlc-osf1","depends_on_id":"gqlc-35yu.12","type":"discovered-from","created_at":"2026-08-04T18:23:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h6h7","title":"resolver: an unlabelled binding is not narrowed by the edges that pin it","description":"Pre-existing on master, found while reviewing `gqlc-0tft`.\n\nA binding that carries no label in the pattern is not narrowed by the edges that\npin it, so it resolves to the union of every declared type its edges could\nreach rather than to the intersection they actually prove.\n\n## Reproducer\n\n    MATCH (p)-[f:FOUNDED]-\u003e(x:Company)\n    OPTIONAL MATCH (p)-[r:WORKS_AT]-\u003e(c:Company)\n    RETURN p\n\nWith the Phase B feature disabled, `p` resolves to `Employee\u0026Person`.\n\n`p` carries no label of its own. Both edges pin it, but the resolver reads the\nbare binding as unconstrained and hands back the union — so a caller writing\nagainst the generated surface is offered a type the query cannot produce, and\nthe fields that differ between the two are the ones that will be empty.\n\n## Why it is filed rather than fixed\n\nNot this epic's territory: it is on master ahead of the multi-backend work and\nis not reachable from any `gqlc-35yu` child. `gqlc-0tft` narrows plural\nendpoints for *labelled* bindings; this is the unlabelled case one axis over,\nand folding it in would have widened that branch past what its fixtures cover.\n\nAdjacent to `gqlc-0tft` (same pass, same `s.bindings` walk) — check whether the\ntwo should be taken together, because a fix here almost certainly lands in the\nsame function.\n\n## Acceptance\n\nThe reproducer above resolves `p` to the intersection its edges prove, not the\nunion.\n\nBoth directions need a fixture: a bare binding that the edges genuinely narrow,\nand a bare binding whose edges prove nothing, which must stay wide rather than\nbeing narrowed to whichever type happened to be seen first. Confirm each\nreddens when the narrowing is reverted — the defect class this repo keeps\nfinding is a guard green over nothing, and a fixture that passes before and\nafter the fix is not coverage for it.\n\nFiled by the coordinator during `gqlc-0tft`'s review round.\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:45:55Z","created_by":"areqag","updated_at":"2026-08-04T21:44:36Z","external_ref":"https://github.com/areqag/gqlc/issues/780","dependencies":[{"issue_id":"gqlc-h6h7","depends_on_id":"gqlc-0tft","type":"discovered-from","created_at":"2026-08-04T05:45:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-v7vu","title":"codegen: ANY PROPERTY VALUE has no fixture on any target, so its column path is unexercised","description":"`gqlc-dcap` fixed `ANY VALUE` query columns (they resolved to Go `any` and took\nthe driver-carrier lane, emitting `neo4j.GetRecordValue[any]`, which does not\ncompile) and added `schema_any_property` fixtures plus an `AnythingColumns`\nruntime corpus entry.\n\n`ANY PROPERTY VALUE` is a distinct declared width in the same family and has\n**no fixture on any target**. Found by `linus-dcap-ibg5-r1` while reviewing that\nbranch.\n\n## Why this is not just a missing test\n\nThe two widths are close enough that the `ANY VALUE` fix might already cover\n`ANY PROPERTY VALUE`, or might not, and nothing in the tree answers which. The\nquestion is whether it resolves to the same Go type and the same `ColumnKind`.\nIf it does, the routing `dcap` added applies and this bead is a fixture. If it\nresolves differently -- to a narrower carrier, or to a different kind -- then it\ntakes a different lane and `dcap`'s fix does not reach it, which would mean the\nsame compile failure is still live under a different declaration.\n\nAnswering that is the first task, and the answer decides whether this is a\nhalf-hour of fixtures or a second fix.\n\n## Where it must land\n\n`schema_any_property` is the natural home, enrolled on all three targets so it\nalso sits inside `TestBackendInvariantSurface`. Cover nullable and non-nullable,\nsingle-column and multi-column, the same axes `dcap` covered.\n\n## The trap\n\nDo not let this rest on golden bytes. A golden-only kill proves the bytes moved,\nnot that behaviour is pinned -- `-update` blesses it away. `dcap` added\n`AnythingColumns` to the runtime corpus for exactly that reason and this needs\nthe equivalent: read the emitted decoder back over driver values, including a\nheld null.\n\nRelated: `gqlc-tez0` records that the `record.Get` lane on neo4j emits no\narrived-null gate at all, which is the exception among column kinds. If\n`ANY PROPERTY VALUE` shares that lane it inherits the same gap, so check the two\ntogether.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T08:51:15Z","created_by":"areqag","updated_at":"2026-08-04T09:34:37Z","external_ref":"https://github.com/areqag/gqlc/issues/769","dependencies":[{"issue_id":"gqlc-v7vu","depends_on_id":"gqlc-dcap","type":"discovered-from","created_at":"2026-08-04T04:51:36Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-s3lt","title":"ci: the vuln gate's directory-coverage postcondition passes vacuously over an empty walk","description":"`gqlc-pig9` added a second half to the govulncheck gate's postcondition: every\ndirectory holding a Go file must have been matched by `go list ./...` under the\nderived tags. It is what catches the wholly-build-constrained directory that\n`go list`'s wildcard silently skips.\n\nIt is checked as a comparison between two sets: the directories the on-disk walk\nfound, and the directories `go list` matched.\n\n## The hole\n\nIf the walk itself returns nothing, both sets are empty, the comparison passes,\nand the gate goes green having verified nothing. The walk is the measurement;\nthe comparison is only as good as it.\n\nThis is the same defect class the commit that introduced it was fixing, one\nlevel up. `gqlc-nsq4` closed exactly this shape on the accepted-advisory\nregister -- 'the register was fail-closed only while it was non-empty' -- and\ngraded the id extraction against the output it reads so an empty measurement\nfails rather than passes. The directory walk did not get the same treatment.\n\nWays the walk can empty that are not hypothetical: the module-root discovery it\nis bounded by returns nothing; a `find` predicate stops matching after a\ncoreutils change; the walk root is computed relative to a cwd that moved.\n\n## Fix\n\nGrade the walk against something independently derived, the way the id\nextraction is now graded against the `Vulnerability #` header count. At minimum\nthe walk must find at least one directory per discovered module, since a Go\nmodule with no directory containing a Go file is not a thing.\n\n## Acceptance\n\nBreak the walk so it returns the empty set and confirm the gate FAILS. Today it\nexits 0. That single reproduction is the whole bead.\n\nNote `test/data/tagblind` already asserts its own shape before any scan runs,\nfor the same reason -- a fixture that quietly stopped having its shape would take\nthe guards out of service silently. This bead extends that reasoning from the\nfixture to the walk.","notes":"QUOTA KILL 2026-08-04 ~17:30 EDT (carmack-vuln-p2, working s3lt+e7oq+oxne together). State preserved, NOT discarded. Worktree gqlc-vuln-p2, branch fix/vuln-gate-walk-and-fence, HEAD 77a8cc12 (no commits yet), with uncommitted justfile edits and a NEW package internal/tools/modscope/ (main.go 8506 bytes, main_test.go 7102 bytes). Backup: /tmp/quota-rescue/20260804/vuln-p2.patch and /tmp/quota-rescue/20260804/modscope/. Resume from the working tree; do NOT reset or git restore.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T08:50:59Z","created_by":"areqag","updated_at":"2026-08-17T02:06:30Z","closed_at":"2026-08-17T02:06:30Z","close_reason":"Closed by branch fix/vuln-gate-walk-and-fence @ 620cea66. Adversarial review PASS\nat round 5 (/tmp/lead/verdict-vulnr5.md).\n\nAn empty walk is now the measurement broken, not the module empty (3e9388ef), and\na walk that failed halfway is not a walk that found nothing (30f1db21). The\nvacuous-pass hole this bead describes — both sets empty, comparison passes, gate\ngreen having verified nothing — is refused rather than shrugged at.\n\nThe branch's thesis is exactly this bead's: an empty measurement must FAIL, not\nsilently pass. Round 3 found the branch had NOT applied that correction to two of\nits own new checks (they compared sets disjoint on this corpus, so the guarded\nordering was untestable); round 4 found a third instance; both are closed and\nindependently re-verified. Round 5 hunted a fourth and did not find one:\n`uses_term` carries 5 live mutation rows, including dropping the control counter\n(KILLED: \"both the derived and the configured tag sets are empty\") and inverting\nthe probe arm.\n\nKnown residue, filed separately: the outermost application of `refuse_stale`\n(justfile:302) can be deleted without consequence, and deleting the whole control\nloop survives. These are last-line-of-the-script gaps, not vacuous-pass gaps.\n\nNOT pushed, no PR opened.\n","external_ref":"https://github.com/areqag/gqlc/issues/770","dependencies":[{"issue_id":"gqlc-s3lt","depends_on_id":"gqlc-pig9","type":"discovered-from","created_at":"2026-08-04T04:51:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nvjz","title":"bd-gh-sync's pull path reports 'held 0' over a bead set nobody witnessed","description":"`gqlc-w4q9` established the rule that a listing this script could not read must\nnot be counted as an empty listing. `gqlc-mbe0` extended it to listings\ntruncated at their `--limit`. Both landed on the **push** path.\n\nThe pull path does not apply it.\n\n## The shape\n\n`bd list` answering `[]` with exit 0 on pull yields:\n\n    bd-gh-sync: pulled 0 bead(s), held 0, left N unmirrored GH issue(s) alone.\n\n`held 0` over a bead set nobody witnessed. Push refuses exactly this shape and\nexits BLIND; pull reports a number as though it had counted something.\n\nIt is loud in practice — every mirrored bead becomes an ORPHAN line, so a human\nwatching the output will notice. That is why this is P2 and not P1. But \"a human\nwould probably notice\" is not the standard the push side was held to, and the\nasymmetry is unexplained: the same failure, on the same script, one line apart,\ngets a refusal on one side and a cheerful zero on the other.\n\n## The trap, which is why this is not a five-minute fix\n\nThe symmetric guard does **not** work here, and the implementer of `gqlc-mbe0`\ndeliberately did not add it. A bead's `external_ref` regex is `/issues/(\\d+)$`\nover any URL, so a ref can legitimately name **another repository**. Mirrored\nbeads plus an empty all-state listing is therefore a reachable, legitimate\nconfiguration — and a guard that refuses it would false-alarm on every\n`git pull`.\n\nSo the fix has to distinguish \"the listing was unreadable\" from \"the listing was\nreadable and genuinely empty\", on a path where an empty answer is a normal\nanswer. Push does not have that problem because a push of N\u003e0 followed by an\nempty open listing is contradictory on its face.\n\nThat distinction is the actual work of this bead. Candidates: separate `bd list`'s\nexit status and its parse from its cardinality, so an unreadable listing is\nrefused on the *status* rather than inferred from the count; or scope the\nwitness to beads whose `external_ref` names *this* repository, which makes the\ncontradiction recoverable.\n\n## Acceptance\n\nAn unreadable pull-side listing exits BLIND naming the reason. A legitimately\nempty one still exits 0. A test for each, and the second must fail if the first\nguard is widened to swallow it.\n\nFiled by the coordinator from `carmack-ghsync-p3b`'s out-of-scope list.\n","notes":"Landed via PR #825 (branch fix/bd-gh-sync-pull-witness, 15 commits, squashed).\nThe prior session's work was never pushed; this session rebased onto master\n(f1f23cb6, clean — no conflicts with PR #778, which was already an ancestor),\nre-reviewed the whole diff adversarially and added three commits.\n\nREVIEW FINDINGS FIXED ON THE BRANCH:\n1. The postcondition python was the only one of the file's three without\n   escaping. A held bead id carrying a space rendered as two names in the\n   summary `(b, bad)`; one carrying a newline split its moved.txt record so the\n   summary reported 2 beads for 1 — an unmeasured count on the one line a\n   `tail -1` caller keeps, which is the exact shape this bead exists to refuse.\n   Fixed by `one_line()` in the postcondition plus an awk join that cuts the\n   record tag by field instead of `cut -d' ' -f2- | tr '\\n' ' ' | sed 's/ /, /g'`.\n2. The escaping was then unasserted on the per-bead WARNING one line above the\n   summary, where the `bd update \u003cid\u003e` remedy lives. Two more assertions.\n3. Prose: `ok`/`bad` \"are the only two ways out of an assertion block, so this\n   file cannot make an assertion that escapes the roll-call\" — contradicted by\n   the census 24 lines below, which exists to detect exactly that, and by a live\n   mutation (neuter one `ok` to `:` -\u003e census reddens, which is only possible\n   because the assertion escaped). Also the census exemption named one of\n   gqlc-tvv7's three holes and quoted a pass count 12 out of date.\n\nEVIDENCE: 42 mutations in a detached worktree. Every guard added by this branch\nis killed by a named assertion except one, filed as gqlc-072v. Suite 60 -\u003e 145\nassertions. The two id fixes proved red-to-green (140/2 exit 1 pre-fix).\n\nRESIDUE: gqlc-072v (empty-last-line arms unexercised), gqlc-0ini (the charset\ngate asymmetry itself, fixed at the rendering layer here, not at the source).","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:48:56Z","created_by":"areqag","updated_at":"2026-08-17T05:28:50Z","closed_at":"2026-08-17T00:26:43Z","close_reason":"Closed by branch fix/bd-gh-sync-pull-witness at 0c13ddcb (12 commits, not yet pushed). Headline commit 99490443 'the bd-gh-sync pull path witnesses its own bead listing'.\n\nAdversarial review returned PASS. Evidence, all reproduced first-party by the reviewer in a detached worktree:\n\n- The pull path now witnesses its own listing, and the witness is itself witnessed by an assertion census (141 assertions, verified as 141 REAL assertions before anything else -- not a zero-assertion green).\n- Census membership is guarded in both directions: deleting a census line exits 1 with 'ran and is not in the census'; neutering an assertion's ok() exits 1 with 'declared in the census and did not run'.\n- The red-run hole is bounded and correct: on a red run the census is not read, and the run still exits 1. Verified 'fail' is written only by bad(), the file's last line is the exit test, and the only trap is rm -rf.\n- 33/33 recorded mutants re-run and all exit 1, with no absent mutation targets. Faithfulness to master spot-checked against f4fb1a19.\n- The gate genuinely blocks: 'CI=1 just test-hooks' exits 1 under a mutant and lint-hooks-test.sh never runs; 'CI=1 just test' exits 1 before go build.\n- Branch protection checked against the real remote areqag/gqlc: every required context maps to a real job id with no name override and a pull_request trigger. No orphaned or permanently-green gate.\n\nReview residue filed as gqlc-tvv7 (the census cannot notice its own deletion, and does not pin assertion strength).","external_ref":"https://github.com/areqag/gqlc/issues/760","dependencies":[{"issue_id":"gqlc-nvjz","depends_on_id":"gqlc-mbe0","type":"discovered-from","created_at":"2026-08-03T20:48:55Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-tez0","title":"ANY VALUE columns diverge between neo4j and AGE on null, behind an identical declared surface","description":"`gqlc-dcap` fixed the compile failure in `ANY VALUE` column projections\n(`neo4j.GetRecordValue[any]` does not compile, because `any` does not satisfy\n`neo4j.RecordValue`) and added `AnythingColumns` to the runtime corpus. Doing so\nexposed two divergences it did not fix, both on the null path.\n\n## The two shapes\n\n**Non-nullable `ANY VALUE` arriving null.** neo4j hands the nil straight\nthrough to the caller's `any` field. AGE fails the row via its `raw == nil`\ngate. Same query, same declared Go type, two different outcomes — one returns a\nvalue the caller must nil-check on a field the schema said was non-nullable, the\nother returns an error.\n\n**Nullable `ANY VALUE` when AGE delivers agtype `null` as text rather than SQL\nNULL.** AGE would give pointer-to-non-nil-holding-nil where neo4j gives a nil\npointer. Both type-check; they differ in what `if x.Maybe == nil` answers.\n\n## Why nothing catches this\n\n`TestBackendInvariantSurface` compares **declarations only**, so an identical\n`Maybe *any` on both backends is all it sees. `gqlc-05tl`'s new decoder gate is\nabout label reachability and does not touch this either.\n\nThe runtime corpus *would* catch it, but only neo4j's half runs: the AGE corpus\nneeds a live container, and the machine this was implemented on has no Docker.\nSo the divergence is currently unmeasurable rather than merely unmeasured — the\nfirst thing this bead needs is a live AGE run, which means CI (`codegen-live.yml`\nruns nightly, and `live-smoke-age` exists).\n\nThis is the same measurement gap the `gqlc-35yu` epic has been closing one\nconstruct at a time; `ANY VALUE` is a construct nobody has pointed the live\nbattery at yet.\n\n## What to decide\n\nWhich behaviour is correct, then make both backends do it. The candidates:\n\n1. **Non-nullable null is an error on both.** Consistent with AGE today, and\n   with the project's general posture that a schema's promise is load-bearing.\n   Changes neo4j's behaviour for existing callers.\n2. **Non-nullable null passes through on both.** Consistent with neo4j today.\n   Means a non-nullable field can hand back nil, which makes the nullability\n   annotation decorative for `ANY VALUE`.\n3. **Refuse `ANY VALUE` non-nullable columns at generate time.** The ADR\n   0025/0027 posture — if the two backends cannot agree, do not emit. Most\n   consistent with the epic's precedent, most disruptive.\n\nDo not decide this from reading. Run the live AGE corpus first and find out what\nAGE actually does with an agtype `null`, because shape two above is stated as a\nconditional (\"*if* AGE delivers it as text\") and nobody has checked.\n\n## Acceptance\n\nA live AGE run recording what actually happens in both shapes; a decision\nrecorded in an ADR; both backends agreeing; and a corpus case that runs on both\nand would fail if they diverged again.\n\nFiled by the coordinator from `carmack-dcap-ibg5`'s out-of-scope list.\n","notes":"SHARPENED (2026-08-04, coordinator) from linus-dcap-ibg5-r1's NB-4, which located the mechanism rather than the symptom.\n\nThe divergence is not a subtle encoding difference. It is a MISSING GATE, and the asymmetry is exact:\n\n- AGE emits writeNonNullGate on this lane.\n- neo4j emits NO arrived-null gate on the record.Get lane AT ALL.\n\nThat makes the record.Get lane the EXCEPTION among neo4j column kinds -- every other column kind on that backend carries a null gate. So this is not 'two backends chose differently'; it is one lane on one backend that skipped the thing all its siblings do. That reframes the fix: the question is not 'which behaviour should both adopt' but 'why does this one lane have no gate', and the answer is likely that gqlc-dcap routed ColumnProperty to record.Get without carrying the gate across with it.\n\nWhich means the regression window opens with gqlc-dcap (PR #767) and the fix belongs close behind it.\n\nAcceptance should assert the gate's PRESENCE structurally -- a sweep over column kinds on the neo4j path confirming each emits an arrived-null gate -- not just a behavioural row for ANY VALUE. A behavioural row pins this one column kind; the sweep is what stops the next lane from skipping it too. Beware the usual trap: a sweep over an empty or mis-derived set of column kinds passes vacuously, so pin the set's size against an independently derived count.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:48:36Z","created_by":"areqag","updated_at":"2026-08-04T08:47:10Z","external_ref":"https://github.com/areqag/gqlc/issues/761","dependencies":[{"issue_id":"gqlc-tez0","depends_on_id":"gqlc-dcap","type":"discovered-from","created_at":"2026-08-03T20:48:36Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2h9w","title":"neo4j decoders consult no label, so decodePerson fills a Person from a Post","description":"`gqlc-05tl` built `TestEmittedDecodersGuardOnlyOnStampableLabels`, which checks\nthat every string an emitted `decode\u003cT\u003e` compares for equality is a label the\nfixture's schema actually declares. It found the AGE join-spelling bug it was\nbuilt for.\n\nBuilding it surfaced a second, larger thing on the other side of the ledger.\n\n## The finding\n\nThe neo4j targets' decoders carry **no label guard at all**. The gate records\nthis as `labelGuardingTargets[neo4j-go-v5] = false`, guarding 0 of 123\ndecoders, and that number is asserted rather than assumed — so this is measured,\nnot suspected.\n\n`decodePerson(node dbtype.Node)` reads properties off the node without ever\nconsulting `node.Labels`. Hand it a `Post` whose property names happen to\noverlap and it fills a `Person` and returns nil error.\n\n## Why it is a separate bead\n\n`gqlc-05tl`'s gate is about **reachability** — a decoder whose guard no wire\nvalue can satisfy is dead code. This is the opposite axis: a decoder with no\nguard accepts too much. Same file, opposite failure, and fixing it means\nemitting a guard that does not exist today rather than correcting one that does.\n\nIt is also a behaviour change on a shipped backend, which is not the kind of\nthing to bolt onto a branch whose subject was a test.\n\n## What needs deciding first\n\nWhether a missing label check is a defect at all. The argument that it is not:\na caller reaches `decodePerson` only through a query the resolver typed, so the\ndriver should not be handing back a `Post` in the first place. The argument\nthat it is: that reasoning is exactly the \"the resolver would never build this\"\nshape that `gqlc-h4ug` moved five taxonomy rows out of §3 for — what the\nresolver builds does not bound what a caller can hand over, and `dbtype.Node`\narrives from the driver, not from gqlc.\n\nNote the asymmetry it creates meanwhile: AGE guards on 95 of 95, neo4j on 0 of\n123. Two backends disagreeing about whether a wrong-label node is an error is a\ndivergence in *behaviour* behind an identical declared surface — precisely what\nADR 0025 line 121-126 already records as invisible to\n`TestBackendInvariantSurface`.\n\n## Acceptance\n\nEither a decision recorded in an ADR that the neo4j decoders are correct\nwithout a label check, with the reasoning; or a guard emitted and\n`labelGuardingTargets` flipped to `true` for both neo4j targets, with the\nledger's count derived rather than re-blessed.\n\nFiled by the coordinator from `carmack-05tl`'s out-of-scope list.\n","notes":"RULING (2026-08-04, coordinator). The bead asks whether a missing label check is a defect at all before any work starts. Deciding: IT IS. Emit the guard.\n\nThe argument that it is not a defect -- 'a caller reaches decodePerson only through a query the resolver typed, so the driver should not be handing back a Post' -- is the 'the resolver would never build this' shape, and THIS REPO HAS ALREADY RULED ON THAT SHAPE ONCE. gqlc-h4ug moved five taxonomy rows out of s3 for exactly it. Re-litigating it here to the opposite answer would leave the codebase holding two incompatible postures about the same reasoning, which is worse than either posture alone.\n\nThe substantive reason it fails: dbtype.Node arrives FROM THE DRIVER, not from gqlc. What the resolver builds bounds what gqlc ASKS FOR; it does not bound what comes back. A decoder is a boundary, and the repo's own rule is that boundaries validate.\n\nThe asymmetry is the second reason and it is not cosmetic. AGE guards 95 of 95, neo4j 0 of 123, behind an identical declared surface. The epic's central design property is that the Go surface must not vary by backend -- and while that property is literally about declarations, ADR 0025's precedent extends it to what a caller can actually rely on: it rejected a shape because 'it emits a column no decoder can fill at exit 0'. This is that inverted -- a decoder that fills from the wrong thing at exit 0. Same objection, other sign.\n\n## What to build\n\nA label guard on the neo4j decoders matching AGE's posture: mismatch is an ERROR, not a silent skip and not a warning. A decoder handed the wrong node type has no correct value to return, so there is nothing for a softer outcome to mean.\n\n## The extensibility property, which is the part that must not be skipped\n\ngqlc-05tl's gate already measures this and ASSERTS the number (labelGuardingTargets[neo4j-go-v5] = false, 0 of 123). That assertion is what makes this bead's completion checkable rather than claimed. When the guard lands, that same gate must flip to 123 of 123 and the assertion must be updated to demand it -- so a future emitter arm that forgets the guard reddens. Do not delete the census to make it pass; invert what it demands.\n\nBeware the standing trap: a census that counts 0 of 0 passes vacuously. Pin the denominator against an independently derived decoder count, not against itself.\n\nSequencing: 05tl's gate must merge first, since this bead's acceptance is expressed in its terms.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:47:57Z","created_by":"areqag","updated_at":"2026-08-04T08:49:13Z","external_ref":"https://github.com/areqag/gqlc/issues/762","dependencies":[{"issue_id":"gqlc-2h9w","depends_on_id":"gqlc-05tl","type":"discovered-from","created_at":"2026-08-03T20:47:57Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-e7oq","title":"ci: build_tags derivation turns !windows into -tags windows, excluding the file it came from","description":"The `build_tags` derivation added on the vuln-gate branch reads `//go:build`\nlines and turns the terms it finds into `-tags` arguments. It cannot tell a\n**build tag** from a **GOOS/GOARCH term** or a **Go-version term**, and the\nthree are syntactically identical in a constraint expression.\n\nThe consequence is not \"a term is ignored\" — it is that a negated platform term\nbecomes a positive tag that **excludes the very file it came from**:\n\n    //go:build !windows      -\u003e      -tags windows\n\nThat is the inverse of the intent. A file guarded to build everywhere *except*\nWindows is handed a flag that makes it build nowhere the scan looks. The scan\nthen reports clean over a file it never compiled.\n\n`go1.N` terms have the same problem in a different direction: `-tags go1.24` is\nmeaningless to the toolchain, which derives release tags itself, so the term is\nsilently inert rather than wrong.\n\n## Why it has not bitten\n\nThe tree's live files use plain custom tags (`codegen_live`) with no negation\nand no platform terms, so every term currently derived is a genuine build tag.\nThe derivation is correct on today's corpus and wrong in general.\n\n## Shape of the fix\n\nTerms fall into three classes and only one belongs in `-tags`:\n\n- **known GOOS/GOARCH values** — `go tool dist list` is authoritative; these\n  should set `GOOS`/`GOARCH` or be dropped, never passed as tags\n- **`go1.N`** — drop; the toolchain owns release tags\n- **everything else** — a custom build tag, pass through\n\nNegation needs a decision of its own: `!foo` cannot be satisfied by adding a\ntag, so the honest handling is to drop the term and let the file build in the\ndefault configuration, which is what its author meant.\n\n## Acceptance\n\nA fixture file carrying `//go:build !windows` is compiled by the scan on Linux.\nVerify by mutation: restore the naive derivation, watch the file drop out of\nthe scanned set, revert.\n\nFiled by the coordinator from `carmack-vulngate-r2`'s out-of-scope list.\nDiscovered-from `gqlc-rohp`.","notes":"CONFIRMED BY MEASUREMENT (2026-08-04, linus-vulngate-r1). The negated case reproduces exactly as filed:\n\n    derived tags:  windows\n    IgnoredGoFiles under -tags windows:  example.com/revneg: unixonly.go\n\n//go:build !windows derives tag 'windows'; -tags windows then satisfies 'windows', so !windows is false and the file is EXCLUDED. The gate does go red, so this is fail-closed rather than fail-open -- but the diagnostic names only two causes (a GOOS/GOARCH filename suffix, or //go:build ignore) and the real cause is a third: the derivation excluded the file itself. The diagnostic wording is being fixed on fix/vuln-gate-derivation; the derivation defect this bead owns is not.\n\nAlso measured: release-tag terms are benign, not merely inert -- 'go list -tags go1.30' is accepted, rc=0. So the go1.N half of this bead is cosmetic and the GOOS/GOARCH half is the load-bearing one.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:16:09Z","created_by":"areqag","updated_at":"2026-08-17T02:06:30Z","closed_at":"2026-08-17T02:06:30Z","close_reason":"Closed by branch fix/vuln-gate-walk-and-fence @ 620cea66. Adversarial review PASS\nat round 5 (/tmp/lead/verdict-vulnr5.md).\n\nA build tag is now a judgement about a term rather than the term with its\npunctuation stripped (59436a0a). `!windows` is classified as a platform term and\nnever becomes `-tags windows`. Two follow-on corrections: a GOOS spelling declared\nin run.build-tags is still a GOOS (603ebefc), and a platform spelling the toolchain\nknows is one whatever the live `go tool dist list` says (a367b89f) — the latter\nadds a hardcoded floor unioned into the vocabulary, byte-faithful 18/18 GOOS and\n24/24 GOARCH to GOROOT's syslist.go.\n\nDisclosed cost, pinned rather than left in prose:\nTestAnUndeclaredFloorOnlySpellingIsDroppedWhereItUsedToBeRefused\n(internal/tools/modscope/main_test.go:582) asserts BOTH halves — pre-floor\ngradePlatformTerms still errors on an undeclared `//go:build hurd`, post-floor\nplatformTerms drops it silently. Verified first-party by the lead: name is unique\nin the tree, runs anchored, passes; and by the reviewer under mutation, where\n`_ = platformFloor` and deleting \"hurd\" from platformFloor both KILL it by name\nwith `go build ./...` rc=0 asserted per row.\n\nThe implementer's own worry that the pin would misfire on a real Go hurd port is\nfalse, and the reviewer showed why: gradePlatformTerms/platformTerms take the\ntable as a PARAMETER (main.go:640, :743), the pin passes a literal\n[]string{\"linux/amd64\",\"windows/amd64\"}, and it guards on\nslices.Contains(platformFloor, \"hurd\") with a t.Fatalf. Hermetic.\n\nNOT pushed, no PR opened.\n","external_ref":"https://github.com/areqag/gqlc/issues/755","dependencies":[{"issue_id":"gqlc-e7oq","depends_on_id":"gqlc-rohp","type":"discovered-from","created_at":"2026-08-03T20:16:25Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-oxne","title":"ci: the govulncheck fence hardcodes test/data/codegen while the sweep discovers modules","description":"`gqlc-rohp` made the govulncheck sweep discover Go modules from disk rather than\nnaming them, so a third module added to the tree is scanned. The **fence** was\nnot given the same treatment and still names `test/data/codegen` literally.\n\nA third module therefore gets a vulnerability scan and no build, vet, tidy or\nlint gate at all — the weaker half of the pair is the one that generalised.\n\n## Sites that hardcode the path\n\n- `justfile:140-142` — `test-codegen-fence`'s `go build` / `go vet -tags\n  codegen_live` / `go mod tidy -diff` all `cd test/data/codegen`.\n- `justfile:159, 216, 221, 229` — the same literal in the lint and check\n  recipes.\n- `check-codegen-external-tests` — sweeps that one directory.\n- `.github/workflows/ci.yml`, job `codegen-fence` — invokes the recipe, so it\n  inherits the literal.\n\n## Why this is worth closing rather than accepting\n\nThe repo has exactly two modules today, so nothing is currently unfenced. The\ncost is that the invariant \"every module in this tree is built, vetted, tidied\nand linted\" is not stated anywhere that can fail. Adding a module is a silent\ndowngrade: `govulncheck` picks it up, everything else does not, and no gate\nreports the asymmetry.\n\n## Shape of the fix\n\nDerive the module set once — the same discovery `gqlc-rohp` built for the\nsweep — and drive both the scan and the fence from it. If the recipes cannot\nshare the discovery, the minimum is a gate asserting that the set of modules\nthe fence covers equals the set the sweep finds.\n\n## Acceptance\n\nAdd a throwaway third module under the tree, confirm the fence covers it\nwithout editing a recipe, then remove it. A hardcoded-list implementation must\nredden that check.\n\nFiled by the coordinator from `carmack-vulngate-r2`'s out-of-scope list.\nDiscovered-from `gqlc-rohp`.","notes":"SCOPE WIDENED by the coordinator, from carmack-vulngate-r2's out-of-scope list.\nTwo more sites of the same hardcoding, both surfaced by the fix for gqlc-pig9.\n\n(1) .golangci.yml's build-tags list. The vuln gate's fix landed a permanent\nfixture test/data/tagblind/tagblind.go, every file behind //go:build tagblind,\nso that the tag derivation has an in-tree witness of a wholly build-constrained\ndirectory. For the fixture to be linted, 'tagblind' had to be added to\n.golangci.yml build-tags by hand. That list is now two entries and grows by one\nevery time a constrained directory is added -- a hand-maintained mirror of a\nset the justfile now derives from a filesystem walk. Two derivations of the same\nfact, one automatic and one manual, is this bead's subject one level down.\n\n(2) The cost side. test/data/codegen holds 148 Go directories, 147 of them\nchecked-in golden output. Both the tag walk and the new coverage postcondition\nenumerate all of them on every 'just vuln'. Correct but expensive, and expensive\nfor the same reason: the fence has no notion of which directories are generated\noutput versus authored code, so it treats them alike.\n\nTake these with the original rather than separately -- the fix for one is very\nlikely the fix for all three, since they are the same missing derivation. If\nthat turns out to be wrong when the work starts, split then.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:16:04Z","created_by":"areqag","updated_at":"2026-08-17T02:06:06Z","closed_at":"2026-08-17T02:06:06Z","close_reason":"Closed by branch fix/vuln-gate-walk-and-fence @ 620cea66 (15 commits on f4fb1a19).\nAdversarial review PASS at round 5 (/tmp/lead/verdict-vulnr5.md).\n\nThe fence no longer names the module set. `test-codegen-fence` (justfile:396)\ndrives off `scope() { go run ./internal/tools/modscope \"$@\"; }` on both halves of\nthe pair, and check-golangci-build-tags derives its tag vocabulary the same way.\nVerified first-party: the only remaining `test/data/codegen` literals in the\njustfile are 13 comment lines documenting the literal that was removed, plus the\nthree live-test recipes at :704/:730/:739, which name one module deliberately and\nare out of this bead's scope.\n\nThe collision guard shipped in 005f3fba to close this bead's coverage gap could\nnot fire: it matched newline-delimited `sort -u` scalars with the space-delimited\n`case \" ${a} ${b} \" in *\" ${x} \"*)` idiom, which only ever matches a set of size\none. Fixed at 620cea66 with a whole-line `grep -qxF` lookup plus a positive\ncontrol that looks up every term the two sets contain and refuses an empty union.\n\nReproduced by the lead, with `//go:build zzstaleprobe` really in the tree and\n`zzstaleprobe` in .golangci.yml run.build-tags:\n  pre-fix  rc=1 under the WRONG arm (\"no file in this tree constrains it\" — about\n           a file that does)\n  post-fix rc=1 naming the collision (\"zzstaleprobe is a term this tree really uses\")\nMutations of the lookup, justfile md5 restored and verified after each:\n  regress to the space-`case` form -\u003e KILLED \"cannot find codegen_live\"\n  uses_term -\u003e { false; }          -\u003e KILLED, same arm\n  uses_term -\u003e { true; }           -\u003e KILLED, collision arm\nSo the original defect now reddens on every run.\n\nResidue filed separately: the application of `refuse_stale` at justfile:302 is\nstill deletable without consequence.\n\nNOT pushed, no PR opened.\n","external_ref":"https://github.com/areqag/gqlc/issues/756","dependencies":[{"issue_id":"gqlc-oxne","depends_on_id":"gqlc-rohp","type":"discovered-from","created_at":"2026-08-03T20:16:24Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vm6l","title":"conformance: AGE un-enrolment from an alternation-carrying fixture is invisible","description":"A conformance fixture under `internal/codegen/conformance/testdata/invalid/`\nnames, in its manifest, the sentinel each enrolled target is expected to\nanswer with. `TestInvalid` compares the sentinel a target actually returned\nagainst the one the manifest names, and reports a mismatch as an ordinary\ntest failure.\n\nThat is the right shape for a fixture whose refusal is a property of the\nfixture. It is the wrong shape for a **structural rule about a backend** —\n\"AGE refuses every query carrying relationship-type alternation\" — because\nthe rule is enforced only where a manifest happens to spell it.\n\n## The concrete hole\n\n`gqlc-35yu.14` un-enrolled `invalid/unrepresentable_edge_union_shared_label`\nfrom `apache-age-pgx-v5`: the fixture is `-[r:LIKES|WROTE]-`, so AGE now\nanswers it with `ErrRelationshipTypeAlternation` rather than the\n`ErrOutOfC6Scope` its manifest names. The un-enrolment is correct, and the\nbranch argues the coverage it carried is preserved by\n`invalid/plural_endpoint_edge_union_shared_label`, which reaches the same\nportable refusal with no `|` in it.\n\nBut nothing in the tree states the rule that made the un-enrolment necessary.\nIf a future fixture adds an `apache-age-pgx-v5` target to a query containing\n`|`, the failure it produces is \"expected sentinel X, got Y\" — indistinguishable\nfrom a plain manifest typo. A reader fixes it by editing the manifest to name\n`ErrRelationshipTypeAlternation`, which is exactly wrong: the fixture should\neither drop the AGE target or lose the alternation.\n\nSymmetrically, if the gate is later weakened so AGE stops refusing alternation,\nno test names that. `unrepresentable_edge_union_shared_label` would quietly\nbecome enrollable again and nothing would notice it had not been re-enrolled.\n\n## Shape of the fix\n\nAssert the rule directly, over the corpus rather than over one fixture: for\nevery fixture enrolled for an `apache-age-pgx-v5` target, its query text\ncontains no relationship-type alternation — reusing\n`cypher.RelationshipTypeAlternations` rather than grepping for `|`, since the\npipe character appears in list comprehensions too. The complement is the\ninteresting half: every fixture that DOES carry an alternation is either\nun-enrolled from AGE, or enrolled with `ErrRelationshipTypeAlternation` named.\n\nEither polarity is defensible; pick one and say why. What must not survive is\nthe current state, where the rule exists only in the reviewer's head and in a\ncommit message.\n\n## Acceptance\n\nAdding an `apache-age-pgx-v5` target to a `|`-carrying invalid fixture turns a\ngate red with a message naming the rule, not a sentinel mismatch. Verify by\nmutation: make the edit, watch it fail, revert.\n\nFiled by the coordinator from a candidate raised by the `gqlc-35yu.14` round-3\nimplementer. Adjacent to `gqlc-rv0h` (a backend's sentinels are unnameable in a\nmanifest) — check whether that bead's fix subsumes this one before starting.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T23:48:03Z","created_by":"areqag","updated_at":"2026-08-04T00:07:09Z","external_ref":"https://github.com/areqag/gqlc/issues/743","dependencies":[{"issue_id":"gqlc-vm6l","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-03T19:48:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-856f","title":"resolver: ResolvedType is not sealed at all — embedding defeats the marker, so no switch on it can be total","description":"`internal/resolver/validated.go:79` documents `ResolvedType` as \"the sealed\nsum of resolved types\". The unexported `isResolvedType()` marker does seal it\nagainst **new implementations**. It does not seal it against the **pointer\nforms of the existing eight**: every variant declares both `isResolvedType()`\nand `String()` with **value receivers** (`validated.go:97-364`), and a\npointer's method set contains its value methods. So `*resolver.ResolvedNode`\nsatisfies `ResolvedType` while `case resolver.ResolvedNode:` does not match\nit. The sum an out-of-package caller can inhabit has **sixteen** forms plus\nnil.\n\nThe doc comment says none of that. Any package that type-switches on\n`ResolvedType` and reads that comment will conclude its default arm is dead,\nand will be wrong in the same way `internal/codegen/prepare.go` was.\n\n## Provenance\n\n`gqlc-h4ug` found and closed this for `internal/codegen`: two branches tagged\n`//gqlc:unreachable` on the strength of that reasoning turned out reachable by\nan assembled `Input`, returning their sentinel cleanly with no panic. Those\nrows moved §3 → §2 and the taxonomy's §5.1 step 5 was rewritten to tell a\nclassifier to count a sum's **inhabitants**, not its declarations.\n\nThat fixed the consumer. It did not fix the claim at the source, and the\nimplementer explicitly flagged that they **did not audit consumers beyond\n`internal/codegen`**.\n\n## Scope\n\nTwo parts, and the second is the one that matters.\n\n1. Correct the doc comment at `validated.go:79` to say what the seal does and\n   does not cover.\n2. **Audit every other type switch on `ResolvedType` in the tree** for the\n   same undercount. Grep is enough to find them; the judgement is per site —\n   a switch whose default already refuses is fine, one whose default is\n   documented or tagged as unreachable is not.\n\nNote the related posture question belongs to `gqlc-edze`, not here: whether\npointer forms should be normalised away at `Prepare`'s boundary so switches\nsee value forms only, or refused with the default arm treated as a permanent\nreachable branch. This bead is about the claim being accurate wherever it is\nmade; `edze` is about choosing the behaviour.\n\n## Acceptance\n\nThe doc comment is accurate, and every `ResolvedType` type switch outside\n`internal/codegen` has been visited with a one-line verdict recorded — either\n\"default refuses, fine\" or a filed follow-up. A grep-and-shrug is not the\ndeliverable; the deliverable is knowing the count.\n\nSuggested by the `gqlc-h4ug` round-5 implementer, filed by the coordinator.\n","notes":"WIDENED (2026-08-03, from the gqlc-h4ug round-6 adversarial review).\n\nThis bead was filed on the premise that `ResolvedType`'s unexported marker seals\nthe sum against new implementations but not against the pointer forms of the\nexisting eight, giving sixteen inhabitants plus nil. **That premise is itself an\nundercount.** The seal is against nothing.\n\nGo promotes an embedded type's **unexported** methods. So an out-of-package\ncaller declares a new implementation in one line:\n\n    type embedNode struct{ resolver.ResolvedNode }\n    var _ resolver.ResolvedType = embedNode{}   // compiles\n\nMeasured from `package conformance_test` — outside `internal/resolver` — such a\nvalue reaches the fail-sites cleanly, with no panic:\n\n    TestProbeEmbeddedColumn    files=0 err=out of C6 scope: query \"Fetch\" column 0 \"n\" resolved as node\n    TestProbeEmbeddedListElem  files=0 err=query \"Fetch\" column 0 \"xs\": out of C6 scope: list element has unknown resolved type node\n    TestProbeEmbeddedParam     files=0 err=out of C6 scope: query \"Fetch\" parameter 0 $p resolved as property: (non-property parameters are post-v1)\n\nThe inhabitant set is **unbounded**, not sixteen. `isResolvedType()` prevents an\nout-of-package type from being written from scratch; it does not prevent one\nfrom being obtained by embedding.\n\n## What this changes about this bead\n\nThe doc-comment correction (part 1) is now a stronger claim: not \"the seal does\nnot cover pointer forms\" but \"the seal does not make the sum closed at all, and\nno type switch over `ResolvedType` can be exhaustive by construction\". A default\narm is therefore permanently reachable, not conditionally so.\n\nThe audit (part 2, the half that matters) is unchanged in shape but its verdict\ncriterion is simpler: any switch whose default is documented or tagged as\nunreachable is wrong, full stop — there is no inhabitant count that could make\nit right.\n\n## Related, measured in the same review\n\nAll eight **typed-nil** pointer forms fault, not just the nil interface: Go\nemits an explicit nil check for a value method reached through a nil pointer, so\neven the zero-sized `(*resolver.ResolvedUnknown)(nil)` panics with\n`value method ...ResolvedUnknown.String called using nil *ResolvedUnknown pointer`.\n`resolver.ResolvedList{Element: nil}` also faults at `prepare.go:1050` with\n`invalid memory address or nil pointer dereference`. That belongs to\n[[gqlc-edze]], which owns the posture question; it is recorded here because it\nwas measured alongside the embedding result.\n\n=====================================================================\nAUDIT COMPLETE (2026-08-17) — branch fix/resolver-sealed-sum-claim @ 52f8662\n\nTHE COUNT: 31 sites dispatch on a value whose static type is\nresolver.ResolvedType — 9 type switches and 22 type assertions. Derived with\ngo/types via golang.org/x/tools/go/packages over ./... with Tests:true (68\npackages, 0 package errors), not by grep: the bead's own survey listed only\n`case resolver.Resolved` arms and so missed all 22 assertions, which carry the\nsame undercount in different syntax (four of them in internal/codegen).\n\nCorrection to the brief's survey: internal/resolver/scope.go has ZERO switches\nor assertions on ResolvedType. Its 25 mentions are map value types, struct\nfields and signatures. The unaudited concentration is resolve.go alone.\n\nVERDICTS — internal/resolver (19 sites)\n  resolve.go:363  describeColumnType switch, default@374 -\u003e `return t.String()`\n                  REFUSES (falls back to the bare wire tag). No unreachability\n                  claim. Fine. Caveat: the ResolvedList arm recurses on a nil\n                  Element into this same default and panics — measured, filed\n                  as gqlc-t802, latent (no originator found), edze-adjacent.\n  resolve.go:396  resolvedTypeEqual switch, default@429 -\u003e `return false`.\n                  REFUSES. Nil-guarded at the top. Fine.\n  resolve.go:1430 unify switch, default@1471 -\u003e `return nil, false`. REFUSES.\n                  Fine.\n  resolve.go:398,401,404,407,418,421,424,427 (8) — comma-ok `b.(X)` inside the\n                  396 switch, each folded into `ok \u0026\u0026 ...`. REFUSE. Fine.\n  resolve.go:1424,1427,1432,1438,1444,1450,1460,1466 (8) — comma-ok assertions\n                  in/around unify. REFUSE. Fine.\n  resolver_test.go:2230 collectEdgeUnions switch, default@2235 -\u003e `return nil`.\n                  REFUSES. Fine.\n  resolver_test.go:2058,2369 — comma-ok, asserted via require.True with a %T\n                  message. REFUSE. Fine.\n\nVERDICTS — internal/codegen (12 sites; h4ug's territory, RE-VERIFIED not redone)\n  prepare.go:602  Phase A column switch, default@639 -\u003e ErrOutOfC6Scope.\n                  REFUSES, tag already removed by h4ug, reachability pinned by\n                  conformance row `column-unknown-variant`. Fine.\n  prepare.go:644  Phase A param assertion, !ok -\u003e ErrOutOfC6Scope. REFUSES,\n                  untagged. Fine.\n  prepare.go:749  Phase B column switch, default@897 tagged\n                  //gqlc:unreachable column-type-invariant. SHADOWED, verified\n                  first-party: Prepare calls phaseAAdmit (prepare.go:273) and\n                  returns on error before phaseBDerive (:281); both iterate\n                  q.Validated.Columns and both switches name the same eight\n                  variants, so anything falling through B falls through A and A\n                  answers first. Matches what codegen-sentinel-taxonomy.md:222\n                  already records. Fine.\n  prepare.go:722  Phase B param assertion, !ok tagged\n                  //gqlc:unreachable param-type-invariant. SHADOWED by the same\n                  argument against prepare.go:644. Fine.\n  prepare.go:1016 findEdgeUnionLeaf switch, NO default; falls to\n                  `return nil, false`. REFUSES. Fine.\n  prepare.go:1047 buildListElemPlan switch, NO default; falls to\n                  `return nil, fmt.Errorf(ErrOutOfC6Scope ...)`. REFUSES. Fine.\n  age/errors.go:397 unservedColumn switch, NO default; falls to\n                  `return \"projects \" + t.String()`. Behaviour REFUSES, but the\n                  comment at :423-425 claims \"ResolvedType is a sealed\n                  interface, so the switch above is its whole membership\" — the\n                  last live restatement of the false claim in Go source.\n                  FILED as gqlc-qv80 (file held by PR #848).\n  age/errors.go:251 `_, union := col.Type.(resolver.ResolvedEdgeUnion)` yields\n                  false for pointer/embedded forms, degrading the edgeUnion\n                  ranking flag. No panic. FILED with gqlc-qv80.\n  age/errors.go:439 unservedParam assertion, !ok -\u003e \"is \" + t.String().\n                  REFUSES. Fine.\n\nSo: no site in the tree fails to refuse. Two tagged branches survive and both\nare legitimately shadowed by Phase A. The residue is claims, not behaviour.\n\nDELIVERED\n  - validated.go:74 doc comment rewritten: states what the marker buys\n    (nothing outside the package DECLARES it) and the two constructions that\n    obtain it anyway (pointer form, embedding), noting they nest so the\n    satisfying set has no bound.\n  - internal/resolver/sealedsum_test.go, package resolver_test (external on\n    purpose — in-package the marker is writable, so an in-package witness\n    measures nothing). TestResolvedTypeSumIsNotClosed derives the declaring set\n    by AST walk and checks it against the enumeration, pins each variant's\n    value form matching its own arm (the ALLOW half), and pins the pointer and\n    embedding forms reaching the default.\n  - 10-row mutation matrix, precondition-screened with\n    `go test -c -o /dev/null ./internal/resolver/`. 8 coverage kills each\n    naming its subtest, 1 compiler kill (expected: the embedding claim is\n    compile-time), 1 control row PASS (a commented-out marker does not register\n    — the same text uncommented is a kill, so the walk reads code not bytes).\n\nRESIDUE FILED: gqlc-qv80, gqlc-dc6k, gqlc-t802.\nNOT DECIDED HERE: gqlc-edze's posture question, per this bead's scope.\n\nCLOSE ON MERGE — PR is open, coordinator holds the merge. A closed bead is not\na merged bead; leave in_progress until origin/master carries 52f8662.","status":"in_progress","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T23:41:34Z","created_by":"areqag","updated_at":"2026-08-17T07:46:51Z","started_at":"2026-08-17T07:46:51Z","external_ref":"https://github.com/areqag/gqlc/issues/744","dependencies":[{"issue_id":"gqlc-856f","depends_on_id":"gqlc-h4ug","type":"discovered-from","created_at":"2026-08-03T19:41:38Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-05tl","title":"codegen: TestBackendInvariantSurface compares declarations only, so an unfillable decoder is invisible","description":"`TestBackendInvariantSurface` (`internal/codegen/conformance/conformance_test.go`)\nis the gate that holds this project's central design property: the Go surface a\ncaller writes against must not vary by backend, only the encode/decode path\ndoes. It compares **declarations only** — `*ast.GenDecl` with `token.TYPE`, and\n`*ast.FuncDecl` with a receiver.\n\nSo an unexported, receiver-less function is invisible to it. `decodeFoo` is\nexactly that shape. **A backend can emit a structurally identical declaration\nwhose decoder can never fire, and the invariant test stays green.**\n\n## Measured, not argued\n\nFound while implementing `gqlc-35yu.15`. AGE stamps exactly one label per\nvertex, so a node type keyed on two labels cannot be represented. One candidate\nposture was \"emit the struct anyway, with a label check\". Built and measured:\npatching `wireEntity` to return `ok=true` with `label = string(labels)` left\n`TestBackendInvariantSurface` **green in 0.034s**, over a `models.go`\ncontaining\n\n    if label != \"Employee\u0026Person\" {\n        return PersonEmployee{}, fmt.Errorf(\"decode PersonEmployee: expected label %q, got %q\", \"Employee\u0026Person\", label)\n    }\n\n`Employee\u0026Person` is gqlc's own `LabelSetKey` join spelling. No AGE vertex\ncarries it and no AGE statement can stamp it, so that decoder is dead on\narrival. `gqlc generate` exits 0 and the invariant gate says the surfaces match\n— which, on its own terms, they do.\n\nThat posture was rejected on ADR 0025's precedent (\"it emits a column no\ndecoder can fill at exit 0\"), **not** on a test. `gqlc-35yu.15` records the\ndistinction in ADR 0027 so a reader does not go looking for the gate that\ncaught it.\n\n## The question this bead has to answer first\n\nIs declarations-only *deliberate*? There is a real argument for it: the\ninvariant is about the surface a **caller** writes against, and a caller cannot\nwrite against an unexported receiver-less function. On that reading the gate is\ncorrectly scoped and the residual belongs to a different gate.\n\nThe counter-argument is that \"identical surface, one of which cannot decode\"\nsatisfies the letter of the property while defeating its purpose. The whole\npoint of holding the surface invariant is that a caller can retarget without\nrewriting; a struct that never fills does not survive retargeting.\n\nSettle that before writing code. If declarations-only is deliberate, this\nbecomes a documentation bead plus a *separate* gate that asserts every emitted\ndecoder is reachable — which is the claim actually wanted. If it is an\noversight, widen the comparison.\n\n## Prior art in this repo\n\n`.13` built the shape for the second option: `internal/codegen/neo4j/corpus_test.go`\nwrites the generated package to a temp dir and compiles and **runs** it under\n`GOPROXY=off` against a driver stub, asserting values rather than bytes. A\ndecoder that cannot fire is visible to a harness that runs it and invisible to\none that parses it.\n\n## Acceptance\n\nWhichever route: a mutation that makes an emitted decoder unfillable while\nleaving its declaration identical must turn some gate red. The `wireEntity`\nmutation above is a ready-made one — it is compile-valid, it reproduces in\nseconds, and it is currently green.\n\nNot filed by the `.15` implementer because it may be deliberate; ruled a bead\nby the coordinator rather than a finding, since the design question outlives\nthe branch that surfaced it.\n","notes":"LEDGER CORRECTION 2026-08-17 (coordinator). The close reason above is STALE and\nits central factual claim is now false. It says the branch is \"@ 6912d310, 18\ncommits, UNPUSHED pending user go-ahead (no PR opened)\". The branch WAS pushed:\nPR #847 exists, head 3cd1e4b2, and at that head an independent adversarial\nreviewer returned REVISE.\n\nSo this bead is CLOSED and GH #745 is CLOSED while the work is UNMERGED and\nunder active revision. That is the gqlc-bd-closed-does-not-mean-merged shape: a\nLinus PASS on an unpushed branch was banked as done, the bd-\u003eGH sync mirrored the\nclosure, and the subsequent review round reopened real findings.\n\nCurrent true state at 3cd1e4b2: VERDICT REVISE, four blockers, all PROSE — no\ncode blockers. The reviewer reproduced all four author mutations at go vet rc=0,\nRUN=2326, zero blank-named FAILs, and found five further load-bearing guards the\nauthor had not claimed (E1-E4, P1), all killed. Blockers are:\n  B1 decoder_reachability_test.go:216-218 - \"a decoder is never in it\" is false,\n     contradicted by the same file at :2081-2085, by ADR 0027:96-101 which this\n     branch adds, by the PR body, and by residue bead gqlc-9xy0. Witness: a\n     receiver-decoder probe yields refusals=0 and the gate PASSES.\n  B2 :26-27 \"no label can be one\" contradicts :815-817.\n  B3 :623-626 says \"Both callers\" hold a key set equal to a ledger's, justifying\n     a dropped error; there are THREE callers (:404,:507,:585) and :507 was added\n     by this branch (c36f2207) holding no ledger.\n  B4 20 AST-extracted history comments, 11 are review-process narration; two ship\n     unresolvable labels (B2 at :1113, R5 at :1856). Precedent c1558e9e (#829).\n\nA lead is fixing these. The bead is deliberately LEFT CLOSED rather than\nreopened, because leaving it closed keeps it out of `bd ready` while a team holds\nit, which is the behaviour we want. GH #745 is left closed for the same reason\nand because the sync would fight a manual reopen. If #847 does NOT merge, reopen\nboth.\n\nVerified first-party: gh issue view 745 -\u003e CLOSED; PR #847 OPEN at 3cd1e4b2.\n\nROUND 2 DELIVERED 2026-08-17 (implementation lead). Head bf7d1b0d -\u003e b34ff720,\ntwo commits, BOTH COMMENT-ONLY: `git diff \u003cparent\u003e \u003chead\u003e -U0` filtered to\nnon-comment changed lines returns 0 for each. All four blockers closed, plus\nnits N1-N3.\n\nB1 :226-238 - the false universal is gone. The exclusion is now stated as\nsyntactic (over the receiver form, not over what its results name) and its cost\nis stated in ADR 0027's own words: a decoder moved onto a receiver leaves this\ncensus, only the codegen.Prepare reconciliation reddens, and a live\npackage-level decoder for the same entity beside it satisfies even that.\nReproduced the reviewer's witness in a detached worktree: a\ndecoderSet.decodePersonDead guarding \"NoSuchLabelAnywhere\" beside a live\ndecodePerson prints PROBE refusals=0, decoders=1, and the gate PASSES.\n\nB2 - the factual question is ANSWERED, not split. A gqlc GQL schema CANNOT\ndeclare a label whose text is exactly true/false/null, for two independent\nreasons, so :26-27 was right and :815-817 was wrong; the latter is deleted.\n  (1) A bare spelling does not parse as a labelName. GQL.g4:3 sets\n      `options { caseInsensitive = true; }`, so BOOLEAN_LITERAL ('TRUE'|'FALSE'\n      |'UNKNOWN') and NULL_KW ('NULL') match in ANY case, and neither is in\n      nonReservedWords -- the only keyword route into regularIdentifier.\n  (2) The delimited spellings DO parse but keep their delimiters, because\n      internal/schema/gql/nodetype.go:41 reads the labelName with GetText().\n  Probed out-of-tree: (:true) (:false) (:null) (:True) all -\u003e \"no viable\n  alternative\"; (:`true`) -\u003e key `true`; (:\"true\") -\u003e key \"true\"; controls\n  (:Person) and (:`Person`) -\u003e Person and `Person`.\n  The docstring at :26-40 now states the negative WITH its mechanism and names\n  the two ways it could break, so it is checkable rather than a convention.\n  :840 replaces the arm-ordering justification with the disjointness that\n  actually holds.\n\nB3 :636-652 - the \"Both callers\" census is gone; it was false (three callers,\none added by this branch holding no ledger) and it was what justified the\ndropped error. Replaced with the real condition, shaped as a PRECONDITION ON\nTHE PARAMETER rather than a caller census so it cannot rot when a fourth caller\nlands (this also closes N2): target must be one of s.backends.Keys(), and a\ncodegen.Registry is immutable after construction with Keys() reporting the map\nLookup() reads (registry.go:20-63), so the lookup cannot miss and an assertion\nwould be a check no input can fail.\n\nB4 - all 11 review-process narration sites rewritten to present-tense\nstatements of the rule, per precedent c1558e9e (#829), which kept mutation IDs\nbut dropped round narration. Both unresolvable labels are gone (\"B2\" at :1113\ndeleted, \"R5 of that boundary\" -\u003e \"the far side of that boundary\"). N1's wider\nclass swept too (8 further sites). Verified with the reviewer's own AST comment\nscanner (/tmp/factory/cmtscan): the original 20-term pattern and a broader\npattern both now return ZERO hits.\n\nMUTATION LEDGER at bf7d1b0d, 6 rows, ALL KILLED. Protocol every row: go vet\nrc=0 as compile precondition, RUN held at 2326 both sides, a NAMED --- FAIL\nrequired, blank-named --- FAIL counted (zero on every row), md5-verified\nrestore, `git status --short` empty after each.\n  M1  AGE single() returns (string(k), true) for a multi-label key set (the\n      acceptance criterion) -\u003e KILLED, names decodePersonEmployee and\n      \"Employee\u0026Person\"\n  M3  walks per declaration 1 -\u003e 2 (arithmetic) -\u003e KILLED, 5 named FAILs\n  M3b the two-walk SHAPE: prune at each literal, hand its BODY to a second\n      walk -\u003e KILLED, 3 named FAILs on the signature row. This row is new this\n      round; it is what makes the `literals` docstring true in BOTH directions\n      rather than only the arithmetic one.\n  M4  node classes descended into, minus *ast.FuncType -\u003e KILLED, 4 named FAILs\n  R7  boundLiteralNames length guard != -\u003e \u003c -\u003e KILLED, exactly 1 named FAIL\n  P1  AGE writeEntityDecoder emits `if len(label) \u003c 0` (a guard comparing no\n      string) -\u003e KILLED, so the ledger's positive half is not vacuous\n  Re-run of M1 and M3 at head b34ff720: both KILLED, RUN=2326, blank_FAIL=0,\n  md5 restore ok. b34ff720 differs from bf7d1b0d by zero non-comment lines, so\n  the other four rows carry.\n\nGATES at b34ff720 in a detached worktree (/tmp/factory/vfy-847r2b-4471, since\nremoved): just fmt-check rc=0, just tidy-check rc=0, just test rc=0 (0 FAIL,\n210 packages), just lint rc=0 (0 issues), just test-codegen-fence rc=0.\nrc=3 on the lint recipes is the GLOBAL golangci-lint lock, not a finding;\nretried waiting on `pgrep -x golangci-lint`, never --no-verify.\n\nHYGIENE: named paths only, .beads/issues.jsonl never staged; no\nCo-Authored-By trailer and no AI-attribution footer; PR body updated to match\nthe corrected comments and still carries `Closes #745`.\n\nSTILL OPEN, deliberately: this bead and GH #745 remain CLOSED per the\ncoordinator ruling above -- do NOT reopen unless #847 fails to merge. The\nlead does NOT hold the merge; the coordinator does. Residue unchanged:\ngqlc-eo74 (conjunction of two live guards), gqlc-9xy0 (receiver/method label\nsites), gqlc-2h9w (within-axis misdirection).","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T17:37:46Z","created_by":"areqag","updated_at":"2026-08-17T07:32:13Z","closed_at":"2026-08-17T04:56:21Z","close_reason":"PASS after seven adversarial rounds. Branch fix/emitted-decoder-reachability @ 6912d310, 18 commits, UNPUSHED pending user go-ahead (no PR opened).\n\nFix: TestBackendInvariantSurface's blind spot is closed by widening the emitted-function sweep to every function literal at every depth of every declaration. packageLevelFuncs now runs ONE walk per declaration (round 6, b1f0c2cf) instead of a two-walk shape that pruned at each literal and handed on only that literal's body -- an *ast.FuncLit's other child is its signature, which the hand-off never carried, so a helper inside one was yielded by neither walk while the identical helper one level down was yielded. Classification turned on where the holder was written rather than on the helper.\n\nLead first-party verification (verdicts at /tmp/lead/verdict-05tlr{5,6,7}.md). Protocol on every row: go vet ./internal/codegen/conformance/ rc=0 as compile precondition, '=== RUN' fixed from outside the run, a NAMED --- FAIL: required for a kill, md5-verified restore, mutations described by arithmetic not English verbs.\n\nRound 6 @ b1f0c2cf, 14 rows, RUN=2311: 13 KILLED including M3 (walks per decl 1-\u003e2, the load-bearing claim of the seam deletion) and M4 (node classes descended into, minus *ast.FuncType, reddening both signature rows). One SURVIVOR found -- the length guard's more-names-than-values direction -- returned as REVISE.\n\nRound 7 @ 6912d310, RUN 2311-\u003e2311 delta 0: that survivor is now KILLED naming TestSweepMarksEachYieldedFunctionByItsSpelling, with a one-entry list diff ('the function literal at line 15' -\u003e 'unpaired'). SURVIVED-at-parent reproduced in two independently created worktrees, so the before/after is genuine. gt and blank rows unchanged.\n\nRound 7 also found a falsehood INSIDE my own finding, which I had graded true: round 6's 'it is != rather than \u003e so that ... binds nothing either' named the wrong counterfactual. Truth table (/tmp/probe-05tl-op/main.go) shows \u003e rejects that direction too and differs from != only by dropping the bounds guard; the operator that breaks the property is \u003c. Sentence rewritten and the false 'that shape does not typecheck' clause deleted rather than softened.\n\nGates at 6912d310: gofmt clean (scoped), go vet rc=0, just fmt-check rc=0, just lint-new rc=0 (0 issues), just test rc=0 / 0 FAIL. Hygiene: worktree clean, 0 .beads files and 0 AI-attribution matches across all 18 commits.\n\nResidue filed as gqlc-mp9n (bounds direction pinned only by a panic) and gqlc-rqu8 (counterfactual-comment review rule).","external_ref":"https://github.com/areqag/gqlc/issues/745","dependencies":[{"issue_id":"gqlc-05tl","depends_on_id":"gqlc-35yu.15","type":"discovered-from","created_at":"2026-08-03T13:37:51Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ibg5","title":"codegen/neo4j: two non-nullable list columns in one query redeclare the accumulator local 'acc'","description":"`writeListColumnDecodeIndent` in `internal/codegen/neo4j/render_queries.go`\nemits the list accumulator under a hardcoded name:\n\n    acc := make(\u003cGoType\u003e, 0, len(\u003cvalue\u003e))\n\nOn the non-nullable arm that `:=` sits at the row-assembly block's own indent,\nso a query projecting two or more non-nullable list columns emits two `acc`\ndeclarations in one scope. The emitted file does not compile:\n\n    ./queries.cypher.go:136:7: no new variables on left side of :=\n    ./queries.cypher.go:136:10: cannot use make([]int32, 0, len(value1)) (value of type []int32) as []string value in assignment\n\nGeneration exits 0; the emitter's format gate only parses.\n\nThe nullable arm hides the defect because its `acc` is emitted inside\n`if !isNil { ... }`, a scope of its own. So a query mixing one non-nullable\nlist column with any number of nullable ones is fine, and only two\nnon-nullable ones collide — which is why no fixture has hit it.\n\n## Reproduction\n\nAgainst a schema declaring `tags :: LIST\u003cSTRING\u003e NOT NULL` and\n`ranks :: LIST\u003cINT32\u003e NOT NULL`:\n\n    // name: ListyColumns :many\n    MATCH (l:Listy) RETURN l.tags AS tags, l.ranks AS ranks\n\n`TestGoldenBuild` fails on the emitted file.\n\n## Fix shape\n\nName the accumulator positionally, the way `valueName(i)` names the column\nlocal and the way `writeSliceNarrow` suffixes its locals by depth. The file's\nown comments already give the rule for exactly this class of bug —\n`writeEntityFieldDecode`: \"The local the value lands in is positional. A name\nderived from the property is any identifier the schema author chose, including\none this scope already holds\". The accumulator escaped it.\n\n## Acceptance\n\nA fixture projecting two non-nullable list columns in one query, whose golden\ncompiles under `TestGoldenBuild`; reverting to the hardcoded `acc` must redden\nit.\n","notes":"SEQUENCING (2026-08-03, epic coordinator): held until fix/neo4j-carrier-followups merges. Both fixes land in internal/codegen/neo4j/render_queries.go, the same file that branch rewrites (carriesElemBare, walkListElemBody's ColumnProperty arm, walkListElemPlan's index gate). dcap's own fix shape — route writeSingleColumnDecodeIndent through ridesADriverCarrier, mirroring what carriesElemBare now does one axis down — only exists once that branch lands. Genuine territory collision, not a conflict-risk excuse. Staff both together as one branch afterwards; they are the same file and the same class.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T17:36:34Z","created_by":"areqag","updated_at":"2026-08-04T08:47:49Z","closed_at":"2026-08-04T08:47:49Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/746","dependencies":[{"issue_id":"gqlc-ibg5","depends_on_id":"gqlc-3kds","type":"discovered-from","created_at":"2026-08-03T13:36:33Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-dcap","title":"codegen/neo4j: a query column of ANY VALUE width emits GetRecordValue[any], which does not compile","description":"A query that projects a property whose declared width is `ANY VALUE` resolves to\na `ResolvedProperty` with `GoType \"any\"` and `Kind: codegen.ColumnProperty`. In\n`internal/codegen/neo4j/render_queries.go`, `writeSingleColumnDecodeIndent`\nfalls through for `ColumnProperty` to\n\n    neo4j.GetRecordValue[any](records[0], \"payload\")\n\n`any` is a member of neither `neo4j.PropertyValue` nor `neo4j.RecordValue`, so\nthat instantiation does not compile:\n\n    any does not satisfy neo4j.RecordValue\n\nGeneration still exits 0, because the emitter's format gate only parses.\n\n## Reproduction\n\nAgainst a schema declaring `payload :: ANY VALUE`, add\n\n    // name: BinPayload :one\n    MATCH (b:Bin {id: $id}) RETURN b.payload AS payload\n\nto a fixture and regenerate. `TestGoldenBuild` (and `just test-codegen-fence`)\nfail on the emitted file.\n\n## Why it is not caught today\n\nNo fixture projects an `ANY VALUE` property as a top-level query column. The\nlist-element case is now covered (gqlc-3kds added `schema_list_any_property`'s\nquery columns), but the scalar case is not.\n\n## Fix shape\n\nThe entity path already has the lane: `writeShapelessFieldDecode` reads the\nProps map directly rather than calling `GetProperty[T]`, gated on\n`ridesADriverCarrier`. The query path has the equivalent lane in\n`writeAnyColumnDecodeIndent` (`record.Get`). Route `ColumnProperty` with\n`!ridesADriverCarrier(f.GoType)` to it, mirroring `carriesElemBare`'s\n`ColumnProperty` arm on the element path.\n\n## Acceptance\n\nA fixture projecting an `ANY VALUE` property as a query column, whose golden\ncompiles under `TestGoldenBuild`; and the neo4j runtime corpus harness\n(`internal/codegen/neo4j/corpus_test.go`) exercising the emitted method over a\nrecord whose value is nil, asserting the caller is handed nil rather than a\ndecode error. Reverting the routing must redden the harness on the build.\n","notes":"SEQUENCING (2026-08-03, epic coordinator): held until fix/neo4j-carrier-followups merges. Both fixes land in internal/codegen/neo4j/render_queries.go, the same file that branch rewrites (carriesElemBare, walkListElemBody's ColumnProperty arm, walkListElemPlan's index gate). dcap's own fix shape — route writeSingleColumnDecodeIndent through ridesADriverCarrier, mirroring what carriesElemBare now does one axis down — only exists once that branch lands. Genuine territory collision, not a conflict-risk excuse. Staff both together as one branch afterwards; they are the same file and the same class.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T17:36:19Z","created_by":"areqag","updated_at":"2026-08-04T08:47:48Z","closed_at":"2026-08-04T08:47:48Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/747","dependencies":[{"issue_id":"gqlc-dcap","depends_on_id":"gqlc-3kds","type":"discovered-from","created_at":"2026-08-03T13:36:19Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rv0h","title":"conformance: a backend's sentinels are unnameable in a manifest, so no invalid fixture can cover one","description":"The conformance corpus can express a negative fixture for a shared-phase sentinel and cannot express one for a backend-local sentinel. invalid/*/manifest.json names its expected error through sentinelByName, whose lanes are built from three packages only: codegen., queryfile. and cypher.. Resolution runs through the hand-written sentinelIdent switch, and the conformance package deliberately imports no single backend — it resolves targets through the composed registry.\n\nSo age.ErrUnsupportedSchema and age.ErrUnsupportedQuery have no spelling a manifest can carry. A fixture naming either fails TestInvalid on the lookup (\"unknown sentinel name %q\"), not on the backend's behaviour, and TestSentinelReachability cannot see them at all.\n\nCONSEQUENCE TODAY. Both AGE sentinels are held only by in-package tests under internal/codegen/age. That is real coverage, but it is not the corpus's, so the corpus's own claim to be the place a backend's refusals are recorded is false for every backend-local refusal. The gap is pre-existing; gqlc-35yu.15 hit it when deciding whether the multi-label refusal should gain an invalid/ fixture and concluded it could not, recording that in ADR 0027.\n\nSHAPE OF THE FIX. Give the registry a way to publish a backend's sentinels — each registered target contributing a name-to-error map that the conformance harness composes into sentinelByName alongside the three existing lanes. That keeps the no-single-backend-import property, which is the constraint that makes the naive fix (import age in conformance_test.go) wrong.\n\nNote the fix must also decide what TestSentinelReachability means for a backend sentinel: a per-target reachability requirement, or an opt-in, since not every target has a fixture for every other target's refusals.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T17:32:35Z","created_by":"areqag","updated_at":"2026-08-04T00:07:13Z","external_ref":"https://github.com/areqag/gqlc/issues/748","dependencies":[{"issue_id":"gqlc-rv0h","depends_on_id":"gqlc-35yu.15","type":"discovered-from","created_at":"2026-08-03T13:32:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-df3d","title":"ci: every live recipe selects tests by a hardcoded -run allowlist, so a new live test never runs","description":"Every live recipe selects tests by a hardcoded `-run` allowlist naming the\ntwo top-level tests that existed when it was written:\n\n    justfile:216  test-codegen-live:\n      go test ... -run 'TestLiveSmoke|TestAGESessionInit' ./...\n    justfile:221  test-codegen-live-neo4j:\n      go test ... -run TestLiveSmoke -skip 'TestLiveSmoke/apache-age' ./...\n    justfile:229  test-codegen-live-age:\n      go test ... -run 'TestLiveSmoke|TestAGESessionInit' -skip 'TestLiveSmoke/neo4j' ./...\n\n`go test -run` matches **top-level** test names, so a new top-level live\ntest is excluded from all three. It does not fail, it does not skip\nvisibly, it does not appear in the log at all — the package still reports\n`ok` and the job still goes green.\n\nOn master the allowlist happens to be exhaustive:\n\n    grep -rhoE \"^func Test[A-Za-z0-9_]+\\(t \\*testing\\.T\\)\" test/data/codegen/*.go\n\nreturns exactly `TestAGESessionInit` and `TestLiveSmoke`. **Nothing\nenforces that.** The property the recipes depend on is maintained by\nwhoever happens to remember, and the failure mode when they do not is a\nlive battery that is green because it never ran.\n\n## This is already live\n\n`feat/age-edge-union-live` adds `TestAGERefusesRelationshipTypeAlternation`\nas a top-level test in `test/data/codegen/live_age_dialect_test.go`, and\ndoes not touch the recipes. That branch's central claim — that AGE 1.7.0\nrejects relationship-type alternation, verified against a live container —\ntherefore has no execution path anywhere: not in PR CI, not in the\nnightly, not under `just`.\n\nIt compounds with the AGE arm's gating. `live-smoke-age` is\n`if: github.event_name != 'pull_request'` (`.github/workflows/codegen-live.yml:54`),\nso the AGE half only runs on the nightly cron. A new top-level AGE live\ntest is thus excluded from the one job that would ever have run it.\n\n## The nightly itself is real\n\nWorth recording, because the sibling bead `gqlc-0oau` raises the adjacent\nworry and this narrows it. `codegen-live.yml` fires on `schedule`\n(cron `17 4 * * *`). Run `30794774319` on 2026-08-03 executed\n\n    go test -count=1 -tags codegen_live \\\n      -run 'TestLiveSmoke|TestAGESessionInit' -skip 'TestLiveSmoke/neo4j' ./...\n\nand reported `ok github.com/areqag/gqlc/test/data/codegen 12.677s`. The\ncontainer starts and the tests run. The defect is the selection, not the\nschedule.\n\n## Fix\n\nSelect structurally rather than by name. The recipes already have a\nstructural axis — the `codegen_live` build tag — and the arm split\n(neo4j versus AGE) is what the `-run`/`-skip` pair is really encoding. A\nrecipe that names no test cannot go stale.\n\nIf a name-based split is genuinely needed for the arm separation, then\nthe exhaustiveness the recipes silently assume has to be **asserted**:\na test in the root module that parses `test/data/codegen/*.go` for\ntop-level `Test*` declarations and requires that set to equal the set the\nrecipes name. Set equality, not a count — it self-updates, and it catches\nthe declared-but-unselected case that a count cannot.\n\nNote `-skip 'TestLiveSmoke/apache-age'` operates on **subtest** paths,\nwhich is a different axis from the top-level `-run` and is not affected.\n\n## Acceptance\n\nAdd a throwaway top-level live test that fails unconditionally. Confirm\nit currently passes CI — that establishes the gate is watching nothing.\nThen confirm the fix makes CI red naming it. Then remove it and confirm\ngreen.\n\nFound while auditing whether the nightly AGE battery had ever actually\nexecuted, during the review round for gqlc-35yu.14. Pre-existing repo\ncondition; gqlc-35yu.14 is the first branch to trip it.\n","notes":"PR #882 (fix/live-recipe-structural-selection @ 771b7447), Closes #749. Close on merge, not before.\n\nWHAT LANDED. internal/liverecipes reads the justfile's live `go test` command lines, the workflows that invoke them, and the top-level tests test/data/codegen declares (go/parser + go/build/constraint, so a commented-out func TestFoo is not a declaration). Three rules, each complaint naming the test or the -run alternative rather than a count: (A) every -run alternative is a declared test name; (B) every declared test is claimed by \u003e=1 CI invocation; (C) a live recipe no workflow reaches runs the battery whole. Both input sets are discovered, not listed.\n\nWHERE THE ASSERTION LIVES. TestEveryLiveTestIsRunByARecipeThatNamesIt is in the ROOT module under no build tag, so `just test` runs it on every PR. Behind codegen_live it would run only in the jobs it audits.\n\nRECIPE CHANGE. test-codegen-live loses its -run (whole battery, no other half to subtract, and rule C holds it to running everything). The two CI halves KEEP theirs — that split is the real arm assignment, and keeping it is what makes the union rule non-vacuous.\n\nSHARED TERRITORY. The command-line reader moved out of internal/codegen/age/dialect_test.go (goTestInvocations, shellFields, stripRecipeComment, testFlagValues, selects, recipe-body lookup) into internal/liverecipes, exported. age's liveBuildTag now = liverecipes.LiveBuildTag. AGE witness sweep unchanged and green.\n\nDRILL. On origin/master 26971c2e with an unconditionally-failing TestLiveThrowawayGate: just test rc=0, and none of the three allowlists selects it. On this branch: just test rc=1 naming TestLiveThrowawayGate. Removed: rc=0.\n\nMUTATIONS. 38 rows, 38 KILLED, 0 SURVIVED. Preconditions: go vet \u003cpkg\u003e for Go rows, just --summary + bash -n on all three live recipe bodies for justfile rows, + actionlint for the workflow row. Driver at /tmp/factory/df3d-mutate.py, results /tmp/factory/df3d-matrix.json. One row SURVIVED first (deleting liveInvocations' comment cut changed no answer — command position already rejects a leading #); two rows added to separate the two, then KILLED.\n\nGATES at 771b7447 in a detached worktree: fmt-check 0, tidy-check 0, lint 0 issues, test 0, test-codegen-fence 0.\n\nRESIDUE: gqlc-vh74 (which half a test belongs in is asserted nowhere; the guard reads the union only).","status":"in_progress","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T17:23:58Z","created_by":"areqag","updated_at":"2026-08-17T07:55:16Z","started_at":"2026-08-17T07:55:16Z","external_ref":"https://github.com/areqag/gqlc/issues/749","dependencies":[{"issue_id":"gqlc-df3d","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-03T13:24:01Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-edze","title":"codegen: resolver.ResolvedType is not the closed sum prepare.go's switches assume","description":"`prepare.go`'s two catch-all arms — tagged `//gqlc:unreachable column-unknown-variant` and `//gqlc:unreachable list-elem-unknown-variant` — end their switch over `resolver.ResolvedType` by wrapping `ErrOutOfC6Scope` with `t.String()`.\n\n`ResolvedType` is a sealed interface, but a nil interface value is not a member of any arm and reaches the default. Calling `String()` on it panics with a nil-pointer dereference before the `fmt.Errorf` that names the sentinel is ever evaluated.\n\nMeasured with an out-of-package caller assembling `codegen.Input` with `resolver.Column{Name: \"x\", Type: nil}` and calling `neo4j.New().Generate`: panic, not `ErrOutOfC6Scope`.\n\nThat is why both sites remain in §3 of `docs/specs/codegen-sentinel-taxonomy.md` after gqlc-h4ug moved the reachable ones to §2: they are unreachable only because the guard that would report them crashes first. A branch that cannot return its sentinel because it faults is not a branch nothing reaches.\n\nFix: guard the nil case (either at the arm, or once at the top of the column/list-element walk) so the refusal is a wrapped `ErrOutOfC6Scope` naming the offending position. Then the two sites move to §2 with their own assembled-Input cases and their tags come off.","acceptance_criteria":"- A nil `resolver.ResolvedType` in a `Column` and in a `ResolvedList` element returns a wrapped `ErrOutOfC6Scope` naming the query and the position, not a panic.\n- `column-unknown-variant` and `list-elem-unknown-variant` move from §3 to §2 of `docs/specs/codegen-sentinel-taxonomy.md`, with assembled-Input cases in `internal/codegen/conformance/assembled_input_test.go` asserting the sentinel and the exact message.\n- Their `//gqlc:unreachable` tags are removed and `TestSentinelTaxonomy` stays green.","notes":"# gqlc-edze — widened description (proposed; tracker owner to apply)\n\nCurrent title still fits, but is narrower than the defect:\n\n\u003e codegen: the unknown-ResolvedType arms panic before they can return their sentinel\n\nSuggested title:\n\n\u003e codegen: resolver.ResolvedType is not the closed sum prepare.go's switches assume\n\n---\n\n## Why the current description is incomplete\n\nThe bead says the two catch-all arms are unreachable because the guard\nthat would report them crashes first, and that they move from §3 to §2\nonce the nil case is guarded.\n\nBoth halves are now false, and `gqlc-h4ug` has landed the correction:\n\n- **The arms are already reachable, and already return their sentinel\n  cleanly.** `resolver.ResolvedType`'s unexported `isResolvedType()`\n  marker seals the interface against *new implementations*. It does not\n  seal it against the **pointer forms of the existing eight**: every\n  variant declares both `isResolvedType()` and `String()` with **value\n  receivers** (`internal/resolver/validated.go:97-364`), and a pointer's\n  method set contains its value methods, so `*resolver.ResolvedNode`\n  satisfies `ResolvedType` while `case resolver.ResolvedNode:` does not\n  match it. The sum an out-of-package caller can inhabit has **sixteen**\n  forms plus the nil; each of the three switches names **eight**.\n  `t.String()` on a non-nil pointer dispatches to the value method, so\n  the refusal completes as written.\n- **The §3→§2 move is done.** `column-unknown-variant` and\n  `list-elem-unknown-variant` are §2 rows in\n  `docs/specs/codegen-sentinel-taxonomy.md`, paid by assembled-`Input`\n  cases using `\u0026resolver.ResolvedNode{}`, and both `//gqlc:unreachable`\n  tags are off. Those three acceptance criteria are satisfied and should\n  be struck.\n\nThe nil panic is real and unfixed — but guarding nil alone leaves the\neight pointer inhabitants, which is the part the current criteria cannot\nsee.\n\n## What is actually broken\n\n**Two distinct defects at the same arms.**\n\n1. **Nil panics at three reachable sites, not two.** The bead names the\n   column arm and the list-element arm. The parameter assertion is a\n   third: `prepare.go:626` calls `p.Type.String()` on the value that just\n   failed the `ResolvedProperty` assertion. Confirmed by an\n   out-of-package caller — a nil column, a nil list element and a nil\n   param each panic with a nil-pointer dereference rather than refusing.\n   Two further sites carry the same call behind a shadow\n   (`prepare.go:705`, `prepare.go:879`); they are §3 today and would\n   inherit the panic if their shadow ever moved.\n\n2. **The sum is not closed.** Go offers no way to seal an interface\n   against the pointer forms of its own value-receiver implementations,\n   so \"the switch names every variant, therefore the default is dead\" is\n   not an argument this codebase can make about `ResolvedType` — which\n   is why §5.1 step 5 now tells a classifier to count *inhabitants*, not\n   declarations. Decide the posture deliberately: either normalise\n   pointer forms away at `Prepare`'s boundary so the switches see value\n   forms only, or keep refusing them and treat the default arm as a\n   permanent §2 branch. What is not acceptable is a default arm whose\n   behaviour nobody chose.\n\n## Corrected acceptance criteria\n\n- A nil `resolver.ResolvedType` in a `Column`, in a `ResolvedList`\n  element, and in a `ResolvedParameter` returns a wrapped\n  `ErrOutOfC6Scope` naming the query and the position — not a panic.\n  Three assembled-`Input` cases, one per site, each asserting the\n  sentinel and the exact message.\n- No `%s`/`String()` on a `ResolvedType` that has not been proven\n  non-nil, at all five sites (`prepare.go:620`, `:626`, `:705`, `:879`,\n  `:1050`).\n- The posture on the eight pointer inhabitants is chosen and recorded:\n  either they are normalised to their value forms before the switches\n  run (and the existing `column-unknown-variant` /\n  `list-elem-unknown-variant` §2 cases are re-pointed at the nil input,\n  which then becomes what reaches those arms), or they stay refused and\n  the §2 rows stand as written. Whichever is chosen, no row anywhere in\n  the taxonomy argues that a `ResolvedType` switch is total because the\n  interface is sealed.\n- `docs/specs/codegen-sentinel-taxonomy.md` §3's `param-type-invariant`\n  and `column-type-invariant` rows drop their `(gqlc-edze)`\n  parentheticals, since the nil panic they cite is gone. Their\n  *conclusions* are unaffected — Phase A still shadows both — so the\n  rows stay in §3.\n- `TestSentinelTaxonomy`, `TestReachableBranchesAreReached` and\n  `just test-codegen-fence` stay green, and every §2 case still names a\n  distinct `prepare.go` line under mutation.\n\n## Note on sequencing\n\nThis still lands after `gqlc-h4ug`. It edits the same `prepare.go` arms\nand the same taxonomy rows, and it now also edits §2 cases `h4ug`\ncreated.\nMEASURED (2026-08-03, gqlc-h4ug round-6 review). Two facts that sharpen this bead's posture question.\n\n1. All EIGHT typed-nil pointer forms fault, not only the nil interface. Go emits an explicit nil check for a value method reached through a nil pointer, so even the zero-sized (*resolver.ResolvedUnknown)(nil) panics: 'value method ...ResolvedUnknown.String called using nil *ResolvedUnknown pointer'. This closes, in the harsher direction, the open question of whether a typed nil panics or not depending on whether the method body dereferences. It always panics.\n\n2. resolver.ResolvedList{Element: nil} also faults, at prepare.go:1050, with 'invalid memory address or nil pointer dereference'. That is a third carrier of the same defect beyond the five call sites this bead already names.\n\n3. The premise is wider still: the marker seals ResolvedType against NOTHING. Go promotes an embedded type's unexported methods, so 'type embedNode struct{ resolver.ResolvedNode }' satisfies ResolvedType from any package and reaches prepare.go:620, :1050 and the parameter assertion cleanly, no panic. So the 'normalise pointer forms away at Prepare's boundary' option in this bead's description does NOT close the sum — an embedded form is neither a value form nor a pointer form. If the posture chosen is normalisation, it must be normalisation to the eight KNOWN value forms with an explicit refusal for everything else, not a pointer-to-value deref. See [[gqlc-856f]].","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T17:06:43Z","created_by":"areqag","updated_at":"2026-08-03T23:57:25Z","external_ref":"https://github.com/areqag/gqlc/issues/741","dependencies":[{"issue_id":"gqlc-edze","depends_on_id":"gqlc-h4ug","type":"discovered-from","created_at":"2026-08-03T13:06:43Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-x5dq","title":"codegen/age: the offset-sidecar collision gate keys on the time.Time carrier, so admitting zoned TIME silently drops its coverage","description":"`rejectOffsetSidecarCollisions` (internal/codegen/age/errors.go) refuses\na schema that declares the property name the AGE backend derives for a\nTIMESTAMP property's zone. It decides which properties derive a sidecar\nby testing `f.GoType != goInstant`, i.e. by the Go carrier text\n`time.Time`.\n\nThat test is right today only because TIMESTAMP is the sole admitted\nwidth that derives a sidecar. The owner-approved encoding table on\ngqlc-35yu.11 gives the same flat `\u003cf\u003eOffset` sidecar to an\noffset-bearing TIME. TIME's carrier is not `time.Time` — it is\n`dbtype.Time` on the neo4j targets, and whatever backend-invariant\ntemporal carrier eventually unblocks it here. So the day TIME is\nadmitted, the gate keeps compiling, keeps passing its own tests, and\nsilently stops covering the width that was just added. The schema that\ndeclares both `startedAt :: TIME` and `startedAtOffset :: INT64` would\nthen generate a decoder that reads one key twice and re-zones the value\nby the author's own property, which is the exact defect this gate was\nbuilt to refuse.\n\nWHAT TO DO: key the gate on \"this field derives a sidecar\" rather than\non the carrier text. The cleanest spelling is a single predicate beside\n`offsetProperty` that both the gate and `writeEntityFieldDecode`\nconsult, so a width that gains zoning gains collision-refusal in the\nsame edit and cannot gain one without the other.\n\nThe negative fixture\ntest/data/codegen/invalid/property_field_collision_offset_sidecar/ and\nthe `TestRejectsAnAuthorOwnedOffsetSidecar` sweep in\ninternal/codegen/age/age_test.go both pin the TIMESTAMP case and should\ngrow a row per zoned width as each is admitted.\n","notes":"Round 2 (PR #848, head 45ae5930): the sweep's width walk was itself fail-open at 'const TypeFoo = PropertyType(\"FOO\")' and 'var TypeFoo PropertyType = \"FOO\"' — this bead's own fail-open survived against either spelling. declaresAWidth now admits a PropertyType conversion and the walk reads var blocks; rows C/D kill by name. The sweep's carried figure is pinned by membership (require.ElementsMatch) rather than require.NotZero. Leave in_progress; close on merge — the coordinator holds the merge.","status":"in_progress","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:58:52Z","created_by":"areqag","updated_at":"2026-08-17T07:48:20Z","started_at":"2026-08-17T05:46:24Z","external_ref":"https://github.com/areqag/gqlc/issues/734","dependencies":[{"issue_id":"gqlc-x5dq","depends_on_id":"gqlc-35yu.11","type":"discovered-from","created_at":"2026-08-03T12:58:52Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-x5wx","title":"codegen/age: agtypeZone accepts any int64 as a UTC offset, so a foreign graph decodes into a fictional zone","description":"The emitted `agtypeZone` helper reads the `\u003cf\u003eOffset` sidecar with\n`agtypeInt64` and hands whatever integer it finds straight to\n`time.FixedZone(\"\", int(offset))`. Any int64 is accepted. `1000000000`\nseconds is a zone 31 years east of UTC, and the decoder returns it\nwithout complaint; the caller gets a `time.Time` whose `Zone()` reports\na fictional offset and whose wall clock is decades off the instant it\nwas stored beside.\n\nThis is not a theoretical hole. The sidecar exists precisely because\ngqlc does not write it — it is the interop affordance for a graph some\nother tool populated (see the read-without-write note on gqlc-35yu.11).\nSo every value that ever reaches this helper comes from outside gqlc,\nwhich is exactly the input a decoder should not trust. The one class of\nmalformed sidecar the helper does reject is a non-integer agtype, and\nthat rejection is incidental: it comes from `agtypeInt64`, not from any\ndecision about what an offset is.\n\nThe instant itself has the same shape of gap one level down —\n`agtypeInstant` accepts any int64 micros, so a sidecar-free property\nholding `9e18` decodes to a year in the 290-thousands rather than\nfailing — but that one is far less reachable, since gqlc does write the\ninstant.\n\nWHAT TO DO: range-check the offset in the emitted helper and return an\nerror naming the property when it falls outside. The bound worth\npicking is the one Go's own zone handling and ISO 8601 agree on:\nstrictly within +/- 24 hours, i.e. (-86400, 86400). Tightening further\nto ISO 8601's actual range (-64800, 57600) is defensible but rules out\noffsets no real zone uses, so it buys nothing and could refuse a graph\na future zone database would accept.\n\nThe test belongs in `internal/codegen/age/testdata/corpus_test.go.txt`,\nbeside `TestAgtypeZoneReadsAnOffsetInSeconds`, which already exercises\nthe malformed-agtype arm and runs the emitted helper for real rather\nthan reading its source.\n","notes":"Round 2 (PR #848, head 45ae5930): independent adversarial review returned REVISE on one blocker + 4 nits; all addressed. Blocker was in the round-1 B1 fix, not in this bead's bound. Bound itself measured clean both edges both directions by the reviewer. Leave in_progress; close on merge — the coordinator holds the merge.","status":"in_progress","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:58:46Z","created_by":"areqag","updated_at":"2026-08-17T07:48:19Z","started_at":"2026-08-17T05:46:24Z","external_ref":"https://github.com/areqag/gqlc/issues/735","dependencies":[{"issue_id":"gqlc-x5wx","depends_on_id":"gqlc-35yu.11","type":"discovered-from","created_at":"2026-08-03T12:58:46Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jhi2","title":"ci: shellcheck is not wired into CI, so the hooks' disable directives are comments","description":"`.githooks/bd-gh-sync:510` carries a load-bearing directive:\n\n    # shellcheck disable=SC2086 # deliberate split\n    bd github push $1\n\nThe unquoted expansion is **wanted** — the ids must become separate argv words —\nand `gqlc-w4q9` added the id gate that makes it safe. Adversarial review\nconfirmed the split is load-bearing: quoting `$1` reddens the batching case and\nthe assertion \"each bead id is its own argv word\", which reports\n`argv was: github|push|b-a1 b-a2|`.\n\nSo the directive documents a genuine, deliberate exception. But **nothing\nenforces it**, because `shellcheck` is not installed on the development machine\nand is not wired into CI. `just check-hooks` only verifies that `core.hooksPath`\npoints at `.githooks`; it does not lint anything.\n\nThe consequence is that every other SC-class defect in the hooks — the ones the\ndirective is carving an exception *out of* — is also unchecked. This repo has\nalready been bitten by three of exactly that class: a pipeline reporting only\nits last command's status, `if cmd | tail -2; then` being unconditionally true,\nand `${_l##*FAILED*}` matching both \"contains\" and \"is empty\".\n\nA disable directive in a tree with no linter is a comment.\n\n## Fix\n\nAdd `shellcheck` to CI over `.githooks/`. It must be a job that already exists\nor, if a new one is genuinely needed, note that **GitHub derives required status\ncontexts from job ids** — adding a job means the branch-protection contexts need\nupdating in the same change, and renaming or splitting an existing job breaks\nevery open PR.\n\nFolding it into the existing `lint` job is the lower-risk option and needs no\nbranch-protection change.\n\n## Acceptance\n\nIntroduce a real SC2086 violation elsewhere in `.githooks/` and confirm CI goes\nred naming the file and line. Then confirm the deliberate `disable` at `:510`\ndoes **not** redden — that is the whole point of the directive, and a linter\nthat fails on the sanctioned exception is one nobody will keep.\n\nVerify the violation currently passes CI first, so the gate is known to be\nwatching something.\n\nFound during the adversarial review of `gqlc-w4q9`. Pre-existing repo\ncondition, not that branch's doing.\n","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:53:55Z","created_by":"areqag","updated_at":"2026-08-04T09:42:15Z","closed_at":"2026-08-04T09:42:15Z","close_reason":"Merged in PR #778 (77a8cc12)","external_ref":"https://github.com/areqag/gqlc/issues/736","dependencies":[{"issue_id":"gqlc-jhi2","depends_on_id":"gqlc-w4q9","type":"discovered-from","created_at":"2026-08-03T12:53:59Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-mbe0","title":"bd-gh-sync: the GH listing is a count over a set the script never witnessed","description":"`gqlc-w4q9` established the rule that **a count reaches the summary only if the\nscript enumerated the set behind it**, and applied it to both bead snapshots.\nIt was not applied to the GitHub listing.\n\n`gh issue list --state open` answering a valid `[]` yields `stale=[]`, which\nwalks to `closed 0 of 0 stale GH mirror(s)` and exit 0 — a count over a set the\nscript never witnessed, which is the class the rule exists to close.\n\nThis is genuinely weaker than the bead case, which is why it was returned\nnon-blocking rather than promoted: an empty open-issue list is a legitimate\nanswer, so emptiness alone is not decidable as failure.\n\n## But a witness exists\n\nAfter a run that pushed **N \u0026gt; 0** new beads, `bd github push` has just minted N\nopen issues. An empty open listing in the same run is therefore a lost one, not\na true empty. That is a decision procedure for the case that matters, and it\ncosts one comparison.\n\n## The same bead covers listing truncation\n\n`--limit 500` and `--limit 1000` (both pre-existing on master) silently cut a\nlisting at the cap. A run that hits the cap produces `closed C of S stale` over\na **truncated** set — the identical defect, arriving by a different route, and\none that gets more likely as the repo grows. A listing that returns exactly the\ncap is indistinguishable from one that was cut, unless the script asks.\n\n## Fix\n\nTwo guards, one rule:\n- If the run pushed N \u0026gt; 0 and the subsequent open listing is empty, the count is\n  unwitnessed. Say so and do not exit 0 claiming success.\n- If a listing returns exactly its `--limit`, treat it as truncated rather than\n  complete — either paginate or refuse to count over it.\n\n## Acceptance\n\nMutate the `gh` stub to return `[]` after a push of N \u0026gt; 0 and confirm the run\nreports the unwitnessed count rather than `closed 0 of 0` at exit 0. Mutate it\nto return exactly `--limit` records and confirm the run refuses to count.\nVerify both currently pass silently first, so the tests are known to be watching\nsomething.\n\nFound during the adversarial review of `gqlc-w4q9`, returned non-blocking.\n","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:53:53Z","created_by":"areqag","updated_at":"2026-08-04T09:42:12Z","closed_at":"2026-08-04T09:42:12Z","close_reason":"Merged in PR #778 (77a8cc12)","external_ref":"https://github.com/areqag/gqlc/issues/737","dependencies":[{"issue_id":"gqlc-mbe0","depends_on_id":"gqlc-w4q9","type":"discovered-from","created_at":"2026-08-03T12:53:58Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-3kds","title":"codegen/neo4j: the query-column path emits elem.(any), which is false for the null it exists to carry","description":"The neo4j emitter's **query-column** path emits `elem.(any)` when narrowing a\nlist whose element width is `ANY VALUE`. That assertion is false for exactly the\nvalue the width exists to carry: `x.(any)` is **false when the interface value\nis nil**. Demonstrated — `[]any{1, nil, \"x\"}` type-asserted element-wise to\n`any` gives `ok=true, ok=false, ok=true`.\n\nSo a list containing a null decodes as a decode error rather than as a nil\nelement. AGE's `agtypeListOfAny` decodes the same value to `nil`, so the two\nbackends disagree at runtime on a value both are supposed to carry — which\n`TestBackendInvariantSurface` cannot see, because it compares **declarations\nonly**.\n\n## Reproduction\n\nAdd `RETURN b.bag, b.piles` to\n`test/data/codegen/valid/schema_list_any_property/queries.cypher`. The emitted\n`queries.cypher.go` contains `elem.(any)` and `elem4.(any)`.\n\n## Why it is not caught today\n\nNo fixture routes an `ANY`-width list through a query column — the entity-property\npath is covered, the query-column path is not. `gqlc-35yu.13`'s driver-carrier\ncensus **does** catch this the moment a fixture reaches it, so the fix is a\nfixture plus the narrowing correction, not new guard machinery.\n\n## Fix\n\nNarrow through the same driver-carrier rule the entity path uses: an `ANY`-width\nelement rides `[]any` and its elements need no assertion at all, because they are\nalready `any`. Emit the element directly rather than asserting it.\n\n## Acceptance\n\nAdd the query columns above as a fixture. Verify the census names `elem.(any)`\nas an offender **before** the fix, so the test is known to be watching\nsomething, then fix the narrowing and confirm the emitted decoder returns a nil\nelement rather than an error for a list containing a null — asserted as a\n**value** in `internal/codegen/neo4j/corpus_test.go`, which runs the emitted\nbytes, not in a golden.\n\n## Provenance\n\nPre-existing on master: `internal/codegen/neo4j/types.go` already recursed lists\nand mapped `ANY VALUE` to `any`, and `internal/codegen/neo4j/render_queries.go`\nis untouched by `gqlc-35yu.13`.\n\nCommit `eeab7ff` on that branch says this \"is filed rather than baked into a\ngolden here\". It was not filed — an adversarial review searched `bd list`, every\nrecord in `.beads/issues.jsonl`, and everything created since 2026-08-02, and\nfound no such bead. This bead makes that claim true.\n","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:47:49Z","created_by":"areqag","updated_at":"2026-08-04T00:15:17Z","closed_at":"2026-08-04T00:15:17Z","close_reason":"Merged: PR #752 (3kds/6rh3/vcvh) and PR #754 (.15).","external_ref":"https://github.com/areqag/gqlc/issues/729","dependencies":[{"issue_id":"gqlc-3kds","depends_on_id":"gqlc-35yu.13","type":"discovered-from","created_at":"2026-08-03T12:47:58Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-u039","title":"codegen/age: decodeFunc's default arm decodes an unknown carrier as text, with no diagnostic","description":"`decodeFunc` (`internal/codegen/age/render_queries.go:547-559`) maps a carrier\ntype to the name of the `models.go` helper that decodes it. It names `bool`,\n`int64` and `float64` explicitly and then has:\n\n    default:\n        return \"agtypeString\"\n\nThat `default` is a fail-open. It does not mean \"everything else is a string\";\nit means \"anything I was not taught about is a string\". A carrier the AGE\nbackend gains later — a new numeric width, a byte slice, a composite — reaches\nthis arm and is silently decoded as text. There is no diagnostic, and the\nemitted package still compiles, because `agtypeString` returns a `string` and\nthe field it is assigned to was typed from the same carrier table that\n`decodeFunc` disagrees with. The failure surfaces at runtime, on real data, as\na wrong value or a decode error attributed to the wrong place.\n\nContrast `agtypeCarrier`, whose job is to *decide* the carrier. Here the\ndecision has already been made upstream, so `decodeFunc` is a total function\nover a closed set — and a closed set is exactly the case where `default` should\nbe a refusal rather than a guess.\n\n## Why this is not caught today\n\nEvery carrier currently reachable is one of the four named. So the `default`\narm is unreachable, which means no test exercises it and no test *can* fail\nwhen it becomes reachable. This is the same shape as the other guards this\nepic has been closing: green because it is looking at nothing.\n\n## Fix\n\nMake the switch exhaustive against the carrier set and refuse the unknown case\nat generate time rather than emitting a guess — either a `codegen` sentinel\nerror returned up the emission path, or, if the emission path genuinely cannot\nfail here, a panic naming the carrier, since an unnamed carrier at that point\nis a generator bug and not a user error.\n\nThen pin it: a test that adds a carrier to the carrier table and does not add\nit to `decodeFunc` must go red. A test that merely asserts the four known\ncarriers map correctly does not close this — that is satisfiable by the table\nit copies.\n\n## Acceptance\n\nAdd a fifth carrier to whatever `agtypeCarrier` can return without touching\n`decodeFunc`. Generation must fail, naming the carrier. Verify it does *not*\nfail today first, so the test is known to be watching something.\n\nFound during the adversarial review of `gqlc-35yu.13` and confirmed\npre-existing on master.\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:37:54Z","created_by":"areqag","updated_at":"2026-08-03T16:52:23Z","external_ref":"https://github.com/areqag/gqlc/issues/730","dependencies":[{"issue_id":"gqlc-u039","depends_on_id":"gqlc-35yu.13","type":"discovered-from","created_at":"2026-08-03T12:37:57Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vac9","title":"codegen: the edge-union sum type name is half author-chosen, so the emitted package scope is not closed","description":"Phase B builds an edge-union sum type as `interfaceName := q.Name + field`\nwhere `field = rowFieldName(col.Name)` (internal/codegen/prepare.go, both\nthe ColumnEdgeUnion and the list-leaf arm). A query named ListActions\nreturning `r` therefore emits the package-level interface `ListActionsR`\nand a marker method `isListActionsR`, and both halves of that name are\noutside the generator's control: the method name comes from the query\nfile and the suffix comes from the author's RETURN clause.\n\nThat makes the emitted package's TOP-LEVEL scope not the closed\ngenerator-owned set the method scope became on\nfix/neo4j-param-name-capture. Two queries whose name-plus-column mangle\ncoincides collide:\n\n    // name: ListActions\n    MATCH (:Person)-[r]-\u003e(:Post) RETURN r\n    // name: ListAction\n    MATCH (:Person)-[sr]-\u003e(:Post) RETURN sr\n\nboth derive `ListActionsR`, so `gqlc generate` exits 0 and `go build`\nreports \"redeclared in this block\". Not silent -- unlike the parameter\ncapture this is a compile error, which is why it was left out of that\nbranch -- but it is a generation that succeeds and produces a package\nthat cannot build, and the diagnostic names neither query.\n\nMeasured, not unmeasured. Two censuses on that branch bound it:\n\n- internal/codegen/conformance/scope_test.go TestColumnNamesBindNothing\n  holds all three targets to renaming every column moving no name the\n  emission BINDS (arguments, receivers, results, body locals).\n- internal/codegen/age/capture_test.go\n  TestOnlyPackageLevelNamesFollowAColumnName holds the AGE corpus to the\n  same thing, plus equality of what each method RESOLVES modulo the\n  package-level declarations that moved. Its substitution is read off\n  the two emissions' own declaration lists, so this residue is exactly\n  the set of names it admits.\n\nClosing it means either putting the sum-type name on the same collision\nsweep the query-text const wants (see gqlc-dfcb's neighbourhood), or\nmaking the suffix positional -- `\u003cMethod\u003eCol0` -- which is a rename of a\ngenerated PUBLIC type and so an API change with a golden-corpus-wide\ndiff. The census stays either way; only what it admits changes.","acceptance_criteria":"Two queries whose `\u003cMethod\u003e + rowFieldName(\u003ccolumn\u003e)` mangle coincides either generate a compiling package or are refused at generate time with a message naming both queries and both columns; a fixture holds the colliding pair; and the guard goes red if the disambiguation is removed. The two censuses that bound the residue — TestColumnNamesBindNothing in internal/codegen/conformance/scope_test.go and TestOnlyPackageLevelNamesFollowAColumnName in internal/codegen/age/capture_test.go — stay, and the AGE one's declRenaming substitution narrows to admit nothing once the suffix stops being author-chosen.","notes":"Promoted from a code comment by the adversarial review of gqlc-lhs3 (2026-08-03): both censuses carried a comment saying the residue 'is an API change and not this branch's', which is deferred work living only in a comment. Those comments now cite this bead.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T10:16:10Z","created_by":"areqag","updated_at":"2026-08-03T10:48:27Z","external_ref":"https://github.com/areqag/gqlc/issues/726","dependencies":[{"issue_id":"gqlc-vac9","depends_on_id":"gqlc-lhs3","type":"discovered-from","created_at":"2026-08-03T06:48:03Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rz0l","title":"spec: C1/C4/C5 still document the parameter-name-derived bare arg lhs3 removed","description":"The C1/C4/C5 specs still document the single-parameter signature as taking a\n**parameter-name-derived** bare argument. That derivation is dead code, and the\nname it derives is the capture vector `gqlc-lhs3` exists to close.\n\n`docs/specs/codegen-stage-c1.md` §5.x `\u003cparam-list\u003e` (lines 662-667) states the\nrule in full: `paramFieldName(name)` → `MinAge` → lowercase → `minAge`. The\nemitter does not do that. It emits `arg`, unconditionally, precisely so that no\nauthor-chosen parameter name reaches the signature and captures a\ngenerator-owned local.\n\nSo a reader implementing to spec reintroduces the vulnerability. This is not\ncosmetic drift — it is a spec that documents the defect as the design.\n\n## Sites\n\n`docs/specs/codegen-stage-c1.md` — the authoritative section, per\n`codegen-stage-c0.md:1112`, which routes \"Method-signature naming rules (Params\nvs bare arg…)\" here:\n\n- §3.1 line 243 — `func (q *Queries) PersonById(ctx context.Context, id int64)`;\n  emitted is `arg int64`.\n- §3.2 lines 273-275 — \"single-parameter takes the bare typed arg\". Shape right,\n  name wrong.\n- §5.x `\u003cparam-list\u003e` lines 662-667 — the whole `\u003cbareParam\u003e` derivation rule.\n  Dead.\n- §5.x `\u003cparamsMap\u003e` line ~678 — \"One parameter:\n  `map[string]any{\"\u003crawName\u003e\": \u003cbareParam\u003e}`\". Emitted is `arg`. Note the map\n  **key** is still the raw name and correctly so; only the value moved.\n- §5.5 worked example lines 1203-1204 — `PersonName(ctx context.Context, id\n  int64)` / `map[string]any{\"id\": id}`.\n- §9 fixture table line 1152 — \"single-param bare arg (`func (q *Queries)\n  PersonName(ctx, id int64)`)\".\n\n`docs/specs/codegen-stage-c4.md` — lines 362, 403, 838. Line 838 is inside the\n`WriteQuerier` **interface** member, so the drift is in a declared surface, not\nonly in prose: `RemovePerson(ctx context.Context, id int64) error`.\n\n`docs/specs/codegen-stage-c5.md` — lines 438, 996: `PersonAction(ctx, id\nint64)`, `GetAction(ctx, since int64)`.\n\n## Separately, §4.6 is falsified\n\n`codegen-stage-c1.md` §4.6 lines 513-529 — \"Why `\u003cmethodName\u003eQueryText` consts\ndo not participate in the sweep\" — rests on the claim that \"a duplicate at this\naxis would already imply a duplicate method-name collision the exported sweep\ncatches first\". `gqlc-igs4` is a worked counterexample: schema label\n`FooQueryText` plus `// name: DecodeFoo` collides a generator-owned const with a\ngenerator-owned func, no method name is duplicated, and the axis §4.6 defers to\nnever sees it. The corresponding code comments were corrected on `lhs3`'s\nbranch; the spec was not.\n\nFix §4.6 to state the hole, or to state the disambiguation once `gqlc-igs4`\nlands. Do not restate `gqlc-igs4`'s argument here — cite it.\n\n## Why this recurs\n\nNothing holds these specs against the emitter. Every `func (q *Queries) …`\nsignature in the specs is a claim about emitted bytes that the golden corpus\nalready contains, and no test compares them. Consider whether the worked\nexamples can be extracted and diffed against a golden, or a fixture named from\nthe spec section — and if that is not tractable, say so explicitly in the fix so\nthe next drift is expected rather than surprising.\n\nAcceptance: no spec under `docs/specs/` shows a single-parameter query method\ntaking a parameter-name-derived argument; §4.6 states what is true about the\nQueryText const namespace; and either a check exists that reddens when a spec\nsignature diverges from the emitted one, or the specs carry a note recording\nthat they are unfenced prose and why.\n","notes":"Round-4 review fixes pushed as a760efd7 on docs/c1-bare-arg-spec-drift (PR #804). Bead + GH #715 were closed while #804 was still unmerged; left as-is per coordinator. CLOSE-ON-MERGE: #804 carries 'Closes #715' and is 1:1 correct, so the merge is what makes the ledger true — no further bead action needed here.\n\nRound 4 fixed two blockers with one cause (round 2 added the bare-list census and did not re-read the summaries of what the fence does not reach):\n- B4 (permissive): sweepSigs dropped ALL of a listed document's parenthesis-less lists, not only its exhibits, so ADR 0029 could print the capture vector in a graded shape and stay green. Took the STRONG fix: specBareListExhibits names the LISTS verbatim, one census entry per site, reconciled both directions. Restores ADR 0029's 'ctx context.Context, arg int64' (a real claim) to grading; it grades green. Residual hole filed as gqlc-x2sg.\n- B3 (conservative): docRoots comment said narrowing docs -\u003e docs/specs was green; the census names a document under docs/adr/, so it is red. A fourth copy of the same wrong negative was found in ADR 0029's consequences, which the reviewer had not reached.\n\n11 mutations, vet-screened, 10 killed by named tests, 1 expected survivor. Re-measured for the PR body: 14 bare sites (9 ADR / 5 C1), 2 graded and both green; binding-half corpus 592 spans across 35 files (was stated 130/21). Gates 5/5 green at a760efd7 in a detached worktree.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T13:04:37Z","created_by":"areqag","updated_at":"2026-08-17T07:54:46Z","closed_at":"2026-08-17T00:31:22Z","close_reason":"Closed by branch docs/c1-bare-arg-spec-drift at 3dd322dc (12 commits, not yet pushed).\n\nRound-2 adversarial review returned PASS. The reviewer ran 76 mutation rows and, critically, MUTATED ITS OWN GUARD FIRST: hiding the fence file gave GUARD-FAIL rc=99, renaming a top-level gave 'green but toplevel RUN count 8 != 9' rc=97. Each row is gated on the fence file existing in the tree under test, 'no tests to run' being absent, and the '=== RUN TestSpec\u003ctoplevel\u003e' count being exactly 9, with a git-status/HEAD/md5 restore check between rows. That guard discipline is what makes the 76 green rows mean anything.\n\nAll six round-1 findings discharged:\n- The unwitnessed 'unclosed' join is closed; the two rows that were GREEN at dee2101e are now RED.\n- 'Four load-bearing arms' corrected to six, each named, all six die by name.\n- 'docRoots cannot shrink quietly' replaced by 9 enumerated clauses, all 9 reproduce.\n- c1.md:797 'the whole of what is fenced' closed; c1c RED / c1j GREEN is the discriminator.\n- c5 §5.6 -\u003e §5.5 corrected, and EVERY other section citation re-verified against the current documents (they moved 191 lines on this branch, so round 1's check was stale). All resolve.\n- The per-document census floor is documented rather than closed, correctly -- see gqlc-0rjn.\n\nThe reviewer also overturned two of the implementer's own self-criticisms as overstated: the synthetic-corpus gap is not a hole (severing the real corpus at either the file list or the reader is RED), and the six-arm claim holds 6/6 with controls (twelve rows, no vacuous passes). It ruled rather than deferred on the last unwitnessed layer: the declined ~40-line extraction could never have witnessed g3, so it was a strict subset of an unclosable hole and declining it was correct.\n\nWent looking for a fifth false universal on the theory that four is a pattern, and found none.\n\nGates clean: gofmt, go build, go vet (only pre-existing generated-ANTLR noise), go test ./internal/... rc 0.","external_ref":"https://github.com/areqag/gqlc/issues/715","dependencies":[{"issue_id":"gqlc-rz0l","depends_on_id":"gqlc-lhs3","type":"blocks","created_at":"2026-08-02T09:04:43Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"01a00e40-e9f1-7106-92c9-8218cbcb4e49","issue_id":"gqlc-rz0l","author":"areqag","text":"First independent review (2026-08-17) returned REVISE with three blockers; all fixed at 8dfad24b, branch rebased onto c1558e9e, head dbeabc3a pushed to PR #804.\n\n1. docs/specs/codegen-stage-c1.md said the fence 'reads nothing off the type at all' and applied it to the \u003cparam-list\u003e bullet, the one place it does read a type. Witness: ', arg \u003cT\u003e' -\u003e ', arg \u003cU\u003e' reddens TestSpecMethodArgIsGeneratorOwned with the name untouched. Scoped to the signature sweep.\n2. 'Three things are left open' was a completeness claim; now an explicit floor of five with beads, adding gqlc-jnsk (HTML-comment site keeps a census entry) and gqlc-173n (unwrapConversions strips * and \u0026).\n3. 599/1630 comment lines carried change narration and rejected-alternative justification, both AGENTS.md violations. Rationale moved to ADR 0029 (docs/adr/0029-the-codegen-spec-fence.md, nine decisions); comments cite it. Now 515 comment lines.\n\nADR 0029 is written without the fence's own anchors: docs/ is swept whole, so the first draft reddened the fence on its own rationale via the undeclared census direction.\n\nGates green at dbeabc3a: lint (0 issues, genuine completion), test, tidy-check, test-codegen-fence, actionlint, vuln (0 called). MERGE IS HELD by the orchestrator pending independent re-review; auto-merge disarmed.","created_at":"2026-08-17T05:45:30Z"}],"dependency_count":1,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-yr3n","title":"codegen/age: the Scalar type table has zero corpus reach","description":"AGE's `Scalar` type table has **zero** corpus reach. It is checked only by a\nhand-written mirror of itself.\n\nReproduction (from `gqlc-9n6t`'s review): replace all six AGE `Scalar` arms in\n`internal/codegen/age/types.go` with garbage — `MUT_bool`, `MUT_int64`, and so\non — and run\n\n    go test -count=1 ./internal/codegen/conformance/ -update\n\n**Zero goldens change.** `TestValid`, `TestDoubleRun`, `TestGoldenBuild` and\n`TestBackendInvariantSurface` all stay green. The only test that fires is\n`internal/codegen/age/types_test.go:TestTypeMapScalar`, which is a hand-written\ncopy of the table it is checking — so it asserts the table equals itself and\nwould move happily alongside any edit to it.\n\n`ScalarBool`, `ScalarInt`, `ScalarFloat` and `ScalarString` are *served* by AGE,\nso this is not a case of nothing being reachable. No valid fixture enrols an AGE\ntarget on a query projecting a bare scalar **expression** column. Property\ncolumns take a different path (`Property`, not `Scalar`) and are well covered,\nwhich is why the gap is easy to miss.\n\nThis is another instance of the pattern this epic keeps turning up: a check that\nis green because it is looking at nothing. It is pre-existing — `9n6t` neither\ncaused it nor worsened it — but `9n6t` added the claim that the type tables are\n\"total, and on a ground that can be checked\", and on AGE the ground is not\nchecked end to end.\n\nFix: enrol at least one AGE-targeted valid fixture per served `Scalar` arm,\nprojecting a bare scalar expression column (e.g. `RETURN 1`, `RETURN 'x'`,\n`RETURN 1.5`, `RETURN true`, or the same via a function call whose return type\nresolves through `callProjectionType`). Then the golden diff is the check and\n`TestTypeMapScalar` stops being the only thing standing between a wrong arm and\na green build.\n\nAcceptance: mutating any served AGE `Scalar` arm reddens the conformance suite\nthrough a golden diff, not only through `TestTypeMapScalar`; and the mutation\nsurvives `-update` (regenerating must not make it green, because the emitted Go\nmust stop compiling or stop matching the invariant surface).\n","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T12:25:29Z","created_by":"areqag","updated_at":"2026-08-02T12:36:10Z","external_ref":"https://github.com/areqag/gqlc/issues/710","dependencies":[{"issue_id":"gqlc-yr3n","depends_on_id":"gqlc-9n6t","type":"discovered-from","created_at":"2026-08-02T08:25:28Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-p6cb","title":"codegen/age: list expression columns still refused though .13 built the decode path","description":"`gqlc-35yu.13` taught the AGE backend to serve list-valued *properties*. It did\nnot lift the refusal on list-valued *expression* columns: `resolver.ResolvedList`\nis still listed in `unservedColumn` for `apache-age-pgx-v5`.\n\nThat refusal was correct when AGE had no list decode path at all. It now has\none — agtype carries arrays natively, which is precisely why `.13` was\ntractable — so the refusal outlives its reason.\n\nThe gap is visible as a backend asymmetry, which is the property this project\nmost cares about: a query returning `collect(p.name)` or a list literal generates\non the neo4j targets and is refused on AGE, so the Go surface a caller writes\nagainst *does* vary by backend for that column kind. `TestBackendInvariantSurface`\ndoes not catch it because the AGE side never generates at all.\n\nNot folded into `.13` because `.13`'s scope was properties, and the expression\npath reaches `ResolvedList` through `callProjectionType` rather than through a\nschema property — different provenance, different width reasoning. Worth its own\nbead precisely so the width argument gets made rather than assumed: a property's\nelement type comes from the schema and is therefore known, whereas a\n`collect()`'s element type is inferred, and the inference may admit element types\nthe decode path cannot represent. The refusal may narrow rather than disappear.\n\nAcceptance: a list *expression* column either generates on AGE with the same Go\nsurface the neo4j targets emit, or is refused for a reason that names the\nelement type it cannot represent rather than the column kind. Either way\n`ResolvedList` stops being an unconditional entry in AGE's `unservedColumn`, and\na fixture pins whichever verdict lands.\n","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T12:25:27Z","created_by":"areqag","updated_at":"2026-08-02T12:36:11Z","external_ref":"https://github.com/areqag/gqlc/issues/711","dependencies":[{"issue_id":"gqlc-p6cb","depends_on_id":"gqlc-35yu.13","type":"discovered-from","created_at":"2026-08-02T08:25:27Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-1dmu","title":"codegen: a relationship type the schema does not declare is narrowed away without a diagnostic","description":"A query naming a relationship type the schema does not declare is accepted\nsilently. gqlc narrows the candidate set to the declared subset and leaves the\nquery text verbatim (ADR 0005), so the emitted code is well-typed over the\ndeclared types while the *server* may return edges of the undeclared one.\n\nSurfaced on `gqlc-35yu.14`, where this is exactly what makes the edge-union\ndispatch's `default:` arm reachable — and there it is legitimate. It models\n**schema drift**: the graph grows a relationship type ahead of the GQL schema,\nand the sealed interface correctly has no member for it, so the row fails at\nruntime with a clear error rather than decoding into the wrong arm.\n\nThe problem is that the same silence covers a **typo**. An author who writes\n`ACTED_INN` gets no diagnostic at generate time — only a runtime failure on\nrows that will never arrive, or worse, a query that quietly matches nothing.\nDrift and typo are indistinguishable to gqlc today.\n\nThey are not indistinguishable in principle: drift means the query names a type\nthe schema lacks *and* the author expects rows of it; a typo means the same\nshape with no such expectation. gqlc cannot read intent, but it can say what it\ndid — narrowing the candidate set is a decision the author should be told about.\n\nFix: emit a diagnostic (warning, not refusal — refusing would break the drift\ncase the `.14` fixture depends on) naming each relationship type in the query\nthat the schema does not declare, and stating that it was excluded from the\ncandidate set. Refusing is wrong here; silence is also wrong.\n\nAcceptance: generating `edge_union_undeclared_relationship_type` still succeeds\nand still reaches the `default:` arm live, but names the undeclared type on\nstderr, and a test holds that message.\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:40:19Z","created_by":"areqag","updated_at":"2026-08-02T11:59:18Z","external_ref":"https://github.com/areqag/gqlc/issues/704","dependencies":[{"issue_id":"gqlc-1dmu","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-02T00:40:18Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-dfcb","title":"[bug] codegen/age: the capture guard parses only .cypher.go, so cross-file emitted names are unswept","description":"The AGE capture guard parses only `.cypher.go` — `emitQueryFiles` filters to that\nsuffix — so package-level declarations emitted into `db.go` and `models.go` are\nnever seen, even though the generated package is one Go package and a query\nmethod's body can resolve them.\n\nDemonstrated by the `gqlc-gjqo` reviewer at `8f80a46`: adding\n`const gqlcDefaultGraph = \"gqlc\"` to `render_db.go` plus a reference to it from\nthe `.cypher.go` body emits a package that compiles (`just test-codegen-fence`:\n0 issues) with **both** capture guards green at exit 0.\n\n## Why this is not already covered\n\n`capture_test.go:322` says capturing a cross-file name \"is a hard failure of the\ngenerated package rather than a silent one (gqlc-ni66)\". That is true **today**\nand **by accident**. The current cross-file package-level set is\n`ErrNoRows`/`ErrMultipleResults` (typed `error`), `DBTX`/`Queries` (types), `New`\n(func), and `maxGraphNameBytes` (untyped int, unreferenced from `.cypher.go`).\nEvery one is loud when shadowed by a `string` parameter because of how it\nhappens to be typed — not because anything prevents a future `string`-typed\ncross-file declaration.\n\nA `string`-typed one is exactly the silent case: it type-checks, and the caller's\nruntime value silently becomes whatever the emitted body uses it for. That is the\nsame shape as the `\u003cbare\u003eQueryText` const, which is the worst instance of this\nwhole class and the reason it blocked `gqlc-35yu.10`.\n\n**Not covered by [[gqlc-ni66]]**, despite the comment's pointer: ni66 is the\n*loud* signature-binder class (`q`, `ctx`, receiver). It does not carry the silent\ncross-file class. **Not covered by [[gqlc-3o3p]]** either, which is the\nlift-the-sweep-to-neo4j axis.\n\n## Why it was not fixed on gqlc-gjqo\n\ngjqo is scoped to shape coverage *within* a backend's `.cypher.go` emission, and\nit closed that. The reviewer notes a per-method free-identifier diff would detect\nthe cross-file class cheaply, but it would go red on findings belonging to other\nbeads, so folding it in would have coupled three separate fixes on one branch.\n\n## Scope\n\nWiden the guard's parse set beyond `.cypher.go` to the whole emitted package, so\nthe reachability check is over the package's real scope rather than one file's.\nExpect it to go red on constructs owned by `gqlc-ni66`; sequence accordingly or\nscope the new assertion to the silent (string-typed) subset.\n\nAlso: correct the `capture_test.go:322` comment, which currently asserts a\nstructural guarantee the code does not have. (A one-line wording fix is being\ntaken on the gjqo branch itself; this bead owns the actual detection.)\n\nAcceptance: a `string`-typed package-level declaration emitted into `db.go` or\n`models.go` and referenced from a `.cypher.go` method body is caught by the\ncapture guard, and the catch survives `go test ./internal/codegen/conformance/\n-update`.\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:25:39Z","created_by":"areqag","updated_at":"2026-08-02T04:36:10Z","external_ref":"https://github.com/areqag/gqlc/issues/699","dependencies":[{"issue_id":"gqlc-dfcb","depends_on_id":"gqlc-gjqo","type":"discovered-from","created_at":"2026-08-02T00:25:39Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-w4q9","title":"[bug] bd-gh-sync push path keeps the id-quoting hazard the pull path just closed","description":"The *pull* path in `.githooks/bd-gh-sync` was hardened on `gqlc-63y`'s branch:\nids are batched by hand rather than through `xargs`, refused by name when they\ncarry characters that would re-parse (quotes, spaces, leading dashes), and the\nsummary reports what was actually pulled rather than what failed to fail.\n\n**The push path was left exactly as it was.** It still does the equivalent of\n`bd github push $_ids` under `xargs`, so:\n\n- an id containing a quote or a space re-parses and aborts the batch;\n- an id with a leading dash arrives as a flag rather than a value;\n- `|| true` swallows the non-zero, so the batch silently pushes nothing and the\n  run still reports success.\n\nThat is the same defect class the pull path just closed, on the arm that\n*mutates GitHub* rather than the one that reads it.\n\nWhy it was not folded into `gqlc-63y`: push has no summary contract to assert\nagainst — the pull fixes are all anchored on \"the summary must say what\nhappened\", and push says nothing at all today. Closing this properly means\nfirst deciding what push's success/failure contract is, which is a design\nquestion rather than a hardening one, and it does not belong riding on a branch\nthat was already on its fourth round.\n\nScope: give push the same id-refusal gate and hand-rolled batching as pull, and\ngive it a summary contract that distinguishes \"pushed N\" from \"pushed nothing\nbecause every batch aborted\". Reuse the `bd_list_fails` / `bd_list_emits` stub\nknobs that branch added — the harness can now express the breaking input.\n\nAcceptance: a bead id containing a space, a quote, or a leading dash makes\n`bd-gh-sync push` exit non-zero and name the offending bead, rather than exiting\n0 having pushed nothing.\n","notes":"AGENT KILLED BY QUOTA LIMIT 2026-08-03 ~12:50 EDT, mid-refix. NOT a failure. Do not discard.\n\nWorktree /home/antranig/Developer/gqlc/gqlc-w4q9-push, branch fix/bd-gh-sync-push-quoting, HEAD 96093cd (advanced from 445f278, so work WAS committed):\n96093cd githooks: the bd-gh-sync push path reports what it did to GitHub\nc198b3d codegen: TypeMap.Temporal gains a rejection channel (#714)\n\nUncommitted survivors: modified .githooks/bd-gh-sync and .githooks/tests/bd-gh-sync-test.sh. Read the diff before resuming.\n\nOutstanding from the failed review: B1 the close pass reports a count derived from a post-push bead snapshot that fails OPEN when empty — pass the first snapshot in and emit a BLIND status when beads_before is non-empty and beads_after is empty; close the same fail-open on any other route you find (first snapshot empty, both empty). The invariant: the script never prints a count it did not measure. B2 the branch's own prose miscounts — the true figures are 31 added and 30 red, not 30/29; recount from the tree and print the command, and delete any number you cannot print. Promoted: N1 the harness stub is argv-blind, so a wrong argument to gh is invisible — log printf '  ARG=[%s]' \"$@\" and assert on it, since quoting is what this branch is about; N4 the header line is false on the mktemp path; N3 ': \u003e\"$_tmp/close_status.txt\"' is dead.\n\nReminder for whoever resumes: bd-gh-sync mutates shared external state. Never run push or sync against the real repo without --dry-run, and never run bd hooks run post-merge.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:08:58Z","created_by":"areqag","updated_at":"2026-08-03T16:57:24Z","closed_at":"2026-08-03T16:57:24Z","close_reason":"Landed in PR #733","external_ref":"https://github.com/areqag/gqlc/issues/695","dependencies":[{"issue_id":"gqlc-w4q9","depends_on_id":"gqlc-63y","type":"discovered-from","created_at":"2026-08-02T00:08:58Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-m5rc","title":"vuln: the ROOT module's in-package tests are still outside govulncheck's call graph","description":"`govulncheck` does not analyse the in-package test variant of a package. Anything\nimported only by an in-package test drops out of the call-graph analysis with no\ndiagnostic and no non-zero exit.\n\n`gqlc-rohp` established this mechanism, proved it with a single-variable A/B, and\nclosed it in the **nested** module (`test/data/codegen`) by converting the live\nbattery to `package fixtures_test` — 9 modules scanned became 54.\n\n**The root module — the one that ships the compiler — still has it.** 34\nin-package test files versus 16 external. Reproduced in this repo by the\nadversarial reviewer: planting a `yaml.Unmarshal` call into\n`internal/schema/gql/propertytype_test.go` (`package gql`) gives\n`No vulnerabilities found`, exit 0; the identical call in a new file declaring\n`package gql_test` gives exit 3 with a call trace.\n\nSeven packages have testify/rapid/godog reachable **only** from in-package\ntests — 23 files:\n\n    internal/cli                  (3)\n    internal/codegen              (2)\n    internal/queryfile            (1)\n    internal/resolver             (3)\n    internal/schema/gql          (12)\n    internal/schema/gql/annexd    (1)\n    internal/schema/gql/isobnf    (1)\n\nThe subtlety that makes this worth a bead rather than a footnote: `-test` at\nroot **does** raise the scanned module count 31 → 42, so godog/testify/rapid\nappear in the scanned set. That is what makes the gate look closed. But module\npresence in the set is not what determines the exit code — call-tracing from\nthose 23 files is absent, so the root half can go green over a *called*\nvulnerability.\n\nDeliberately NOT done on `gqlc-rohp`: converting 34 files touches every package,\nmay require exporting internals purely to satisfy a scanner, and would be a\nlarge diff riding on a CI-plumbing branch. `rohp` instead states the residual\nplainly in the justfile comment and points here.\n\nScope: convert root in-package test files to external test packages, package by\npackage, so the root scan reaches the same call graph the nested one now does.\nWhere a test genuinely needs unexported access, say so and leave it — the goal\nis to shrink the residual and know its size, not to reach zero by exporting\nthings that should stay private.\n\nAcceptance: plant a called vulnerability reachable only from a root in-package\ntest and confirm `just vuln` exits non-zero. It does not today.\n","notes":"SEQUENCING RULING (2026-08-03, epic gqlc-35yu coordinator): held, not deferred indefinitely. This bead converts 34 root in-package test files across internal/cli, internal/codegen, internal/queryfile, internal/resolver, internal/schema/gql, internal/schema/gql/annexd and internal/schema/gql/isobnf. Five branches are in flight editing tests in internal/codegen, internal/codegen/age, internal/codegen/neo4j and internal/codegen/conformance right now (35yu.11, 35yu.14, 35yu.15, h4ug, the neo4j carrier follow-ups). A package-level test-package conversion rewrites the package clause of files those branches are actively adding to, so this is a genuine territory collision, not a conflict-risk excuse. Its siblings gqlc-nsq4, gqlc-pig9 and gqlc-k22l touch only justfile recipes, .github/workflows/vuln.yml and test/data/codegen/go.mod, so they are being staffed now instead. Take m5rc once the 35yu epic's last bead merges, when the codegen tree is quiet.\n\n## Prose finding from the fix/vuln-gate-walk-and-fence review (PR #813, 2026-08-17)\n\njustfile (master, ~line 692; unchanged by that branch) says of vuln-root-residual:\n\n    \"Third-party\" is anything outside the main module whose first path element\n    contains a dot, which is what puts a package in a vulnerability database at\n    all; .TestImports is exactly the in-package test variant's import set.\n\nThe last clause is false as stated, measured against `go list`:\n\n    go list -f '{{.TestImports}}' ./internal/resolver\n      -\u003e imports of the _test.go files only (19 paths)\n\n    go list -test -f '{{.Imports}}' for `internal/resolver [internal/resolver.test]`\n      -\u003e a strict SUPERSET: the same 19 plus the package's own non-test imports\n         (sort, strconv, ... )\n\n`.TestImports` is the import set of the in-package TEST FILES, not of the test\nVARIANT package. The variant's import set is the union of the package's own\nimports and the test files'.\n\nThe CODE is right and the comment is wrong. The package's own non-test imports\nare already inside govulncheck's closure via the non-test package, so counting\nthem in a \"reachable only through the in-package variant\" residual would inflate\nit. `.TestImports` is the correct field for the metric; only the sentence\ndescribing it needs the one-word repair (\"the in-package test FILES' import\nset\").\n\nFiled here rather than fixed on that branch because the line predates it and is\noutside its diff.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:02:17Z","created_by":"areqag","updated_at":"2026-08-17T05:02:48Z","external_ref":"https://github.com/areqag/gqlc/issues/690","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-320w","title":"codegen/neo4j: the decoder-local guard probes only STRING and FLOAT32, so a new emitter arm fails open","description":"`TestNoDecoderLocalTakesAPropertyName` (added by `gqlc-ak39`) is the best test\nin that change: it emits a decoder, reads the local scope off the emission, and\nasserts scope **equality** rather than scope disjointness. That shape is why it\ncould not be blessed away by `-update` — the adversarial reviewer reverted the\nrenderer and confirmed the test stayed red through a full golden regeneration.\n\nBut `decoderProbeSchema` only declares properties of two widths: STRING and\nFLOAT32. The doc comment claims\n\n  \"a local a decoder gains later is held by this without anyone remembering to\n  add it\"\n\nwhich is true only for locals reachable from those two types plus the\nnullable/narrow arms. A future emitter arm for LIST, or a new temporal carrier,\nthat introduced (say) an `elem` local would never enter `declared` and would\nnever be probed. The guard would stay green while the class reopened.\n\nThis is the same failure mode as [[gqlc-gjqo]] on the AGE side — a guard that is\nstructural on one axis and enumerated on another, so it defends the shapes it\nwas written alongside and silently exempts the rest. Filing it separately\nbecause it is a different guard in a different backend, but the two should\nprobably be fixed with the same idea.\n\nFix: widen `decoderProbeSchema` to one property per emitted `GoType`, so the\nprobe's coverage is derived from the type table rather than from whatever the\nauthor happened to think of. Then the doc comment is true as written.\n\nAcceptance: add a new emitter arm with a fresh local reachable only from a type\nabsent from the current probe schema, and confirm the guard goes red. Today it\ndoes not.\n","notes":"Round-2 final head is 428dfb84 (not 5c4e9c8b). While re-reading 5c4e9c8b I found I had introduced a fresh instance of the very defect B1 named: the new both-directions clause read 'every carried width is one the probe declares', which my own depth-3 row falsifies — a three-deep list IS carried and is NOT declared by the probe. 428dfb84 scopes that clause to the set the loop actually iterates ('over that set ... every carried constant'). Both round-2 commits are comment-only; `git diff -U0 795ecbdf..HEAD` has no non-comment line. Gates re-run at 428dfb84 in a fresh detached worktree: fmt-check, tidy-check, test, lint (0 issues), test-codegen-fence (0 issues) all rc=0.","status":"in_progress","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:00:30Z","created_by":"areqag","updated_at":"2026-08-17T07:50:56Z","started_at":"2026-08-17T06:17:37Z","external_ref":"https://github.com/areqag/gqlc/issues/691","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-b7lt","title":"bd-gh-sync: a held bead deleted between snapshots is silent","description":"`.githooks/bd-gh-sync` takes two `bd list` snapshots around the pull and warns\nwhen a bead that was held out of the pull changed anyway — the detector that\ncatches a concurrent writer (typically `bd hooks run post-merge` replaying JSONL\nover newer DB state) clobbering state the sync deliberately protected.\n\nAt `:239` the comparison skips any `bid not in after`. So a held bead that is\n**deleted** between the two snapshots produces no warning at all: it vanishes\nsilently, and the run reports as healthy.\n\nFound in the round-3 adversarial review of `fix/bd-gh-sync-state-fidelity` at\n`d25d947`. Ruled out of scope for that branch, which was already carrying two\nblockers plus three promoted findings.\n\nDeletion is the most destructive thing that can happen to a held bead, and it is\nthe one transition the detector is blind to. The skip is presumably there to\navoid false positives from beads legitimately absent from the second snapshot —\nso the fix is not simply to drop the guard, but to distinguish \"absent because\ndeleted\" from \"absent because it was never in scope\", which means the detector\nneeds to reason about the set of ids it actually held rather than about the\nsnapshot intersection.\n\nNote this interacts with [[bd-gh-sync-postcondition-blind]]: if the second\nsnapshot fails to load at all, every held bead is \"not in after\" and the whole\ndetector goes quiet. Fix that one first or this one's test will be built on\nsand.\n\nAcceptance: a test where a held bead is deleted between snapshots must produce\na warning on the last line. Verify by mutation that the test bites.\n","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:47:00Z","created_by":"areqag","updated_at":"2026-08-17T00:26:44Z","closed_at":"2026-08-17T00:26:44Z","close_reason":"Closed by branch fix/bd-gh-sync-pull-witness at 0c13ddcb (not yet pushed). Commit c41b12fb 'a held bead deleted between the pull's snapshots is not silent', with 861fbf29 carrying the deleted bead's record tag through to the summary.\n\nAdversarial review of the branch returned PASS; the deletion path is covered by the assertion census and by recorded mutants that all exit 1. See gqlc-nvjz for the full evidence list and gqlc-tvv7 for the residue.","external_ref":"https://github.com/areqag/gqlc/issues/686","dependencies":[{"issue_id":"gqlc-b7lt","depends_on_id":"gqlc-63y","type":"blocks","created_at":"2026-08-01T23:47:10Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-gjqo","title":"codegen: the capture guard is shape-enumerated, so unswept shapes fail open","description":"gqlc-35yu.10's commit message describes its capture guard as \"structural, not\nenumerated\" — i.e. it claims to catch any future generator-owned declaration\nrather than a fixed list of known-bad names.\n\nThe adversarial review found the claim is half true. The *name* side is\nstructural (it sweeps whatever the emitter declared, it does not compare\nagainst a list). The *shape* side is enumerated: the sweep only ever runs over\nthe three hand-built emission shapes in `emitParamBatch`. It never sees\n\n  - `*Params` structs (multi-parameter methods)\n  - `*Row` structs (multi-column projections)\n  - entity returns\n  - edge-union returns\n  - temporal returns\n  - list returns\n\nConcretely: a capturable declaration emitted under a `len(p.RowFields) \u003e= 2`\nbranch passes the guard green, because no swept shape has two row fields.\n\nSo the guard defends the shapes it was written alongside and silently exempts\nevery other one. That is the same failure mode this epic has hit repeatedly —\nfixtures shaped so the bug cannot appear — just moved up a level from the\nimplementation to its guard.\n\nFix: drive the sweep from the codegen fixture corpus rather than from\nhand-built shapes, so every shape the corpus already exercises is swept\nautomatically and new fixtures are covered on arrival with no test edit. If\nthat is impractical, the fallback is to enumerate the shapes explicitly AND add\na test that fails when a new shape is added to the emitter without being added\nto the sweep list — an unenumerated shape must not fail open.\n\nAcceptance: add a capturable declaration behind a `len(p.RowFields) \u003e= 2`\nbranch in the emitter and confirm a test goes red. Today it does not.\n\nRelated: [[F2]] (the sweep is AGE-only) is the other axis of the same gap —\nthis bead is about shape coverage within a backend, F2 is about backend\ncoverage. Both should be settled before the guard is described as structural\nanywhere.\n\nBlocked on [[gqlc-35yu.10]] merging.\n","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:40:56Z","created_by":"areqag","updated_at":"2026-08-02T11:57:34Z","closed_at":"2026-08-02T11:57:34Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/674","dependencies":[{"issue_id":"gqlc-gjqo","depends_on_id":"gqlc-35yu.10","type":"blocks","created_at":"2026-08-01T23:41:04Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-3o3p","title":"codegen: the capture guard is AGE-only — neo4j emitters are unswept","description":"gqlc-35yu.10 introduced a structural guard —\n`TestNoEmittedNameTakesAQueryParameterName` — which sweeps the emitted file for\ngenerator-owned declarations that a query parameter could capture. It is a real\ntest: the reviewer added an unprotected package-level declaration to the\nemitter without telling the test, and it failed on 3 subtests.\n\nBut **all of that machinery lives in `internal/codegen/age/age_test.go`**. The\nneo4j backends have no equivalent, which is how [[F1's defect]] survived .10's\nreview green.\n\n`TestBackendInvariantSurface`\n(`internal/codegen/conformance/conformance_test.go`) cannot backstop this: its\n`declaredSurface` helper skips non-TYPE `GenDecl` nodes\n(`conformance_test.go:610-616`), so emitted consts and vars are not part of\n\"surface\" as it defines them. The capture class is precisely about consts and\nvars.\n\nTwo ways out, and the choice is part of the work:\n\n1. Lift the sweep into a backend-agnostic helper (somewhere under\n   `internal/codegen/` proper, not under `age/`) and run it from each backend's\n   test package. Keeps per-backend fixtures, shares the analyser.\n2. Extend `declaredSurface` to include CONST/VAR `GenDecl`s and let\n   `TestBackendInvariantSurface` carry it. Cheaper, but changes what\n   \"surface\" means for every other assertion that helper feeds, so it needs a\n   careful read of the callers first.\n\nWhichever is picked, the acceptance criterion is the same: an unprotected\ngenerator-owned declaration added to the *neo4j* emitter must turn a test red.\nVerify by mutation, not by reading.\n\nBlocked on [[gqlc-35yu.10]] merging (the machinery to lift does not exist on\nmaster until then).\n","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:40:55Z","created_by":"areqag","updated_at":"2026-08-02T03:41:42Z","external_ref":"https://github.com/areqag/gqlc/issues/675","dependencies":[{"issue_id":"gqlc-3o3p","depends_on_id":"gqlc-35yu.10","type":"blocks","created_at":"2026-08-01T23:41:04Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-3o3p","depends_on_id":"gqlc-gjqo","type":"blocks","created_at":"2026-08-01T23:48:07Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lhs3","title":"codegen/neo4j: query body locals are capturable by a query parameter name","description":"Every neo4j query body declares locals with hard-coded literal names in\n`internal/codegen/neo4j/render_queries.go`: `records`, `out`, `err`, `record`,\n`row`, `value`, `valuePtr`, `acc`, `isNil`, `ok`. A single-parameter query\nmethod names its Go argument after the `$param` the query author wrote, so any\nquery binding one of those names lands a second declaration of it in the same\nscope.\n\nFound by the adversarial review of gqlc-35yu.10 at c476707. That bead added\n`codegen.Unshadowed` and routed AGE's body locals through it (`bodyLocal`), so\nthe AGE arm is now safe. **The neo4j arms were not given the same treatment** —\nthere is no neo4j analogue of `bodyLocal`, and zero references to\n`Unshadowed`/`ParamLocal`/`QueryTextConst` anywhere under\n`internal/codegen/neo4j/`.\n\nReproduction shape (a fixture binding `$out` or `$err`) emits non-compiling Go\non BOTH neo4j-go-v5 and neo4j-go-v6:\n\n    no new variables on left side of :=\n    cannot use make([]string, 0, len(records)) (value of type []string) as string value\n\nThis is a loud compile failure, not the silent injection class — which is why\nit was ruled non-blocking for .10 rather than reopening a passed review.\n\n**Not covered by [[gqlc-ni66]]**: ni66 is scoped to the *signature* binders\n(`q`, `ctx`) — names the method header declares. These are *body* locals, a\ndifferent scope and a different declaration site.\n\nIt also contradicts .10's own backend-invariance rationale: the same query text\nnow compiles on AGE and fails on neo4j, which is exactly the property\n`TestBackendInvariantSurface` exists to defend.\n\nFix is small (~10 lines): route neo4j's body locals through\n`codegen.Unshadowed`, the function .10 just introduced, the same way\n`internal/codegen/age` does. `Unshadowed` returns the name unchanged when there\nis no collision, so existing goldens should not move — a golden diff on this\nchange is a signal that something else is wrong.\n\nTDD: red first. Add codegen fixtures binding `$out` and `$err` (at minimum) and\nassert the neo4j goldens compile, before touching the renderer. A green run\nagainst the current renderer means the fixture is not actually reaching the\ndeclaration site.\n","notes":"SHARPENED BY THE gqlc-ak39 REVIEW (2026-08-02). The reviewer reproduced this end to end through the built CLI and the framing in the description undersells it on one axis and overstates it on another.\n\nThe collision site is writeMethodSignature (internal/codegen/neo4j/render_queries.go:227) and paramsMapLiteral (:403), both spelling a single query parameter with codegen.LowerFirstRune(p.ParamFields[0].Field). So the capture happens in the METHOD SIGNATURE, not only in the body:\n\n  func (q *Queries) ByCtx(ctx context.Context, ctx string) (N, error)    // ctx redeclared in this block\n  func (q *Queries) ByQ(ctx context.Context, q string) (N, error)        // q shadows the receiver; q.db.run fails\n  func (q *Queries) ByRecords(ctx context.Context, records string) (...) // records, err := q.db.run(...) type error\n\nThe first two belong to gqlc-ni66 (signature binders q and ctx). The third is this bead: the signature parameter collides with a BODY local. Same emitter, same line, two beads — coordinate rather than fixing half.\n\nAlso: the AGE arm has the signature half identically at internal/codegen/age/render_queries.go:126 and :263. gqlc-35yu.10 fixed AGE's body locals via codegen.Unshadowed but did NOT fix the signature, so ni66 is open on both backends, not just neo4j.\nMANAGER RULING (2026-08-02): fixed jointly with gqlc-ni66 on branch fix/neo4j-param-name-capture, because the ak39 review established they are the same two emitter lines (writeMethodSignature and paramsMapLiteral).\n\nThe approach in this bead's description — routing generator-owned names through codegen.Unshadowed — is SUPERSEDED. Rejected because the surface a rename-on-collision scheme must cover is unbounded: ni66's notes show it includes fmt, every decode\u003cEntity\u003e helper, agtypeEntity/Span/Object/Property/NullableProperty. You cannot rename fmt without aliasing the import, and the decode\u003cEntity\u003e set varies with the user's schema. A reserved-name list is exactly the failure mode that produced this bug (AGE's set was raw/label/props/out/err and missed the carrier argument).\n\nInstead: the emitted Go parameter name stops deriving from the query parameter name and becomes generator-owned, mirroring what gqlc-ak39 (375594e) did for decoder locals. No author-chosen identifier enters the emitted file's scope at all, so the class CLOSES rather than shrinking, and the guard can assert scope EQUALITY rather than disjointness — the assertion -update cannot bless away.\n\nCheap because: Go parameter names are not part of the call site (callers pass positionally); the multi-parameter form already binds a generator-owned name; and *Params/*Row fields keep deriving from the query parameter names, so the author-facing vocabulary survives where it is structurally uncapturable. Also closes the $_ case, where paramFieldName(\"_\") returned \"\" and gofmt rejected the empty identifier with a misleading message.\nROUND-3 BLOCKER, derived by the epic manager 2026-08-03 after the reviewer was killed mid-probe by a quota limit.\n\nThe round-3 scope probe sweeps only the SINGLE-parameter signature arm. The multi-parameter arm is unreached, so the fence does not cover it.\n\nEvidence. The killed reviewer left a compile-valid mutation in the worktree, on an axis distinct from round 2's. Round 2 mutated the single-param arm (textDerivedArg). This one mutates the multi-param arm, in BOTH emitters:\n\n  internal/codegen/neo4j/render_queries.go writeMethodSignature default: and paramsMapText\n  internal/codegen/age/render_queries.go   writeMethodSignature default: and argsMapText\n\n    func multiArg(p codegen.Query) string {\n        if len(p.ParamFields) \u003e 1 {\n            return codegen.LowerFirstRune(p.ParamFields[0].Field)\n        }\n        return codegen.ParamArg\n    }\n\nreplacing codegen.ParamArg at the ', %s %sParams' signature site and at the 'access = codegen.ParamArg + \".\" + f.Field' site. So the Params struct argument is named after the first parameter the query author wrote.\n\nI ran it: go test -count=1 -run Scope ./internal/codegen/conformance/ =\u003e ok, 2.989s. The probe PASSES. The mutation also regenerated 28 golden files under -update, so the corpus blesses it too.\n\nRoot cause, and why it is the round-2 finding one level up: every query in the probe census carries at most ONE parameter. Confirmed by extracting parameter sigils from scope_test.go — $_, $records, $q, $param, $fmt, $err, $ctx, $agtypeArgs, each appearing once, none co-occurring. So the census never reaches writeMethodSignature's default: arm nor the len(p.ParamFields) \u003e 1 branch in paramsMapText/argsMapText. Round 2's B1 was 'the census's axis is column shape, and the vulnerability lives on a shape the census does not build'. This is the same sentence with PARAMETER ARITY substituted for column shape.\n\nNot a live vulnerability today: shipped code emits codegen.ParamArg unconditionally in the multi-param arm, and the Params argument is a struct type, so capturing the string-typed QueryText const would not type-check. The exposure is a body LOCAL — a two-parameter query whose first parameter is named elem, rel, raw, ok, acc, narrowed or decoded would shadow the emitted local under the mutation. The fence exists to stop that being reintroduced, and it would not.\n\nFix: the census must vary parameter ARITY and parameter NAME, not only column shape. At minimum one multi-parameter shape per decode arm whose first parameter is named after a generator-owned local, so the default: signature arm and both map-access branches are swept. Then re-run the multiArg mutation above and require it red.\n\nThe reviewer's mutation was left uncommitted when the agent died. I have recorded it here verbatim and restored the worktree to 8de415f, so nothing is lost.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:40:50Z","created_by":"areqag","updated_at":"2026-08-03T17:28:33Z","closed_at":"2026-08-03T17:28:33Z","close_reason":"Merged in PR #742 (squash c6d0321..). The single-parameter Go argument is now generator-owned (arg), while the wire key stays the author's name. TestParameterNamesBindNothing closes every parameter index at once and reads no golden.","external_ref":"https://github.com/areqag/gqlc/issues/676","dependencies":[{"issue_id":"gqlc-lhs3","depends_on_id":"gqlc-35yu.10","type":"blocks","created_at":"2026-08-01T23:41:03Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-lhs3","depends_on_id":"gqlc-ak39","type":"blocks","created_at":"2026-08-01T23:48:06Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-6mx3","title":"bd-gh-sync: trailing-block deletion is indistinguishable from a GH append without a timestamp","description":"The append-only eligibility rule (`gh_body.startswith(desc)`, landed on gqlc-63y) closes three of the four ways GitHub could be behind bd and still be pulled — re-indent, reorder, and de-dup all now HOLD. It cannot close the fourth, and no rule over the two bodies can:\n\n  bd description: \"line one\"\n  GH body:        \"line one\\nline two\"\n\nis byte-identical in shape whether GitHub APPENDED \"line two\" (the case --prefer-github exists to serve, pinned by the `b-ahead` fixture in .githooks/tests/bd-gh-sync-test.sh, which the suite requires to pull) or bd DELETED a trailing block. Two states cannot distinguish an addition on one side from a deletion on the other. That needs a third input — the classic two-way vs three-way merge problem.\n\nRULING (manager, 2026-08-01): I ruled for append-only during the gqlc-63y refix; the implementer implemented it verbatim, then correctly pushed back that this row is undecidable under it, pinned the residual with a passing test (`trailing-block deletion is indistinguishable from a GH append (known)`) and a comment naming the collision, rather than asserting a HOLD that would have to be weakened later. That was the right call — I accepted the disagreement rather than forcing a fix that would have to lie. Filing it here instead of smuggling a design change into that branch.\n\nFIX: add the third input. Both sides already carry one — `bd list --json` emits `updated_at` (confirmed present alongside created_at/started_at), and `gh issue list --json` can emit `updatedAt`. When the bodies are in the prefix relation, compare the two: if the bead is NEWER than its mirror, bd carries the later edit and the bead must HOLD rather than be pulled. Only pull when GitHub is demonstrably the newer side.\n\nWatch for: clock skew between the local Dolt DB and GitHub, and whether a bd-side write that does not change the description still bumps updated_at (which would make the comparison over-conservative and re-hold the legitimate append case). Prove the rule the way the rest of this script's guards were proven — a fixture per shape, red before the fix.\n","status":"in_progress","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:34:25Z","created_by":"areqag","updated_at":"2026-08-17T05:46:25Z","started_at":"2026-08-17T05:46:25Z","external_ref":"https://github.com/areqag/gqlc/issues/677","dependencies":[{"issue_id":"gqlc-6mx3","depends_on_id":"gqlc-63y","type":"blocks","created_at":"2026-08-01T23:34:39Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-mdhr","title":"bd-gh-sync: --prefer-github write-back fidelity is unverified","description":"Two things could not be established during the bd-gh-sync review without mutating the real corpus, and both are load-bearing for the eligibility rule:\n\n1. Does `bd github sync --pull-only --prefer-github` write the GH body back VERBATIM? If it normalizes at all (whitespace, line endings, markdown), every pulled bead becomes permanently not-in-sync and the corpus-rewrite failure mode reopens SELF-SUSTAININGLY — each run re-selects what the last run touched. Evidence against: 261/270 beads currently match byte-for-byte and have been through unscoped pulls. Suggestive, not proof.\n\n2. Does --prefer-github clobber the fields the eligibility rule never compares — priority, issue_type, assignee, labels, design, acceptance_criteria, notes, parent? 46 beads carry acceptance_criteria, 75 carry notes, 10 carry design. For 261 beads desc == body exactly, so bd maps description\u003c-\u003ebody 1:1 and those fields have no GH counterpart to read back from — the reassuring reading, but an unverified one.\n\nSAFETY: this MUST be answered in a scratch repo with a throwaway bd DB and a throwaway GH repo. Never against the real corpus. That constraint is why it was deferred rather than answered in review.\n\nPROVENANCE: 'could not verify without violating the safety rules' section, bd-gh-sync adversarial review (2026-08-01).\n","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T21:23:54Z","created_by":"areqag","updated_at":"2026-08-02T03:41:44Z","external_ref":"https://github.com/areqag/gqlc/issues/678","dependencies":[{"issue_id":"gqlc-mdhr","depends_on_id":"gqlc-63y","type":"blocks","created_at":"2026-08-01T17:24:17Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4r01","title":"codegen: no test holds allSentinels against what Prepare can actually return","description":"internal/codegen has no reachability fence for its sentinel set. Nothing asserts that a sentinel in allSentinels is still returned by any construct, and — after gqlc-h4ug — nothing audits the \"deliberately unreachable\" section of the taxonomy doc either.\n\nDemonstrated during the gqlc-h4ug review. Three coordinated edits make a live sentinel disappear with the entire codegen tree still green:\n\n  1. drop ErrUnrepresentableEdgeUnion from allSentinels\n  2. move its §1 row in docs/specs/codegen-sentinel-taxonomy.md to §3\n     with the text \"Silenced by fiat.\"\n  3. delete its two invalid fixtures\n\n  go test -count=1 ./internal/codegen/...\n  ok internal/codegen | ok .../age | ok .../conformance | ok .../neo4j\n\nwhile prepare.go:671 still returns that sentinel for\nMATCH (x:Person)-[r:LIKES|WROTE]-(y:Post) RETURN r.\n\nWith the fixtures left in place the conformance sweep does object\n(unknown sentinel name \"codegen.ErrUnrepresentableEdgeUnion\"), so this is\nnot a one-edit escape. It is still an escape.\n\nWHY THIS IS ITS OWN BEAD AND NOT PART OF gqlc-h4ug. h4ug fences drift between the doc and allSentinels: consistency. This is a different axis — whether allSentinels corresponds to anything the code still does. h4ug's fence is structurally incapable of seeing it, because every one of its three directions compares the document to the slice or the slice to the declarations, and all three agree with each other after the edits above. I ruled it out of that bead deliberately rather than let its scope grow; recording that here so the exclusion is not read as an oversight.\n\nNote also that h4ug's §3 gives this gap a sanctioned, official-looking home it did not previously have. That is an argument for doing this bead reasonably soon after h4ug lands, not an argument against h4ug.\n\nTHE COUNTER-MEASURE ALREADY EXISTS IN THIS REPO, in another package. internal/schema/gql/sentinel_coverage_test.go declares TestAllSentinelsAreReachable, which runs the parser and reads errors off the wire, and its own rationale says why: a sentinel that stays in allSentinels while the construct it guards stops being reached silently passes TestSentinelReachability. internal/codegen has no equivalent. Read that test first and follow its shape rather than inventing one.\n\nScope note: the honest form of this drives Prepare (or Generate) with real inputs and collects which sentinels actually come back, then holds that set against allSentinels. That is more work than a static check and is the entire point — a static check is what the three edits above defeat.\n\nFound by the adversarial review on gqlc-h4ug, filed as non-blocking there by manager ruling.","status":"open","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T21:11:41Z","created_by":"areqag","updated_at":"2026-08-02T03:41:45Z","external_ref":"https://github.com/areqag/gqlc/issues/679","dependencies":[{"issue_id":"gqlc-4r01","depends_on_id":"gqlc-h4ug","type":"blocks","created_at":"2026-08-01T17:11:45Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0tft","title":"resolver: edge closure does not narrow plural endpoints it pins","description":"Under ADR 0022 a labelled node binding that several declared types satisfy binds\nplural (nodeCands). Phase C then closes each touching edge over the endpoint\ncross-product. When that closure commits a SINGLE EdgeKey, the key names one\nnode type on each end — so the pattern's endpoints are determined — but\ncloseEdge writes only edgeTypes/edgeKeys. The node bindings stay plural.\n\nReproduction (test/data/resolver/invalid/satisfy_plural_edges_reversed.gql\ndeclares Person, Person\u0026Employee, Company and exactly one WORKS_AT edge,\nEmployee\u0026Person -\u003e Company):\n\n  MATCH (p:Person)-[r:WORKS_AT]-(c:Company) RETURN p\n    -\u003e ErrAmbiguousLabel: p is satisfied by more than one declared node type\n\n  MATCH (p:Person)-[r:WORKS_AT]-(c:Company) RETURN p.employeeId\n    -\u003e ErrUnknownProperty: p.employeeId missing on plural-satisfying type Person\n\nBoth refusals are wrong on the facts: the only declared WORKS_AT edge has\nEmployee\u0026Person on its source side, so p cannot be the bare Person type. The\nfirst is pinned by invalid/plural_endpoint_whole_entity_after_edge_closure.cypher.\n\nThis is a precision gap, not a soundness one — the resolver refuses queries it\ncould accept and generates nothing wrong for the ones it takes. Widening is\nnon-breaking (ADR 0006), which is why gqlc-kph4 pinned the current answer\nrather than changing it.\n\nShape of the fix: after Phase C commits, intersect each plural endpoint's\ncandidate set with the node types the committed candidates put on that\nendpoint's side — the same per-edge contribution rule Phase B already applies\nto unlabelled bindings (R3 spec 4.5.2), extended to labelled plural ones. Note\na binding can sit on both sides of an undirected candidate set, so the\ncontribution is a union across sides before the intersection across edges.\nRe-running the pinned fixture must then flip it to accepted, and the fixture\nmoves from invalid/ to valid/.","notes":"2026-08-17 land run: PR #806 (https://github.com/areqag/gqlc/pull/806) opened at 40ce99a7 and squash auto-merge armed. lint, test, tidy, actionlint, live-smoke, codegen-fence all SUCCESS. govulncheck FAILURE, and it is NOT this diff: go.mod pins go 1.26.5 and the 2026-08-17 stdlib advisories (GO-2026-6089/6088/5942/5026 and the three called ones) are all fixed in 1.26.6, so every PR touching a .go file is red. That is gqlc-9322, whose fix commit ba46263f exists on local branch build/go-1266-toolchain and is unpushed. Once it lands on master, re-run the vuln job on PR 806 — the pull_request checkout uses the merge ref, so it picks up the bumped go.mod without a new commit on this branch — and auto-merge fires.\n\nFinal commit 40ce99a7 is comment-only: it retracts two prose claims that the six open follow-up beads falsify. The spec called the witness predicate's OPTIONAL arm §4.4.3's demotion gate 'spelled the same way'; DemoteNullability exempts an OPTIONAL edge in a proven group and this arm does not (gqlc-o8oc). readsLeftToRight's doc implied the right-to-left reading is irrelevant on a directed close; endpointContribution applies it regardless (gqlc-pv0u).\n\nRe-verified first-party at 40ce99a7: both halves of the Phase B conjunction killed independently by named tests with go build green first — dropping the covering conjunct reddens TestAnInferredEndpointIsTrustedOnlyWhenItsOwnEndsWereEnumerated + TestInvalid; dropping witnessesItsEndpoints reddens that test plus TestPhaseBAsksTheWholeWitnessPredicateNotOneArmOfIt. No fake REDs.\nMERGED 2026-08-17T05:29:18Z as squash aade7a40 on master. PR #806 head 422377e1 (rebased onto f1f23cb6 for the go 1.26.6 toolchain bump); all seven required contexts SUCCESS. GH #680 confirmed CLOSED. Residue filed: gqlc-rxq9 (two unpinned resolvedCovers writes, blocked on gqlc-etj6). All six pre-existing follow-up beads left open.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T20:51:44Z","created_by":"areqag","updated_at":"2026-08-17T05:29:45Z","closed_at":"2026-08-17T01:35:13Z","close_reason":"Closed by branch feat/resolver-narrow-plural-endpoints (local, unpushed) at 643832a7, 8 commits off f4fb1a19. Adversarial review PASS at round 4 (/tmp/lead/verdict-0tftr4.md).\n\nNarrowing is gated three ways: witnessesItsEndpoints on the narrowing edge, covering() on both ends, and resolvedCovers written only when covered. Phase B's candidateTypes now takes writtenBindings() and sets covered=false for any folded edge failing the WHOLE witness predicate — no OPTIONAL special case.\n\nRound-3 blocking finding F3 (the branch accepted what master REFUSED: an OPTIONAL hop is an outer join, so folding it narrowed p to bare Person and emitted p.personOnly as STRING NOT NULL on rows whose p is Employee\u0026Person). Reproduced at lead level before relay; closed and re-verified at 643832a7 across all six spellings (OPTIONAL, *0..1, *0, *2, CREATE, MERGE) — all six returned STRING NOT NULL at 79479fb0 and now refuse, matching master.\n\nReviewer evidence: 409 === RUN / 22 top-level fixed from outside the run; E01/E03/E05/E09 KILLED, E05 killing exactly a_CREATE + a_MERGE; E06 proven an equivalent mutant by grep (covered never read in-loop, never re-set true); the skip-instead-of-covered=false alternative built and measured to produce ErrAmbiguousBinding on 4 rows where master and the fix both accept. Regression sweep 59 queries / 2 schemas / 3 batteries vs f4fb1a19: every diff is master-refuses -\u003e branch-accepts, 310-fixture corpus answers identically, no golden modified.\n\ngqlc-3uof verified untouched and stays open. NB1 (degraded diagnostic) withdrawn: master itself emits the bare unknown-property message for a Phase-B-resolved singular binding, so the plural-lane suffix would be false once p is correctly pinned.\n\nNOT pushed, no PR opened.","external_ref":"https://github.com/areqag/gqlc/issues/680","dependencies":[{"issue_id":"gqlc-0tft","depends_on_id":"gqlc-kph4","type":"blocks","created_at":"2026-08-01T17:24:42Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ni66","title":"codegen: a query parameter named q or ctx collides with the emitted signature itself","description":"Every backend emits query methods as `func (q *Queries) Name(ctx context.Context, \u003cparam\u003e T)`. The single-parameter form names its Go argument after the query parameter the author wrote, so a query binding `$q` or `$ctx` puts that name into the same scope as the receiver or the context argument.\n\nReproduced against apache-age-pgx-v5 at b9feaa6 (manager probe, out of tree):\n\n    // name: ByQ :one\n    MATCH (p:Person) WHERE p.name = $q RETURN p.name\n\nemits\n\n    func (q *Queries) ByQ(ctx context.Context, q string) (string, error)\n\nand `go build` reports\n\n    ./queries.cypher.go:187:44: q redeclared in this block\n            ./queries.cypher.go:187:7: other declaration of q\n\nThis is LOUD — it is a build failure of the generated package, not the silent wrong-value class that gqlc-35yu.10 fixed with `bodyLocal`. That is why it was not promoted to blocking on that bead. It is still a defect: a legal query text makes gqlc emit a package that does not compile, with no diagnostic from gqlc itself, and the author has no way to know which parameter names are reserved.\n\nIt is not AGE-specific. Both neo4j targets emit the same receiver and the same `ctx` argument, so the collision is in the shared signature shape and should be fixed once, for all backends.\n\n`bodyLocal` deliberately does not reach this: it keeps *body* locals clear of the signature, and here the collision is between two things the signature itself declares. Note the receiver name is NOT part of the exported surface — `func (qq *Queries) ByQ(ctx context.Context, q string)` has an identical declared surface — so renaming the receiver away from a colliding parameter is available without moving the backend-invariant signature. `ctx` is the harder half, because TestBackendInvariantSurface compares declared signatures and the parameter name may be part of what it holds; check that before choosing.\n\nTwo candidate shapes, both worth weighing:\n  1. Rename the generator-owned identifiers out of the way (receiver, and ctx if the surface test permits), the same move gqlc-35yu.10 made for body locals. Keeps every query text legal.\n  2. Reject the query at generation time with a clear error naming the reserved parameter. Cheaper, but tells an author their query is unwritable for a reason that is purely an artefact of Go codegen.\n\nPrefer 1 if the surface test allows it; fall back to 2 only for the residue 1 cannot reach.\n\nWhatever is chosen must be pinned by a test in the same property-based style as TestNoBodyLocalTakesAQueryParameterName in internal/codegen/age/age_test.go, which reads the names off an emission rather than listing them, and must cover all three backends rather than AGE alone.\n\nFound by the manager's own probe during verification of gqlc-35yu.10, not by that bead's review.","notes":"BROADENED after the gqlc-35yu.10 review round 2, which mapped this class properly.\n\nThe bead as filed covered only $q and $ctx — collisions with the signature's own receiver and context argument. The real class is wider: a single-parameter method's argument can shadow ANY package-level identifier the emitted body REFERENCES, not just the two the signature declares.\n\nConfirmed reachable, all emitting non-compiling Go at exit 0:\n\n  $fmt              -\u003e fmt.Errorf undefined (type string has no field or method Errorf)\n  $ctx              -\u003e ctx redeclared in this block\n  $q                -\u003e q redeclared in this block\n  $agtypeArgs       -\u003e cannot call agtypeArgs (variable of type string)\n  $agtypeString     -\u003e same\n  $decode\u003cEntity\u003e   -\u003e same\n  $_                -\u003e generation FAILS with a misleading error:\n                       \"format failure: querier.go: 8:32: missing parameter type\"\n                       because paramFieldName(\"_\") returns \"\", so an empty\n                       identifier is emitted and gofmt rejects it. The message\n                       names gofmt and a column, and says nothing about the\n                       parameter that caused it.\n\nNOT in this bead, because gqlc-35yu.10 fixes it: the \u003cbare\u003eQueryText const. That one is silent rather than loud — it is string-typed and cypherStmt takes a string, so the shadow type-checks and the caller's runtime value becomes the statement text. That asymmetry is the whole reason it blocked .10 and these do not: every collision listed above is a compile failure of the generated package, which the author hits immediately.\n\n$_ is worth separating from the rest. It is not a shadow at all — it is an empty identifier reaching gofmt — and it is the only one where GQLC ITSELF fails rather than emitting bad Go. It may want a different treatment (reject with a clear message naming the parameter) even if the shadows get renamed.\n\nScope note for whoever takes this: .10 establishes the pattern for the silent half, on both backends, including growing the emission test to walk REFERENCED identifiers rather than declared ones. Read that commit first — the closed-set test technique is the reusable part, and this bead should extend the same walk rather than enumerate a second list. A list is what let the QueryText const survive a fix aimed at exactly it.\n\nFound by the adversarial review on gqlc-35yu.10; the $_ case and the specific error strings are from that review's mutation testing.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T20:51:25Z","created_by":"areqag","updated_at":"2026-08-03T17:28:34Z","closed_at":"2026-08-03T17:28:34Z","close_reason":"Merged in PR #742. MidOfThree plan plus set-equality arm census closes the middle-of-parameter-list blind spot in TestEmittedScopeIsGeneratorOwned.","external_ref":"https://github.com/areqag/gqlc/issues/681","dependencies":[{"issue_id":"gqlc-ni66","depends_on_id":"gqlc-35yu.10","type":"blocks","created_at":"2026-08-01T16:51:32Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-kph4","title":"resolver: undirected edge closure under plural endpoints has no corpus coverage","description":"gqlc-35yu.9 made the candidate set an edge binding closes to a set (dedupe in\nedgeProbes, internal/resolver/resolve.go). That changes behaviour on a path the\nvalid corpus never walks: an undirected edge binding whose endpoints are still\nPLURAL when the edge closes.\n\nThe dedupe was justified and tested for the singular self-loop case (srcs and\ntgts each one key, src == tgt, so the reversed orientation reproduces the\nforward key). Under ADR 0022 plural endpoints there is a second way two probes\nname one key: overlapping srcs/tgts produce (A,B) twice with A != B. The\npostcondition still holds — the result is a set either way — but nothing in the\ncorpus exercises it. satisfy_singular.gql is the only plural-satisfaction\nschema and it declares no edges, so no fixture reaches this combination at all.\n\nTwo things are unpinned:\n\n1. The dedupe's effect on the R3 §4.6 candidate-count dispatch when endpoints\n   are plural. §4.6 reads the count to decide singular / edgeUnion / ambiguous.\n   Pre-.9 a duplicate inflated that count; post-.9 it does not. No fixture\n   measures which verdict the path now reaches.\n\n2. On that path, committing the edge type no longer narrows the endpoints, so\n   the source and target bindings stay plural after edge closure. That is a\n   newly reachable resolver state — the spurious ambiguity error used to mask\n   it by refusing the query first. Whether downstream phases (Phase A\n   admission, entity naming) are correct with plural endpoints surviving edge\n   closure is untested.\n\nFix: add a plural-endpoint schema that declares edges (or extend\nsatisfy_singular.gql), and fixtures for the undirected × plural combination on\nboth sides of the §4.6 dispatch — one that should close singular, one that\nshould reach edgeUnion. Then confirm item 2 is either fine or a real defect.\n\nNot blocking .9: the change is a strict improvement on the paths the corpus\ndoes walk, and the mutation test (dropping the dedupe) fails five guards across\ntwo packages. This is a gap in what holds it, not a known break.","notes":"=== kph4 findings and forks ===\n\nMEASURED FIRST. New schema valid/schemas/satisfy_plural_edges.gql (Person,\nPerson\u0026Employee, Company; one WORKS_AT from Employee\u0026Person; FOUNDED from both\nperson types) plus invalid/schemas/satisfy_plural_edges_reversed.gql (same,\nwith REVIEWED declared Person-\u003eCompany, Employee\u0026Person-\u003eCompany AND\nCompany-\u003ePerson). Probed before deciding anything.\n\nITEM 1 — the count dispatch under plural endpoints.\n\n  (p:Person)-[r:WORKS_AT]-(c:Company)   -\u003e case B, ResolvedEdge (one candidate)\n  (p:Person)-[r:FOUNDED]-\u003e(c:Company)   -\u003e case D, edgeUnion of two candidates\n  (p:Person)-[r:FOUNDED]-(c:Company)    -\u003e ErrAmbiguousEdgeOrientation   \u003c-- BUG\n\nThe third was a real defect, not a gap. Both candidates run Person-ish -\u003e\nCompany; nothing about them is an orientation. The directed twin closes to the\nbyte-identical candidate set and case D types it, so the direction marker was\nflipping accept/reject on a set it does not change, and the sentinel's own\nadvice (\"write a directed arrow\") named a fix that alters nothing.\n\nFORK: what should case C's guard be?\n  (a) leave it, fix only the message      — rejected: the accept/reject split\n      between the directed and undirected twins survives, and it is arbitrary.\n  (b) refuse the directed twin too        — rejected: contradicts ADR 0022.\n      Plurality came from the label expression, and 0022 already ruled that\n      satisfaction is satisfaction; the union is the honest reading. It also\n      contradicts case D, which has emitted that union since R3.\n  (c) case C fires only on a SWAPPED PAIR — taken. `swappedEndpoints` scans for\n      two candidates {A,L,B} and {B,L,A}: the ones that disagree about which of\n      the pattern's endpoints is the source. That is the fact the sentinel is\n      named for, and it is what case C's rationale argues about (\"does the\n      pattern refer to authors reviewing books, or books reviewing authors?\").\n      Candidate count was only ever a proxy for it, exact while endpoints were\n      singular.\n\nThe dedupe from .9 stays load-bearing under (c): an undirected self-loop's two\nprobes are one key AND a swapped pair with itself, so without the dedupe\nundirected_self_loop_single_label still reaches case C. Verified by re-running\n.9's mutation — same five guards across two packages still fail. Two further\nmutations verified: reverting to count-only fails 3 guards; scanning only the\nfirst candidate pair fails ambiguous_edge_orientation_plural_endpoints.\n\nITEM 2 — plural endpoints surviving edge closure. NOT a downstream defect.\n\nConfirmed by running codegen.Prepare end-to-end on the plural schema. A plural\nnode binding cannot cross the resolver boundary at all: RETURN p and RETURN *\nare refused (ErrAmbiguousLabel) and property projection intersects to a plain\nResolvedProperty, so Phase A sees nothing new on the node axis, and Phase Z is\nschema-driven (it already demands an explicit Name for the multi-label type).\nThe edge axis does newly produce a union whose members share one label — Phase\nA refuses exactly that with ErrUnrepresentableEdgeUnion and names the endpoint\nconstraint that fixes it; the distinct-label plural union generates fine. No\nnew codegen fixture: both land on code paths the codegen corpus already covers\n(unrepresentable_edge_union_shared_label).\n\nWHAT ITEM 2 DID TURN UP — filed as gqlc-0tft, deliberately not fixed here.\nClosing WORKS_AT to its single key proves p is Employee\u0026Person, but closeEdge\nrecords only the edge tables; the endpoints are never narrowed. So RETURN p is\nrefused as ambiguous, and RETURN p.employeeId as unknown, on a binding the\nschema pins. Precision loss, not unsoundness — the resolver refuses queries it\ncould accept and mistypes nothing. Widening is non-breaking (ADR 0006), so kph4\npins the current answer (invalid/plural_endpoint_whole_entity_after_edge_closure)\nrather than changing it; gqlc-0tft flips that fixture when it lands.\n\nCONTRADICTS THE BEAD: the bead frames item 1 as unmeasured and item 2 as the\nsuspect. It is the other way round — item 1 held the defect, item 2's downstream\nis correct, and the gap item 2 exposed is one the bead does not mention.\n\nSPEC: R3 4.4 (endpoint cross-product in the signature, loop and set-property\nrationale — it still described singular endpoints and named only the self-loop\nreason for the dedupe), 4.6 (verdict table, swapped-pair definition, two new\nrationale blocks), 3.2, 3.5, 5.1, 2.2, 6.3.\nHANDOFF 2026-08-01 (session quota halt). Adversarial review of 188b63f returned FAIL: swappedEndpoints tests an EXACT syntactic swap (a.Source==b.Target \u0026\u0026 a.Target==b.Source) but its own doc comment claims the broader semantic predicate 'candidates that disagree on which pattern endpoint is the source'. Under ADR 0022 plural satisfaction those differ, so a real orientation ambiguity is now ACCEPTED where the parent refused it. Counterexample: REVIEWED declared Author-\u003eBook and Book-\u003eAuthor\u0026Editor; the undirected pattern is accepted, while its DIRECTED twin closes to ONE candidate — i.e. the commit deleted the refusal in exactly the case where its own prescribed remedy (write the arrow) works. RETURN r is backstopped two stages later by codegen ErrUnrepresentableEdgeUnion, but RETURN r.rating is caught by nothing and generates compiling Go.\n\nRefix was IN PROGRESS when the session halted. Five UNTRACKED fixture files are on the branch and are correct — keep them:\n  test/data/resolver/invalid/schemas/satisfy_plural_edges_reversed_subtype.gql\n  test/data/resolver/invalid/ambiguous_edge_orientation_reversed_subtype.cypher\n  test/data/resolver/invalid/ambiguous_edge_orientation_reversed_subtype_property.cypher\n  test/data/resolver/valid/plural_endpoint_reversed_subtype_directed_closes_singular.cypher\n  test/data/resolver/valid/schemas/satisfy_plural_edges_reversed_subtype.gql\ninternal/resolver/resolve.go was NOT yet modified. Remaining: replace the predicate (classify each candidate by which pattern endpoint set its Source came from; refuse when both classes non-empty; skip keys whose Source is in both sets — closeEdge already holds srcs,tgts), fix spec sections 3.5/4.6/5.1 which assert the syntactic and semantic definitions are the same thing, add require.Equal pinning the two identical union goldens (currently asserted by NOTHING and machine-written, so -update absorbs any divergence), fix the stale doc comment at internal/resolver/errors.go:67-72, make the predicate enforce its own KeyLabels precondition, and add one codegen conformance fixture pinning that this newly-admitted class dies at ErrUnrepresentableEdgeUnion.\n\nReviewer measured the replacement predicate keeps every accept this bead wanted (plural_endpoint_undirected_edge_union still passes) with one fixture message-string edit.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T19:51:16Z","created_by":"areqag","updated_at":"2026-08-02T11:57:33Z","started_at":"2026-08-01T20:43:25Z","closed_at":"2026-08-02T11:57:33Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/671","dependencies":[{"issue_id":"gqlc-kph4","depends_on_id":"gqlc-35yu.9","type":"discovered-from","created_at":"2026-08-01T15:51:16Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-e6mh","title":"codegen: an entity named Queries collides with type Queries in db.go — exit 0, non-compiling Go","description":"A schema declaring a node or edge type whose Phase Z entity name is \"Queries\" makes\ncodegen emit `type Queries struct` twice in one package: once in db.go (the handle\nevery backend emits) and once in models.go (the entity struct). `gqlc generate` exits\n0 and the emitted package does not compile.\n\nFound by Linus during review of gqlc-35yu.9. Reproduced on both apache-age-pgx-v5 and\nneo4j-go-v5, and pre-existing on master bd29f01 for neo4j, so this is not introduced\nby the AGE work.\n\nRepro: NODE TYPE Queries (p :Person { ... }) -\u003e db.go:31 and models.go:14 both declare\ntype Queries.\n\nSame defect class as gqlc-ak39 (emitted identifiers colliding with identifiers the\ntemplate already holds), and same reason it was not fixed on the AGE branch: the fix\nchurns the neo4j goldens. There is an existing identifier-collision taxonomy —\ntest/data/codegen/invalid/identifier_collision_* — and this belongs in it.\n\nFix shape: extend the Phase B/Z identifier-collision sweep to reserve the names db.go\ndeclares (Queries, and whatever else the handle template emits), with an invalid\nfixture per reserved name. Consider doing this together with gqlc-ak39, since both are\n\"a user-chosen identifier lands in a scope the template already occupies\" and a single\nsweep covering emitted-template identifiers would close both classes at once.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T19:03:03Z","created_by":"areqag","updated_at":"2026-08-17T05:48:09Z","closed_at":"2026-08-17T05:48:09Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/665","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.13","title":"codegen/age: entity properties of LIST or ANY width refuse the whole schema","description":"gqlc-35yu.9 gave the AGE backend an entity surface, which made the schema's\nwhole property table reachable: codegen.Prepare's Phase Z builds an entity\nstruct for every node and edge type in the schema, and it fails on the first\nproperty whose width the backend's TypeMap refuses.\n\nage.typeMap.Property refuses LIST and ANY, because the emitted decode\nvocabulary is one helper per agtype scalar and a field of either width would\nhave no helper to fill it. The consequence is wider than the field: a schema\ndeclaring `tags :: LIST\u003cSTRING\u003e` on any node type now fails AGE generation\nwith ErrUnrepresentableWidth even for a batch of pure scalar queries that\nnever touch it. Before .9 the same schema generated, because AGE emitted no\nentity structs at all and only the query's own columns were checked.\n\ntest/data/codegen/valid/schema_list_property is the shape in question; it is\nenrolled for neo4j-go-v5 only, where the field emits as *[]string.","acceptance_criteria":"- age.typeMap.Property admits LIST whose element width has a scalar carrier,\n  at whatever nesting depth, and the emitted entity field is the same Go type\n  the neo4j target emits for it.\n- Emitted list decode is unit-tested against captured agtype list text in\n  internal/codegen/age/testdata/corpus_test.go.txt, including a nested list, a\n  narrow element width, and an element of the wrong agtype scalar.\n- schema_list_property is enrolled for apache-age-pgx-v5 and passes the golden\n  and compile gates.\n- ANY is decided separately and explicitly: either an agtype-value decoder\n  lands with it, or the bead records why ANY stays refused while LIST does not.","notes":"IN-FLIGHT WORK PRESERVED (2026-08-02): worktree /home/antranig/Developer/gqlc/gqlc-35yu13-list, branch feat/age-list-property, base 375594e, UNCOMMITTED. The implementing agent was killed mid-task by a quota limit, not by a failure. Dirty set: internal/codegen/age/{age_test,errors,render_models,render_queries,types,types_test}.go, its testdata corpus, and a fixture MOVE — test/data/codegen/invalid/unrepresentable_width_any_schema/ renamed to valid/schema_any_property/, i.e. ANY was being promoted from refused to served. Do not discard; read the diff before resuming.\nAGENT KILLED BY QUOTA LIMIT 2026-08-03 ~12:50 EDT, mid-refix. NOT a failure and NOT a rejection of the work. Do not discard; read the diff before resuming.\n\nWorktree /home/antranig/Developer/gqlc/gqlc-35yu13-list, branch feat/age-list-property, HEAD ed2eccc (unchanged from before the refix started, so nothing was committed).\n\nUncommitted survivors:\n internal/codegen/age/render_models.go            | 53 +++++++-----------------\n internal/codegen/age/testdata/corpus_test.go.txt | 33 +++++++++++----\n 2 files changed, 40 insertions(+), 46 deletions(-)\n\nModified: internal/codegen/age/render_models.go, internal/codegen/age/testdata/corpus_test.go.txt\n\nThe refix was addressing three blockers from the failed review: B1 agtypeList's emitted 'out := make([]T, 0)' claims the empty list decodes to a zero-length slice and not nil, but nothing enforces it; B2 a NOT NULL list element emits non-compiling Go; B3 the committed neo4j goldens assert element types the Bolt driver never produces (v.([]bool), v.([]int32), v.([]string), v.([][]float32), elem.([]float32)) — the driver's PropertyValue set is bool|int64|float64|string|Point2D|Point3D|[]byte|[]any and hydrator.go:423 builds []any, so there is no path producing []string. The list_list_int fixture's golden already emits the correct elem.([]any) plus inner loop for the same shape, which is the proof B3 is a defect and not a design choice.\n\nResume by reading the uncommitted diff on its merits, then finishing B1-B3. Commit early and often — this is the second agent on this epic to be killed with an uncommitted tree.\nB1/B2/B3 DONE 2026-08-03. Branch feat/age-list-property, HEAD c3f10e9, rebased on origin/master c198b3d (verified with merge-base --is-ancestor). Four commits: 0ee0092 (LIST/ANY served on AGE), 17ff06e (AGE unconditional helpers stop being flags), d9c9182 (B3: neo4j list decode narrows from []any), c3f10e9 (B2: time import gate walks slice leaves).\n\nB1 held by internal/codegen/age/testdata/corpus_test.go.txt, which asserts `== nil` explicitly on six sites and discriminates nil in slicesEqual by construction. Its harness (TestEmittedHelpersDecodeTheAgtypeCorpus) compiles bytes from Generate, not from the golden tree, so -update cannot bless it. Mutating `out := make([]T, 0)` to `var out []T` reddens it with 8 assertions.\n\nB2 root cause: internal/codegen/neo4j/render_models.go renderModels gated the time import on `f.GoType == \"time.Time\"` exactly, so LIST\u003cTIMESTAMP\u003e ([]time.Time) and LIST\u003cLIST\u003cTIMESTAMP\u003e\u003e emitted `*[]time.Time` fields and `elem0.(time.Time)` assertions in a file with no time import. Now reuses goTypeNeedsImports, which already walks slice leaves for the query files. Held by new fixture test/data/codegen/valid/schema_list_temporal_property (neo4j-go-v5/v6 only).\n\nB3 was pre-existing neo4j breakage and was fixed on this branch as ruled. New invariant test TestNeo4jGoldensAssertOnlyDriverCarriers in internal/codegen/conformance/conformance_test.go parses every neo4j golden and fails any slice-typed type assertion outside {[]any, []byte}. It was red with 12 offenders before the fix. It reads the golden tree but asserts a property, so -update cannot bless it.\n\nAGE is deliberately NOT enrolled on schema_list_temporal_property: age.typeMap.Property still refuses LIST\u003cDATE NOT NULL\u003e with ErrUnrepresentableWidth (\"which the Apache AGE backend has no carrier for\"). Temporal carriers on AGE are gqlc-35yu.11's scope. Enrol AGE on that fixture once .11 lands.\n\nAll gates green on a fresh run: just test, just lint, just test-codegen-fence, just actionlint, just fmt-check, just tidy-check, go test -count=1 ./..., go vet -tags codegen_live ./... (only pre-existing unreachable-code notes in internal/grammar/*/gen). Final `-update` produced zero churn.\n\nThe review notes N1, N9 and N5 were promoted to must-fix in the resumption brief but their text is not carried in this bead, so they were not addressed. Restate them to close them out.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T17:25:07Z","created_by":"areqag","updated_at":"2026-08-03T17:06:11Z","closed_at":"2026-08-03T17:06:11Z","close_reason":"Merged as PR #740 (squash). Linus PASS re-derived by mutation; harness vacuity hole closed in-branch.","external_ref":"https://github.com/areqag/gqlc/issues/666","dependencies":[{"issue_id":"gqlc-35yu.13","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-08-01T13:25:07Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0oau","title":"ci: the AGE arm's only gate is a nightly that can fail silently or stop firing","description":"gqlc-35yu.8 moved the Apache AGE live arm off the PR critical path: job\nlive-smoke-age in codegen-live.yml runs on schedule and workflow_dispatch\nonly. That trade was deliberate -- PRs are not charged for AGE containers --\nand it is sound only while the nightly is actually a gate. Two things weaken\nthat.\n\nNo failure alerting. A red nightly notifies GitHub's default recipient, the\nlast committer to the cron file, and nothing else. Nobody owns it. An AGE\nregression can sit on master indefinitely with every PR green, because no PR\nruns the AGE arm.\n\nScheduled workflows stop firing. GitHub disables cron-triggered workflows\nafter 60 days with no repository activity. The gate would then be off with no\nsignal at all -- the failure mode is silence, not a red check.\n\nThere is a third, narrower hazard worth closing at the same time: a SKIPPED\ncheck satisfies a required status context on GitHub. live-smoke-age is\ncorrectly absent from master's required list today, but if it were ever added\nit would be permanently green on every PR, because it skips on pull_request.\ngqlc-35yu.8 leaves a comment on live-smoke recording that its job id is the\nrequired context; the mirror-image warning belongs on live-smoke-age.\n\nOptions, roughly in order of cost: route nightly failure somewhere owned;\nhave a failed nightly open or reopen an issue so the signal is durable; add a\nliveness assertion that the AGE job ran at all recently, so disablement is\nitself detectable.\n\nFound by Linus as NB3/NB5 in round-2 review of gqlc-35yu.8, alongside the\nblocking branch-protection defect. Non-blocking for that bead -- the arm and\nits wiring are correct; what is unowned is the signal they produce.","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T16:14:40Z","created_by":"areqag","updated_at":"2026-08-01T16:17:08Z","external_ref":"https://github.com/areqag/gqlc/issues/661","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rohp","title":"vuln: govulncheck never scans the codegen_live dependency tree","description":"`just vuln` runs `govulncheck ./...` untagged and against the ROOT module only. Two consequences, both meaning the live battery's dependencies have never been scanned:\n\n1. Untagged: files behind //go:build codegen_live are outside the default build configuration, so govulncheck does not load them. gqlc-3eyw fixed exactly this blindness for golangci-lint and go vet by putting the tag in .golangci.yml — govulncheck reads neither that config nor the tag, so it is the one gate that inherited nothing from that change.\n\n2. Root module only: the live battery lives in the NESTED module test/data/codegen, which has its own go.mod. The root go.mod has no require or replace on it, so `go list ./...` at root emits zero packages from that tree. Its dependency graph — testcontainers-go, the neo4j Go driver, docker/docker and their transitive trees — is invisible to the root scan by module boundary, independent of build tags.\n\nThat dependency set is large and network-facing. It is test-only and never ships in a user binary, which is why this is P2 and not P1, but it does run in CI with credentials in the environment.\n\nFIX: extend the vuln recipe to cover both axes — the codegen_live tag and the nested module — mirroring how test-codegen-fence already reaches into test/data/codegen. Prove it the way gqlc-3eyw proved its claims: demonstrate the scan reports a known-vulnerable pin before trusting a green result.\n\nPROVENANCE: raised as NIT 3 in the gqlc-3eyw adversarial review (2026-08-01), out of scope there. Verified by grep: no direct govulncheck invocation exists outside the just recipe.","notes":"Design forks resolved while fixing the scan (all conclusions measured, govulncheck v1.6.0, DB 2026-07-27):\n\n1. The bead premise was incomplete. Adding -tags codegen_live and scanning the nested module is necessary but NOT sufficient. Measured: with the battery as package fixtures (in-package tests), `govulncheck -tags codegen_live -test ./...` in test/data/codegen scanned 9 modules — pgx, neo4j v5/v6, x/text, x/sync, pgpassfile, pgservicefile, puddle. testcontainers, moby/docker, otel and x/crypto were still absent. Cause: govulncheck keys loaded packages by import path, so the in-package test variant `p [p.test]` collides with plain `p` and loses; anything only that variant imports drops out of the scanned module set with no diagnostic. Isolated in a minimal module with a single-variable A/B (identical deps, identical flags, only `package v` vs `package v_test` changed): in-package reported nothing, external reported the vulnerability with a call trace.\n\n2. Chose to convert the live battery to `package fixtures_test` rather than work around the scanner. Alternatives rejected: (a) -mode=binary on `go test -c -tags codegen_live` output — measured dead, `go version -m` shows a test binary embeds zero dep records, so binary mode saw 1 module and found nothing; (b) -scan module — measured worse, 7 modules, it is derived from loaded packages too, not from go.mod requires; (c) adding a tagged non-test file that imports testcontainers purely to widen the graph — a decoy with no other reason to exist. The conversion is free here: the nested module non-test files (include.go, contract_age.go) export nothing and the battery references none of them. Effect measured: 9 -\u003e 54 modules scanned.\n\n3. Positive control, as the bead asked. Pinned golang.org/x/crypto to v0.30.0 via replace (MVS floor from testcontainers-go v0.43.0 is v0.51.0, so a require edit cannot downgrade it; source mode honours replace, verified separately — -scan module does not). Before the conversion that pin produced ZERO findings. After, it produced 7 called vulnerabilities from 2 modules. The scan is now demonstrably capable of failing.\n\n4. The scan was red on the real tree, not just hypothetically. Once the live tree became visible it reported 6 called vulnerabilities, exit 3: GO-2026-5970 (x/text v0.37.0, reached via pgx.Connect) plus GO-2026-5013/5017/5018/5019/5020 in x/crypto/ssh v0.51.0, all reached via testcontainers.GenericContainer. Only the x/text one was visible through the drivers; the other five needed the package conversion. Fixed by bumping the nested module to x/text v0.39.0 and x/crypto v0.52.0 (go mod tidy also moved x/sync v0.20.0 -\u003e v0.21.0). Gate is now exit 0.\n\n5. Added a guard to test-codegen-fence asserting no live file declares `package fixtures`. Reasoning: the fix silently depends on a source convention with no compiler or tool enforcement, and this bead exists precisely because a required gate was silently scanning nothing. Put it in the fence because that is the only always-run required job over the nested module — `just vuln` itself only runs on dependency changes, so a guard there would not fire on a PR that only changed the package clause.\n\n6. Root module also got -test (previously godog, testify, rapid, gherkin, messages and go-memdb were unscanned: 32 -\u003e 43 modules). Measured clean.\n\n7. Residual, all uncalled so the gate is green by govulncheck design: GO-2026-5158 (otel v1.41.0, imported not called), GO-2026-5932 (x/crypto/openpgp, no fix available), GO-2026-5841 (klauspost/compress v1.18.5, required not imported). Not bumped — churn on indirect deps with no reachable exposure.\n\nNO JOB IDS CHANGED. The govulncheck job id in .github/workflows/vuln.yml is untouched, no job added, renamed or split, so the required status contexts (lint, test, tidy, actionlint, govulncheck, live-smoke, codegen-fence) are all still produced. The workflow change is confined to the dependency-change detection paths, which now also diff test/data/codegen/go.mod and go.sum.\nHANDOFF 2026-08-01. Branch fix/govulncheck-live-tree clean at 0475cdb. Adversarial review was dispatched and DIED ON SESSION QUOTA before returning a verdict — NO VERDICT EXISTS, do not merge. Re-dispatch.\n\nManager verification already done (do not redo): job ids are IDENTICAL between f1fd981 and the branch in BOTH .github/workflows/ci.yml and .github/workflows/vuln.yml, so no required branch-protection context was silently deleted — this was the highest-stakes check because GitHub derives a check context from the job id and a rename blocks every future PR repo-wide. Dependency bumps confirmed real in test/data/codegen/go.mod (x/crypto 0.51.0-\u003e0.52.0, x/sync 0.20.0-\u003e0.21.0, x/text 0.37.0-\u003e0.39.0), and 'just vuln' exits 0 on both modules.\n\nStill UNREVIEWED and the highest-value attacks for the next reviewer: (a) does the live battery still actually RUN after the package clause changed to fixtures_test in live_age_session_test.go / live_age_test.go / live_neo4j_test.go / live_test.go — an external test package loses access to unexported identifiers, so it could compile-and-skip while still printing ok; always -count=1 since Go serves a cached ok in 0.2s without starting a container and a real run is ~18-25s; (b) reproduce the positive control independently (pin x/crypto low via replace, confirm the gate goes red, restore exactly); (c) can vuln.yml's widened change-detection still fail open — what changes the dependency graph WITHOUT touching the four listed go.mod/go.sum files; (d) does the new 'package fixtures' guard in test-codegen-fence actually fire, and is its file-matching narrow enough to be escaped by a live file in a subdirectory.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T04:50:22Z","created_by":"areqag","updated_at":"2026-08-03T10:31:48Z","started_at":"2026-08-01T20:43:26Z","closed_at":"2026-08-03T10:31:48Z","close_reason":"Merged as PR #725. govulncheck now scans both modules under -tags codegen_live with -test; the nested battery converted to package fixtures_test to escape the x/vuln p [p.test] path collision (9 -\u003e 54 modules). Six called vulnerabilities found and fixed (x/text v0.39.0, x/crypto v0.52.0). Two guards added: a fence assertion that no live file declares package fixtures, and a ratchet on the root module's residual blind set. vuln.yml's dependency probe no longer fails open. Both go directives bumped to 1.26.5 after the first real scan found 5 called stdlib vulnerabilities. ADR 0026.","external_ref":"https://github.com/areqag/gqlc/issues/650","dependency_count":0,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-9n6t","title":"codegen: TypeMap.Temporal has no rejection channel, so an unrepresentable temporal silently generates 'any'","description":"`internal/codegen/typemap.go:13` gives `Property` a rejection channel and denies one to `Temporal`:\n\n    Property(pt graph.PropertyType) (goType string, ok bool)   // ok=false -\u003e ErrUnrepresentableWidth\n    Temporal(k resolver.Temporal) string                        // documented 'Total over the closed enum'\n    Scalar(k resolver.Scalar) string                            // documented 'Total over the closed enum'\n\n'Total over the closed enum' was true while neo4j was the only backend. AGE falsifies it: agtype carries no temporal scalar, so there is no faithful Go carrier for ANY temporal kind. Surfaced during gqlc-35yu.6.\n\n## The failure mode is silent wrong code, not an error\n\nWith no way to say 'I cannot carry this', the AGE TypeMap returns `\"any\"` for every kind (`internal/codegen/age/types.go:84-86`). A query projecting a temporal expression against AGE therefore generates a column typed `any` that no decoder can fill — code that compiles, passes the fence, and is meaningless at runtime. Compare the `Property` path, which refuses at generate time with `ErrUnrepresentableWidth` naming the offending width. Same class of gap, opposite outcome, purely because of the missing bool.\n\nGenerate-time refusal is the established posture in this codebase for 'this backend cannot represent that'. Temporal expressions escape it by accident of interface shape, not by decision.\n\n## Reachability\n\nUnreachable at C0 (no query methods emitted), which is why gqlc-35yu.6 ships with the hole open and a comment acknowledging it. It becomes reachable as soon as query methods exist for AGE — gqlc-35yu.7 onward — and it is squarely load-bearing at gqlc-35yu.11, which decides the temporal encodings.\n\n## Shape of the fix\n\nWiden to `Temporal(k resolver.Temporal) (goType string, ok bool)` mirroring `Property`, route ok=false to a sentinel naming the kind (either reuse the unrepresentable family or add a sibling), update the neo4j TypeMap to return ok=true unconditionally, and let AGE return ok=false until the temporal arm commits an encoding. Mechanical apart from the sentinel choice.\n\nAudit `Scalar` at the same time — it carries the identical 'Total' claim. AGE appears to cover the scalar enum today, so it may be genuinely total; confirm rather than assume, and if it is total, say WHY in the doc comment instead of restating the claim that just failed for Temporal.","acceptance_criteria":"TypeMap.Temporal exposes a rejection channel; a temporal projection against a backend that cannot carry it fails at generate time with a sentinel naming the kind, proven by a negative fixture; no backend returns a placeholder Go type for a kind it cannot decode; the Scalar totality claim is either verified and justified in the doc comment or given the same treatment.","notes":"=== ADDITIONAL SCOPE (from gqlc-35yu.6 round 4, 2026-08-01): Temporal has no drift fence either. ===\ninternal/codegen/age/types.go declares:\n    func (typeMap) Temporal(resolver.Temporal) string { return \"any\" }\nIt IGNORES ITS ARGUMENT. There is no switch, so the exhaustive linter has\nnothing to hold: resolver can GAIN a Temporal kind and nothing in the age\npackage notices. Property and Scalar both switch and are exhaustive-linted, so\nthis is the one type-map entry with no growth coverage at all.\n\nFolded here rather than filed separately because the fence and this bead's\nrejection channel are the same edit: once Temporal can refuse a kind it has to\nbranch per kind, and the branch is what the linter holds.\n\nRelated: gqlc-fb4a adds domain-sweep fences (ElementsMatch against a canonical\nenumeration) to the type-map tables in both backends, which is the SHRINK half\nof the same problem. It deliberately leaves Temporal to this bead.\n\nReachability note: harmless while AGE emits no query methods. gqlc-35yu.7 is\nwhere query methods first appear for AGE, and its notes already carry the\nguardrail forbidding enrolment of any fixture with a temporal projection for\napache-age-pgx-v5 until this lands.\n=== REACHABILITY CLAIM CORRECTED (manager, verified 2026-08-01 during .7 review) ===\n\nThe Reachability section says the silent-'any' hole \"becomes reachable as soon\nas query methods exist for AGE -- gqlc-35yu.7 onward\". That is FALSE. .7 ships\nquery methods and the hole stays shut. Surfaced by the .7 reviewer; I verified\nit in the .7 worktree rather than taking the report's word.\n\ntypeMap.Temporal has exactly two callers, prepare.go:752 and prepare.go:998,\nboth inside codegen.Prepare. For AGE, nothing reaches Prepare with a temporal\nin it, because .6 reordered generate() so rejectUnservedQueries runs BEFORE\nPrepare, and unservedColumn (age/errors.go:78) rejects on both channels:\n  - resolver.ResolvedTemporal is named in the reject arm outright, alongside\n    ResolvedList, so a temporal projection and a list of temporals both go.\n  - a temporal-WIDTH property goes through scalarWidth (errors.go:131), which\n    is `ty, ok := typeMap{}.Property(pt); return ok \u0026\u0026 ty != \"any\"` -- so a\n    width the AGE Property table refuses OR types as \"any\" is rejected too.\n\nCONSEQUENCE FOR THIS BEAD. The defect is real but it is LATENT, not live: no\nquery that reaches Prepare on the AGE path can carry a temporal, so no user can\ncurrently generate the meaningless `any` column this bead describes. It becomes\nlive at gqlc-35yu.11, which lifts the rejection when it commits the temporal\nencodings. Fix it BEFORE .11 lands the encodings, not urgently as a shipping\nbug. The [BUG] type and the \"silent wrong code\" framing are both still accurate\nabout the interface shape; they overstate the current exposure.\n\nThe ADDITIONAL SCOPE note below is UNAFFECTED and is now the stronger half of\nthis bead: Temporal ignoring its argument means resolver can gain a kind and\nthe age package notices nothing. That drift gap is live today regardless of\nwhether the value is reachable, because it is about growth, not calls.\n\nAlso settled by the same review, so it is not re-opened here: .7 correctly\nenrols NO temporal fixture for apache-age-pgx-v5, and the absence is fenced\nrather than merely observed -- there are positive tests pinning both rejection\nchannels (\"a temporal column is dropped\" pinning `column \"t\" projects\ntemporal(date)`, and TestTypeMapPropertyRejectionReachesTheCaller pinning\nErrUnrepresentableWidth for TIMESTAMP). Those two tests are what will go red if\n.11 lifts the rejection without widening the interface, so they are this bead's\ntripwire. Do not delete them when widening -- change them deliberately.\nIN-FLIGHT WORK PRESERVED (2026-08-02): worktree /home/antranig/Developer/gqlc/gqlc-9n6t-temporal, branch fix/typemap-temporal-rejection, base dc89506, UNCOMMITTED. Agent killed mid-task by a quota limit, not by a failure. Dirty set spans the rejection channel end to end: internal/codegen/{typemap,prepare,errors}.go plus both backends' types.go and the conformance test, plus a new untracked invalid fixture test/data/codegen/invalid/unrepresentable_temporal_date_column/. Do not discard; read the diff before resuming.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T03:49:12Z","created_by":"areqag","updated_at":"2026-08-02T12:50:33Z","closed_at":"2026-08-02T12:50:33Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/644","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-3eyw","title":"lint: build-tagged code (codegen_live) is outside every lint gate","description":"`.golangci.yml` sets no `build-tags`, so golangci-lint analyses only the default build configuration. Every file behind `//go:build codegen_live` — today `test/data/codegen/live_test.go` and `live_neo4j_test.go` — is invisible to `just lint` and to `just test-codegen-fence` (whose `go build ./...` / `go vet ./...` are also untagged).\n\nScope the damage accurately: COMPILE and behaviour coverage do exist, because `.github/workflows/codegen-live.yml` runs the real `just test-codegen-live` against docker on every PR. What is missing is LINT coverage specifically. So the failure mode is not broken code shipping — it is that the tagged files silently diverge from the standards the rest of the tree is held to, and nobody finds out.\n\nSurfaced during gqlc-35yu.4. Running `golangci-lint run --build-tags codegen_live` in the nested module reports 10 issues against that bead's new seam: 8 x ireturn, 2 x thelper. (An earlier draft of this description said 8 issues / 6 ireturn; the measured inventory in the NOTES is authoritative and was confirmed a third time during review.) The pre-refactor baseline was coincidentally clean, so nothing regressed against a gate that was running — the gate was never running.\n\n## Why this is not just 'set build-tags and fix them'\n\nThe two linters disagree with the code for opposite reasons and need opposite treatment:\n\n- `ireturn` fires on the adapter constructors and per-fixture querier accessors. Returning an interface IS the seam that lets one scenario body serve N backends; that is the point of the bead, not an accident. Needs an allow-list entry, not a code change.\n- `thelper` wants `t.Helper()` on the scenario bodies. Adding it would mark them as helpers and push require-failures up to the caller — destroying the failure-pointing that gqlc-35yu.4 carries as an explicit acceptance criterion, and that the pre-refactor file's inlined bodies existed to preserve. Satisfying this linter would make the suite worse. Needs suppression for scenario bodies.\n\nSo the resolution is a config decision with a rationale, not a mechanical cleanup, which is why it is its own bead rather than scope creep on .4.\n\n## Growth\n\ngqlc-35yu.8 adds the AGE live adapter arm behind the same tag. Every bead after it that touches live code widens the unlinted surface. Cheaper to settle the config before .8 than after.","acceptance_criteria":"just lint (or an equivalent gate) analyses codegen_live-tagged files; the ireturn allow-list covers the adapter seam with a recorded reason; thelper does not fire on scenario bodies and the failure-pointing acceptance criterion of gqlc-35yu.4 still holds — proven by breaking an assertion and showing the failure points at the scenario's own line.","notes":"Schedule opportunistically alongside gqlc-35yu.7, i.e. after .4 merges and before .8 starts. Not a blocking edge on .8 (see the note there), but the cost grows once .8 lands more tagged code.\n=== MEASURED INVENTORY (manager, 2026-08-01, on branch test/codegen-live-battery @ 23384c3) ===\nRan the tagged config by hand so whoever picks this up does not have to:\n  cd test/data/codegen \u0026\u0026 golangci-lint run --build-tags codegen_live\n  branch: 10 issues — ireturn 8, thelper 2\n  master: 0 issues\nSo the entire delta is created by .4's seam, and it is two linters, not a\nlong tail. This is a small allow-list job, not a cleanup job.\n\nireturn (8) — every one is a seam accessor returning one of the package's own\ninterfaces: startNeo4jV5/startNeo4jV6 returning harness; (*neo4jV5).scenario\nand (*neo4jV6).scenario returning backend; and the four\nmixedReadWriteBatch()/manyColMany() accessors returning the querier\ninterfaces. These returns ARE the mechanism that lets one scenario body run\nagainst N backends, and Linus separately confirmed the alternative (structs of\nfunction fields) would dodge 6 of the 8 hits while destroying the compile-time\nexhaustiveness the interfaces buy. So: allow-list, do not refactor.\n\nthelper (2) — oneAndExec and manyWithParams, the scenario bodies. thelper wants\nt.Helper() on both. DO NOT SATISFY THIS ONE. t.Helper() marks the frame as a\nhelper, which pushes every require-failure up to the battery loop and collapses\nall scenario failures onto one line. .4 carries 'a failing assertion points at\nthe scenario, not the battery' as an ACCEPTANCE CRITERION, proven by deliberate\ninjection. Satisfying thelper would silently revoke a shipped guarantee.\nSuppress it for this package.\n\nNet shape of the fix: ireturn gets an allow-list; thelper gets suppressed with\na comment saying why. Both scoped to test/data/codegen, not global.\n=== REVIEW OUTCOME + MANAGER VERIFICATION (2026-08-01) ===\nAdversarial review: PASS, no blocking findings. Every load-bearing claim in the\ncommit message was tested by breaking it, from a pristine tree left\nbyte-identical to the reviewed commit.\n\nThe two checks that mattered most, because they are what a path-scoped\nexclusion would have failed:\n  - identically-NAMED type planted in the ROOT module still fires ireturn.\n    The regex is anchored to the qualified name, so production cannot be\n    weakened by a name collision.\n  - reachable UNLISTED interface return planted inside live_test.go fires;\n    a listed one is silent. The exemption is per type, not per file.\n\nAlso settled: no global side effects from run.build-tags. Four-way measurement\n(root x nested, pre-change x shipped) run with --max-issues-per-linter=0\n--max-same-issues=0, because the DEFAULT 50/3 TRUNCATION WOULD HAVE HIDDEN A\nDELTA. All four zero. Zero //go:build !codegen_live files exist anywhere, so\nthe tag is strictly additive -- nothing dropped out of any analysis.\n\nMANAGER VERIFICATION (did not take the review's word): worktree confirmed clean\nof all review scratch (git status --porcelain empty, no probe files anywhere),\nthen re-ran every gate myself after go clean -testcache -- test, lint (0),\nfence (0), actionlint all green, and the live suite FORCED with -count=1 gave a\nreal 18.077s two-arm container run with four scenario subtests. A cached `ok`\nhas already handed me one false PASS this epic, so an unforced live result is\nnot evidence.\n\nNITS ACTIONED BEFORE THE PR EXISTED (commit amended, so no fixup noise):\n  N1 .golangci.yml overclaimed `just fmt` as an entry point the tag affects.\n     It is not: golangci-lint fmt is FILE-based, not package-load-based, and\n     produced byte-identical diffs under both configs. It was never blind.\n     Comment and commit message both corrected.\n  N2 \"the guarantee cannot be revoked quietly\" was half true. nolintlint\n     catches the ADDITIVE mistake (add t.Helper(), keep the directive). It\n     does NOT catch the wholesale one: add t.Helper() AND delete the\n     directive and every gate is green. Claim narrowed; the prose at\n     live_test.go:81 is named as the remaining guard.\n  N4 this bead's DESCRIPTION said 8 issues / 6 ireturn. Corrected to\n     10 / 8 ireturn to agree with the measured inventory.\n\nNIT DEFERRED, NOT DROPPED: N3 -\u003e filed as gqlc-rohp. `just vuln` is the one\ngate that inherited nothing from this bead -- govulncheck runs untagged AND\nroot-module-only, so the live battery's dependency tree (testcontainers, the\nneo4j driver, docker/docker) has never been scanned by anything.\n\nPR: #651 (Closes #645).","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T03:37:36Z","created_by":"areqag","updated_at":"2026-08-01T04:57:45Z","started_at":"2026-08-01T04:18:37Z","closed_at":"2026-08-01T04:57:45Z","close_reason":"Merged as PR #651 (squashed to master). Verified: adversarial review PASS with no blocking findings, all four review nits actioned or filed (N3 -\u003e gqlc-rohp), every gate re-run by the manager independently including a forced -count=1 live container run, all 7 CI checks green.","external_ref":"https://github.com/areqag/gqlc/issues/645","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.11","title":"codegen/age: temporal encodings per spike table, gated on live round-trips","description":"Implement the spike-validated temporal encoding table in the AGE backend: param encode + decode helpers for TIMESTAMP/DATE/DURATION/zoned types (fallback table pending spike: timestamp=epoch-micros integer, date=ISO string, duration=ISO-8601 string, zoned=offset-preserving string). The Go surface stays identical to the neo4j target (time.Time etc.) — only the wire representation differs, owned entirely by emitted helpers. Any type the spike marked unfaithful becomes a generate-time first-offender error naming property, type, and backend, extending the existing unrepresentable-width taxonomy (invalid fixture per errored type).\n\nGates are live, not just golden: per encoding, a round-trip scenario (write via :exec, read back, require.Equal on the original value) AND an ordering probe (WHERE n.ts \u003e param returns the correct subset) where the encoding claims ordering. Duration claims no ordering (not totally ordered even in neo4j) — its gate is component-faithful round-trip only.\n\nDocument the convention in the generated package doc header: encoded values are a gqlc convention; other tools reading the graph see the raw encoding.","acceptance_criteria":"Temporal fixtures enrolled for AGE pass goldens; live round-trip + ordering scenarios green per encoding; unfaithful types have invalid fixtures proving the generate-time error; zoned round-trip preserves the offset (not normalized to UTC) with a test pinning it.","notes":"=== SPIKE gqlc-35yu.5 OUTCOME — THE ENCODING TABLE THIS BEAD IMPLEMENTS ===\nOwner-approved 2026-07-31. Full ordering battery + captured output in\ngqlc-35yu.5.\n\nAGE has NO native temporal types (prior CONFIRMED empirically, not just from\nthe header enum). No temporal casts exist — the full pg_cast table is captured\nin the spike. The only temporal function is timestamp(), which returns epoch-\nMILLIS (note: millis, not micros — do not confuse it with the encoding below).\n\nFINAL TABLE:\n  timestamp/instant | epoch-MICROS int64                     | CONFIRMED\n  date              | ISO 'YYYY-MM-DD' string, zero-padded   | CONFIRMED\n  duration          | total-MICROS int64                     | AMENDED\n  zoned datetime    | TWO int64 props: \u003cf\u003e epoch-micros      | AMENDED\n                    |   plus \u003cf\u003eOffset offset-seconds        |\n  calendar duration | no faithful encoding — months/years    | generate-time\n                    |   are not a fixed micro count          |   error\n\nTHE GATE IS ORDERING, NOT ROUND-TRIPPING. Both amended encodings round-tripped\nfine and still failed; that is exactly why they were amended:\n  - duration as ISO-8601 string sorted P1D, PT10H, PT1H, PT2H, PT90M, and\n    WHERE v \u003e 'PT2H' returned only PT90M (1.5h) — one false positive and two\n    false negatives.\n  - zoned as an offset-preserving string returned an 07:00Z row for\n    WHERE v \u003e '...08:00+00:00'.\n  - zoned as a map {t,o} ALSO sorts wrong (jsonb key order) AND would force\n    gqlc to rewrite user ORDER BY. The FLAT SIDECAR was chosen because it\n    keeps the sort key as the property itself, so user ORDER BY passes through\n    untouched. Preserve that property.\nSo every encoding here needs a live ORDER BY + range-predicate test, not just\nan encode/decode round-trip test.\n\ndate stays a STRING by owner decision despite agtype string comparison using\nDB COLLATION rather than byte order (readability in the stored graph wins;\nISO dates are fixed-width digits and hyphens). If a collation is ever found\nwhere ISO dates misorder, that reopens this — worth one explicit test.\n\nSTILL OPEN, NOT COVERED BY THE SPIKE: time-of-day encoding. Resolve it in this\nbead, using the same ordering battery.\nPin: apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\nSEQUENCING TRIPWIRES from gqlc-9n6t (2026-08-02), which gave TypeMap.Temporal a rejection channel: (1) internal/codegen/age/types_test.go:TestTypeMapTemporal now expects SIX refusals. Lifting a kind's encoding must flip exactly that kind's switch arm and exactly that row — anything else means the change is wider than intended. The negative fixture test/data/codegen/invalid/unrepresentable_temporal_date_column/ pins 'date' specifically, so if .11 admits date first, that fixture MOVES to a still-refused kind rather than being deleted; deleting it would remove the only proof the refusal path still works. (2) AGE's unservedColumn no longer answers for ResolvedTemporal but still drops ResolvedList wholesale, so a LIST OF temporals is refused by the gate ('projects list') and never reaches the per-kind refusal. Harmless today, but it means the two gates must be lifted in the right order: per-kind first, then the list arm, or a list of admitted temporals stays refused with a misleading reason.\nSTAFFING DECISION 2026-08-02 (epic manager). Started .11 concurrently with .13's refix rather than serialising behind it.\n\nTerritory does collide: both beads edit internal/codegen/age/types.go and internal/codegen/age/render_models.go, and both add corpus fixtures whose goldens regenerate. .13 is widening the Property width table for LIST and rewriting agtypeList; .11 lifts arms of the Temporal switch and adds temporal encode/decode helpers. Different methods in the same two files, plus a shared golden tree.\n\nRuled to run them in parallel anyway. .11 is the epic's longest pole — the full spike table across six kinds, each needing a live ordering battery on top of a round-trip — and .13 has already failed one review round with three blockers, so its finish time is unbounded. Serialising the longest pole behind an unbounded refix costs more than a merge conflict in one file, and golden conflicts here are mechanical: regenerate with -update rather than hand-resolve.\n\nMitigation given to the .11 implementer: keep edits surgical and confined to the Temporal method and new temporal helpers, do not opportunistically refactor Property or agtypeList, expect to rebase, and resolve golden conflicts by regeneration rather than by hand.\n\nIf this turns out badly the recovery is a rebase, which is reversible. The alternative — an idle night — is not.\nAGENT STALL (2026-08-03, ~2nd watchdog kill of the session). Preserving state rather than discarding, per standing directive.\n\nWorktree /home/antranig/Developer/gqlc/gqlc-35yu11-temporal, branch feat/age-temporal-encodings.\nHEAD = c198b3d (origin/master, correctly current). NOTHING COMMITTED.\nUncommitted, 237 insertions / 36 deletions across four files, all tracked, no untracked files:\n  internal/codegen/age/render_models.go  +141\n  internal/codegen/age/render_queries.go  +27\n  internal/codegen/age/types.go           +40\n  internal/codegen/age/types_test.go      +65\n\nLast thing the agent said before the watchdog fired: 'Now the time import gates for querier.go and the per-source cypher files.' So the encode/decode arms were in place and it had moved on to making the emitted files import \"time\" only when a temporal column is actually projected — i.e. the work was mid-flight on import gating, not mid-flight on the encoding table.\n\nThe encoding table it was implementing is the owner-approved spike result and is SETTLED — do not relitigate it:\n  timestamp / instant  -\u003e epoch-MICROS int64            (CONFIRMED by live spike)\n  date                 -\u003e ISO 'YYYY-MM-DD' zero-padded  (CONFIRMED)\n  duration             -\u003e total-MICROS int64            (AMENDED)\n  zoned datetime       -\u003e TWO int64 props: \u003cf\u003e epoch-micros + \u003cf\u003eOffset offset-seconds, flat sidecar (AMENDED)\n  calendar duration    -\u003e no faithful encoding; generate-time error\n  time-of-day          -\u003e STILL OPEN, to be resolved inside this bead\n\nThe gate is ORDERING, not round-tripping. Evidence from the spike: ISO-8601 duration strings sort P1D, PT10H, PT1H, PT2H, PT90M and 'WHERE v \u003e PT2H' returns only PT90M. Offset-preserving zoned strings return an 07:00Z row for 'WHERE v \u003e ...08:00+00:00'. The flat sidecar was chosen because it keeps the sort key as the property itself, so a user ORDER BY passes through untouched.\n\nResuming now. The successor is told to judge the inherited diff on its merits rather than assume it is correct.\n=== TIME-OF-DAY DECIDED (the one open row) — 2026-08-03 ===\nTIME / LOCAL TIME = microseconds since midnight, int64, in\n[0, 86_400_000_000). Offset-bearing TIME normalises to UTC before\ncounting and carries its offset in the same flat \u003cf\u003eOffset sidecar\nthe zoned datetime uses; LOCAL TIME carries no sidecar.\n\nRationale, on the bead's own gate (ordering, not round-tripping): a\nnon-negative fixed-range integer is ordered by agtype's integer\ncomparison exactly as chronology within a day, so ORDER BY n.t and\nWHERE n.t \u003e $t are answered correctly with the author's text run\nverbatim (ADR 0005) — same argument that carried ENC1 and the amended\nduration encoding live, one width down. UTC-normalising is not\noptional: an offset-preserving count makes two spellings of one moment\ncompare unequal, which is the exact defect that killed the\noffset-preserving zoned-datetime string in this spike. The day-boundary\nwrap a UTC normalisation can produce is a property of the width (a bare\ntime of day has no date to carry into), not of the encoding.\n\nThe table is now complete:\n  timestamp/instant | epoch-MICROS int64                   | CONFIRMED\n  date              | ISO 'YYYY-MM-DD' string, zero-padded | CONFIRMED\n  duration          | total-MICROS int64                   | AMENDED\n  zoned datetime    | \u003cf\u003e epoch-micros + \u003cf\u003eOffset secs    | AMENDED\n  time / local time | micros-since-midnight int64,         | DECIDED\n                    |   UTC-normalised, + \u003cf\u003eOffset        |\n  calendar duration | none — generate-time error           | PERMANENT\n\n=== WHAT LANDED (81d2205 on feat/age-temporal-encodings) ===\nOnly TIMESTAMP is served. typeMap.Property admits graph.TypeTimestamp -\u003e\ntime.Time; encode via agtypeMicros / agtypeNullableMicros, decode via\nagtypeInstant, zone via agtypeZone reading the flat \u003cf\u003eOffset sidecar.\n\ntypeMap.Temporal still refuses ALL SIX kinds and that is deliberate. An\nearlier working tree admitted TemporalDateTime; reverted. A temporal\n*expression* column exists only because the query called a temporal\nconstructor, and AGE 1.7.0 has none (date/datetime/localdatetime/\nduration/toTimestamp all \"function does not exist\"; one hit in 348\nag_catalog functions, age_timestamp, an epoch-MILLIS integer that is not\na temporal value). Admitting a kind emits a compiling method the server\nrejects at run time — worse than the failure ADR 0025 built the channel\nto prevent. No `default` arm in either Temporal switch, so `exhaustive`\nwith default-signifies-exhaustive: true remains the growth fence.\n\n=== CARRIER, NOT ENCODING, IS WHAT BLOCKS THE OTHER FOUR ===\nDATE, TIME, LOCAL TIME and DURATION have settled encodings (above) and\nstill refuse in Property, because every other backend spells them with a\nneo4j driver type (dbtype.Date / dbtype.Time / dbtype.LocalTime /\ndbtype.Duration) and a pgx package cannot declare one without making the\ncaller-facing surface vary by backend. TIMESTAMP is admitted precisely\nbecause time.Time is what neo4j spells it with too. Unblocking the other\nfour is a backend-invariant temporal carrier, not more encoding work.\n\n=== KNOWN GAP: THE OFFSET SIDECAR HAS NO WRITE PATH ===\nagtypeZone is the read half only. gqlc binds the instant alone; writing\n\u003cf\u003eOffset would need gqlc to inject a $\u003cname\u003eOffset parameter into the\nauthor's CREATE/SET, which ADR 0005 forbids, and an auto-bound parameter\ndoes not resolve anyway (confirmed: a non-property parameter fails C6\nscope). A graph that wants its original zone back must name the sidecar\nin its own query text. Documented in the emitted helper docs. Value\nwritten through gqlc reads back at the same instant, in UTC.\n\n=== LATENT NEO4J BUG FIXED IN PASSING ===\nneo4j render_queries.zeroValueText routed a single-column :one over a\nstored temporal property to `default: return \"0\"` — the ColumnTemporal\narm above it is the kind a temporal *expression* takes, and a projection\nof a stored property is ColumnProperty. No fixture had run a :one over\none, so the emitted `return 0, err` had never had to compile. Both neo4j\ntargets affected; six carrier arms added.\n\n=== MUTATION SWEEP: 18/18 ARMS KILLED BY GOLDEN DIFF ===\nEvery encoding arm mutated to a compile-valid wrong value, goldens\nregenerated, diff confirmed, mutant reverted. Three arms initially\nSURVIVED for want of corpus reach and two fixtures were added to close\nthem: timestamp_projection (entity with a single NON-nullable instant,\nand queries that project instants while binding none) and\ntimestamp_nullable_param (a batch binding a nullable instant and nothing\nelse temporal). Both enrolled in all three targets.\n\n=== LIVE TESTS ARE UNVERIFIED LOCALLY ===\ntimestamp_property_roundtrip has a codegen_live scenario across all\nthree arms: four instants under non-chronological ids (pre-epoch, the\nepoch, one carrying micros, year 9999), asserting column and\nwhole-vertex round trip, nil for an unwritten nullable instant, an\nORDER BY that must disagree with insertion order, a strict \u003e at the\nepoch, and a comparison resolving to the microsecond. Docker was\ninaccessible in the environment that wrote it; `go vet -tags\ncodegen_live ./...` is clean but the suite has NOT been run. Someone\nwith docker must run `just test-codegen-live-age` before this closes.\n=== REVIEW ROUND 1 CLOSED — 2026-08-03 ===\nThree blockers, all fixed on feat/age-temporal-encodings.\n\nB1 THE BRANCH WAS RED IN CI. `just test-codegen-fence` failed at HEAD:\ntimestampRoundtripQuerier was missing from the ireturn allowlist in\n.golangci.yml. test/data/codegen is a SEPARATE Go module and `just lint`\nlints the root module only, so a branch can be green on `just test` and\n`just lint` and still red in CI. RUN ALL THREE.\n\nB2 THE FLAT \u003cf\u003eOffset SIDECAR COULD COMMANDEER AN AUTHOR-OWNED PROPERTY\nNAME. A schema declaring takenAt :: TIMESTAMP beside takenAtOffset ::\nINT64 emitted a decoder that read one key twice and re-zoned the instant\nby the author's value; the STRING variant compiled but could never\ndecode a vertex at all. Ruled: KEEP the read path (it is the deliberate\ninterop affordance) and refuse the schema at generate time. New\nrejectOffsetSidecarCollisions in internal/codegen/age/errors.go reports\ncodegen.ErrPropertyFieldCollision, with negative fixture\ntest/data/codegen/invalid/property_field_collision_offset_sidecar/ and a\nboth-nullabilities sweep in age_test.go. The read-without-write\nasymmetry is now documented on offsetProperty so it is not\nindistinguishable from dead code.\n\nB3 12 OF 26 MUTANTS SURVIVED. Every mutant that died, died on\nTestGoldenBuild — a type-checker, which proves the emitted Go compiles\nand nothing about what it computes. Every semantic claim in the encoding\ntable was unpinned. Closed by extending\ninternal/codegen/age/corpus_test.go, the one instrument here that\nCOMPILES AND RUNS the emitted bytes under GOPROXY=off and reads fixture\nINPUTS rather than goldens, so `-update` cannot reach it. Nine\npreviously-surviving mutants now die: sidecar NAME, instant unit,\nUTC normalisation, offset seconds-vs-minutes, the absent-sidecar rule,\nboth param-encode units, the sidecar never being read, and an instant\nparameter crossing as an ISO string. A02/A03/A07 are inert (no\nobservable differs).\n\nALSO LANDED, both promoted from the review:\n  - resolver.TemporalCount, taken from iota one line past the last\n    member of the Temporal enum, replaces `int(TemporalDuration)+1` as\n    the pin on the temporal sweeps in both the age and neo4j type-table\n    tests. The old form was satisfiable by appending a member: the last\n    member's ordinal does not move, so the pin could not detect the one\n    thing it existed to detect. Demonstrated: append a member and give\n    both type tables an arm, and both sweeps now fail; before, both\n    passed.\n  - The DATE lexical-ordering claim in internal/codegen/age/types.go was\n    false outside [0001-01-01, 9999-12-31] and is now stated with that\n    bound. The check it implies is filed as gqlc-2fz1.\n\nDISCOVERED, FILED, NOT FIXED HERE: gqlc-x5wx (agtypeZone accepts any\nint64 as an offset), gqlc-x5dq (the collision gate keys on the\ntime.Time carrier, so admitting zoned TIME silently drops coverage),\ngqlc-2fz1 (DATE year-range check plus the collation ordering test).\n\nSTILL UNVERIFIABLE HERE: Docker is inaccessible on this machine, so the\ncodegen_live timestamp_property_roundtrip battery has STILL never been\nexecuted. `go vet -tags codegen_live ./...` is clean in the nested\nmodule. This remains a closing condition.\n\nTERRITORY NOTE: gqlc-35yu.13 (feat/age-list-property, commit e0ead6a)\nindependently rewrote internal/codegen/age/corpus_test.go to close a\nvacuity hole — the harness passed when its testdata template was emptied\nto a package clause, because `go test` exits 0 on a file declaring no\ntests. That fix and this branch's extension of the same file will\nconflict on rebase. Both are additive; resolve by keeping the -json pass-set\ncheck from .13 around the extended fixture set from .11.\nQUOTA KILL 2026-08-03 ~19:00 EDT — state preserved. Worktree gqlc-35yu11-temporal, branch feat/age-temporal-encodings, HEAD d935a2d ('bound the DATE lexical-ordering claim to four-digit years'), with uncommitted work: age_test.go +78, render_models.go +6, and a NEW internal/codegen/age/render_models_test.go (140 lines). Round 5 was fixing linus-35yu11-r4's three blockers — the corpus harness asserting only NoError, errors.go:78's Fields[:min(1,len)] surviving because fixtures name the instant takenAt and map-key sort puts it at index 0, and errors.go:74's EntityNode guard surviving because every fixture row is a node. Resume from this working tree.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:12:31Z","created_by":"areqag","updated_at":"2026-08-04T00:27:54Z","closed_at":"2026-08-04T00:27:54Z","close_reason":"Merged: PR #759.","external_ref":"https://github.com/areqag/gqlc/issues/622","dependencies":[{"issue_id":"gqlc-35yu.11","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:12:31Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.11","depends_on_id":"gqlc-35yu.10","type":"blocks","created_at":"2026-07-31T19:12:57Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.11","depends_on_id":"gqlc-35yu.8","type":"blocks","created_at":"2026-07-31T19:12:57Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.11","depends_on_id":"gqlc-9n6t","type":"blocks","created_at":"2026-07-31T23:49:20Z","created_by":"areqag","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.10","title":"codegen/age: write path — :exec methods and write-param coverage","description":"The :exec cardinality for AGE: CREATE/SET/DELETE query emission through the same server-side param mechanism, zero-column cypher() invocation shape (AGE still requires an AS projection clause — the spike corpus documents the zero/one-column form), and command execution over DBTX.Exec. Completes param-encoding coverage for every property type the scalar stage admits, on the write side.\n\nTDD: red goldens first; the live battery's mixed read-write scenarios (write via :exec, re-read via :one/:many, delete, confirm ErrNoRows) enabled for the AGE arm — these already exist backend-invariantly from the live-refactor bead.","acceptance_criteria":"Write-path fixtures enrolled for AGE pass golden + compile gates; mixed read-write live scenarios green on the AGE arm; :exec on a projection-shaped query still rejected by shared admission (existing invalid fixtures run under the AGE target).","notes":"MANAGER NOTE AT START (2026-08-01).\n\nBranch feat/codegen-age-write-path, worktree gqlc-35yu10-write, cut from\ncdd09ca (post-.9).\n\nCorrection to this bead's own description before anyone acts on it: it says\n\"the spike corpus documents the zero/one-column form\". It does not. I read\ngqlc-35yu.5 end to end — it captures the temporal encoding table with ordering\nevidence, the param mechanism with server-log proof of server-side binding, and\nthe agtype result-text corpus for reads. Every cypher() invocation recorded\nthere projects at least one column from a RETURN. There is no CREATE, SET or\nDELETE anywhere in it, and so no evidence for what the AS clause must look like\nwhen the Cypher body returns nothing.\n\nSo the write invocation shape is NOT settled by prior work and must not be\nassumed from the AGE docs alone. Establish it live against the pinned image\nfirst (apache/age@sha256:4241e2d8... — PostgreSQL 18.1, AGE 1.7.0), record the\nexact statement and observed output in this bead the way .5 did, and only then\nemit against it. The specific things unproven: the column-definition list AGE\ndemands for a RETURN-less body, whether a write body may project columns at\nall, and what pgconn.CommandTag carries back for a cypher() call (AGE's write\nhappens inside a set-returning function, so the tag is the SELECT's, not the\nCREATE's — if RowsAffected is meaningless the emitted :exec must not pretend\notherwise).\n\nTwo drop reasons in internal/codegen/age/errors.go unservedReason() are what\nthis bead removes: \"writes to the graph\" (Validated.Statement ==\nresolver.StatementWrite) and \":exec returns no rows to decode\"\n(CardinalityExec). They are separate axes and they come off separately — a\n:many that writes and returns rows is a different shape from a :exec that\nwrites and returns none. Whichever this bead does not serve must keep its drop\nreason and keep its invalid fixture.\n\nSTANDING CONSTRAINTS, unchanged: string interpolation of user args is banned\nunconditionally — only the graph name may be interpolated, escaped and\nlength-checked; everything else binds. No AI attribution in any commit or PR.\n\nLIVE EVIDENCE (2026-08-01), apache/age@sha256:4241e2d8... — AGE 1.7.0 on\nPostgreSQL 18.1. Probes were scratch (psql scripts + two pgx v5 programs);\nnothing shipped. The three unproven questions, answered.\n\n(1) The column-definition list for a RETURN-less body must be EXACTLY ONE\nag_catalog.agtype column.\n\n  no AS clause:\n    SELECT * FROM ag_catalog.cypher('writeprobe', $$ CREATE (:P {n:1}) $$);\n    ERROR:  a column definition list is required for functions returning \"record\"\n  AS () :\n    ... $$) AS ();\n    ERROR:  syntax error at or near \")\"          -- zero columns is not expressible\n  two agtype columns:\n    ... $$) AS (v0 ag_catalog.agtype, v1 ag_catalog.agtype);\n    ERROR:  column definition list for CREATE clause must contain a single\n            agtype attribute  (SQLSTATE 42804)\n    HINT:   ... cypher($ ... CREATE ... $) AS t(c agtype) ...\n  one non-agtype column:  same 42804.\n  one agtype column: OK, zero rows returned.\n\n  The same 42804 (text unchanged, still saying \"CREATE clause\") is what a\n  RETURN-less DELETE and a RETURN-less SET give for a two-column list. So the\n  emitted shape is \"v0 ag_catalog.agtype\" whatever the write clause is.\n\n(2) A write body MAY project, and then the list must match the RETURN arity\nexactly; a mismatch either way is\n    ERROR:  return row and column definition list do not match\n  Verified for CREATE ... RETURN p / RETURN p.n, SET ... RETURN p / RETURN\n  p.name, MERGE ... RETURN r, DETACH DELETE ... RETURN 1. The existing\n  recordShape (one v\u003ci\u003e per RowField) was therefore already correct for\n  write-with-projection; only the zero-RowFields case was new.\n\n(3) pgconn.CommandTag is the enclosing SELECT's, not the write's. Measured\nthrough pgx v5.10.0 Conn.Exec:\n\n    CREATE 1 vertex,  bound params      -\u003e \"SELECT 0\"  RowsAffected=0\n    CREATE 3 vertices in one statement  -\u003e \"SELECT 0\"  RowsAffected=0\n    DELETE 2 vertices                   -\u003e \"SELECT 0\"  RowsAffected=0\n    SET on 2 vertices                   -\u003e \"SELECT 0\"  RowsAffected=0\n    DELETE matching nothing             -\u003e \"SELECT 0\"  RowsAffected=0\n    CREATE (p:P {n:5}) RETURN p.n       -\u003e \"SELECT 1\"  RowsAffected=1\n    MATCH (p:P) RETURN p.n  (3 rows)    -\u003e \"SELECT 3\"  RowsAffected=3\n\n  RowsAffected is the projected row count and nothing else. It cannot tell\n  \"created three vertices\" from \"matched nothing\". DECISION: the emitted :exec\n  returns error alone and discards the tag. Justified twice over — the\n  measurement above, and TestBackendInvariantSurface, since the neo4j targets\n  emit Method(ctx, ...) error for :exec. A RowsAffected in the AGE signature\n  would have broken the surface invariant AND lied.\n\nOTHER LIVE FINDINGS\n\n- All four non-interpolating pgx QueryExecModes (CacheStatement,\n  CacheDescribe, DescribeExec, Exec) work on writes. SimpleProtocol — the one\n  interpolating mode — fails closed on the write path:\n    ERROR:  third argument of cypher function must be a parameter (SQLSTATE 22023)\n  Note for the next reader: a QueryExecMode must be passed as the FIRST query\n  argument to pgx, not the last; passing it last gives a misleading\n  \"mismatched param and argument count\" (pgx v5 conn.go:41).\n- An injection payload bound as a write value is stored as data. Payload\n  {\"name\": \"a$gqlc$, $1) AS (v0 ag_catalog.agtype); DROP TABLE canary; --\"}\n  round-tripped intact and the canary table survived.\n- A pgx Query of a RETURN-less write whose rows are never drained still applies\n  the write.\n- An inline property map matched against a JSON-null parameter matches nothing;\n  AGE drops null properties at write time, so no stored vertex carries one.\n- MERGE (p:Person {id:9})-[r:AUTHORED|LIKES]-\u003e(q:Person {id:10}) RETURN r\n    ERROR:  syntax error at or near \"|\"\n  Independently confirms gqlc-35yu.14: AGE 1.7.0 has no relationship-type\n  alternation. edge_union_write_projection is still enrolled for AGE (its\n  sibling edge-union READ fixtures already are, and the golden/compile gates\n  hold the emission); .14 remains the open P1 for the missing live coverage.\n\nCORRECTION TO THE MANAGER NOTE ABOVE. The claim \"There is no CREATE, SET or\nDELETE anywhere in it\" about the gqlc-35yu.5 spike is false. Section C9 of that\nspike says verbatim: \"Write without RETURN -\u003e ZERO ROWS, but the AS(...) list\nstill needs exactly one agtype column, e.g. AS (v ag_catalog.agtype).\" The\ncorpus-graph setup in Q3 is a RETURN-less CREATE ... $) AS (v agtype); C8(c)\nholds CREATE (n:NullProp {a:1, b:null}) RETURN n and MATCH (n:NullProp) SET\nn.a = null RETURN n; C10 holds a RETURN-less CREATE (x:A:B {n:1}). The spike's\nC9 answer agrees exactly with what was re-measured here. The manager was right\nabout the second half though: the probe evidence for the projection question\nand for CommandTag genuinely was absent, and CommandTag is the one that changed\na design decision, so the live work was necessary regardless.\n\nDESIGN FORKS TAKEN\n\n- :exec returns error, tag discarded. See (3).\n- One statement composition path for reads and writes (writeStatement), so the\n  graph name stays the only interpolated thing and it stays inside the escaped,\n  length-checked cypherStmt. Writes bind through $1 exactly as reads do.\n- write_exec_temporal_param deliberately NOT enrolled for AGE: the type table\n  has no TIMESTAMP carrier, so it stays with gqlc-35yu.11.\n- Two unit tests had used \"a write\" as their stand-in for an unserved query\n  (TestUnservedQueriesOutrankUnrepresentableWidths in the age package, plus the\n  CLI-surface tests TestRunApacheAgeRejectionIsASentinel and\n  TestGenerateApacheAgeRejectsQueriesItCannotServe). Writes are served now, so\n  each moved to a list-projecting query, which is the narrowest axis AGE still\n  refuses. The CLI ones needed a schema of their own: a LIST on the shared\n  fixtureSchema would fail the AGE arm of the driver-vocabulary sweep on the\n  width sweep instead.\n\nGATES: just test / just lint / just test-codegen-fence / just actionlint /\njust test-codegen-live all green. The AGE arm now runs\nmixed_read_write_batch: exec + re-read live (arms table flipped writes: true,\nwhich TestLiveSmoke asserts against the harness type, so the arm cannot\nsilently drop the write scenarios).\n\nMANAGER CORRECTION (2026-08-01), same day, before review.\n\nMy start-of-bead note above is WRONG on its central claim and I am leaving it\nin place rather than editing it, so the record shows what the implementer was\ntold.\n\nI wrote that gqlc-35yu.5 has \"no CREATE, SET or DELETE anywhere in it\" and that\n\"the write invocation shape is NOT settled by prior work\". The implementer\nchecked and it is false. Spike section C9 says verbatim:\n\n    Write without RETURN -\u003e ZERO ROWS, but the AS(...) list still needs exactly\n                            one agtype column, e.g. `AS (v ag_catalog.agtype)`.\n\nI have re-read it at gqlc-35yu.5 lines 751-752 and confirmed. C8(c) and C10\nalso carry RETURN-less writes, and the Q3 corpus setup is one.\n\nCause: I searched the spike with grep patterns anchored on `cypher(...CREATE`\nand on the `AS (` form, and truncated the output with head before reaching\nC9 at line 742. I then reported the absence as a finding. Searching for a\nthing, not finding it, and concluding it is not there is only valid if the\nsearch was capable of finding it — mine was not, and I did not check that\nbefore writing the claim into a bead where it would be read as established.\n\nWhat survives from the note, and what the implementer confirmed was genuinely\nunproven and worth the live work:\n\n  - Whether a write body may PROJECT columns. It may; the AS list then matches\n    the RETURN arity exactly. C9 covers only the RETURN-less case.\n  - What pgconn.CommandTag carries. It is the enclosing SELECT's, not the\n    write's: CREATE of 3 vertices, DELETE of 2, and a DELETE matching nothing\n    all report \"SELECT 0\". RowsAffected is the projected-row count and nothing\n    else. This changed a design decision — the emitted :exec returns error\n    alone and discards the tag, which is also what keeps its signature\n    identical to the neo4j targets.\n\nSo the probing was worth doing on its own merits, and the corrected lesson is\nabout how I established the premise, not about whether to measure.\n\nMANAGER FINDING, PRE-REVIEW (2026-08-01). BLOCKING. Reproduced myself before\nruling; not taken from anyone's report.\n\nThe AGE query body declares locals named stmt, args, rows and err. A query\nwith exactly ONE parameter emits that parameter under a name taken from the\nquery text — writeMethodSignature's `case 1:` arm emits\ncodegen.LowerFirstRune(p.ParamFields[0].Field). The two namespaces are the\nsame block, so a parameter the author happens to name $stmt collides with the\ncomposed statement.\n\nReproduced with a two-file fixture, one query, no other content:\n\n    MATCH (p:Person) WHERE p.name = $stmt RETURN p.name\n\nemits, for apache-age-pgx-v5:\n\n    func (q *Queries) PersonByStmt(ctx context.Context, stmt string) (string, error) {\n        stmt, err := q.cypherStmt(\"$gqlc$\", personByStmtQueryText, \"v0 ag_catalog.agtype\")\n        ...\n        args, err := agtypeArgs(map[string]any{\"stmt\": stmt})\n        ...\n        rows, err := q.db.Query(ctx, stmt, args)\n\nThe caller's argument is overwritten by the composed SQL on the line after it\narrives, and the SQL text is then bound as the value of $stmt. The caller's\nvalue is never read. PersonByStmt(ctx, \"Alice\") searches for a Person whose\nname is the entire generated SQL statement.\n\nTHIS COMPILES. `go test ./internal/codegen/conformance/ -update` reports ok,\nthe golden is written, the compile gate passes, every one of the five gates is\ngreen. There is no error at generate time and none at run time — the query\nsimply returns the wrong answer forever.\n\nTyped differently it is only a compile error: the same query written against an\nINT64 property gives \"cannot use q.cypherStmt(...) (value of type string) as\nint64 value in assignment\". So the failure mode is decided by the property's\nwidth, and the silent one is the STRING case, which is the common one.\n\nNOT a general codegen defect. neo4j-go-v5 emits, for the identical fixture:\n\n    records, err := q.db.run(ctx, personByStmtQueryText, map[string]any{\"stmt\": stmt}, ...)\n\nwhich reads the caller's stmt on the first line and declares nothing that\ncollides. This is AGE's alone, because AGE's body opens by declaring the\ncomposed statement.\n\nPRE-EXISTING at cdd09ca on the read path — writeQueryCall already opened with\n`stmt, err := q.cypherStmt(...)` before this bead. So it ships on master today.\n\nRULED BLOCKING FOR THIS BEAD ANYWAY, and I want the reasoning on the record\nbecause it goes the opposite way to my ruling on gqlc-ak39:\n\n  - ak39 was deferred because the defect was in the NEO4J backend, owned by\n    another bead, and fixing it churned ~77 neo4j goldens on an AGE branch.\n    Territory and churn both pointed away.\n  - This one is in the AGE backend, which this bead owns. Its churn is AGE\n    goldens, which this branch already regenerates. Territory and churn both\n    point in.\n  - This bead extends the defect: writeStatement is the refactored\n    writeQueryCall, and the write path now reaches the same collision on a\n    surface that did not exist before.\n  - gqlc-35yu.9 established exactly this principle one bead ago — \"a name\n    taken from the query text or the schema is any Go identifier the author\n    chose, including one the body already holds\" — and fixed it for decoder\n    locals via valueName(i). The query-body locals are the unfinished half of\n    the same fix. Landing .10 without it means the principle was stated and\n    then not applied to the function the same bead was editing.\n  - A silent wrong-value bug outranks a compile error. .9's B1 at least failed\n    to build.\n\nREQUIRED PROPERTY: no identifier the emitted body declares may be one the\ncaller's query text could have chosen. The public signature must not move —\nthe single-parameter form is part of the backend-invariant surface and neo4j\nemits the same one. Fix on the body-local side, not the parameter side.\nRegression fixture enrolled the way entity_property_shadows_decoder_local was,\ncovering the silent STRING case specifically, and enrolled on a neo4j target\ntoo so the invariant surface holds it.\n\nBODY-LOCAL SHADOWING FIX (2026-08-01), on top of 7ddaed6.\n\nThe property held: no identifier the emitted body declares may be one the\ncaller's query text could have chosen.\n\nScheme. paramLocal(p) returns the single identifier in an emitted method's\nsignature that the query text chose -- LowerFirstRune(ParamFields[0].Field) for\nthe one-parameter form, and \"\" otherwise, because the two-or-more form passes a\nParams struct under this package's own name and the zero-parameter form\ncontributes nothing. bodyLocal(p, name) appends \"_\" while name equals that.\nEvery local the query bodies declare now goes through it: stmt, args, rows, out,\nerr, decoded, narrowed, and the positional raw%d / value%d / label%d.\n\nWhy it cannot collide. The loop's exit condition is the non-collision property\nitself, so the scheme does not depend on guessing which names a query text can\nproduce -- $value0 and $raw0 are both producible and both are in the fixture.\nIt terminates on the first pass, since a signature contributes at most one\nquery-chosen identifier, but the loop is what makes the result unconditionally\nclear rather than clear by that argument. The mangled names cannot collide with\neach other either: each family is disjoint and nothing emits a literal trailing\nunderscore.\n\nThe public signature did not move. The single-parameter form is\nbackend-invariant surface and neo4j emits the same one; the fix is entirely on\nthe body-local side, and the agtype args map still keys on the wire parameter\nname, not on a Go identifier.\n\nBlast radius. Zero golden churn across the existing corpus -- conformance passed\nwithout -update, because no query in it collides. The 128 lines added to\nage_test.go are TestNoBodyLocalTakesAQueryParameterName, which discovers the\nbody locals by walking the emission's AST rather than listing them, so a local\nadded later is covered without touching the test.\n\nMutation check. Neutering bodyLocal to return its argument failed all nine\ndiscovered subtests (args, decoded, err, narrowed, out, raw0, rows, stmt,\nvalue0); restoring made them green. The test is not vacuous.\n\nFixture query_param_shadows_body_local is enrolled on apache-age-pgx-v5 and\nneo4j-go-v5, so TestBackendInvariantSurface holds it. It covers the silent\nSTRING case specifically -- against a STRING property the widths agree, so the\ncomposition assigns the SQL text over the caller's argument, binds it as the\nvalue of the parameter, and every gate stays green while the query searches for\na person named after its own statement. Against INT64 the same shadowing is only\na build failure, which is to say the property's width decided whether the defect\nwas loud. Both the read path and :exec are covered, since both now reach the\ncollision through writeStatement.\n\nNot fixed, and deliberately out of this fixture. A parameter named $err collides\non neo4j, where \"records, err := ...\" assigns an error to a string parameter --\na loud compile error, in a backend another bead owns, so enrolling it jointly\nwould have broken the neo4j arm. $ctx and $q collide with the signature's own\nctx and receiver on every backend; also loud, and not fixable without moving the\nbackend-invariant signature.\n\nGates, all green: just test, just lint, just test-codegen-fence, just\nactionlint, and the live battery under docker (TestLiveSmoke all three arms\nincluding the AGE mixed_read_write_batch scenario, plus TestAGESessionInit).\nMANAGER VERIFICATION of the collision fix (b9feaa6), independent of the implementer's report.\n\nRead the emitter diff rather than the summary. bodyLocal/paramLocal are as described, and every emitted local routes through them — including the positional rawN/valueN/labelN, which is the part that matters, since $value0 and $raw0 are as writable by an author as $stmt.\n\nI then probed OUT OF TREE, deliberately using collision names the new fixture does NOT contain, so the fix was tested against something other than what it was written to pass. Generated /tmp/mgr-probe against apache-age-pgx-v5 binding $out, $decoded, $narrowed, $label0 and $q. Result:\n\n  $out       -\u003e out_    (the :many accumulator renamed; caller's `out` intact)\n  $decoded   -\u003e decoded_ (nullable decode branch renamed)\n  $narrowed  -\u003e no rename needed and none applied; `decoded` still emitted\n                unrenamed because `narrowed` was the parameter, not `decoded`\n  $label0    -\u003e no rename needed (no edge union in that body, so no label0\n                declared) — bodyLocal correctly renames on collision only,\n                not on membership of a reserved list\n\nAll correct. The fix holds against names it was not written against, which is the property I wanted and could not have got from the fixture alone.\n\nONE RESIDUE, NOT BLOCKING, FILED AS gqlc-ni66.\n\n$q does still collide, with the receiver rather than with a body local:\n\n  func (q *Queries) ByQ(ctx context.Context, q string) (string, error)\n\n  ./queries.cypher.go:187:44: q redeclared in this block\n          ./queries.cypher.go:187:7: other declaration of q\n\nI confirmed by building the probe package that this is a compile error, i.e. LOUD. That is why it does not block: the class this bead had to close was the silent one, where the widths agree and the query returns wrong answers with every gate green, and that class is closed. A generated package that refuses to build is a defect the author finds immediately.\n\nIt is also not this bead's territory. The receiver and ctx are in the shared signature shape, identical on all three backends, so fixing it here would fix it for AGE alone and leave neo4j carrying it. gqlc-ni66 is blocked on this bead and takes it across all three.\n\nI disagree with one line of the implementer's commit message, and record it so the next reader does not inherit the error: it says $ctx and $q \"are not fixable without moving the invariant signature\". The receiver name is not part of the declared surface — `func (qq *Queries) ByQ(ctx context.Context, q string)` is surface-identical — so the receiver half is fixable without moving anything. ctx is the genuinely constrained half, and only if TestBackendInvariantSurface compares parameter names. ni66 says to check that before choosing a shape.\n\nNot correcting the commit message: it is accurate about what blocks and what is loud, and the residue is now recorded where the fix will happen.\nREVIEW ROUND 2: VERDICT FAIL. Manager confirms, and agrees it blocks.\n\nLinus found a defect that both the implementer and I missed, and missed for the same reason, which is the part worth recording.\n\nTHE DEFECT. render_queries.go:99 emits the query text as a package-level const named \u003cbare\u003eQueryText, and line 304 references that const BY NAME inside the method body. bodyLocal only renames identifiers the body DECLARES. A single-parameter signature can shadow a package-level identifier the body REFERENCES, and paramFieldName + LowerFirstRune can reproduce \u003cbare\u003eQueryText exactly.\n\nI reproduced it independently rather than trusting the report. /tmp/mgr-probe2, plain `gqlc generate`, driver apache-age-pgx-v5:\n\n  // name: PersonByStmt :one\n  MATCH (p:Person) WHERE p.name = $personByStmtQueryText RETURN p.name\n\nemits\n\n  const personByStmtQueryText = `MATCH (p:Person) WHERE p.name = $personByStmtQueryText RETURN p.name`\n  func (q *Queries) PersonByStmt(ctx context.Context, personByStmtQueryText string) (string, error) {\n      stmt, err := q.cypherStmt(\"$gqlc$\", personByStmtQueryText, \"v0 ag_catalog.agtype\")\n\nThe identifier on that third line resolves to the PARAMETER, not the const. `go build` exits 0. I confirmed the build myself.\n\nSo the caller's runtime string becomes the statement text. That is hard constraint #1 — query arguments bind, only the graph name is interpolated — and it is worse than the concatenation the constraint was written against, because there is no concatenation to find: the whole statement is the argument. The :exec form of the same shape (verified, WipeByStmt) makes it an arbitrary attacker-chosen WRITE.\n\nWHY IT BLOCKS RATHER THAN DEFERRING AS PRE-EXISTING. It is present at cdd09ca and on neo4j too, so by my usual test it would be a follow-up. It is not, for three reasons. b9feaa6 is the dedicated fix for exactly this defect class and leaves its most severe instance open. paramLocal's doc comment asserts \"this is the whole of what a body local has to keep clear of\" — the premise is true, the conclusion is false, and shipping the false conclusion as a comment is worse than not having written it. And 7ddaed6 extends the reach to the write path.\n\nWHY THE TEST CANNOT SEE IT, WHICH IS THE REAL LESSON. declaredIdents (age_test.go:891) collects only AssignStmt/ValueSpec/RangeStmt binders, so bodyLocalsOf is structurally blind to a shadowed package-level identifier. TestNoBodyLocalTakesAQueryParameterName passes with the hole wide open. My own verification probe had the identical blind spot: I fed it $out, $decoded, $narrowed, $label0 — every one of them a name the body DECLARES. A probe drawn from the same mental model as the fix tests the model, not the code. That is why the review round exists and why I do not skip it on work I have already checked.\n\nMY RULING ON SCOPE. Fix the query-text const on BOTH backends on this bead, not AGE alone.\n  - The const name is generator-owned and not part of the backend-invariant surface, so it is the side that is free to move. paramLocal cannot move.\n  - Note the obvious fix does NOT work: `text := \u003cbare\u003eQueryText` inside the function still resolves to the parameter. Renaming the CONST is the reachable fix.\n  - Zero golden churn either way, because no fixture in the corpus names a parameter \u003cbare\u003eQueryText — so the cost of including neo4j is close to nothing, while excluding it ships a known arbitrary-statement vector on the primary backend.\n  - Territory check against ak39, in flight: ak39 is in internal/codegen/neo4j/render_models.go, this is render_queries.go. Different files. Rebase, do not serialize.\n\nThe LOUD package-level shadows Linus lists as non-blocking ($fmt, $ctx, $q, $agtypeArgs, $agtypeString, $decode\u003cEntity\u003e, and $_ which fails generation with a misleading gofmt error) stay out of this bead and go to gqlc-ni66, which I am broadening. They are all compile failures of the generated package, i.e. the loud class; only the QueryText const is silent, because it is string-typed and cypherStmt takes a string, so the shadow type-checks.\n\nALSO REQUIRED: bodyLocalsOf must grow to see REFERENCED identifiers, not only declared ones. Without that the test keeps certifying a fix that is not there, which is the failure mode this bead exists to prevent.\n\nLinus's non-blocking coverage finding (writeScenarios holds one scenario; write-with-projection on AGE is pinned only by goldens the emitter produced) is real and is gqlc-35yu.14's territory. Not promoting it here.\n\nQUERY-TEXT CONST SHADOWING (2026-08-01). Review FAIL on b9feaa6; this is\nthe follow-on fix.\n\nb9feaa6 fixed half a defect. bodyLocal renamed identifiers the body\nDECLARES; it did nothing about package-level identifiers the body\nREFERENCES, and the query-text const is one. render_queries.go emitted\n\"const \u003cbare\u003eQueryText\" and referenced it by name from the method body,\nand LowerFirstRune derives that const name and the single-parameter\nargument name both -- its own doc comment says so -- so\n$\u003cbare\u003eQueryText reproduces it exactly.\n\nReproduced with plain gqlc generate, not asserted. Driver\napache-age-pgx-v5, query\n\"MATCH (p:Person) WHERE p.name = $personByStmtQueryText RETURN p.name\":\n\n  const personByStmtQueryText = `MATCH ... $personByStmtQueryText ...`\n  func (q *Queries) PersonByStmt(ctx context.Context, personByStmtQueryText string) (string, error) {\n      stmt, err := q.cypherStmt(\"$gqlc$\", personByStmtQueryText, \"v0 ag_catalog.agtype\")\n\nThe identifier on the third line resolves to the parameter. go build on\nthe emitted package exits 0 -- measured, by building it inside the\ntest/data/codegen module. So the caller's runtime string BECOMES the\nstatement text. That is hard constraint #1 and it is worse than\nconcatenation, because there is no concatenation to grep for: the whole\nstatement is the argument. The :exec form makes it an arbitrary\ncaller-chosen write.\n\nScheme. ParamLocal, Unshadowed and QueryTextConst now live in\ninternal/codegen/names.go rather than in the AGE backend, because the\nsingle-parameter signature they describe is backend-invariant surface --\nevery target emits it, so every target inherits the collision.\nQueryTextConst(p) = Unshadowed(p, p.Bare+\"QueryText\"), and the const name\nis the side that moves because it is generator-owned and no part of the\nsurface a caller writes against. The parameter cannot move and reordering\ndoes not help: \"text := \u003cbare\u003eQueryText\" inside the function still\nresolves to the parameter.\n\nFixed on BOTH backends per the scope ruling: neo4j had the identical hole\nat render_queries.go 179/332/380 (:exec and the read path are two\nseparate reference sites there, one each). Verified by regenerating the\nsame repro against neo4j-go-v5. No collision with gqlc-ak39, which is in\nrender_models.go.\n\nZero golden churn across the existing corpus on either backend -- no\nfixture names a parameter \u003cbare\u003eQueryText.\n\nTest, and this is the part that mattered. The old declaredIdents\ncollected only AssignStmt/ValueSpec/RangeStmt binders, so bodyLocalsOf\nwas structurally blind to a shadowed package-level name and the test\npassed with the hole open. My own earlier probe had the same blind spot,\ndrawing its names from the same mental model as the fix. The candidate\nset is now every identifier the emitted file mentions, walked off the\nsyntax tree -- 45 of them, including the three QueryText consts, none\nenumerated by hand.\n\nThe verdict is structural rather than a name comparison:\nrequireNothingDeclaredIsCaptured asserts that every package-level name\nthe emitted file declares is a free identifier of some method. Capture\nhas exactly one signature -- the declaration stays, the body still spells\nthe name, and the name now resolves to the parameter, leaving the\ndeclaration referenced by nobody. Asserting reachability never has to\nknow what the emitter chose to call anything, which is what makes the set\nclosed rather than enumerated: a package-level name added later is held\nby the same assertion.\n\nOut-of-scope names stay out without special-casing. fmt, agtypeArgs,\nagtypeString and decode\u003cEntity\u003e arrive from an import or another file of\nthe package, so they are not package-level declarations OF THIS FILE and\nthe assertion does not fire on them; capturing one is a compile failure\nof the generated package anyway. ctx and q are signature binders, so they\nnever enter the free set. That is gqlc-ni66's territory, and the\nasymmetry is real: the query-text const is the only silent one, because\nit is string-typed and lands where a string is expected.\n\nMutation checks, both halves. Reverting QueryTextConst to\np.Bare+\"QueryText\" fails exactly manyQueryText, oneQueryText and\npurgeQueryText with \"declared but no method resolves it\". Neutering\nUnshadowed fails twelve: those three plus args, decoded, err, narrowed,\nout, raw0, rows, stmt, value0. Restored, green.\n\nFixture renamed query_param_shadows_body_local -\u003e\nquery_param_shadows_emitted_name, since body locals are no longer the\nwhole of what it holds, and extended with ConstShadowOne :one,\nConstShadowMany :many and ConstShadowExec :exec, each named to reproduce\nits own const. Enrolled on apache-age-pgx-v5 and neo4j-go-v5, so\nTestBackendInvariantSurface holds the signatures identical across both.\n\nGates, all green: just test, just lint, just test-codegen-fence, just\nactionlint, live battery under docker.\nHANDOFF 2026-08-01. c476707 VERIFIED BY ME (manager), independently of the implementer's account. Rebuilt the CLI at c476707 and regenerated /tmp/mgr-probe2. Both backends now emit a renamed const AND reference the renamed const, while the wire-level parameter name is preserved:\n  AGE:   const personByStmtQueryText_ = `...` ; q.cypherStmt(\"$gqlc$\", personByStmtQueryText_, \"v0 ag_catalog.agtype\") ; agtypeArgs(map[string]any{\"personByStmtQueryText\": personByStmtQueryText})\n  neo4j: q.db.run(ctx, personByStmtQueryText_, map[string]any{\"personByStmtQueryText\": personByStmtQueryText}, neo4j.AccessModeRead)\nThe headline defect (caller's runtime string becoming the entire statement; arbitrary caller-chosen write in the :exec form) is closed on both arms.\n\nNEW OBSERVATION for the reviewer, from probe /tmp/mgr-probe3: I fed $personByStmtQueryText_ and $wipeByStmtQueryText__ and the Go parameter came out as personByStmtQueryText / wipeByStmtQueryText — the trailing UNDERSCORES WERE STRIPPED in the Go name derivation while the wire name kept them. If query-derived Go names can never contain an underscore, then Unshadowed's name+=\"_\" escape hatch is unreachable by construction: the loop can never iterate twice, and any test claiming to exercise it is testing nothing. That needs establishing either way. Corollary worth checking: underscore-stripping collapses $foo and $foo_ to the same Go name, which in the 2+ parameter form means two Params struct fields both named Foo — verify that is a loud refusal, not a silent overwrite.\n\nSTATUS: adversarial review was dispatched and DIED ON SESSION QUOTA before returning a verdict. No verdict exists. Branch is clean at c476707. Re-dispatch a fresh review before any merge — do not merge on my verification alone, which only covers the reported defect and not the structural claim (requireNothingDeclaredIsCaptured walks only ONE file's declarations, so a package-level name declared in db.go/models.go/graph.go/querier.go and referenced from queries.cypher.go would be capturable and invisible).","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:12:23Z","created_by":"areqag","updated_at":"2026-08-02T03:47:47Z","started_at":"2026-08-01T20:02:36Z","closed_at":"2026-08-02T03:47:47Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/623","dependencies":[{"issue_id":"gqlc-35yu.10","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:12:23Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.10","depends_on_id":"gqlc-35yu.7","type":"blocks","created_at":"2026-07-31T19:12:55Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.10","depends_on_id":"gqlc-35yu.8","type":"blocks","created_at":"2026-07-31T19:12:56Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.9","title":"codegen/age: entity columns — vertex/edge agtype decode into entity structs","description":"Entity-typed columns for the AGE backend: decode agtype vertex/edge composite text (id/label/properties per the spike corpus, including the ::vertex / ::edge suffixes) into the SAME entity structs phase Z names for every backend — the Go surface is backend-invariant; only the decode source differs. Covers node-entity and edge-entity columns and the edge-union scope at parity with the neo4j impl (whatever the shared admission in phase A admits for the target). Emitted decode helpers live in models.go beside the scalar ones.\n\nTDD: red goldens per enrolled fixture first; live entity-read scenarios added to the shared battery (they run on the neo4j arms too — battery stays backend-invariant).","acceptance_criteria":"Entity-column fixtures enrolled for AGE pass golden + compile gates; live battery gains entity read scenarios green on all arms; decode helpers unit-tested against corpus text including missing-property and wrong-label inputs; entity struct definitions unchanged from the neo4j target (same models.go structs modulo decode helpers).","notes":"MANAGER RULING ON FORK B (the LIST/ANY refusal), verified empirically before\nruling.\n\nThe implementer disclosed this himself as \"a real regression and it is\nstructural\". He was right that the behaviour changed, and right to say so\nloudly. I reproduced it in both directions rather than take it on report.\n\nMethod: enrolled test/data/codegen/valid/schema_list_property for\napache-age-pgx-v5 and reduced its query batch to a single scalar read\n(RETURN e.id), so the LIST property is declared in the schema and touched by\nno query.\n\n  master bd29f01                -\u003e ok, generation SUCCEEDS\n  branch feat/...entity-columns -\u003e FAIL, \"unrepresentable property width:\n                                   entity \\\"Event\\\" property \\\"tags\\\" has\n                                   LIST\u003cSTRING\u003e\"\n\nSo it is real and user-reachable: the AGE driver is offerable from `gqlc init`,\nand a schema declaring any LIST or ANY property now fails AGE generation\noutright even for a pure scalar batch.\n\nRULED NOT A DEVIATION, and not blocking on the design. gqlc-35yu.11's own\ndescription specifies this exact shape for this exact class:\n\n  \"Any type the spike marked unfaithful becomes a generate-time first-offender\n   error naming property, type, and backend, extending the existing\n   unrepresentable-width taxonomy (invalid fixture per errored type).\"\n\nA schema-wide first-offender error naming property and type IS the epic's\ndeclared taxonomy. What master did was not a better behaviour, it was silence:\nAGE emitted no entity surface at all, so the schema's property table was never\nconsulted. .9 makes it reachable, and reachable means the taxonomy applies.\n\nBLOCKING ON COVERAGE, however. As shipped the change is invisible: no fixture\nexercises it, schema_list_property is neo4j-only, so nothing in CI states the\nnew behaviour, nothing would catch a later change to it, and gqlc-35yu.13's\neventual fix would have no red test to turn green. .11 prescribes the remedy in\nthe same breath as the rule -- \"invalid fixture per errored type\". Required for\n.9: an invalid fixture pinning the LIST refusal, and the error must name the\nbackend as well as the property and type, which the current message does not.\n\ngqlc-35yu.13 stays open but is re-scoped: it is not that the refusal is wrong,\nit is that its blast radius is the whole schema rather than the queries that\nreach the property. A future implementer must not read .13 as licence to make\nunrepresentable widths generate silently again.\nManager round-2 verification (independent, not taken on report):\n\nB1 (decoder-local shadowing) — VERIFIED FIXED STRUCTURALLY. I deleted the\nentity_property_shadows_decoder_local golden entirely and regenerated: it comes back\nbyte-identical, so the golden is machine-produced, not hand-written. I then built my\nown novel adversarial fixture (properties value0, value1, narrowed, b, key, ok, raw,\nerr, label, out, props on one :Probe) that Carmack had not seen. The apache-age-pgx-v5\ntarget compiled with zero errors. Property names no longer enter decoder scope at all\n— they survive only as %q string literals — so this is a structural fix, not an\nenumerated blocklist.\n\ngqlc-ak39 scoping pushback — ACCEPTED, claim verified. The same adversarial fixture\nagainst neo4j-go-v5 on THIS branch fails with 6 compile errors (err repeated on left\nside of :=, no new variables on left side of :=). I re-ran the identical fixture in a\nclean checkout of master at bd29f01 with a neo4j-only manifest and got the same 6\nerrors. Pre-existing on master, therefore not introduced here. Fixing it churns ~77\nneo4j goldens on an AGE-owned branch, which is a scope violation in the other\ndirection. Deferred to gqlc-ak39 (P1) as filed.\n\nTestBackendInvariantSurface — VERIFIED IT BITES. It exercises 33 multi-target\nfixtures. I injected five divergences into goldens by hand and every one failed the\ntest: (1) rename a struct field, (2) change a field's nullability *string -\u003e string,\n(3) delete a doc-comment line from a type, (4) delete the //sumtype:decl directive,\n(5) delete a marker method. Tree restored clean after each.\n\nB2 (LIST/ANY refusal) — VERIFIED. Two invalid fixtures pin the sentinel; the\nbackend-naming message is separately pinned by TestTypeMapPropertyRejectionReachesTheCaller,\nwhich now includes LIST\u003cSTRING\u003e and asserts the full string including \"which the Apache\nAGE backend has no carrier for\".\n\nGates re-run by me on a2e8610: just test PASS, just lint PASS (0 issues),\njust test-codegen-fence PASS (0 issues), just actionlint PASS.\nLINUS ROUND 3: VERDICT FAIL, one blocking item. Manager reproduced it independently\nbefore accepting it.\n\nDEFECT: an edge-union column whose candidates share a label emits a Go switch with a\nduplicate case. `gqlc generate` exits 0 and the emitted package does not compile.\n\nMy repro, via the repo's own conformance harness rather than a standalone binary:\nfixture with DIRECTED EDGE TYPE LikesFwd (:Person)-[:LIKES]-\u003e(:Post), DIRECTED EDGE\nTYPE LikesRev (:Post)-[:LIKES]-\u003e(:Person), plus Wrote, queried undirected as\n`MATCH (x:Person)-[r:LIKES|WROTE]-(y:Post) RETURN r`.\n\n  branch a2e8610, apache-age-pgx-v5 -\u003e goldens WRITTEN, then TestGoldenBuild fails:\n      queries.cypher.go:56:7: duplicate case \"LIKES\" in expression switch\n  master bd29f01, apache-age-pgx-v5 -\u003e clean refusal: \"unsupported query: the Apache\n      AGE backend serves scalar reads, so 1 query would be dropped: GetAction\n      (column \\\"r\\\" projects edgeUnion)\"\n  master bd29f01, neo4j-go-v5      -\u003e SAME DEFECT, pre-existing:\n      queries.cypher.go:47:7: duplicate case \"LIKES\" in expression switch\n\nNote the anonymous-edge form of this schema is already refused by the shared phase\n(\"edge label \\\"LIKES\\\" is shared across endpoint pairs — requires an explicit Name\").\nOnly the *named* edge-type form gets through. That gap is the whole bug.\n\nRULED BLOCKING. Round 1 failed this bead for exactly this defect class: generate exits\n0 and hands the user Go that does not compile. I cannot enforce that standard for\nproperty-name shadowing and waive it for edge-union labels in the same bead.\n\nRULED AGAINST LINUS ON WHERE TO FIX. He proposed an AGE-local gate in\ninternal/codegen/age/errors.go on the grounds that it churns zero neo4j goldens. I\nverified that claim is true, and that a SHARED gate also churns zero goldens — master's\nsuite is green and TestGoldenBuild compiles every golden, so no existing valid fixture\nhas duplicate-label candidates. Given equal churn, the shared fix is strictly better:\nneither backend can represent this. An AGE edge and a neo4j relationship both carry one\nlabel/type plus properties and endpoint ids, never endpoint labels, so two edge types\nsharing a label are indistinguishable from the column value alone on both. An AGE-local\ngate would knowingly leave neo4j emitting non-compiling Go while this bead claims to\nhave established a backend-invariant surface.\n\nDISTINGUISHED FROM gqlc-ak39. ak39 was deferred because fixing it rewrites ~77 neo4j\ngoldens on an AGE-owned branch. This one rewrites none. The deferral rationale does not\ntransfer.\n\nAlso blocking, from Linus's non-blocking list, because AGENTS.md binds on both:\n- the wiredEntity doc comment claims \"Only wireEntity builds one, so an entity the wire\n  has no form for cannot reach an emission\". That is a package-internal convention, not\n  a type-level guarantee, and the package writes wiredEntity{} literals. Make the claim\n  true or make it accurate.\n- TestBackendInvariantSurface skips receiver-less functions on the unchecked premise\n  that they are all unexported. Assert it rather than assume it.\n\nAccepted as non-blocking: the AGE live arm runs 4 scenarios to neo4j's 5\n(mixed_read_write_batch is the write path, which is bead .10).\n\nManager follow-ups from this round, filed separately: an entity named Queries collides\nwith type Queries in db.go on every backend (pre-existing, cross-backend); narrow-width\nint32/uint64 conversions wrap silently (pre-existing, cross-backend). gqlc-35yu.14\n(edge-union dispatch has no live coverage) is raised in priority — this defect is direct\nevidence the gap costs real money.\nLINUS ROUND 4: VERDICT FAIL. He conceded the placement ruling and closed blocking item\n2, then found that the round-4 gate OVER-REFUSES. Reproduced by me, and the root cause\nis deeper than he diagnosed.\n\nDEFECT: an undirected edge pattern whose endpoints share a label makes the resolver\nappend the SAME schema.EdgeKey twice, because edgeCandidates (internal/resolver/\nresolve.go:477) tries both orientations and for src==tgt both orientations are one key.\nThe round-4 gate reads that repeat as two indistinguishable candidates and refuses.\n\nMy repro, schema with two self-loop edge types under DISTINCT labels:\n    DIRECTED EDGE TYPE Likes (:Person) -[:LIKES]-\u003e (:Person),\n    DIRECTED EDGE TYPE Wrote (:Person) -[:WROTE]-\u003e (:Person)\n    MATCH (x:Person)-[r:LIKES|WROTE]-(y:Person) RETURN r\n\n  branch 95dd0b2, apache-age-pgx-v5 -\u003e REFUSED: \"candidates Likes and Likes both carry\n      edge label \\\"LIKES\\\"\". The message names one entity twice and prescribes a remedy\n      that cannot be followed: both endpoints are already pinned to :Person.\n  master bd29f01, neo4j-go-v5 -\u003e 4 cases emitted, LIKES twice and WROTE twice,\n      non-compiling.\n  master bd29f01, apache-age-pgx-v5 -\u003e refused as out of scope.\n\nThat last line is what makes it blocking rather than pre-existing. On AGE this branch is\nwhat introduces edge-union support, and an undirected self-loop union is mainstream. A\ncapability that refuses it does not meet this bead's acceptance criterion.\n\nLINUS'S PROPOSED FIX SITE IS WRONG, AND I RULE AGAINST IT. He said to dedupe in\ncloseEdge and NOT in edgeCandidates, because R3 spec 4.6 case C detects the single-label\nundirected self-loop off the pre-dedupe length. He read the verdict table and not the\nrationale under it. docs/specs/resolver-stage-r3.md, case C rationale, says verbatim:\n\n  \"Person -\u003e FOLLOWS -\u003e Person where the reciprocal ... is also declared: the same key,\n   so there is no ambiguity. The genuine double-match case requires distinct\n   source/target label sets in the two directions.\"\n\nThe pre-dedupe length IS the bug. The implementation contradicts its own spec, and I\nconfirmed that on master with a single self-loop edge type:\n\n    DIRECTED EDGE TYPE Likes (:Person) -[:LIKES]-\u003e (:Person)\n    MATCH (x:Person)-[r:LIKES]-(y:Person) RETURN r\n  master bd29f01 -\u003e ambiguous edge orientation: edge \"r\" matches both\n      Person-[LIKES]-\u003ePerson, Person-[LIKES]-\u003ePerson\n\nOne key listed twice, from a query the spec says is unambiguous. So deduping in\nedgeCandidates is the spec-correct fix and closes three things at once: this spec\nviolation, the duplicate-case non-compiling emission on neo4j, and the branch's new\nover-refusal.\n\nI verified the dedupe does not weaken genuine case C. Both resolver fixtures pinning\nErrAmbiguousEdgeOrientation (test/data/resolver/invalid/ambiguous_edge_orientation*.cypher)\nuse Person/Post, so the two orientations are two DISTINCT keys, the set stays at size 2\nafter dedupe, and case C still fires.\n\nSCOPE RULING: in scope and blocking. The branch caused the over-refusal, so it fixes it;\nyou do not ship a regression you introduced and file a bead for it. The correct fix\nhappens to be ~10 lines in the resolver and also closes a documented spec violation.\n\nAlso blocking: the valid corpus contains ZERO undirected multi-label edge patterns. That\nblind spot is why round 3's duplicate-case defect and round 4's over-refusal both reached\nreview, and it is why golden churn kept measuring zero. A valid fixture for the\nundirected self-loop union is required.\nMANAGER VERIFICATION OF ROUND 5 (cc9d21e), independent.\n\nThe over-refusal is fixed at the root. edgeProbes now dedupes by whole schema.EdgeKey\n(membership-only map, never iterated, first-occurrence order); edgeCandidates is that\nset filtered by s.Edges; describeTriedEdges reuses edgeProbes rather than rebuilding\nthe enumeration, so the two can no longer drift. Carmack found a third instance of the\nduplication in describeTriedEdges that neither Linus nor I had spotted, and fixed it in\nthe same seam.\n\nVerified by me:\n- My exact repro is now a valid fixture (edge_union_undirected_self_loop, verbatim\n  schema and query) and emits a clean two-case switch — LIKES then WROTE, once each —\n  under BOTH apache-age-pgx-v5 and neo4j-go-v5.\n- Zero existing goldens modified. git diff --name-status 95dd0b2 cc9d21e shows every\n  golden path as an addition; the only modified non-source file is\n  test/data/resolver/valid/schema.mapping.json, which registers the new fixtures.\n- unrepresentable_edge_union_shared_label and its _list companion are still red for the\n  right reason (LikesFwd and LikesRev are distinct keys, so the dedupe leaves both and\n  the round-4 gate still fires).\n- Both ambiguous_edge_orientation fixtures still fail with ErrAmbiguousEdgeOrientation.\n- Mutation test, mine: deleting the dedupe fails FIVE independent guards across two\n  packages — TestEdgeFailMessagesListEachTriedKeyOnce, TestEdgeUnionKeysAreASet, both\n  new resolver valid fixtures, and TestConformanceSuite. The fix is braced.\n- Gates on cc9d21e: just test PASS, just lint PASS (0 issues), just test-codegen-fence\n  PASS (0 issues), just actionlint PASS.\n\nSPEC EDIT ACCEPTED. I had told him not to edit the spec if the spec was right and the\ncode wrong. He found that a DIFFERENT passage was itself wrong and said so with the\nquote rather than complying quietly, which is what I asked for. R3 section 4.4 claimed\n\"Duplicate EdgeKeys are impossible by construction — the same (label, orientation) pair\ngenerates the same key exactly once\". The postcondition is right and the proof is false:\nit counts probes, not the keys they name, and (S,T) and (T,S) are two probes but one key\nwhen S == T. He corrected the false lemma, kept the postcondition, left section 4.6 case\nC's rationale untouched, and documented the two new fixtures in the coverage sketch.\nThat is the correct scope.\n\nROUND 6: REVIEWER VERDICT PASS at cc9d21e. Merged as PR #670, closing #624.\n\nReview took six rounds (three FAILs). What the FAILs found is worth recording,\nbecause two of the three were not AGE defects at all — the AGE work was the\nfirst thing to walk paths that were already broken.\n\nRound 1 FAIL, decoder-local shadowing. A schema property named err/label/out/\nprops/raw emitted a redeclaration and `generate` still exited 0, because the\nformat gate only parses. Fixed structurally: every local an emitted decoder\ndeclares is positional. I verified it by deleting the golden and regenerating\nbyte-identical, then building an 11-hostile-name fixture the implementer had\nnot seen. The same defect exists on neo4j (I reproduced it on master bd29f01,\n6 compile errors) — deferred as gqlc-ak39, since fixing it churns ~77 neo4j\ngoldens on an AGE-owned branch.\n\nRound 3 FAIL, duplicate-label edge union emitting a duplicate switch case. I\nruled against the reviewer on placement: he wanted it AGE-local, I put it in\nthe shared admission phase. Neither backend can represent it — an edge value\ncarries its label and its properties, never its endpoint types — and churn is\nzero either way. Reproduction detail worth keeping: the ANONYMOUS edge form was\nalready refused by Phase Z; only the named DIRECTED EDGE TYPE form got through.\n\nRound 4 FAIL, the gate I ordered OVER-REFUSED. Two self-loop edge types under\nDISTINCT labels, queried undirected, were refused as sharing a label — naming\none entity twice and prescribing an unfollowable remedy. The reviewer proposed\na fix site and I found it backwards: he said do not dedupe the enumeration\nbecause R3 §4.6 detects ambiguity off the pre-dedupe length. But §4.6's own\nrationale (case C) says the same key in both orientations is unambiguous, so\nthe pre-dedupe length IS the bug and §4.4's lemma asserting duplicates are\nimpossible was simply false. Confirmed independently on master: a single\nself-loop edge type already gave `ambiguous edge orientation: edge \"r\" matches\nboth Person-[LIKES]-\u003ePerson, Person-[LIKES]-\u003ePerson`. Fixed by deduping in the\nenumeration; §4.4's lemma corrected rather than worked around.\n\nThe implementer then found a third instance himself — describeTriedEdges\nrebuilt the same enumeration for ErrUnknownEdge's message and repeated keys for\nthe same reason. Both now read one enumeration.\n\nIndependent verification at cc9d21e before authorizing merge: zero MODIFIED\ngoldens (git diff --name-status 95dd0b2..cc9d21e — every golden path is A);\nthe new fixture emits one case per declared type on both targets; both\nunrepresentable_edge_union fixtures still red with the right sentinel; both\nambiguous_edge_orientation fixtures still red. Mutation test: deleting the\ndedupe fails FIVE guards across two packages. Five gates green including\n`sg docker -c 'just test-codegen-live'`.\n\nNon-blocking items from round 6 filed as gqlc-kph4 (P2 — the plural-endpoint ×\nundirected edge-closure path has zero corpus coverage, and round 5 changes\nbehaviour there) and gqlc-monh (P3 — rationale text incomplete or reaching\nacross layers at three sites).\n\nMethod note for later beads: the reviewer's PASS was accepted only after\nreading his actual verdict and re-running the evidence. Twice in this bead the\nreviewer's diagnosis was right and his prescribed fix was wrong. A FAIL is\ninformation about where to look, not an instruction.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:12:13Z","created_by":"areqag","updated_at":"2026-08-01T19:58:07Z","started_at":"2026-08-01T16:31:15Z","closed_at":"2026-08-01T19:58:07Z","close_reason":"Merged as cdd09ca (PR #670), closing #624. Reviewer PASS at cc9d21e after six rounds.","external_ref":"https://github.com/areqag/gqlc/issues/624","dependencies":[{"issue_id":"gqlc-35yu.9","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:12:13Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.9","depends_on_id":"gqlc-35yu.7","type":"blocks","created_at":"2026-07-31T19:12:53Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.9","depends_on_id":"gqlc-35yu.8","type":"blocks","created_at":"2026-07-31T19:12:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.8","title":"codegen/age: live adapter arm + nightly CI wiring","description":"Bring the AGE arm into the shared live scenario battery: adapter implementing the per-backend harness interface — apache/age container image pinned by digest, not tag (gqlc-9u5 precedent: tag drift can serve a stale cached PASS from the Go test cache); pgx pool constructed with SessionInit as AfterConnect; seed(cypher) via ag_catalog.cypher over the pool; isolation = unique graph per scenario via EnsureGraph + Cleanup DropGraph, so AGE sub-tests run in parallel (unlike the serialized neo4j DETACH-wipe arms) and the lifecycle helpers get exercised as the isolation mechanism itself.\n\nCI: the arm joins the existing codegen_live nightly/manual job. PR CI wall-time must be untouched — the codegen_live tag boundary already guarantees that; do not add the arm to any PR-triggered workflow. GQLC_SKIP_LIVE behavior extends to the AGE arm. Watch the second nightly run for cache steady-state if any actions/cache path changes (bd memory: first run after a path change is cold).","acceptance_criteria":"Shared battery (scalar scenarios) green against a live AGE container locally under -tags codegen_live; nightly job runs the AGE arm and fails red on a seeded assertion break (prove the gate fails, not just that a check exists); AGE sub-tests demonstrably parallel (unique graph names in logs); PR CI workflow diff shows no new PR-time steps; SessionInit FAILURE path covered — a pool whose sessions lack ag_catalog on search_path must fail at pool ACQUISITION, with the test proving the canary discriminates (a canary literal that survives agtype_to_bool would pass on the broken session and must be shown not to be the one shipped).","notes":"SCHEDULING: gqlc-3eyw (codegen_live code is outside every lint gate) is deliberately NOT a blocking dependency of this bead — .8 can ship unlinted exactly as .4 did, and a hard edge would stall the epic critical path on a config decision. But .8 widens the unlinted surface with the AGE adapter arm, so settle gqlc-3eyw BEFORE starting .8 if the slot is free. It is small config work.\n=== SessionInit FAILURE-PATH REQUIREMENT, INHERITED FROM .6 (recorded 2026-08-01) ===\n.6's notes carry: \"Test the failure path, not just the happy path: a connection\nwhose search_path lacks ag_catalog must be REJECTED by SessionInit, and that\nrejection must be observable as a pool-acquisition failure.\"\n\n.6 is a golden-EMISSION bead — it has no live container and no pool, so that\nrequirement is not dischargeable there and .6 will not be held for it. It lands\nhere, where the pool and the container both exist for the first time. Recorded\nnow so the deferral survives outside a commit message.\n\nWHY IT IS SHARP, NOT CEREMONIAL: the canary's discriminating power depends on\nthe literal chosen. The .6 review established that\n  SELECT '1'::ag_catalog.agtype = '1'::ag_catalog.agtype\nfails on a misconfigured session inside agtype_to_bool — i.e. for a reason\nunrelated to operator resolution via search_path. A canary using a literal that\nhappens to survive agtype_to_bool (e.g. 'true') would PASS on a connection with\nag_catalog off the path, which is exactly the deployment the canary exists to\nreject. So the failure path is what proves the canary is the right canary; the\nhappy path proves nothing.\n\nHOW TO APPLY: add a scenario that builds a pool whose AfterConnect runs\nSessionInit against a session with ag_catalog deliberately off search_path, and\nassert pool acquisition FAILS. Assert on the rejection being observable at\nacquire time, not merely that some error exists somewhere.\n=== NON-SUPERUSER ARM REQUIREMENT, INHERITED FROM .6 (recorded 2026-08-01) ===\nThe harness MUST exercise at least one arm connecting as a NON-SUPERUSER role,\nnot only as postgres.\n\nWHY: .6 round 2 shipped a SessionInit whose first statement was LOAD 'age'. I\nverified against the pinned image that LOAD 'age' is superuser-only:\n\n  LOAD 'age' as postgres  -\u003e LOAD, rc=0\n  LOAD 'age' as appuser   -\u003e ERROR: access to library \"age\" is not allowed\n\nBecause an error returned from pgxpool's AfterConnect DISCARDS the connection,\nthat statement bricked the entire pool for any least-privilege role: the pool\nwould never yield a usable connection at all. A total, deployment-fatal defect.\n\nAnd it would have sailed straight through this bead's battery, because the\nharness connects as postgres. A green live suite would have certified it. The\nbug was caught by adversarial review, not by the tests that exist to catch\nexactly this class of thing. That is the gap this requirement closes.\n\nHOW TO APPLY: create a role with only the privileges a generated client\nactually needs (no SUPERUSER), and run the scalar battery — or at minimum the\npool-construction and one read scenario — as that role. Treat \"works as\npostgres\" as proving nothing about deployability. Any future statement added to\nSessionInit is then privilege-checked by construction rather than by review.\n\nRelated: LOAD 'age' is now DELETED from SessionInit (see gqlc-35yu.6 notes).\nThe canary is the loader -- its '+' is an AGE C function, so evaluating it\nforces the implicit library load and _PG_init before any user query is parsed.\n=== TWO RECORDED FACTS ON THIS BEAD ARE WRONG (verified 2026-08-01) ===\n\n(1) \"PR CI wall-time must be untouched -- the codegen_live tag boundary\nalready guarantees that\". FALSE. .github/workflows/codegen-live.yml carried\n`on: pull_request` and its own header called it a \"PR-blocking merge gate\"\n(bd gqlc-rvw promoted it). The tag boundary keeps codegen_live out of the\nDEFAULT build; it does not keep it out of PR CI, because that workflow passes\nthe tag explicitly. Adding the AGE arm to `just test-codegen-live` would have\nadded an apache/age container boot to every pull request.\n\nRESOLUTION: the battery is now split by backend at the recipe level and the\nworkflow runs one job per backend.\n  test-codegen-live        everything (local + as the union)\n  test-codegen-live-neo4j  -run TestLiveSmoke -skip TestLiveSmoke/apache-age\n  test-codegen-live-age    -run 'TestLiveSmoke|TestAGESessionInit'\n                           -skip TestLiveSmoke/neo4j\nJob `neo4j` keeps every existing trigger; job `apache-age` carries\n`if: github.event_name != 'pull_request'`. -skip rather than a narrower -run\nso an arm named in neither half shows up as skipped instead of silently\nvanishing. Verified empirically that -skip does not run the excluded\nsubtest body: the neo4j half boots only neo4j (18.3s, unchanged), the AGE\nhalf boots only apache/age.\n\n(2) HOW TO APPLY for the SessionInit failure path -- \"build a pool whose\nAfterConnect runs SessionInit against a session with ag_catalog deliberately\noff search_path\" -- is NOT CONSTRUCTIBLE. SessionInit's own first statement\nunconditionally prepends ag_catalog to the search_path, so no session it runs\non can be off the path by the time the canary executes. Worse, every failure\nreachable that way (extension absent, role lacks USAGE) fails the shipped\ncanary and a weak canary IDENTICALLY, so it does not demonstrate\ndiscrimination at all.\n\nRESOLUTION: decomposed into three separately provable properties, all in\nTestAGESessionInit (test/data/codegen/live_age_session_test.go):\n\n  (A) acquisition-time refusal on genuinely broken deployments. Table over\n      a role with no USAGE on ag_catalog (SQLSTATE 42501) and a database with\n      no age extension (3F000). Each asserts: a plain pgx.Connect on the same\n      DSN SUCCEEDS (so it is the session being refused, not the credentials);\n      pgxpool.NewWithConfig succeeds; Acquire fails wrapped in \"gqlc: AGE\n      operator canary\"; pool.Stat().TotalConns() == 0.\n\n  (B) acquire-observable A/B, which is the discrimination proof. A database\n      with age installed, granted and on the path, from which only\n      ag_catalog.+(agtype,agtype) has been removed (ALTER EXTENSION age DROP\n      OPERATOR, then DROP OPERATOR). A pool running a hook whose probe is\n      'true'::agtype = 'true'::agtype ACQUIRES; a pool running the shipped\n      SessionInit FAILS to acquire with 42883. The two hooks share the\n      shipped hook's own search_path statement, captured from it via a\n      pgx.QueryTracer, so the probe is the only thing they differ in.\n\n  (C) statement replay on a real off-search_path session, with the canary\n      taken from the tracer capture rather than retyped. Rejected-literal\n      table:\n        shipped '1'+'1' = '2'  -\u003e 42883 (operator resolution)\n        'true' = 'true'        -\u003e succeeds, returns true (would false-pass)\n        '1' = '1'              -\u003e 22023, cannot cast agtype integer to\n                                  type boolean, i.e. fails for an unrelated\n                                  reason\n      NOTE: .6's note names the '1' = '1' failure as being \"inside\n      agtype_to_bool\". Confirmed exactly: cannot_cast_agtype_value,\n      agtype.c:2744. Its SQLSTATE is 22023, not 42846.\n\n  (D) a fence that every AGE target's emitted SessionInit issues byte-identical\n      statements, which is what licenses one pool serving handles generated\n      into several packages.\n=== NON-SUPERUSER ARM: MINIMUM GRANT SET, EMPIRICALLY DERIVED ===\n\n.6's note implies a bare `appuser` could run the canary. It cannot. A plain\nLOGIN role with no grants gets, on the shipped canary:\n  ERROR: 42501: permission denied for schema ag_catalog\nAGE grants nothing to PUBLIC. The minimum set a generated client needs is\nthree grants, one per emitted requirement (ageAppGrants in live_age_test.go):\n  GRANT USAGE ON SCHEMA ag_catalog          every emitted statement names it\n  GRANT SELECT ON ag_catalog.ag_graph       EnsureGraph/DropGraph existence guard\n  GRANT CREATE ON DATABASE \u003cdb\u003e             create_graph builds a schema\nEVERY pool in the package -- the battery arm and all of TestAGESessionInit's\npools -- logs in as this role. The superuser connection exists only for\nbootstrap DDL and is closed before anything under test opens.\n\n=== MUTATION PROOFS (all run against real containers, -count=1) ===\n\nRequirement 1, the canary discriminates. Mutated the EMITTER\n(internal/codegen/age/render_graph.go canaryStmt) to\n  SELECT 'true'::ag_catalog.agtype = 'true'::ag_catalog.agtype\nand regenerated every AGE golden, i.e. the mutation is \"the emitter shipped a\nweaker canary\", not a hand-edit of one file. Result: exit 1 in 5.65s, red on\nexactly the two discrimination assertions and nothing else --\n  .../the_shipped_canary  \"An error is expected but got nil\"      (property C)\n  .../the shipped canary refuses a session a weaker probe admits\n                          \"An error is expected but got nil\"      (property B)\nProperty (A) stayed GREEN under the mutation, which is the bead's point stated\nas a test result: a coarse broken-deployment test cannot tell the two canaries\napart, so only B and C carry the requirement.\n\nRequirement 2, the non-superuser arm. Two mutations, run as a pair:\n  2a  removed GRANT CREATE ON DATABASE from ageAppGrants\n      -\u003e TestLiveSmoke/apache-age-pgx-v5 exit 1, both scenarios red with\n         gqlc: ensure graph \"gqlc_live_1\" (the name bound at New):\n         ERROR: permission denied for database postgres (SQLSTATE 42501)\n      The arm genuinely runs on the limited role's privileges.\n  2b  same grant still removed, arm's pool DSN switched to ageSuperuser\n      -\u003e exit 0, PASS in 4.686s.\n2b is .6's recorded defect reproduced in miniature: a battery that connects as\npostgres certifies green a deployment the application role cannot run at all.\n\n=== NIGHTLY GATE FAILS RED (not merely \"a check exists\") ===\nSeeded require.Equal(t, \"Alice\", name) -\u003e \"AliceBROKEN\" in the shared battery\nand ran the literal nightly command, `just test-codegen-live-age`:\n  exit 1, --- FAIL: TestLiveSmoke/apache-age-pgx-v5/one_col_one_param_one\n  expected: \"AliceBROKEN\"  actual: \"Alice\"\n\n=== PARALLELISM AND ISOLATION ===\nageArm.parallelScenarios() reports true; scenario graph names come from an\natomic counter and appear in the logs as gqlc_live_1 / gqlc_live_2. Isolation\nmutation-proven earlier: forcing the name to a constant reds the arm with\nduplicate key value violates unique constraint \"pg_namespace_nspname_index\"\n(SQLSTATE 23505).\n=== DESIGN DECISIONS THE BEAD DID NOT DICTATE ===\n\n1. Battery interface split into read and write halves. The AGE target does not\n   emit :exec yet (that is .10), so it cannot serve mixed_read_write_batch.\n   Rather than let the AGE arm silently skip write scenarios, `harness` now\n   carries only the scalar surface and `writeHarness` extends it; the `arms`\n   table records `writes: bool` and TestLiveSmoke asserts\n   `arm.writes == (h implements writeHarness)`. An arm that gains or loses\n   :exec emission fails the battery until both are updated, instead of\n   dropping scenarios unremarked. .10 flips one bool.\n\n2. one_col_one_param_one added to the shared scalar battery, and its manifest\n   gained the neo4j-go-v6 target (it had v5 + age only). Without it the only\n   shared :one/sentinel coverage came from mixed_read_write_batch, which AGE\n   cannot serve. Golden subtree for v6 regenerated via the conformance\n   -update path.\n\n3. TestAGESessionInit boots its own apache/age container rather than sharing\n   the battery arm's. It creates roles, creates databases and drops an\n   operator out of an extension; none of that should be observable by the\n   arm the scalar battery drives. Cost is one extra postgres container, which\n   is negligible next to the neo4j pair, and all four boot concurrently\n   (full suite, -count=1, real containers: 35.6s).\n\n4. The AGE session-init contract lives in its own file rather than as an arm\n   scenario. It is not backend-parametric -- there is no neo4j analogue of a\n   pool-acquisition hook -- so putting it in the shared battery would have\n   meant a scenario only one arm can run.\n\n5. openAGEPool now takes the AfterConnect hook as a parameter. The\n   discrimination proof needs two pools over one DSN differing only in their\n   hook; parametrising the one helper beats a near-duplicate of it.\n\n6. `refuses(ctx, pool)` releases a connection the pool unexpectedly hands out\n   before returning nil. Found the hard way: the first mutation run took the\n   full 10m test timeout and died in a goroutine dump, because a leaked\n   acquired resource blocks pgxpool.Close in t.Cleanup. With the release, the\n   same mutation reds in 5.65s with a legible assertion. A red that takes ten\n   minutes to print is a red nobody reads.\n=== TWO RECORDED FACTS IN THIS BEAD ARE WRONG (found by the implementer,\nverified by the manager, 2026-08-01) ===\n\n--- 1. \"PR CI wall-time must be untouched -- the codegen_live tag boundary\nalready guarantees that.\" FALSE, and it was a live trap. ---\n\nI verified this at master 1335fae. .github/workflows/codegen-live.yml carried\n\n    on:\n      pull_request:\n      workflow_dispatch:\n      schedule:\n\nand its own header opens \"PR-blocking merge gate\", noting that gqlc-rvw\npromoted it to blocking. The build tag keeps codegen_live out of the DEFAULT\nbuild; it does not keep it out of PR CI, because that workflow passes\n-tags codegen_live explicitly. So the guarantee this bead relies on does not\nexist, and the obvious implementation -- add the AGE arm to\njust test-codegen-live -- would have booted an apache/age container on every\nPR, which is the exact outcome the bead forbids while believing itself immune.\n\nRESOLUTION (implementer's, accepted): split the recipe by backend rather than\ngating the workflow wholesale.\n    test-codegen-live-neo4j = -run TestLiveSmoke -skip 'TestLiveSmoke/apache-age'\n    test-codegen-live-age   = -run 'TestLiveSmoke|TestAGESessionInit' -skip 'TestLiveSmoke/neo4j'\nJob `neo4j` keeps every trigger; job `apache-age` carries\n    if: github.event_name != 'pull_request'\nI confirmed the only PR-time step change is\n    - just test-codegen-live\n    + just test-codegen-live-neo4j\nand that the new age recipe appears solely in the gated job. He also verified\nempirically that -skip does not execute the excluded subtest body, by observing\nwhich images actually boot: neo4j half boots neo4j@sha256:362542 only (18.3s),\nage half boots apache/age@sha256:4241e2 only (7.8s). That is the right proof --\na -skip that still ran the body would show the other image.\n\n--- 2. This bead's own \"HOW TO APPLY\" for the SessionInit failure path is not\nconstructible as written. ---\n\nIt says to \"build a pool whose AfterConnect runs SessionInit against a session\nwith ag_catalog deliberately off search_path\". You cannot: SessionInit's first\nstatement unconditionally PREPENDS ag_catalog to the search_path, so the\npremise cannot be set up through the shipped hook. And every failure reachable\nthat way (extension absent, role lacks USAGE) fails a WEAK canary and the\nshipped canary identically -- so it would have demonstrated nothing about\ndiscrimination, which was the entire point of the requirement.\n\nThe requirement itself stands and is discharged; only the recipe was wrong.\nDecomposed into: (A) an acquisition-time refusal table (42501 no-USAGE role,\n3F000 no-extension DB) asserting Connect succeeds, NewWithConfig succeeds,\nAcquire FAILS wrapped in \"gqlc: AGE operator canary\", TotalConns()==0;\n(B) an acquire-observable A/B on a database with ONLY\nag_catalog.+(agtype,agtype) dropped -- the weak-probe pool acquires, the\nshipped pool fails 42883; (C) statement replay off the search_path with the\ncanary captured via a pgx.QueryTracer, plus a rejected-literal table; (D) a\nfence that every AGE target's SessionInit issues identical statements.\n\nNote (B) is the sharp one: dropping a single operator is what separates the two\ncanaries, and it is exactly what \"extension absent\" cannot do.\n\n--- SMALLER CORRECTIONS TO .6's RECORD ---\n.6's note that '1' = '1' fails \"inside agtype_to_bool\" is right; its SQLSTATE\nis 22023 (cannot_cast_agtype_value, agtype.c:2744).\n\n.6's transcript implies a bare `appuser` can run the canary. It cannot --\n42501, permission denied for schema ag_catalog. AGE grants NOTHING to PUBLIC.\nThe minimum set for a generated client is USAGE ON SCHEMA ag_catalog,\nSELECT ON ag_catalog.ag_graph, and CREATE ON DATABASE. This matters for the\nnon-superuser requirement: \"non-superuser\" is not a single role, it is a\nspecific grant set, and that set is now pinned by ageAppGrants.\nManager rulings on Linus round-1 observations (round-1 verdict: PASS).\n\nIndependent verification of the PASS, done before ruling: I re-ran the unit\nand lint gates green, then re-injected the historical gqlc-35yu.6 defect\nmyself -- `LOAD 'age'` prepended to the emitted SessionInit of\none_col_one_param_one -- and drove the AGE live arm both ways.\n\n  RED as gqlc_app: `gqlc: load age: ERROR: access to library \"age\" is not\n  allowed (SQLSTATE 42501)`, failing both TestLiveSmoke/apache-age-pgx-v5\n  scenarios at EnsureGraph and TestAGESessionInit at capture.\n\n  GREEN with the identical mutation and the app role created LOGIN SUPERUSER,\n  nothing else changed: ok 4.59s, both scenarios passing against real graphs\n  gqlc_live_1 / gqlc_live_2.\n\nSo the arm's redness is attributable to the least-privilege role and to\nnothing else. Requirement 2 is load-bearing. Worktree restored to 84b63de.\n\nNB1 (+0.7s PR-time on an 18.4s job) -- DECLINED. Proportionate.\n\nNB2 (\"every AGE target prepares a session identically\" iterates a\nhand-maintained 2-element list) -- PROMOTED, as a naming defect. There are 12\nAGE golden targets; the list names 2. The property the arm actually depends\non is narrower and is what the list checks: the two packages the arm binds\ntogether issue the same statements, which is what lets one pool serve handles\ngenerated into both. Fix the name and the comment to claim that, not a\nuniversal. The exhaustive claim over all 12 belongs offline, not behind a\ncontainer -- filed separately.\n\nNB3 (shipped[0]/shipped[1] order coupling) -- DECLINED. Guarded by the\nrequire.Len above it and fail-safe: a reordering fails loudly.\n\nNB4 (justfile comment contrasts a rejected alternative) -- PROMOTED. Direct\nAGENTS.md violation. The mechanism is worth keeping, the contrast is not:\nstate positively that -skip names the excluded half, so an arm named in\nneither half still runs and reports as skipped.\n\nNB5 (live recipes carry no -count=1) -- PROMOTED, scoped. gqlc-9u5's cache\nrationale is a PR-cost argument, and it still holds for\ntest-codegen-live-neo4j, which is the PR gate -- leave that one cached.\ntest-codegen-live-age is nightly and manual only and test-codegen-live is\nwired to no CI job at all, so neither has a PR cost to protect. For those two\nthe cache buys nothing and can serve a nightly a PASS it did not run.\nMERGED as 5dd3c0a (PR #660). Issue #625 closed; umbrella #633 verified still\nopen. Epic gqlc-35yu now 8/12.\n\nRound-1 review PASS over 11 mutations; I verified it independently by\nre-injecting the historical .6 defect myself, red as the least-privilege role\n(SQLSTATE 42501) and green with the same role created SUPERUSER.\n\nA BLOCKING defect got past both the implementer and round-1 review, and is\nworth recording because the class is not code:\n\nThe workflow split replaced the single job id `live-smoke` with `neo4j` and\n`apache-age`. GitHub derives a check context from the job id, and master's\nbranch protection required the context list [\"lint\",\"test\",\"tidy\",\"actionlint\",\n\"govulncheck\",\"live-smoke\",\"codegen-fence\"]. So the split silently deleted a\nrequired context. PR #660 could never have satisfied branch protection, and had\nit merged, master would have required a context no workflow produces --\nblocking every subsequent pull request in the repo.\n\nI caught it by diffing the reported check names against the live protection\nAPI, not by reading the YAML, which looks entirely correct on its own. Fixed\nin-PR by renaming the jobs `live-smoke` / `live-smoke-age`, so no shared\nbranch-protection state had to be mutated. Both jobs now carry a comment\nrecording why their id is load-bearing.\n\nGENERAL LESSON for the remaining epic beads: a workflow job rename is an\ninterface change against branch protection. Any bead touching .github/workflows\njob ids must diff the produced contexts against\n`gh api repos/areqag/gqlc/branches/master/protection`. Reviewing the YAML in\nisolation cannot catch this.\n\nRound 2 focused on that blind spot returned PASS. Two further comment\noverclaims were promoted from it and fixed: the justfile comment described\narm-level -skip subtraction as if it also governed top-level tests, which are\nbounded by each recipe's -run allowlist; and live-smoke-age gained the\nmirror-image warning that it skips on pull requests and GitHub counts a\nskipped check as satisfying a required context.\n\nSpun out: gqlc-p6mh (offline fence that all 12 AGE targets emit an identical\nSessionInit) and gqlc-0oau (P2: the AGE arm's only gate is a nightly with no\nfailure alerting, subject to GitHub's 60-day cron disablement).","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:12:04Z","created_by":"areqag","updated_at":"2026-08-01T16:21:21Z","started_at":"2026-08-01T14:34:15Z","closed_at":"2026-08-01T16:21:21Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/625","metadata":null,"dependencies":[{"issue_id":"gqlc-35yu.8","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:12:04Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.8","depends_on_id":"gqlc-35yu.4","type":"blocks","created_at":"2026-07-31T19:12:51Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.8","depends_on_id":"gqlc-35yu.7","type":"blocks","created_at":"2026-07-31T19:12:52Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.7","title":"codegen/age: scalar read path — :one/:many methods, param encoding, agtype scalar decode, error taxonomy","description":"First query-bearing AGE stage. Emits per-query :one/:many methods for scalar params + scalar columns: the cypher() invocation uses the parameter mechanism the spike selected (server-side binding — string interpolation of user args is banned), column lists emitted as the AS (v0 ag_catalog.agtype, ...) projection, and emitted agtype scalar decode helpers (bool/integer/float/numeric/string/null, driven by the spike text corpus) in models.go. Error taxonomy mirrors the neo4j impl: ErrNoRows when :one matches nothing, ErrMultipleResults on more than one — identity-matchable sentinels (errors.Is).\n\nTDD: for each fixture enrolled for the AGE target, write the golden red first, then emit; decode helpers get direct unit tests against the spike corpus text before any golden lands.","acceptance_criteria":"Applicable valid fixtures gain AGE golden subtrees and pass; goldens compile under the generic build gate; decode helpers unit-tested against captured agtype text including malformed input; ErrNoRows/ErrMultipleResults emitted and sentinel-matchable; no fmt.Sprintf or string concatenation of user params anywhere in emitted query text.","notes":"=== SPIKE gqlc-35yu.5 OUTCOME — DECISIONS THIS BEAD DEPENDS ON ===\nFull evidence in gqlc-35yu.5 (818 lines, incl. the complete agtype text\ncorpus this bead's decode helpers should be TDD'd against).\n\nPARAMETER MECHANISM (selected, with server-log proof of server-side binding):\ndirect cypher('\u003cgraph\u003e', $$...$$, $1) over the pgx extended protocol.\nage_prepare_cypher was REJECTED — it latches query text into the plan via\nconnection-global state at Parse time, which pgx statement caching schedules\nunpredictably. String interpolation of user args remains banned unconditionally;\nnote pgx SimpleProtocol (the only interpolating mode) is the one mode AGE\nrejects outright, so the unsafe path fails closed.\n\nBINDING GOTCHA — params must be bound as a Go `string`. Both []byte and\nmap[string]any FAIL. agtype travels as TEXT format in both directions, and its\nOID is DATABASE-SPECIFIC (16461 on the spike container) — do NOT hard-code it.\n\nDECODE GOTCHAS the corpus exposes, each of which a naive parser gets wrong:\n  - ::vertex / ::edge / ::path / ::numeric are VALUE-LEVEL annotations at\n    ARBITRARY DEPTH, not merely trailing suffixes.\n  - Map keys come back in jsonb order: LENGTH FIRST, then lexicographic.\n    Goldens must expect this ordering, not insertion or plain lexicographic.\n  - NaN and Infinity are valid agtype but are NOT valid JSON — a plain\n    encoding/json decode path will break on them.\n  - NULL properties are DROPPED entirely. A nullable field comes back as an\n    ABSENT KEY, never as an explicit null. This directly shapes the nullable\n    decode + error taxonomy in this bead.\n\nSESSION PRECONDITION (see gqlc-35yu.6): ag_catalog must be on the session\nsearch_path or every WHERE/operator query fails — SessionInit enforces this.\nPin: apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\nGUARDRAIL from gqlc-35yu.6 review: internal/codegen/age/types.go returns \"any\" from Temporal for every kind, because TypeMap.Temporal has no rejection channel (gqlc-9n6t). Harmless at C0 — no query methods are emitted — but .7 is where query methods first appear for AGE, so this becomes reachable HERE. Until gqlc-9n6t lands, do NOT enrol any fixture with a temporal projection for the apache-age-pgx-v5 target: it would generate a column typed 'any' that nothing can decode, and the golden would freeze that as correct. Scalar projections are unaffected.\n=== TWO .6-ROUND-2 GUARDS THIS BEAD MUST REMOVE (recorded 2026-08-01) ===\n.6 makes the AGE target CLI-UNREACHABLE on purpose: generate hard-errors on any\nquery (age.ErrUnsupportedQuery), because emitting a client that silently drops\nthe user's queries is worse than refusing. .7 is the bead that makes AGE\nreachable, so .7 owns retiring both guards. Recorded here so they are not\ndiscovered by a user instead.\n\nGUARD 1 -- init wizard picker. internal/cli/init.go builds its driver picker\nfrom config.DriverValues(). .6 filters apache-age-pgx-v5 OUT of that picker,\nbecause until queries are served the entry is a guaranteed-to-fail choice. The\nexclusion is deliberately explicit and named at the call site rather than a\nsilent slice trim, precisely so .7 finds it. DELETE THE FILTER in .7.\n\nGUARD 2 -- the query rejection itself. rejectUnservedQueries runs before\nemission in internal/codegen/age/generate.go. It does NOT simply disappear in\n.7: it NARROWS. .7 serves SCALAR params + SCALAR columns only. Everything else\n-- entity columns (.9), writes (.10), temporal (.11) -- must still be REJECTED,\nnot silently emitted. Rewrite the predicate to reject what .7 does not serve;\ndo not delete the mechanism. It is load-bearing through .11.\n\nGUARD 3 (consequence) -- the driver-axis test. .6's review (B3) established that\nregistry/vocabulary parity proved nothing about coverage: a fake fourth backend\nregistered cleanly and only three string-level assertions in internal/config\nnoticed. TestRunDriverAxis is therefore DERIVED from config.DriverValues() with\na per-driver expectation. AGE's expectation in .6 is the rejection behaviour.\n.7 must UPDATE that expectation to the scalar-read behaviour rather than\ndeleting the AGE row -- deleting it re-opens exactly the coverage hole B3 shut.\n=== GUARD 1 REFINEMENT: DELETE THE ENTRY, NOT THE MECHANISM (2026-08-01) ===\nCorrecting the \"GUARD 1 -- init wizard picker\" note above. My original ruling\nwas to filter AGE out of the picker. That was incomplete and the implementation\nfound the hole before it shipped.\n\nWHAT WENT WRONG WITH THE NAIVE FILTER: huh resolves a Select value that is\nabsent from its options to INDEX 0. So on an EDIT of a config already naming\napache-age-pgx-v5, withholding the value made the wizard silently rewrite the\ndriver to neo4j-go-v5 the moment the user accepted the default. Withholding a\nSTORED value destroys the user's data -- strictly worse than the dead menu\nentry the filter existed to remove. Caught by TestInitEditVocabularyPrefill\nfailing for a real reason.\n\nSHIPPED SHAPE: offerableDrivers(current config.Driver) withholds from the\nFRESH-CHOICE vocabulary but ALWAYS RETAINS THE CURRENT VALUE. A fresh `init`\nnever sees AGE; an edit of a config that already names it preserves it. The\nwithheld set is the named var driversWithoutQueryEmission at the call site.\n\nHOW .7 APPLIES THIS: remove apache-age-pgx-v5 from driversWithoutQueryEmission.\nDO NOT remove offerableDrivers or the retention behaviour. The list going empty\nis the correct end state for this epic; the mechanism generalises to any future\nbackend that is registered before it can serve queries, and the prefill trap it\nguards is a property of huh, not of AGE. Deleting the mechanism re-arms a bug\nthat silently rewrites user config.\n=== INHERITED DEFECT from .6 (B2, found in .6 review round 3): the 63-BYTE name cast. This bead adds a THIRD site. ===\n\n.6 binds the graph name through PostgreSQL's `name` type in two statements:\n    EnsureGraph:  ag_catalog.create_graph($1::name)\n    DropGraph:    ag_catalog.drop_graph($1::name, true)\n`name` is NAMEDATALEN-1 = 63 BYTES, and the cast TRUNCATES SILENTLY -- no\nerror, no warning, no notice. Consequences proved against the pinned image:\ntwo distinct names sharing a 63-byte prefix collapse to ONE graph; the second\nEnsureGraph returns nil having created nothing; DropGraph on the second\nCASCADE-DROPS the first's graph, its labels and its data.\n\n.6 fixes it by REJECTING len(graph) \u003e 63 (Go len, i.e. BYTES) inside\nEnsureGraph and DropGraph -- not in New, which stays I/O-free, error-free and\npanic-free, matching the neo4j constructor. See the ruling on .6.\n\nWHY IT LANDS HERE: this bead passes the bound graph name into\n    ag_catalog.cypher($1, $$ ... $$) AS (...)\nwhich is a third site where a truncated name silently addresses the WRONG\nGRAPH -- and the worst-behaved of the three, because it is on the READ path.\nA query against a 68-byte name would return another graph's rows as if they\nwere its own. EnsureGraph/DropGraph at least fail loudly once .6 lands; a\nread that silently answers from a neighbouring graph does not.\n\nWHAT TO DO HERE: do NOT add a fourth ad-hoc length check at the cypher() call\nsite. The guard belongs at ONE choke point that every graph-name-binding\nstatement passes through, and .7 is the bead where the second consumer appears\nand the choke point becomes worth having. Factor .6's check into that shared\nguard and route cypher() through it. Test the boundary in BYTES, not\ncharacters -- a 63-character multibyte name is over the limit and a\nrune-counting test will not see it (see .6's note on the two length axes).\n\n=== RE-ENROLMENT OWED HERE (from .6 round 4, 2026-08-01) ===\n.6 removed apache-age-pgx-v5 from the targets of\ntest/data/codegen/invalid/identifier_collision_graph_lifecycle.\n\nWhy it had to go: .6 reordered generate() so rejectUnservedQueries runs BEFORE\ncodegen.Prepare, so AGE returns ErrUnsupportedQuery before Prepare can reach\nErrIdentifierCollision, and a manifest carries ONE expectedError for all its\ntargets. At C0 the enrolment was never exercising the path it names -- AGE\nrejects any query outright, and a query-free batch has no names to collide.\n\n.7 IS WHERE IT BECOMES REACHABLE: scalar queries are servable here, so a query\nnamed EnsureGraph or DropGraph survives rejectUnservedQueries and reaches\nPrepare's Phase A for real. PUT apache-age-pgx-v5 BACK in that manifest's\ntargets and confirm it goes red for the right reason -- ErrIdentifierCollision,\nnot ErrUnsupportedQuery.\n\nHarness constraint to know before starting: sentinelByName in\ninternal/codegen/conformance/conformance_test.go is built only from\ncodegenSentinels and queryfile.AllSentinels(). Backend-package sentinels like\nage.ErrUnsupportedQuery are NOT registrable as a manifest expectedError without\na harness change. If .7's narrowed rejection needs fixture coverage of its own,\nthat harness change is part of this bead.\n\nThe reserved set itself is uniform across backends and stays fenced throughout\nby TestReservedIdentifiersAreUniformAcrossBackends (internal/codegen/prepare_test.go:342).\n=== HAZARD THAT DIES WITH GUARD 1 (from .6 round-4 review, 2026-08-01) ===\ninternal/cli/init.go:206-209 does:\n    offerable := offerableDrivers(noCurrentDriver)\n    ... offerable[0]\nThat index is unguarded. It is safe today only because driversWithoutQueryEmission\nis a strict subset of config.DriverValues(), so the filter can never empty the\nslice. A future stage that withheld EVERY driver would panic in the init wizard.\n\nI declined to add a fence in .6 because .7 DELETES the filter (guard 1 in the\nnote above), which removes offerableDrivers and the index with it. A fence for\ncode with a scheduled deletion date is churn.\n\nSO: when you delete guard 1, confirm the deletion actually takes the unguarded\nindex with it. If any part of offerableDrivers survives — e.g. the picker keeps\na derived slice for some other reason — then the hazard survives too and needs\neither a non-empty invariant test or a guard at that point. Do not leave the\nfilter half-removed.\n=== CORRECTION to the inherited-B2 note above (manager, verified live 2026-08-01) ===\n\nThe mechanism I recorded above is WRONG. The note says this bead \"passes the\nbound graph name into ag_catalog.cypher($1, ...)\". It cannot. Carmack found\nthis while implementing; I reproduced it against the pinned image\n(apache/age@sha256:4241e2d8bb86...feb70eb) before accepting it.\n\nAGE resolves cypher()'s FIRST argument at parse analysis, and accepts a\nliteral constant there and nothing else. All of these fail identically with\nERROR: a name constant is expected:\n    PREPARE q(name) AS SELECT * FROM ag_catalog.cypher($1, $$ ... $$) ...\n    PREPARE q(text) AS SELECT * FROM ag_catalog.cypher($1, $$ ... $$) ...\n    PREPARE q      AS SELECT * FROM ag_catalog.cypher($1, $$ ... $$) ...\n    SELECT * FROM ag_catalog.cypher(repeat(E'a',1), $$ ... $$) ...\nThe last one matters: it is not merely that BIND parameters are refused, it is\nthat no runtime-evaluated expression is accepted at all. Splicing the name into\nthe statement text is the ONLY mechanism available, not a chosen one.\n\nWHAT SURVIVES THE CORRECTION: the conclusion, entirely. cypher() IS a third\nname-binding site, it IS the worst-behaved of the three because it is on the\nread path, and the guard DOES belong at one shared choke point. Confirmed live:\n  - create_graph(repeat('a',70)) SUCCEEDS and stores 63 octets. NOTICE only,\n    no error, no warning.\n  - a'70 and a'63||'XXXXXXX' are distinct names that collapse to ONE graph --\n    the second create_graph reports \"already exists\". This is exactly the\n    \"could address another graph\" hazard boundGraph's error message names.\n\nCONSEQUENCE FOR THE GUARD'S PLACEMENT: because the name is composed into text\nrather than bound, the length check must run BEFORE composition, not at the\ncall site. It sits at boundGraph() in render_db.go, the only reader of q.graph;\nEnsureGraph, DropGraph and cypherStmt all source the name from it.\n\nON THE INTERPOLATION BAN: I told the implementer that string interpolation of\nuser args is banned unconditionally. That ban is intact. Query ARGUMENTS still\nbind -- they travel as a third ag_catalog.agtype parameter, $1, verified:\n  PREPARE qargs(agtype) AS SELECT * FROM ag_catalog.cypher(E'g',\n    $$ MATCH (n) WHERE n.k = $k RETURN n.k $$, $1) AS t(k agtype);\n  EXECUTE qargs('{\"k\": 42}');  -- returns 42\nOnly the graph name is spliced, escaped as an E-string (backslash then quote).\nI verified the escape contains a break-out attempt: a name payload of\n    x', $q$ $q$) AS (n agtype); DROP TABLE victim; --\nemitted through that escaping parsed as ONE string literal and the DROP never\nran (victim survived). Separately, AGE independently rejects graph names\ncontaining ' or \\ with \"graph name is invalid\" at create_graph -- so the escape\nis defence in depth at the SQL layer, not the only thing standing there.\n=== REVIEW ROUND 1 (Linus) — FAIL. Manager rulings, 2026-08-01 ===\n\nWHAT HELD, so a later round does not re-litigate it. The reviewer mutated all\nfour agtype decode helpers and the ErrNoRows path, ran conformance with\n-update, and every fence stayed red -- the compiled-corpus harness extracts\nmodels.go into a throwaway module and runs it, so -update cannot reach it.\nMutating len(q.graph) to len([]rune(q.graph)) went red on both the AST fence\nand the multibyte corpus rows. He also established live that an absent property\nand a literal RETURN null both come back as SQL NULL, not the 4-byte text\n\"null\", so writeScan's raw == nil discriminator rests on the right model.\n\nTEMPORAL: no finding, and the earlier worry is answered. typeMap.Temporal is\nUNREACHABLE from generate() -- unservedColumn (errors.go:88) rejects\nResolvedTemporal before Prepare, and scalarWidth rejects the five temporal\nwidths via typeMap.Property. Both have positive tests. Not enrolling a temporal\nfixture was correct and the absence is fenced, not merely observed. This\nsupersedes the concern that fed gqlc-9n6t for the AGE side of that bead.\n\n--- B1, BLOCKING, verified by me before relaying ---\ndollarTag (render_queries.go:46) guards with !strings.Contains(text, tag), but\nwhat reaches the parser is tag+text+tag, so the tag can STRADDLE the text/tag\nboundary while being absent from text alone. Text ending in $gqlc composes to\n...$gqlc$gqlc$ and PostgreSQL closes at the straddle.\n\nReproduced by me against the pinned image:\n    SELECT $gqlc$MATCH (p:Person) RETURN p.name\n    // trailing $gqlc$gqlc$ AS should_fail;\n    ERROR:  syntax error at or near \"AS\"\nand strpos(E'...// trailing $gqlc', '$gqlc$') = 0 confirms the Go-side guard\npasses the tag through. Control case with no $gqlc closes correctly.\n\nGenerated code that cannot run, emitted with no diagnostic, and dollarTag's own\ndoc comment asserts exactly the property it lacks. Reachable today via a\ntrailing comment; ordinary at .10, where SET p.name = $gqlc ends a write query\nin a property-typed parameter. Ruled: strings.Index(text+tag, tag) == len(text)\n-- subsumes the old check rather than sitting beside it. The i \u003e 0 escalation\nbranch has never executed in the suite and must be covered.\n\n--- N1, PROMOTED to required ---\nThe E-string escaping in cypherStmt (render_db.go:114) is fenced ONLY by a\ngolden: deleting the ReplaceAll pair and running -update leaves test, lint and\nfence all green. Filed non-blocking because the behaviour is correct. I\npromoted it because I verified the escaping is load-bearing -- an injection\npayload emitted through it arrives as one literal and the DROP does not run --\nand a security-relevant splice fenced by an artefact -update rewrites is the\nexact laundering class this suite is otherwise built to defeat. Fix is one\nline: add cypherStmt to the declarations(...) call at corpus_test.go:49, which\nalready compiles boundGraph. B1's tag choice goes in the same fence.\n\n--- N2, PROMOTED to required. The .6 conditional fired. ---\n.6's note said: if any part of offerableDrivers survives guard 1's deletion,\nthe unguarded offerable[0] at internal/cli/init.go:206 survives too and needs a\nnon-empty invariant test or a guard. driversWithoutQueryEmission was EMPTIED\nrather than removed -- correct, the retention behaviour stays useful for later\nstages -- so the index survives and the conditional fires. Existing coverage\ndoes not close it: init_test.go:825 passes the CURRENT driver, which the\nd != current condition always retains, so that call can never return empty.\nLine 206 passes noCurrentDriver, which can. One require.NotEmpty on\nofferableDrivers(noCurrentDriver) closes it.\n\n--- N3, DECLINED ---\nNarrow-width overflow (int8(value0) for 300, float32 for 1e300) is silent, but\nneo4j/render_queries.go:493-499 established carrier-then-convert as the\nepic-wide convention, matching sqlc. Raised only so it is not mistaken for an\nAGE-specific gap. Not this bead's to change; if revisited it is an epic-wide\ndecision. No bead filed.\n=== REVIEW ROUND 2 (fresh Linus) — PASS, verified by the manager. 2026-08-01 ===\n\nAll three required fixes landed in ba6cd23..4db6bdb (4fb05db B1, c6fdfc7 N1,\n4db6bdb N2). PR #657.\n\nWHAT THE REVIEWER ESTABLISHED that is worth keeping, because it is reasoning\nabout the SERVER, not about our code, and the next backend bead will want it:\n\nB1's predicate is correct for the right reason, not just for the reported\nexample. PostgreSQL's scan.l consumes the opening tag as one token, so the\ndollar-quote scan begins at index 0 of text+tag; when a different dollar\ndelimiter matches inside the body the lexer does yyless(1), advancing exactly\none byte, so it can never skip a candidate start. The literal therefore closes\nat the FIRST occurrence at or after that point, which is exactly what\nstrings.Index(text+tag, tag) == len(text) names. Occurrences straddling the\nOPENING tag are correctly out of scope because that region is already tokenised.\nTermination: rejection needs an occurrence starting at some j \u003c len(text), and\ntwo candidates cannot both match at one j (they diverge at byte 5), so the loop\nends within len(text)+1 turns.\n\nHe also checked whether the straddle class exists ELSEWHERE rather than assuming\ndollarTag was the only site, and found the one other raw composition --\nrender_queries.go:89 emitting SourceText into a Go raw string, where the grammar\ndoes admit backtick-escaped identifiers. Already closed upstream:\nprepare.go:571 rejects any query whose source text holds a backtick with\nErrOutOfC6Scope, confirmed live. Every other user-controlled value reaching\nemitted source goes through %q or is a validated Go identifier. Only the graph\nname is spliced raw, which is N1's subject.\n\nMANAGER'S INDEPENDENT VERIFICATION, not taken from the report:\n- All four gates re-run by me. test/lint/fence green; live at 18.068s with\n  -count=1 after `just test-codegen-live` first handed me a CACHED ok -- the\n  trap fired on me, noting it so the next bead does not trust a bare recipe run.\n- I re-ran the N1 laundering attack myself, since I was the one who promoted it\n  to blocking. Replaced the escaping with a no-op ReplaceAll (keeping `strings`\n  used so the failure is behavioural, not a compile error). The corpus fence\n  went red naming my injection payload; I then ran conformance -count=1 -update,\n  which rewrote all 11 AGE db.go goldens and reported ok -- i.e. the laundering\n  reproduced -- and the fence STAYED RED. That is the property the promotion\n  demanded. Worktree restored to 4db6bdb clean afterwards.\n- I verified the 11-golden reflow from c6fdfc7 is comment-only: every changed\n  line in those files is a comment or blank, no code churn.\n\nFOLLOW-UP FILED: gqlc-7f9a (P3). Go raw string literals discard \\r, so the\nemitted query constant can differ from the bytes dollarTag was computed on --\nthe same invariant as B1 failing for a different reason. Epic-wide (age and\nneo4j emit byte-identically) and reachable only via a bare \\r inside the token,\nso not held against this bead. There is no CRLF normalisation in\ninternal/queryfile; I checked.\n\nDECLINED AGAIN at round 2: the reviewer re-raised narrow-width overflow as\nepic-wide context only, and noted graphStub's hardcoded import list in\ncorpus_test.go:29 is a future confusing-compile-error risk rather than a gap.\nNeither blocks; neither gets a bead.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:11:56Z","created_by":"areqag","updated_at":"2026-08-01T14:32:29Z","started_at":"2026-08-01T12:52:40Z","closed_at":"2026-08-01T14:32:29Z","close_reason":"Merged via PR #657 (squash 1335fae). Linus round-2 PASS, verified independently by the manager: all four gates re-run (live 18.068s with -count=1 after a cached ok), the N1 laundering attack re-run by hand (fence stayed red through -update while conformance went green), and the 11-golden reflow confirmed comment-only.","external_ref":"https://github.com/areqag/gqlc/issues/626","dependencies":[{"issue_id":"gqlc-35yu.7","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:11:56Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.7","depends_on_id":"gqlc-35yu.5","type":"blocks","created_at":"2026-07-31T19:12:49Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.7","depends_on_id":"gqlc-35yu.6","type":"blocks","created_at":"2026-07-31T19:12:50Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.6","title":"codegen/age: C0 — compiling empty package, graph lifecycle helpers, registry entry","description":"First AGE emission stage, mirroring the neo4j C0 posture (compiling-but-empty package). internal/codegen/age emits: db.go — DBTX interface over pgx v5 (Query/Exec, satisfied by pgxpool.Pool / pgx.Conn / pgx.Tx), New(db DBTX, graph string), WithTx(pgx.Tx); querier.go — the Querier interfaces; models.go — empty; graph.go — EnsureGraph and DropGraph as methods on the Queries handle (they join the reserved-identifier set in the cross-query collision sweep and are EXCLUDED from the Querier interfaces — graph DDL does not belong in the mock contract), plus package-level SessionInit matching the pgxpool AfterConnect shape (func(ctx, *pgx.Conn) error) running LOAD age.\n\nEvery emitted SQL string fully qualifies ag_catalog.cypher / ag_catalog.agtype — search_path must never be a session dependency (pool sessions come and go). Import paths are major-only (github.com/jackc/pgx/v5/...): the consumer go.mod picks the minor, identical to the neo4j v5/v6 posture.\n\nWiring: registry Entry with key apache-age-pgx-v5; config const DriverApacheAgePgxV5 + enum + parity test; nested test module go.mod gains pgx. AGE golden subtrees for the fixtures C0 can serve (targets lists updated). TDD: write the C0 golden fixtures red first, then emit.\n\nSpike findings (parent epic child spike bead) inform graph.go details — check its notes before implementing EnsureGraph/SessionInit.","acceptance_criteria":"gqlc generate with gen.go.driver apache-age-pgx-v5 emits the C0 package; TestGoldenBuild compiles AGE goldens; fixture suite green with the AGE target enrolled; parity test green; a query annotated with name EnsureGraph or DropGraph is a generate-time collision error with a test proving it; generated files contain zero unqualified cypher( or agtype references.","notes":"=== SPIKE gqlc-35yu.5 OUTCOME — AMENDS THIS BEAD'S SessionInit CONTRACT ===\nThe epic's \"search_path is never a session dependency\" claim was empirically\nDISPROVED and the decision reversed (owner-approved 2026-07-31; full evidence\nin gqlc-35yu.5, summary note on the epic).\n\nAGE's Cypher-\u003eSQL transform emits BARE OPERATORS (=, \u003e, +, @\u003e) rather than the\nOPERATOR(ag_catalog.=) form, so Postgres resolves them via search_path. Fully\nqualifying ag_catalog.cypher / ag_catalog.agtype is necessary but NOT\nsufficient — with ag_catalog off the path, WHERE / property-map MATCH / any\narithmetic all fail while RETURN 1 and bare MATCH still succeed.\n\nSessionInit must therefore do THREE things, in order:\n  1. LOAD 'age'\n  2. SET search_path to include ag_catalog (ahead of the caller's own path)\n  3. CANARY: execute an operator-exercising statement (RETURN 1 = 1) and\n     return an error if it fails\nThe canary is the point of the design, not decoration: SessionInit is the\npgxpool AfterConnect hook, so an error there keeps the connection OUT of the\npool and the failure surfaces at connection establishment rather than at first\nWHERE. AGE fails PARTIALLY when misconfigured — simple queries keep working —\nso without the canary a broken deployment looks healthy until first real use.\n\nNew(db DBTX, graph string) is UNCHANGED: no ctx, no error return. Verification\nlives in SessionInit precisely so the constructor stays I/O-free.\n\nTest the failure path, not just the happy path: a connection whose search_path\nlacks ag_catalog must be REJECTED by SessionInit, and that rejection must be\nobservable as a pool-acquisition failure.\n\nAlso relevant here: AGE 1.7.0 REJECTS multi-label vertices — worth knowing when\nEnsureGraph/DropGraph fixtures are built.\nPin: apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\n=== DECISION 2026-08-01: C0 must REJECT batches with queries, not silently drop them ===\n(Manager ruling on Linus round-1 non-blocking finding #11: 'apache-age-pgx-v5\nsilently discards queries at C0 — should the target be marked experimental or\nhidden?')\n\nRULING: no experimental/hidden flag. generate() returns an error when the batch\ncarries queries.\n\nWhy not an experimental flag: it is a new user-visible concept on the registry\nand the CLI that exists only to survive two beads, and it does not actually fix\nthe problem — an 'experimental' label still lets 'gqlc generate' exit 0 having\nthrown the user's queries away. Silently discarding input is the failure mode;\na label is a disclaimer, not a guard.\n\nWhy an error instead: it is the smallest change that makes the emitted contract\nhonest, and it is NOT throwaway scaffolding. The backend acquires capability in\nstages across this epic — .7 scalar reads, .9 entity columns, .10 writes — so a\n'this backend cannot serve that yet' rejection path is load-bearing for the\nwhole sequence. .7 narrows the condition rather than deleting the machinery.\nIt also matches AGENTS.md 'make illegal states unrepresentable': a Querier that\nprovably omits queries the user wrote is an illegal state we currently emit.\n\nBlast radius is nil: skeleton is the only AGE-enrolled fixture and its manifest\nhas queryFiles: [], so the fixture suite is unaffected.\n\nSCOPE: folded into .6 round 2 rather than filed as a follow-up — a follow-up\nbead would land after .7 has already removed the need for it.\n=== DECISION 2026-08-01: SessionInit step 1 (LOAD 'age') IS REMOVED. This REVERSES part of the gqlc-35yu.5 spike outcome recorded above. ===\nThe spike note above says SessionInit must do three things in order:\n1. LOAD 'age'  2. SET search_path  3. CANARY.\nSTEP 1 IS WRONG AND HARMFUL. Steps 2 and 3 stand unchanged.\n\nFound by adversarial review of .6; I reproduced it myself against the pinned\nimage apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\nwith a least-privilege role (CREATE ROLE appuser LOGIN):\n\n  postgres (superuser):  LOAD 'age'  -\u003e LOAD                                rc=0\n  appuser  (non-super):  LOAD 'age'  -\u003e ERROR: access to library \"age\" is\n                                        not allowed                          rc=1\n\nLOAD is superuser-only unless the library sits under $libdir/plugins, and\nAGE installs age.so to $libdir. Review separately confirmed that putting age\nin shared_preload_libraries does NOT relax the check — it is unconditional.\n\nWHY THIS IS FATAL, NOT COSMETIC: SessionInit is wired as pgxpool.Config\nAfterConnect (pinned by test/data/codegen/contract_age.go). An error there\nDISCARDS the connection. So every acquisition retries and every retry fails\nidentically: the pool never yields a connection. Under the deployment posture\nany competent operator uses — an application role that is not superuser — the\ngenerated code cannot work at all.\n\nWHY IT IS ALSO UNNECESSARY, WHICH IS WHAT MAKES IT INDEFENSIBLE: I ran a fresh\nappuser session with NO LOAD, only the search_path statement, and got both the\ncanary and real Cypher:\n  set_config('search_path', concat_ws(', ','ag_catalog',\n             nullif(current_setting('search_path'),'')), false)  -\u003e ok\n  SELECT '1'::ag_catalog.agtype + '1'::ag_catalog.agtype\n         = '2'::ag_catalog.agtype                                -\u003e t\n  SELECT * FROM ag_catalog.cypher('probe_g',\n         $$ MATCH (p:Person) RETURN p.name $$)\n         AS (n ag_catalog.agtype)                                -\u003e \"ann\"\nPostgreSQL loads the library implicitly on first call into it, and that runs\n_PG_init, so the hooks are installed.\n\nTHE CANARY IS THE LOADER. This is the load-bearing reason removal is safe and\nmust be stated in the emitted comment: the canary's + is an AGE C function, so\nrunning it forces the implicit load before any user query is ever parsed. Any\nhook-ordering worry about a session whose first AGE touch is a user cypher()\ncall is answered by SessionInit having already touched AGE itself.\n\nDO NOT RE-ADD LOAD, and do not add it back defensively wrapped in an\nignore-permission-error fallback. The canary already proves the session is\nusable; a fallback would be error handling for a condition that cannot\nsurvive the canary. AGENTS.md forbids exactly that.\n\nWHY .8 WOULD NOT HAVE CAUGHT THIS: the live harness connects as postgres. The\nsingle configuration under which the LOAD works is the single configuration\nthe tests exercise. gqlc-35yu.8 should add a NON-SUPERUSER arm; recorded there.\n=== DECISION 2026-08-01: NO CLIENT-SIDE GRAPH-NAME VALIDATION. ===\nReverses my own instruction. Review nit N8 said New() does not validate its\ngraph argument, so a bad name fails late and obscurely at EnsureGraph. I ruled\n\"make illegal states unrepresentable, validate at construction\". That produced\na validGraphName rejecting RuneCountInString(name) \u003c 3 or a leading digit, with\nNew PANICKING on failure. I then probed the pinned image and the rule is wrong\nin BOTH directions.\n\nFALSE REJECTIONS -- AGE accepts these, validGraphName rejects them, New panics:\n  '中中'  6 bytes, 2 runes   ACCEPTED by AGE\n  'ér'    3 bytes, 2 runes   ACCEPTED by AGE\n  'aé'    3 bytes, 2 runes   ACCEPTED by AGE\n  'éé'    4 bytes, 2 runes   ACCEPTED by AGE\nversus '中' (3 bytes, 1 rune) and 'ab' (2 bytes, 2 runes), both rejected by AGE.\nSo the boundary is not rune count. We shipped a PANIC ON VALID INPUT to improve\nan error message.\n\nWHY THE ORIGINAL EVIDENCE MISSED IT: the spike sample was '中中中' accepted /\n'中' rejected. That is equally consistent with a rune rule and a byte rule.\n'中中' is the discriminating case and it was never tested.\n\nFALSE ACCEPTANCES -- all pass validGraphName, all rejected by AGE:\n  'a b'  'ab '  'a\"b'  'a''b'  'ab;'  'x$y'  'a!b'  'a#b'  'a@b'  'a%b'\n  '-ab'  '.ab'  'ab-'  'a--'  '   '\nSo the doc claim \"reports whether AGE's create_graph will accept name\" is false.\n\nTHE RULE IS NOT LEARNABLE FROM OUTSIDE. Across ~44 samples: 'a-b' accepted but\n'ab-' and 'a--' rejected; 'a.b' accepted but '.ab' rejected; ' ab' accepted but\n'ab ' and 'a b' rejected. No clean characterisation emerged. A rule we cannot\nstate is a rule we cannot maintain, and it would drift the moment AGE changes.\n\nRULING: no client-side name validation and no panic. New returns the handle.\nN8's real complaint -- late, obscure failure -- is answered where the authority\nlives: EnsureGraph wraps AGE's own \"graph name is invalid\" with the graph name\nas context. AGE is the only thing entitled to decide what it accepts.\n\nDO NOT re-add this, including as a \"narrower, certainly-invalid-only\" check: a\nnecessary-condition check still has to be right about certainty, still drifts,\nand still panics on caller-supplied input. \"Make illegal states unrepresentable\"\napplies when legality is characterisable by us; here it is a C function in\nanother project.\n=== CORRECTION 2026-08-01: two errors in MY OWN notes above. Both rulings stand; the evidence recorded for them was defective. ===\n\nCORRECTION 1 -- the LOAD-removal evidence above is CONTAMINATED. The conclusion\nholds; the transcript I recorded does not prove it.\n\nThe block above headed \"WHY IT IS ALSO UNNECESSARY\" shows a no-LOAD session\nrunning the canary and a real cypher() call. I ran that against the pinned\nimage with its DEFAULT CMD, which is:\n    postgres -c shared_preload_libraries=age\nSo the library was already loaded into every backend before my session opened.\nThat transcript cannot distinguish \"the canary loaded AGE\" from \"preload loaded\nAGE, and the canary rode along\". The claim THE CANARY IS THE LOADER -- the\nload-bearing reason removal is safe -- was not tested by it.\n\nClean re-run (review round 3), same image, shared_preload_libraries emptied:\n    virgin session, no search_path, no canary:\n        SELECT * FROM ag_catalog.cypher('nope', $$ MATCH (n) RETURN n $$)\n            -\u003e ERROR: unhandled cypher(cstring) function call\n           (the AGE hooks are absent: _PG_init has not run)\n    same session shape, search_path set + canary run FIRST:\n        same cypher() call\n            -\u003e ERROR: graph \"nope\" does not exist\n           (AGE's own graph-resolution error: the hooks ARE installed)\nThe only difference between the two is the canary. So the canary does force\n_PG_init, without preload and without LOAD. THE RULING IS UNCHANGED and DO NOT\nRE-ADD LOAD still stands -- now on evidence that actually isolates the variable.\n\nMethod note for this epic: the pinned image preloads age by default. Any probe\nthat asks \"does X load AGE?\" must empty shared_preload_libraries first, or it\nanswers a question nobody asked. This applies to .8's live harness too.\n\nCORRECTION 2 -- \"THE RULE IS NOT LEARNABLE FROM OUTSIDE\" is FALSE as written,\non the length axis.\n\nI wrote \"Across ~44 samples ... No clean characterisation emerged.\" A 50-sample\nprobe does characterise the length axis cleanly:\n\n    AGE accepts a name iff   characters \u003e= 2  AND  bytes \u003e= 3\n\nThat single rule explains every pair I cited as evidence of unlearnability:\n    '中中'  2 chars, 6 bytes  -\u003e accept\n    'ér'    2 chars, 3 bytes  -\u003e accept\n    'aé'    2 chars, 3 bytes  -\u003e accept\n    'éé'    2 chars, 4 bytes  -\u003e accept\n    '中'    1 char,  3 bytes  -\u003e reject (fails chars \u003e= 2)\n    'ab'    2 chars, 2 bytes  -\u003e reject (fails bytes \u003e= 3)\nThe rune-count rule I originally shipped was wrong not because length is\nunknowable but because length has TWO axes and I had only found one.\n\nWHAT THIS DOES NOT CHANGE: the ruling. NO CLIENT-SIDE NAME VALIDATION, NO PANIC.\nThe ruling never depended on unlearnability -- it rests on the arguments that\nsurvive this correction:\n  - the CHARACTER-CLASS axis remains uncharacterised ('a-b' accepted but 'ab-'\n    and 'a--' rejected; 'a.b' accepted but '.ab' rejected; ' ab' accepted but\n    'ab ' rejected). Length is one axis of a rule with at least two.\n  - it is version drift, not ignorance, that makes the check unmaintainable: the\n    rule is a C function in another project with no compatibility promise to us.\n  - a necessary-condition check still panics on caller-supplied input.\nAGE remains the only thing entitled to decide what it accepts.\n\nWHAT MUST CHANGE: the overstatement is repeated in the code, at\ninternal/codegen/age/age_test.go:331-337. Correct the wording there to claim\nwhat is true -- the character-class axis is uncharacterised and the rule is\nAGE's to version -- not \"no characterisation emerged\". A false justification in\na test comment is worse than none: the next reader probes for ten minutes,\nfinds the length rule, and concludes the ruling was wrong.\n\n=== DECISION 2026-08-01: N2 residual gap (table-shrink) is ACCEPTED here and filed out. The require.Len removal STANDS. ===\n\nRound-4 implementation flagged that removing the tautological\nrequire.Len(t, representable, 16) / (unrepresentable, 14) / (tests, 6)\nassertions from internal/codegen/age/types_test.go loses a SHRINK tripwire:\ndeleting a row from one of those tables now fails nothing.\n\nThe removal stands. The assertion it replaced was not a fence. A literal\ncount asserted against a literal table sitting directly above it fails only\nwhen someone edits the table AND forgets to edit the number -- and when it\ndoes fire it says \"expected 16, got 15\", naming nothing about what was lost.\nThat is a reminder addressed to the editor, not a guard addressed to the code.\n\nTHE GAP IS REAL, and is not uniform across the three tables:\n  Property  -- graph.PropertyType growth IS caught: the switch is exhaustive-\n               linted. Table shrink is not caught.\n  Scalar    -- resolver.Scalar is a closed enum, switch is exhaustive-linted.\n               Growth caught, shrink not.\n  Temporal  -- NEITHER. `func (typeMap) Temporal(resolver.Temporal) string`\n               ignores its argument and returns \"any\" unconditionally. There\n               is no switch, so no linter sees it. resolver can gain a\n               Temporal kind and nothing in this package notices.\n\nWHY IT IS NOT FIXED HERE:\n1. The correct fence is a DOMAIN SWEEP -- assert the table's coverage against\n   a canonical enumeration of the domain, the way TestRunDriverAxis sweeps\n   config.DriverValues(). That bites on growth and shrink both, and is not\n   tautological because the expected side is derived, not counted by hand.\n2. The enumerators DO NOT EXIST. There is no graph.PropertyTypeValues(), no\n   resolver.TemporalValues(), no resolver.ScalarValues(); config.DriverValues()\n   exists only because the YAML loader needed it. Adding them touches\n   internal/graph and internal/resolver, which every backend reads. That is a\n   cross-cutting change and does not belong in an AGE C0 bead.\n3. internal/codegen/neo4j/types_test.go carries the IDENTICAL pattern at five\n   sites (:45, :60, :96, :118, :155). Fixing one backend and not the other\n   leaves the same hole plus an inconsistency.\n\nSEVERITY, stated honestly: low, because deleting a table row is a deliberate\nedit visible in a diff under review. The drift that fences exist to catch is\nthe SILENT kind -- a domain growing elsewhere while this table stands still --\nand that is already covered for Property and Scalar.\n\nTemporal is the exception and the one genuinely silent hole. It is NOT filed\nseparately: Temporal returning \"any\" for every kind with no rejection channel\nis already gqlc-9n6t, and the enumerator fence and the rejection channel are\nthe same edit. Noted on gqlc-9n6t rather than duplicated.\n\nFOLLOW-UP: gqlc-fb4a -- domain-sweep fences for the type-map tables in BOTH\nbackends.\n\n=== DECISION 2026-08-01: apache-age-pgx-v5 leaving the identifier_collision_graph_lifecycle fixture is CORRECT, not a coverage regression. ===\n\nN3 reordered generate() to run rejectUnservedQueries BEFORE codegen.Prepare.\nConsequence: AGE returns ErrUnsupportedQuery before Prepare can reach\nErrIdentifierCollision, and a manifest carries ONE expectedError for all its\ntargets, so AGE had to leave that fixture's target list.\n\nThis is not coverage lost. It is coverage that was never reachable:\n  - the fixture's entire content is queries;\n  - at C0 AGE rejects ANY query outright;\n  - a query-free batch has no query names, so nothing to collide.\nThere is no input that reaches collision detection through AGE at C0. The\nfixture was green for AGE by arriving at a different error, not by exercising\nthe path it names.\n\nKeeping AGE enrolled would have meant teaching the manifest per-target\nexpectedError -- a harness feature added to express a state that cannot occur.\n\nTHE ACCEPTANCE CRITERION IS STILL MET, elsewhere and better. The reserved set\nis UNIFORM ACROSS BACKENDS (internal/codegen/prepare.go:29, deliberately so per\nD2: a name free on one backend and taken on another is the renaming scheme D2\nrefused). It is fenced by TestReservedIdentifiersAreUniformAcrossBackends\n(prepare_test.go:342) which uses require.ElementsMatch -- biting on growth AND\nshrink -- and then runs a subtest per name asserting ErrIdentifierCollision,\nEnsureGraph and DropGraph among them. End-to-end coverage remains on the two\nneo4j arms.\n\nAND THE ORDERING ITSELF IS NOW FENCED: TestUnservedQueriesOutrankUnrepresentable\nWidths, verified to fail when the two blocks are swapped back. Nothing guarded\nthat order before this bead.\n\nRE-ENROLMENT IS OWED AT .7, where scalar queries become servable and a query\nnamed EnsureGraph reaches Prepare for real. Recorded on gqlc-35yu.7.\n\nHarness note for whoever does it: sentinelByName in conformance_test.go is\nbuilt only from codegenSentinels and queryfile.AllSentinels(), so backend-\npackage sentinels like age.ErrUnsupportedQuery are not registrable as a\nmanifest expectedError without a harness change.\n\n=== ROUND-4 REVIEW: PASS (2026-08-01). Merge authorised. ===\n\nRound 4 (7 commits, 3ab1cea..cc2cc9a) cleared adversarial review. Recording the\nevidence, because a PASS asserted without it is worth nothing and the three\nblockers were each found by disproving a claim someone had already believed.\n\nB1 -- the abort fence is real, verified by MUTATION, not by reading. Each of\nSessionInit's three exits was turned into `return nil` one at a time:\n    M1 search_path exec abort      FAIL before -update, FAIL after\n    M2 canary Scan err abort       FAIL before -update, FAIL after\n    M3 !ok arm                     FAIL before -update, FAIL after\nAll three fail on the real assertion (age_test.go:329, \"SessionInit must abort\non this failure, not fall through it\"), NOT on an incidental unused-import\ncompile error -- which was how round 3's M3 was caught, and the reason the old\nfence was theatre. The laundering path was exercised explicitly: running\n`go test ./internal/codegen/conformance/ -update` between the two columns DID\nrewrite the golden (confirmed by git status) and the age test kept failing.\nIt cannot be quieted that way because s.files comes from Generate in-process,\nnot from the golden tree. Under M3 the -update run itself also failed.\n\nB2 -- the guard holds and the boundary is exactly right, verified LIVE:\n    length(repeat('a',70)::name)                 -\u003e 63\n    repeat('a',70)::name = repeat('a',63)::name  -\u003e t\n    create_graph((repeat('a',63)||'X')::name)    -\u003e created\n    create_graph((repeat('a',63)||'Y')::name)    -\u003e ERROR: graph already exists\n    ag_graph                                     -\u003e ONE row, 63 octets\nSo the aliasing is real and EnsureGraph's WHERE NOT EXISTS turns that server\nerror into a SILENT no-op -- exactly the failure the emitted comment describes.\n`\u003e 63` is not off by one: PostgreSQL clips only at len \u003e= NAMEDATALEN (64), so\na 63-byte name passes through untouched. Byte semantics are PINNED, not\nincidental: mutating the emission to utf8.RuneCountInString fails\nTestLifecycleRejectsANameTheCastWouldShorten. Both helpers are guarded (the\ntest pins the literal guard block for each), nothing was added to New, and\nthere is no panic anywhere in the emitted package.\n\nThe canary's design was re-confirmed live, including why the rejected\nalternative is wrong: with ag_catalog off the path\n    '1'::agtype + '1'::agtype     -\u003e ERROR: operator does not exist\n    'true'::agtype = 'true'::agtype -\u003e t\nAn equality-only canary FALSE-PASSES a misconfigured connection. The arithmetic\nprobe is load-bearing.\n\nB3 -- server_reset_query and pgbouncer appear nowhere in the tree.\n\nN4's corrected length characterisation reproduces exactly against 1.7.0:\n'ab' invalid, 'ér' created, '中' invalid, '中中' created, 'é' invalid,\n'abc' created. Character class remains orthogonal and uncharacterised\n('a-b' created, 'ab-' invalid), which is what the corrected comment now says.\n\nThe N3 reordering is itself fenced: reverting generate.go to Prepare-first\nmakes TestUnservedQueriesOutrankUnrepresentableWidths fail, reporting the\nproperty-width error instead of the unsupported-query one. Nothing guarded that\norder before this bead.\n\nGATES, from a pristine tree: just test PASS (shuffled, all packages);\njust lint PASS 0 issues; just test-codegen-fence PASS 0 issues;\njust test-codegen-live PASS -- testcache cleared, -count=1, ok 18.361s / 18.8s\nwall, a real container rather than a cached ok.\n\nCOMMENT AUDIT: all four round-3 violations gone, no new ones introduced.\n\nONE NON-BLOCKING FINDING WAS FIXED BEFORE MERGE ANYWAY -- see the next note.\n\nFive other non-blocking findings were ruled no-change and are recorded where\nthey land: AGE now appears in zero invalid fixtures (re-enrolment recorded on\n.7); the unguarded offerable[0] index in init.go (dies with guard 1, recorded\non .7); maxGraphNameBytes assuming a default NAMEDATALEN (the emitted comment\nalready frames it as a compile-time property of the server's type);\ncheckGraphName's completeness expiring at C1 (the shared choke point is\nrecorded on .7).\n\n=== N1 FIXED BEFORE MERGE despite being non-blocking (2026-08-01) ===\nRound 4's review PASSed with one non-blocking wording finding against the\nparagraph B3 had just rewritten. I held the merge for it anyway.\n\nThe sentence was: 'Session state set at connect time survives only under\nsession pooling'. That is FALSE for the commonest deployment there is --\npgxpool straight to PostgreSQL, no pooler, no pooling mode at all, and the\nstate survives perfectly. The clause after the dash repaired it, but the\ndeclarative sentence is what gets read and quoted, so the majority of readers\nwere being told their configuration is broken.\n\nWHY IT DID NOT GET DEFERRED: it is the same class of defect as B3 itself -- a\nfalse factual claim, in the same paragraph, in a comment that ships to every\nuser of the generated code. Shipping it out of the round that existed to fix\nthat paragraph is not a tradeoff, it is a miss.\n\nFIXED in e9e0d3e (pre-rebase 2f1ddf7). The warning is now keyed to whether the\nconnection holds ONE BACKEND FOR ITS LIFETIME, not to what a pooler's mode is\ncalled: a direct connection holds one and needs nothing (stated affirmatively\nand FIRST, so the majority reader is told their setup is fine before the hazard\nis described); an intermediary multiplexing many client connections over a\nshared set of backends routes later queries to a backend SessionInit never ran\non; session mode is the remedy.\n\nAlso dropped 'the backend it opened', which was loose in exactly the case being\nwarned about: behind transaction-mode pgbouncer the pgx connection terminates\nat the pooler and no server backend is assigned until a statement arrives. Now\n'whichever backend runs these statements', which is true in every arrangement.\n\nNo test pinned the old text, so this could not have been laundered past a\nfence -- checked before editing rather than assumed.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:11:43Z","created_by":"areqag","updated_at":"2026-08-01T12:47:43Z","started_at":"2026-08-01T03:22:47Z","closed_at":"2026-08-01T12:47:43Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/627","metadata":null,"dependencies":[{"issue_id":"gqlc-35yu.6","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:11:43Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.6","depends_on_id":"gqlc-35yu.2","type":"blocks","created_at":"2026-07-31T19:12:48Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.6","depends_on_id":"gqlc-35yu.3","type":"blocks","created_at":"2026-07-31T19:12:48Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.5","title":"spike: AGE temporal + parameter mechanics against a live container","description":"Research-only spike against a live apache/age container (scratch code in /tmp or the nested test module on a throwaway branch — nothing ships). Three questions, each answered with reproducible evidence (exact SQL/cypher run + observed output), recorded in this bead via bd update --notes/--design:\n\n1. TEMPORAL REALITY. src/include/utils/agtype.h shows agtype has no temporal value types (enum: NULL/STRING/NUMERIC/INTEGER/FLOAT/BOOL/VERTEX/EDGE/PATH/ARRAY/OBJECT — verified 2026-07-31). User is skeptical; verify empirically: what happens storing timestamp-ish values, does any casting or function surface exist (Postgres-type interop at the SQL boundary, any toTimestamp-style functions), and validate the fallback encoding table — timestamp=epoch-micros integer, date=ISO string, duration=ISO-8601 string, zoned=offset-preserving string — including ORDERING: WHERE n.ts \u003e param must compare correctly under each encoding.\n2. PARAMETER MECHANICS. AGE cypher() third-argument params require the prepared-statement dance; the upstream Go driver instead does fmt.Sprintf interpolation (disqualifying, injection vector). Validate the mechanism generated pgx code will use: cypher() third arg via pgx extended-protocol prepared statements vs the age_prepare_cypher two-step. Pick one; string interpolation of user args is banned unconditionally.\n3. AGTYPE TEXT CORPUS. Capture exact result-text forms for scalars, vertex, edge, path, arrays, maps, nulls (including the ::vertex-style suffixes) to drive decode-helper TDD in the emission beads.\n\nOutput: a confirmed-or-amended encoding table + the chosen param mechanism + the text corpus. If AGE turns out to handle temporals natively, the encoding table shrinks accordingly and the epic design note gets amended. No production code in this bead.","acceptance_criteria":"All three questions answered with reproducible commands + captured output in the bead; encoding table confirmed or amended per finding; one param mechanism selected with evidence it binds server-side; corpus covers every agtype shape the emission beads decode.","notes":"# SPIKE RESULTS (gqlc-35yu.5) — empirical, live container, 2026-07-31\n\n## Environment (reproducible)\n\n    Image:  apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\n            (= docker.io/apache/age:latest as of 2026-07-31)\n    Server: PostgreSQL 18.1 (Debian 18.1-1.pgdg13+2)\n    AGE:    extversion 1.7.0\n    Client: github.com/jackc/pgx/v5 v5.10.0, go1.26.5\n\n    docker run -d --name age-spike -e POSTGRES_PASSWORD=spike -e POSTGRES_USER=postgres \\\n      -e POSTGRES_DB=spikedb -p 5455:5432 \\\n      apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\n\nEvery psql run below is a single session prefixed by:\n    LOAD 'age';\n    SET search_path = ag_catalog, \"$user\", public;\n\n=====================================================================\n# *** BLOCKER: contradicts a LOCKED epic decision — NOT re-decided ***\n=====================================================================\n\nEpic gqlc-35yu locked design states:\n\n  \"all emitted SQL fully qualifies ag_catalog.cypher / ag_catalog.agtype\n   so search_path is never a session dependency.\"\n\nEMPIRICALLY FALSE. Full qualification is necessary but NOT sufficient.\nAGE's Cypher-\u003eSQL transform emits bare OPERATOR references (=, \u003e, +, @\u003e).\nPostgreSQL resolves operators through search_path, and AGE does not emit\nthe OPERATOR(ag_catalog.=) form. So ag_catalog must be on the session\nsearch_path for essentially every non-trivial query.\n\nEvidence — search_path deliberately WITHOUT ag_catalog, every identifier\nfully qualified:\n\n    SET search_path = \"$user\", public;\n    SHOW search_path;   --\u003e  \"$user\", public\n\n    -- (1) literal RETURN\n    SELECT * FROM ag_catalog.cypher('spike', $$ RETURN 1 $$) AS (v ag_catalog.agtype);\n     v\n    ---\n     1                                                     \u003c-- OK\n\n    -- (2) MATCH + property projection\n    SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P) RETURN n.name $$) AS (v ag_catalog.agtype);\n     \"alice\"\n     \"bob\"                                                 \u003c-- OK\n\n    -- (3) ORDER BY\n    SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P) RETURN n.name ORDER BY n.name $$) AS (v ag_catalog.agtype);\n     \"alice\"\n     \"bob\"                                                 \u003c-- OK\n\n    -- (4) WHERE with '='\n    SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P) WHERE n.name = 'alice' RETURN n.age $$) AS (v ag_catalog.agtype);\n    ERROR:  cannot cast agtype string to type boolean      \u003c-- FAIL\n\n    -- (5) WHERE with '\u003e'\n    SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P) WHERE n.age \u003e 35 RETURN n.name $$) AS (v ag_catalog.agtype);\n    ERROR:  cannot cast agtype integer to type boolean     \u003c-- FAIL\n\n    -- (6) inline property-map match (uses the @\u003e operator)\n    SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P {name:'alice'}) RETURN n.age $$) AS (v ag_catalog.agtype);\n    ERROR:  operator does not exist: ag_catalog.agtype @\u003e ag_catalog.agtype   \u003c-- FAIL\n\n    -- (7) arithmetic\n    SELECT * FROM ag_catalog.cypher('spike', $$ RETURN 1 + 1 $$) AS (v ag_catalog.agtype);\n    ERROR:  operator does not exist: ag_catalog.agtype + ag_catalog.agtype    \u003c-- FAIL\n\nAdding ag_catalog back fixes all four, same statements verbatim:\n\n    SET search_path = ag_catalog, \"$user\", public;\n    ... WHERE n.name = 'alice' ...   --\u003e  30\n    ... WHERE n.age \u003e 35 ...         --\u003e  \"bob\"\n    ... RETURN 1 + 1 ...             --\u003e  2\n\nConsequence (for the owner to decide — NOT decided here): SessionInit /\npgxpool AfterConnect must do BOTH `LOAD 'age'` AND\n`SET search_path = ag_catalog, ...`. search_path becomes genuine per-session\nstate the generated code depends on — relevant to pgbouncer transaction\npooling / DISCARD ALL, and to any user who re-SETs it. Full qualification of\ncypher()/agtype is still worth keeping, but it does not remove the dependency.\n\nAll working results elsewhere in this note were obtained with ag_catalog on\nthe search_path.\n=====================================================================\n# Q1. TEMPORAL REALITY — prior CONFIRMED; encoding table AMENDED (2 of 4 fail)\n=====================================================================\n\n## 1a. No temporal types, no temporal casts, no temporal functions\n\nComplete cast catalogue to/from agtype in AGE 1.7.0:\n\n    SELECT castsource::regtype AS src, casttarget::regtype AS tgt, castcontext AS ctx\n    FROM pg_cast WHERE castsource='ag_catalog.agtype'::regtype\n                    OR casttarget='ag_catalog.agtype'::regtype ORDER BY 1,2;\n\n           src          |        tgt         | ctx\n    --------------------+--------------------+-----\n     boolean            | ag_catalog.agtype  | e\n     bigint             | ag_catalog.agtype  | e\n     integer            | ag_catalog.agtype  | e\n     double precision   | ag_catalog.agtype  | e\n     ag_catalog.graphid | ag_catalog.agtype  | e\n     ag_catalog.agtype  | boolean            | i\n     ag_catalog.agtype  | bigint             | a\n     ag_catalog.agtype  | smallint           | e\n     ag_catalog.agtype  | integer            | e\n     ag_catalog.agtype  | text               | e\n     ag_catalog.agtype  | json               | e\n     ag_catalog.agtype  | double precision   | e\n     ag_catalog.agtype  | integer[]          | e\n     ag_catalog.agtype  | ag_catalog.graphid | i\n     ag_catalog.agtype[]| ag_catalog.agtype  | e\n    (15 rows)\n\n  -\u003e NO timestamp / timestamptz / date / time / interval in either direction.\n     Also: there is no text -\u003e agtype cast (only agtype -\u003e text).\n\nDirect attempts:\n\n    SELECT now()::timestamptz::agtype;\n    ERROR:  cannot cast type timestamp with time zone to agtype\n\n    SELECT '\"2024-01-01T00:00:00Z\"'::agtype::timestamptz;\n    ERROR:  cannot cast type agtype to timestamp with time zone\n\n    SELECT '2024-01-01T00:00:00Z'::agtype;          -- bare, unquoted\n    ERROR:  invalid input syntax for type agtype\n    DETAIL:  Token \"-01\" is invalid.\n\nCypher-level function surface — the only temporal-ish name that exists:\n\n    SELECT * FROM cypher('spike', $$ RETURN timestamp() $$) AS (v agtype);\n     1785541437366                    \u003c-- plain INTEGER agtype, epoch MILLIS\n\n    RETURN datetime()                 --\u003e ERROR: function datetime does not exist\n    RETURN date()                     --\u003e ERROR: function date does not exist\n    RETURN localdatetime()            --\u003e ERROR: function localdatetime does not exist\n    RETURN duration({days:1})         --\u003e ERROR: function duration does not exist\n    RETURN toTimestamp('2024-01-01')  --\u003e ERROR: function toTimestamp does not exist\n    RETURN datetime('2024-01-01T00:00:00Z') --\u003e ERROR: function datetime does not exist\n\n    SELECT proname FROM pg_proc p JOIN pg_namespace n ON n.oid=p.pronamespace\n    WHERE n.nspname='ag_catalog'\n      AND proname ~* 'time|date|dur|epoch|local|zone|instant';\n     age_timestamp        \u003c-- the ONLY hit out of 348 ag_catalog functions\n\n    \\df ag_catalog.age_timestamp\n     ag_catalog | age_timestamp | ag_catalog.agtype | (no args) | func\n\nVERDICT: the prior HOLDS. AGE 1.7.0 has no native temporal value type, no\ntemporal casts, no temporal constructors. AGE's own \"timestamp\" notion is an\ninteger epoch-millis. The encoding table is required; it does not shrink.\n\n## 1b. One usable SQL-boundary bridge (not a temporal type)\n\n`agtype -\u003e text` UNWRAPS string scalars, so Postgres-type interop is one hop:\n\n    SELECT ('\"hello\"'::agtype)::text AS as_text, length(('\"hello\"'::agtype)::text) AS len;\n     as_text | len\n    ---------+-----\n     hello   |   5                       \u003c-- quotes stripped\n\n    SELECT ('\"2024-01-01T00:00:00Z\"'::agtype)::text::timestamptz;\n          timestamptz\n    ------------------------\n     2024-01-01 00:00:00+00               \u003c-- works, via text\n\n    SELECT ('\"2024-01-01T00:00:00Z\"'::agtype)::json;\n     \"2024-01-01T00:00:00Z\"\n\nCAVEAT — ::text does NOT work on entities:\n\n    SELECT (v)::text FROM cypher('corpus', $$ MATCH (n:Person {name:'Carol'}) RETURN n $$) AS (v agtype);\n    ERROR:  agtype_value_to_text: unsupported argument agtype 6\n\nSo ::text is a scalar/array/map-only bridge; decode helpers cannot lean on it\nfor vertex/edge/path.\n\n## 1c. agtype string comparison follows the DATABASE COLLATION, not byte order\n\nThis governs whether any string encoding sorts correctly.\n\n    SELECT datcollate, datlocprovider FROM pg_database WHERE datname=current_database();\n     en_US.UTF-8 | c\n\n    RETURN 'Z' \u003c 'a'                          --\u003e  false   (agtype)\n    SELECT 'Z' \u003c 'a';                         --\u003e  f       (SQL, DB collation)\n    SELECT 'Z' COLLATE \"C\" \u003c 'a' COLLATE \"C\"; --\u003e  t       (byte order disagrees)\n\n  -\u003e agtype string ordering == DB collation ordering. For the digits+hyphen\n     alphabet used by ISO dates all collations agree, so the date encoding is\n     safe; but ANY string encoding is in principle collation-sensitive. That is\n     a portability caveat for a fixed encoding table.\n\n## 1d. ORDERING VALIDATION of the four proposed encodings\n\nMethod: create nodes, ORDER BY the property, then run `WHERE prop \u003e \u003cliteral\u003e`,\ncomparing against the true chronological answer.\n\n---- ENC1  timestamp = epoch-micros INTEGER ................. PASS ----\n\n    CREATE (:TS {name:'a', ts: 1704067200000000})     -- 2024-01-01\n    CREATE (:TS {name:'b', ts: 1735689600000000})     -- 2025-01-01\n    CREATE (:TS {name:'c', ts: -2208988800000000})    -- 1900-01-01 (pre-epoch)\n    CREATE (:TS {name:'d', ts: 253402300799000000})   -- 9999-12-31\n\n    MATCH (n:TS) RETURN n.name, n.ts ORDER BY n.ts\n     name |         ts\n    ------+--------------------\n     \"c\"  | -2208988800000000\n     \"a\"  | 1704067200000000\n     \"b\"  | 1735689600000000\n     \"d\"  | 253402300799000000     \u003c-- chronologically correct, negatives fine\n\n    MATCH (n:TS) WHERE n.ts \u003e 1704067200000000 RETURN n.name ORDER BY n.name\n     \"b\"\n     \"d\"                           \u003c-- correct\n\n    Precision exact, no float coercion:\n    MATCH (n:TS) RETURN n.name, n.ts, toString(n.ts)\n     \"d\" | 253402300799000000 | \"253402300799000000\"\n\n    int64 range:\n    RETURN 9223372036854775807, -9223372036854775808\n     9223372036854775807 | -9223372036854775808\n    RETURN 9223372036854775808          -- overflow\n     9.223372036854776e+18              \u003c-- SILENTLY becomes a float.\n    (Year 9999 in micros ~2.5e17, ~37x headroom. Safe — but the overflow mode\n     is silent precision loss, so the encoder must range-check.)\n\n---- ENC2  date = ISO string 'YYYY-MM-DD' ................... PASS ----\n\n    CREATE (:D {n:'d1',d:'2024-01-01'}),(:D {n:'d2',d:'2024-02-01'}),\n           (:D {n:'d3',d:'2023-12-31'}),(:D {n:'d4',d:'0999-01-01'}),\n           (:D {n:'d5',d:'2024-01-10'})\n\n    MATCH (n:D) RETURN n.n, n.d ORDER BY n.d\n      n   |      d\n    ------+--------------\n     \"d4\" | \"0999-01-01\"\n     \"d3\" | \"2023-12-31\"\n     \"d1\" | \"2024-01-01\"\n     \"d5\" | \"2024-01-10\"\n     \"d2\" | \"2024-02-01\"          \u003c-- chronologically correct\n\n    MATCH (n:D) WHERE n.d \u003e '2024-01-01' RETURN n.n ORDER BY n.n\n     \"d2\"\n     \"d5\"                         \u003c-- correct\n\n    Requires zero-padded fixed width. Verified true: '0999-01-01'\u003c'2023-12-31',\n    '2024-01-09'\u003c'2024-01-10', '2024-09-01'\u003c'2024-10-01'.\n    Caveat: breaks for years \u003e9999 or negative (BC) years — encoder must reject\n    or widen the field.\n\n---- ENC3  duration = ISO-8601 string ....................... *** FAIL *** ----\n\n    CREATE (:DUR {n:'x1',v:'PT1H'}),(:DUR {n:'x2',v:'PT2H'}),(:DUR {n:'x3',v:'P1D'}),\n           (:DUR {n:'x4',v:'PT10H'}),(:DUR {n:'x5',v:'PT90M'})\n\n    True chronological order:\n      PT1H(3600s) \u003c PT90M(5400s) \u003c PT2H(7200s) \u003c PT10H(36000s) \u003c P1D(86400s)\n\n    MATCH (n:DUR) RETURN n.n, n.v ORDER BY n.v\n      n   |    v\n    ------+---------\n     \"x3\" | \"P1D\"        \u003c-- LONGEST duration sorts FIRST\n     \"x4\" | \"PT10H\"\n     \"x1\" | \"PT1H\"\n     \"x2\" | \"PT2H\"\n     \"x5\" | \"PT90M\"      \u003c-- order is completely wrong\n\n    MATCH (n:DUR) WHERE n.v \u003e 'PT2H' RETURN n.n ORDER BY n.n\n     \"x5\"                \u003c-- PT90M (1.5h) returned as \"\u003e 2h\" : FALSE POSITIVE\n                             PT10H and P1D missing            : FALSE NEGATIVES\n\n    ISO-8601 duration text is not order-isomorphic to duration magnitude.\n    Round-trips fine, sorts wrong =\u003e FAILED ENCODING.\n\n---- ENC4  zoned = offset-preserving string ................. *** FAIL *** ----\n\n    CREATE (:ZD {n:'z1',v:'2024-01-01T12:00:00+05:00'})   -- = 07:00Z\n    CREATE (:ZD {n:'z2',v:'2024-01-01T09:00:00Z'})        -- = 09:00Z\n    CREATE (:ZD {n:'z3',v:'2024-01-01T02:00:00-05:00'})   -- = 07:00Z\n    CREATE (:ZD {n:'z4',v:'2024-01-01T08:00:00+00:00'})   -- = 08:00Z\n\n    True instant order: z1 == z3 (07:00Z) \u003c z4 (08:00Z) \u003c z2 (09:00Z)\n\n    MATCH (n:ZD) RETURN n.n, n.v ORDER BY n.v\n      n   |              v\n    ------+-----------------------------\n     \"z3\" | \"2024-01-01T02:00:00-05:00\"\n     \"z4\" | \"2024-01-01T08:00:00+00:00\"\n     \"z2\" | \"2024-01-01T09:00:00Z\"\n     \"z1\" | \"2024-01-01T12:00:00+05:00\"  \u003c-- tied-EARLIEST instant sorts LAST\n\n    MATCH (n:ZD) WHERE n.v \u003e '2024-01-01T08:00:00+00:00' RETURN n.n ORDER BY n.n\n     \"z1\"           \u003c-- z1 is 07:00Z, BEFORE the cutoff : FALSE POSITIVE\n     \"z2\"\n\n    Extra defect: one instant has several spellings ('Z' vs '+00:00') that sort\n    differently ('Z'=0x5A vs '+'=0x2B) — the encoding is not even canonical.\n    =\u003e FAILED ENCODING.\n## 1e. AMENDED ENCODINGS — tested against the same ordering battery\n\n---- duration -\u003e total-micros INTEGER ........................ PASS ----\n\n    CREATE (:DUR2 {n:'PT1H',v:3600000000}),(:DUR2 {n:'PT90M',v:5400000000}),\n           (:DUR2 {n:'PT2H',v:7200000000}),(:DUR2 {n:'PT10H',v:36000000000}),\n           (:DUR2 {n:'P1D',v:86400000000})\n\n    MATCH (n:DUR2) RETURN n.n, n.v ORDER BY n.v\n        n    |      v\n    ---------+-------------\n     \"PT1H\"  | 3600000000\n     \"PT90M\" | 5400000000\n     \"PT2H\"  | 7200000000\n     \"PT10H\" | 36000000000\n     \"P1D\"   | 86400000000       \u003c-- correct\n\n    MATCH (n:DUR2) WHERE n.v \u003e 7200000000 RETURN n.n ORDER BY n.v\n     \"PT10H\"\n     \"P1D\"                       \u003c-- correct\n\n    Only valid for durations reducible to a fixed micro count. Calendar-relative\n    components (months/years) are NOT totally ordered and have no faithful\n    integer — those should hit the epic's reserved generate-time error.\n\n---- zoned -\u003e map {t: epoch-micros, o: offset-seconds} ....... PASS, with caveat ----\n\n    CREATE (:ZD2 {n:'z1', v:{t:1704092400000000, o:18000}})\n    CREATE (:ZD2 {n:'z2', v:{t:1704099600000000, o:0}})\n    CREATE (:ZD2 {n:'z3', v:{t:1704092400000000, o:-18000}})\n    CREATE (:ZD2 {n:'z4', v:{t:1704096000000000, o:0}})\n\n    MATCH (n:ZD2) RETURN n.n, n.v.t, n.v.o ORDER BY n.v.t, n.n\n           n       |        t         |   o\n    ---------------+------------------+--------\n     \"z1_12:00+05\" | 1704092400000000 | 18000\n     \"z3_02:00-05\" | 1704092400000000 | -18000\n     \"z4_08:00+00\" | 1704096000000000 | 0\n     \"z2_09:00Z\"   | 1704099600000000 | 0       \u003c-- correct instant order\n\n    MATCH (n:ZD2) WHERE n.v.t \u003e 1704096000000000 RETURN n.n\n     \"z2_09:00Z\"                                \u003c-- correct\n\n    CAVEAT — correct ONLY when the query sorts on n.v.t. Sorting the whole map\n    is WRONG, because jsonb key order puts \"o\" before \"t\":\n\n    MATCH (n:ZD2) RETURN n.n, n.v ORDER BY n.v\n     \"z3\" | {\"o\": -18000, \"t\": 1704092400000000}\n     \"z4\" | {\"o\": 0, \"t\": 1704096000000000}\n     \"z2\" | {\"o\": 0, \"t\": 1704099600000000}\n     \"z1\" | {\"o\": 18000, \"t\": 1704092400000000}   \u003c-- wrong\n\n    =\u003e a map encoding forces gqlc to REWRITE user ORDER BY / WHERE to reach .t.\n\n---- zoned -\u003e UTC-normalised fixed-width string + offset suffix ... FAIL on ties ----\n\n    '2024-01-01T07:00:00.000000Z+05:00' etc. does sort correctly overall\n    (z1,z3 | z4 | z2) and `WHERE v \u003e '...08:00:00.000000Z+00:00'` returns z2\n    only — correct. BUT equal instants with different offsets mis-compare:\n\n    RETURN '2024-01-01T07:00:00.000000Z+05:00' = '2024-01-01T07:00:00.000000Z-05:00',\n           '2024-01-01T07:00:00.000000Z+05:00' \u003e '2024-01-01T07:00:00.000000Z+00:00'\n      eq   |  gt\n    -------+------\n     false | true       \u003c-- instant-correct answers are eq=true, gt=false\n\n---- zoned -\u003e FLATTENED SIDECAR: \u003cf\u003e int64 + \u003cf\u003eOffset int64 .. PASS (recommended) ----\n\n    CREATE (:ZD4 {n:'z1', ts:1704092400000000, tsOffset:18000})\n    CREATE (:ZD4 {n:'z2', ts:1704099600000000, tsOffset:0})\n    CREATE (:ZD4 {n:'z3', ts:1704092400000000, tsOffset:-18000})\n    CREATE (:ZD4 {n:'z4', ts:1704096000000000, tsOffset:0})\n\n    MATCH (n:ZD4) RETURN n.n, n.ts, n.tsOffset ORDER BY n.ts, n.n\n      n   |        ts        |   o\n    ------+------------------+--------\n     \"z1\" | 1704092400000000 | 18000\n     \"z3\" | 1704092400000000 | -18000\n     \"z4\" | 1704096000000000 | 0\n     \"z2\" | 1704099600000000 | 0       \u003c-- correct\n\n    MATCH (n:ZD4) WHERE n.ts \u003e 1704096000000000 RETURN n.n\n     \"z2\"                              \u003c-- correct\n\n    MATCH (a:ZD4 {n:'z1'}), (b:ZD4 {n:'z3'}) RETURN a.ts = b.ts, a.tsOffset = b.tsOffset\n     true | false                      \u003c-- instants correctly EQUAL, offsets differ\n\n    Decisive advantage over the map form: the sort key IS the property, so\n    `WHERE n.ts \u003e $param` and `ORDER BY n.ts` in USER-WRITTEN Cypher are correct\n    with no query rewriting by gqlc.\n\n## 1f. END-TO-END round trip through Go/pgx (ENC1)\n\nWritten as bound params, read back with ORDER BY n.ts:\n\n    in:  2024-01-01T12:34:56.123456Z\n         1969-07-20T20:17:40Z            (pre-epoch, negative micros)\n         9999-12-31T23:59:59.999999Z\n\n    read back, ORDER BY n.ts:\n       1969-07-20T20:17:40Z          (micros=-14182940000000)\n       2024-01-01T12:34:56.123456Z   (micros=1704112496123456)\n       9999-12-31T23:59:59.999999Z   (micros=253402300799999999)\n    exact round-trip \u0026 sorted ascending: true\n\n    WHERE n.ts \u003e $cut  (cut = 2000-01-01, passed as a bound param) --\u003e count = 2  (expected 2)\n\n## 1g. *** AMENDED ENCODING TABLE (deliverable) ***\n\n    Go type            | agtype encoding                          | ordering | status\n    -------------------+------------------------------------------+----------+--------------\n    timestamp/instant  | epoch-MICROS int64                       | correct  | CONFIRMED\n    date               | ISO 'YYYY-MM-DD' string, zero-padded     | correct  | CONFIRMED\n                       |   (years 1..9999 only)                   |          |\n    duration           | total-MICROS int64                       | correct  | AMENDED\n                       |   (was ISO-8601 string -\u003e SORTS WRONG)   |          |\n    zoned datetime     | TWO int64 props: \u003cf\u003e epoch-micros plus   | correct  | AMENDED\n                       |   \u003cf\u003eOffset offset-seconds               |          |\n                       |   (was offset string -\u003e SORTS WRONG)     |          |\n    calendar duration  | none faithful (months/years are not      |   n/a    | generate-time\n      (months/years)   |   totally ordered)                       |          | error\n    time-of-day        | NOT TESTED in this spike                 |    -     | OPEN\n\nSummary: 2 of the 4 proposed encodings round-trip but SORT WRONG and are\ntherefore failed encodings; both have working amendments verified above.\n=====================================================================\n# Q2. PARAMETER MECHANICS — SELECTED: cypher() 3rd arg via pgx extended protocol\n=====================================================================\n\n## 2a. AGE hard-rejects every non-parameter third argument\n\n    SELECT * FROM cypher('spike', $$ ... $name ... $$, $$ {\"name\":\"alice\"} $$) AS (v agtype);\n    ERROR:  third argument of cypher function must be a parameter at character 85\n\n    SELECT * FROM cypher('spike', $$ ... $$, ('{\"name\":\"alice\"}')::agtype) AS (v agtype);\n    ERROR:  third argument of cypher function must be a parameter at character 85\n\n    SELECT * FROM cypher('spike', $$ ... $$, (SELECT '{\"name\":\"alice\"}'::agtype)) AS (v agtype);\n    ERROR:  third argument of cypher function must be a parameter at character 84\n\n  -\u003e Only a raw $n placeholder is accepted. This is a STRUCTURAL guarantee:\n     AGE physically cannot be handed an interpolated parameter map.\n\n## 2b. pgx QueryExecMode matrix\n\nStatement used verbatim for all five modes:\n\n    SELECT * FROM ag_catalog.cypher('spike',\n      $$ MATCH (n:P) WHERE n.name = $name RETURN n.age $$, $1) AS (v ag_catalog.agtype)\n    arg: `{\"name\": \"alice\"}` (Go string)\n\n    CacheStatement (pgx default) OK  -\u003e \"30\"\n    CacheDescribe                OK  -\u003e \"30\"\n    DescribeExec                 OK  -\u003e \"30\"\n    Exec                         OK  -\u003e \"30\"\n    SimpleProtocol               ERR: ERROR: third argument of cypher function\n                                      must be a parameter (SQLSTATE 22023)\n\nSimpleProtocol is the ONLY pgx mode that interpolates client-side, and it is the\nONLY one AGE rejects. The server log shows exactly why — pgx had already spliced\nthe value into the statement text:\n\n    LOG:  statement: SELECT * FROM ag_catalog.cypher('spike',\n          $$ MATCH (n:P) WHERE n.name = $name RETURN n.age $$,  '{\"name\": \"alice\"}' )\n          AS (v ag_catalog.agtype)\n    ERROR: third argument of cypher function must be a parameter\n\n  =\u003e The unsafe path FAILS CLOSED. Generated code cannot accidentally ship\n     interpolation through pgx without an immediate hard error.\n\n## 2c. Alternative REJECTED on evidence: age_prepare_cypher\n\n    \\df ag_catalog.age_prepare_cypher\n     ag_catalog | age_prepare_cypher | boolean | cstring, cstring | func\n       (args are (graph_name, query_string))\n\nIt does work:\n\n    SELECT * FROM ag_catalog.age_prepare_cypher('spike', $$ MATCH (n:P) WHERE n.name = $name RETURN n.age $$);\n     t\n    PREPARE pc(ag_catalog.agtype) AS SELECT * FROM ag_catalog.cypher(NULL, NULL, $1) AS (v ag_catalog.agtype);\n    EXECUTE pc('{\"name\":\"alice\"}');   --\u003e 30\n    EXECUTE pc('{\"name\":\"bob\"}');     --\u003e 41\n\nBut it couples through per-session global state consumed at PLAN time:\n\n    -- prepare a DIFFERENT cypher query, then re-run the OLD prepared stmt:\n    SELECT * FROM ag_catalog.age_prepare_cypher('spike', $$ MATCH (n:P) RETURN n.name $$);\n    EXECUTE pc('{\"name\":\"alice\"}');   --\u003e 30      (still the OLD query text)\n\nThe cypher text is latched into the plan when the PREPARE is parsed. The binding\nbetween age_prepare_cypher and the following PREPARE is invisible,\nconnection-global, and order-dependent. Under pgx statement caching, Parse\nhappens once at an unpredictable moment (cache miss, fresh pooled conn, cache\neviction), so the required \"prime the session global, THEN Parse\" ordering\ncannot be guaranteed by generated code. And without a preceding prepare:\n\n    ERROR:  session info requires cypher(NULL, NULL) to be passed\n\n## 2d. *** SELECTED: direct cypher('\u003cgraph\u003e', $$...$$, $1) ***\n\nSingle self-contained statement, no session coupling beyond LOAD + search_path,\nworks in all four extended-protocol pgx modes, and the interpolating mode is\nrejected by the server.\n\n## 2e. *** EVIDENCE OF SERVER-SIDE BINDING ***\n\nServer config: log_statement=all, log_min_duration_statement=0,\nlog_parameter_max_length=-1, log_parameter_max_length_on_error=-1.\nAn injection payload was passed as the bound VALUE. Server log, verbatim:\n\n  LOG:  duration: 0.022 ms  bind stmtcache_30c7c47319fbc2aa5f6fc580ba98420299639e3e2c17a16d:\n        SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P) WHERE n.name = $name RETURN n.age $$, $1) AS (v ag_catalog.agtype)\n  DETAIL:  Parameters: $1 = '{\"name\": \"a$$, $1) AS (v ag_catalog.agtype); DROP TABLE IF EXISTS canary; --\"}'\n  LOG:  execute stmtcache_30c7c47319fbc2aa5f6fc580ba98420299639e3e2c17a16d:\n        SELECT * FROM ag_catalog.cypher('spike', $$ MATCH (n:P) WHERE n.name = $name RETURN n.age $$, $1) AS (v ag_catalog.agtype)\n  DETAIL:  Parameters: $1 = '{\"name\": \"a$$, $1) AS (v ag_catalog.agtype); DROP TABLE IF EXISTS canary; --\"}'\n\nTHE PROOF: parse / bind / execute are separate protocol phases. The statement\ntext logged at EVERY phase ends in `..., $1) AS (v ag_catalog.agtype)` — the\nliteral token `$1`. The payload appears ONLY in the Bind message's\n`DETAIL: Parameters:` line. The value is never part of the query text.\n\nClient-side confirmation — payloads treated as data, canary table intact:\n\n    payload[0] {\"name\": \"alice\"}                                    rows=1\n    payload[1] {\"name\": \"alice\\\"}) RETURN 1 // \"}                   rows=0\n    payload[2] {\"name\": \"a$$, $1) AS (...); DROP TABLE ...; --\"}    rows=0\n    payload[3] {\"name\": \"'; DROP TABLE canary; --\"}                 rows=0\n    canary table still present, rows=1  =\u003e no injection\n\n## 2f. Binding/scanning details the emission beads need\n\n    agtype OID in this DB = 16461.\n    *** NOT stable across databases — it is a non-builtin extension type, so it\n        must NEVER be hard-coded in generated code. ***\n\n    Prepared-statement describe:\n      ParamOIDs=[16461]\n      result field \"v\" OID=16461 format=0        (0=text, 1=binary)\n    =\u003e agtype travels as TEXT in BOTH directions. No binary codec needed.\n\n  Param encoding — what pgx accepts into the agtype slot:\n    string           -\u003e \"30\"   err=\u003cnil\u003e                              \u003c-- USE THIS\n    []byte           -\u003e ERROR: invalid input syntax for type agtype (SQLSTATE 22P02)\n    map[string]any   -\u003e failed to encode args[0]: unable to encode\n                        map[string]interface {}{\"name\":\"alice\"} into text format\n                        for unknown type (OID 16461): cannot find encode plan\n    nil              -\u003e no rows in result set  (binds as NULL; AGE treats that as\n                        \"no params\", so $name stays unbound)\n\n  =\u003e generated code must marshal the parameter map to an agtype/JSON STRING and\n     pass a Go `string`. []byte and map[string]any are both unusable.\n\n  Result scanning of an agtype column (all succeed):\n    Scan(*string)  err=\u003cnil\u003e val=\"30\"\n    Scan(*[]byte)  err=\u003cnil\u003e val=\"30\"\n    Scan(*int64)   err=\u003cnil\u003e val=30\n    Scan(*any)     err=\u003cnil\u003e val=\"30\"   (Go string — unknown OIDs land as string)\n\n  Also: declaring the param as text does NOT work —\n    PREPARE p3(text) AS SELECT * FROM ag_catalog.cypher(...,$1) AS (...);\n    ERROR:  function ag_catalog.cypher(unknown, unknown, text) does not exist\n  The param must resolve to agtype, which the server infers when the client\n  sends an unspecified param type. That is pgx's default behaviour — no special\n  handling required.\n=====================================================================\n# Q3. AGTYPE TEXT CORPUS\n=====================================================================\n\nCaptured with `psql -A -t` (raw bytes) and cross-checked byte-for-byte against\nwhat pgx v5 receives (Go %q of the scanned []byte) — IDENTICAL (see C11).\n\nCorpus graph:\n\n    SELECT create_graph('corpus');\n    SELECT * FROM cypher('corpus', $$\n      CREATE (a:Person {name:'Alice', age:30, score:9.5, active:true, nick:null})\n            -[r:KNOWS {since:2019, weight:0.5}]-\u003e\n             (b:Person {name:'Bob', age:41, tags:['x','y'], meta:{k:'v', n:[1,2]}})\n      CREATE (b)-[:KNOWS {since:2021}]-\u003e(c:Person {name:'Carol'})\n    $$) AS (v agtype);\n\n## C1. SCALARS\n\n    RETURN null                     -\u003e (SQL NULL — see C8, NOT the text \"null\")\n    RETURN true                     -\u003e true\n    RETURN false                    -\u003e false\n    RETURN 42                       -\u003e 42\n    RETURN -42                      -\u003e -42\n    RETURN 9223372036854775807      -\u003e 9223372036854775807\n    RETURN -9223372036854775808     -\u003e -9223372036854775808\n    RETURN 1.5                      -\u003e 1.5\n    RETURN -0.0                     -\u003e -0.0\n    RETURN 1.0                      -\u003e 1.0\n    RETURN 1e100                    -\u003e 1e+100\n    RETURN 0.1 + 0.2                -\u003e 0.30000000000000004      (17 sig digits)\n    RETURN toFloat(1)/3             -\u003e 0.3333333333333333\n    RETURN toFloat('1.5')           -\u003e 1.5\n\n  NUMERIC is a distinct type with a SUFFIX form (distinct from FLOAT):\n    SELECT '1.5::numeric'::agtype;  -\u003e 1.5::numeric\n\n  NON-JSON literals that are valid agtype (decoder MUST handle these):\n    SELECT 'NaN'::agtype;           -\u003e NaN\n    SELECT 'Infinity'::agtype;      -\u003e Infinity\n    SELECT '-Infinity'::agtype;     -\u003e -Infinity\n    SELECT '{\"n\": NaN}'::agtype;    -\u003e {\"n\": NaN}\n  (`RETURN 1.0/0.0` raises `ERROR: division by zero`, so these arrive via stored\n   data / agtype_in, not via Cypher arithmetic.)\n\n  STRINGS — JSON-escaped, double-quoted:\n    RETURN 'plain'                  -\u003e \"plain\"\n    RETURN 'has \"quote\"'            -\u003e \"has \\\"quote\\\"\"\n    RETURN 'back\\\\slash'            -\u003e \"back\\\\slash\"\n    RETURN 'tab\\there'              -\u003e \"tab\\there\"\n    RETURN 'new\\nline'              -\u003e \"new\\nline\"\n    RETURN 'unicode: e' cafe' ...'  -\u003e raw UTF-8 passed through, NOT \\u-escaped\n    RETURN ''                       -\u003e \"\"\n\n## C2. ARRAYS   (separator is \", \" — comma AND space)\n\n    RETURN []                       -\u003e []\n    RETURN [1,2,3]                  -\u003e [1, 2, 3]\n    RETURN ['a','b']                -\u003e [\"a\", \"b\"]\n    RETURN [1,'a',true,null,1.5]    -\u003e [1, \"a\", true, null, 1.5]\n    RETURN [[1,2],[3]]              -\u003e [[1, 2], [3]]\n    RETURN [{a:1},{b:[2]}]          -\u003e [{\"a\": 1}, {\"b\": [2]}]\n    SELECT '[1.5::numeric, NaN, Infinity, -Infinity]'::agtype;\n                                    -\u003e [1.5::numeric, NaN, Infinity, -Infinity]\n\n  NOTE: `null` INSIDE a container is the literal text `null` (contrast C8).\n\n## C3. MAPS   (`\"key\": value`, separator \", \")\n\n    RETURN {}                       -\u003e {}\n    RETURN {a:1}                    -\u003e {\"a\": 1}\n    RETURN {z:1, a:2, m:3}          -\u003e {\"a\": 2, \"m\": 3, \"z\": 1}\n    RETURN {s:'x', n:null, b:false, f:1.5, arr:[1], nested:{k:'v'}}\n          -\u003e {\"b\": false, \"f\": 1.5, \"s\": \"x\", \"arr\": [1], \"nested\": {\"k\": \"v\"}}\n    RETURN {`key with space`: 1, `q\"uote`: 2}\n          -\u003e {\"q\\\"uote\": 2, \"key with space\": 1}\n    SELECT '{\"a\": 1.5::numeric, \"b\": NaN}'::agtype;\n          -\u003e {\"a\": 1.5::numeric, \"b\": NaN}\n\n  *** KEY ORDER IS jsonb-STYLE: by key LENGTH first, then lexicographically. ***\n  (b,f,s = len 1; arr = len 3; nested = len 6.) Input order is NOT preserved.\n  Decoders must be order-independent; GOLDEN TESTS MUST EXPECT THIS ORDER.\n  Note also that `n:null` vanished from that map entirely — see C8(c).\n\n## C4. VERTEX — `{...}::vertex`, keys id, label, properties (length order)\n\n    MATCH (n:Person {name:'Alice'}) RETURN n\n    {\"id\": 844424930131969, \"label\": \"Person\", \"properties\": {\"age\": 30, \"name\": \"Alice\", \"score\": 9.5, \"active\": true}}::vertex\n\n    MATCH (n:Person {name:'Bob'}) RETURN n\n    {\"id\": 844424930131970, \"label\": \"Person\", \"properties\": {\"age\": 41, \"meta\": {\"k\": \"v\", \"n\": [1, 2]}, \"name\": \"Bob\", \"tags\": [\"x\", \"y\"]}}::vertex\n\n    MATCH (n:Person {name:'Carol'}) RETURN n\n    {\"id\": 844424930131971, \"label\": \"Person\", \"properties\": {\"name\": \"Carol\"}}::vertex\n\n  id is a graphid: (label_id \u003c\u003c 48) | sequence. 844424930131969 = (3\u003c\u003c48)|1.\n\n## C5. EDGE — `{...}::edge`, keys id, label, end_id, start_id, properties\n\n    MATCH ()-[r:KNOWS]-\u003e() RETURN r\n    {\"id\": 1125899906842625, \"label\": \"KNOWS\", \"end_id\": 844424930131970, \"start_id\": 844424930131969, \"properties\": {\"since\": 2019, \"weight\": 0.5}}::edge\n    {\"id\": 1125899906842626, \"label\": \"KNOWS\", \"end_id\": 844424930131971, \"start_id\": 844424930131970, \"properties\": {\"since\": 2021}}::edge\n\n  NOTE: end_id comes BEFORE start_id (length order again: 6 \u003c 8).\n\n## C6. PATH — `[...]::path`, alternating vertex/edge, EACH keeping its own suffix\n\n    MATCH p=(:Person {name:'Alice'})-[:KNOWS]-\u003e(:Person) RETURN p\n    [{\"id\": 844424930131969, \"label\": \"Person\", \"properties\": {\"age\": 30, \"name\": \"Alice\", \"score\": 9.5, \"active\": true}}::vertex, {\"id\": 1125899906842625, \"label\": \"KNOWS\", \"end_id\": 844424930131970, \"start_id\": 844424930131969, \"properties\": {\"since\": 2019, \"weight\": 0.5}}::edge, {\"id\": 844424930131970, \"label\": \"Person\", \"properties\": {\"age\": 41, \"meta\": {\"k\": \"v\", \"n\": [1, 2]}, \"name\": \"Bob\", \"tags\": [\"x\", \"y\"]}}::vertex]::path\n\n    -- variable-length, 2 hops (`[:KNOWS*1..2]` returns both the 1-hop and this):\n    [ ...Alice::vertex, ...KNOWS(2019)::edge, ...Bob::vertex, {\"id\": 1125899906842626, \"label\": \"KNOWS\", \"end_id\": 844424930131971, \"start_id\": 844424930131970, \"properties\": {\"since\": 2021}}::edge, {\"id\": 844424930131971, \"label\": \"Person\", \"properties\": {\"name\": \"Carol\"}}::vertex]::path\n\n## C7. ENTITIES NESTED IN CONTAINERS — suffixes occur at ARBITRARY DEPTH\n\n    RETURN [n]        -\u003e [{\"id\": ..., \"label\": \"Person\", \"properties\": {...}}::vertex]\n    RETURN {node:n}   -\u003e {\"node\": {\"id\": ..., \"label\": \"Person\", \"properties\": {...}}::vertex}\n    RETURN collect(n) -\u003e [\u003c...\u003e::vertex, \u003c...\u003e::vertex, \u003c...\u003e::vertex]\n\n  *** A decoder CANNOT just strip a trailing suffix. `::vertex` / `::edge` /\n      `::path` / `::numeric` are VALUE-LEVEL type annotations that can appear at\n      any nesting depth. ***\n\n## C8. NULL SEMANTICS — three distinct cases\n\n  (a) Top-level Cypher null, a null property, and a MISSING property all become\n      SQL NULL — not the text \"null\":\n\n    MATCH (n:Person {name:'Alice'}) RETURN n.name, n.nick, n.missing, n.age\n      name   |    nick    |  missing   | age\n    ---------+------------+------------+-----\n     \"Alice\" | \u003cSQL NULL\u003e | \u003cSQL NULL\u003e | 30\n\n    SELECT (v IS NULL) FROM cypher('corpus', $$ ... RETURN n.nick $$) AS (v agtype);\n     t                                        \u003c-- genuinely SQL NULL\n\n    From Go: Scan into *string leaves the pointer nil with err=\u003cnil\u003e.\n\n  (b) null INSIDE an array/map is the literal text `null` (see C2/C3).\n\n  (c) *** NULL PROPERTIES ARE DROPPED, NOT STORED ***\n\n    CREATE (n:NullProp {a:1, b:null}) RETURN n\n    {\"id\": 1407374883553281, \"label\": \"NullProp\", \"properties\": {\"a\": 1}}::vertex\n                                                                 ^^^ \"b\" is GONE\n\n    MATCH (n:NullProp) SET n.a = null RETURN n\n    {\"id\": 1407374883553281, \"label\": \"NullProp\", \"properties\": {}}::vertex\n                                                                 ^^ key REMOVED\n\n    Matters for bead .9 (entity columns): a nullable field's key will be ABSENT\n    from the properties map — never present-with-null.\n\n## C9. AGGREGATES / MISC\n\n    MATCH (n:Person) RETURN count(*)          -\u003e 3\n    MATCH (n:Person) RETURN avg(n.age)        -\u003e 35.5\n    MATCH (n:Person) RETURN collect(n.name)   -\u003e [\"Alice\", \"Bob\", \"Carol\"]\n    MATCH (n:Person {name:'Alice'}) RETURN id(n), label(n)\n                                              -\u003e 844424930131969 | \"Person\"\n\n    Empty match          -\u003e ZERO ROWS (not a null row)\n    Write without RETURN -\u003e ZERO ROWS, but the AS(...) list still needs exactly\n                            one agtype column, e.g. `AS (v ag_catalog.agtype)`.\n\n## C10. AGE 1.7.0 LIMITATION worth recording\n\n    SELECT * FROM cypher('corpus', $$ CREATE (x:A:B {n:1}) $$) AS (v agtype);\n    ERROR:  syntax error at or near \":\"\n\n  Multi-label vertices are NOT supported. Vertex text always carries exactly one\n  scalar `\"label\"`, never a list. Relevant to schema admission (phase Z/A) if\n  gqlc's GQL/Cypher schema surface permits multiple labels.\n\n## C11. pgx byte-for-byte cross-check\n\nScanned as []byte through pgx v5 and printed with Go %q — identical to psql -A:\n\n  vertex      \"{\\\"id\\\": 844424930131969, \\\"label\\\": \\\"Person\\\", \\\"properties\\\": {\\\"age\\\": 30, \\\"name\\\": \\\"Alice\\\", \\\"score\\\": 9.5, \\\"active\\\": true}}::vertex\"\n  edge        \"{\\\"id\\\": 1125899906842625, \\\"label\\\": \\\"KNOWS\\\", \\\"end_id\\\": 844424930131970, \\\"start_id\\\": 844424930131969, \\\"properties\\\": {\\\"since\\\": 2019, \\\"weight\\\": 0.5}}::edge\"\n  path        \"[{...}::vertex, {...}::edge, {...}::vertex]::path\"\n  array-mixed \"[1, \\\"a\\\", true, null, 1.5]\"\n  map-nested  \"{\\\"b\\\": false, \\\"f\\\": 1.5, \\\"s\\\": \\\"x\\\", \\\"arr\\\": [1], \\\"nested\\\": {\\\"k\\\": \\\"v\\\"}}\"\n  string-esc  \"\\\"has \\\\\\\"q\\\\\\\" and \\\\\\\\ and \\\\t\\\"\"\n  float       \"0.30000000000000004\"\n  int64max    \"9223372036854775807\"\n\n=====================================================================\n# SUMMARY FOR DOWNSTREAM BEADS\n=====================================================================\n\n.7 (scalar reads):\n  - param: ONE Go `string` holding an agtype/JSON object, bound to $1 of\n    cypher('\u003cgraph\u003e', $$...$$, $1). Never []byte, never map[string]any.\n  - agtype arrives as TEXT (format=0); its OID is DB-specific (16461 here) and\n    must never be hard-coded. Scan into *string / *[]byte and parse.\n  - decoder must handle NaN / Infinity / -Infinity / `X::numeric` (all non-JSON),\n    plus SQL NULL vs the literal `null`.\n\n.9 (entity columns):\n  - `::vertex` / `::edge` / `::path` are value-level annotations at ANY depth.\n  - jsonb key order (length, then lexicographic) — bake into goldens.\n  - absent key, not null value, for unset properties.\n  - single label only in AGE 1.7.0.\n\n.11 (temporals):\n  - timestamp = epoch-micros int64            (CONFIRMED)\n  - date      = ISO string, years 1..9999     (CONFIRMED)\n  - duration  = total-micros int64            (AMENDED — ISO-8601 string SORTS WRONG)\n  - zoned     = \u003cf\u003e int64 + \u003cf\u003eOffset int64   (AMENDED — offset string SORTS WRONG\n                                               and mis-compares equal instants)\n  - calendar durations (months/years): generate-time error.\n  - time-of-day: NOT covered by this spike — still open.\n\nALL BEADS:\n  - the epic's \"search_path is never a session dependency\" claim is FALSE;\n    ag_catalog MUST be on the search_path. Flagged for the owner; not re-decided.\n\nScratch code (nothing shipped): /tmp/age-spike (SQL probes) and\n/tmp/age-spike/gospike + /tmp/age-spike/gospike/corpusrun (pgx programs).\nNo files under internal/ were touched; worktree gqlc-35yu5-age-spike is clean.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:11:34Z","created_by":"areqag","updated_at":"2026-08-01T00:11:55Z","started_at":"2026-07-31T23:40:45Z","closed_at":"2026-08-01T00:11:55Z","close_reason":"Spike complete: 3/3 questions answered with reproducible evidence. Encoding table amended (duration, zoned -\u003e int64); param mechanism selected (direct cypher() 3rd arg via pgx extended protocol) with server-log proof of server-side binding; agtype corpus captured. Reversed the epic's search_path decision (owner-approved). Outcomes recorded on the epic and on .6/.7/.11.","external_ref":"https://github.com/areqag/gqlc/issues/628","dependencies":[{"issue_id":"gqlc-35yu.5","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:11:33Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.4","title":"codegen live tests: shared scenario battery + per-backend adapters (v5/v6 arms)","description":"Refactor test/data/codegen/live_test.go from per-arm inlined copies into a shared scenario battery. Enabler: every backend generates from the same fixture inputs, so generated method sets are signature-identical across backends — scenario bodies are written ONCE against a test-local structural interface. A per-backend adapter owns: container start, connect + handle construction, seed(cypher), and the isolation strategy (neo4j: serialized DETACH-wipe; AGE later: unique graph per scenario, parallel). Scenario coverage keeps the existing battery: ErrNoRows on empty graph, single-row read, ErrMultipleResults on duplicates, exec write + re-read.\n\nThis bead lands the v5/v6 arms only; the AGE arm is a later bead. Seed cypher must stay in the openCypher dialect intersection (CREATE/MATCH basics) — fine for smoke scenarios. Preserve failure-pointing (the current file inlines bodies for exactly this reason) via t.Helper + per-scenario named subtests + require messages. Update golden import paths for the per-target subtree relocation. Stays in the nested module behind the codegen_live tag; digest-pinned image and GQLC_SKIP_LIVE behavior unchanged.","acceptance_criteria":"Each scenario body exists exactly once; v5 and v6 arms both run the full battery green under -tags codegen_live; a deliberately broken assertion reports the scenario name and assertion line, not a wrapper; adding a backend arm requires only an adapter.","notes":"=== CONSTRAINT: \"shared scenario battery\" cannot be a plain Go interface ===\nMeasured on the .3 branch, not assumed.\n\nThe bead says scenario bodies are written once \"against a test-local\nstructural interface\". The method SIGNATURES are textually identical\nacross arms:\n\n  PeopleByAgeAndLocale(ctx context.Context, arg PeopleByAgeAndLocaleParams) ([]PeopleByAgeAndLocaleRow, error)\n\nbut Params/Row are package-local named types, and each target emits its\nown package (golden/neo4j-go-v5 vs golden/neo4j-go-v6). So\nv5.PeopleByAgeAndLocaleParams != v6.PeopleByAgeAndLocaleParams and no\nsingle non-generic interface can be satisfied by both handles.\n\nSame problem for the error assertions, and worse: ErrNoRows and\nErrMultipleResults are per-package `errors.New` values, so\nerrors.Is(v6err, v5.ErrNoRows) is FALSE. A scenario body cannot name one\nsentinel and have it hold for both arms.\n\nImplication for the adapter seam: the adapter must narrow the generated\nsurface to types the TEST owns — plain scenario-local structs/args and\nthe arm's own sentinel values exposed as methods (errNoRows() error,\nerrMultipleResults() error) — not merely own container/connect/seed/\nisolation. That narrowing is the mechanism that makes one scenario body\nserve N backends; it is not incidental glue.\n\nGenerics were considered and are the wrong tool here: a type parameter\ncan carry P and R through the call, but a scenario body must CONSTRUCT a\nP and READ FIELDS off an R, which needs per-arm code regardless. The\nadapter pays that cost once per fixture per arm, explicitly.\n\nConsequence for the AGE arm (.8): adding a backend stays \"write one\nadapter\", but the adapter is ~15 lines of field mapping per fixture, not\nzero. Acceptance criterion \"adding a backend arm requires only an\nadapter\" is still met.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:11:22Z","created_by":"areqag","updated_at":"2026-08-01T04:15:46Z","started_at":"2026-08-01T03:22:46Z","closed_at":"2026-08-01T04:15:46Z","external_ref":"https://github.com/areqag/gqlc/issues/629","metadata":null,"dependencies":[{"issue_id":"gqlc-35yu.4","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:11:22Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.4","depends_on_id":"gqlc-35yu.3","type":"blocks","created_at":"2026-07-31T19:12:47Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.3","title":"codegen tests: single-fixture layout with per-target golden subtrees","description":"Restructure test/data/codegen so each fixture holds its inputs exactly once: manifest.json gains targets (list of registry wire keys; omitted = all registered targets); golden/ gains one subtree per target key; the _v6 twin fixtures (verbatim input copies per codegen_test.go:36 doc) are deleted. The suite iterates fixtures x declared targets, constructing each generator via the registry (from the registry bead). A registered target enrolled in a fixture with a missing golden subtree is a RED test — this is the forcing function that means a new backend cannot register without entering the golden + compile gates.\n\nInvalid fixtures split the same way: core (backend-independent) errors run under all targets; backend-specific errors (e.g. the unrepresentable_width family) declare their target list.\n\nTestGoldenBuild needs no changes — it compiles the whole nested module, so per-target subtrees are swept automatically; it remains the fully generic compile gate.","acceptance_criteria":"_v6 twin fixture dirs gone; every input file exists once per fixture; golden contents byte-identical to pre-migration modulo relocation (diff the trees to prove it); suite has no per-target switch; deliberately deleting one golden subtree turns the suite red; TestGoldenBuild still compiles everything.","notes":"ARCHITECTURAL CONSTRAINT surfaced by the .2 adversarial review (Linus, finding 5) — fold into this bead's restructuring:\n\nThe codegen conformance suite (internal/codegen/neo4j/neo4j_test.go) now imports internal/cli/backends to construct generators through the registry. That import direction means the suite lives inside the neo4j package while iterating over ALL registered targets. Once AGE registers (bead .6), this suite will construct AGE generators from the neo4j package's test directory.\n\nThe suite therefore needs a new home as part of this bead's \"suite iterates fixtures x declared targets\" work — a backend-neutral package (e.g. internal/codegen/conformance or a test/ package) that imports every backend via the registry, rather than living under one backend's directory. Doing this here is cheap because the suite is being restructured anyway; deferring it to .6 makes AGE's bead carry a neo4j refactor.\n\nThis is an implementation constraint, not a change to the acceptance criteria.\n\n=== ENROLLMENT MODEL — OWNER DECISION 2026-08-01 (amends the locked \"omitted = all registered\" clause) ===\nThe locked rule read: manifest targets omitted = all registered targets; registering a backend auto-enrolls it into every fixture, and a missing golden subtree is red.\n\nTaken literally at migration time that obliges a v6 golden subtree for all 67 fixtures that currently carry a v5 golden only (~258 new generated files, ~12k lines differing from v5 by import path and DriverWithContext -\u003e Driver), and obliges ~289 AGE subtrees the moment AGE registers at .6.\n\nOWNER CHOSE: explicit enrollment, coverage unchanged. Every fixture declares its targets:\n  - the 8 _v6 twins collapse into their base fixture with targets [\"neo4j-go-v5\",\"neo4j-go-v6\"]\n  - the remaining 67 carry targets [\"neo4j-go-v5\"]\n  - ZERO new golden files. Migration is pure relocation; existing golden bytes must not change.\n\nCONSEQUENCE: targets is a REQUIRED manifest field. Omission is a loud fixture-authoring error, not a default. A silent \"omitted = all registered\" default would be a trap once AGE registers — a new fixture authored without targets would enroll every backend and go red for reasons unrelated to the fixture.\n\nCONSEQUENCE: the \"registering a backend auto-enrolls it\" forcing function does not survive this choice. Backend enrollment becomes an explicit, per-bead act: .6-.11 add \"apache-age-pgx-v5\" to fixture target lists as AGE capability lands. Each of those beads owns the goldens for the fixtures it enrolls.\n\n=== CORRECTION 2026-08-01 (adversarial review, Linus) — unrepresentable_width is NOT backend-independent ===\nAn earlier implementation note claimed all 24 invalid fixtures behave identically under\nevery target, so labelling the unrepresentable_width_* family \"neo4j-specific\" was\ndocumentary only, and suggested enrolling them everywhere at .6. THAT IS WRONG.\n\ncodegen.TypeMap (internal/codegen/typemap.go) is a PER-BACKEND interface. The set of\nwidths for which Property returns ok=false is owned by the backend, and the eight-width\nlist lives in internal/codegen/neo4j/types.go. AGE runs on postgres and can plausibly\ncarry DECIMAL faithfully, so it may not raise ErrUnrepresentableWidth at all.\n\nThe fixtures' declared targets in the .3 commit are correct as written\n([\"neo4j-go-v5\",\"neo4j-go-v6\"]). Do NOT enrol unrepresentable_width_{decimal,float16,\nint128,uint256} into apache-age-pgx-v5 at .6 without first checking what AGE's TypeMap\nactually rejects; the ones AGE also rejects get enrolled, the rest stay neo4j-only.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:11:05Z","created_by":"areqag","updated_at":"2026-08-01T03:22:29Z","started_at":"2026-08-01T01:06:17Z","closed_at":"2026-08-01T03:22:29Z","close_reason":"Merged as PR #642. Fixtures are keyed by input, goldens by target; manifest.targets is required with no default. 289 goldens relocated byte-identical, zero new. Suite moved to internal/codegen/conformance, resolving via codegen.Registry. Two -update blast-radius bugs found and fixed in review.","external_ref":"https://github.com/areqag/gqlc/issues/630","metadata":null,"dependencies":[{"issue_id":"gqlc-35yu.3","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:11:05Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.3","depends_on_id":"gqlc-35yu.2","type":"blocks","created_at":"2026-07-31T19:12:46Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.2","title":"codegen: runtime Registry + config parity test + pipeline wiring (no init())","description":"Add codegen.Registry + Entry{Key string, New func(pkg string) Generator} with NO package-level state and NO init() registration (explicit user requirement — init-based registration is a hack). The CLI composition root (internal/cli) constructs the registry explicitly, listing the neo4j package entries (neo4j-go-v5, neo4j-go-v6); pipeline.Run takes the registry and resolves the config wire key through it, replacing the hardcoded switch at internal/cli/pipeline/pipeline.go:221. Also replace the test-side generatorFor switch (internal/codegen/codegen_test.go:49) with registry lookup so the conformance suite constructs generators exactly the way production does.\n\nConfig posture: internal/config keeps its closed Driver enum, UnmarshalYAML validation, and parse-time error messages. Drift between the two lists is killed by a parity test asserting registry keys == config.DriverValues() (chosen over making the registry the single vocabulary source, which would ripple the loader signature through every call site).","acceptance_criteria":"Registry constructed only at the composition root; grep proves no init() registration and no package-level registry var; pipeline driver switch deleted; test generatorFor switch deleted; parity test red if a registry key or enum const is added without its twin; adding a backend = one Entry + one enum const.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:10:59Z","created_by":"areqag","updated_at":"2026-08-01T01:05:06Z","started_at":"2026-08-01T00:29:51Z","closed_at":"2026-08-01T01:05:06Z","close_reason":"Merged via PR #640 (squash 44e-series on master). Registry + parity test + pipeline wiring landed; generatorFor switch deleted; no init(), no package-level state.","external_ref":"https://github.com/areqag/gqlc/issues/631","dependencies":[{"issue_id":"gqlc-35yu.2","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:10:58Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-35yu.2","depends_on_id":"gqlc-35yu.1","type":"blocks","created_at":"2026-07-31T19:12:45Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.1","title":"codegen: split core from the neo4j backend (internal/codegen/neo4j)","description":"Pure refactor, no behavior change. Move the neo4j-specific emission layer — render_*.go, driverTarget (driver.go), and the neo4j-shaped column-decode strategy mapping in prepare.go — into internal/codegen/neo4j, which implements codegen.Generator end-to-end. internal/codegen keeps Input/File/Generator/errors and phases Z/A/B (schema admission + entity naming, batch admission, name derivation), with the Go-type mapping parametrized so a second backend supplies its own table.\n\nWhy: the core currently hardcodes neo4j — prepare.go strategy enums are named after neo4j.GetRecordValue / dbtype.Node shapes; render templates inline the neo4j API; only driverTarget abstracts v5/v6 import paths. Epic gqlc-35yu locks shared-core + full-Generators as the seam.\n\nNotes: the exact split line is discovered during the refactor — this bead pins intent and gate, not a file list. No internal-API freeze applies (bd memory no-internal-api-freeze); refactor freely. Sibling worktree + stacked branch; TDD posture here means the gate below is red until the split is complete.","acceptance_criteria":"Existing golden trees byte-identical (this IS the gate — any golden diff fails the bead); go test ./... green; core files emit no neo4j import strings or API names; internal/codegen/neo4j implements codegen.Generator; consumers (pipeline, tests) compile against the new layout.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:10:52Z","created_by":"areqag","updated_at":"2026-08-01T00:22:56Z","started_at":"2026-07-31T23:40:44Z","closed_at":"2026-08-01T00:22:56Z","close_reason":"Merged in PR #638. Linus review PASS (no blocking findings); three nits fixed in a follow-up commit. Goldens byte-identical — no golden-update path run. Deferred narrow-width list-element fixture tracked as gqlc-hhd0.","external_ref":"https://github.com/areqag/gqlc/issues/632","dependencies":[{"issue_id":"gqlc-35yu.1","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:10:51Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu","title":"[EPIC] codegen: multi-backend architecture + Apache AGE (pgx v5) target","description":"Why: internal/codegen emits only neo4j-go-driver v5/v6, and neo4j specifics live in the shared core (prepare.go decode strategies are named after neo4j.GetRecordValue/dbtype shapes; render templates inline neo4j API). Goal: refactor so each backend is its own sub-package implementing the Generator seam, then add Apache AGE (Postgres extension, github.com/apache/age) as a second backend family.\n\nLocked design (grill session 2026-07-31):\n- Seam: internal/codegen keeps Input/File/Generator/errors + phases Z/A/B (admission, naming) with the Go-type mapping parametrized per backend; internal/codegen/neo4j and internal/codegen/age each implement Generator end-to-end and own their render layer. No fine-grained render-plugin interface — AGE emission differs too radically for holes shaped by n=1 backend.\n- AGE runtime: generated code imports github.com/jackc/pgx/v5 only (major-only import paths; consumer go.mod picks the minor, like neo4j today). DBTX interface satisfied by pool/conn/tx + WithTx(pgx.Tx). gqlc emits its own agtype decode helpers. The apache/age Go driver is disqualified for generated code: its parameter path is fmt.Sprintf interpolation into Cypher text (injection vector), it drags lib/pq + the ANTLR runtime into consumer builds, and it has been dormant since 2024 (verified from drivers/golang source 2026-07-31).\n- Registry: codegen.Registry + Entry{Key, New}, constructed explicitly at the CLI composition root — no init() registration, no package-level state. Keyed by config wire strings. Config keeps its closed Driver enum and parse-time errors; a parity test asserts registry keys == config.DriverValues(). New wire key: apache-age-pgx-v5 (const DriverApacheAgePgxV5).\n- Graph lifecycle: New(db DBTX, graph string) — graph name is a runtime arg (AGE analogue of neo4j database selection). EnsureGraph/DropGraph are methods on the generated Queries handle (join the reserved-identifier sweep; excluded from the Querier interfaces); package-level SessionInit matching pgxpool AfterConnect runs LOAD age; all emitted SQL fully qualifies ag_catalog.cypher / ag_catalog.agtype so search_path is never a session dependency. This makes parallel acceptance tests trivial: unique graph per test + EnsureGraph + Cleanup DropGraph, one shared container.\n- Type gaps: agtype has NO native temporal types (verified against src/include/utils/agtype.h enum: NULL/STRING/NUMERIC/INTEGER/FLOAT/BOOL/VERTEX/EDGE/PATH/ARRAY/OBJECT). Spike bead validates real temporal + parameter behavior against a live container FIRST; agreed fallback is faithful per-type encodings (timestamp=epoch-micros integer, date=ISO string, duration=ISO-8601 string, zoned=offset-preserving string) with the Go surface identical across backends; generate-time error reserved for types with no faithful encoding.\n- Tests: fixtures restructure to one dir + per-target golden subtrees with a manifest targets list (omitted = all registered); the suite iterates fixtures x registry, so registering a backend auto-enrolls it and a missing golden subtree is a red test. TestGoldenBuild stays the fully generic compile gate (compiles the whole nested module). Live tests: shared scenario battery against a test-local structural interface + per-backend adapters (generated method sets are signature-identical across backends); AGE arm uses unique-graph-per-scenario parallel isolation; joins the codegen_live nightly job; AGE image digest-pinned (gqlc-9u5 precedent). PR CI wall-time untouched.\n- Process: every implementation bead follows TDD (failing golden/fixture/live test first), one sibling worktree + stacked branch per bead, PR per bead.\n\nChild DAG: core-split -\u003e registry -\u003e fixture-migration -\u003e live-refactor; spike independent (parallel); C0 needs registry+fixtures; scalar-reads needs spike+C0; live-arm+CI needs live-refactor+scalar-reads; entity-columns and write-path need scalar-reads+live-arm; temporals need write-path+live-arm. Dialect-gap posture is unblocked backlog.","acceptance_criteria":"All child beads closed; gqlc generate with gen.go.driver apache-age-pgx-v5 produces a package that compiles (generic golden build gate) and passes the shared live scenario battery against a live AGE container.","notes":"=== SPIKE gqlc-35yu.5 OUTCOME (2026-07-31) — TWO LOCKED DECISIONS AMENDED ===\nOwner-approved amendments. Full evidence (818 lines) in gqlc-35yu.5.\nAGE image pinned for all downstream work:\n  apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb\n  (PostgreSQL 18.1, AGE 1.7.0)\n\n1. SEARCH_PATH — the locked claim is FALSE, decision reversed.\n   Locked text said: \"all emitted SQL fully qualifies ag_catalog.cypher /\n   ag_catalog.agtype so search_path is never a session dependency.\"\n   Empirically: full qualification is NECESSARY BUT NOT SUFFICIENT. AGE's own\n   Cypher-\u003eSQL transform emits BARE OPERATORS (=, \u003e, +, @\u003e) and does not emit\n   the OPERATOR(ag_catalog.=) form, so Postgres resolves them through\n   search_path. With ag_catalog off the path and every identifier qualified:\n   RETURN 1, bare MATCH and ORDER BY succeed, but\n     WHERE n.name = 'alice'      -\u003e ERROR: cannot cast agtype string to boolean\n     MATCH (n:P {name:'alice'})  -\u003e ERROR: operator does not exist: agtype @\u003e agtype\n     RETURN 1+1                  -\u003e same, for +\n   gqlc CANNOT fix this by emitting better SQL: the operators come from AGE's\n   transform, not from us.\n   AMENDED DECISION (owner, 2026-07-31): ag_catalog MUST be on the session\n   search_path, and SessionInit is the enforcement point. SessionInit runs\n   LOAD 'age', then SET search_path = ag_catalog + the caller's, then a CANARY\n   that exercises an operator (RETURN 1 = 1) and returns an error if it fails.\n   Rationale for the canary: SessionInit is the pgxpool AfterConnect hook, so\n   returning an error there keeps the connection OUT of the pool — the failure\n   surfaces at connection establishment (at startup for a pinging/pre-warming\n   consumer) rather than when the first WHERE clause runs. This matters because\n   AGE's failure mode is PARTIAL: simple queries succeed while operator queries\n   fail, so a misconfigured deployment otherwise looks healthy until first use.\n   Costs one round trip per new pool connection; pool conns are long-lived.\n   New(db, graph) is UNCHANGED — verification lives in SessionInit, so the\n   constructor still does no I/O and still returns no error.\n\n2. TEMPORAL ENCODING TABLE — 2 of 4 amended (spike mandate covered this).\n   Failed encodings round-trip correctly but SORT WRONG, which breaks\n   WHERE n.ts \u003e $param. Ordering, not round-tripping, is the real gate.\n     timestamp/instant | epoch-MICROS int64                    | CONFIRMED\n     date              | ISO 'YYYY-MM-DD' string, zero-padded  | CONFIRMED\n     duration          | total-MICROS int64                    | AMENDED\n     zoned datetime    | TWO int64 props: \u003cf\u003e epoch-micros     | AMENDED\n                       |   plus \u003cf\u003eOffset offset-seconds       |\n     calendar duration | no faithful encoding (months/years    | generate-time\n                       |   are not a fixed micro count)        |   error\n   duration as ISO-8601 string sorted P1D, PT10H, PT1H, PT2H, PT90M — and\n   WHERE v \u003e 'PT2H' returned only PT90M (1.5h): one false positive, two false\n   negatives. zoned as offset-preserving string returned an 07:00Z row for\n   WHERE v \u003e '...08:00+00:00'. A map {t,o} also sorts wrong (jsonb key order)\n   and would force gqlc to rewrite user ORDER BY; the FLAT SIDECAR keeps the\n   sort key as the property itself, so user ORDER BY passes through untouched.\n   date stays a string by owner decision (2026-07-31) despite agtype string\n   comparison using DB COLLATION rather than byte order — ISO dates are\n   fixed-width digits and hyphens, and readability in the stored graph wins.\n   No temporal casts exist in AGE (full pg_cast table captured in the spike);\n   the only temporal function is timestamp(), returning epoch-MILLIS.\n\n3. PARAMETER MECHANISM — selected: direct cypher('\u003cgraph\u003e', $$...$$, $1) over\n   the pgx extended protocol. age_prepare_cypher REJECTED: it latches query\n   text into the plan via connection-global state at Parse time, which pgx\n   statement caching schedules unpredictably. Server-log evidence: parse/bind/\n   execute are separate phases and the logged statement text ends in the\n   literal $1 at every phase, with the payload appearing only under\n   DETAIL: Parameters. A Cypher-injection canary table survived intact.\n   Note: pgx SimpleProtocol — the only interpolating mode — is the one mode\n   AGE rejects outright, so the unsafe path fails closed.\n\n=== AMENDMENT 3 (owner, 2026-08-01) — FIXTURE ENROLLMENT IS EXPLICIT, NOT AUTOMATIC ===\nLocked text said: \"fixtures restructure to one dir + per-target golden subtrees with\na manifest targets list (omitted = all registered); the suite iterates fixtures x\nregistry, so registering a backend auto-enrolls it and a missing golden subtree is a\nred test.\"\n\nThe auto-enroll half is SUPERSEDED. Taken literally at migration time it obliges a v6\ngolden subtree for the 67 fixtures that carry a v5 golden only (~258 new files, ~12k\nlines differing by import path and DriverWithContext -\u003e Driver), and ~289 AGE subtrees\nthe moment AGE registers at .6 — which would also put master red from .6 through .11,\nviolating the \"each branch independently mergeable\" rule in AGENTS.md.\n\nAMENDED DECISION: targets is a REQUIRED manifest field; there is no \"omitted = all\nregistered\" default. Enrollment is an explicit, per-bead act.\n  - .3 migrates with coverage unchanged: the 8 _v6 twins collapse into their base\n    fixture with targets [\"neo4j-go-v5\",\"neo4j-go-v6\"]; the other 67 carry\n    targets [\"neo4j-go-v5\"]. ZERO new golden files — pure relocation.\n  - .6-.11 each add \"apache-age-pgx-v5\" to the target lists of the fixtures that\n    bead's capability covers, and own those goldens.\nWhat survives: a target enrolled in a fixture with a missing golden subtree is still a\nRED test, and TestGoldenBuild still compiles every subtree. The gate is intact; only\nthe enrollment trigger changed from implicit to explicit.\nSEQUENCING PLAN (2026-08-01), after .3 merged as PR #642. Remaining chain is near-serial: .6 -\u003e .7 -\u003e .8 (also needs .4) -\u003e {.9 ∥ .10} -\u003e .11 -\u003e .12. Parallel slots taken: .4 ∥ .6 in flight now (worktrees gqlc-35yu4-live, gqlc-35yu6-age). Next slot is .9 ∥ .10 once .8 lands; .12 is held for last by choice, see its note. Epic is 4/12.","status":"open","priority":2,"issue_type":"epic","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:10:31Z","created_by":"areqag","updated_at":"2026-08-01T03:29:02Z","external_ref":"https://github.com/areqag/gqlc/issues/633","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jwuw","title":"bd pull re-derives in_progress from started_at, silently reverting a deliberate 'open'","description":"`bd update \u003cid\u003e --status open` does not stick for any bead that was ever\nclaimed. The next `bd-gh-sync pull` re-derives `in_progress` from the bead's\nnon-nil `started_at`, and the next `chore(bd)` export PR then silently\nreverts master.\n\n## Evidence\n\n`gqlc-4np` was deliberately set to `open` last session, pending an ADR call,\nand committed to master that way. It came back as `in_progress`.\n\nIt is not coming from GitHub: `GH#503` is OPEN with zero assignees, and\nGitHub has no in-progress state to map from. It is bd reconstructing the\nstatus locally from `started_at = 2026-07-28T09:55:09Z`.\n\nProof it was the pull and not a human: `gqlc-4np.updated_at` is\n`2026-07-31T00:05:31Z`, byte-identical to the epoch embedded in the ghost\nbead id the SAME pull minted (`gqlc-1785456331014`). The hook reported\n\"Pulled 2 issues (1 created, 1 updated)\" -- this is the updated one.\n\n## Why it cannot be worked around\n\n`bd update` exposes no flag to clear `started_at`. `--defer` and `--due` both\ntake an empty string to clear; `started_at` has no equivalent. So `open` is\nnot a reachable steady state for a previously-claimed bead.\n\nWorked around for now by using `blocked`, which carries the same intent\n(\"not being worked, waiting on a decision\") and sits outside the\nre-derivation path. Whether `blocked` also gets re-derived is untested --\nthe next pull will tell.\n\n## Review implication\n\nDiff every `chore(bd)` export record by record, parsing the +/- JSON lines\nand printing `id | status`. This revert is one line out of 234 and is\ninvisible in a diffstat. Cf. memory `patch-check-does-not-detect-reverts`.\n\nMemory: `bd-pull-rederives-in-progress-from-started-at`.\n\n## Correction 2026-07-31: `blocked` is NOT a workaround\n\nThe original text above proposed `blocked` as a status that carries the same\nintent and sits outside the re-derivation path. That was asserted without\ntesting and it is **false**. Measured, both arms, deterministic:\n\n    bd update gqlc-4np --status blocked ; .githooks/bd-gh-sync pull  -\u003e in_progress\n    bd update gqlc-4np --status open    ; .githooks/bd-gh-sync pull  -\u003e in_progress\n\nThe re-derivation overrides *any* non-closed status. For a bead that was ever\nclaimed, `in_progress` is the only reachable steady state short of closing it.\n\nPR #612's body and commit message carry the same wrong claim; this bead is the\ncorrection of record.\n\nCaught only because `just init` re-activated the local hooks, so `post-merge`\nstarted firing the pull again and the revert reappeared within one merge. It\nhad been latent while `core.hooksPath` was pointing at an empty directory.","notes":"=== SAME CLASS, DIFFERENT TRIGGER: a git-side revert of the JSONL rolls the DB\nback silently. Observed 2026-08-01 while merging gqlc-35yu.7's ledger PR. ===\n\nThis bead is about `bd pull` re-deriving status. I hit the mirror image of it\nthrough a plain git operation, with no bd sync command involved.\n\nSequence, all of it routine ledger hygiene:\n  1. bd update gqlc-35yu.8 --status in_progress   -\u003e DB: in_progress. Correct.\n     (an auto-export wrote AND staged .beads/issues.jsonl)\n  2. git checkout master  -\u003e refused: local changes to .beads/issues.jsonl\n  3. I inspected the staged diff, saw the ONLY delta was\n     gqlc-35yu.8 open -\u003e in_progress, i.e. state already in the DB, and\n     discarded it with `git restore --staged --worktree .beads/issues.jsonl`\n     on the reasoning that the Dolt DB is authoritative and the JSONL is a\n     passive export, so the file could be regenerated.\n  4. merged the PR, pulled master, re-ran `bd export -o .beads/issues.jsonl`\n  5. The export wrote status \"open\". `bd show` confirmed the DB itself had\n     reverted to OPEN -- while still carrying `Started: 2026-08-01`.\n\nSo the export is NOT passive in the direction the docs imply: reverting the\nworking-tree file propagated back into the DB. started_at survived and status\ndid not, which is the same torn state this bead already describes, reached\nwithout `bd pull`.\n\nWHY IT MATTERS MORE THAN IT LOOKS: the reasoning in step 3 is correct given a\ngenuinely passive export, and it is exactly what anyone will do when git blocks\na branch switch on this file -- which happens on EVERY bead, because closing a\nbead and starting the next one both dirty it. The failure is silent: no\nwarning, and the next `bd export` cheerfully writes the reverted value into the\ncommitted ledger, so the wrong state can reach master looking freshly exported.\nI only caught it because I diffed the regenerated file against what I expected.\n\nWORKAROUND until fixed: after any git operation that reverts, stashes or checks\nout .beads/issues.jsonl, re-assert status with `bd update` and verify with\n`bd show` before exporting. Do not trust an export taken straight after a\nworking-tree revert.\n\nWorth deciding as part of the fix whether the JSONL should be import-on-change\nat all. If it is meant to be an export, the DB should ignore it; if it is meant\nto be bidirectional, `git restore` is an unguarded write path into the DB and\nthe guard (internal/tools/bdguard) does not cover this direction.\n=== INVESTIGATED 2026-08-01 (branch fix/bd-gh-sync-state-fidelity): the stated\nmechanism does NOT reproduce on bd 1.0.4, and the opposite one does ===\n\nThis bead says the pull re-derives in_progress from a non-nil started_at, and\nthe 2026-07-31 correction escalates that to \"the re-derivation overrides ANY\nnon-closed status ... in_progress is the only reachable steady state\". Both\nclaims fail against measurement.\n\nDISCONFIRMING EVIDENCE 1 -- the live corpus. Four beads currently sit with\nstarted_at set and a non-in_progress, non-closed status, some for weeks:\n  gqlc-do1    open    started 2026-07-12\n  gqlc-h9n.33 open    started 2026-07-28\n  gqlc-h9n.1  blocked started 2026-07-28\n  gqlc-23e    blocked started 2026-07-02\nIf any non-closed status were overridden, none of these could persist. The\ncorrection claim that `blocked` is also re-derived is contradicted twice over\nby gqlc-h9n.1 and gqlc-23e.\n\nDISCONFIRMING EVIDENCE 2 -- isolated scratch DB (/tmp, `bd init`, no GitHub,\nno shared state). Create bead, --claim (-\u003e in_progress, started_at set),\n`bd update --status open` -\u003e open WITH started_at retained. Export, then\nimport that file back: status stays open. No re-derivation.\n\nWHAT DOES REPRODUCE, deterministically, is the reverse direction -- and it is\ngqlc-63y third repro, not this one:\n  DB set to in_progress; issues.jsonl on disk says open; then\n    bd list            -\u003e in_progress survives (auto-sync does NOT import)\n    bd hooks run post-merge -\u003e status becomes OPEN, started_at retained\nSo the clobber is the JSONL replay inside `bd hooks run post-merge`, it goes\nin_progress -\u003e open, and it produces the torn open+started_at pair. The two\nbeads are NOT mutually contradictory observations of one bug; 63y direction is\nreal and this bead direction is not reproducible. Treat this as the stale one.\n\nCorollary: open+started_at is a NORMAL bd-producible state (see evidence 2),\nnot the signature of a torn write, so it cannot be used as a detector.\n\nMANAGER FORK (remove the re-derivation vs gate it, teaching the sync to record\nwhat it last wrote): declined as posed, because it presumes a re-derivation\nthat does not exist here. It would also be unreachable from this script -- any\nsuch logic lives inside the bd binary, and `.githooks/bd-gh-sync` cannot gate\nit. Nothing is removed, so nothing is lost: the \"started but never updated\"\nsignal the fork worried about is still carried by started_at itself.\n\nWHAT SHIPPED INSTEAD, which serves this bead intent without guessing at a\ncause: a postcondition. After the scoped pull, bd list is re-read and any bead\nthat was HELD OUT of the pull but whose status moved anyway is reported by\nname with the exact `bd update \u003cid\u003e --status \u003cold\u003e` needed to re-assert it.\nThat is agnostic about which component moved it, so it catches the JSONL\nreplay above even though this script never invokes it. Pinned by tests in\nboth arms (moved -\u003e warns; unchanged -\u003e silent).\n\nNOT FIXED, and out of reach of this branch: `bd hooks run post-merge` still\nreplays the JSONL over newer DB state. Note it is currently UNREACHABLE in\nthis repo -- core.hooksPath is .githooks, and nothing in .githooks, the\njustfile or .claude/settings.json chains to .beads/hooks/ or runs `bd hooks\nrun`, so the beads-managed hooks are inert. That is why the postcondition\nwarns rather than blocks. If those hooks are ever re-activated, the underlying\nreplay needs its own bead against bd itself.\nMANAGER CORRECTION: this bead's stated mechanism does not reproduce. I verified the refutation myself.\n\nThe bead claims `bd pull` re-derives `in_progress` from a non-nil `started_at`, so `--status open` cannot stick for a previously-claimed bead. If that were true, no bead could sit in open-with-started_at for any length of time. Four do, and two have for over a month:\n\n  gqlc-h9n.1    blocked  started=2026-07-28  updated=2026-07-28\n  gqlc-h9n.33   open     started=2026-07-28  updated=2026-07-28\n  gqlc-do1      open     started=2026-07-12  updated=2026-07-14\n  gqlc-23e      blocked  started=2026-07-02  updated=2026-07-19\n\ngqlc-do1 has been open with started_at set since 12 July and gqlc-23e blocked since 2 July, across many pulls. The implementer independently reproduced the negative in an isolated scratch DB: claim, then --status open, leaves it open with started_at retained, and re-importing that export re-derives nothing.\n\nNote also that the bead's own Correction says `blocked` sits outside the re-derivation path and is a usable workaround; two of the four above are blocked, but two are plain `open`, so `open` is not special either.\n\nWHAT ACTUALLY HAPPENS, and it is the opposite direction. `bd hooks run post-merge` replays the JSONL over newer DB state, reverting in_progress -\u003e open while KEEPING started_at. That is deterministic and was reproduced. So the status damage is real; the arrow was drawn backwards, and the bead named started_at re-derivation for what is actually stale-export replay.\n\nHOW THE ORIGINAL EVIDENCE MISLED. The bead's strongest-looking argument was that gqlc-4np.updated_at is byte-identical to the epoch embedded in the ghost bead id minted by the same pull (gqlc-1785456331014). That correctly proves the same pull touched both. It does not prove WHY the status moved — identical timestamps establish co-occurrence, not mechanism. The ghost-minting is gqlc-onji; the status move is replay. One pull, two effects, and the shared timestamp says nothing about which caused which.\n\nI RESTATED THIS BAD DIAGNOSIS TO THE IMPLEMENTER as settled fact when I added jwuw to their scope, including the \"no flag to clear started_at\" framing that follows from it. They checked instead of believing me, and were right to. Recording that here rather than quietly dropping it: the bead was filed from a real symptom and a plausible story, and the story was wrong.\n\ngqlc-63y and gqlc-jwuw were flagged as mutually contradictory. That is resolved: 63y's direction is the real one.\n\nDISPOSITION: not fixed as filed, because there is nothing to fix at the stated site. The implementer added a POSTCONDITION instead — held beads must return unchanged from a pull, warning by name with the re-assert command. That is the right shape for a bead whose mechanism was misidentified: it pins the observable property the bead actually cared about without encoding the wrong cause. Whether that fully retires this bead is for the review round to say.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T00:13:26Z","created_by":"areqag","updated_at":"2026-08-02T04:29:27Z","closed_at":"2026-08-02T04:29:27Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/615","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-w29n","title":"gqlc-fex has two GH mirrors (#597/#598) plus a sync-created ghost bead — cleanup needs an owner decision","description":"Bead `gqlc-fex` acquired two GitHub mirrors, and the pull sync then manufactured\na second bead for one of them.\n\n## Timeline (2026-07-30, all UTC)\n\n- 00:49:40  GH #597 created — short body ending \"Bead: gqlc-fex\".\n- 00:52:02  GH #598 created — same title, long body (TDD plan, acceptance\n            criteria). `gqlc-fex.external_ref` points here, orphaning #597.\n- 00:53:41  PR #599 merged carrying `Closes #597` → #597 closed at 00:53:42.\n- 00:53:43  Bead `gqlc-1785373131289-1-b6686b0c` closed — a ghost bead the\n            GH→bd pull created for #597, whose description is #597's body,\n            which itself reads \"Bead: gqlc-fex\".\n- 00:59:30  #598 closed separately.\n\n## Current state\n\nBoth GH issues are closed and both beads are closed, so nothing is incorrectly\nopen — the residue is cosmetic but misleading:\n\n- Two GH issues describe one bug, neither marked as a duplicate of the other.\n- The ghost bead is now in `.beads/issues.jsonl` (landed by #609, deliberately,\n  because the export must mirror the Dolt DB rather than be hand-edited).\n- PR #599's `Closes #597` did not match `gqlc-fex`'s mirror (#598). The\n  check-pr-closes gate did not catch it: `gqlc-fex` was not yet in the\n  committed export, so the gate took its \"unknown bead — PASS (don't block on\n  stale export)\" branch. That escape hatch is load-bearing for same-day beads\n  and is worth re-examining.\n\n## Likely cause\n\nThe `--prefer-github` amendment ordering recorded in memory\n`bd-gh-sync-reverts-descriptions`: the description was amended and a second\nissue was opened instead of editing the first. This is plausibly downstream of\n`gqlc-63y` — the divergence guard that would have flagged the amendment never\nran.\n\n## Decision needed\n\n1. Delete the ghost bead from the Dolt DB, or keep it as an inert record?\n   Deletion is destructive and propagates, so it needs an owner call.\n2. Mark #597 or #598 as a duplicate on GitHub so the pair reads correctly.\n3. Decide whether the gate's unknown-bead PASS should stay, given it is what\n   let the wrong `Closes` number through.\n","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-30T23:33:43Z","created_by":"areqag","updated_at":"2026-07-31T01:08:15Z","started_at":"2026-07-30T23:51:48Z","closed_at":"2026-07-31T00:15:31Z","close_reason":"Ghost bead deleted and the drop declared (PR #611). #597 closed as DUPLICATE of #598; gqlc-fex intact on #598. Deletion is not permanently durable — the pull re-mints a ghost for any touched orphan issue; durable options filed as a P1 follow-up.","external_ref":"https://github.com/areqag/gqlc/issues/616","dependencies":[{"issue_id":"gqlc-w29n","depends_on_id":"gqlc-63y","type":"discovered-from","created_at":"2026-07-30T19:33:42Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-w4al","title":"check-pr-closes gate does not re-evaluate on body edits — unre-triggerable, and bypassable after opening","description":"The `check-pr-closes` gate in `ci.yml` (tidy job) reads the PR body from the\nworkflow event payload:\n\n    PR_BODY: ${{ github.event.pull_request.body }}\n\nand `ci.yml` declares `on: pull_request:` with no `types:`, which defaults to\n`[opened, synchronize, reopened]` — `edited` is not included.\n\nTwo consequences, found while fixing PR #600 on 2026-07-30.\n\n## 1. A correct fix cannot re-trigger the gate\n\nAdding the missing `Closes #N` to the body fires no event, so the check stays\nred. A re-run does not help either: a re-run replays the original event\npayload, so the gate re-reads the stale body. The only ways out are pushing a\ncommit or closing/reopening the PR. Neither is discoverable from the failure\nmessage, which just says \"Add 'Closes #598' to the PR body\".\n\n## 2. The gate is bypassable after the fact\n\nA PR can carry `Closes #N` at open time, pass the gate, then have the line\nedited out of the body. No `edited` event, no re-evaluation, and the PR merges\ngreen with the GH issue left open — the exact failure the gate exists to\nprevent.\n\n## 3. The gate assumes any PR naming a bead is closing it\n\nThe bead id is taken from the branch name when the body has no `Bead:` line,\nand the gate then demands a `Closes #N`. That is wrong for a PR that changes a\nbead's status without resolving it. PR #609 reopened `gqlc-4np`; had the branch\nbeen named after the bead, the gate would have demanded `Closes #503` and\nclosing that issue would have been precisely the wrong outcome. Working around\nit by choosing a branch name that avoids the id is not a control.\n\n## Fix direction\n\nAdd `types: [opened, synchronize, reopened, edited]`, or have the step fetch\nthe body from the API at run time rather than trusting the payload. The API\nread also fixes consequence 2, since a re-run would then see current state.\nSeparately, give the gate a way to express \"this PR touches the bead but does\nnot resolve it\" so item 3 does not depend on branch naming.\n\n## Acceptance\n\n- Editing a PR body to add the missing `Closes #N` turns the check green with\n  no push and no close/reopen.\n- Removing `Closes #N` from a previously-passing PR body turns the check red.\n- A status-change PR can name its bead without being forced to close it.\n","notes":"PR #867 https://github.com/areqag/gqlc/pull/867 — head e9119f75, branch fix/pr-closes-gate-reevaluates, worktree /home/antranig/Developer/gqlc/gqlc-w4al. AWAITING MERGE (orchestrator holds merge authority; do not merge from this session).\n\nLIVE WITNESS of the acceptance property, run on PR #867 itself, head unchanged at e9119f75 throughout (no push, no reopen):\n 1. baseline: all 8 required contexts green, mergeStateStatus CLEAN.\n 2. 07:21:00Z removed the 'Closes #617' line -\u003e run 32005420996 created 07:21:03Z, same SHA.\n 3. its tidy (job 95313776365) FAILED: \"ERROR: PR body is missing 'Closes #617'\".\n 4. with the line still absent, re-ran the PRE-EDIT run's tidy ('gh run rerun 32004817300 --job 95312044713') -\u003e job 95313989391, run_attempt 2, FAILED, same message. Attempt 1 of that same job had PASSED on the frozen payload. This is the half 'edited' alone would not cover.\n 5. mergeStateStatus BLOCKED; both tidy check runs on the SHA were failure.\n 6. 07:22:56Z restored the body -\u003e run 32005559232 created 07:22:57Z; tidy (job 95314195022) SUCCESS, '[check-pr-closes] gqlc-w4al -\u003e Closes #617 (ok)'.\nAcceptance property HOLDS: a body that loses its Closes line is not mergeable green, and a body edit alone both breaks and repairs the verdict.\n\nCERTIFIED at e9119f75 (head), detached worktree: fmt-check, tidy-check, actionlint, test, lint all exit 0. Docker down locally so test-codegen-live* not run.\n\nRESIDUE: gqlc-1ekq (consequence 3, gate assumes bead-naming == closing), gqlc-2rli (full-re-run cost / separate-workflow shape), gqlc-xdd0 (NEW: selective tidy re-run can leave lint/test/codegen-fence 'skipped' = pass at a SHA where they never ran; this change is what makes tidy re-passable at an unchanged SHA).\nMEMORY: bd remember workflow-closing-issue-refs-index-lags-body-edit.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-30T23:33:26Z","created_by":"areqag","updated_at":"2026-08-17T07:58:29Z","closed_at":"2026-08-17T07:58:29Z","close_reason":"Merged as c575ae8b (PR #867). GH #617 closed by the Closes line. Both halves shipped: ci.yml gains types:[...,edited] and the check-pr-closes step fetches the body from the API at run time; tidy gains pull-requests: read. Consequence 3 (a status-change PR forced to Closes its bead) is NOT closed here — deferred to gqlc-1ekq / GH #862, named in both the PR body and the commit message. Independent review PASS, zero blockers, verdict at /tmp/factory/verdict-867.md.","external_ref":"https://github.com/areqag/gqlc/issues/617","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-25k","title":"schema/gql: which endpoint's rejection wins when both edge endpoints are defective is unpinned","description":"A mutation sweep over `internal/schema/gql/listener.go` (81 cases, 73 caught) left one\nsurvivor that is a genuine coverage gap rather than a defensive tripwire.\n\n## The survivor\n\n`EnterEdgeTypePattern` reads its two endpoints in written source-then-destination\norder, short-circuiting on the first failure (listener.go:353-365):\n\n```go\nif r := directed.EdgeTypePatternPointingRight(); r != nil {\n        src, refErr = sourceRef(r.SourceNodeTypeReference())\n        if refErr == nil {\n                dst, refErr = destRef(r.DestinationNodeTypeReference())\n        }\n        filler = r.ArcTypePointingRight().EdgeTypeFiller()\n}\n```\n\nSwapping the two so the destination is read first — keeping the short-circuit\nintact, so this changes only *which* rejection is reported when both ends are\ndefective — leaves the entire suite green.\n\n## Why nothing sees it\n\n`sourceRef`/`destRef` return only what `fillerLabels` returns, which is one of two\nsentinels: `ErrEndpointFillerHasProperties` and `ErrEndpointFillerImpliesLabels`.\nEvery existing fixture has a defect on exactly one end, or the same defect on both:\n\n- `invalid/endpoint_filler_properties.gql` — destination only, HasProperties\n- `corpus/18.3-edge-type/endpoint_key_label_set.gql` — source only, ImpliesLabels\n- `TestEndpointFillerRejectsProperties`' \"both sides carry properties\" case — both\n  ends, but the *same* sentinel, so the order cannot be observed\n\nWith one end defective the short-circuit reports that end whichever way round the\nreads go. With both ends carrying the *same* sentinel the answer is that sentinel\neither way. The order is only observable when the two ends carry *different*\nsentinels, and no file does. The \"both sides\" case above is the near miss: it was\nwritten for this construct and stops one sentinel short.\n\n## The behaviour, confirmed by probe\n\nThe source's rejection wins, and it wins *by role, not by written position*:\n\n| written                                              | reported          |\n|------------------------------------------------------|-------------------|\n| `(:Person =\u003e :Employee) -[:E]-\u003e (:Post { x :: INT })` | ImpliesLabels (source) |\n| `(:Person { x :: INT }) -[:E]-\u003e (:Post =\u003e :Article)`  | HasProperties (source) |\n| `(:Post { x :: INT }) \u003c-[:E]- (:Person =\u003e :Employee)` | ImpliesLabels (source) |\n\nThe last row is the one worth pinning. In a left-pointing arc the grammar reports\nthe *rightmost written* endpoint as the source, so \"source first\" and \"leftmost\nfirst\" disagree, and the code follows the role. That is the right answer — the\nreported end is the same end regardless of which direction the author wrote the\narc in, so the diagnostic does not move when the edge is respelled — but nothing\nsays so and nothing would notice it changing.\n\nThis is the same class as the check ordering in `EnterEdgeTypePhrase` and\n`EnterEdgeTypePattern`, where the mismatch sentinel deliberately fires before the\ndirected-only bail so the message names the author's actual mistake. That ordering\ncarries a comment arguing for it and tests that go red when it is swapped\n(sweep cases 43 and 44, both caught). This one has neither.\n\n## Proposed fix\n\nTest-only; no product change. Extend the endpoint-filler coverage with the\nmixed-sentinel pairing on both arc spellings, asserting the source's sentinel is\nthe one reported and the other is *not* — a bare `ErrorIs` on the winner would\nstill pass if both were somehow reported. Add the sentence about role-vs-position\nto `sourceRef`/`destRef` or to the pattern collector.\n\n## Provenance\n\nSweep script `/tmp/gqllist_sweep.py`, results `/tmp/gqllist_sweep.json`, case 56.\nThe other seven survivors were each instrumented with a logging probe and run\nagainst the whole suite; all seven are defensive tripwires or equivalent mutants\nand are correct as written:\n\n- `SyntaxError`'s `tok.GetText() != \"\"` — 0 hits on an empty-text token, 4 on a\n  non-Token offending symbol, so the `ok` half is load-bearing and the text half\n  cannot fire\n- `SyntaxError`'s `return` after the token branch — masked by `fail()` idempotence\n- `walk`'s pre-walk `l.err != nil` bail — taken 6 times, but removal is invisible\n  because `fail()` is first-wins; it guards against a panic on an unreliable tree\n- `unsupportedSource`'s `src == nil` arm and its sentinel — 0 hits, error recovery only\n- `unsupportedSource`'s trailing `return ErrUnsupportedSource` — 0 hits, unreachable\n  by construction: `graphTypeSource` has exactly three alternatives (GQL.g4:347-350)\n  and the guard tests for the third, so it is the documented forward-compat arm\n- `nestedDepth` starting at `ctx` rather than its parent — 0 hits; no caller passes\n  a `nestedGraphTypeSpecification` context, so it is an equivalent mutant","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T16:42:32Z","created_by":"areqag","updated_at":"2026-07-28T23:58:03Z","started_at":"2026-07-28T16:42:39Z","closed_at":"2026-07-28T23:58:03Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/585","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vb9","title":"schema/gql: nothing reaches fillerLabels' trailing nil bail; ({ }) is the only spelling that does","description":"`fillerLabels`' trailing bail (internal/schema/gql/edgetype.go:125-127) was reached\nby no test:\n\n    if impliedLabels == nil {\n        return nil, nil\n    }\n\nNeutralising it — `impliedLabels == nil \u0026\u0026 false` — leaves the whole suite green:\n\n    go build ./...            ok\n    go test ./internal/...    ok\n\nIt is not dead. Falling through calls `impliedLabels.LabelSetPhrase()` on a nil\ninterface, so any input taking this path would panic rather than fail a test.\n\nTHE ONE SPELLING THAT REACHES IT is `({ })`, an endpoint filler that is an empty\nproperty block and nothing else. The path is narrow, which is why it was missed:\n\n  - `f == nil` (`()`, no filler at all) returns from the guard at :96, not here.\n    endpoint_no_filler.gql covers that.\n  - `({ x :: INT })` is rejected at :112 as ErrEndpointFillerHasProperties before\n    reaching this line. invalid/endpoint_filler_properties.gql covers that.\n  - `(:Person { })` — the label-bearing sibling — leaves impliedLabels non-nil and\n    returns at :128. corpus 18.3-edge-type/endpoint_empty_property_block.gql\n    covers that, and it is the near miss: it was written for this construct and\n    stops one label short of this line.\n\nSo only an EMPTY block with NO label lands here. Confirmed by probe:\n`(:Person) -[:AUTHORED]-\u003e ({ })` parses and rejects with ErrUnknownEndpoint,\nwhich is endpoint_no_filler.gql's outcome reached through the other arm.\n\nFOUND BY the 21-case mutation sweep of edgetype.go + nodetype.go (2026-07-28),\nwhich caught 18. The two other survivors are inert by grammar and are recorded in\nbd memory rather than filed:\n\n  edgetype.go:106  `spec != nil` — nodeTypePropertyTypes has exactly one\n                   mandatory child, propertyTypesSpecification (GQL.g4:1542-1544)\n  nodetype.go:44   `spec == nil` — all three labelSetPhrase alternatives yield a\n                   LabelName or a labelSetSpecification (GQL.g4:1679-1682)\n\nTHE FIX is a corpus entry, not an invalid/ fixture. The reading of a label-less\nendpoint is open — endpoint_no_filler.gql records both, and gqlc-h9n.35 holds the\nquestion — so if it is ever settled the \"any node type\" way, `()` and `({ })` flip\nto resolves together and the entry moves with it. An invalid/ fixture would\nsurvive that commit as an orphan.\n\nSECOND FINDING, forced by the first. Adding the corpus entry turned\nTestCorpusResolvingCarriers red, and the failure is a real one rather than\nbookkeeping: `nodeTypeImpliedContent#2` was filed in declinedCarriers under\nErrUnnamedNodeType, and the register requires every carrier of a name to reject\nwith the group's sentinel. The new file carries it and rejects with\nErrUnknownEndpoint.\n\nBoth are true, and neither is a misfiling. The alternative is a filler that is a\nproperty block and nothing else; declared as a node type it has no key label set\n(ErrUnnamedNodeType), and used as an endpoint it references the type with the\nempty key label set, which ADR 0018 makes undeclarable (ErrUnknownEndpoint). One\nmissing identity, reported from the two sides it can be seen from.\n\nThe register's own doctrine has an escape for a name carried by two families —\nfile it under the class — but that was written for ADR 0019's five value-type\nfamilies, which have ErrUnsupportedType. These two have no class, and inventing\none to satisfy a test register would be backwards.\n\nSo: a `declinedAlso` map keyed by name, one row, guarded four ways so it cannot\nbecome a laundering channel. Each guard verified by breaking it:\n\n  row deleted                     -\u003e carrier unaccounted (the original failure)\n  row for an unregistered name    -\u003e \"accounts for nothing\"\n  row repeating the group sentinel-\u003e rejected\n  row naming an unexercised error -\u003e carrier unaccounted\n\nIt is a map rather than a field on declinedName so the fifty-odd existing\ntwo-field positional literals stay untouched, and so a second row has to be\nargued rather than appended.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T16:13:00Z","created_by":"areqag","updated_at":"2026-07-28T16:15:54Z","started_at":"2026-07-28T16:13:03Z","closed_at":"2026-07-28T16:15:54Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/581","dependencies":[{"issue_id":"gqlc-vb9","depends_on_id":"gqlc-0ri","type":"related","created_at":"2026-07-28T12:13:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-9gz","title":"resolver: ErrOutOfR0Scope has 18 emitters and 5 fixtures — path and unwind bindings rejected untested","description":"An audit of every ErrOutOfR0Scope emitter, prompted by gqlc-bef item 4. The\nsentinel has eighteen emitters and five negative fixtures, and the five map 1:1\nonto the five proven sites — so thirteen emitters are unproven, and the sentinel\nreads as fully covered anyway.\n\nMethod: swap ErrOutOfR0Scope for a different sentinel at one site at a time and\nrun the suite. CAUGHT means some fixture reaches that site; SURVIVED means none\ndoes.\n\n    CAUGHT     scope.go:701   refProjectionType property on var-length edge\n    CAUGHT     scope.go:933   PropertyUseWitness property on var-length edge\n    CAUGHT     resolve.go:464 untyped edge\n    CAUGHT     resolve.go:791 list-of-nodes projection\n    CAUGHT     resolve.go:793 list-of-edges projection\n\n    SURVIVED   scope.go:622   wildcard-expanded name resolves to nothing\n    SURVIVED   scope.go:642   unknown projection variant\n    SURVIVED   scope.go:680   refProjectionType out-of-scope ref\n    SURVIVED   scope.go:930   PropertyUseWitness out-of-scope ref\n    SURVIVED   scope.go:983   WitnessUse unknown ExprUse position\n    SURVIVED   scope.go:986   WitnessUse unknown Use variant\n    SURVIVED   resolve.go:24  empty branches\n    SURVIVED   resolve.go:133 empty parts\n    SURVIVED   resolve.go:200 unsupported binding kind\n    SURVIVED   resolve.go:394 no branch scope tables\n    SURVIVED   resolve.go:406 Use Branch index out of range\n    SURVIVED   resolve.go:453 PropertyUse Part index out of range\n    SURVIVED   resolve.go:917 unknown Effect variant\n\nTestSentinelReachability (resolver_test.go:499) asserts every canonical sentinel\nhas at least one negative fixture. It passes here while 13 of 18 emitters are\nuntested, because it is per-sentinel, not per-emitter.\n\n## The one that was a real gap, and why the obvious fixture does not pin it\n\nresolve.go:200 is the default arm of the Binding type switch. Five binding kinds\nexist (query.go: node, edge, call, path, unwind); the resolver handles three and\nrejects path and unwind as out of R0 scope. Both are ordinary Cypher —\n`MATCH p = ...` and `UNWIND ... AS x` — and the parser emits them, verified\ndirectly. Neither had a fixture.\n\nWriting the obvious ones does not help:\n\n    MATCH p = (a:Person)-[:KNOWS]-\u003e(b:Person) RETURN p\n    UNWIND [1, 2, 3] AS x RETURN x\n\nBoth pass, and both are useless. Delete the rejection arm and they *still* fail\nwith ErrOutOfR0Scope, because p / x are then unbound and the projection walk\nraises the same sentinel from scope.go:680. Measured: the sentinel-swap mutation\nis caught, but dropping the arm outright survives. The guard and its own\ndownstream fallthrough collide on one sentinel, so errors.Is cannot separate\nthem.\n\nThe fix is in the fixture. Exercise the rejection without naming the rejected\nbinding, so that removing the guard makes the query *succeed* rather than fail\nidentically:\n\n    MATCH p = (a:Person)-[:KNOWS]-\u003e(b:Person) RETURN a\n    MATCH (a:Person) UNWIND [1, 2, 3] AS x RETURN a\n\nBoth mutations are then caught by exactly these two fixtures.\n\nThis also explains scope.go:680's SURVIVED above: with :200 intact, nothing\nreaches :680, and with :200 relaxed, :680 covers for it. They mask each other\nthrough the sentinel, across two files. See bd memory\nsweep-negative-fixture-must-not-name-rejected-binding.\n\n## The other twelve\n\nTen are defensive tripwires on closed sums or resolver-internal invariants, and\nshould not be re-filed:\n\n  scope.go:642, :983, :986 and resolve.go:917  default arms of closed type\n        switches / a position enum. Unreachable without adding a variant.\n  scope.go:622  the comment says it outright — a name in scopeOrder that\n        resolves to nothing is a resolver-side bug.\n  resolve.go:24, :133  empty branches / empty parts; the parser never emits an\n        empty query.\n  resolve.go:394, :406, :453  index and table-presence checks on structures the\n        resolver itself builds.\n\nTwo remain genuinely open and are the same guard-chain shape as gqlc-bef item 3:\n\n  scope.go:930  PropertyUseWitness's out-of-scope arm, unreachable because\n        WitnessUse gates on Contains() first (:963). Relax the gate and the arm\n        starts mattering, with no test on it.\n  scope.go:680  reachable only once :200 is relaxed, as above. Pinning it on its\n        own needs either a message assertion or a distinct sentinel — that is\n        gqlc-ka1's subject and wants the ADR decision, so it is not done here.\n\n## Fixed here\n\ntest/data/resolver/invalid/path_binding.cypher and unwind_binding.cypher, their\nschema.mapping.json entries (social_r3.gql, already present) and their\ninvalidFixtures rows. No production change.\n\nVerified 1:1 — both the sentinel-swap and the arm-dropped mutation at\nresolve.go:200 are caught by these two fixtures and by nothing else.\n\nRelated: gqlc-bef items 3 and 4; gqlc-ka1.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:36:21Z","created_by":"areqag","updated_at":"2026-07-28T15:40:36Z","started_at":"2026-07-28T15:36:25Z","closed_at":"2026-07-28T15:40:36Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/577","dependencies":[{"issue_id":"gqlc-9gz","depends_on_id":"gqlc-bef","type":"related","created_at":"2026-07-28T11:36:25Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-9gz","depends_on_id":"gqlc-ka1","type":"related","created_at":"2026-07-28T11:36:26Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0ul","title":"resolver: CloseEdges' Phase C deferred-close error is swallowable — no fixture reaches the second emitter","description":"CloseEdges closes edges in two passes: Phase A2 for edges whose endpoints are\nalready typed, and Phase C for the edges deferred because an endpoint was still\nunlabelled, retried after Phase B inference. Both passes propagate closeEdge's\nerror. Only the first was tested.\n\n    scope.go:259-261  Phase A2   `if err := closeEdge(...); err != nil { return err }`\n    scope.go:275-277  Phase C    the same, for the deferred list\n\nThe Phase C propagation could be replaced with `_ = closeEdge(...)` and the\nwhole tree stayed green. That is the fail-open shape: a resolution failure on a\ndeferred edge is silently discarded, and the Part resolves as if the edge had\nclosed, leaving edgeTypes / edgeKeys / edgeCands with no entry for it.\n\n## Why no existing fixture reaches it\n\nSix negative fixtures cover the A2 site — ambiguous_edge_orientation,\nunknown_edge, unknown_edge_undirected, unknown_edge_multi_type_all_miss,\ncreate_unknown_edge, merge_unknown_edge — and every one of them spells both\nendpoints, so the edge never defers. Reaching Phase C needs an edge that both\n(a) has an unlabelled endpoint at A2 and (b) still fails closure after that\nendpoint is inferred, which is a narrow target: inference derives the endpoint\ntype *from* the touching edges' candidate sets, so an edge that constrains the\ninference generally closes against it by construction.\n\nThe way through is orientation rather than labels. closeEdge's `default:` arm\n(resolve.go:547) rejects an undirected single-label edge with more than one\nsurviving candidate, and that check runs after the label intersection succeeds:\n\n    MATCH (a:Person)-[:KNOWS]-\u003e(x) MATCH (x)-[r:AUTHORED]-(b:Post) RETURN r\n\nx is unlabelled, so both edges defer. Phase B intersects: KNOWS from a Person\nsource gives {Person}, and undirected AUTHORED against a Post gives {Person} in\nboth orientations, so x commits unambiguously as Person. Phase C then closes\nthe KNOWS edge fine and hits the AUTHORED edge with src=Person, tgt=Post,\nundirected, one label — Person-AUTHORED-\u003ePost and Post-AUTHORED-\u003ePerson both\nsurvive, so it is ErrAmbiguousEdgeOrientation, raised from the deferred site.\n\n## Fixed here\n\ntest/data/resolver/invalid/ambiguous_edge_orientation_after_inference.cypher,\nits schema.mapping.json entry (social_r3.gql, which already carries both\nAUTHORED directions), and its invalidFixtures row in resolver_test.go. No\nproduction change.\n\nVerified 1:1:\n\n    CAUGHT   Phase A2 closeEdge error swallowed   -\u003e the six pre-existing fixtures\n    CAUGHT   Phase C deferred error swallowed     -\u003e this fixture, and only this one\n\nNote the shape for the next sweep: a sentinel that already has a negative\nfixture reads as covered, but coverage is per-sentinel, not per-emitter.\nErrAmbiguousEdgeOrientation had a fixture; the *second* site that raises it had\nnone. Same class-2 collision as gqlc-bef item 4.\n\nRelated: gqlc-bef item 6.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:29:41Z","created_by":"areqag","updated_at":"2026-07-28T15:32:34Z","started_at":"2026-07-28T15:29:42Z","closed_at":"2026-07-28T15:32:34Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/575","dependencies":[{"issue_id":"gqlc-0ul","depends_on_id":"gqlc-bef","type":"related","created_at":"2026-07-28T11:29:47Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-dg3","title":"resolver: two anonymous edges collide on the empty-string nullability key, unpinned","description":"Two anonymous edges in the same Part collide on the empty-string key in\nDemoteNullability's tables. Two guards stop it, they mask each other exactly,\nand nothing in the suite pinned either — so the pair could be removed with the\nwhole tree green, silently reporting OPTIONAL-matched bindings as non-nullable.\n\n    scope.go:340  SeedLocalNullability's  `if !ok || v == \"\"`\n    scope.go:403  addMember's             `if v == \"\" || g \u003c= 0`\n\n## The defect\n\nquery.go:311 and listener.go:521 record that an anonymous *node* is not a\nbinding, and NewNodeBinding rejects the empty variable. Only an EdgeBinding may\nbe anonymous (query.go:86). So the \"\" key is reachable, and reachable more than\nonce per Part — every anonymous edge writes to the same slot.\n\n    OPTIONAL MATCH (a:Person)-[:AUTHORED]-\u003e(b:Post)\n    MATCH (c:Person)-[:KNOWS]-\u003e(d:Person)\n    RETURN a, b, c, d\n\nTwo anonymous edges: the first nullable in group 1, the second required in\ngroup 0. With both guards dropped:\n\n  - SeedLocalNullability writes nullableBinding[\"\"] twice, and the required\n    edge is last, so the slot ends false.\n  - addMember enters (\"\", 1) — the required edge's group 0 is still rejected by\n    the `g \u003c= 0` half — so groupsOf[\"\"] = [1] and members[1] = [a, \"\", b].\n  - The pre-fixed-point closure at :446 then sees nullableBinding[\"\"] present\n    and false, reads it as a proven witness for group 1, and demotes every\n    member: a and b come out non-nullable.\n\nThe two anonymous edges are unrelated — different clauses, different labels, no\nshared endpoint — and the required one's existence says nothing about whether\nthe OPTIONAL match found a row. This is a plain query, not a scope-layer\nconstruction.\n\n## Why the sweep saw nothing\n\nEach guard survives alone, because each blocks the channel on its own: without\nthe \"\" seed the closure finds nothing present, and without the \"\" membership\nthere is no group to demote. Class-5 mutually-masking, measured:\n\n    SURVIVED   SeedLocalNullability anon guard alone\n    SURVIVED   addMember anon guard alone\n    CAUGHT     PAIR seed + addMember\n    CAUGHT     ALL FOUR anon guards\n\n## The other two anonymous guards are not defects\n\nRecording these so the next sweep does not re-derive them:\n\n  :481  the endpoint loop's `v == \"\"`. Unreachable — NewVarEndpoint rejects the\n        empty variable (query.go:1158-1161), and an endpoint with no name is an\n        InlineEndpoint, a different sum member. Only the zero value\n        VarEndpoint{} could carry \"\", and no constructor produces one. Class-3\n        defensive tripwire.\n\n  :759  Export's localGroup `v == \"\"`. A dead write — localGroup is read at\n        :836 only for names in exportedNames, and \"\" never reaches\n        exportedNames because buildScopeOrder excludes it (:540). Class-4\n        equivalent mutant.\n\n## Fixed here\n\ntest/data/resolver/valid/no_demote_disjoint_anonymous_required_edge.cypher plus\nits golden and schema.mapping.json entry. No production change — the guards are\ncorrect, they were merely unpinned. The golden records a and b nullable, c and d\nnot; the closest existing fixture, demote_from_anonymous_required_edge, shares\nthe endpoint `a` between the two clauses, so its group is demoted legitimately\nand the collision is invisible in it.\n\nVerified 1:1 — the pair mutation is caught by this fixture and by nothing else.\n\nRelated: gqlc-bef item 2.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:24:56Z","created_by":"areqag","updated_at":"2026-07-28T15:28:56Z","started_at":"2026-07-28T15:25:01Z","closed_at":"2026-07-28T15:28:56Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/573","dependencies":[{"issue_id":"gqlc-dg3","depends_on_id":"gqlc-bef","type":"related","created_at":"2026-07-28T11:25:01Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-efe","title":"resolver: BindCall's whole edge shadow cascade is unpinned — three scope unit tests assert through predicates that do not read the lanes they seed","description":"Found by a 120-case mutation sweep of internal/resolver/scope.go (83/120 caught). One shape accounts for every fixable survivor: a unit test seeds one carry lane and asserts through a read-only predicate that reads *different* lanes, so the seeded lane and the asserted lanes never intersect and the assertion holds vacuously.\n\n## BindCall's entire edge cascade — a test that pins one fifth of what it claims\n\nTestScopeBindCallShadowCascade's own comment says it seeds \"carried node + edge state so BindCall's shadow cascade drops both\". It seeds exportedNodeTypes and exportedEdgeBindings, then asserts HasNode(\"c\") is false and HasEdge(\"c\") is false. HasEdge (scope.go:319-325) reads edgeTypes and edgeCands — neither of which the carry seeds. The edge half of the assertion is therefore true before BindCall runs, and all four of BindCall's edge deletes survived:\n\n    scope.go:199  delete(s.edgeTypes, v)\n    scope.go:200  delete(s.edgeKeys, v)\n    scope.go:201  delete(s.edgeCands, v)\n    scope.go:202  delete(s.edgeBindings, v)\n\nDropping all four *together* also survived, so nothing elsewhere grounds them either — the cascade is unpinned as a whole, not merely under-discriminated. Only delete(s.nodeTypes, v) was caught.\n\nThis is the flip side of gqlc-2y7: the Has* trio has no production callers, and reaching for it in a test is exactly how the cascade went unpinned. TestScopeBindNodeShadowCascade is the model — it asserts NotContains on all four maps directly, and caught all four of its own.\n\n## BindEdge's closed-edge reset — same shape, two lanes\n\nTestScopeBindEdgeShadowCascade seeds only exportedNodeTypes, so BindEdge's reset arm has nothing to erase. scope.go:185 (edgeKeys) and :186 (edgeCands) each survived alone. :184 (edgeTypes) is caught by the golden suite, and that single catch masks the other two: the three-way combined mutation is caught, so the pair is only visible one at a time.\n\n## Export's alias guard has two halves and one of them is tested\n\nscope.go:805 is `if ref.Property != \"\" || alias != ref.Variable { continue }`. TestScopeExportWildcardVsExplicit drives `WITH v, v.name AS x`, which trips the property half. Nothing in the suite writes `WITH v AS w` — a rename of a bare binding — so the alias half could be dropped with the tree green, exporting v's node type into the downstream carry under a binding name that no longer leaves the Part.\n\n## Fixed here\n\ninternal/resolver/scope_test.go only; no production change. Verified 1:1 — each mutation caught by exactly the test written for it, nothing redundant:\n\n    TestScopeBindEdgeShadowCascade    -\u003e :185, :186\n    TestScopeBindCallShadowCascade    -\u003e :199, :200, :201, :202, and the four combined\n    TestScopeExportWildcardVsExplicit -\u003e :805's alias half\n\nSweep score 83/120 -\u003e 91/120. The remaining survivors are triaged separately.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:10:51Z","created_by":"areqag","updated_at":"2026-07-28T15:13:10Z","started_at":"2026-07-28T15:10:57Z","closed_at":"2026-07-28T15:13:10Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/568","dependencies":[{"issue_id":"gqlc-efe","depends_on_id":"gqlc-2y7","type":"related","created_at":"2026-07-28T11:10:57Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rvc","title":"schema/gql: six fail-open arms in listener.go — the phrase-form collectors, and the entire edge property-error lane","description":"Found by a 64-case mutation sweep of internal/schema/gql/listener.go (54/64 caught). Six survivors are real fail-open arms, and they fall into two groups with one shape in common: a guard that exists in duplicate, where only one copy was ever exercised.\n\n## Group 1 — the phrase form is systematically less covered than the pattern form\n\nlistener.go has four element collectors. EnterNodeTypePattern / EnterNodeTypePhrase and EnterEdgeTypePattern / EnterEdgeTypePhrase each copy fillerContent's fields through their own hand-written assignments and forward nodeContent/edgeContent's error through their own `if err != nil { l.fail(err) }`. Nothing is shared, so a pattern-form fixture grounds nothing about the phrase form.\n\nFour mutations survived on that asymmetry:\n\n    listener.go:189  EnterNodeTypePhrase  drop l.fail on the filler error\n    listener.go:192  EnterNodeTypePhrase  n.hasKeyLabelSet = false\n    listener.go:254  EnterEdgeTypePhrase  drop l.fail on the filler error\n    listener.go:257  EnterEdgeTypePhrase  e.hasKeyLabelSet = false\n\nTheir pattern-form counterparts at :154, :157 and :416/:449 were all caught. Measured cause: every key-label-set corpus file is pattern form (18.2's pattern_key_label_set_only.gql — the name says the author saw the gap), and every value-type decline in 18.9 is written as a pattern-form node, so all of that coverage lands on EnterNodeTypePattern's error arm.\n\nConsequence of the hasKeyLabelSet drop is not a missing flag but a different type: resolve() reads it to tell a declared key label set from an absent one, so dropping it sends the type down GG22's inference path instead.\n\n## Group 2 — nothing anywhere puts an unsupported property type on an EDGE\n\n    listener.go:374  EnterEdgeTypePattern  drop l.fail on the filler error\n    listener.go:465  edgeContent           return fc, nil instead of the error\n\nedgeContent's error arm never returned a non-nil error across the entire suite, because no fixture declares a declining value type on an edge. That is the root cause of :254 as well: with no erroring edge anywhere, neither edge collector's error arm could be reached. An edge carrying an unsupported property type was silently dropped from the schema, taking the relationship with it and reporting nothing — the class-2 shape.\n\n## Fixed here\n\nFive corpus files, verified 1:1 (each mutation caught by exactly the file written for it, nothing redundant):\n\n    18.2-node-type/phrase_key_label_set_only.gql          -\u003e :192\n    18.2-node-type/phrase_unsupported_property_type.gql   -\u003e :189\n    18.3-edge-type/phrase_key_label_set_only.gql          -\u003e :257\n    18.3-edge-type/pattern_unsupported_property_type.gql  -\u003e :374\n    18.3-edge-type/phrase_unsupported_property_type.gql   -\u003e :254\n    (the two edge unsupported files jointly ground :465)\n\nSweep score 54/64 -\u003e 60/64.\n\n## The four remaining survivors are inert, not gaps — do not re-file them\n\n  :67   walk's refusal to descend a tree that already has a syntax error. fail() is idempotent, so the returned error is the same either way; the guard protects against a panic on an unreliable tree, and no input in the suite panics. A tripwire, and see below.\n  :92   the `src == nil` half of the source guard. The doc comment says src is nil only under error recovery — and walk's :67 guard is what makes error recovery unreachable here, so the two are a chain rather than two independent facts.\n  :113  unsupportedSource's nil guard. Same fact as :92: GraphTypeSource() is never nil at any call.\n  :487  nestedDepth counting from ctx rather than ctx.GetParent(). ctx is always one of the four element-type contexts and never a nestedGraphTypeSpecification, so the mutant is equivalent.\n\nWorth a separate look some time: :67, :92 and :113 form a three-link chain where the first guard is what makes the other two unreachable. If :67 were ever relaxed, both downstream nil guards would start mattering and neither has a test.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T14:44:46Z","created_by":"areqag","updated_at":"2026-07-28T14:50:17Z","started_at":"2026-07-28T14:44:50Z","closed_at":"2026-07-28T14:50:17Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/566","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-zm2","title":"schema/gql: three unpinned guards in resolve() — implied labels in idx.declared, source-endpoint failure, and rejectInheritance past the first implied set","description":"A 51-case mutation sweep of internal/schema/gql's resolution lane (resolve.go,\nnodetype.go, edgetype.go) run 2026-07-28 at 1cd1d1f. 45/51 caught after\nrepairing all 6 BUILD-FAILs and re-running them — BUILD-FAIL is an unknown\nresult, not a pass.\n\nThree survivors are real, pinnable gaps.\n\n## 1. idx.declared stops covering implied labels (resolve.go:66-72)\n\nThe comment there makes an explicit claim:\n\n    // Every label the type answers to, implied ones included: idx.declared\n    // exists to tell \"you named a type where an alias belongs\" apart from\n    // \"no such type\", and an author who writes an implied label has made\n    // that same mistake.\n\nNarrowing the loop from complete.Split() to key.Split() SURVIVES. So the\n\"implied ones included\" half of that claim is untested: no schema names an\nIMPLIED label where an edge endpoint alias belongs. Under the mutation such a\nschema reports ErrUnknownEndpoint instead of ErrEndpointNotAlias — the exact\nmisdiagnosis the comment says the code exists to prevent.\n\nNeeds: a node type declared with `=\u003e` implied labels, and an edge endpoint\nnaming one of those implied labels. Expect ErrEndpointNotAlias.\n\n## 2. The source endpoint failure stops being propagated (resolve.go:94-97)\n\n    source, err := e.source.resolve(idx)\n    if err != nil { return schema.Schema{}, err }   // SURVIVES when disabled\n\nThe TARGET arm four lines down is caught, so the corpus has a bad-target\nendpoint fixture and no bad-SOURCE one. With the source arm disabled the edge\nis built with an empty Source key and resolution proceeds, so the schema is\nsilently accepted with a bogus edge.\n\nNeeds: a schema whose edge SOURCE endpoint names an undeclared node type while\nthe target is fine. Expect ErrUnknownEndpoint.\n\n## 3. rejectInheritance stops checking every implied set (resolve.go:173-181)\n\nCapping the outer `for _, set := range implied` at its first element SURVIVES.\nEvery existing ErrImpliedLabelIsKeyLabel fixture puts the colliding declaration\nfirst. The comment at resolve.go:80-82 says the check is deferred past the loop\nprecisely because \"a later declaration's key label can collide with an earlier\none's implied label\" — that ordering is the thing not covered.\n\nNeeds: a schema with two or more `=\u003e` declarations where a LATER one carries\nthe colliding implied label.\n\n## Non-defects, recorded so the next sweep does not re-flag them\n\n- \"the alias guard stops skipping the empty alias\" (resolve.go:60) and \"the\n  name guard stops skipping the empty name\" (resolve.go:63) both SURVIVE.\n  Writing idx.aliases[\"\"] / idx.declared[\"\"] is unobservable because\n  rawEndpoint.resolve only consults either map inside its own `ref.alias != \"\"`\n  branch. This is a MUTUALLY-MASKING GUARD PAIR — the 6th instance of category\n  5 in resolver-mutation-survivor-triage — and neither `!= \"\"` is load-bearing\n  on its own.\n\n- \"labelSets stops cloning before appending\" (resolve.go:149) SURVIVES.\n  Every construction path for a graph.LabelSet has cap == len — labelSet()\n  uses make(graph.LabelSet, len(names)) and the single-label form is a\n  one-element literal — so append always reallocates and slices.Clone can\n  never be observed. Category 3, defensive tripwire: correct to keep, provably\n  redundant today.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T14:15:22Z","created_by":"areqag","updated_at":"2026-07-28T14:21:29Z","started_at":"2026-07-28T14:15:27Z","closed_at":"2026-07-28T14:21:29Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/562","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-xem","title":"resolver: demotion, Export and the effect validators are 60/92 under mutation — and nine guards are unpinnable by construction","description":"Two more mutation sweeps, same verify-by-failure discipline.\n\nLane C — DemoteNullability, Export, ValidateEffects (scope.go).\n48 mutations, 31 caught.\n\nLane D — validateEffect and the eight per-variant effect validators\n(resolve.go). 44 mutations, 29 caught.\n\nCombined 60/92.\n\nREAL GAPS (4) — fixed here, each verified 1:1:\n\n1. demoteGroupsOf visits every group a name belongs to, but no fixture\n   put a name in two groups at once. The code comment at scope.go:401\n   says groups \"may span carry + local\"; nothing tested it. Truncating\n   the loop to its first group left the suite green.\n\n2. validateSetPropertyEffect's SINGLE-candidate edge property check.\n   Only the multi-candidate (unionProperty) arm had a fixture.\n\n3. validateRemovePropertyEffect, same arm, same gap. DELETE's\n   equivalent arm was already covered — the asymmetry is what makes\n   this the kind of hole reading alone does not find.\n\n4. ValidateEffects short-circuits on the first failing effect. No\n   fixture had TWO failing effects with distinguishable sentinels, so\n   returning the LAST error instead of the first was invisible.\n\nNINE GUARDS ARE UNPINNABLE AS THE SUITE IS BUILT TODAY:\n\nEvery per-variant effect validator ends in the same three-arm ladder:\n\n    if _, ok := sc.edgeBindings[v]; ok  { ...ErrInvalidEffectTarget }\n    if _, ok := sc.carriedResolvedTypes[v]; ok { ...ErrInvalidEffectTarget }\n    return ...ErrInvalidEffectTarget\n\nAll three arms raise ErrInvalidEffectTarget, and TestInvalid asserts\nwith errors.Is. Deleting either of the first two arms therefore falls\nthrough to the third and the suite stays green — for all seven\nprojection-alias arms and both SET/REMOVE-labels-on-edge arms.\n\nThere ARE fixtures named for these guards —\nset_property_on_projection_alias, set_labels_on_projection_alias,\ndelete_projection_alias and five more. They exercise the arm. They\ncannot pin it. Nine tests that look like coverage and are not.\n\nThis directly contradicts gqlc-ka1's stated premise that \"the sentinel\nis correct and TestInvalid asserts via errors.Is, so nothing is\nfail-open\". Nine things are fail-open, precisely because the assertion\nstops at the sentinel. See the comment added to gqlc-ka1.\n\nClosing them needs a decision that is NOT test-local:\n  (a) assert message substrings for these fixtures, or\n  (b) split ErrInvalidEffectTarget into distinct sentinels.\n(b) changes exported errors and wants an ADR. Neither is taken here.\n\nREMAINING SURVIVORS, TRIAGED (19):\n\nDefensive / parser-protected (6): the CREATE and MERGE anonymous-skip\nand unbound-variable arms (the parser's referential-integrity check\nrejects an unbound name before the resolver runs — confirmed by\nobserving the parser error \"unbound variable: a\"), the bare-ref\npre-pass's EdgeBinding arm (its comment says the parser passes a\nconstant false; the mutation confirms it), and Export's renamed-alias\nexclusion (same parser gate).\n\nMutually-masking pair (2): addMember's `g \u003c= 0` and demoteGroup's\n`g == 0`. Either one alone can be deleted with no effect, because the\nother still filters group zero. Only deleting BOTH would regress. A\nfifth instance of the taxonomy category.\n\nEquivalent mutants (9): the anonymous-name skips whose maps are never\nkeyed on \"\", the `present` guards whose spurious entries are never\nread back, addMember's dedup (demoteGroup is idempotent), and Export's\nzero-group suppression (masked twice downstream).\n\nFixed-point round two (2): the two `changed = true` assignments that\ndrive a SECOND iteration of the demotion loop. No fixture needs one.\nSame shape as gqlc-3rj, which records that inferUnlabelled's\nmulti-round loop is likewise uncovered — worth folding together.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T13:50:48Z","created_by":"areqag","updated_at":"2026-07-28T13:55:36Z","started_at":"2026-07-28T13:50:53Z","closed_at":"2026-07-28T13:55:36Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/559","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-l7v","title":"resolver: binding-admission and helper lanes are 52/73 under mutation — seven fail-open gaps","description":"Two mutation sweeps over the resolver, run under the verify-by-failure\ndiscipline (break the guard, prove a test fails).\n\nLane A — binding admission (scope.go: Ingest, BindNode, BindEdge,\nBindCall, CloseEdges, InferUnlabelled, SeedLocalNullability,\nbuildScopeOrder, materialiseReturns, virtualProjection,\nprojectionType). 48 mutations, 35 caught.\n\nLane B — resolve.go helpers (useSitesToScopes, callProjectionType,\nunionProperty, qualifiedDemoter, argAssignable). 25 mutations, 17\ncaught.\n\nCombined 52/73. Triage of the 21 survivors:\n\nREAL GAPS (7) — a guard can be deleted and the suite stays green:\n\n1. BindEdge stops resetting carried closed-edge state. closeEdge writes\n   edgeTypes/edgeKeys for a single candidate and edgeCands for many,\n   and never clears the opposite lane. A carried SINGLE-candidate\n   binding re-bound to a MULTI-candidate one keeps the stale\n   single-candidate answer.\n\n2. unionProperty stops reporting a nullability-only mismatch. The guard\n   is `hit.Type != first.Type || hit.Nullable != first.Nullable`; every\n   fixture differed in type, so the nullability half was masked by the\n   type half raising the same sentinel first — the sentinel-collision\n   pattern again.\n\n3. unionProperty stops widening on a nullable binding. No schema had a\n   NOT NULL property shared across two edge labels, so\n   `first.Nullable = first.Nullable || bindingNullable` never had to do\n   anything.\n\n4. qualifiedDemoter's lower-bound threshold moves off 1. The three\n   var-length demoter fixtures used *1..3 on an already-non-nullable\n   endpoint, *0..3 (below the threshold either way), and unbounded\n   (short-circuits before the comparison). None straddled 1.\n\n5. argAssignable stops admitting an unknown-typed argument. No fixture\n   passed a parameter as a CALL argument.\n\n6. argAssignable's FLOAT token stops admitting a float. Only\n   int-at-FLOAT was pinned; the exact-match arm was free.\n\n7. argAssignable's STRING token stops requiring a string.\n   call_arg_type_mismatch passes a good STRING and a bad INTEGER, so\n   the INTEGER arm raises first — sentinel collision.\n\nDEFENSIVE, UNREACHABLE (6) — tripwired with a panic, never fired:\nfive binding-lane arms (TRIPWIRE-T/U/V/W/X) plus argAssignable's\n`default: return false`. The last is unreachable by construction:\nprocsig.validToken rejects any token outside the four-value closed set\nat registry-build time, so the switch is total.\n\nEQUIVALENT MUTANTS (6) — the mutation cannot change behaviour:\nInferUnlabelled's method-level empty-pending check (redundant with\nresolve.go:570), its labelled-binding filter (redundant with the\ncarry-wins resolved filter), SeedLocalNullability's anonymous skip\n(nullableBinding[\"\"] is never read), the empty-scope wildcard fast\npath, buildScopeOrder's local-loop `|| seen[v]` (the cypher parser\nalready dedups binding records — proved with TRIPWIRE-S), and\ncallProjectionType's TypeUnknown arm (identical to its default).\n\nDEAD CODE (2) — evidence for gqlc-2y7:\nvirtualProjection's TypeList stamp is confirmed never read (mutating\nit to stop projecting as a list SURVIVED), and callBindingSlot declares\nfour fields of which only resultType is ever read — nullable,\nprocedure and sourceField are write-only.\n\nFix: seven fixtures, each verified 1:1 (the mutation it targets fails\nthat fixture and only that fixture).","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T13:35:52Z","created_by":"areqag","updated_at":"2026-07-28T13:40:52Z","started_at":"2026-07-28T13:35:55Z","closed_at":"2026-07-28T13:40:52Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/557","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-puk","title":"resolver: the parameter-witness scope gate fails open (8/33 mutations survive)","description":"A 33-case mutation sweep over the parameter-witness lane -- unifyParameterUsesAcrossBranches, selectPartScope, WitnessUse, partScope.Contains, PropertyUseWitness, scope.Snapshot -- caught 25 and let 8 through. No BUILD-FAILs. This is the best-pinned lane swept so far (write-effects 13/26, UNION 13/26, inference 25/37), and only ONE real gap came out of it.\n\nTHE REAL GAP -- one fixture, two survivors:\n  * WitnessUse's out-of-scope PropertyUse arm ('if !sc.Contains(ref.Variable) { return nil, nil }') and unifyParameterUsesAcrossBranches' witnessless fallback ('if !seen { unified = ResolvedUnknown{} }') were BOTH unpinned. They are two halves of one path: a Use whose Ref names something partScope cannot see yields zero witnesses, and a parameter with zero witnesses across all its uses must resolve to unknown.\n\n    Nothing in the corpus reached it because partScope deliberately narrows to the three entity lanes (nodeTypes, edgeTypes, edgeCands) per §2.3 invariant #3 -- callTypes is NOT observable through it. So a property Ref on a CALL YIELD variable is in parser scope but out of partScope scope. New fixture param_witnessless_out_of_scope.cypher:\n\n        CALL test.labels() YIELD label\n        MATCH (p:Person) WHERE label.zip = $z\n        RETURN p.name AS nm\n\n    $z's only use is label.zip; it resolves to kind:unknown. Verified: this single fixture catches both mutations and nothing else does.\n\nSIX SURVIVORS ARE NOT GAPS. Five confirmed by planted panics across go test ./... -- none fired:\n  * TRIPWIRE-M: Use.Branch index out of range. Parser attributes a valid query-level branch index by construction.\n  * TRIPWIRE-N: PropertyUse.Part index out of range. Same posture.\n  * TRIPWIRE-P: ExprUse's resolveType error propagation. resolveType returns an error on exactly two inputs -- TypeList{TypeNode} and TypeList{TypeEdge} -- and no ExprUse in the corpus carries a list-of-entities enclosing type. Every other resolveType arm either succeeds or panics.\n  * TRIPWIRE-Q: unknown ExprUse position. The parser emits only the four declared positions.\n  * TRIPWIRE-R: PropertyUseWitness' '!singleCand \u0026\u0026 !multiCand' out-of-scope arm. STRUCTURALLY unreachable, not merely uncovered: WitnessUse gates every call behind Contains, and Contains tests exactly the three lanes PropertyUseWitness consults (nodeTypes, then edgeTypes/edgeCands). If Contains passed and nodeTypes missed, one of edgeTypes/edgeCands must hit. Note this is the mirror image of the real gap above -- the SAME Contains gate that makes this arm dead is the thing that had no fixture.\n\n  The sixth is an equivalent mutant, argued from code rather than comment: selectPartScope hands a zero partScope to non-PropertyUse Uses. Swapping in branchScopes[0] survives because WitnessUse's ClauseSlotUse and ExprUse arms never dereference the receiver -- read the switch body, not the doc comment.\n\nFixed test-only. No production change.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T13:18:46Z","created_by":"areqag","updated_at":"2026-07-28T13:21:22Z","started_at":"2026-07-28T13:18:51Z","closed_at":"2026-07-28T13:21:22Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/555","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-oou","title":"resolver: parameter-unification and inference-round guards fail open (12/37 mutations survive)","description":"A 37-case mutation sweep over the binding-inference and type-unification lane -- edgeCandidates, closeEdge, endpointLabels, inferUnlabelled, candidateTypes, touchingSide, unify, resolveNodeLabels, satisfyingNodeTypes -- caught 25 (after repairing 2 BUILD-FAILs) and let 12 through. This lane is markedly better pinned than the write-effect (13/26) and UNION (13/26) lanes swept earlier; five survivors are real gaps and are now pinned 1:1.\n\nTHE REAL GAPS, all fixed test-only:\n  * unify's property-type check. Third instance tonight of a sentinel collision. parameter_type_conflict_two_properties.cypher is 'p.name = $x AND p.age = $x' -- STRING NOT NULL against a nullable INT, so it differs in type AND nullability. unify tests both side by side and both raise ErrParameterTypeConflict, so the adjacent nullability check caught the fixture and left the type check free for its whole life. The new fixture uses two NOT NULL properties.\n  * inferUnlabelled's CALL YIELD scalar collision (R7 4.1.2.1 addendum). part_binding_type_conflict_call_vs_node.cypher pinned the LABELLED arm's Phase A1 check; the unlabelled path carries its own copy and had no fixture.\n  * candidateTypes' undirected reverse orientation. Needed a pattern where ONLY the reverse orientation supplies a candidate: 'MATCH (a:Person)-[:EMPLOYS]-(b)' against social_r5, where Company-EMPLOYS-\u003ePerson means b can only be Company via the reverse.\n  * candidateTypes' non-touching-edge skip. Needed a second edge in the pattern that does not touch the unlabelled binding.\n  * resolveNodeLabels' empty-satisfying-set arm -- every queried label declared somewhere, but no declared type carrying all of them.\n\nSEVEN SURVIVORS ARE NOT GAPS. All verified by planted panics, not by reading:\n  * unify's ResolvedNode, ResolvedEdge and ResolvedList arms, and its absorb-unknown-on-the-left arm (TRIPWIRE-G/H/I/J). None fired across go test ./... . unify is reachable only from unifyParameterUsesAcrossBranches, whose witnesses come from WitnessUse: ResolvedProperty, ResolvedScalar, or resolveType's output -- and resolveType PANICS on bare TypeNode/TypeEdge, so the node and edge arms are structurally unreachable. Defensive tripwires.\n  * closeEdge's orientation-ambiguity check requires BOTH !e.Directed() and len(e.Labels()) == 1. Dropping the directedness half is an equivalent mutant: a directed single-label edge yields at most one candidate (one orientation x one label), so the multi-candidate arm the check guards cannot be entered. TRIPWIRE-K confirmed -- never fired.\n  * satisfyingNodeTypes' sort affects only diagnostic ordering, and the suite asserts sentinels rather than messages. Unpinnable as the suite stands; see gqlc-ka1.\n\nONE SURVIVOR IS UNCOVERED PRODUCTION CODE, not a fail-open. TRIPWIRE-L (panic when inferUnlabelled starts a second round) never fired: no fixture in the corpus needs more than one inference round, so the outer 'for len(pending) \u003e 0' loop is dead in practice. Truncating it to one round is therefore invisible. This is not provably unreachable -- it needs a binding that is ambiguous when evaluated but unambiguous once a later binding in the same round commits -- so it wants a purpose-built schema. Filed separately.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T13:00:27Z","created_by":"areqag","updated_at":"2026-07-28T13:13:12Z","started_at":"2026-07-28T13:00:39Z","closed_at":"2026-07-28T13:13:12Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/550","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rlt","title":"resolver: UNION branch-column comparison and grouping-key fills fail open (13/26 mutations survive)","description":"A 26-case mutation sweep over fillGroupingKeys, compareBranchColumns, resolvedTypeEqual and computeDistinct (internal/resolver/resolve.go:254-381) caught 13 and let 13 through. Ten of the thirteen are real fail-opens and are now pinned by ten new fixtures, each catching exactly one mutation.\n\nHEADLINE: a mutually-masking guard pair. test/data/resolver/invalid/union_column_name_mismatch.cypher is\n\n    MATCH (a:Person) RETURN a.name UNION MATCH (a:Person) RETURN a.age AS name2\n\nBranch 1 differs from branch 0 in BOTH name and type. compareBranchColumns checks name first and type second, both raising ErrUnionColumnMismatch, and the suite asserts only the sentinel. Deleting the name check outright left the fixture red for the other reason, so the name check has never been pinned despite carrying a fixture named after it.\n\nThe same masking runs through resolvedTypeEqual, reachable only from compareBranchColumns. Four arms were unpinned: ResolvedNode value equality, ResolvedEdgeUnion key identity, ResolvedEdgeUnion nullability, and ResolvedList element type. No fixture unioned two branches projecting different whole-entity or list types under a common column name.\n\nAlso unpinned and now fixed:\n  * compareBranchColumns only ever compared branch 1 against branch 0; no fixture had three branches.\n  * computeDistinct folds Part.Distinct across every branch x Part, but truncating either loop to its first element survived. No fixture carried DISTINCT in a non-first Part, or in a non-first branch under UNION ALL (plain UNION masks it via the UnionDistinct combinator fold).\n  * fillGroupingKeys treats LiteralProjection and FuncProjection as grouping keys; neither appeared in a Part alongside an aggregate.\n\nTHREE SURVIVORS ARE NOT GAPS:\n  * resolvedTypeEqual's nil guard. TRIPWIRE-E (panic on a nil operand) never fired across go test ./... — the function is never reached with nil. Defensive tripwire.\n  * The 'break' short-circuiting the hasAggregate scan. hasAggregate is monotone, so the break is pure optimisation and the mutation is provably equivalent.\n  * The 'if part.ReturnsAll { return }' early exit is behaviourally inert, but NOT for the reason the source claimed. The comment asserted 'A ReturnsAll-expanded Part's Returns is empty (parser guarantees mutual exclusion)'. That is FALSE: TRIPWIRE-D fired on seven standalone CALL ... YIELD fixtures, which set ReturnsAll AND a non-empty Returns. TRIPWIRE-F then showed cols and Returns are index-aligned in exactly those cases (the only length mismatches are the 0-Returns plain RETURN * expansions), so no latent indexing bug is being masked. The guard is inert because neither a RETURN * expansion nor a YIELD item list can carry an AggregateProjection. Comment corrected in place; that is the only production change.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T12:46:45Z","created_by":"areqag","updated_at":"2026-07-28T12:58:41Z","started_at":"2026-07-28T12:46:50Z","closed_at":"2026-07-28T12:58:41Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/546","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-pzz","title":"resolver: REMOVE LABELS and DELETE effect validators fail open (13/26 mutations survive)","description":"A 26-case mutation sweep over the write-effect validators in internal/resolver/resolve.go (validateEffect and its eight variant validators) caught 13 and let 13 through.\n\nBead gqlc-ag5 previously swept this lane and pinned nine fail-opens, but it mirrored only the SET family. The survivors are concentrated entirely in the halves ag5 did not mirror:\n\n  * REMOVE LABELS is SET LABELS' twin and carries the same three guards (edge-binding rejection, projection-alias rejection, node-target requirement). SET had set_labels_on_edge.cypher behind it; REMOVE had nothing, so its edge rejection and the node-target gate the two share were both free.\n  * Neither SET nor REMOVE pinned the projection-alias arm on labels at all.\n  * DELETE is the third property validator. SET and REMOVE each pin the variable-length-edge guard and the property-existence check on both the single-type and multi-type edge shapes. DELETE pinned neither guard, on either shape.\n  * Two loops that walk a list of things to validate stopped at their first element with the suite still green: validateDeleteEffect over e.Targets(), and validateMergeEffect over e.OnMatch().\n\nTwelve of the thirteen are real fail-opens. The thirteenth -- validateCreateEffect's 'if v == \"\" { continue }' skip of anonymous variables -- is a defensive tripwire, not a gap: a panic planted on that branch never fired across go test ./..., matching the source comment that its reachability from parser input is zero.\n\nFix is test-only: eleven new fixtures under test/data/resolver, ten invalid plus one valid (DELETE of a bare multi-type edge binding). No production behaviour changes.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T12:42:09Z","created_by":"areqag","updated_at":"2026-07-28T12:45:27Z","started_at":"2026-07-28T12:42:12Z","closed_at":"2026-07-28T12:45:27Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/543","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-kq6","title":"resolver: five WITH-boundary carry lanes in scope.Export are unpinned","description":"A 26-case mutation sweep over scope.Export / scope.ValidateEffects (internal/resolver/scope.go:727-860) caught 17. Of the nine survivors, five are real test gaps: each mutation silently changes resolved output and no fixture notices.\n\n1. scope.go:840 — the else-if fallback to s.carriedGroups. It only fires for a name exported WITHOUT a local re-declaration, which needs a second WITH. Every OPTIONAL fixture had at most one WITH, so an OPTIONAL group id that survives one boundary but not two was undetectable. Deleting the fallback leaves a provably non-null column nullable.\n\n2. scope.go:750 — the EdgeBinding arm of the localGroup lookup. The NodeBinding arm was pinned; the edge arm was not (the recurring 'one half of a symmetric pair' motif). An edge's OPTIONAL-group id was dropped at the boundary with the suite green.\n\n3. scope.go:818 — the exportedEdgeCands lane. Masked for a bare 'WITH r RETURN r' by the section 4.5.4 carried-alias bypass, which serves the whole type out of carriedResolvedTypes. Only a PROPERTY projection on the carried union reaches the edgeCands table, and no fixture did that.\n\n4. scope.go:836-842 — the group-id precedence rule (local shadows carry). Needs a name carried under one group id AND locally re-declared under a different one; no fixture had that shape. Inverting the precedence strands a name in a stale group so a later proof of its real group does not demote it.\n\n5. scope.go:854 — ValidateEffects' loop over s.effects. No fixture had two effects where the SECOND is the invalid one, so validating only s.effects[0] passed the suite.\n\nThe remaining four survivors are equivalent mutants, proven not argued:\n- 'alias must match the variable' (scope.go:806): the cypher parser rejects a reference to a renamed-away binding first ('unbound variable: a'), so the leak is unobservable.\n- 'non-Ref projection' (scope.go:797): masked by the following alias != ref.Variable guard, since a zero RefProjection has Variable \"\". Tripwire on a non-Ref projection with an empty alias never fired.\n- 'localGroup keeps anonymous bindings' (scope.go:759): tripwire on an empty variable reaching the export binding loop never fired.\n- 'propagate a zero local group id' (scope.go:837): s.carriedGroups has exactly two readers, DemoteNullability's addMember (guards g \u003c= 0) and scope.go:840 (guards g \u003e 0), so a zero entry is inert.\n\nFix is five fixtures, each proven load-bearing by re-running its mutation.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T12:26:55Z","created_by":"areqag","updated_at":"2026-07-28T12:31:49Z","started_at":"2026-07-28T12:27:00Z","closed_at":"2026-07-28T12:31:49Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/541","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-v1t","title":"schema/gql: the last-wins duplicate property name is not pinned anywhere, so it can flip to first-wins silently","description":"Splits the shippable half off gqlc-4np, and deliberately does NOT decide gqlc-4np's ADR.\n\ngqlc-4np measures that `{ id :: INT NOT NULL, id :: STRING }` parses, resolves, and yields ONE property — the last one — with the first declaration's type and its NOT NULL discarded and no diagnostic. Whether that should be rejected (a new sentinel, consistent with ErrDuplicateNodeType / ErrDuplicateEdgeType) or defined as explicit last-wins precedence is a dialect choice that needs an ADR, and gqlc-lir declined to buy the PDF that would arbitrate it. That question stays open on gqlc-4np.\n\nBut gqlc-4np states a second thing that needs no decision at all:\n\n    REGARDLESS OF WHICH IS CHOSEN, the current behaviour is unrepresented in\n    the tree today: no corpus entry, no semantic case, no invalid fixture.\n\nThat is a live risk independent of the ADR. Last-wins today is an accident of `out[p.Name] = p` in listener.properties (listener.go:498-516) writing in walk order — not a decision, and nothing pins it. A refactor that iterated the property type list in reverse, or that skipped a name already present, would flip the tool to first-wins with the whole suite green and no reviewer signal. Whichever way the ADR lands, silently changing behaviour beforehand is strictly worse than either outcome.\n\n## Fix\n\nA corpus semanticCase in area B (18.2-node-type), which is exactly the construct for this: per corpus_test.go, 'a semantic case is a promise that something is broken.' Recording one asserts the model is WRONG; it does not endorse the behaviour, so it cannot pre-empt the ADR. semanticAreaB is currently empty and its comment invites the edit in place.\n\n  file:     18.2-node-type/property_name_repeated.gql\n  bead:     gqlc-4np   (the case is discharged when the ADR ships, not by this bead)\n  spelling: the doubled declaration, verbatim\n  siblings: the single-declaration spelling its model cannot be told apart from\n\nWhen gqlc-4np's ADR lands, this row either becomes an `unsupported` entry with the new sentinel (option a) or gains a corpus entry recording the decided precedence (option b). Either way the flip is caught in the meantime.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T12:08:12Z","created_by":"areqag","updated_at":"2026-07-28T12:14:57Z","started_at":"2026-07-28T12:08:17Z","closed_at":"2026-07-28T12:14:57Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/539","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-8ez","title":"resolver: the target half of edge-driven demotion and the source half of endpoint inference failure are both unpinned","description":"A 26-mutation sweep over DemoteNullability, CloseEdges, InferUnlabelled and\nBindCall caught 18. Two survivors are real, reachable gaps, and they have the\nsame shape: a symmetric pair where one half is pinned and the other is not.\n\n## 1. Only the SOURCE endpoint of a required edge is pinned as a demoter\n\nDemoteNullability's fixed point walks both ends of a qualifying required edge:\n\n    for _, side := range [2]query.Endpoint{e.Source(), e.Target()} {\n\nNarrowing that to [1]{e.Source()} leaves the whole suite green. Probed at\ncd2826c against social_r6.gql, two queries discriminate:\n\n    OPTIONAL MATCH (b:Post) MATCH (a:Person)-[:AUTHORED]-\u003e(b) RETURN b\n        today:   b nullable=false\n        mutated: b nullable=TRUE\n\n    OPTIONAL MATCH (a:Person) OPTIONAL MATCH (b:Post)\n    MATCH (a)-[:AUTHORED]-\u003e(b) RETURN a, b\n        today:   a=false b=false\n        mutated: a=false b=TRUE\n\nThis is fail-open in the direction that matters for codegen: a column that is\nprovably non-null becomes nullable, so generated Go gets a pointer where a value\nbelongs. Every existing demotion fixture happens to demote through the source\nposition, or demotes the target through some other channel (the group cascade,\nor a local re-MATCH), so the target arm of the loop has never been the thing\nunder test.\n\n## 2. Only the TARGET half of CloseEdges' post-Phase-B failure is pinned\n\nCloseEdges reports an endpoint still unresolved after inference with two arms,\none per side. Replacing the SOURCE arm with a no-op leaves the suite green;\nreplacing the TARGET arm is caught, by empty_inline_endpoint.cypher\n(`MATCH (p:Person)-[r:AUTHORED]-\u003e() RETURN r`). The source twin does not exist.\n\nProbed -- reachable, and rejects today with the source-named message:\n\n    MATCH ()-[r:AUTHORED]-\u003e(b:Post) RETURN r\n    -\u003e unknown label: cannot infer type of source endpoint of edge \"r\"\n\n## Fix\n\n- valid/demote_target_endpoint.cypher (and the both-optional variant), with\n  goldens, for (1). Positive fixtures, because the mutation is fail-open: only a\n  golden that records nullable=false can catch it.\n- invalid/anonymous_source_endpoint.cypher -\u003e ErrUnknownLabel, for (2).\n\n## Survivors that are NOT gaps\n\n- `5xg pre-pass skips edge bindings` -- the EDGE arm of the bare-reference\n  pre-pass is unreachable by parser construction. cypher/pattern.go:314 says so\n  outright: \"an edge is grammatically never bare -- it always sits inside -[...]-\n  between two node positions -- so the parameter is a compile-time constant false\n  at this site.\" So EdgeBinding.ReferencedInRequiredBarePattern() is always false\n  and the resolver arm can never fire. Worth one comment linking the two, since\n  the resolver side does not mention the guarantee it depends on. The NODE arm is\n  caught.\n- `addMember accepts a zero or negative group id` and `demoteGroup stops guarding\n  against group zero` -- a mutually-masking pair. Group 0 means \"not in an\n  OPTIONAL group\"; either guard alone keeps group 0 inert, so removing one is\n  invisible. Removing BOTH would not be. Not a test gap: no single-mutation\n  fixture can distinguish a redundant pair of guards.\n- `addMember stops deduplicating group ids` -- equivalent mutant. Duplicates in\n  members[g] only cause repeated idempotent writes of false.\n- `BindCall stops checking procedure arity` and `BindCall's duplicate-variable\n  tripwire is removed` -- both documented in situ as parser-authoritative\n  pre-conditions (\"parser drift\", \"grammar-impossible; defensive tripwire\").","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T11:50:56Z","created_by":"areqag","updated_at":"2026-07-28T12:06:24Z","started_at":"2026-07-28T11:53:44Z","closed_at":"2026-07-28T12:06:24Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/537","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-445","title":"schema/gql: an empty inline endpoint property block is accepted by an unpinned boundary","description":"A 20-mutation sweep over the endpoint-reference and resolve() lanes caught 18.\nOne survivor is a real gap.\n\nfillerLabels rejects an inline endpoint that carries property types\n(ErrEndpointFillerHasProperties, gqlc-h9n.18). The guard is\n\n    if list := spec.PropertyTypeList(); list != nil \u0026\u0026 len(list.AllPropertyType()) \u003e 0 {\n        return nil, ErrEndpointFillerHasProperties\n    }\n\nDropping `\u0026\u0026 len(list.AllPropertyType()) \u003e 0` leaves the whole suite green. So\nnothing asserts what that clause decides: an endpoint written with an EMPTY\nproperty block is accepted.\n\nProbed at ce07430 -- reachable, and accepted in both endpoint positions:\n\n    (:Person { }) -[:KNOWS]-\u003e (:Person)      RESOLVED  nodes=1 edges=1\n    (:Person) -[:KNOWS]-\u003e (:Person { })      RESOLVED  nodes=1 edges=1\n    (:Person { id :: INT }) -[:KNOWS]-\u003e ...  REJECTED  edge endpoint filler carries properties\n\nThis is the endpoint half of a case the node types already have.\n18.2-node-type/pattern_empty_property_block.gql pins that `(:Person { })` is a\nvalid node type declaration. The same spelling in an endpoint has nothing, so\nthe boundary between \"declares no properties\" and \"declares properties\" is\nimplemented and undefended: narrowing the guard to `list != nil` turns a valid\nschema into a rejected one with no test going red.\n\n## Fix\n\nA corpus file under 18.3-edge-type with an empty property block on an inline\nendpoint, outcome resolves. A positive case, because the mutation is fail-closed\n-- which is exactly why no negative fixture would catch it.\n\n## One survivor that is NOT a gap\n\n`rejectInheritance stops naming the colliding label` -- replacing\nfmt.Errorf(\"%w: %s\", ErrImpliedLabelIsKeyLabel, label) with the bare sentinel\nsurvives. The corpus asserts sentinels via errors.Is and never message text, so\nno schema test can distinguish it. This is the same policy question as gqlc-ka1\non the resolver side (ErrParameterTypeConflict's message dropping the kind):\nwhether these suites should start asserting message content at all. Recorded\nthere rather than fixed here.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T11:40:32Z","created_by":"areqag","updated_at":"2026-07-28T11:53:43Z","started_at":"2026-07-28T11:40:43Z","closed_at":"2026-07-28T11:53:43Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/535","comments":[{"id":"019fa88a-4598-77fa-a958-2959c833df6a","issue_id":"gqlc-445","author":"areqag","text":"PREMISE WAS WRONG — correcting it here rather than shipping the test it implied.\n\nThe bead read the surviving mutation as a missing case: an endpoint written\n`(:Person { })` accepted by an unasserted boundary. I wrote the corpus file for\nthat and re-ran the mutation against the branch. It still survived.\n\nThe length clause is UNREACHABLE. propertyTypeList is optional inside the braces\nand requires at least one propertyType when present (GQL.g4:1691-1697), so `{ }`\nyields a nil list, not an empty one — a non-nil list can never have length zero.\nConfirmed by planting a panic on the non-nil-but-empty state and running the full\nsuite: never fired.\n\nSo there was no gap to close, and no test could have closed it. Shipping instead:\n\n- the clause deleted, replaced by a comment saying why the nil check carries the\n  whole test (a reader's default assumption is the opposite, which is what made\n  the mutation look like a gap)\n- the corpus file kept on its own merits — `(:X { })` in an endpoint is a\n  grammar-reachable spelling nothing covered, running through fillerLabels where\n  the node-type twin runs through nodeContent — but NOT claimed as a mutation\n  kill, in either its comment or the commit message.","created_at":"2026-07-28T11:44:22Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-afo","title":"schema/gql: the bare-bar closed dynamic union is unpinned, and a corpus reason claims otherwise","description":"A 22-mutation sweep over the property-type lowering in internal/schema/gql\ncaught 17. One survivor is a real gap, and it comes with a corpus comment that\nstates the opposite.\n\n## The gap\n\ndeclineValueType lists four contexts under ErrDynamicUnionType. Deleting\n*gen.ClosedDynamicUnionTypeAtl2Context from that list leaves the whole suite\ngreen. closedDynamicUnionTypeAtl2 is the bare-bar spelling (GQL.g4:1732,\n`valueType VERTICAL_BAR valueType`), as opposed to Atl1's angle-bracket form at\n:1731.\n\nIt is reachable and it does reject today:\n\n    (:Person { id :: INT NOT NULL, p :: STRING | INT })\n    -\u003e unsupported property value type: ANY VALUE and the closed unions need a\n       property type that carries alternatives ...\n\nWithout the case it falls through declineValueType to normaliseType, misses the\nspelling table and lands on the bare ErrUnsupportedType instead. So the family\nattribution ADR 0019 makes for this spelling is unasserted: the file rejects\neither way, and only the sentinel changes.\n\n## The comment that says otherwise\n\ncorpus_area_d2_test.go's entry for 18.9-value-type/constructed_dyn_closed_union.gql\nreads:\n\n    \"ANY VALUE\u003cSTRING | INT\u003e discharges valueType alts 9 and 10 in one parse,\n     because the inner STRING | INT is itself a nested closedDynamicUnionTypeAtl2\"\n\nThat is not what happens. declineValueType reads the outermost context and does\nnot descend — its own doc comment says so, and gives the reason (a subtree search\nwould report a RECORD field's family in place of the property's). The outer\ncontext is Atl1; the inner Atl2 is never dispatched on.\n\nVerified by failure rather than by reading: replacing the Atl2 arm with a panic\nand running `go test ./...` produced no panic. The one file that claims to reach\nit does not.\n\n## Fix\n\n- New corpus file 18.9-value-type/constructed_dyn_closed_union_bare.gql carrying\n  `p :: STRING | INT`, outcome unsupported, sentinel ErrDynamicUnionType.\n- Correct the reason on constructed_dyn_closed_union.gql: it discharges alt 9,\n  not alts 9 and 10, and the nested Atl2 is invisible to declineValueType by\n  design.\n\n## Two survivors that are NOT gaps (recorded so they are not re-swept)\n\n- `truncateParenthetical stops trimming the space before (` — unreachable.\n  normaliseType only ever calls it on canonicalSpelling's output, and\n  canonicalSpelling ends with collapseParenthetical, which already trims that\n  space. A panic-if-the-trim-would-change-anything tripwire never fired across\n  the full suite.\n- `collapseParenthetical stops collapsing spaces around a comma` — reachable but\n  outcome-inert. The tripwire DOES fire (unrepresentable_width_decimal_schema\n  feeds it `10, 2`), but no typeSpellings key contains a comma: all 18\n  parenthesised rows are the scale-free binary-width forms INT(n)/UINT(n)/\n  INTEGER(n). Collapsed or not, `DECIMAL(10, 2)` misses the exact lookup and\n  falls to truncateParenthetical -\u003e DECIMAL. The normalisation targets keys that\n  do not exist, so no test can distinguish it without adding one.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T11:33:29Z","created_by":"areqag","updated_at":"2026-07-28T11:40:30Z","started_at":"2026-07-28T11:33:43Z","closed_at":"2026-07-28T11:40:30Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/533","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-v9r","title":"schema/gql: rejectInheritance is half-pinned — the edge-vs-edge decline and the per-label (not first-label) claim have no corpus case","description":"A 13-mutation sweep over the semantic guards in internal/schema/gql caught 11.\nBoth survivors are in rejectInheritance, the ADR 0015 decline of the one part of\nGG21 gqlc does not implement. Both were probed and are reachable.\n\n## 1. rejectInheritance is never exercised for edge types\n\nresolve() calls rejectInheritance twice — once for nodes against node key labels,\nonce for edges against edge key labels. Deleting the EDGE call leaves the suite\ngreen. No corpus file has an edge type implying another EDGE type's key label:\n\n    CREATE PROPERTY GRAPH TYPE T AS {\n        (:Person { id :: INT NOT NULL }),\n        (:Person) -[:KNOWS { since :: DATE }]-\u003e (:Person),\n        (:Person) -[:FRIEND =\u003e :KNOWS { since :: DATE }]-\u003e (:Person)\n    }\n    -\u003e implied label is also a declared element type's key label: KNOWS\n\n18.3-edge-type/implied_label_is_node_key_label.gql covers the adjacent case and\npins the opposite outcome — an edge implying a NODE key label RESOLVES, because\nthe two vocabularies are separate namespaces. Its own comment states the rule as\n\"called once for nodes against node key labels and once for edges against edge key\nlabels\", but only the resolves half is verified. The rejects half for edges is not,\nso the edge call can be deleted and only that comment would still claim otherwise.\n\n## 2. The per-label check is only ever exercised at the first label\n\nrejectInheritance loops over every label in each implied set. Narrowing the loop to\nthe first label of each set leaves the suite green — every existing collision case\nhas the colliding label first. The doc comment makes the per-label claim explicitly\n(\"`(:A\u0026B)` holds both A and B as key labels, so implying either collides\") and\nnothing tests it:\n\n    CREATE PROPERTY GRAPH TYPE T AS {\n        (:Zeta { id :: INT NOT NULL }),\n        (:Engineer =\u003e :Alpha\u0026Zeta { level :: INT })\n    }\n    -\u003e implied label is also a declared element type's key label: Zeta\n\nThe label is second both as written and in sort order, so the case discriminates\nregardless of how the implied set is ordered. The mirrored control (collider first,\n:Alpha declared instead) is rejected today too, which is what the existing corpus\nalready covers.\n\n## Fix\n\nTwo corpus files with entries in the matching area tables:\n- 18.3-edge-type/implied_label_is_edge_key_label.gql   (unsupported, ErrImpliedLabelIsKeyLabel, GG21)\n- 18.2-node-type/implied_key_label_not_first.gql       (unsupported, ErrImpliedLabelIsKeyLabel, GG21)\n\nRe-run the sweep on the branch and show both survivors flip to caught.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T11:17:27Z","created_by":"areqag","updated_at":"2026-07-28T11:28:43Z","started_at":"2026-07-28T11:17:36Z","closed_at":"2026-07-28T11:28:43Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/531","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-76y","title":"resolver: scope.go's parameter-witness and RETURN * lanes are 4/12 pinned — Contains' edge-union arm, PropertyUseWitness' guards, and virtualProjection's var-length/carry arms are all unpinned","description":"A 12-mutation sweep over the cold methods in internal/resolver/scope.go caught only\n4. Seven of the eight survivors are reachable. Each was probed against social_r7.gql:\nthe five error-path cases are rejected today, the two type-path cases resolve to a\ntype the mutation changes.\n\n## Reachable, error path (fixture asserts a sentinel)\n\npartScope.Contains drops the edge-union arm -\u003e a parameter used on a multi-type edge\nproperty is never witnessed, so a conflict goes undetected:\n\n    MATCH (a:Person)-[r:AUTHORED|LIKES]-\u003e(b:Post) WHERE r.likedAt = $x RETURN a LIMIT $x\n        -\u003e parameter type conflict: parameter \"x\": property:TIMESTAMP vs scalar\n\nPropertyUseWitness drops the variable-length edge rejection. The existing coverage\n(var_length_edge_property_projection.cypher) is on the PROJECTION path\n(refProjectionType); the witness path has the same guard and no test:\n\n    MATCH (a:Person)-[r:KNOWS*1..3]-\u003e(b:Person) WHERE r.since = $x RETURN a\n        -\u003e query construct not supported at resolver stage R0: property projection\n           on variable-length edge binding\n\nPropertyUseWitness drops nullability demotion (prop.Nullable || nullableBinding), so\na NOT NULL property behind OPTIONAL MATCH is witnessed as non-nullable and stops\nconflicting. parameter_type_conflict_nullability.cypher does not reach this — it\nconflicts two non-optional bindings whose DECLARED nullability differs, so the\ndemotion term is never the deciding factor. Both node and edge arms are unpinned:\n\n    MATCH (p:Person) WHERE p.name = $x OPTIONAL MATCH (q:Person) WHERE q.name = $x RETURN p.name\n    MATCH (a:Person)-[r:AUTHORED]-\u003e(b:Post) WHERE r.views = $x OPTIONAL MATCH (c:Person)-[s:AUTHORED]-\u003e(d:Post) WHERE s.views = $x RETURN a\n        -\u003e parameter type conflict\n\nPropertyUseWitness stops reporting an unknown edge property (the node arm IS pinned):\n\n    MATCH (a:Person)-[r:KNOWS]-\u003e(b:Person) WHERE r.notAProp = $x RETURN a\n        -\u003e unknown property: r.notAProp\n\n## Reachable, type path (fixture is valid, the golden pins the type)\n\nvirtualProjection's RETURN * expansion has two unpinned arms. No returns_all_*\nfixture uses a variable-length edge or a carried projection alias:\n\n    MATCH (a:Person)-[r:KNOWS*1..3]-\u003e(b:Person) RETURN *   -\u003e [a node] [r list] [b node]\n        mutation types r as a plain edge instead of a list\n    MATCH (p:Person) WITH p.name AS n RETURN *             -\u003e [n property:STRING]\n        mutation drops the carry arm and the query is rejected (fail-closed)\n\n## Not a defect: scope.HasNode / HasEdge / HasCall are dead production code\n\nThe eighth survivor (HasEdge stops seeing edge-union bindings) is not a fail-open.\nAll three Has* methods have ZERO production callers — grep across internal/ excluding\n_test.go returns nothing. They are test-only affordances used by scope_test.go, and\nthe only HasEdge assertions there are negative (require.False), which is why removing\nan arm survives. Worth a separate cleanup decision, not a fixture.\n\n## Fix\n\nFive invalid fixtures, two valid fixtures with goldens, and a valid control for the\nnullability pair (same shape, both sides nullable, must still be accepted — otherwise\na blanket-reject mutation satisfies the negatives). Re-run the sweep on the branch and\nshow the survivors flip to caught.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T11:06:08Z","created_by":"areqag","updated_at":"2026-07-28T11:17:04Z","started_at":"2026-07-28T11:06:12Z","closed_at":"2026-07-28T11:17:04Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/528","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ag5","title":"resolver: R6 write-effect validators are 4/15 pinned — ON CREATE is entirely unvalidated, and every multi-type/var-length edge guard is fail-open","description":"A 15-mutation sweep over the validate*Effect family in internal/resolver/resolve.go\ncaught only 4. Nine of the eleven survivors are REACHABLE fail-opens: a query the\nresolver rejects on master becomes ACCEPTED under the mutation. Verified by probe,\nnot by reading — each query below returns an error today, and three control queries\nof the same shape without the offending part are accepted.\n\n## Reachable fail-opens (each needs an invalid fixture)\n\nMERGE ... ON CREATE is not validated at all. validateMergeEffect walks ON MATCH\neffects and ON CREATE effects through the same code, but only ON MATCH is pinned\n(invalid/merge_on_match_unknown_property.cypher). Delete the ON CREATE arm and the\nsuite stays green:\n\n    MERGE (a:Person) ON CREATE SET a.notAProp = 30 RETURN a\n        -\u003e unknown property: a.notAProp\n    MERGE (a:Person) ON CREATE SET a:NotALabel RETURN a\n        -\u003e unknown label: label \"NotALabel\" not declared on any node type\n\nThe variable-length edge guard is pinned for exactly one of the five places it\nappears. invalid/set_property_on_var_length_edge.cypher covers SET property on a\nsingle-type edge. The other four are unpinned:\n\n    MATCH (a:Person)-[r:AUTHORED|LIKES*1..3]-\u003e(b:Post) SET r.likedAt = date('2020-01-01') RETURN a\n    MATCH (a:Person)-[r:KNOWS*1..3]-\u003e(b:Person) SET r = {since: date('2020-01-01')} RETURN a\n    MATCH (a:Person)-[r:AUTHORED|LIKES*1..3]-\u003e(b:Post) SET r = {likedAt: date('2020-01-01')} RETURN a\n    MATCH (a:Person)-[r:KNOWS*1..3]-\u003e(b:Person) REMOVE r.since RETURN a\n    MATCH (a:Person)-[r:AUTHORED|LIKES*1..3]-\u003e(b:Post) REMOVE r.likedAt RETURN a\n        -\u003e invalid effect target: SET/REMOVE on variable-length edge \"r\"\n\nThe multi-type edge property lookup (unionProperty) is unpinned on the write path.\nReads are covered (unknown_property_union_*), writes are not:\n\n    MATCH (a:Person)-[r:AUTHORED|LIKES]-\u003e(b:Post) SET r.notAProp = 1 RETURN a\n    MATCH (a:Person)-[r:AUTHORED|LIKES]-\u003e(b:Post) REMOVE r.notAProp RETURN a\n        -\u003e unknown property: property r.notAProp missing on union member Person-[AUTHORED]-\u003ePost\n\n## Not defects\n\nThree survivors are diagnostic-only: dropping the edge-binding rejection in\nvalidateSetLabelsEffect / validateRemoveLabelsEffect, and the projection-alias\nrejection in validateDeleteEffect, each falls through to the \"not in any Part scope\"\nbranch and returns the SAME ErrInvalidEffectTarget sentinel. The suite asserts\nsentinels via errors.Is and never asserts message text, so these are indistinguishable\nby construction — consistent with the package's posture, not a hole. See\nbd memory resolver-formatdeclaredcarrying-diagnostic for the same call.\n\nTwo more (validateCreateEffect / validateMergeEffect rejecting an unbound variable)\nare documented defensive tripwires and were not probed; they may be unreachable.\n\n## Fix\n\nAdd invalid fixtures for the nine reachable cases against social_r6.gql, plus valid\ncontrols where the shape is otherwise untested. Re-run the sweep on the branch and\nshow the survivors flip to caught.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:55:14Z","created_by":"areqag","updated_at":"2026-07-28T11:05:45Z","started_at":"2026-07-28T10:55:21Z","closed_at":"2026-07-28T11:05:45Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/525","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-23o","title":"resolver: resolvedTypeEqual's edge-union arity check is unpinned — a prefix union is declared equal, and the reverse ordering panics","description":"Found by mutation sweep of internal/resolver. Dropping\n`len(aa.EdgeKeys) != len(bb.EdgeKeys)` from resolvedTypeEqual's\nResolvedEdgeUnion arm (resolve.go:339) survives the full suite.\n\nresolvedTypeEqual backs compareBranchColumns (resolve.go:312), the R5 UNION\ncolumn compatibility check. Without the arity guard the arm loops\n`for i := range aa.EdgeKeys` and indexes bb.EdgeKeys[i] unchecked, so there are\nTWO failure modes depending on which branch is longer — and note the call is\nresolvedTypeEqual(other[i].Type, base[i].Type), so aa is the non-zero branch:\n\n  branch 0 longer  -\u003e aa shorter, prefix matches, returns true\n                      FAIL-OPEN: two different edge-union types declared equal,\n                      and the model emits branch 0's type for a query whose\n                      other branch cannot produce it\n  branch 0 shorter -\u003e aa longer, bb.EdgeKeys[i] runs off the end\n                      PANIC on user input\n\nNeither is reachable today, and the reason is specific: the guard only matters\nwhen one key list is a strict PREFIX of the other. The obvious candidate pair\nis not — against social_r3.gql,\n\n  [r:AUTHORED|LIKES]-\u003e(post:Post)  -\u003e  [P-AUTHORED-Post, P-LIKES-Post]\n  [r:AUTHORED|LIKES]-(post:Post)   -\u003e  [P-AUTHORED-Post, Post-AUTHORED-Person,\n                                        P-LIKES-Post]\n\ndiffer at index 1, so the element-wise loop rejects them with the arity check\nalready removed. Verified empirically: both orderings still error under the\nmutation, and neither panics.\n\nA prefix pair needs three edge types between ONE ordered endpoint pair, so the\n2-set is the 3-set minus its last element:\n\n  [r:A|B]-\u003e(post:Post)    -\u003e [K1, K2]\n  [r:A|B|C]-\u003e(post:Post)  -\u003e [K1, K2, K3]\n\nNo schema fixture has 3+ edge types on a single ordered pair — checked all 9 in\ntest/data/resolver/*/schemas. So this needs a small purpose-built schema\n(a third Person-\u003ePost edge type alongside AUTHORED and LIKES) plus two UNION\nfixtures, one per ordering, to pin the fail-open and the panic separately.\n\nThe undirected 3-key set cannot be reached with a bare [r:AUTHORED]- because\nundirected single-type AUTHORED is rejected as ErrAmbiguousEdgeOrientation.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:43:54Z","created_by":"areqag","updated_at":"2026-07-28T10:54:34Z","started_at":"2026-07-28T10:44:08Z","closed_at":"2026-07-28T10:54:34Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/521","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ka1","title":"resolver: ErrParameterTypeConflict messages read 'scalar vs scalar' — String() drops the kind","description":"Observed while shipping gqlc-9ka. unifyParameterUsesAcrossBranches formats the\nconflict as:\n\n  fmt.Errorf(\"%w: parameter %q: %s vs %s\", ErrParameterTypeConflict, p.Name,\n             unified.String(), w.String())\n\nbut ResolvedScalar.String() returns the constant \"scalar\" and\nResolvedTemporal.String() returns the constant \"temporal\" (validated.go:247,\n:310). So a genuine kind conflict renders as:\n\n  parameter type conflict: parameter \"x\": scalar vs scalar\n  parameter type conflict: parameter \"x\": temporal vs temporal\n\nwhich tells the user nothing about which two kinds disagreed. Both are real\nconflicts — the fixtures added in gqlc-9ka produce exactly these strings.\n\nDiagnostic-only, hence P3: the sentinel is correct and TestInvalid asserts via\nerrors.Is, so nothing is fail-open. But it is the message a user actually sees\nwhen their query is rejected, and the information needed to fix it (bool vs\nint; date vs duration) is present and discarded.\n\nNote the resolver suite asserts NO error-message content anywhere today, so\nfixing this needs a decision about whether to start — see bd memory\nresolver-formatdeclaredcarrying-diagnostic for the same tension.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:41:21Z","created_by":"areqag","updated_at":"2026-07-30T02:09:07Z","started_at":"2026-07-30T01:55:03Z","closed_at":"2026-07-30T02:09:07Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/522","comments":[{"id":"019fa886-f185-7895-b6ce-924111c1405b","issue_id":"gqlc-ka1","author":"areqag","text":"Schema-side instance of the same policy question, found by mutation sweep at ce07430 (see gqlc-445).\n\ninternal/schema/gql's rejectInheritance formats its diagnostic as\n\n    fmt.Errorf(\"%w: %s\", ErrImpliedLabelIsKeyLabel, label)\n\nso the message names WHICH implied label collided. Replacing that with the bare\nsentinel survives the full suite: the corpus asserts sentinels through errors.Is\nand never reads message text, so the label name is unprotected.\n\nThat makes two suites with the same shape -- a message carrying information the\ntests cannot see. Whatever this bead decides about ErrParameterTypeConflict's\nkind should decide the schema corpus too, or say explicitly why not.","created_at":"2026-07-28T11:40:44Z"},{"id":"019fa8fe-73a5-736b-81ec-95df17cca48a","issue_id":"gqlc-ka1","author":"areqag","text":"This bead's premise is now empirically false, and that changes its\npriority. Evidence from the mutation sweep in gqlc-xem.\n\nThe description says: 'the sentinel is correct and TestInvalid asserts\nvia errors.Is, so nothing is fail-open'. Nine guards are fail-open,\nand the sentinel-only assertion is the reason.\n\nEvery per-variant effect validator in resolve.go ends in the same\nthree-arm ladder:\n\n    if _, ok := sc.edgeBindings[v]; ok         { ... ErrInvalidEffectTarget }\n    if _, ok := sc.carriedResolvedTypes[v]; ok { ... ErrInvalidEffectTarget }\n    return                                       ... ErrInvalidEffectTarget\n\nDeleting either of the first two arms makes control fall through to the\nthird, which raises the SAME sentinel. errors.Is cannot tell them\napart, so the suite stays green. Confirmed by mutation for all seven\nprojection-alias arms plus both SET/REMOVE-labels-on-edge-binding arms:\n\n  validateSetPropertyEffect     alias arm      SURVIVED\n  validateSetEntityEffect       alias arm      SURVIVED\n  validateSetLabelsEffect       alias arm      SURVIVED\n  validateSetLabelsEffect       edge arm       SURVIVED\n  validateRemovePropertyEffect  alias arm      SURVIVED\n  validateRemoveLabelsEffect    alias arm      SURVIVED\n  validateRemoveLabelsEffect    edge arm       SURVIVED\n  validateDeleteEffect          bare alias arm SURVIVED\n  validateDeleteEffect          prop alias arm SURVIVED\n\nThere are named fixtures for these — set_property_on_projection_alias,\nset_labels_on_projection_alias, delete_projection_alias and five more.\nThey reach the arm. They cannot pin it. Nine tests that look like\ncoverage and are not.\n\nSo the question this bead frames as diagnostic-quality ('the message a\nuser actually sees') is also a coverage question. Two ways out:\n\n  (a) assert message substrings for the fixtures where two arms share a\n      sentinel. Test-only, no exported-API change, closes all nine.\n  (b) split ErrInvalidEffectTarget into distinct sentinels. Changes\n      exported errors, so it wants an ADR — and TestSentinelReachability\n      would then require a fixture per new sentinel, which is the\n      coverage guarantee (a) only approximates.\n\nRelated: the same shape has now produced six confirmed\nsentinel-collision instances across the suite (bd memory\nsentinel-collision-disjunct-heuristic). This is the largest cluster.","created_at":"2026-07-28T13:51:16Z"}],"dependency_count":0,"dependent_count":0,"comment_count":2}
{"_type":"issue","id":"gqlc-9ka","title":"resolver: unify's scalar and temporal Kind discriminators are unpinned — a parameter used at two kinds silently takes the first","description":"Found by mutation sweep of internal/resolver. Two mutations of unify\n(internal/resolver/resolve.go:840) survive the full suite:\n\n  case ResolvedScalar:   drop `bb.Kind != aa.Kind`   SURVIVED\n  case ResolvedTemporal: drop `bb.Kind != aa.Kind`   SURVIVED\n\nunify is reached only from unifyParameterUsesAcrossBranches (resolve.go:421),\nso it IS the ErrParameterTypeConflict detector. With either check gone, a\nparameter witnessed at two different kinds unifies to whichever use is seen\nfirst, and the resolved model reports a confidently wrong parameter type —\nwhich downstream codegen turns into a wrong Go type on the parameter binding.\nFail-open, not diagnostic.\n\nThe four existing parameter_type_conflict_* fixtures all conflict at the type\nASSERTION (`!ok`), never at the Kind field:\n\n  clause_slot_vs_string    ResolvedProperty vs ResolvedScalar\n  property_vs_expr_bool    ResolvedProperty vs ResolvedScalar\n  two_properties           ResolvedProperty vs ResolvedProperty (Type differs)\n  nullability              ResolvedProperty vs ResolvedProperty (Nullable differs)\n\nNo fixture has ever put two ResolvedScalars, or two ResolvedTemporals, of\ndifferent kinds against each other. The Kind arms are dead for the whole suite.\n\nReachability proved against schemas/social_r7.gql — both already error today:\n\n  MATCH (p:Person) WHERE p.name = 'a' AND $x RETURN p.name LIMIT $x\n      ScalarBool (predicate) vs ScalarInt (LIMIT slot)\n      -\u003e parameter type conflict: parameter \"x\": scalar vs scalar\n\n  MATCH (p:Person) RETURN date($x) AS d, duration($x) AS e\n      TemporalDate vs TemporalDuration\n      -\u003e parameter type conflict: parameter \"x\": temporal vs temporal\n\nAnd the same-kind control resolves cleanly, so the fixtures fail for the kind\nmismatch rather than for using the construct at all:\n\n  MATCH (p:Person) RETURN date($x) AS d, date($x) AS e   -\u003e temporal/date\n\nNote the witness for an ExprUse is resolveType(uu.EnclosingType()) — the type\nof the ENCLOSING expression (scope.go:975) — which is why date($x) witnesses\nthe parameter as temporal/date.\n\nFix: two invalid fixtures mapped to ErrParameterTypeConflict, plus the\nsame-kind positive control as a valid fixture.\n\nSeparately observed, filed apart: both messages read \"scalar vs scalar\" /\n\"temporal vs temporal\" because ResolvedScalar.String() and\nResolvedTemporal.String() drop the kind.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:37:30Z","created_by":"areqag","updated_at":"2026-07-28T10:44:06Z","started_at":"2026-07-28T10:37:41Z","closed_at":"2026-07-28T10:44:06Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/518","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-wyh","title":"resolver: intersect is unpinned — no fixture has two edges touching one unlabelled variable, so cross-edge narrowing is fail-open","description":"Found by mutation sweep of internal/resolver (3/11 caught). intersect\n(internal/resolver/resolve.go:686) has 0.0% coverage and every mutation of it\nsurvives the full suite, including the maximally destructive one:\n\n  - `return a`                                 SURVIVED\n  - union instead of intersection              SURVIVED\n  - `return map[graph.LabelSetKey]struct{}{}`  SURVIVED\n  - candidateTypes: `acc = cand` (drop the accumulate, last edge wins) SURVIVED\n\nWhy: intersect exists to narrow an unlabelled binding's candidate set across\nseveral edges, and its only caller is candidateTypes (resolve.go:627) via\ninferUnlabelled (resolve.go:568). But every unlabelled fixture in\ntest/data/resolver has exactly ONE edge touching the unlabelled variable —\nunlabelled_binding_from_edge, unlabelled_binding_target_inferred,\nunlabelled_via_undirected, unlabelled_via_multi_type,\ncarry_wins_over_unlabelled_rebind. With one edge the accumulator is seeded and\nnever intersected, so the function body is dead for the whole suite.\n\nThis is a fail-open, not an equivalent mutant: reachability is proved. Against\nschemas/social_r7.gql this query resolves today and pins all four mutations at\nonce:\n\n  MATCH (a)-[:AUTHORED|KNOWS]-\u003e(x:Person), (a)-[:EMPLOYS|KNOWS]-\u003e(y:Person) RETURN a\n\n  edge 1 alone -\u003e {Person, Post}    (ErrAmbiguousBinding)\n  edge 2 alone -\u003e {Company, Person} (ErrAmbiguousBinding)\n  intersection -\u003e {Person}          resolves cleanly today\n\nThe intersection is strictly smaller than BOTH operands, so `return a` and\n`acc = cand` each yield an ambiguous binding, the union yields\n{Person, Post, Company} ambiguous, and the empty set yields ErrUnknownLabel.\nEvery surviving mutation becomes a failure.\n\nExisting invalid/ambiguous_unlabelled_binding.cypher pins that ambiguity is\nrejected; nothing pins that a second edge RESOLVES the ambiguity, which is the\nentire reason intersect is written.\n\nFix: add the fixture above to test/data/resolver/valid plus its golden and\nschema.mapping.json entry.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:30:13Z","created_by":"areqag","updated_at":"2026-07-28T10:37:35Z","started_at":"2026-07-28T10:30:18Z","closed_at":"2026-07-28T10:37:35Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/515","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0be","title":"schema/gql: a stale alternativeExemption is silently inert — requiredAlternatives' rejection branches are unpinned","description":"Mutation sweep of corpus_grammar_test.go at 70cba7d. All four rejection branches in requiredAlternatives (corpus_grammar_test.go:762-771) can be deleted with the whole suite green:\n\n  - `!candidates[ex.tag]` — exemption naming a tag that is not a candidate\n  - `exempt[ex.tag]` — duplicate exemption\n  - `ex.stolenBy == \"\" || ex.bead == \"\" || ex.why == \"\"` — missing fields\n  - `ex.stolenBy == ex.tag` — self-stealing exemption\n\nThe first is a genuine fail-open on a hand-maintained registry, the recurring shape in this package (cf. gqlc-ii8, gqlc-1uf, gqlc-d9b). Without the guard, an exemption whose tag stops being a candidate — a grammar change, or a typo — marks a tag nothing ever consults: the loop over invisible never matches it, so required is unchanged and the exemption silently stops excusing whatever it was written for. TestAlternativeExemptions does not catch this, because it asserts got.alternatives[ex.tag] is false and a tag no file can take is false for the stale entry too. The guard is the only thing standing between a stale exemption and silence.\n\nThe other three are integrity checks on the same registry, unpinned for the same reason: the checked-in list has exactly one well-formed entry, so no branch is ever taken.\n\nrequiredAlternatives already takes exemptions as a parameter precisely so a synthetic list can be supplied — the comment at :746-750 says so, and TestRequiredAlternativesDemandsTheThief uses it for the positive path. The rejection half was never written. TestIsValidFeature makes the identical argument one file over: 'the rejection half is the one that matters'.\n\nFix: a table test supplying one malformed exemption list per branch (three separate cases for the missing-field disjunction, so each disjunct is pinned individually) and asserting the error names the problem.\n\nNOT in scope, deliberately — three other survivors from the same sweep are message-sharpeners already backstopped by the membership goldens, not fail-opens:\n  - the root/cut-is-a-real-rule check (:705-709): a mistyped cut silently fails to apply, the closure explodes, and wantObligationRules fails by name.\n  - the cut-reachability check (:712-716): same backstop.\n  - scanGrammarRules' zero-rule guard (:486-488): an empty scan empties the closure and the goldens fail.\nTwo further survivors are inert against the SHA-pinned grammar and are forward-defence for a re-vendor: withoutLiterals (GQL.g4 has 0 parser rules containing a quoted literal) and cleanRuleBody's alt-label stripping (all 44 alt labels are lowercase-initial and none collides with a rule name, so grammarRefs' parserRules membership check already drops them). Both measured directly.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:18:32Z","created_by":"areqag","updated_at":"2026-07-28T10:28:37Z","started_at":"2026-07-28T10:18:36Z","closed_at":"2026-07-28T10:28:37Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/512","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-1us","title":"schema/gql: the lexer error listener is unpinned — dropping it silently accepts trailing garbage","description":"Mutation sweep of parser.go at 70cba7d: deleting `lex.AddErrorListener(l)` (Parse, parser.go:82-83) leaves the entire suite green. It is a genuine fail-open, not an equivalent mutant.\n\nProbed directly: `CREATE PROPERTY GRAPH TYPE T AS { (:A { p :: INT }) } #` currently yields `syntax error at 1:54: token recognition error at: '#'`. With the lexer listener removed it parses SUCCESSFULLY — nil error, one node type. Trailing garbage is silently accepted and a caller gets a schema it did not write.\n\nWhy nothing catches it: every negative fixture that exercises a bad character puts it where the *parser* also objects (`(:A$`, `% CREATE ...`, a stray backtick inside a body), so the parser listener alone still produces an error and the message merely changes. The discriminating shape is an unrecognisable token positioned where the parser is already content — after a complete graph type, where the token stream the parser sees ends at EOF either way. No fixture has that shape, so the lexer half of the error plumbing has never been exercised.\n\nFix: add `test/data/schema/gql/invalid/lexer_error.gql` carrying a trailing unrecognisable token, map it to nil in invalidFixtures (TestInvalid is fail-closed on registration, so the mapping is forced), and add a test that parses the same source with and without the offending token — the without-it parse must succeed, which is what proves the rejection is the lexer's doing and not the parser's, without coupling to ANTLR message wording.\n\nSibling of gqlc-p9v (PR #507): same package, same plumbing, same shape of gap.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T10:10:00Z","created_by":"areqag","updated_at":"2026-07-28T10:18:43Z","started_at":"2026-07-28T10:10:04Z","closed_at":"2026-07-28T10:18:43Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/509","dependencies":[{"issue_id":"gqlc-1us","depends_on_id":"gqlc-p9v","type":"discovered-from","created_at":"2026-07-28T06:10:04Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-211","title":"schema/gql: the lossiness rewrite changes every occurrence of a spelling, not the argument under test","description":"TestNoUndeclaredLossiness decides whether a type argument is discarded by rewriting it and comparing models. discardedArguments builds the variant with strings.ReplaceAll(src, spelling, ...), so it rewrites every occurrence of that spelling in the file rather than the occurrence the typeArgument scan reported.\n\nGQL spells CHAR/VARCHAR and BINARY/VARBINARY, and typeArgument's leading \\\\b makes VARCHAR(10) a single match. So in a file holding both VARCHAR(10) and CHAR(10), testing CHAR(10) rewrites the tail of VARCHAR(10) too, and the verdict returned under the name CHAR(10) is really a verdict about both.\n\nThe direction that bites is fail-open: if the neighbour records its argument and the one under test discards it, the variant's model differs and the discard goes unreported -- which is the exact silence this gate was written to close (gqlc-eh4).\n\nInert today because no type argument reaches the model at all, so both halves are discarded and the answer is right by accident. It activates the moment some arguments start reaching the model, i.e. when gqlc-5md lands -- which is when this gate starts having anything to distinguish.\n\nFix: rewrite at the offset typeArgument reported instead of by string match, and witness the mechanism directly (as perturb already is), because the corpus cannot witness a verdict that is currently correct either way.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:00:24Z","created_by":"areqag","updated_at":"2026-07-28T09:06:49Z","started_at":"2026-07-28T09:00:31Z","closed_at":"2026-07-28T09:06:49Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/489","dependencies":[{"issue_id":"gqlc-211","depends_on_id":"gqlc-eh4","type":"discovered-from","created_at":"2026-07-28T05:00:30Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ii8","title":"schema/gql: carrierExemptions violates its own stated cap, with 29 entries encoding one derivable rule","description":"`carrierExemptions` (corpus_carriers_test.go:109) documents its own failure mode and threshold:\n\n\u003e If this list grows past a handful the ownership model is wrong, not the exemptions — file the finding rather than adding a tenth entry.\n\nIt has **35 entries**. Nothing enforces the threshold: unlike the sibling `isoGaps` list in corpus_iso_test.go, which has `isoGapRatchet`, `carrierExemptions` is referenced nowhere outside its own file and has no cap.\n\n## The 29 are one rule, written 29 times\n\nMeasured against the grammar (`scanRuleSections` + `grammarObligation`):\n\n- **29 obligation names are declared under ISO clause 21** (21.1 identifier lexis, 21.2 literals/vocabulary). All 29 are currently exempted, and every one carries the same prose: `STRUCTURAL: ... No retirement path — clause 21 has no corpus directory by design.`\n- **0 clause-21 names are un-exempted**, so the partition is exact.\n- **6 exemptions are not clause 21**: `graphExpression` (11.1, the query-surface cut) and the five gqlc-h9n.27 section-heading misfits (`graphTypeLikeGraph`, `LIKE` at 12.4; `isOrColon`, `IS`, `COLON` at 16.7).\n\nThe list's stated value is that \"an exemption is a reviewer's checklist item with a stated reason\". Twenty-nine copies of an identical sentence are not a checklist — the premise the design rests on is contradicted by the data.\n\n## What to change\n\nSplit the register:\n\n1. `clause21Unowned` — a **membership golden** of the 29 names, one line each, with the reason stated once for the class. Exact-set matched against the obligation names whose section is 21.x, so it cannot drift and so adding a clause-21 name to the grammar reddens the build. That answers the doc's objection to widening (\"a widened rule is invisible\"): this widening is counted and diffed.\n2. `carrierExemptions` — keeps the 6 genuine entries, each with its own reason and bead. That is the \"handful\" the doc asked for.\n\nA membership golden rather than a count, per the corpus convention: a count would let one clause-21 name replace another silently.\n\n**No ratchet on the clause-21 list.** `isoGaps` ratchets because an unimplemented ISO production is debt that must not grow. A clause-21 name is not debt — it means the grammar named another identifier production — so capping it would be a number that gets raised rather than a guard.\n\n## Verification\n\nMutation sweep as with gqlc-cer/eh4/w2o: prove the exact-set match fires when a clause-21 name is added, removed, or moved between the two registers.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T08:24:17Z","created_by":"areqag","updated_at":"2026-07-28T08:36:31Z","started_at":"2026-07-28T08:24:21Z","closed_at":"2026-07-28T08:36:31Z","close_reason":"Landed as PR #487 (squash 2026-07-28). carrierExemptions cut 35 -\u003e 6; the 29 clause-21 names moved to clause21Unowned, a membership golden exact-set matched against the grammar by TestClause21Unowned. Mutation sweep: 10 mutations, 9 caught; the survivor (token attribution) is filed as gqlc-1uf.","external_ref":"https://github.com/areqag/gqlc/issues/485","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-w2o","title":"schema/gql: perturb type arguments as well as remove them, to close the no-verdict hole","description":"TestNoUndeclaredLossiness (gqlc-eh4, PR #482) decides whether a resolving corpus file discards a parenthesised type argument by removing the argument and re-resolving: an identical model proves the argument reached nothing. Removal is sound but incomplete, because the bare spelling is not always grammatical, and an ungrammatical variant yields no verdict rather than a verdict of \"lossless\".\n\nThe demonstrated instance is DECIMAL. decimalExactNumericType (GQL.g4:1832) puts notNull? inside the parenthesised group, so `DECIMAL(10,2) NOT NULL` cannot become `DECIMAL NOT NULL` — that is a syntax error, the comparison never happens, and a file of that shape could hide a discard indefinitely. The same is true of DURATION: bare `DURATION` does not parse (\"no viable alternative\").\n\n## What to change\n\nTry a *perturbed* argument before falling back to removal. Probed and confirmed working:\n\n- `DECIMAL(10,2)` vs `DECIMAL(11,3)` — parses, and the models collide, so the discard IS detectable this way\n- `STRING(5)` vs `STRING(6)` — parses, collides\n- `DURATION(DAY TO SECOND)` vs `DURATION(YEAR TO MONTH)` — parses, collides\n\nA numeric perturbation is mechanically derivable: if the argument is made only of digits, commas, whitespace and 0x/0b/0o literals, substitute a different literal of the same shape. That covers every gqlc-5md and gqlc-h9n.31 case and is grammatical wherever the original is, because the argument's syntactic slot is unchanged.\n\n## Residual after that\n\nA non-numeric qualifier still needs a substitute nobody can derive — DURATION's field list is the known example, and its two files are already registered as semantic cases, so nothing is hiding there today. Either leave it as a stated limit or register the qualifier alternatives by hand; the former is probably right until a second non-numeric qualifier exists.\n\n## Why it is P2 and not urgent\n\nThe sweep found nothing hiding behind this hole right now: every corpus file that spells a parenthesised argument is either declared lossy or is an unsupported entry. This closes a future gap, and makes the check's \"no verdict\" branch rare enough to be worth trusting.","notes":"PR #484 (branch test/w2o-perturb-type-arguments, commit eb55910). Replaced removal with a rewrite in discardedArguments; dropped removal entirely rather than keeping both.\n\nTwo findings beyond the bead's plan:\n\n1. The hole was live. scalar_decimal_precision_scale.gql is a real corpus file removal could not reach, and the corpus had ALREADY worked around it by hand — that entry's semanticCases sibling is DECIMAL(8), with a comment explaining bare DECIMAL will not substitute. This makes mechanical what an author had derived manually.\n\n2. Removal turned out to be redundant, not merely weaker. Probed every type keyword in the corpus vocabulary: none rejects T(1) while accepting bare T. So removal could not reach a case a rewrite misses. A mutation deleting the removal variant left the suite green, which is what settled it.\n\nVerified by 10 mutations, 9 caught. W10 (strip the bead+reason+semanticCases row from scalar_decimal_precision_scale.gql and repin wantSemanticCases) was SILENT before this change and now reddens TestNoUndeclaredLossiness — that is the regression this bead existed to make visible.\n\nW8 (drop the !ok guard) survived: DURATION() is not GQL either, so the outcome matches by accident of the grammar. Guard kept with a comment saying so rather than a claim it is witnessed.\n\nResidual unchanged and stated in the test doc: a non-numeric qualifier has no derivable rewrite. DURATION's field list is the only instance and both files are registered (gqlc-8pe).","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T08:04:57Z","created_by":"areqag","updated_at":"2026-07-28T08:21:32Z","started_at":"2026-07-28T08:08:19Z","closed_at":"2026-07-28T08:21:32Z","close_reason":"Landed PR #484 (merged bed5030). Replaced argument removal with argument rewriting in TestNoUndeclaredLossiness, and dropped removal entirely rather than keeping both — probing showed no type in GQL's vocabulary rejects a rewritten argument while accepting a bare one, so removal could not reach a case a rewrite misses, and the mutation deleting it left the suite green. Verified by 10 mutations, 9 caught; the headline (W10) is that undeclaring scalar_decimal_precision_scale.gql was silent before and now reddens the gate. Residual stated in the test doc: a non-numeric qualifier has no derivable rewrite (DURATION's field list, both files already registered under gqlc-8pe).","external_ref":"https://github.com/areqag/gqlc/issues/483","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-fer","title":"ci: decide whether required_status_checks.strict should be on for master","description":"Split out of gqlc-0qu, which deliberately did not bundle it.\n\n'Require branches to be up to date before merging' (required_status_checks.strict) is FALSE on master. Confirmed again 2026-07-27 while adding codegen-fence to the required contexts; strict was left as-is on purpose.\n\n## Why it matters\n\nWith strict false, a PR's green CI can be computed against a base that no longer exists by the time it merges. Observed on PR #421: its CI was computed against a base that #422 had already replaced, and both touched internal/schema/gql. The combined tree was checked by hand and was fine — but nothing forced that check.\n\n## Why it was not just turned on\n\nTurning strict on serialises every merge: each PR must be rebased onto the new master and re-run CI after any other PR lands. With an autonomous loop merging several PRs an hour, and two loops running concurrently as of 2026-07-27, that could become the dominant cost.\n\n## What to decide\n\nEither accept the risk and record why in an ADR, or turn strict on and measure the added wall-clock per merge. A middle option: leave strict off but add a merge-time guard that refuses when the PR's base SHA is not an ancestor of current master AND the two touch overlapping paths.\n\nWhichever is chosen, the reasoning belongs in an ADR, because the current state is an unrecorded default rather than a decision.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T03:23:14Z","created_by":"areqag","updated_at":"2026-07-30T02:09:08Z","started_at":"2026-07-30T01:55:05Z","closed_at":"2026-07-30T02:09:08Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/453","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-kwp","title":"ci: setup-just has no retry or cache — one network blip fails the job","description":"`.github/actions/setup-just/action.yml` installs the pinned `just` binary with a\nbare `curl | bash` on every job of every workflow:\n\n```\ncurl --proto '=https' --tlsv1.2 -sSf https://just.systems/install.sh \\\n  | bash -s -- --tag 1.55.1 --to \"$HOME/.local/bin\"\n```\n\nNo retry, no cache. Every job therefore has two network single points of failure\n(the install script, then the release tarball it fetches), and a transient reset\nfails the job outright before a single test runs.\n\nObserved 2026-07-27 on PR #443, job 89860643310, 15s in:\n\n```\ncurl: (35) Recv failure: Connection reset by peer\n##[error]Process completed with exit code 35.\n```\n\nA `gh run rerun --failed` cleared it, which is the manual workaround; the\npipeline fix is to make the install survive a blip.\n\n## Candidate fixes\n\n- `--retry 5 --retry-all-errors --retry-delay 2` on the curl — one line, covers\n  the script fetch. The tarball fetch inside `install.sh` is not covered by it,\n  so this alone is partial.\n- `actions/cache` on `~/.local/bin/just` keyed by the pinned version, with the\n  curl as the cache-miss path. Removes the network from the steady-state run\n  entirely and shaves the install off every job, which also helps wall time.\n\nPrefer the cache, with the retry as belt-and-braces on the miss path. Note the\nfirst run after adding a cache `path:` is cold by construction — judge steady\nstate on the second run, not the first.\n\n## Acceptance\n\nA CI run where the just install step is a cache hit and makes no network call,\nplus a green run with the registry made unreachable on the miss path (or, less\nstrictly, the retry flags present and a documented rerun-free week).\n","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-27T00:36:00Z","created_by":"areqag","updated_at":"2026-07-28T03:05:12Z","started_at":"2026-07-28T03:00:37Z","closed_at":"2026-07-28T03:05:12Z","close_reason":"Shipped in PR #448. Added actions/cache@v4 keyed on just-1.55.1-OS with conditional install + curl retry flags.","external_ref":"https://github.com/areqag/gqlc/issues/445","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-eh4","title":"schema/gql: corpus manifest lets lossy 'resolves' entries hide by omission","description":"corpus_test.go:385 gates the semanticCase requirement behind `if entry.bead != \"\"`. An entry that resolves lossily (discards a qualifier) but names no bead is structurally invisible to the check — the only assertion it faces is that its sentinel is nil. The guard catches bead-without-semantic-case; it cannot catch lossy-without-bead. Silence is a valid answer where it should not be.\n\n## The incident\n\nPR #439 (gqlc-h9n.4) shipped 11 entries flipped to `resolves`. Five of them (BINARY(4), BYTES(1, 10), VARBINARY(8), DURATION(YEAR TO MONTH), DURATION(DAY TO SECOND)) discard a declared qualifier — the same shape as the five pre-existing bead:gqlc-5md entries (CHAR(4), VARCHAR(10), STRING(5), STRING(2,5), DECIMAL(10,2)). All five walked through the gap on the first read, were caught by team-lead's pre-review, and were fixed in a follow-up commit on the same branch. The gap is demonstrated, not hypothetical, and demonstrated on a single commit.\n\n## What to change\n\nMake losslessness a positive declaration on the `resolves` outcome, not an omission. Design shape (author's call):\n\n- add a `lossless bool` (or `losslessness lossy|lossless`) field on corpusEntry\n- for outcome == resolves, require exactly one of: (lossless=true AND bead==\"\") OR (lossless=false AND bead!=\"\"\u0026\u0026reason!=\"\"\u0026\u0026semanticCase-row-exists)\n- an unset `lossless` on a `resolves` entry is a test failure with a message that names the choice being demanded\n\nEither shape closes the gap: the \"I forgot to say\" state stops being valid.\n\n## Out of scope for gqlc-h9n.4\n\ngqlc-h9n.4 is a schema/gql feature bead. Fixing the harness is a separate concern; the follow-up commit on that branch only fixes the entries that fell through. This bead tracks fixing the mechanism.","notes":"## Design: mechanical finder, not the declared field\n\nThe bead's suggested shape was a `lossless bool` / `losslessness lossy|lossless`\nfield on corpusEntry, required on every `resolves` entry. I built a mechanical\ncheck instead, and the reason is that the declared field is not load-bearing.\n\nToday `lossy` is exactly `bead != \"\"`: all 13 semantic cases are discarded\nqualifiers (gqlc-5md length/precision/scale, gqlc-8pe duration qualifier,\ngqlc-h9n.31 bit width), and the manifest already enforces resolving+bead ⟺\nsemanticCase. So the field would carry no information the bead field does not,\nand 42 of the 55 resolving entries would gain a boilerplate `losslessness:\nlossless` line. Its whole value would be the forcing function — and an author who\ndid not notice the qualifier will type `lossless` as readily as they typed\nnothing. The gate would be theatre in exactly the shape gqlc-cer was chartered to\nfind.\n\nTestNoUndeclaredLossiness takes the verdict off the author. For each resolving\nentry with no bead it removes each parenthesised argument from the file and\nre-resolves: a variant that produces the identical model proves the argument\nreached nothing. The author declares nothing, and cannot lie.\n\n## It found four live instances\n\nThe check was written against a green corpus and immediately failed on four\nentries that were `resolves`, `mandatory`, no bead, no reason — the exact\n\"hide by omission\" state, still present after PR #439's follow-up commit:\n\n- 18.9-value-type/scalar_dec_alias.gql — DEC(8) collides with bare DEC\n- 18.9-value-type/scalar_string_length_binary.gql — STRING(0b101) with STRING\n- 18.9-value-type/scalar_string_length_hex.gql — STRING(0xFF) with STRING\n- 18.9-value-type/scalar_string_length_octal.gql — STRING(0o17) with STRING\n\nAll four are gqlc-5md's discard (PropertyType has no length/precision field)\nreached through spellings nobody had connected to it: DEC is DECIMAL's alias, and\nthe three base files exist to exercise the lexer's binary/hex/octal literals, so\ntheir lossiness was incidental to why they were written. That is precisely the\nrow the bead predicted — \"the one whose author did not think identity was at\nstake\". wantSemanticCases 13 → 17; the entry and resolving counts are unchanged.\n\n## Limits, stated because the test cannot state them by passing\n\nIt is a search, not a proof. A variant that does not parse yields no verdict —\n`DECIMAL NOT NULL` is not GQL, notNull? sitting inside the parenthesised group —\nso a qualifier whose bare form is ungrammatical can still hide. The two duration\ncases are of that shape; both are already registered, so nothing is lost today,\nbut the hole is real and a future qualifier could sit in it. The check also only\nlooks at parenthesised arguments, which is the only demonstrated shape of\nlossiness in this model, not the only conceivable one.\n\n## Verified by mutation (gqlc-cer's method)\n\nFive mutations, all caught:\n- un-register one of the four (drop bead, reason and its semanticCase row) →\n  TestNoUndeclaredLossiness + TestCorpusSize\n- discardedArguments returns nil → the positive witness fires\n- discardedArguments reports everything it sees → the no-verdict witness fires\n- the regex stops matching type arguments → the positive witness fires\n- the no-verdict skip becomes a report → the no-verdict witness fires\n\nThe two witnesses exist because once the corpus is clean every file this test\nexamines has nothing to report, and a search that reports nothing is\nindistinguishable from a search that looks for nothing.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T23:41:47Z","created_by":"areqag","updated_at":"2026-07-28T08:05:46Z","started_at":"2026-07-28T07:54:15Z","closed_at":"2026-07-28T08:05:46Z","close_reason":"Landed PR #482. Built a mechanical finder rather than the proposed losslessness field: TestNoUndeclaredLossiness removes each parenthesised type argument from every resolving entry with no bead and re-resolves, treating an identical model as proof the argument was discarded. Declined the declared field because lossy is already exactly bead != \"\" (all 13 semantic cases are discarded qualifiers), so the field would carry no new information and would be only as good as the author's attention. The finder found four live instances hiding by omission — DEC(8), STRING(0b101), STRING(0xFF), STRING(0o17), all gqlc-5md's discard reached through spellings nobody had connected to it. wantSemanticCases 13 to 17. Verified by five mutations, all caught. Residual no-verdict hole filed as gqlc-w2o.","external_ref":"https://github.com/areqag/gqlc/issues/440","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-xtq","title":"schema/gql: register ErrEdgeKindArcMismatch as a provisional deviation, resolve against ISO semantics","description":"`gqlc-h9n.3` (PR #432) adds sentinel `ErrEdgeKindArcMismatch`: an edge type\ndeclaration whose `\u003cedge kind\u003e` contradicts its arc/connector direction is\nrejected as unsupported. Two corpus entries move under it —\n`18.3-edge-type/kind_undirected_arc_directed.gql` (UNDIRECTED + `-\u003e`) and\n`18.3-edge-type/phrase_form_undirected_connector.gql` (DIRECTED + `~`, previously\n`ErrUndirectedEdge`).\n\nThis is a **new deliberate deviation** and belongs in the gqlc-0ri register\nalongside `ErrLabelImplication` / `ErrUndirectedEdge` / `ErrUnsupportedSource`.\nIt is not yet recorded in any ADR.\n\n## Why this one is harder than the other three\n\nThe other three reject constructs we can argue have no faithful mapping onto the\nopenCypher/neo4j execution model, or reference catalog state a static generator\ncannot see. This one is different: **the grammar accepts the contradictory\nspelling.**\n\n`UNDIRECTED EDGE TYPE ... CONNECTING (a TO b)` parses. Confirmed by probe\n(2026-07-25, `/tmp/h9n2probe/leadconn`): `endpointPair` lists\n`endpointPairDirected` first, and ANTLR's prediction for `endpointPair` sees only\nthe parenthesised token run — `edgeKind` is never consulted, because\n`endpointPairPhrase` is a sibling of `edgeTypePhraseFiller` under\n`edgeTypePhrase`. The spelling yields a DIRECTED endpoint pair with\n`AMBIG alts={1,2} exact=true`.\n\nSo whether such a declaration is *valid ISO GQL* is not answerable from the\ngrammar. It is a Syntax-Rules / General-Rules question, and those live only in\nthe paid PDF. Three outcomes are possible and we cannot currently tell them\napart:\n\n1. ISO forbids the contradiction — our rejection is conformant, no deviation at\n   all, and the register entry should say so.\n2. ISO specifies a winner (kind wins, or arc wins) — our rejection **rejects a\n   valid ISO program**, which directly violates the owner principle \"we should be\n   able to accept any valid ISO GQL; we should not have our own GQLC flavour.\"\n3. ISO leaves it implementation-defined — rejection is fine but must be listed in\n   the implementation-defined register.\n\n## What this bead must NOT do\n\nDo not write an ADR that says \"we decided to reject, reasoning below, standard\nnot consulted.\" Where the standard is the only possible arbiter, that phrasing\nlaunders a coin-flip into documentation. A deviation record is honest when we\nmade a choice; it is not honest when we skipped the only source that could tell\nus whether there was a choice to make.\n\nRejecting is a defensible *interim* posture — it fails loudly rather than\nsilently picking a reading, which is strictly better than the pre-h9n.3 behaviour\nof resolving UNDIRECTED+`-\u003e` as though it were directed. Record it as interim and\nprovisional, blocked on the semantic text.\n\n## Acceptance\n\n- Register the sentinel in the gqlc-0ri deviation table, marked provisional.\n- The sentinel's doc comment in `internal/schema/gql/errors.go` must not imply\n  ISO backing for a decision we made ourselves. Reviewer on #432 has been asked\n  to check this specifically.\n- Resolve to outcome 1/2/3 once `gqlc-lir` (buy ISO/IEC 39075, CHF 227) is\n  decided. Cross-check `-implementation-defined.xml` at\n  standards.iso.org/iso-iec/39075/ed-1/en/ first — it is free, and if the\n  contradiction is listed there, outcome 3 is confirmed without the purchase.\n","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T22:59:26Z","created_by":"areqag","updated_at":"2026-07-28T13:07:09Z","closed_at":"2026-07-28T13:07:09Z","close_reason":"ErrEdgeKindArcMismatch moved to standalone var with provisional doc comment; corpus bead fields updated to gqlc-xtq; PR #551 merged. ISO implementation-defined.xml checked (127 items, none for edge kind/arc mismatch) — confirmed outcome 3 ruled out.","external_ref":"https://github.com/areqag/gqlc/issues/437","dependencies":[{"issue_id":"gqlc-xtq","depends_on_id":"gqlc-0ri","type":"parent-child","created_at":"2026-07-26T18:59:31Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-xtq","depends_on_id":"gqlc-lir","type":"blocks","created_at":"2026-07-26T18:59:31Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4jm","title":"ci: pin the iso.org free artefacts we cite, and detect drift","description":"Once we cite `ISO_IEC_39075(en).bnf.txt` and `ISO_IEC_39075(en)-features.xml`\nas normative sources, the citations are only as good as the copies we checked\nthem against. Those files are served from a live URL with no version in the\npath:\n\n    https://standards.iso.org/iso-iec/39075/ed-1/en/\n\nThey can change under us silently. There is a concrete reason to expect they\nwill: **ISO/IEC 39075:2024/CD Cor 1 is at Committee Draft stage** as of\n2026-01-06 (https://www.iso.org/standard/93701.html). When a corrigendum\npublishes, the BNF and features files are the most likely artefacts to be\nregenerated. A feature description changing, or a production gaining an\nalternative, would silently invalidate citations we had already landed.\n\n## What to do\n\nRecord a checksum of each artefact we cite, alongside the fetch date, and add\na check that the live file still matches. On mismatch the check should fail\nloudly and name the file — a corrigendum landing is a thing we want to know\nabout, not a thing to auto-accept.\n\n## Open question that must be answered first — do not skip it\n\n**Whether the ISO Customer Licence permits vendoring these files into a public\ngit repository is not established.** The licence text grants use \"in their\noriginal format without any modifications for the purposes specified in their\nrespective ISO standard(s)\". That is plausibly a use grant rather than a\nredistribution grant, and the two readings lead to different designs:\n\n- **If vendoring is permitted:** commit the files, diff against the live copy\n  in CI. Simple, hermetic, no network in the common path.\n- **If it is not:** commit only the checksums and fetch at check time. That\n  puts a network dependency in CI, so it must not be on the PR-blocking path\n  — an ISO outage would then block all merges. Run it on a schedule instead\n  and open an issue on drift.\n\nResolve the licence question before writing any code. Getting this wrong in\nthe permissive direction means committing a third party's copyrighted artefact\nto a public repo, which is not a defect we can fix by reverting.\n\n## Scope note\n\nThis is infrastructure for `gqlc-cfj` (Annex D feature ids) and the ISO-BNF\ncoverage gate. It is not a blocker for either — both can land citing the URL\nand the fetch date. File this as the durable version of that citation.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:32:40Z","created_by":"areqag","updated_at":"2026-07-30T03:24:04Z","started_at":"2026-07-30T03:14:07Z","closed_at":"2026-07-30T03:24:04Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/431","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nyo","title":"ci: gate that a PR body carries Closes #N when its bead has an external_ref","description":"Two PRs in one session (#421 h9n.16, #422 h9n.18) opened with no `Closes #N`\nline at all. Both were caught by hand at merge time. Different authors, same\nomission, so this is a missing gate rather than two mistakes.\n\n## Why it matters\n\nBeads IDs in a PR body do not auto-close the mirrored GitHub issue — GitHub\nonly recognises `Closes` / `Fixes` / `Resolves` plus the **GH issue number**.\nWhen the line is absent the PR merges, the bead gets closed by the lead, and\nthe GH issue silently stays open. The repo accumulates open issues whose work\nshipped weeks ago, and the bd↔GH mirror stops being trustworthy in the\ndirection people actually read it.\n\nThe existing memory `pr-body-closes-gh-issue` covers getting the *number*\nright. That is a different failure. Here the line was simply never written, so\nno amount of number-checking would have caught it.\n\n## What the gate cannot be\n\nNot a blanket \"every PR body must contain Closes #N\". Three legitimate\nexceptions:\n\n- `chore(bd)` export-sync PRs map to no issue at all.\n- Child PRs under an epic must **not** close the umbrella issue. `gqlc-h9n` is\n  `#359` and closing it early would wipe the epic's tracking.\n- Infra/tooling PRs filed without a bead.\n\nA gate that fires on those trains people to ignore it, which is worse than no\ngate.\n\n## Shape that works\n\nDerive the obligation from the bead, not from the PR:\n\n1. Extract the bead id from the PR body (`Bead: gqlc-xxx` is the house\n   convention) or the branch name. If there is none, PASS — nothing to check.\n2. `bd show \u003cid\u003e` → read the `External:` link. If the bead has no external_ref,\n   PASS.\n3. If it has one, require `Closes #\u003cthat exact number\u003e` in the body. FAIL\n   naming both the bead and the number if absent, and FAIL differently if a\n   `Closes #M` is present with M ≠ N — that is the wrong-close case\n   `workflow-verify-bd-gh-mapping-before-closes` describes, and it is worse\n   than absence because it closes someone else's issue.\n4. Exempt beads whose type is `epic`, so umbrella issues can never be targeted.\n\nStep 4 is the load-bearing one. Without it the gate would demand `Closes #359`\non every child PR of the epic and actively cause the harm it exists to prevent.\n\n## Notes for whoever takes this\n\n- Needs `bd` available in CI, or the external_ref has to come from the\n  committed `.beads/issues.jsonl` export instead. The export is the cheaper\n  read and is already in the tree — but note it is a *passive* export and can\n  be stale, so a bead filed in the same session as the PR may not be in it yet.\n  If that turns out to be common, PASS on unknown-bead rather than FAIL, and\n  say so in the message; a gate that blocks on its own staleness gets disabled.\n- Mutation-test it against #421 and #422 as they were at open time (bodies with\n  no `Closes` line) and confirm it FAILS naming `#403` and `#401`. Then against\n  #423 (`chore(bd)`, no bead) and confirm PASS.\n- Fold into an existing job. This is one `bd show` and one grep.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:13:54Z","created_by":"areqag","updated_at":"2026-07-28T03:31:35Z","started_at":"2026-07-28T03:14:17Z","closed_at":"2026-07-28T03:31:35Z","close_reason":"Shipped in PR #450. New .github/scripts/check-pr-closes.py + step folded into tidy job.","external_ref":"https://github.com/areqag/gqlc/issues/426","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.29","title":"BLOCKED (no ISO arbiter, gqlc-lir): retire or confirm ErrEndpointFillerHasProperties against ISO 18.3","description":"\u003e **BLOCKED — permanently, as of 2026-07-27. Do not resolve this by inference.**\n\u003e\n\u003e This question is answerable only from ISO/IEC 39075's General Rules, which are\n\u003e in the paid PDF. `gqlc-lir` decided against the CHF 227 purchase, so there is\n\u003e no arbiter and there will not be one. The free BNF positively proves the\n\u003e ambiguity is not resolvable syntactically.\n\u003e\n\u003e A web-search paraphrase, a vendor conformance table, or an analogy to SQL is\n\u003e **not** a citation. If gqlc must pick a behaviour to ship, record it as a gqlc\n\u003e decision under gqlc's own name — never as \"ISO says\" — and leave this bead\n\u003e open, because the standards question stays unanswered either way.\n\nOpened 2026-07-26 alongside PR #422, which introduces\n`ErrEndpointFillerHasProperties`. That sentinel is correct today but may be\nprovisional, and this bead holds the evidence needed to decide.\n\n## What #422 settled and what it did not\n\nBefore #422, an inline endpoint filler carrying properties —\n`(:Person)-[:AUTHORED]-\u003e(:Post { extra :: STRING })` — parsed, resolved with\n`err=nil`, and produced a model byte-identical to the label-only spelling. The\nproperty declarations had no consumer and were discarded. That is the epic's\nsignature defect: accepted input, nil error, wrong model.\n\n#422 rejects the spelling instead. What it did NOT settle is what the spelling\n*means*, and the implementer was explicit that the grammar does not adjudicate:\n\n- **Declaration reading.** `propertyType` is `propertyName typed?\n  propertyValueType` — that is declaration syntax, not constraint syntax. On\n  this reading the filler declares properties on the endpoint node type, and\n  supporting it needs merge-and-conflict semantics against the standalone\n  declaration of that node type.\n- **Reference reading.** Alt 2 of `sourceNodeTypeReference` /\n  `destinationNodeTypeReference` (GQL.g4:1620/1625) strips both the\n  `nodeSynonym TYPE? nodeTypeName` prefix and the `localNodeTypeAlias?` from\n  the full `nodeTypePattern`. CONTEXT.md:50 already codifies the endpoint as\n  \"resolved to the referenced node type's label set\". On this reading the\n  properties are a constraint over something declared elsewhere.\n\nThe load-bearing observation, and the reason #422 could land without waiting:\n**accept-and-discard is not correct under either reading**, so the sentinel is\nright now regardless of how this resolves.\n\n## What this bead must produce\n\nThe ISO/IEC 39075:2024 subclause 18.3 semantic sentence for alt 2 of\n`\u003csource node type reference\u003e` / `\u003cdestination node type reference\u003e`. A\ncitation, not an inference — `gqlc-cfj` is open because this repo has a habit of\nasserting what the standard says without sourcing it, and the two readings above\nare exactly the kind of gap that gets filled by plausible-sounding reasoning.\n\nThen one of:\n\n1. **Declaration.** Implement merge semantics against the standalone\n   declaration, and decide what a *conflicting* redeclaration does (same\n   property name, different value type). Lift the sentinel. Note this drags in\n   whatever gqlc decides about one-graph-type-per-file scoping.\n2. **Constraint.** Implement satisfaction-checking: the referenced node type\n   must already declare a compatible property signature, else a *different*\n   error — a constraint violation, not an unsupported-construct rejection.\n   Overlaps gqlc-h9n.22 (plural label satisfaction, property intersection),\n   which is the same shape of question one level up. Sequence after it.\n3. **Standard does not specify.** Keep the sentinel permanently, record it in\n   the gqlc-0ri deviation register, and rewrite the error's doc comment from\n   \"not yet supported\" to a stated deviation. Silence in the standard is itself\n   the finding and must be written down, not left as an open bead forever.\n\n## Guardrails for whoever takes this\n\n- The retirement must be a **local** change. #422 was reviewed on the claim that\n  lifting the sentinel later is not a redesign. If implementing (1) or (2) turns\n  out to require restructuring how endpoints resolve, that invalidates a premise\n  of #422's review and should come back to team-lead rather than being absorbed\n  quietly.\n- `(:Post {})` with empty braces is deliberately ACCEPTED and pinned by\n  `TestEndpointFillerEmptyBraceAllowed`. Any widening must keep that pin green —\n  it exists because an ANTLR accessor is non-nil for the empty-braces form, so a\n  nil-check guard would over-reject. Do not regress it into an existence test.\n- Symmetry is a stated invariant: source and destination behave identically,\n  including under left-pointing-arc canonicalisation. Whatever replaces the\n  sentinel keeps that property or explains why not.","status":"open","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T17:10:36Z","created_by":"areqag","updated_at":"2026-07-27T13:08:47Z","external_ref":"https://github.com/areqag/gqlc/issues/427","dependencies":[{"issue_id":"gqlc-h9n.29","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T13:10:36Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-h9n.29","depends_on_id":"gqlc-lir","type":"blocks","created_at":"2026-07-26T13:58:57Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f9f7c-dcb9-7088-a192-888b4def0693","issue_id":"gqlc-h9n.29","author":"areqag","text":"2026-07-26, team-lead. Same verdict as gqlc-h9n.28: BLOCKED ON ISO PURCHASE, not on research.\n\nISO's free normative BNF (https://standards.iso.org/iso-iec/39075/ed-1/en/, verified by fetch) confirms the two-alternative structure this bead is about, verbatim at line 1250:\n\n    \u003csource node type reference\u003e ::=\n        \u003cleft paren\u003e \u003csource node type alias\u003e \u003cright paren\u003e\n      | \u003cleft paren\u003e [ \u003cnode type filler\u003e ] \u003cright paren\u003e\n\n    \u003cdestination node type reference\u003e ::=\n        \u003cleft paren\u003e \u003cdestination node type alias\u003e \u003cright paren\u003e\n      | \u003cleft paren\u003e [ \u003cnode type filler\u003e ] \u003cright paren\u003e\n\nSo the second alternative really does admit a full \u003cnode type filler\u003e — the bead's premise is sourced, not assumed. But whether that filler DECLARES a node type or REFERENCES one is a semantic question the BNF cannot answer, and both readings stay viable. Subclause 18.3's General Rules are the arbiter and they are in the paid PDF only.\n\nDifference from h9n.28, and why this one is lower stakes: PR #422 already fixed the immediate defect (endpoint fillers carrying properties were being accepted and discarded — accepted input, nil error, wrong model). What remains is only whether ErrEndpointFillerHasProperties should be RETIRED, i.e. whether we are currently rejecting something the standard permits. Rejecting a valid construct is a visible, loud failure that a user can report; it is not the silent-wrong-model class. So this does not on its own justify the CHF 227.\n\nRecommendation: hold at current state. If the standard is purchased for h9n.28, resolve this in the same sitting — the marginal cost is one subclause read.","created_at":"2026-07-26T17:33:08Z"}],"dependency_count":1,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-h9n.26","title":"test(schema): gate resolving-path incidental carriers, not just parse-time ones","description":"Found 2026-07-26 by the gqlc-h9n.17 non-vacuity check, which is the best kind of\nfinding: the new gate was run against the two historical orphans it was meant to\nprevent, reproduced one, and did NOT reproduce the other.\n\n## The two carriage classes\n\n`gqlc-h9n.17` gates **parse-time** incidental carriage: a grammar name that the\nwhole corpus reaches but that no file in the *owning* area reaches. Verified by\nreplay at `0b000c60` (parent of the TIMESTAMP fix), where it correctly reports\n`localdatetimeType#2 (alt) owned by D1, D2`.\n\nReplayed at `22e02c02` (has the TIMESTAMP fix, lacks the NODE-synonym fix\n`7217b180`) it **passes**, i.e. it does not catch the NODE orphan. That is not a\nbug in h9n.17. The `NODE` token IS entered at that SHA — by\n`18.2-node-type/phrase_form.gql` and `18.9-value-type/constructed_node_closed.gql`\n— so parse coverage is genuinely satisfied. But **both of those files are\n`unsupported`**: the listener rejects them after the parse succeeds, so nothing\ndownstream of the parse tree ever sees the name.\n\n`7217b180`'s own commit message says it exactly: \"no *resolving* path exercised\nan explicit node synonym in nodeTypePattern\".\n\nSo there are two distinct classes and we currently gate one:\n\n- **parse-time carriage** — some file's parse tree enters the name. GATED by\n  h9n.17.\n- **resolving-path carriage** — some file that actually *resolves* enters the\n  name. NOT GATED. A name can be carried exclusively by files that are expected\n  to fail, and every existing gate stays green.\n\nThe second class is strictly the more dangerous one, because \"the only files\nexercising this construct are ones we reject\" is a coverage illusion that reads\nas coverage in every report we currently produce.\n\n## Why it was not folded into h9n.17\n\nh9n.17's method spec is written in terms of the coverage gate, which measures\nthe parse tree. Measuring resolving-path carriage needs a different instrument:\nrun `Parse` (not just the parser) over every corpus file, keep only the files\nwhose outcome is `resolves`, and recompute carriage over that subset. That is a\nsecond traversal with different inputs, not a parameter on the existing one.\n\n## Sketch\n\nPlausibly cheap: `carried_resolving(area)` = carriage computed over\n`{f in area : outcome(f) == resolves}`, compared against the same\nsection-derived ownership map h9n.17 already builds. The ownership machinery,\nthe exemption list, and the orphan-reporting format are all reusable as-is;\nonly the file filter changes.\n\nOpen question worth deciding before implementing: should the obligation set for\nthe resolving gate be the same grammar closure? A construct we deliberately\nreject (see gqlc-h9n.6) can never have a resolving carrier, so the resolving\ngate would report it as a permanent orphan. Either the obligation set is\nnarrowed to resolvable constructs, or every deliberate rejection needs an\nexemption — and the second option turns the exemption list into a duplicate of\nthe sentinel registry. Prefer the first, but measure how many names it moves\nbefore committing to it.\n\nRelates to gqlc-h9n.13 (parse-shape recording promoted from report to gate) and\ngqlc-h9n.24 (the manifest cannot express a changed-but-still-resolving model).\nAll three are the same underlying complaint: our instruments measure the parse\ntree, and most of the defects this epic finds live downstream of it.\n","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T07:17:53Z","created_by":"areqag","updated_at":"2026-07-28T05:47:04Z","started_at":"2026-07-28T05:32:59Z","closed_at":"2026-07-28T05:47:04Z","close_reason":"Shipped in PR #465. TestCorpusResolvingCarriers gates resolving-path carriage in two tiers: corpus-wide (does any resolving file reach the name) and per-area (does the owning area). The bead's open question, answered by measurement first: neither listed option survives. Narrowing the obligation to resolvable constructs is VACUOUS on the motivating case — before 7217b180 no file spelling NODE resolved, so a corpus-derived narrowing would have dropped NODE and missed the defect. Per-name exemptions would be ~89 entries carrying 89 copies of 7 reasons. Shipped: full obligation kept, excuse derived from the sentinel (7 accounts), names still listed as the pin, and the account itself checked — every carrier of a listed name must reject with that exact sentinel. Tier 2 found a real gap on first run: 18.3-edge-type owns the alias endpoint alternatives but every area-C file reaching them was unsupported; pattern_endpoint_alias.gql closes it. Also corrected six carrier exemptions that wrongly claimed this gate would retire them (verified by mutation: they are structurally unowned by section prefix).","external_ref":"https://github.com/areqag/gqlc/issues/404","dependencies":[{"issue_id":"gqlc-h9n.26","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T03:17:53Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.25","title":"schema/gql: non-power-of-two binary precision (INT(7)) silently widens to machine int — decide and record","description":"Noticed 2026-07-26 while ratifying the gqlc-h9n.16 width fold. Not a blocker for\nthat bead — this behaviour is byte-identical before and after the fold — but the\nfold makes the question unavoidable, so record the answer rather than inherit it.\n\n## The question\n\n`GQL.g4:1801` is `INT (LEFT_PAREN precision RIGHT_PAREN)? notNull?`, under\n`binaryExactNumericType`. Precision there is a **bit width** (that is what\nseparates `binaryExactNumericType` from `decimalExactNumericType`, whose\nprecision pairs with a `scale` — GQL.g4:1788 vs 1831).\n\n`INT(8)` has an exact counterpart in the model (`TypeInt8`) and gqlc-h9n.16\nfolds it. `INT(7)` does not. Today, and after h9n.16, it canonicalises to `INT`\nand resolves to `graph.TypeInt` — a machine int. The schema author said seven\nbits; the model says sixty-four, silently.\n\nThat is precisely what ADR 0002's rationale forbids:\n\n  \"Losing signedness and width would force codegen to emit int/uint everywhere\n   and discard information the schema author explicitly stated.\"\n\nSo the status quo for non-power-of-two widths contradicts the same sentence that\njustifies fixing the power-of-two ones. h9n.16 fixes the half with an exact\nmodel counterpart and leaves the other half exactly as it was.\n\n## Why it was not just fixed inside h9n.16\n\nThe obvious fix — reject `INT(7)` with `ErrUnsupportedType` — collides with a\nstanding project directive: *if it is valid ISO GQL, we should allow it; we do\nnot get our own gqlc flavour.* `INT(7)` is valid ISO GQL. Narrowing acceptance\nto protect model fidelity trades one stated goal against another, and that\ntrade needs a decision, not an implementer's judgement call at edit time.\n\n## Candidate resolutions\n\n1. **Status quo, recorded.** `INT(7)` -\u003e `TypeInt`. Accepts everything valid;\n   loses the stated width. Requires an entry in the gqlc-0ri deviation register\n   saying so out loud, and a comment at the fallback in\n   `internal/schema/gql/propertytype.go` naming it lossy.\n2. **Round up to the next supported width.** `INT(7)` -\u003e `TypeInt8`,\n   `INT(10)` -\u003e `TypeInt16`, `INT(24)` -\u003e `TypeInt32`. Representationally safe\n   in the sense that the wider type holds every value of the narrower one, and\n   loses far less than jumping to 64. BUT it depends on reading precision as a\n   *minimum* capacity (\"at least p bits\") rather than an exact one. Nobody here\n   has sourced that from ISO 39075, and gqlc-cfj is an open bead about exactly\n   this habit of citing the standard without sourcing it. Do not adopt this\n   without a citation.\n3. **Reject.** `ErrUnsupportedType` for any precision without an exact model\n   counterpart. Maximum fidelity, violates the accept-all-valid-GQL directive.\n\n## What to do\n\nPick one and write it down. If the answer is (1) — which is the cheapest and\nthe current behaviour — that is a fine answer, but it has to be *stated*, because\nright now a reader of ADR 0002 would reasonably predict (3) and a reader of the\nproject directive would reasonably predict (1), and nothing in the tree\nadjudicates.\n\nApplies equally to `UINT(p)`, `INTEGER(p)`, and `FLOAT(p)` for any p without an\nexact counterpart. `DECIMAL(p,s)` is NOT in scope: its precision is decimal\ndigits, not bits, and it is gqlc-5md's problem.\n","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T07:13:36Z","created_by":"areqag","updated_at":"2026-07-28T05:20:19Z","started_at":"2026-07-28T05:08:22Z","closed_at":"2026-07-28T05:20:19Z","close_reason":"Decided in docs/adr/0017-off-lattice-binary-widths-accepted-and-lost.md, PR #463\n(squash 3ba0f7b… see origin/master), merged 2026-07-28.\n\n## The answer: option 1, accept and record\n\nINT(7) / INT(10) / UINT(p) / INTEGER(p) at any width PropertyType cannot express\nresolve to the unqualified type. FLOAT(p) likewise at every p.\n\n## Why, in one paragraph\n\nADR 0016's discriminator (an undirected edge answers IS DIRECTED *incorrectly*,\nso rejecting removes a wrong answer) does not transfer, and the reason is about\nwhat the alternative costs rather than what is lost. For INT(7), the alternative\nto accepting is not compiling the schema at all: a property declaration is one\nline inside a graph type, and rejecting it takes down every node type, edge type\nand other property in the file to suppress one discarded qualifier. Rejecting\ndoes not make gqlc right about the width — it makes gqlc unable to say anything.\n\nThe nearest analogue to IS DIRECTED was checked rather than assumed away:\nvalueTypePredicate (GQL.g4:2052, `\u003cvalue expression\u003e IS [NOT] TYPED \u003cvalue\ntype\u003e`) is real, takes a valueExpressionPrimary — a *value*, where what gqlc\ndiscards is a *declaration* — and gqlc has no GQL query surface (ADR 0007). The\nresidual \"could ISO's General Rules reach a declared type\" is paywalled\n(gqlc-lir) and is flagged in the ADR as a hedge, not offered as proof. The\ndecision does not rest on it.\n\nOption 2 (round up) declined twice over: it reads precision as a minimum, which\nnobody has sourced — this bead's own instruction, gqlc-h9n.28's standing rule,\ngqlc-cfj's open habit-bead — and it buys no correctness anyway, since a signed\n7-bit value in an int8 is the same permissive-imprecise wrong in a smaller\nquantity.\n\nOption 3 (reject) also answered against ADR 0006's conservatism argument, which\na reviewer will reach for: that asymmetry is about gqlc's own inferences.\nNarrowing the accepted *language* is a different act — a rejection of valid ISO\nGQL is not a default that can be relaxed later, it is a gqlc dialect for as long\nas it stands.\n\n## Delivered beyond what was asked\n\nThe bead asked for a comment at the propertytype.go fallback and a deviation\nregister entry. Both done, but the loss is now *asserted* rather than described,\nusing the siblings machinery from gqlc-h9n.21 (PR #461):\n\n- scalar_signed_integer.gql — INT(10), siblings INT / INTEGER / INTEGER(10) / INT(7)\n- scalar_unsigned_integer.gql — UINT(10), siblings UINT / UINT(7)\n- scalar_float.gql — FLOAT(10), siblings FLOAT / FLOAT(10, 2)\n\nINT(8) is deliberately absent from that class: it folds, and its absence is the\ncontrast that shows the loss is jagged rather than uniform. Verified by mutation\nboth ways — adding INT(8) reddens TestSemanticCaseCollisions on exactly that\nsubtest; deleting an entry's bead/reason while leaving its semanticCase reddens\nTestCorpusManifest. wantSemanticCases 10 -\u003e 13.\n\n## Register correction\n\ngqlc-0ri gains no table row, and has a note saying why: its table is one row per\n*rejection sentinel*, and this decision adds no rejection. A row there would be\nthe half-wrong row ADR 0016 declined to write when it split ErrUnsupportedSource.\n\n## Follow-up filed\n\ngqlc-h9n.31 — declared bit width on PropertyType, third sibling to gqlc-5md and\ngqlc-8pe, blocked on the same bar (name the consumer first). Sibling rather than\npart of gqlc-5md because the model here *has* a width axis and quantises it, and\nbecause integer widths are ordered and nested, making \"unify to the wider\" an\navailable answer with no counterpart for string lengths.\n\ngqlc-h9n.28 is unaffected and stays open; answering it could move FLOAT(p)'s\nownership from gqlc-h9n.31 to gqlc-5md.","external_ref":"https://github.com/areqag/gqlc/issues/405","dependencies":[{"issue_id":"gqlc-h9n.25","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T03:13:36Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.24","title":"test(schema): the corpus manifest cannot express \"resolves to a different right thing now\"","description":"Surfaced 2026-07-26 while designing gqlc-h9n.16 (Problem A, the width fold).\n\n## The gap\n\nA corpus entry's `outcome` records whether a file parses, resolves, or is\nrejected. It cannot express **\"this file resolves to a DIFFERENT right thing\nafter this change than it did before\"**.\n\nConcretely: after the width fold, a file declaring `x :: INTEGER(8)` resolves\nbefore AND after. Outcome is `resolves` in both cases. The manifest is satisfied\nby both. But the model it resolves to changes from `TypeInt` to `TypeInt8` —\nwhich is the entire point of the change, and the corpus is blind to it.\n\nThis is the same blindness as gqlc-uhb (phrase forms parsed and silently\ndiscarded) seen from the other side. There, a wrong model hid behind a passing\n`resolves`. Here, a *newly correct* model hides behind the same passing\n`resolves`, so the corpus cannot certify that the fix took effect, and — worse —\ncould not have caught a regression that silently un-fixed it later.\n\n## Why this is not a duplicate of gqlc-h9n.13\n\ngqlc-h9n.13 promotes **parse-shape** recording from report to gate. Parse shape\nis unchanged here: `INTEGER(8)` parses identically before and after the fold.\nThe divergence is entirely downstream of the parse tree, in normalisation. A\nparse-shape gate would not fire.\n\n## Why this is not a duplicate of gqlc-h9n.21\n\ngqlc-h9n.21 asserts the model collision a `semanticCase` *describes*. That is a\nmechanism for cases the harness already knows are about model identity. This\nbead is about ordinary corpus rows, whose whole contract today is an outcome\nstring.\n\n## Candidate shapes (not yet decided)\n\n1. An optional `model:` assertion on a corpus row — a small structural\n   fingerprint (property name -\u003e resolved type constant) checked when present.\n   Cheap; opt-in; only as good as the rows that opt in.\n2. A golden model snapshot per corpus file, diffed in CI. Catches everything,\n   including changes nobody thought to assert; costs a rebaseline ritual on\n   every intentional change, which gqlc-exl already shows is expensive here.\n3. Leave the corpus alone and require every normalisation change to carry its\n   own pin table (which is what gqlc-h9n.16 Problem A does). Zero machinery;\n   relies on author discipline; the failure mode is silent.\n\nOption 3 is the status quo. The bead exists to decide whether that is\ndeliberate. Record the decision either way — if the answer is \"the corpus is a\ncoverage instrument, not a model-identity instrument\", that belongs in writing\nnext to the manifest, because two beads have now independently expected\notherwise.\n","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T07:11:43Z","created_by":"areqag","updated_at":"2026-07-28T05:44:17Z","started_at":"2026-07-28T05:21:22Z","closed_at":"2026-07-28T05:44:17Z","close_reason":"Shipped in PR #464. Decision recorded next to the manifest: the corpus is a coverage instrument, not a model-identity instrument. Option 1 (opt-in model: field) is only as good as the rows that opt in; option 2 (golden snapshots) is ruled out by gqlc-exl's evidentiary-circularity doctrine. Shipped option 3 mechanised: TestTypeSpellingsEveryRowPinned computes a grounding closure over hand-written pin/equivalence tables and fails while any typeSpellings row lacks an end-to-end assertion. Filled three real gaps (FLOAT32/64/128). DURATION needed no exemption — DURATION(YEAR TO MONTH) grounds it via the same truncateParenthetical fallback the production code takes. Verified by three mutations.","external_ref":"https://github.com/areqag/gqlc/issues/406","dependencies":[{"issue_id":"gqlc-h9n.24","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T03:11:43Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.22","title":"resolver: plural label satisfaction — property intersection, whole-entity rejection, and the ADR","description":"Step 2 of the gqlc-h9n.7 split. Step 1 makes subset matching work but refuses\nany query whose label set is satisfied by two or more declared node types. This\nbead makes the plural case resolve.\n\n## The rule\n\nA query label expression resolves to the **set** of declared node types whose\nlabel set is a superset of it (with an exact match winning outright, per step 1).\nWhere that set is plural:\n\n- a **property** reference resolves to the **intersection** — identical name,\n  type and nullability across every member, else `ErrUnknownProperty`;\n- a **whole-entity** reference (`RETURN p`) is **rejected**.\n\nThe property half is not a new commitment: `unionProperty`\n(`internal/resolver/resolve.go:731`) already implements exactly this for\nmulti-candidate edges, including the type-mismatch case\n(`resolve.go:744-746`). Substitute `[]schema.NodeType` for `[]schema.EdgeKey`.\nProbed behaviour on the edge lane today:\n\n    match (a:A)-[r:R|S]-\u003e(b:B) return r.w      -\u003e OK, INT\n    match (a:A)-[r:R|S]-\u003e(b:B) return r.only   -\u003e unknown property: missing on union member A-[S]-\u003eB\n    match (a:A)-[r:R|S]-\u003e(b:B) return r.skew   -\u003e unknown property: type differs: property:INT vs property:STRING\n\n## Why whole-entity is rejected rather than unioned\n\nThe obvious move is to copy the edge side's synthesised sealed interface\n(`type TR interface{ isTR() }` with a `switch rel.Type` dispatch). **It does not\ncarry over, and this is the crux of the bead.** The edge dispatch is total\nbecause a Neo4j relationship has exactly one type, so `rel.Type` is a single\nstring. The node analogue would dispatch on `dbtype.Node.Labels`, which is a\n**set**, and under the very superset rule this bead introduces one runtime node\ncan satisfy two declared types at once: a node labelled\n`{Person, Employee, Contractor}` conforms to both `Person\u0026Employee` and\n`Person\u0026Contractor`. \"First match wins\" makes the emitted struct depend on map\nordering and there is no honest `default`.\n\nThe considered alternative was a per-query **intersection struct** carrying only\nthe common properties. It is implementable and total (decode the intersection\ndirectly, no dispatch) — it is not blocked, only expensive: a new codegen entity\nkind with its own naming rule, decode function and collision check against\nschema entity names. It is rejected **for now** on the ADR 0006 argument:\nwidening a rejected query to an accepted one is non-breaking; replacing an\nemitted struct that user code already names is not.\n\n## Ripple — this is the bulk of the work\n\nPluralising the node lane is unavoidable and drags the edge endpoint lane with\nit. `endpointLabels` (`resolve.go:503`) returns one `graph.LabelSetKey` read\nfrom `resolved[v] -\u003e nt.Labels`; `closeEdge`, `edgeCandidates`,\n`candidateTypes` and `describeTriedEdges` all consume that single key. So\n`scope.nodeTypes` goes from `map[string]schema.NodeType` to a candidate lane\nmirroring `edgeCands`.\n\nCounted: 20 `nodeTypes[...]` access sites in non-test code (`scope.go`,\n`resolve.go`). 13 are presence-only (`_, ok :=`) and survive untouched. **7 read\nthe value and must each decide what plural means**: `scope.go:144` (BindNode's\n`ErrPartBindingTypeConflict` identity compare), `:654` (`refProjectionType`),\n`:807` (`Export`), `:918` (`PropertyUseWitness`), and `resolve.go:972` / `:1057`\n/ `:1130` (SET / REMOVE / DELETE property validators) — plus `endpointLabels`.\n\nOnce the node lane exposes a set, the endpoint predicate composes cleanly:\n\"does declared `(:Person\u0026Employee)-[:KNOWS]-\u003e(...)` match `(a:Person)-[r:KNOWS]-\u003e(...)`\"\nis just \"is `Person\u0026Employee` in the satisfying set of `{Person}`\" — the same\npredicate reused. `edgeCandidates` becomes a cross-product over\nsrc-set x labels x tgt-set x orientations. Cost is combinatorial, not\nconceptual; schemas are small and `labelDeclared` (`resolve.go:1166`) already\ndocuments naive iteration as a deliberate choice.\n\n## ADR\n\n**This bead carries an ADR; step 1 does not.** The plural decision picks a\npermanent shape for the type model and, per ADR 0010, that shape leaks into\ngenerated code users name. ADR 0006's argument — the conservative default is\nsafe because relaxing later is non-breaking and the reverse is not — decides\nthis one too, which is why it belongs in the same series. ADR 0006 is 64 lines;\nthis need not be longer.\n\nAlso fold in here: a runtime node labelled `{Person, Employee}` matches the\npattern `(:Person)` and is decoded as the `Person` struct, silently dropping\n`employeeId`/`title`. That unsoundness predates this epic and step 1 neither\nadds nor removes it, but the ADR is where it gets stated rather than left\nimplicit.","notes":"\n## Step-2 caveat from the designer's post-implementation pass (2026-07-26)\n\nTwo additions after reviewing the step-1 implementation (`160a4bf`).\n\n**1. Exact precedence is a provisional tie-break, not the satisfaction rule.**\nStep 1 resolves an exact `s.Nodes[key]` hit outright and only consults the\nsuperset-satisfying set on a miss. That is correct for step 1 — it preserves\nobservable behaviour exactly, so the change is strictly widening. But under\nISO 39075 §16.8 **both** an exact type and a superset type satisfy the queried\nlabel set, so precedence *hides* the plural case rather than resolving it.\n\nStep 2 must explicitly reopen it. The hazard is that it is invisible: **no\nfixture under `test/data/resolver/{valid,invalid}/schemas/` declares `\u0026` at\nall**, so nothing exercises the disagreement. A step-2 implementer who does not\nknow precedence was provisional will inherit it as settled semantics without\never seeing a test that questions it. The ADR should record it as a step-1\ntie-break and say plainly that step 2 revisits it.\n\n**2. A sharper statement of the soundness hazard than this bead's description\ncarries.** The description above says a `{Person, Employee}` node decoded as the\n`Person` struct silently drops `employeeId`/`title`. That understates it. The\nworse direction is a **missing property flowing into a typed field**:\n\n    (:Person        { id, name, email })\n    (:Person\u0026Employee { employeeId, title })     -- no email\n\n`MATCH (p:Person) RETURN p.email` types `email` as `STRING` from the bare\n`Person` type. At runtime that pattern also matches `:Person:Employee` nodes,\nwhich carry no `email` at all. So the generated code has a typed field fed by a\nproperty the matched node may not possess — not an extra field discarded, but a\ndeclared field with nothing behind it.\n\nThis **pre-dates the epic** and step 1 neither adds nor removes it. It is\nrecorded here because it is the strongest argument that plural whole-entity\nprojection must be *rejected* rather than papered over, and because it is the\nkind of thing that reads as a step-2 implementation detail until someone ships\na nil deref.\n\n## Agent triage 2026-07-28: blocked on an owner decision, not on effort\n\nClaimed, scoped, then stood down — this bead's ADR is not in the delegated set,\nand the undecided half of it gates the implementation rather than merely\ndocumenting it. Recording the fork so it can be settled quickly.\n\n**The fork: does exact-match precedence survive step 2?**\n\n`resolveNodeLabels` (resolve.go:1195) returns `s.Nodes[labels.Key()]` outright on\nan identity hit and only computes the satisfying set on a miss. NOTES point 1\nabove calls that a provisional step-1 tie-break and says step 2 must reopen it.\nIt is not reopened here, and the two answers are not close:\n\n- **(A) Keep precedence.** An exact identity hit resolves singular; the plural\n  lane only runs when no declared type's identity equals the queried set. The\n  bead's rule (property intersection, whole-entity rejection) applies only in\n  that residue.\n- **(B) Satisfaction is satisfaction.** Drop the fast path; the satisfying set is\n  the answer whether or not one member matches exactly. This is what ISO 39075\n  §16.8 says — `:Person` is satisfied by any element whose label set contains\n  Person, and a `Person\u0026Employee` element is such an element.\n\n**One existing fixture decides between them, and it is a live instance of NOTES\npoint 2's soundness hazard.** `test/data/resolver/valid/label_satisfy_exact_wins.cypher`\nis `MATCH (p:Person) RETURN p.name` against `valid/schemas/satisfy_singular.gql`,\nwhich declares `(:Person { id, name })` and `(:Person\u0026Employee { employeeId, title })`\n— no `name` on the second.\n\n- Under (A) it stays valid: `p.name` types as `STRING NOT NULL` off bare Person.\n  At runtime the pattern also matches `:Person:Employee` nodes, which carry no\n  `name`. That is precisely \"a declared field with nothing behind it\".\n- Under (B) it becomes invalid: `name` is missing on a satisfying member, so the\n  intersection rejects with ErrUnknownProperty — and the hazard is closed by\n  construction.\n\nSo (B) is the conformant and sound answer, and it **narrows an accepted query to\na rejected one** — the direction ADR 0006's argument says to be careful about.\nThat is an owner call, not an agent call. The fixture's name already encodes (A),\nwhich is why an implementer who does not read NOTES will inherit it as settled.\n\n**Correction to NOTES point 1.** It says \"no fixture under\ntest/data/resolver/{valid,invalid}/schemas/ declares `\u0026` at all\". Stale — step 1\n(160a4bf) added both `valid/schemas/satisfy_singular.gql` and\n`invalid/schemas/satisfy_ambiguous.gql`, and the three `label_satisfy_*.cypher`\nqueries that drive them. The disagreement IS exercised today; it is just\nexercised in a way that reads as settled.\n\n**Undisputed, ready to implement once the fork is settled:** whole-entity plural\nrejection (`invalid/label_satisfy_ambiguous.cypher`, `MATCH (p:Person) RETURN p`,\nis already the right shape and stays invalid under both answers); property\nintersection mirroring `unionProperty` (resolve.go:737-756) with\n`[]schema.NodeType` for `[]schema.EdgeKey`; and the nodeTypes ripple, which the\ncount of 20 sites / 13 presence-only / 7 value-reading was checked against and\nholds.","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T06:11:48Z","created_by":"areqag","updated_at":"2026-07-28T19:35:15Z","started_at":"2026-07-28T04:59:26Z","closed_at":"2026-07-28T19:35:15Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/407","dependencies":[{"issue_id":"gqlc-h9n.22","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T02:11:47Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-h9n.22","depends_on_id":"gqlc-h9n.7","type":"blocks","created_at":"2026-07-26T02:11:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.23","title":"resolver: multi-label edge types are declarable but unreachable from any query spelling","description":"Discovered while designing gqlc-h9n.7; not covered by that bead as written, and\na stricter statement than it makes.\n\nA declared edge type may carry a multi-label label set — `EdgeKey.Label` is a\nfull `LabelSetKey` and `internal/schema/gql/resolve.go:65` builds it via\n`e.labels.Key()`. So `-[:KNOWS\u0026FOLLOWS]-\u003e` is a legal declaration.\n\n**There is no query spelling anywhere that reaches it.** Probed against a schema\ndeclaring `EdgeKey{Source:\"A\" Label:\"FOLLOWS\u0026KNOWS\" Target:\"B\"}`:\n\n    match (a:A)-[r:KNOWS]-\u003e(b:B) return r.since\n        -\u003e RESOLVE ERROR: unknown edge: A-[KNOWS]-\u003eB\n    match (a:A)-[r:KNOWS:FOLLOWS]-\u003e(b:B) return r.since\n        -\u003e PARSE ERROR: no viable alternative at input 'match (a:A)-[r:KNOWS:'\n    match (a:A)-[r:KNOWS|FOLLOWS]-\u003e(b:B) return r.since\n        -\u003e RESOLVE ERROR: unknown edge: A-[KNOWS]-\u003eB, A-[FOLLOWS]-\u003eB\n\nNodes at least have the `:A:B` conjunction spelling. Edges have nothing: Cypher\nedge patterns take `|` as *alternation*, not `:` as conjunction, and\n`edgeCandidates` (`internal/resolver/resolve.go:471-487`) hardcodes the\nsingleton assumption by building `graph.LabelSet{L}.Key()` per label.\n\nSo a schema author who writes a multi-label edge type has declared something\n**dead** — it parses, it resolves into the schema model, and no query can ever\nname it. That is the same silent-wrong-answer shape the gqlc-h9n epic exists to\neliminate, one layer down.\n\n## Fix, and the open question\n\nThe resolver half is the same shape as the node fix: replace the exact `Label`\ncompare with `declared.Label ⊇ queried labels`.\n\nThe genuinely open part is the **query surface**, and it should be decided\nbefore implementing: there is no conjunction syntax for edge labels in the\nCypher dialect the parser accepts, so subset matching on `-[r:KNOWS]-\u003e` would\nmean a bare single label silently selects a multi-label declared type. Decide\nwhether that is wanted, or whether the honest fix is instead to **reject a\nmulti-label edge type at schema load** with a named sentinel until a query\nspelling exists. The second is cheaper, is consistent with the epic's posture of\nconverting silent gaps into named ones, and is widenable later.\n\nNote the interaction with gqlc-h9n.7 step 2: if node bindings become plural, the\nendpoint components of an edge key become sets too, and that machinery is shared.\nSequence this after that bead or accept duplicated work.","notes":"2026-07-28, autonomous h9n loop: skipped, not started. Two independent reasons. (1) It DEPENDS ON gqlc-h9n.22, which is reserved for an owner decision on precedence and is outside this loop's delegated scope. (2) Its own open question — subset matching on -[r:KNOWS]-\u003e versus rejecting a multi-label edge type at schema load with a named sentinel — is an ADR-shaped call, and this loop's ADR authority covers only h9n.6, h9n.9, h9n.12 and h9n.25. Loop proceeded to the P3s.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T06:11:48Z","created_by":"areqag","updated_at":"2026-07-28T19:47:50Z","started_at":"2026-07-28T19:35:29Z","closed_at":"2026-07-28T19:47:50Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/408","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.23","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T02:11:48Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-h9n.23","depends_on_id":"gqlc-h9n.22","type":"blocks","created_at":"2026-07-26T02:11:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.21","title":"schema/gql: assert the model collision a semanticCase describes, not just its spelling","description":"A `semanticCase` records a corpus file that parses, resolves, and is still wrong\nbecause the model has no field for what the parse discarded — `CHAR(4)` resolving\nto the same `PropertyType` as bare `CHAR`. Eleven are declared, citing\n`gqlc-h9n.16`, `gqlc-h9n.3`, `gqlc-h9n.4` or `gqlc-8pe`.\n\nAs of PR #392 each case pins its **spelling**: `TestCorpusManifest` requires the\nnamed file to still contain `\"CHAR(4)\"`, so the construct cannot be edited out\nfrom under a row that still claims it. That closed the hole where gutting\n`scalar_char_fixed_length.gql` left the whole package green at 133/133, 133/133,\n46/46. The pin is real, not a declaration row — verified live during the PR #439\nreview by mutating `scalar_bytes.gql` from `BYTES(1, 10)` to `BYTES(1,10)`, which\ngoes red at `corpus_test.go:361`.\n\nWhat is still unpinned is the **collision itself** — that the two spellings really\ndo resolve to the same model. Nothing in the tree asserts it. If `PropertyType`\nsilently gained a width field tomorrow, all eleven rows would keep passing while\ndescribing a blind spot that had closed, and the PR body would keep citing them as\nevidence of a limit that no longer exists.\n\n## Fix\n\nParse the semantic-case file and each spelling it must currently resolve\nidentically to, and `require.Equal` the resolved models. The pairing has to be\ndeclared rather than inferred (the sibling is not always the bare form, and not\nalways an existing file), and must accommodate equivalence classes of ≥3 members\n(see the design section below), so it is a `siblings []string` field alongside\n`spelling`.\n\n| semantic case | siblings it must currently equal | bead |\n|---|---|---|\n| `scalar_char_fixed_length.gql` (`CHAR(4)`) | `[\"CHAR\"]` (bare, in `scalar_character_string.gql`'s `c_bare :: CHAR`) | h9n.16 |\n| `scalar_decimal_precision_scale.gql` (`DECIMAL(10,2)`) | `[\"DEC\"]` (in `scalar_dec_alias.gql`) | h9n.16 |\n| `scalar_string_max_length.gql` (`STRING(5)`) | `[\"STRING\"]` (bare carrier — file may need to be added) | h9n.16 |\n| `scalar_string_min_max_length.gql` (`STRING(2, 5)`) | `[\"STRING\"]` (ditto) | h9n.16 |\n| `scalar_varchar_max_length.gql` (`VARCHAR(10)`) | `[\"VARCHAR\"]` (bare carrier — file may need to be added) | h9n.16 |\n| `scalar_binary_fixed_length.gql` (`BINARY(4)`) | `[\"BINARY\"]` (bare carrier — file may need to be added) | h9n.4 |\n| `scalar_varbinary_max_length.gql` (`VARBINARY(10)`) | `[\"VARBINARY\"]` (bare carrier — file may need to be added) | h9n.4 |\n| `scalar_bytes_max_length.gql` (`BYTES(10)`) | `[\"BYTES\"]` (bare carrier — file may need to be added) | h9n.4 |\n| `scalar_bytes_min_max_length.gql` (`BYTES(1, 10)`) | `[\"BYTES\"]` (ditto) | h9n.4 |\n| `scalar_duration_year_month.gql` (`DURATION(YEAR TO MONTH)`) | `[\"DURATION(DAY TO SECOND)\"]` — widens when `DURATION(DAY TO HOUR)` is added, without a schema change | 8pe |\n| `kind_undirected_arc_directed.gql` | **not in the equality loop** — this row is a different shape entirely (accepted-where-should-be-rejected, not X-resolves-equal-to-Y); `siblings: nil`, assertion filed separately | h9n.3 |\n\n## Sibling design: `siblings []string`, not a scalar\n\nEvery pre-`3625f89` row pairs `X(n)` against a bare `X` carrier — the sibling\nconcept reads naturally as \"the un-annotated spelling\". The four byte-string rows\nfit that shape (though several need a carrier that does not yet exist). The\nDURATION pair does not fit it at all: the collision is `DURATION(YEAR TO MONTH)`\nagainst `DURATION(DAY TO SECOND)`, two *qualified* spellings colliding with each\nother. Bare `DURATION` is a syntax error — PR #439's\n`TestPropertyBareDurationRejectedAtParse` pins that — so the sibling cannot be the\nbare form.\n\n**A scalar `sibling string` handles the today-known DURATION pair but does not\ncompose.** The two DURATION spellings today are two members of a partition of\nDURATION spellings into equivalence classes over \"same populated\n`dbtype.Duration` subset\". `DURATION(DAY TO HOUR)` will be a third member of the\nDAY/SECOND class; adding it to the corpus later must not require a *second*\nh9n.21 amendment. So the field is `siblings []string` from day one:\n\n```go\nrow := semanticSibling{\n    spelling: \"DURATION(YEAR TO MONTH)\",\n    siblings: []string{\"DURATION(DAY TO SECOND)\"}, // widens to add DAY-HOUR etc without schema change\n}\n// assertion: for _, s := range row.siblings { require.Equal(t, model(row.spelling), model(s)) }\n```\n\nThe nine existing `h9n.16` / `h9n.4` rows degenerate to a one-element slice\n(`siblings: []string{\"CHAR\"}` etc.) — reads no worse than today's scalar field\nwould have.\n\n**Alternative not preferred:** a `class string` label with rows sharing a class\nvalue asserted mutually equal. Reads better with ≥3 members but introduces a new\nconcept; the `[]string` approach handles the same widening without one.\n\n**Explicitly out of the equality loop: `kind_undirected_arc_directed`.** That row\nis a third shape entirely — not \"X resolves equal to Y\" but \"X is accepted where\nit should be rejected\". Its `siblings` slice is `nil`, and the required assertion\nfor it is filed separately (a rejection check on the parser side, not an equality\non resolved models). Folding it into the equality loop would either mask the real\ndefect or turn the loop into a two-mode conditional that erodes the pin's clarity.\n\n## Why the assertion is worth more than it looks\n\nIt inverts the signal. Today a semantic case is a promise that something is\nbroken, checked by nobody. With the equality asserted, **the test fails when the\nbead lands** — the model gains its field, the two spellings stop resolving\nidentically, `require.Equal` goes red, and the case gets deleted. That is the same\nred-green shape `wantCorpusResolving` already gives for support progress, applied\nto the one class of defect coverage can never see.\n\nFollow-up from PR #392 review (finding Q1-E, fix 2). Fix 1 — the spelling pin —\nlanded in that PR.\n\n## Amendment (PR #392 review, rev-claims final pass)\n\nThe reviewer probed the spelling pin for three routes past it. Route 1 is now\nclosed in #392; routes 2 and 3 belong here, because both close properly only by\nasserting the collision.\n\n1. **Comment satisfies the pin.** `require.Contains` read the whole file, so a\n   header quoting `CHAR(4)` stood in for GQL that no longer contained it. **Fixed\n   in #392** (`546d6da`): the pin now matches against a lexer-stripped copy, so\n   none of GQL's three comment forms can carry a spelling on the code's behalf.\n   Mutation-tested 6 cases x 4 variants, 24/24 caught; with the strip reverted,\n   18 of the 24 escape.\n2. **Degenerate spelling.** `spelling: \"A\"` on the `CHAR(4)` case passes. Nothing\n   requires a spelling to discriminate anything — only that it appears. Asserting\n   the collision makes the spelling's job checkable: the sibling pair either\n   resolves identically or it does not, regardless of what string was declared.\n3. **Delete the case wholesale.** Removing the `semanticCase` row along with the\n   entry's `bead`/`reason` is green. So the *count* of declared blind spots is\n   self-declared: deleting a semantic case is indistinguishable from never having\n   had one. Same class as the corpus-dilution gap — adding a file that covers\n   nothing is also unpoliced. **Symmetric hole, same shape:** adding a semantic\n   case whose collision no equality-assertion covers is equally unpoliced — which\n   is exactly what the five rows added by PR #439 would have been without this\n   amendment.\n\nCorrectly caught already, and worth not regressing: a `semanticCase` naming an\n`unsupported` file. The `wrongModel`/`semanticFiles` `ElementsMatch` rejects it,\nbecause a semantic case must by definition resolve.\n","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T05:43:38Z","created_by":"areqag","updated_at":"2026-07-28T05:03:30Z","started_at":"2026-07-28T04:51:06Z","closed_at":"2026-07-28T05:03:30Z","close_reason":"Shipped in PR #461. Added semanticCase.siblings []string + TestSemanticCaseCollisions: each case's file is parsed with each sibling spelling substituted in, and the resolved schema.Schema values must be equal. Substituting into the case's own comment-blanked source means no second corpus file and no bare-carrier files to invent. Added wantSemanticCases to close the delete-the-row route. Three routes mutation-swept and caught. Two findings: bare DECIMAL cannot be a sibling (GQL.g4:1832 puts notNull? inside the parenthesised group, so DECIMAL NOT NULL is not GQL — DECIMAL(8) isolates the same discard); and the bead's kind_undirected_arc_directed row is stale, it became an unsupported entry under gqlc-h9n.3, so the sibling-less shape has no instance and siblings is required non-empty.","external_ref":"https://github.com/areqag/gqlc/issues/409","metadata":null,"labels":["gql","schema","test"],"dependencies":[{"issue_id":"gqlc-h9n.21","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T01:43:37Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-ayq","title":"spec: config-multi-target §8.2 calls three fields distinguishing; only one is","description":"§8.2's prefill paragraph blanks `queries`, `gen.go.out` and `gen.go.package` in\nan appended target and calls them \"the three that must distinguish it\". Only\n`gen.go.out` must. The same document's §11 says so in as many words:\n\n  Cross-entry checks beyond output overlap. Shared query directories,\n  repeated package names, and repeated schema paths are all legal.\n\nVerified: two entries sharing `queries` and `package` with distinct `out` load\ncleanly (2 targets, no error). So the document contradicts itself, and the\nfalse half is the one the implementation comment was copied from — the wording\nnow also sits in internal/cli/init.go's addPrefill doc comment and in\ncli-stage-2.md §3.5, both inherited verbatim from §8.2 on branch 4.\n\nThe BEHAVIOUR is right and stays: blank is still the honest prefill for all\nthree, because a second target usually wants its own query directory and\npackage even though nothing forces it. Only the justification is wrong — 'must\ndistinguish' should be 'that a second target normally gives its own', with the\n`out` requirement stated separately as the one the loader enforces.\n\nFix in three places: config-multi-target.md §8.2, cli-stage-2.md §3.5,\ninit.go's addPrefill comment. Prose only, no code change.\n\nSame class as gqlc-5ta: a merged spec sentence that the branch's own evidence\nfalsifies.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T21:56:46Z","created_by":"areqag","updated_at":"2026-07-28T12:48:15Z","started_at":"2026-07-28T12:43:41Z","closed_at":"2026-07-28T12:48:15Z","close_reason":"prose fixed in 3 places; PR #545 merged","external_ref":"https://github.com/areqag/gqlc/issues/410","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5yg","title":"cli: pin the blank-out prompt case in the init wizard","description":"Three independent mutations of the init wizard's `out` handling survive the\nsuite, all reducing to one untested case: nobody ever types a blank (or\nwhitespace-only) `out` at the prompt.\n\nFound by the branch-4 review (gqlc-0gb.4), reproduced on a scratch\norigin/master clone as well — these are PRE-EXISTING, not a branch-4\nregression:\n\n- dropping `validateNonBlank` from `validateOut` (init.go)\n- dropping the post-form trim of `Go.Out` (§4.2's trim)\n- swapping the two checks inside `validateOut`\n\nThe current order is the correct one and worth pinning: `CheckOutAgainst(\"   \")`\nreturns nil, while `CheckOutAgainst(\"\")` returns a misleading \"contains\ngraph[0]'s output directory\" — so validateNonBlank must run first. Nothing\nasserts that today.\n\nFix: add the blank and whitespace-only rows to the `out` validator's table\ntest, asserting the non-blank message rather than an overlap message.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T21:56:22Z","created_by":"areqag","updated_at":"2026-07-28T12:56:35Z","started_at":"2026-07-28T12:43:41Z","closed_at":"2026-07-28T12:56:35Z","close_reason":"TestInitOutValidator added; PR #547 merged","external_ref":"https://github.com/areqag/gqlc/issues/411","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5ta","title":"spec: config-multi-target §4.3's sharing bullet overstates a stable outcome","description":"docs/specs/config-multi-target.md:513-517 says of two targets sharing an\noutput directory: \"Files whose names collide (models.go) end up as the\nlater target's; the rest coexist, so the directory carries two package\nclauses and does not compile. The run itself succeeds.\"\n\nThe first and last clauses hold. The middle one (\"the rest coexist\")\nis true on odd runs only. Reproduced 2026-07-25 against\nfeat/pipeline-multi-target tip with entry 0 out: /tmp/shareprobe2/shared\n(absolute) and entry 1 out: shared (relative), five consecutive runs,\nexit=0 every time:\n\n  run 1: db.go models.go querier.go invoices.cypher.go -\u003e invoicedb\n         people.cypher.go -\u003e people          (5 files, two package clauses)\n  run 2: 4 files, invoicedb only            (people.cypher.go deleted)\n  run 3: 5 files, two package clauses\n  run 4: 4 files, invoicedb only\n  run 5: 5 files, two package clauses\n\nMechanism: inspectOutputs computes BOTH targets' wipe lists before\ncommitOutputs mutates anything, so target 1's wipe list is derived from\nthe pre-run contents and never contains a file that target 0's commit\ncreated during this run. From a clean tree target 1's list is empty, so\npeople.cypher.go survives; next run it is in the list, so it is removed.\nPeriod 2.\n\nSo the directory's compilability alternates rather than being stably\nbroken, and a reader following the spec would predict the wrong state\nhalf the time.\n\nCONTEXT.md's \"Output directory\" entry was corrected in d301828 on\nfeat/pipeline-multi-target; it now states the invariant (colliding names\nare always the later target's, so the earlier target's package is never\nintact) and names the alternation for the non-colliding remainder. The\nspec bullet still carries the old wording.\n\nScope: prose only, one bullet. No code change — the behaviour is a\nconsequence of the two-phase split working as designed (§7), not a bug.","notes":"TRAIL CORRECTION: commit d301828 on feat/pipeline-multi-target ends its body with \"Filed gqlc-h8q for the spec bullet.\" That ID was written before the bead was created and is wrong — gqlc-h8q does not exist. This bead, gqlc-5ta, is the one that commit meant. Left uncorrected in git history because amending a pushed commit needs a force-push, which is not taken without explicit instruction.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T21:06:34Z","created_by":"areqag","updated_at":"2026-07-28T13:11:43Z","started_at":"2026-07-28T13:07:13Z","closed_at":"2026-07-28T13:11:43Z","close_reason":"§4.3 alternation prose corrected; PR #552 merged","external_ref":"https://github.com/areqag/gqlc/issues/412","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-6rv","title":"just test-codegen-fence reports stale results from removed sibling worktrees","description":"`just test-codegen-fence` failed in worktree gqlc-0gb-3-pipeline with 12 issues (errcheck 3, gofumpt 3, ireturn 6) — every one reported against a path under `../gqlc-0gb-2-config/test/data/codegen/...`, a sibling worktree that had already been removed from disk. The worktree under test had zero local changes to test/data/codegen.\n\n`.bin/golangci-lint cache clean` made the recipe green (0 issues) with no source change.\n\nWhy it matters here: CLAUDE.md mandates a sibling worktree per session, so worktrees are created and removed constantly, and .bin/golangci-lint's cache is per-worktree but keyed on content that survives the directory. An agent hitting this sees a red gate naming files it cannot open and has no signal that the result is stale — the failure is indistinguishable from a real one until you notice the path points outside the tree.\n\nPossible fixes: have the recipe pass --no-cache or an explicit per-worktree GOLANGCI_LINT_CACHE, or prune the cache in ensure-golangci. Needs a repro (create worktree, run fence, remove worktree, run fence elsewhere) before picking one — CI wall-time is critical here, so --no-cache is not free.","notes":"\n## Correction + partial repro attempt, 2026-07-26 (team-lead)\n\n**The description's premise is wrong in one load-bearing detail.** The cache is\nNOT per-worktree. `.bin/golangci-lint cache status` reports\n`Dir: /home/antranig/.cache/golangci-lint` — one global cache (57 MiB here)\nshared by every worktree. `.bin/` holds only the binary. So the mechanism is not\n\"a per-worktree cache keyed on surviving content\"; it is one cache serving\nresults across worktrees, with the cached issue records still carrying the\nabsolute paths of whichever worktree first computed them.\n\nThat fits the reported symptom exactly: issues named under\n`../gqlc-0gb-2-config/test/data/codegen/...` while linting\n`gqlc-0gb-3-pipeline`.\n\nIt also reframes the fix menu. A per-worktree `GOLANGCI_LINT_CACHE` does not\nmerely isolate — it *discards the cross-worktree reuse that is currently making\nthe recipe fast*. Given CLAUDE.md mandates a fresh worktree per session, every\nsession would start cold. Measure that before choosing it.\n\n**Repro attempt that did NOT reproduce, and why it is still informative.** Five\nsibling worktrees (`gqlc-h9n2-corpus-{a,b,c,d1,d2}`) plus seven `/tmp/rev-*`\ntrees were removed this session; `just test-codegen-fence` in a surviving\nworktree then reported `0 issues`. So removal alone is not sufficient. The\nremoved trees' `test/data/codegen` was clean, so the only thing cached under\ntheir paths was \"no issues\" — there was no stale *issue* record to misreport.\n\nThe repro therefore needs a worktree whose `test/data/codegen` has a **real lint\nissue** at lint time, removed, then a lint from a sibling. That is the missing\nstep; note that if the sibling's content is identical the issue is genuinely\npresent in it too, so the interesting question is what cache key lets a *stale*\nresult survive a content change — the bead reports `cache clean` going green\nwith no source change, which means the key is not purely content. Establish the\nkey before picking a fix.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T20:16:57Z","created_by":"areqag","updated_at":"2026-07-26T06:50:24Z","closed_at":"2026-07-26T06:50:24Z","close_reason":"Fixed in PR #396 (squash-merged 2026-07-26). golangci-lint's cache key is content-only for the current module (internal/cache/cache.go:178 rewrites paths to module-relative) while the cached payload's EncodingIssue.Pos.Filename stays absolute (pkg/goanalysis/issue.go:23), so a hit from worktree A was served verbatim to worktree B. Fix exports GOLANGCI_LINT_CACHE to a per-worktree .bin/golangci-cache in the justfile and moves the CI actions/cache path to match; both halves must land together or CI silently runs cold forever.\n\nReviewer reproduced the bug under an isolated cache (removed tree-A, linted tree-B, got 4 issues all pathed ../tree-A/ plus missing-file warnings, including contamination of an unrelated init.go) and confirmed the fix. Costs measured: cold 78.4s local, warm 0.6s, 20 MiB per warm worktree. git worktree remove without --force still exits 0 because .bin/ is gitignored.\n\nScope note the reviewer added: CI runners are ephemeral and codegen-fence has no cache step, so this defect was local-workflow-only and never manifested in CI. Observed one-time CI transition cost on #396 itself: lint took 4m56s against a 47s-1m2s baseline, because the restore-key hit an entry saved from the old ~/.cache/golangci-lint path. Steady state is being verified on the next PR.\n\nFollow-up filed as gqlc-b63: nothing in the tree enforces the justfile/ci.yml path coupling.","external_ref":"https://github.com/areqag/gqlc/issues/385","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.15","title":"test(schema): pin the three inert measurement guards in the coverage harness","description":"A mutation sweep over the coverage harness' measurement guards found that some are\n**inert, not absent**: mutating them changes nothing, because no input in today's nine-file\nseed corpus exercises the condition they guard. They are correct code with no test\ndistinguishing them from a no-op — the class the fabricated-token guard belonged to before\nee53078 pinned it.\n\nBaseline: `TestCorpusGrammarCoverage` red at `rules 78/133, tokens 32/133, alternatives\n17/46`, every other test green. Detector: a mutation that moves the gate's reported\ncoverage while every test keeps its baseline verdict is an unguarded inflation hole.\n\nSweep script: `/tmp/carm-guardsweep2.sh`. **Use that one, not `/tmp/carm-guardsweep.sh`** —\nthe first pass was wrong twice and its results should not be quoted. See \"sweep method\"\nbelow.\n\n## Result\n\n| Guard | Site | Verdict |\n|---|---|---|\n| `VisitErrorNode` no-op | `corpus_coverage_test.go:166` | PINNED by `TestFabricatedTokenDoesNotScore` |\n| `elementTypes` counts depth 1 only | `corpus_coverage_test.go:97` | PINNED by `TestCoverageElementCount` (nested cases) |\n| `declaresElements` fires at depth 0 only | `corpus_coverage_test.go:101` | PINNED by `TestCoverageElementCount` |\n| `nestingDepth` counts `nestedSpecRule` | `corpus_coverage_test.go:135` | PINNED by `TestCoverageElementCount` + `TestCorpusOutcomes` |\n| `children` blanks error nodes so they contribute no name to attribution | `corpus_coverage_test.go:111-125` | **INERT** |\n| `measureCoverage` requires the file to be syntactically valid | `corpus_coverage_test.go:257` | **INERT** — see caveat |\n| `stolenBy` joins the required set | `corpus_grammar_test.go:394-396` | **INERT** — needs a seam |\n\n## The three that are open\n\n**`children` blanks error nodes — the one real hole.** This is the attribution half of the\nerror-recovery defence; `VisitErrorNode` is the token half and is pinned. Removing the\nblanking lets a tree built by error recovery match a plausible alternative and score it.\n`TestFabricatedTokenDoesNotScore` already feeds a broken source (`CREATE GRAPH TYPE t {\n(:A)`, `corpus_test.go:374`) through the `walkCoverage(t, src)` seam, but that particular\nrecovery shape does not shift any child sequence far enough to steal an alternative. The\npin is therefore: find a broken source whose recovery *does* change attribution, and assert\nthe alternative is not scored. Finding that source is most of the work, and if no such\nsource exists the guard is unnecessary — either outcome is worth knowing.\n\n**`measureCoverage` requires syntactic validity.** Inert because no seed file is broken,\nand it is not really a measurement guard — it is a fail-fast assertion aimed at corpus\nauthors, whose \"test\" is that a broken corpus file fails loudly. Pinning it properly means\nasserting that a `require` fires, which needs either a fake `*testing.T` or extracting the\ncheck into a pure function. Low value, non-trivial cost; decide whether it is worth it\nbefore doing it.\n\n**`stolenBy` joins the required set.** Inert because today's only thief is itself a\ncandidate, so the append is a no-op — `corpus_grammar_test.go:390-392` already says so.\nPinning needs a seam first: `requiredAlternatives` reads package-level\n`alternativeExemptions` directly, so an exemption whose `stolenBy` is not a candidate\ncannot be injected. Parameterise it, or split out a pure helper.\n\n## Sweep method — two mistakes worth not repeating\n\n**1. Do not run a `-run` subset.** The first pass used `-run\n'TestCorpus|TestAlternative|TestExemption|TestFabricated|TestGrammar|TestObligation'`,\nwhich silently excluded `TestCoverageElementCount` — the *only* test that feeds the\ncollector a nested element type, and therefore the only test that can see either depth\nguard. Three guards were reported inert that are in fact pinned. A sweep that decides\nwhether a guard is tested must run every test.\n\n**2. Prefer a mutation that relocates a guard over one that widens it.** Widening\n`declaresElements`' depth check from `== 0` to `\u003e= 0` is an *equivalent mutant*: the field\nis a bool that saturates, and a depth-0 `nestedSpec` exists whenever any nested one does,\nso no input can distinguish the two. Relocating it to `== 1` fails four tests immediately.\nA widening mutation on a saturating value proves nothing.\n\n## Do not\n\nDo not pin any of these by adding a file to `test/data/schema/gql/invalid/` or a seed\ncorpus file. They must be tested through the corpus-independent `walkCoverage(t, src)` seam\n(`corpus_coverage_test.go:233`), so the tests stay independent of what any corpus author\ndoes — and each guard's own inertness proves no such fixture exists today.\n","notes":"RESOLVED (pending commit) 2026-07-25: the 'children blanks error nodes' guard is NOT inert, it is load-bearing, and there is now a witness.\n\nProbe /tmp/h9n2probe/carmerr reimplements children() twice over the same tree (error nodes blanked vs named) and reports every rule whose child sequence differs. 21 of 33 broken sources differ somewhere. The dangerous class is ANTLR's single-token INSERTION: the error node's token type is exactly the one the alternative wanted, so the unblanked sequence can be a perfect match for an alternative the input never took. Substitution and deletion recoveries are harmless because the resulting sequence matches nothing.\n\nWitness: CREATE GRAPH TYPE t { (:A), (:B), (:A)-[:R]-\u003e(:B }\n\nRecovery invents the closing RIGHT_PAREN, leaving destinationNodeTypeReference with children LEFT_PAREN nodeTypeFiller RIGHT_PAREN — exactly its alternative 2 (GQL.g4:1623-1626), which is REQUIRED and on the authors' worklist. Removing the blanking scores destinationNodeTypeReference#2 from a file that does not parse.\n\nPinned by TestFabricatedTokenDoesNotAttribute in corpus_test.go, mutation-verified: passes as-is, fails on 'Should be false / the recovered endpoint must not score' when the ErrorNode case is dropped from children(). TestFabricatedTokenDoesNotScore still passes under that same mutation, which confirms the two guards are independent — as the comment at corpus_test.go:369-372 already claimed.\n\nWhy earlier candidates were all false leads: arcTypePointingRight and nestedGraphTypeSpecification also show a sequence diff, but both are SINGLE-alternative rules, and alternativeIndex.tag returns early for those, so nothing about them is ever attributed. A sequence diff is necessary but not sufficient; the rule must be multi-alternative AND the unblanked sequence must match one of its alternatives.\n\nRemaining on this bead: measureCoverage's syntactic-validity require (low value, needs a fake *testing.T) and stolenBy joining required (needs a seam).\nCOMMITTED 4c9ff26 (2026-07-25). The children/error-node blanking guard is now pinned by TestFabricatedTokenDoesNotAttribute. One of the three items on this bead is discharged; the two that remain are measureCoverage's syntactic-validity require (needs a fake *testing.T or a pure-function extraction; low value) and stolenBy joining required (needs a seam in requiredAlternatives).","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T13:17:29Z","created_by":"areqag","updated_at":"2026-07-28T04:55:46Z","started_at":"2026-07-28T04:41:15Z","closed_at":"2026-07-28T04:55:46Z","close_reason":"Shipped in PR #460. Pinned measureCoverage's syntactic-validity guard (new subtest of TestMeasureCoverageRejects, witness is an unterminated body so only that guard can reject it) and the stolenBy append (requiredAlternatives now takes the exemption list as a parameter; TestRequiredAlternativesDemandsTheThief runs the thief in and out of the candidate set). Third row of the sweep table was stale: children blanking error nodes is pinned by TestFabricatedTokenDoesNotAttribute, added in #392 after the sweep ran — verified by mutation. Both new pins mutation-swept; each fails nothing else.","external_ref":"https://github.com/areqag/gqlc/issues/376","dependencies":[{"issue_id":"gqlc-h9n.15","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-25T09:17:29Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-h9n.15","depends_on_id":"gqlc-h9n.2","type":"discovered-from","created_at":"2026-07-25T09:17:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-cfj","title":"schema/gql: source Annex D conformance feature ids, or stop citing them","description":"The corpus `corpusEntry.feature` field is documented as \"the ISO GQL Annex D conformance feature id, or `mandatory`\". Its purpose is narrow and it is only load-bearing on `unsupported` entries: for a construct we decline, the *only* thing that makes declining conformant is that construct being an Annex D **optional** feature. On a `resolves` entry the field carries no weight, because we support the construct either way.\n\nThe problem is that on exactly the entries where the field matters, we cannot currently source it. There is no Annex D material anywhere in the repo (checked `docs/specs`, `docs/adr`, the grammar, 2026-07-25), and the ids that reached the tree were written from memory:\n\n- `internal/schema/gql/corpus_area_a_test.go` carried `feature: \"GG02\"` on `12.6-graph-type-statement/like_graph.gql`, with a reason string that claimed \"conformant because GG02 is an optional Annex D feature\". That is an unsourced id being used to discharge a conformance claim.\n- `/tmp/h9n2-areas.md` carried `GV50` for LIST/ARRAY on the same basis.\n- The struct doc comments in `corpus_test.go` and the author brief both use `GG01` / `GV50` as format examples, which teaches the fabrication to five authors.\n\nNote that `\"mandatory\"` is NOT the safe fallback for a declined construct — it is worse than an unsourced id. If a construct is mandatory then declining it is non-conformant, so `feature: \"mandatory\"` on an `unsupported` entry asserts a defect rather than excusing one. The two values are not interchangeable and the fallback direction depends on the outcome.\n\nInterim position taken for the corpus PR: no entry cites an Annex D id. Declined constructs record the dependency honestly — that conformance rests on the construct being optional, unsourced — and point here.\n\nThis bead closes when either the ids are sourced against the actual standard text and the citing entries carry real ones, or the field is replaced by something we can actually substantiate.","notes":"2026-07-25, team-lead, measured. Two corrections to the description above, both found by\nrunning the harness rather than reasoning about it.\n\n1. THREE entries carried a fabricated id, not one. Full inventory:\n     corpus_area_a_test.go:18  like_graph.gql                 GG02  (unsupported)\n     corpus_area_c_test.go:16  pattern_undirected.gql         GE03  (unsupported)\n     corpus_area_c_test.go:23  kind_undirected_arc_directed   GE03  (resolves)\n   I had only catalogued GG02. The other two surfaced from a require.Len failure dump,\n   i.e. by accident -- which is the argument for item 2.\n\n2. The description's claim that \"mandatory\" on an unsupported entry \"asserts a defect\n   rather than excusing one\" is right about the mechanics and wrong about the intent.\n   Asserting a defect is EXACTLY what the corpus should do for the phrase forms: the\n   repo owner's standing rule is \"if it is valid ISO GQL, we should allow it; we should\n   not have our own GQLC flavour of ISO GQL\", so an unsupported entry for valid ISO GQL\n   is a declared conformance gap, and the bead is what closes it. Both edgeTypePhraseFiller\n   entries already carry mandatory + gqlc-uhb on exactly that basis and they are correct.\n\n   The sharpened rule, which is what the field should mean:\n     - \"mandatory\"  = declining this is non-conformant. Free on a resolves entry;\n                      on an unsupported entry it declares a gap, and the bead closes it.\n     - \"unsourced\"  = we do not know. This is the honest value ONLY for a construct\n                      declined PERMANENTLY, because there the claim is \"declining this is\n                      still conformant\" -- precisely the unsourceable one.\n   Applied, exactly one entry qualifies as unsourced: like_graph.gql, bead gqlc-0ri,\n   permanently declined because LIKE needs live data. Everything else is mandatory.\n\nFIX SHAPE, prototyped and verified in /tmp/goldencheck, diff at /tmp/lead/patch/:\nclose the class with a mechanism instead of an instruction. featureValues is a closed\nset {\"mandatory\",\"unsourced\"} and TestCorpusManifest does require.Contains against it.\nVerified it fires on the real violation:\n    []string{\"mandatory\",\"unsourced\"} does not contain \"GG02\"\n    12.6-graph-type-statement/like_graph.gql: feature must be \"mandatory\" or \"unsourced\"\n\nWhy a mechanism and not the brief: \"do not cite an Annex D id\" was a rule with no\nmechanism -- A14's exact failure mode -- and it was violated 3 times in 9 entries before\nanyone noticed. Worse, the corpusEntry.feature doc comment itself said \"(GG01, GV50, ...)\",\nso the struct was teaching the fabrication it forbade. Five authors were about to read it.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T13:06:37Z","created_by":"areqag","updated_at":"2026-07-26T23:12:51Z","closed_at":"2026-07-26T23:12:51Z","close_reason":"PR #434 merged as 1b8affd; GH #377 auto-closed. Vendored ISO Annex D 228-code list + isValidFeature membership guard.","external_ref":"https://github.com/areqag/gqlc/issues/377","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-cer","title":"schema/gql: mutation-sweep every harness guard, prove each is load-bearing by deletion","description":"The corpus harness is a gate whose only job is to fail when the corpus stops discharging its obligations. Three separate defects this epic (amendments A11, A14, A17) were guards that looked right and asserted nothing: a `required \\ covered` difference that is empty when `required` is empty, a constraint written into the brief with no mechanism behind it, and a count pin that cannot observe a membership change.\n\nThe general lesson linus drew is stronger than any of the three fixes: **every configuration entry and every assertion must be load-bearing, and the only proof is deletion**. Measured instance: two of the three `coverageRoots` entries were +0 rules / +0 tokens — dead config that looked like the load-bearing part of the closure.\n\nThis bead is the systematic version of that check across the whole harness, run once the corpus is complete and the gate is green, because a mutation sweep against a red gate cannot distinguish a kill from the pre-existing failure.\n\nMethod, per guard: mutate it so the property it claims to enforce is violated, run the package, and record whether the suite fails and *which* assertion fired. A mutation that leaves the suite green is a guard that is not load-bearing; a mutation that fails at a *different* assertion than the mutated one means the guard is redundant with an earlier check and should say so or go.\n\nKnown-suspect classes to cover, not an exhaustive list:\n- every count pin (`wantObligationRules`, `wantObligationTokens`, `wantInvisibleAlternatives`, `wantCorpusEntries`, `wantCorpusResolving`) — a count cannot see a compensating pair\n- every set-difference assertion — check the empty-`required` case explicitly\n- each entry of `coverageRoots` and each cut, deleted individually, with the rule/token delta recorded\n- the syntax-validity and attribution checks in `measureCoverage`, whose ordering is what actually makes a fabricated token unscoreable (see the corrected justification for the token-fabrication pin)\n- the area-ownership disjointness assertion","notes":"## Sweep result: 38 mutations, 8 batches, zero meaningful survivors\n\nDriver: each mutation names a guard, applies source edits (or deletes/creates a\nfile) that violate the property the guard claims to enforce, runs `go test\n./internal/schema/gql/`, records which assertions fired, and restores. Run against\na green gate at 620e7de, as the bead requires.\n\nAll five named suspect classes were covered. Every guard is load-bearing. The one\nmutation that stayed green (M30) only *weakened* an assertion — ElementsMatch to\nSubset on wantInvisibleAlternatives — which is undetectable by construction, since\nno test tests a test. It is not a finding.\n\n### The two real findings, both comment defects rather than missing guards\n\n**1. wantCorpusEntries was underdocumented.** M23 deletes a resolving corpus file\nfrom disk *and* from the manifest in one change: `TestCorpusSize` fires ALONE.\nTestCorpusManifest's `require.ElementsMatch(corpusFiles(t), files)` bijection stays\nsatisfied because both sides shrink together, and every coverage gate is content to\nbe discharged by the files that remain. So the entry count is the only assertion\nleft that the corpus was ever larger. The comment block made exactly this\nanti-silent-deletion argument for wantSemanticCases and never for wantCorpusEntries.\nM24 (a declining file, same treatment) fires TestCorpusSize + TestOptionalityCoverage,\nthe latter only because that particular file uniquely carried an optionality class.\n\n**2. wantCorpusResolving was overdocumented.** The comment claimed it \"makes a\nsupport regression fail the build\". It cannot: both counts are computed from the\noutcomes the *manifest declares* and never run the resolver. M37 forces the edge\ntype guard in resolve.go to reject; M38 makes propertytype.go accept the RECORD\nvalue type it declines. Both are real support regressions, both leave TestCorpusSize\ngreen, and both redden TestCorpusOutcomes, which is the test that executes each file\nagainst its declared outcome. This is the bead's own \"fails at a different assertion\nthan the mutated one ... should say so or go\" case; it says so now rather than going,\nbecause finding 1 gives it an independent job.\n\n### Confirmations worth keeping\n\n- **TestMeasureCoverageRejects is exactly what it claims.** M16/M17/M18 each delete\n  one of measureCoverage's three guards (syntax validity, statement, attribution) and\n  each fires TestMeasureCoverageRejects and nothing else. Sole detector, three times.\n- **The empty-`required` vacuity is closed**, and by four independent mechanisms\n  depending on which register you empty: wantInvisibleAlternatives' ElementsMatch\n  (M8, M31), the frontier-cut reachability check (M34), the exemption-is-a-candidate\n  check in requiredAlternatives (M13, M20), and — with all of those emptied at once —\n  optionalityClassGolden from the other family (M36). No single-register mutation\n  escapes, and a six-register coordinated repin is still caught by four tests.\n- **Both exemption registers are demanded, not decorative.** M20 (alternative\n  exemption tag blanked) and M21 (optionality exemption removed) each fire.\n- **A count cannot see a compensating pair, as the bead predicted.** M2c swaps two\n  entries' outcomes cleanly: TestCorpusOutcomes fires alone, TestCorpusSize stays\n  green. Execution catches it, not the pin. Same shape at M26 (a semantic case deleted\n  with its entry demoted so the manifest stays consistent) — TestCorpusSize alone,\n  which is the case the wantSemanticCases comment already described.\n- **Narrow, clean guards:** M7 → TestCorpusAreasAreDisjoint alone; M11 and M22 →\n  TestEveryAreaPrefixIsBacked alone; M12 → TestCorpusManifest alone; M14 →\n  TestOptionalityExemptions alone; M28 → TestSemanticCaseCollisions alone;\n  M29 → TestOptionalityClassGolden alone.\n\n### The 19-test avalanche is deliberate, not a defect\n\nEight mutations to the obligation closure inputs (M5a, M5b, M6a, M6b, M8, M13, M19,\nM20) redden an identical set of 19 tests. The working suspicion was that this makes\nthe failure unreadable. It does not: all 19 fail inside `grammarObligation` and print\nthe *same* actionable sentence naming the offending cut or root. That helper asserts\nthe pinned sets on every call precisely because an empty obligation fails open —\ncorpus_grammar_test.go:649-653 argues this hypothetically, and M19 (coverageRoots\nemptied) is the concrete witness that it is right. The avalanche is the price of the\nfail-open defence and buys diagnosability rather than costing it. Nothing to fix.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T13:03:54Z","created_by":"areqag","updated_at":"2026-07-28T07:54:06Z","started_at":"2026-07-28T07:34:56Z","closed_at":"2026-07-28T07:54:06Z","close_reason":"Swept 38 mutations across 8 batches against a green gate. Every named suspect class covered; no guard found that fails to guard. Landed PR #481 correcting the two count-pin comments the sweep contradicted: wantCorpusResolving cannot see a support regression (it reads declared outcomes, never runs the resolver — TestCorpusOutcomes is the detector), and wantCorpusEntries is the sole detector of a file deleted from disk and manifest together, which TestCorpusManifest's ElementsMatch bijection cannot see. Full per-mutation results in NOTES.","external_ref":"https://github.com/areqag/gqlc/issues/378","dependencies":[{"issue_id":"gqlc-cer","depends_on_id":"gqlc-h9n.2","type":"blocks","created_at":"2026-07-25T09:04:00Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.12","title":"schema/gql: ground the GH02 undirected-edge and GG04 LIKE rejections in defensible reasons","description":"Two optional features gqlc rejects today with sentinels, whose stated reasons do\nnot survive scrutiny. Both rejections are probably right; both need a defensible\nreason before the ADR (gqlc-0ri) can record them.\n\n## GH02 — undirected edge types (`ErrUndirectedEdge`)\n\n`EnterEdgeTypePatternUndirected` (listener.go:150-152) fails on any\n`arcTypeUndirected` (`~[...]~`).\n\n**The reason to avoid.** \"An undirected edge cannot be faithfully represented\nagainst a directed-edge store\" overstates the case.\n`edgeTypePatternUndirected : sourceNodeTypeReference arcTypeUndirected\ndestinationNodeTypeReference` keeps the endpoint *types* ordered, so a lossy\nmapping — canonical stored direction plus undirected matching — is mechanically\navailable. Rejecting on unrepresentability invites a reviewer to point at that\nmapping.\n\n**The reason that holds.** An undirected edge is a **distinct element kind**, not\na directed edge matched in both directions.\n\nCITED — *A Researcher's Digest of GQL* (ICDT 2023; \"We follow the formal\ndefinition adapted by the GQL Standard\"), Def. 2: a property graph is\n`⟨N, Ed, Eu, lab, endpoints, src, tgt, prop⟩` where `Eu` is a **separate** set of\nundirected edge ids, `src, tgt : Ed → N`, and `endpoints : Eu → 2^N` with\n`|endpoints(e)| ∈ {1,2}` — unordered.\n\nAnd the distinction is **observable at runtime**, so a lossy mapping is detectably\nwrong rather than merely imprecise: `\u003cedge\u003e IS [NOT] DIRECTED` (§19.8) and\n`n IS SOURCE OF e` (§19.10) would both answer incorrectly for an undirected edge\nstored with a canonical direction. Neo4j has no such element kind — every\nrelationship has a startNode and an endNode.\n\nAdd that GH02 *\"Undirected edge patterns\"* carries an Annex D feature ID and is\ntherefore optional, so declining it is conformant.\n\n**Also note this reaches the query side, not just DDL.** GQL distinguishes\n`\u003c-[]-\u003e` (`fullEdgeLeftOrRight`, directed either way) from `-[]-`\n(`fullEdgeAnyDirection`, includes undirected). Cypher's `-[r]-` is GQL's\n`\u003c-[]-\u003e`, not `-[]-`. Any future GQL query-language support inherits this, so the\nADR should scope the decision to the whole language rather than to graph-type DDL.\n\n## GG04 — graph type `LIKE` a graph (`ErrUnsupportedSource`)\n\n    graphTypeLikeGraph : LIKE graphExpression\n    graphExpression    : graphReference | objectExpressionPrimary\n                       | objectNameOrBindingVariable | currentGraph\n\n`CURRENT_GRAPH` and binding variables are session state. A static generator has\nno session, so `LIKE` is genuinely unresolvable — this reason does hold. Record it\nagainst GG04 *\"Graph type like a graph\"* and note the contrast with `COPY OF`\n(gqlc-h9n.1), whose catalog-path form **is** statically resolvable, so the two\nrejections do not share a justification even though they share a sentinel.\n\nSplitting `ErrUnsupportedSource` into two errors is likely worthwhile for exactly\nthat reason: one says \"session state, never resolvable\", the other says \"resolvable\nin principle, blocked by gqlc's one-graph-type rule\".\n","acceptance_criteria":"The undirected-edge rejection is justified by undirected edges being a distinct element kind whose directedness is observable via IS DIRECTED and IS SOURCE OF, plus GH02 optionality — not by an unrepresentability claim.\nThe LIKE rejection is justified by graphExpression reaching session state, recorded against GG04.\nErrUnsupportedSource is split so that LIKE and COPY OF no longer share one error with two incompatible justifications.\nThe ADR scopes the undirected decision to the whole language, noting GQL's \u003c-[]-\u003e versus -[]- distinction affects future query support.\nBoth decisions are pinned by tests naming the specific error.","status":"closed","priority":2,"issue_type":"decision","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:39:32Z","created_by":"areqag","updated_at":"2026-07-28T04:41:27Z","started_at":"2026-07-28T04:29:53Z","closed_at":"2026-07-28T04:41:27Z","close_reason":"Shipped in #459. Undirected edges: the unrepresentability reason was refutable from the grammar (both undirected productions keep endpoint types ordered), so grounded instead in undirected being a distinct element kind whose directedness is observable via IS DIRECTED (§19.8) and IS SOURCE OF (§19.10) — a canonical-direction encoding answers standard predicates wrongly, not imprecisely. Scoped to the whole language in ADR 0016, noting GQL's -[]- (fullEdgeAnyDirection) is not Cypher's -[r]- (which is \u003c-[]-\u003e). LIKE: grounded in graphExpression reaching CURRENT_GRAPH/binding variables. ErrUnsupportedSource split into ErrLikeGraphSource + ErrCopyOfSource, both wrapping the class so errors.Is keeps matching. Feature ids verified against ISO's Annex D XML (SHA matches the annexd pin): GG04 exact, GH02 an inference and labelled as such.","external_ref":"https://github.com/areqag/gqlc/issues/365","dependencies":[{"issue_id":"gqlc-h9n.12","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:39:32Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.9","title":"schema/gql: decide GG21 label implication (=\u003e) — implement key label sets or ADR the rejection","description":"`rejectLabelImplication` (listener.go:140-148) fails on any `IMPLIES` token, so\nthe whole key-label-set-with-implication form is refused:\n\n    nodeTypeKeyLabelSet : labelSetPhrase? IMPLIES     # GQL.g4:1534-1536\n    edgeTypeKeyLabelSet : labelSetPhrase? IMPLIES     # GQL.g4:1577-1579\n\nThis is optional feature **GG21 \"Explicit element type key label sets\"**, so\ndeclining it is legitimate under §24.2 minimum conformance. Two things are\nnonetheless wrong today.\n\n## 1. The rejection is undocumented and its scope is mislabelled\n\n`ErrLabelImplication` says `label implication (\"=\u003e\") is not supported`, and the\nlistener comment calls the accepted form \"the plain key label set (`:Label`)\".\nBut per the standard's BNF the part *before* `=\u003e` is the key label set and the\npart *after* is implied content — so what gqlc calls \"the plain key label set\" is\nin fact a complete label set with an **inferred** key label set (optional feature\n**GG22**). The naming inverts the standard's own vocabulary. Record the rejection\nas \"GG21 not supported\" in the ADR, and fix the vocabulary.\n\nNote `labelSetPhrase?` is optional, so `=\u003e :Thing` with an *empty* key label set\nis also grammar-legal and also refused.\n\n## 2. Deciding whether to implement it needs the identity model first\n\n`=\u003e` is the syntax for declaring a key label set explicitly. Without a\nkey-label-set concept in the model there is nothing for the syntax to populate,\nso this bead is blocked on that model change rather than being a listener\naddition.\n\n## Vendor semantics diverge — do not pick one by accident\n\n- **Microsoft Fabric**: secondary-label properties \"are automatically inherited\"\n  (property inheritance across declarations).\n- **Neo4j** (GA 2026.06): same syntax, called *identifying label* + *implied\n  labels*, with **no** property inheritance, and a label may not be both\n  identifying and implied — which rules out hierarchies.\n\nTwo implementations, same syntax, incompatible semantics. Whether 39075:2024\nmandates cross-declaration property inheritance is **UNRESOLVED** and could not\nbe settled from public sources. Fabric's own conformance table marks GG21 **No**\nwhile its graph-types article uses `=\u003e` and warns the syntax \"isn't currently\nsupported directly for graph\" — i.e. `=\u003e` is the standard's syntax, unimplemented\nthere.\n\nSo implementing `=\u003e` means choosing an inheritance semantics that the standard\nmay not endorse. Under the no-dialect principle that argues for implementing only\nthe part both vendors agree on — the key label set as identity, with implied\nlabels contributing to the complete label set — and explicitly *not* implementing\nproperty inheritance until the standard's position is known.\n","acceptance_criteria":"The rejection or support of =\u003e is recorded against feature ID GG21, not as a generic deviation.\nThe vocabulary is corrected so 'key label set' names the part before =\u003e and 'implied content' the part after, matching the standard's BNF.\nThe empty-key-label-set spelling (=\u003e :Thing) has a decided, tested outcome.\nIf implemented: implied labels contribute to the complete label set, and property inheritance is explicitly NOT implemented, with the standard's unresolved position recorded.\nIf rejected: the ADR states GG21 is an optional feature declined pending a resolution of the Fabric/Neo4j inheritance divergence.","status":"closed","priority":2,"issue_type":"decision","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:37:26Z","created_by":"areqag","updated_at":"2026-07-28T04:29:49Z","started_at":"2026-07-28T04:11:21Z","closed_at":"2026-07-28T04:29:49Z","close_reason":"Implemented GG21 explicit key label sets in #458. Key insight from GQL.g4: nodeTypeImpliedContent is entirely inline, so the Fabric/Neo4j property-inheritance divergence is reachable only when an implied label is also a declared type's key label — a syntactically decidable condition, rejected with ErrImpliedLabelIsKeyLabel. Explicitly empty key label sets (=\u003e :Thing) reuse ErrUnnamedNodeType/ErrUnnamedEdgeType; inline endpoints that imply labels get ErrEndpointFillerImpliesLabels. ErrLabelImplication removed. Rationale in ADR 0015.","external_ref":"https://github.com/areqag/gqlc/issues/366","dependencies":[{"issue_id":"gqlc-h9n.9","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:37:26Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.8","title":"schema/gql: model the key label set as node type identity, distinct from the complete label set","description":"gqlc's schema model has no key-label-set concept. `internal/graph/labelset.go`\n`LabelSet.Key` canonicalises a type's *complete* label set and that string is\nthen used as the type's identity throughout (`schema.Schema.Nodes` is keyed by\nit).\n\nISO GQL separates three notions (researched 2026-07-25; see the\n`gqlc-iso-node-type-identity` bd memory for the full evidence):\n\n| Notion | Feature | Grammar |\n|---|---|---|\n| element type **name** | GG20 | `nodeTypeName`, `localNodeTypeAlias` |\n| **key label set** — identity | GG21/22/23 | the part *before* `=\u003e` |\n| **complete** label set | — | the whole `labelSetPhrase` |\n\n`nodeTypeKeyLabelSet : labelSetPhrase? IMPLIES` (GQL.g4:1534) — the standard's\nown BNF names the pre-`=\u003e` part the key label set and the post-`=\u003e` part implied\ncontent.\n\n## The evidence that the key label set is identity\n\nAnnex D defines **GG25 \"Relaxed key label set uniqueness for edge types\"**. A\nfeature that *relaxes* key-label-set uniqueness for edge types establishes that\nthe base rule is key-label-set uniqueness across element types. CITED at the\nlevel of Annex D feature names, which Ultipa and Microsoft Fabric publish\nidentically (Fabric's page is reviewed by Stefan Plantikow, a GQL editor).\n\n`ErrDuplicateNodeType` therefore enforces the right *rule* against the wrong\n*key*: it dedupes on complete label sets where the standard dedupes on key label\nsets.\n\n## Scope\n\nThis is the model change, not the `=\u003e` syntax. Even with `=\u003e` still rejected, the\ndegenerate case matters: when a declaration has no `=\u003e`, its key label set is\ninferred (GG22) and merely *coincides* with its complete label set. Representing\nthat coincidence explicitly is what makes the model correct and makes GG21\nimplementable later as a syntax addition rather than a model rewrite.\n\nDepends on the satisfaction fix landing first, because changing what identifies a\nnode type while lookup is still exact-match would change which schemas are\naccepted for reasons unrelated to identity.\n\n## Do not import vendor semantics\n\nFabric says secondary-label properties are \"automatically inherited\". Neo4j\nimplements the same construct as identifying + implied labels with **no** property\ninheritance, and forbids a label being both. Whether 39075:2024 mandates\ncross-declaration property inheritance is **UNRESOLVED**. Model the key label set\nwithout committing to an inheritance rule.\n","acceptance_criteria":"The schema model represents a node/edge type's key label set separately from its complete label set, including the inferred (no =\u003e) case.\nNode and edge type identity and the duplicate-type check key on the key label set, not the complete label set.\nEdge endpoint resolution for the inline-filler form matches on the key label set, so an endpoint (:A) resolves against a node type whose key label set is {A} and whose complete label set is larger.","status":"closed","priority":2,"issue_type":"feature","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:36:26Z","created_by":"areqag","updated_at":"2026-07-28T04:09:28Z","started_at":"2026-07-28T03:54:17Z","closed_at":"2026-07-28T04:09:28Z","close_reason":"Shipped in PR #457 (squash-merged 2026-07-28). schema.NodeType/EdgeType now carry KeyLabels (identity, also the map key) and CompleteLabels separately; resolver satisfaction reads CompleteLabels while endpoint/edge keying reads KeyLabels. Divergence is asserted by hand-built schemas in internal/resolver/keylabelset_test.go and the GG22 inference invariant by internal/schema/gql/keylabelset_test.go.","external_ref":"https://github.com/areqag/gqlc/issues/367","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.8","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:36:26Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.5","title":"schema/gql: LIST / ARRAY property types require a parameterised type model","description":"## Design note 2026-07-28: why the element type cannot be encoded in the string\n\nInvestigated while picking this up; recording so the next run does not redo it.\nNo code written.\n\nThe obvious cheap representation — keep `type PropertyType string` and spell a\nlist `\"LIST\u003cSTRING\u003e\"` — is **not viable**, and the reason is specific rather\nthan aesthetic.\n\n`goType` (internal/codegen/types.go:163) ends with a bare `return \"\", false`\nafter its switch. That `false` is not an \"unknown value\" signal: it is a\n*defined and load-bearing* one, meaning **the eight permanently unrepresentable\nwidths** (INT128/256, UINT128/256, FLOAT16/128/256, DECIMAL — types.go:205-212,\npermanent per spec §9). A structured string would fall into the same `false` and\nbe indistinguishable from \"no faithful Go carrier exists\", i.e. lists would be\nreported to users as permanently unsupportable rather than as unimplemented.\n\n**Correction to the first version of this note.** I initially wrote that the two\ncall sites which discard `ok` —\n\n    internal/codegen/prepare.go:666    ty, _ := goType(prop.Type)\n    internal/codegen/prepare.go:689    ty, _ := goType(t.Type)\n\n— would silently emit an empty Go type. That is **wrong**, and they are not\nlatent bugs. Both are in `phaseBDerive`, and Phase A (`prepare.go:572` and\n`prepare.go:630`) already rejects unrepresentable widths with\n`ErrUnrepresentableWidth` before Phase B runs; the invariant is stated in the\ncomment at prepare.go:641 and in the one at prepare.go:663. Discarding `ok`\nthere is correct by construction.\n\nSo a structured-string list would be caught **loudly**, not silently. The defect\nis that it would be caught *as the wrong thing*: users would be told their\n`LIST\u003cSTRING\u003e` property has no faithful Go carrier and is permanently\nunsupportable, when it is simply not implemented yet. Diagnosis, not silence,\nis the failure here.\n\n### Consequence for the shape of this bead\n\nThe type model needs an explicit discriminator — a `Kind()` on `PropertyType`,\nor a composite representation — read *before* any switch, so that \"is a list\"\nand \"is an unrepresentable scalar\" cannot collapse into one boolean. Whatever is\nchosen, the two `ty, _ :=` sites must stop discarding `ok`; they are a latent\nbug independent of lists and could be fixed first, on their own.\n\nSerialisation is a smaller constraint than expected but real: `PropertyType` is\na bare string in JSON at `internal/schema/schema.go:45` (`Property.Type`) and\n`internal/resolver/validated.go:114` (`ResolvedProperty.Type`), and appears in\nresolver golden files as `\"type\": \"STRING\"`. A non-string representation needs\ncustom marshalling to keep those goldens stable. It is used as a map *value*,\nnot a map key, so nothing depends on its comparability as a key.\n\n### Sequencing\n\nThis remains the largest single item in the epic and it embeds a design decision\nthat is **not** in the ADR set delegated to the overnight loop (h9n.6, h9n.9,\nh9n.12, h9n.25). Recommend it gets an ADR of its own, or an explicit decision\nfrom the owner, before implementation.\n\nAlso note: closing this bead retires four entries from `isoGaps` in\n`internal/schema/gql/corpus_iso_test.go` (`constructed value type`,\n`list value type`, `component type`, `component type list`), so\n`isoGapRatchet` drops 14 -\u003e 10.\n\n---\n\n`valueType` has three list alternatives (GQL.g4:1723-1725), none supported:\n\n    | listValueTypeName LEFT_ANGLE_BRACKET valueType RIGHT_ANGLE_BRACKET (LEFT_BRACKET maxLength RIGHT_BRACKET)? notNull?  #listValueTypeAlt1\n    | valueType listValueTypeName (LEFT_BRACKET maxLength RIGHT_BRACKET)? notNull?                                          #listValueTypeAlt2\n    | listValueTypeName (LEFT_BRACKET maxLength RIGHT_BRACKET)? notNull?                                                    #listValueTypeAlt3\n\nwith `listValueTypeName : LIST | ARRAY` (1968-1975). So `LIST\u003cSTRING\u003e`,\n`STRING LIST`, `STRING ARRAY`, `LIST` and `ARRAY[10]` are all one type family\nspelled five ways. Verified 2026-07-25: `LIST\u003cSTRING\u003e` fails with\n`ErrUnsupportedType`.\n\nLists matter more than the other composite types because neo4j property values\nare natively list-capable — a homogeneous list of primitives is a *storable\nproperty*, unlike `RECORD` or `PATH`. This is the one composite family that a\nproperty-graph code generator arguably must support.\n\n## The blocker: the type model is string-keyed\n\n`normaliseType` (propertytype.go:110-113) maps a canonicalised *spelling* to a\nflat `graph.PropertyType` enum. A list type is parameterised by its element\ntype, which a flat enum and a string key cannot express. So this bead is not a\ntable entry — it needs `graph.PropertyType` to gain a composite representation,\nand every consumer (Go type rendering, driver value conversion, the resolver's\nproperty type-checking) to handle it.\n\nThat makes it the largest single item in the epic and the reason it is filed\nseparately from the missing scalars.\n\n## Latent bug this activates\n\n`hasNotNull` (propertytype.go:41-46) reports NOT NULL by scanning the whole\nsubtree for a `NotNullContext`. For `LIST\u003cSTRING NOT NULL\u003e` the inner element's\nqualifier would be read as the outer list's, so a nullable list of non-null\nstrings would be modelled as a non-null list. Harmless while lists are rejected;\na correctness bug the moment they are not. The alternatives are labelled\n(`#listValueTypeAlt1` etc.), so the outer `notNull?` is directly addressable and\nthe subtree scan can be replaced with a per-alternative read.\n\n## Alt3 has no element type\n\n`LIST` and `ARRAY` with no `\u003cT\u003e` are legal and mean a list of any value type.\nDecide whether that maps to something (`[]any`) or is rejected with a precise\nerror; it must not fall through to a generic `ErrUnsupportedType`.\n\n","acceptance_criteria":"All five list spellings (LIST\u003cT\u003e, T LIST, T ARRAY, bare LIST, ARRAY[n]) resolve or are rejected with a precise error, never a generic ErrUnsupportedType.\ngraph.PropertyType represents a parameterised list type; Go rendering and driver conversion handle it.\nhasNotNull no longer attributes an element type's NOT NULL to the enclosing list.\nNested lists (LIST\u003cLIST\u003cINT\u003e\u003e) have a decided, tested outcome.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:34:29Z","created_by":"areqag","updated_at":"2026-07-28T20:28:27Z","started_at":"2026-07-28T19:49:02Z","closed_at":"2026-07-28T20:28:27Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/368","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.5","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:34:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.4","title":"schema/gql: missing predefined property types — BYTES, TIME, DURATION, SIGNED/UNSIGNED numerics","description":"`typeSpellings` (propertytype.go:53-108) covers boolean, character-string,\nnumeric, date and timestamp families. Four grammar-reachable predefined-type\nfamilies are absent and therefore fail with `ErrUnsupportedType`, even though\neach has an unambiguous Go and neo4j mapping.\n\nAll four verified rejected 2026-07-25 against a built binary.\n\n## 1. Byte strings — GQL.g4:1760-1763\n\n    byteStringType : BYTES (LEFT_PAREN (minLength COMMA)? maxLength RIGHT_PAREN)? notNull?\n                   | BINARY (LEFT_PAREN fixedLength RIGHT_PAREN)? notNull?\n                   | VARBINARY (LEFT_PAREN maxLength RIGHT_PAREN)? notNull?\n\n`byteStringType` is listed in `predefinedType` (1743) but has no entry.\nMaps to `[]byte`.\n\n## 2. Time-of-day — GQL.g4:1881-1889\n\n    timeType      : ZONED TIME notNull? | TIME WITH TIME ZONE notNull?\n    localtimeType : LOCAL TIME notNull? | TIME WITHOUT TIME ZONE notNull?\n\nBoth are `temporalInstantType` alternatives (1863-1864) and both are missing,\nwhile their datetime siblings (`ZONED DATETIME`, `LOCAL DATETIME`, `TIMESTAMP\nWITH/WITHOUT TIME ZONE`) are present. neo4j has native `Time` and `LocalTime`,\nso this is an omission rather than a mapping problem.\n\nNote there is no bare `TIME` spelling in the grammar — the zone qualifier is\nmandatory — so the four spellings above are the complete set.\n\n## 3. Duration — GQL.g4:1891-1898\n\n    temporalDurationType      : DURATION LEFT_PAREN temporalDurationQualifier RIGHT_PAREN notNull?\n    temporalDurationQualifier : YEAR TO MONTH | DAY TO SECOND\n\nThe qualifier is mandatory, so exactly two spellings exist. neo4j has a native\n`Duration`. Whether the two qualifiers collapse onto one Go type or need\ndistinguishing is the only real design question here.\n\n## 4. SIGNED / UNSIGNED verbose numerics — GQL.g4:1803, 1816\n\n    signedBinaryExactNumericType   : ... | SIGNED? verboseBinaryExactNumericType\n    unsignedBinaryExactNumericType : ... | UNSIGNED verboseBinaryExactNumericType\n\nThese are plain aliases for spellings already in the table — `SIGNED INTEGER` is\n`INTEGER`, `UNSIGNED INTEGER32` is `UINT32`, and so on across all nine verbose\nforms. Both verified reachable and rejected 2026-07-25.\n\n**The comment at propertytype.go:50-52 asserts the opposite** and must be\ncorrected:\n\n    // Only grammar-reachable spellings appear: e.g. SIGNED, UNSIGNED,\n    // NUMERIC, CHARACTER, bare DATETIME/LOCALDATETIME are listed in some\n    // references but the GQL grammar does not accept them, so they cannot occur.\n\n`NUMERIC`, `CHARACTER` and the bare `DATETIME`/`LOCALDATETIME` halves of that\nclaim are correct; the `SIGNED`/`UNSIGNED` half is not.\n\n## Note on canonicalSpelling\n\n`canonicalSpelling` (propertytype.go:118-125) truncates at the first `(`, which\nis what makes `DURATION(DAY TO SECOND)` collapse to `DURATION` and lose its\nqualifier. Adding duration therefore needs the qualifier read from the tree, not\nfrom the truncated spelling — the string-keyed lookup cannot express it.\n","acceptance_criteria":"BYTES, BINARY and VARBINARY resolve to a byte-slice property type.\nZONED TIME, TIME WITH TIME ZONE, LOCAL TIME and TIME WITHOUT TIME ZONE resolve to time-of-day property types.\nDURATION(YEAR TO MONTH) and DURATION(DAY TO SECOND) resolve, with the qualifier read from the parse tree rather than the truncated spelling.\nAll nine SIGNED/UNSIGNED verbose numeric spellings resolve to the same types as their non-prefixed equivalents.\nThe false SIGNED/UNSIGNED unreachability claim in the propertytype.go comment is corrected.\nEach new spelling has a test; generated Go and driver conversion handle the new types.","notes":"2026-07-26 (team-lead, while ratifying gqlc-h9n.16): the doc comment on `typeSpellings` (internal/schema/gql/propertytype.go) is FACTUALLY WRONG about this bead's subject. It reads: \"SIGNED, UNSIGNED, NUMERIC, CHARACTER, bare DATETIME/LOCALDATETIME are listed in some references but the GQL grammar does not accept them, so they cannot occur.\"\n\nThe grammar does accept SIGNED and UNSIGNED. GQL.g4:1803 is `SIGNED? verboseBinaryExactNumericType` and GQL.g4:1816 is `UNSIGNED verboseBinaryExactNumericType` — where UNSIGNED is MANDATORY, i.e. it is the only spelling of that alternative. Both are real lexer tokens (SIGNED at :3448, UNSIGNED at :3478). So `SIGNED INTEGER8` and `UNSIGNED INTEGER8` parse, canonicalise to \"SIGNED INTEGER8\" / \"UNSIGNED INTEGER8\", miss typeSpellings, and return ErrUnsupportedType.\n\nTwo consequences. (1) The rejection is honest — ErrUnsupportedType, not a wrong model — so this is a gap, not the accepted-input-nil-error-wrong-model family. (2) The comment's stated REASON for the gap is false, which matters more than the gap: it tells the next reader not to bother looking, and it is exactly the unsourced-universal shape that gqlc-cfj is about. The NUMERIC / CHARACTER / bare-DATETIME half of that sentence has NOT been checked and may well be right.\n\nThe gqlc-h9n.16 implementer is rewriting that doc comment and has been told to correct the SIGNED/UNSIGNED clause in passing, without implementing the types. Implementing them stays here.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:34:27Z","created_by":"areqag","updated_at":"2026-07-27T00:10:10Z","started_at":"2026-07-26T23:25:24Z","closed_at":"2026-07-27T00:10:10Z","close_reason":"Landed in PR #439 (squash ce9c656). BYTES/BINARY/VARBINARY, TIME/LOCAL TIME, DURATION, and SIGNED/UNSIGNED verbose numerics now resolve; 11 corpus entries flip unsupported→resolves. Five lossy entries carry bead+reason+semanticCases rows. Follow-ups: gqlc-8pe (duration-qualifier field), gqlc-eh4 (positive losslessness declaration), gqlc-h9n.21 (collision assertion, table extended to 11 rows).","external_ref":"https://github.com/areqag/gqlc/issues/369","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.4","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:34:27Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.3","title":"schema/gql: UNDIRECTED edge kind with a directed arc is silently reinterpreted as directed","description":"`edgeTypePattern` (GQL.g4:1553-1555) lets `edgeKind` and the arc direction\ndisagree:\n\n    edgeTypePattern\n        : (edgeKind? edgeSynonym TYPE? edgeTypeName)? (edgeTypePatternDirected | edgeTypePatternUndirected)\n        ;\n    edgeKind : DIRECTED | UNDIRECTED ;\n\n`EnterEdgeTypePattern` (listener.go:154) never reads `edgeKind` at all. It\nbranches solely on `EdgeTypePatternDirected()`. So an `UNDIRECTED` keyword in\nfront of a right-pointing arc is silently discarded and the edge is generated as\ndirected.\n\nVerified 2026-07-25:\n\n    CREATE GRAPH TYPE G AS {\n      NODE TYPE Person (p :Person {id STRING, name STRING}),\n      UNDIRECTED EDGE TYPE K (p)-[:KNOWS]-\u003e(p)\n    }\n\nresolves, and `MATCH (p1:Person)-[r:KNOWS]-\u003e(p2:Person)` type-checks against it —\ni.e. the schema author wrote `UNDIRECTED` and got a directed edge type with no\ndiagnostic. The mirror case is handled by accident: `DIRECTED EDGE TYPE K\n(p)~[:KNOWS]~(p)` is rejected, but with `ErrUndirectedEdge` (because the arc is\nundirected), not because the two disagree.\n\n## What to decide\n\nWhether ISO GQL's static semantics make a kind/arc contradiction a syntax rule\nviolation is a prose question. Two candidate outcomes, both acceptable under the\nno-dialect principle:\n\n- the standard forbids the contradiction -\u003e reject it with a new sentinel naming\n  both the declared kind and the arc direction;\n- the standard permits it and `edgeKind` is redundant with the arc -\u003e say so in\n  the ADR and add a test pinning that `UNDIRECTED ... -\u003e` is deliberately read\n  as directed.\n\nWhat is *not* acceptable is the current state, where nothing records which of\nthose two is intended. Note the interaction with the undirected-edge-type\nrejection decision: if undirected edges are rejected outright, the natural\nreading is that `UNDIRECTED` in any position must be refused, which makes this\na one-line fix rather than a semantic question.\n","acceptance_criteria":"edgeKind is read by EnterEdgeTypePattern rather than ignored.\nA kind/arc contradiction produces a decided, tested outcome — either a sentinel naming both the declared kind and the arc direction, or a pinned deliberate-acceptance test.\nThe decision is recorded in the subset ADR.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:33:17Z","created_by":"areqag","updated_at":"2026-07-26T23:05:30Z","closed_at":"2026-07-26T23:05:30Z","close_reason":"Landed as ccd6618 (PR #432, squash). Adds ErrEdgeKindArcMismatch covering six mismatch spellings across pattern and phrase forms. Linus PASS after 7-mutation matrix; PR body corrected pre-merge (the 'every guard pinned' universal was falsified by the two defensive returns after l.fail, and the corpus count was 9 files not 11, none of them .gql changes). Deviation-register follow-up filed as gqlc-xtq.","external_ref":"https://github.com/areqag/gqlc/issues/370","dependencies":[{"issue_id":"gqlc-h9n.3","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:33:16Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f977c-3e4a-7649-a5df-4fe55f96f914","issue_id":"gqlc-h9n.3","author":"areqag","text":"Resolution guidance, verified 2026-07-25 with an out-of-tree ANTLR probe (linus, gqlc-h9n.2 design audit):\n\n`schema.EdgeType` embeds `EdgeKey{Source,Label,Target}` and has **no undirectedness field** (internal/schema/schema.go:28-40). Probe result: `UNDIRECTED EDGE E (:A)-[:E]-\u003e(:B)` and `DIRECTED EDGE E (:A)-[:E]-\u003e(:B)` both parse clean and both yield edge `{A E B}` — byte-identical. So a golden test cannot pin this defect at all; there is no discriminating output to capture.\n\nThat kills the option of 'fix it by representing undirectedness in a golden' and narrows this bead to two real choices:\n\n(a) Give Schema an undirected representation first, then map the kind onto it. Larger blast radius: EdgeKey identity, resolver, codegen, and the generated Cypher all assume directedness.\n\n(b) **Reject** `edgeKind = UNDIRECTED` in the listener, converting this from class 2 (silent reinterpretation) to class 3 (documented rejection). No model change, and it matches the owner's rule better than silently changing an edge's meaning — declining an optional feature is conformant under subclause 24.2, but silently reinterpreting one is a defect regardless. Consistent with the existing ErrUndirectedEdge treatment of edgeTypePatternUndirected.\n\nTeam-lead's read (2026-07-25): (b) is the likely resolution. Confirm before implementing.\n\nCorpus interaction: the gqlc-h9n.2 corpus **cannot** demand this file, because the UNDIRECTED token is fully discharged by `UNDIRECTED EDGE E (:A)~[:E]~(:B)`, which is a different (already-rejected) construct. So the corpus carries it as a hand-maintained `semanticCases` entry, classified `resolves` today with a reason line saying the corpus cannot detect the defect. **This bead's red is reclassifying that entry to `unsupported`** — do not expect an existing failing test to go green.","created_at":"2026-07-25T04:15:30Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-0ri","title":"schema/gql: audit the accepted GQL subset against ISO 39075 — record or remove every deviation","description":"Owner principle (2026-07-24): *\"we should be able to accept any valid ISO GQL. we\nshould not have our own GQLC flavour of ISO GQL.\"*\n\nTaken at face value, that principle reaches further than the phrase-form bug\n(gqlc-uhb). `internal/schema/gql` currently rejects several constructs that **are\nvalid ISO GQL**:\n\n| Sentinel | Construct | Site |\n|---|---|---|\n| `ErrLabelImplication` | label implication `=\u003e :Label` | listener.go:132-148 |\n| `ErrUndirectedEdge` | undirected edge types | listener.go:150-152 |\n| `ErrUnsupportedSource` | graph type `LIKE` source (and `COPY OF`) | listener.go:87-90 |\n\nEach of these is a deliberate, documented-in-code deviation from full ISO GQL.\nNone is recorded in an ADR. So today the supported GQL subset exists only as\nscattered sentinels in one file, which is how the phrase-form gap went unnoticed:\nthere is no single place that states what gqlc accepts.\n\n## Two deliverables\n\n**1. Decide each deviation.** For every rejection above: either implement it, or\nrecord an ADR justifying the deviation with a concrete reason. Some are likely\nlegitimate — undirected edges may have no faithful mapping onto the\nopenCypher/neo4j execution model that the generated code targets, and\n`COPY OF`/`LIKE` reference catalog state that a static code generator cannot see.\nThose are good reasons to reject. They should be *written down*, not implicit,\nso the subset is a decision rather than an accident.\n\n**2. Make unhandled grammar branches impossible to ship.** Enumerate every\nalternative of every element-type rule in `GQL.g4` and assert the listener either\nhandles it or explicitly rejects it. The phrase-form bug existed precisely because\na grammar branch was neither handled nor rejected — it fell through into silence.\nA test that fails when a new branch is left unhandled prevents the next one.\n\nNote this is a stronger guarantee than the `ErrEmptyGraphType` guard in\ngqlc-uhb. That guard catches the *symptom* (a schema that resolved to nothing);\nthis catches the *cause* (a branch nobody wired up), including cases where some\ndeclarations resolve and only one form is silently skipped.\n","acceptance_criteria":"An ADR states the supported graph-type DDL subset in one place, replacing the implicit subset defined by scattered l.fail calls.\nEvery declined construct is recorded against its Annex D feature ID with the phrasing 'optional feature GGxx not supported because ...', not as a deviation from ISO.\nMandatory features are identified as such and none is declined.\nThe undirected-edge rejection is grounded in observable nonconformance (IS DIRECTED, IS SOURCE OF) plus GH02 optionality, not in an unrepresentability claim.\nThe COPY OF rejection is grounded in gqlc's one-graph-type rule and missing catalog namespace, not in offline-ness.\nFabric extensions and GQL-2 prereserved words are recorded as out of scope rather than as gaps.\nEvery sibling bead's decision is reflected; the ADR lands last in the epic.","notes":"## Evidence gathered 2026-07-25: ErrLabelImplication is a bigger gap than it looks\n\nWhile researching the endpoint question for gqlc-uhb, two strong independent\nsources showed that label implication (`=\u003e`) is not a fringe corner of ISO GQL —\nit is the standard's mechanism for **node type hierarchies and property\ninheritance**, i.e. a core graph-type modelling feature.\n\n1. Microsoft Fabric's GQL graph types reference (reviewed by Stefan Plantikow, a\n   GQL standard editor) uses `=\u003e` throughout, and explains its semantics:\n   `(:University =\u003e :Organization)` and `(:Company =\u003e :Organization)` where\n   \"`University` and `Company` are the key labels of the two node types defined,\n   while `Organization` is a secondary label shared by both\". It states that\n   \"the properties of node types identified by secondary labels are automatically\n   inherited when using this syntax\", and that one label — the key label — must\n   uniquely identify the node type.\n   https://learn.microsoft.com/en-us/fabric/graph/gql-graph-types\n\n2. Alex Miłowski's worked example declares node types the same way:\n   `NODE :Person =\u003e :Thing { ... } AS Person`.\n   https://www.milowski.com/journal/entry/2024-06-26T12:00:00-07:00/\n\nlistener.go:132-148 currently rejects every `=\u003e` outright via\n`rejectLabelImplication`, so gqlc cannot express any node type hierarchy at all.\nUnder the no-dialect principle this is the most significant of the three\ndeviations and probably deserves its own bead once this audit prioritises them.\n\nAlso worth noting for the audit's scope: the Fabric page documents `ABSTRACT`\nnode types and a `+=` inheritance shortcut, and its property types include\n`LIST\u003cT\u003e`. Whether those are ISO GQL proper or Fabric extensions needs checking\nbefore treating them as conformance obligations — the page itself cautions that\nsome syntax it shows \"isn't currently supported directly for graph\", so it is\ndocumenting the standard's syntax rather than only its own.\n\n## gqlc-h9n.25 resolved 2026-07-28: adds no row to this register\n\nNon-power-of-two binary precision (INT(7), INT(10), UINT(p), INTEGER(p)) was\ndecided in docs/adr/0017-off-lattice-binary-widths-accepted-and-lost.md:\n**accepted**, resolving to the unqualified type, with the declared width lost.\n\nThe bead asked for \"an entry in the gqlc-0ri deviation register saying so out\nloud\". On inspection that is the wrong home and the note is here instead of a\ntable row. This register's table is one row per *rejection sentinel* — the\nquestion it answers is \"what valid ISO GQL does gqlc refuse, and why\". h9n.25\nrefuses nothing, so there is no sentinel to key a row on. Writing one anyway\nwould put a non-rejection in a rejection table, which is exactly the half-wrong\nrow ADR 0016 declined to write when it split ErrUnsupportedSource.\n\nWhat the decision does produce is a *fidelity* limit, which lives in the ADR\nwhere ADR 0002's DURATION and CHAR(4) limits already live, and — new — is\nasserted rather than described: three semanticCase rows with siblings, so\nINT(10) == INT == INTEGER == INT(7) goes red the day the model can tell them\napart.\n\nConsequence for this bead's closure accounting: h9n.25 is not one of the entries\ngqlc-0ri must account for. gqlc-h9n.31 (declared bit width on PropertyType,\nblocked on a named consumer) carries the follow-up.","status":"closed","priority":2,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:01:29Z","created_by":"areqag","updated_at":"2026-07-30T03:02:00Z","started_at":"2026-07-30T02:29:36Z","closed_at":"2026-07-30T03:02:00Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/354","metadata":null,"dependencies":[{"issue_id":"gqlc-0ri","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:39:04Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-0ri","depends_on_id":"gqlc-uhb","type":"discovered-from","created_at":"2026-07-24T23:01:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nab","title":"config: rename schema_language value 'gqlc' → 'gql' (name collision with the tool)","description":"The schema_language enum value 'gqlc' collides with the name of the tool itself. The schema language is GQL (ISO Graph Query Language); gqlc is the code generator. Rename the wire value 'gqlc' → 'gql' across README, config-file-format spec, cli-stage-1/2 and cli-generate-pipeline specs, and the Go const SchemaLangGQLC → SchemaLangGQL (internal, no consumers).","status":"closed","priority":2,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-19T04:57:22Z","created_by":"areqag","updated_at":"2026-07-19T05:15:59Z","started_at":"2026-07-19T04:57:26Z","closed_at":"2026-07-19T05:15:59Z","close_reason":"Merged via PR #351 (squash into master)","external_ref":"https://github.com/areqag/gqlc/issues/350","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-oe0","title":"chore(ci): add live-smoke to master's required status checks","description":"gqlc-rvw (PR #323) landed the CODE half of promoting codegen-live to\nPR-blocking — the workflow now runs on every PR. But the CONFIG half is\nmissing: master's branch-protection required_status_checks.contexts is\nstill [lint, test, tidy, actionlint, govulncheck]. \\`live-smoke\\` (the\ncodegen-live workflow's job name) is NOT in the required list, so it\nruns but doesn't block auto-merge.\n\nProof (PR #323 itself): the live-smoke job was still IN_PROGRESS when\nauto-merge fired at 05:37:22Z. Job completed successfully at 05:37:32Z\n(10s later) — but had it failed, the merge would have happened anyway.\nWall-time on the runner was 63s (setup-go + setup-just + docker + both\narms in parallel).\n\nFix (one-line via the GH API):\n\n  gh api -X PATCH \\\\\n    repos/areqag/gqlc/branches/master/protection/required_status_checks \\\\\n    -f 'contexts[]=lint' -f 'contexts[]=test' -f 'contexts[]=tidy' \\\\\n    -f 'contexts[]=actionlint' -f 'contexts[]=govulncheck' \\\\\n    -f 'contexts[]=live-smoke'\n\nVerify: after apply, \\`gh api repos/areqag/gqlc/branches/master/protection\\`\nshould include \\`live-smoke\\` in contexts. Then any PR whose live-smoke\njob FAILS will block auto-merge.\n\nAlternative: do this via the GitHub UI at\nhttps://github.com/areqag/gqlc/settings/branches — edit the master rule,\nadd \\`live-smoke\\` to required status checks.\n\nThis is a shared-infrastructure config change, not a code change — needs\nowner action (or explicit owner sign-off before an agent runs the API\ncall).\n\nDescends from: gqlc-rvw (which shipped the code half in PR #323).","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T05:38:36Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T13:47:34Z","closed_at":"2026-07-18T13:47:34Z","close_reason":"Applied via gh api PATCH — master's required_status_checks.contexts now includes live-smoke alongside the existing 5 checks. Verified via GET on branches/master/protection. Any future PR whose live-smoke job fails or is still in-progress will now block auto-merge. The gqlc-rvw promotion is fully complete — both the code half (on.pull_request trigger, PR #323) and the config half (required-checks list).","external_ref":"https://github.com/areqag/gqlc/issues/324","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2r4","title":"master-guard claude-pre-bash hook false-positives on worktree commits","description":"The PreToolUse master-guard hook (.githooks/claude-pre-bash:31) resolves the branch via 'git rev-parse --abbrev-ref HEAD' in the hook's own pwd (the Bash tool's persistent cwd), not the git command's actual target. A 'git -C \u003cworktree\u003e commit' (or a command that cd's inside the worktree within the same call) targeting a feature branch is denied when the session cwd sits on master. Observed 2026-07-14 during gqlc-ls8.2 (commit-5 halt in .claude/worktrees/ls8-2-cypher). Workaround: persistently cd the Bash tool into the worktree so the hook resolves the right branch. Fix direction: make the hook resolve the branch at the command's effective target — parse 'git -C \u003cpath\u003e' and leading 'cd \u003cpath\u003e \u0026\u0026' from tool_input.command before rev-parse. Needs its own PR + review; do NOT patch mid-loop.","status":"closed","priority":2,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-14T12:25:46Z","created_by":"antranig-yeretzian","updated_at":"2026-07-17T10:06:20Z","started_at":"2026-07-17T09:12:58Z","closed_at":"2026-07-17T10:06:20Z","close_reason":"Shipped in PR #310 (squash 6a83f0e). PreToolUse master-guard rewritten as a python3 analyzer: effective-target resolution (cd/pushd chains, -C joins), quote-aware heredoc stripping, substitution-body recursion, shlex tokenization kills prose false positives; symbolic-ref fallback covers unborn HEADs. 29-case harness wired into just test (CI parity). Adversarial Linus review: PASS after 2 SHOULD-FIXes applied.","external_ref":"https://github.com/areqag/gqlc/issues/303","metadata":null,"comments":[{"id":"019f609b-8fb1-72aa-9b5d-151deba5b20f","issue_id":"gqlc-2r4","author":"antranig-yeretzian","text":"Second finding (2026-07-14, same ls8.2 halt): the hook also has a FALSE NEGATIVE. Regex at line 25 requires the word git immediately followed by the word commit, so the form [git -C \u003cpath\u003e commit ...] does not match at all and is never branch-checked — this is how the ls8-2 worktree commits (c4deb99..6f2b2c1) landed before the halt. Empirically verified: the bare form and the cd-compound form MATCH (then branch-check the hook pwd -\u003e false positive for worktree work); the -C form passes unchecked. Also noted: the hook matches the raw command string including quoted literals and prose (a for-loop echoing the bare form as a string was denied; so was a bd-comment command whose prose contained it). Fix should: (1) parse -C / leading-cd to resolve the command's effective target dir, (2) branch-check THAT dir, (3) ideally ignore quoted literals. Interim team ruling for ls8.2: worktree commits proceed via the -C form — target branch is cypher-collection-sink, the hook's stated intent (block commits on master/main) is preserved, and repo git hooks (core.hooksPath pre-commit gates) still fire normally. This interim allowance dies when this bead's fix lands.","created_at":"2026-07-14T12:30:35Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-ls8.2.1","title":"cypher-collection-sink spec §5 phase-ordering fix (Rev 3)","description":"Rev 3 (9843af7) hoists the addParameterUse sink gate from Rev 2's Phase D bundle into a new Phase B.5 that MUST land before any Phase C handler migration. Rev 2 (452c6b7) shipped a phase-ordering bug: its §5 bundled l.fail and addParameterUse into Phase D (post-migration), but every Phase-C handler (Match/With/Set/Delete/Remove) has a reach path into addParameterUse via mineWhere/collectSetItem/typeExpressionMining. Dropping the Match guard during Phase C surfaced the leak: TestMustParse/exists_body_mixed_limit_skip_and_where_params (inline require.Equal, no -update path) went red because $threshold picked up a spurious PropertyUse via EnterOC_Match → mineWhere → pairAddSub → addParameterUse. Fix shipped as three commits: b5e206d (Phase B.5 sink gate, observably-nil at then-current stack tip), 764dcd8 (Match migration re-run under the gate, now green), 9843af7 (spec Rev 3: revision-history stanza with failure narrative, §3.4 approved-map correction, §5 restructure with Phase B/B.5/D). Note: an earlier attempt to formalise this fix committed as b126e87 with a misleading subject and a stanza that falsely claimed Rev 2 had already contained the fix; that commit was amended into 9843af7 after linus-2 grill against git receipts (git show 452c6b7 verifies Rev 2 shipped bundled Phase D, not Phase B.5). Close as part of the eventual chore(beads) close-out PR for gqlc-ls8.2, do NOT create a separate close-out PR just for it.","status":"closed","priority":2,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-14T03:01:40Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T23:28:18Z","closed_at":"2026-07-14T23:28:18Z","close_reason":"Shipped inside the gqlc-ls8.2 stack (b5e206d Phase B.5 gate, 764dcd8 Match re-migration, 9843af7 spec Rev 3), merged to master via PR #305 per this bead's close-out instruction. GH mirror #284 closed manually.","external_ref":"https://github.com/areqag/gqlc/issues/284","dependencies":[{"issue_id":"gqlc-ls8.2.1","depends_on_id":"gqlc-ls8.2","type":"parent-child","created_at":"2026-07-13T23:01:40Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ls8.5","title":"cypher parser tests: migrate hand-built shape expectations into the golden corpus","description":"internal/query/cypher/parser_test.go (3,657 lines). query.Query has two faces (Go API and JSON wire, ADR 0008) and the parser tests pin both by hand: ~400 hand-built expectation values (oneBranch/oneWriteBranch/must helper constructions) duplicate shape facts the 3,199-file golden corpus already pins mechanically. Every additive model axis (ay9's 100-golden rebaseline was one -update) also costs manual edits across this file — the maintenance outlier of the whole test suite per the 2026-07-12 test-topology audit. BLOCKED BY gqlc-ls8.2: same package; start with a fresh team stacked on ls8.2's branch after its Linus PASS.","design":"Migrate shape-pinning cases into the golden corpus (new fixtures where a TCK golden does not already cover the case); keep hand-built construction only where it IS the point — adversarial inputs, smart-constructor invariants, property tests. Wire-face goldens become the single shape-pinning surface; the Go-face tests shrink to invariants. Team decides the migration split in a docs/specs/ design doc, grilled by Linus BEFORE code. Careful: this is test architecture — parser source must not change (except possibly test helpers).","acceptance_criteria":"No parser source changes (internal/query/cypher/*.go non-test untouched, modulo test helper files). Coverage does not shrink: every deleted hand-built case is either demonstrably covered by an existing golden or gets a new golden fixture. Property tests and sentinel-reachability sweep untouched and green. TCK counts unchanged. just test / fmt-check / lint green. parser_test.go materially smaller; a model-change rebaseline is one -update plus zero manual expectation-table edits.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-13T03:15:40Z","created_by":"antranig-yeretzian","updated_at":"2026-07-17T04:01:31Z","started_at":"2026-07-15T00:04:20Z","closed_at":"2026-07-17T04:01:31Z","close_reason":"Shipped in PR #308 (squash-merged 2026-07-17, merge sha on master). parser_test.go 4,151 -\u003e 3,528 lines (-623, ~15.0%); 42 shape-mirror pins migrated to golden corpus (34 sig-less + 8 sig-equal), sole sig-divergent pin retained; permanent dual-invariant TestMustParseGoldenTwins + harvestExecutingScenarios; goldens byte-identical; TCK counts unchanged; zero parser source changes. Three phase changes 5ccd1de/f7f6ffe/f33296a, adversarial review v1-v5 spec grill + per-phase verdicts + whole-diff PASS by linus-3. GH #285 auto-closes via PR body.","external_ref":"https://github.com/areqag/gqlc/issues/285","metadata":null,"dependencies":[{"issue_id":"gqlc-ls8.5","depends_on_id":"gqlc-ls8","type":"parent-child","created_at":"2026-07-12T23:15:39Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-ls8.5","depends_on_id":"gqlc-ls8.2","type":"blocks","created_at":"2026-07-12T23:15:53Z","created_by":"antranig-yeretzian","metadata":"{}"}],"comments":[{"id":"019f631c-c754-7ed1-b67a-afce0c3d72c2","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Team ls8-5 spun up (carmack-3 implementer, linus-3 reviewer), worktree ls8-5-goldens @ cypher-golden-migration, base master 05cb728. linus-3 oriented with independent census: parser_test.go is 4,151 lines (bead's ~3,657 stale); true migration surface is 190 mustParse cases (157 helper-built + 33 raw Query literals) — the bead's ~400 conflated per-binding must() constructor calls with cases. Only 35/190 carry corpus citations. Two design tensions flagged for the spec grill: (1) TestPropertyReadCoreParses iterates mustParse pins by reference, so deletions shrink its input surface — needs explicit disposition; (2) the mustParse doctrine (parser_test.go:15-44, evidentiary circularity) appears to forbid NEW authored goldens — spec must respect it or amend it with justification. carmack-3 authoring spec first-party; census collision resolves at grill.","created_at":"2026-07-15T00:10:58Z"},{"id":"019f632a-2a0b-7d5d-83b1-1f4282b1ea33","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Spec round 1: FAIL from linus-3 at 3538a90. Spec claimed ~140 Class-A deletions; linus-3's mechanical census found 43 true shape-mirrors, 4 verbatim-TCK-but-skiplisted, 112 authored (no golden possible; TestGoldenOrphans + mustParse doctrine forbid minting authored goldens). Value proposition drops ~70%-\u003e~15% file reduction. Lead ruling: v2 spec at honest 43-case scope + skiplisted-4 disposition + explicit TestPropertyReadCoreParses treatment (its input surface shrinks with deletions); doctrine amendment for the 112 authored pins ruled OUT (circularity objection stands; appeal path to lead available); every v2 number reproducible by a stated command, census before prose. Amended acceptance: every demonstrable shape-mirror deleted with mechanical proof, zero coverage shrink, suite green — 'materially smaller' framing from the stale ~400 census is retired. Side note: linus-3 self-flagged a transient in-tree scratch probe (removed, tree verified clean); constraint restated, disclosure commended.","created_at":"2026-07-15T00:25:35Z"},{"id":"019f633a-1d35-7825-92ee-f948adde1176","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Spec round 2: v2 (9b192b8) got REVISE from linus-3 — census reproduced independently (43/4/112), both sig-audit spot-checks verified (Call3[1] deletable, sig-order pin confirmed keep), honest delete range 34-42 pending Phase-2 per-pin sig audit, ~15% reduction floor. Four spec-text issues to fix: §3.1 census-intact assertion underspec (bare strings vs (uri,name)), §3.4 thereExistsAProcedure not harvester-callable (must name Phase-1 path), §5 sig-audit as pre-computed artifact not conflated with deletion commit, §5 scratch-test naming collision under go test -run. No doctrine/scope disputes remain. Crossing #2 this run: carmack-3's v2.1 (33dc7cc, lead-ruling items only — §6 gqlc-exl cite + §9 Q3 retirement) crossed the REVISE; both sides disambiguated, v3 with the four fixes ordered before re-grill. Also relayed the golangci-lint sibling-worktree cache-leak fix (stale ls8-2 ghost warning) and folded carmack-3's §9 Q3 consolidation idea into gqlc-exl instead of a new bead.","created_at":"2026-07-15T00:43:01Z"},{"id":"019f6346-c9f6-7ad8-bd9d-5c644b35746e","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Spec round 3 (crossing storm): linus-3 issued v3 PASS at 86c0f27, then flagged A (honest ~11-12% reduction) + B (permanent golden-twin invariant) as misattributed to him, then fully retracted — both ARE his round-1 lead-ratified bullets dropped from his context (exact failure mode of the round1-bullets-outlive-REVISE-deltas rule) — and flipped v3 to REVISE prescribing A+B for a single v4. Meanwhile carmack-3 had already pushed v4 (3328fec, docs-only +62/-23) folding A+B, acting on linus-3's crossed supersede message rather than the formal verdict; sequencing norm restated (hold push until the formal verdict lands), no rework — content matches the lead order. Residual mechanism delta disambiguated to both sides: v4 implements B as a positive trimmed keeper-list (catches keeper losing its golden twin); linus-3's REVISE prescribed a negative-invariant repurpose (catches delete-set re-forming under future TCK bumps). Ruled: the ratified bullet mandates permanence, not mechanism — linus-3 to issue ONE verdict on 3328fec choosing positive-only / negative-only / both on failure-mode merits, as a fresh finding not defiance. Gates green on v4 per carmack-3 (fmt-check, test, lint 0 issues).","created_at":"2026-07-15T00:56:51Z"},{"id":"019f6348-ba46-77f8-8108-dfe89bcab8a2","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Spec round 4: linus-3's single verdict on v4 (3328fec) = REVISE narrow, text-consistency only. B closed cleanly — reviewer accepted the positive trimmed keeper-list over his own negative-invariant suggestion (simpler, same hazard coverage); §3.1/§4/§5 permanence wiring verified. A landed in §6 (~11-12% with arithmetic) but three stale ~15% sites contradict it: §2 line 226 arithmetic block, §2 line 231 'the 15% is the honest number', §9 Q1 line 750 '~15% floor'. Fix ordered: one stacked v4.1 (new commit, no amend, ~15 lines, no design changes), full fmt gate, push, then final PASS check on the v4.1 tip. Also this round: late-arriving stale deliveries of linus-3's v2-era messages (the four-item v2 REVISE + a v2.2 fold proposal) hit carmack-3's inbox AFTER the v4 verdict; carmack-3 correctly identified them as stale against HEAD via git-show reads and held; lead marked them VOID with explicit event labels. carmack-3's no-force-push/no-amend handling of the premature v4 push ratified. On v4.1 PASS: spec merge gate, then Phase 1 opens.","created_at":"2026-07-15T00:58:58Z"},{"id":"019f6349-db6e-7085-b26d-a2dc2f5b1787","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Round 4 correction: the 'REVISE narrow' verdict logged above was superseded minutes later by linus-3's consolidated verdict on 3328fec after the lead's B-mechanism delta reached him. Operative scope for the next revision (labeled v5, single label decreed): (1) B = BOTH invariants in one test body — positive trimmed keeper-list + negative walk over mustParse (~45 lines); carmack-3 had independently proposed the same both-mechanisms design, so adoption expected without a merits round; (2) A = three-site ~15% -\u003e ~11-12% sweep (§2 L226, §2 L231, §9 Q1 L750); (3) §3.4 L391 census-helper vestige -\u003e TestMustParseGoldenTwins. One stacked revision on 3328fec, fmt gate, push. Also voided a third stale delivery (linus-3's original v3 PASS surfacing late in carmack-3's inbox). linus-3 self-flagged his earlier positive-only endorsement as a hand-wave, redid the hazard analysis — reviewer discipline holding. Next: v5 sha -\u003e linus-3 verdict -\u003e on PASS spec merge gate + Phase 1 opens.","created_at":"2026-07-15T01:00:13Z"},{"id":"019f634f-11fe-7a9e-a588-14d00376f0d8","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Spec round 5: carmack-3 shipped v4.1 (00fae8b, +15/-12) acting on the lead's earlier SUPERSEDED three-edit order — delivery lag meant he never processed the consolidated-verdict brief. Lead verified committed content: A-sweep complete and high quality (§2 arithmetic aligned to §6; §9 Q1 rewritten as range/midpoint-not-floor; surviving ~15% mentions L233/L592 correctly historical); B both-mechanisms absent (zero negative-walk text) and §3.4 census vestige survives at L392. Residual ordered as ONE commit labeled v5 stacked on 00fae8b: (1) B = positive keeper-list + negative mustParse walk in one test body (~45 lines), (2) L392 vestige -\u003e TestMustParseGoldenTwins. linus-3 told to hold verdict for the v5 tip. Also this round: carmack-3 disclosed two wrong-role messages sent while mis-identifying as team-lead (an 'attribution reversal' to linus-3 + a self-addressed hold notice — explains the anomalous idle summary the lead had flagged); immediate retraction sent, ratified, no re-litigation. Lead's interim 'deadlock break' wake was based on that anomalous summary — premise corrected by the disclosure. Spec history now v1..v3, v4, v4.1, v5 pending.","created_at":"2026-07-15T01:05:54Z"},{"id":"019f6e02-f472-7882-8ff5-0ac9f1cb98cc","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Spec track CLOSED: linus-3 issued full PASS on v5 (f82843a) with per-item receipts via git-show verification — dual-invariant §3.1 (positive keeper walk + negative mustParse walk, one body, shared harvester), §3.4 vestige fixed (landed L423, carmack's L392 report was a benign line-number drift the reviewer caught), ~10-12% honest range consistent across §2/§6/§9 with endpoints 9.2%/12.1% named and 'not a floor' framing; all five round-1 ratified reviewer bullets verified CLOSED; scope discipline confirmed (no collateral drift outside touched sections). Spec history: v1 3538a90 / v2 9b192b8 / v2.1 33dc7cc / v3 86c0f27 / v4 3328fec / v4.1 00fae8b / v5 f82843a. Lead ratified PASS and authorized Phase 1 (dual-invariant test + harvestExecutingScenarios + TestPropertyReadCoreParses rewrite + sigaudit scratch pair under build tag + committed ledger; one gated push). Session halted before Phase 1 started (tip still f82843a on origin, verified 2026-07-16); resuming with full re-brief to carmack-3 assuming teammate compaction.","created_at":"2026-07-17T02:58:35Z"},{"id":"019f6e11-c00d-7a26-8a85-e69ae2ced904","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Phase 1 shipped: 5ccd1de (test-only, +519/-15, 5 files). TestMustParseGoldenTwins dual-invariant + goldenTwinKeepers (34 keys), harvestExecutingScenarios/scenarioMeta in acceptance_test.go, TestPropertyReadCoreParses rewritten corpus-backed per §3.4, scratch census_test.go+sigaudit_test.go under sigaudit_scratch build tag, ledger committed (9 rows: 8 equal + 1 divergent = the named sig-order keep; Phase 2 N=8). Census reproduction matches spec: 3805 unique corpus queries / 3131 with goldens / 159 -\u003e 43/4/112. Lead verified independently: origin sync, zero non-test parser sources touched, goldens byte-identical to master, just test green. Two disclosed deviations routed to linus-3's Phase 1 grill: (1) negative-walk exemption for ALL sig-carrying pins — resolves the spec's red-interval contradiction at HEAD but may be too broad as a permanent invariant (future duplicative pin WITH Background-equal sigs would evade the walk); alternatives: atomic Phase-2 drain, or narrow exemption to ledger-divergent survivors; (2) §7 ledger fence phrasing (-v redirect interleaves go-test framing; implementation writes the file directly) — docs tidy candidate. linus-3 re-briefed self-contained post-halt; his verdict gates Phase 2.","created_at":"2026-07-17T03:14:45Z"},{"id":"019f6e1c-1a36-7c43-b3db-8371a0170ca8","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"Phase 1 verdict: PASS on merits at 5ccd1de, zero rework; one forward-facing REVISE as Phase-2 acceptance criterion — narrow the negative-walk exemption from len(sigs)\u003e0 to an explicit sigDivergentKeepers allowlist (one entry: the sig-order divergent pin), closing the post-Phase-3 bypass hole where a future Background-equal sig-carrying duplicate would evade both the retired sig-audit and the exempted walk. All five carmack design notes ACCEPTED (Phase-1 exemption breadth, os.Create ledger, positive\u0026\u0026!negative polarity filter, ~65-line body vs ~45 budget absorbed in the ±1% range, loose golden-existence). Ledger locks Phase 2 delete set at 42 (34 sig-less + 8 ledger-equal; best case realized). Q2 ruled: §7 fence phrasing correction rides with Phase 3 (ledger+scratch retire there anyway). Ledger-path fragility accepted (scratch lifetime). Reviewer's three-place rule-1 check held. Phase 2 authorized: 42 deletions with per-pin ledger receipts in the body, positive-list drain, divergent keeper in-file comment, REVISE-1 tightening, suite green, TCK+golden fences unchanged.","created_at":"2026-07-17T03:26:03Z"},{"id":"019f6e2b-c24b-7a26-a322-91a1bdc373d6","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"PHASE 2 SHIPPED + PASSED + RATIFIED (2026-07-17).\n\nShipment: f7f6ffe (parser_test.go only, +44/-871; file 4,296 -\u003e 3,469 lines; mustParse 159 -\u003e 117). 42 pins deleted reading the committed ledger (34 sig-less + 8 sig-equal); per-pin receipts (\u003ckey\u003e -\u003e goldenPath) in the change body; divergent keeper retained with in-file comment quoting the ledger reason. REVISE-1 tightening applied: len(pin.sigs)\u003e0 exemption replaced with closed sigDivergentKeepers allowlist (single entry: authored CALL standalone Returns signature-declaration-order).\n\nlinus-3 verdict: PASS, no REVISE items. Receipts: bidirectional set-difference proof (his independently computed delete set vs shipped = empty both directions); all 42 receipts matched ledger -\u003e body -\u003e on-disk golden; REVISE-1 verified line-by-line; golden byte-identity fence vs master holds; scope discipline (single file changed); round-1 ratified bullets all honored. Design-note rulings: (1) Phase 3 repopulates goldenTwinKeepers with the one divergent survivor AND keeps sigDivergentKeepers (lists logically distinct: positive walk catches scenario DROPS, negative walk catches shape-mirror ADDS); (2) error-message phrasing accepted; (3) scratch receipt tool discarded, not preserved.\n\nSide item closed: transient receipts_test.go LSP sighting written off as stale snapshot — carmack-3 provided find/diff-filter=A/fsck/reflog receipts showing no in-tree scratch ever existed.\n\nPHASE 3 AUTHORIZED (single final change): (a) preamble L15-42 rewrite; (b) retire scratch pair + ledger; (c) keeper repopulation per ruling; (d) Q2 §7 fence-phrasing fix; (e) lead directive — restate §6/§2/§9 with post-hoc actuals labeled outturn vs estimate (actuals beat estimate: ~20.7 lines/pin vs ~13-15; 3,469 already below §6 projected floor 3,649); (f) receipt tool discarded. Then whole-diff linus PASS -\u003e single squash+auto-merge PR (Closes #285).","created_at":"2026-07-17T03:43:09Z"},{"id":"019f6e3b-af2b-7e0b-b9d1-fdedd81aca05","issue_id":"gqlc-ls8.5","author":"antranig-yeretzian","text":"PHASE 3 SHIPPED + COMBINED PASS + PR OPEN (2026-07-17).\n\nShipment: f33296a — single final change per authorization, all six scope items landed: (a) Layer-2 preamble rewrite (Class A/B/C taxonomy + cluster carve-outs, dual-invariant tie-back); (b) scratch pair + ledger retired, zero sigaudit_scratch files remain; (c) goldenTwinKeepers repopulated with the sole divergent survivor, sigDivergentKeepers retained — both lists carry the same key, logically distinct per linus design-note-1; (d) §7 Q2 fence phrasing corrected verbatim (no -v redirect, \u0026\u0026 cat ledger form), reframed historical; (e) §2/§6/§9 restated with outturn tables — 4,151 -\u003e 3,528 lines, -623 net, ~15.0% (beats the v4 ~10-12% honest range; drivers: best-case N=8 and ~20.7 lines/pin vs ~13-15 est.), estimates preserved as retracted per honest-numbers doctrine; (f) receipt tool discarded. §3.1 tense flag ruled leave-as-is (plan sections keep plan tense; counts were accurate).\n\nlinus-3 combined verdict: Phase 3 PASS + WHOLE-DIFF PASS (master...f33296a), no REVISE items, all five round-1 ratified reviewer bullets closed, reviewer chair signed off. Whole-diff surface: 3 files (spec added, acceptance_test.go harvester, parser_test.go); goldens byte-identical; zero non-test parser sources touched; per-phase line deltas independently re-derived (+145 / -827 / +59 = -623); mustParse residual 117 at both P2 and P3 tips.\n\nMerge gate: PR #308 opened by lead (squash + auto-merge armed 03:59:40Z, Closes #285). On merge: close this bead, beads-export PR, epic gqlc-ls8 completes 5/5, close GH umbrella #290 manually, teardown team ls8-5. carmack-3 released to log the §9 Q3 by-stage consolidation detail on gqlc-exl.","created_at":"2026-07-17T04:00:33Z"}],"dependency_count":1,"dependent_count":1,"comment_count":12}
{"_type":"issue","id":"gqlc-ls8.4","title":"cli: extract the generate pipeline (config file -\u003e []File) from runGenerate","description":"internal/cli/generate.go (334 lines). The whole pipeline run — config load, schema parse, query-file discovery/split, parse, resolve, generate — is embedded in the cobra command (runGenerate:66, frontEndWalk:207, writeOutput:249), so its interface is 'a filesystem in the right shape'; all CLI tests (974 lines) pay tmpdir setup to reach any pipeline behavior. ADR 0010 D1 already assigns pipeline orchestration to the CLI bead ('tests wire it inline, the CLI owns it in production') — this names the module that decision implied. Explicit NON-GOAL: no parser registry/factory for the tool axes — each axis has one adapter today (gqlc; openCypher), one adapter is a hypothetical seam, the hardcoded pick at :87/:118 stays honest.","design":"Extract a pipeline module (location team's choice, e.g. internal/pipeline or internal/cli/pipeline): config file in, []codegen.File + diagnostics out; the cobra command keeps exactly its ADR 0012 role — flags, TTY, writeOutput with the all-marked tripwire. Two adapters justify the seam: the command in production, in-memory tests. Diagnostics formatting separates from pipeline traversal. Team decides the concrete interface in a docs/specs/ design doc, grilled by Linus BEFORE code.","acceptance_criteria":"Existing CLI tests green (may be simplified where they only exercised pipeline logic through tmpdirs, but coverage must not shrink). New pipeline tests run in memory or with minimal fixture dirs — no cobra, no stdout capture. gqlc generate behavior unchanged end-to-end (generate_test.go fixtures byte-identical output). ADR 0012 tripwire behavior unchanged and still CLI-owned. just test / fmt-check / lint green.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-13T03:15:39Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:13Z","started_at":"2026-07-13T03:18:49Z","closed_at":"2026-07-13T22:16:15Z","close_reason":"Shipped in PR #279 (squash-merged 2026-07-13). internal/cli/pipeline extracted: Run(cfgPath)-\u003e(Result,error), in-memory testable; CLI keeps flags/tripwire/writes per ADR 0012; generate.go 334-\u003e199 lines. Linus PASS verified; gates green; generate_test.go byte-identical.","external_ref":"https://github.com/areqag/gqlc/issues/286","dependencies":[{"issue_id":"gqlc-ls8.4","depends_on_id":"gqlc-ls8","type":"parent-child","created_at":"2026-07-12T23:15:38Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ls8.3","title":"codegen: prepare commits every render decision; render stops re-deriving resolver types","description":"internal/codegen/{prepare.go (847), types.go, render_queries.go (853), render_models.go}. The prepare/render seam exists in name (preparedQuery) but decisions leak across it: prepare switches on resolver.ResolvedType (18 cases), types.go carries 24 more, and render_queries.go re-switches at emission time (10 cases, :592-663) plus references resolver.StatementWrite directly (:294). The EdgeKey canonical-order discipline between prepare.go and render_queries.go is coupled by comment (spec §5.5), not structure. Failure mode: a new ResolvedType variant compiles through default: arms and renders silently wrong generated code — the scariest failure class in the repo since generated output is the product (ADR 0010).","design":"Deepen prepare: it commits every rendering decision (column kind, Go type string, decode strategy, required imports) into preparedQuery; render_*.go walks prepared data only and never touches a resolver type again — ADR 0010's 'load-bearing table' (D3) gets one home, and an unhandled ResolvedType becomes one loud sentinel in prepare instead of silent wrong output. Team decides the concrete preparedQuery shape in a docs/specs/ design doc, grilled by Linus BEFORE code. Generate's exported API and the []File contract (ADR 0010 D4) must not move.","acceptance_criteria":"All 67 valid + 24 invalid codegen goldens byte-identical (no -update). Double-run determinism test green. Nested-module compile fence (go build \u0026\u0026 go vet in test/data/codegen) green. just test / fmt-check / lint green. render_*.go contains zero type-switches on resolver.ResolvedType and zero references to resolver enums (grep-verifiable). New unit tests pin the prepare mapping table (ResolvedType -\u003e committed decision), including the unrepresentable-width sentinels.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-13T03:15:12Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:14Z","started_at":"2026-07-13T03:18:49Z","closed_at":"2026-07-13T22:21:16Z","close_reason":"Shipped in PR #280 (squash-merged 2026-07-13). Prepare commits every render decision: preparedListElem plans via closed columnKind enum, accessMode enum, IsWrite; render_*.go has zero resolver.* references (pinned by TestRenderBoundaryNoResolverRef); 43-row mapping-table tests; 67+24 goldens byte-identical. Linus PASS verified, all fences run independently.","external_ref":"https://github.com/areqag/gqlc/issues/287","dependencies":[{"issue_id":"gqlc-ls8.3","depends_on_id":"gqlc-ls8","type":"parent-child","created_at":"2026-07-12T23:15:12Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ls8.2","title":"cypher parser: one collection sink replaces 14 per-handler subqueryDepth guards","description":"internal/query/cypher/listener.go (+expr.go, pattern.go, call.go). The invariant 'bindings/refs/effects inside EXISTS { ... } never enter the outer query part' is enforced by 14 separate 'if l.subqueryDepth \u003e 0' guards (listener.go:237,254,273,294,369,394,494,531,556,576,589,604,621,678) — every present and future Enter* handler must remember the guard, and a forgotten one produces a phantom binding the resolver types with the wrong scope (the exact failure ADR 0003's Stage-11 note names). ADR 0008's amendment history shows the cost: the 33k.3 EXISTS-param precision fix needed three sweep passes plus an idempotence guard.","design":"Deepen the mutable listener state into one collection-sink module (a scope stack): handlers write bindings/returns/effects/params unconditionally; the sink routes to the outer part or discards, so suppression is decided at one seam instead of fourteen call sites. This preserves ADR 0003 Stage-11's invariant and its chosen posture (structural enforcement, not name-collision rules) — it concentrates enforcement, it does not re-litigate. Team decides the concrete interface in a docs/specs/ design doc, grilled by Linus BEFORE code. Wire shape must not move.","acceptance_criteria":"All 3,199 parser goldens byte-identical (no -update). TCK acceptance suite: same pass/pending counts, zero failed. Referential-integrity and binding-uniqueness property tests green. Sentinel reachability sweep green. just test / fmt-check / lint green. cypher.New / Parse exported API unchanged. Zero remaining 'subqueryDepth \u003e 0' guards in Enter* handlers (the counter may survive inside the sink).","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-13T03:14:53Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T23:28:17Z","started_at":"2026-07-13T03:18:48Z","closed_at":"2026-07-14T23:28:17Z","close_reason":"Shipped in PR #305 (squash 119365c, merged 2026-07-14T23:26:53Z). 23-commit spec-first stack (docs/specs/cypher-collection-sink.md Revs 1-5) + merge-update 832f151. linus-2 whole-diff PASS at 3a12686 carried to merged tip with first-party §7.6 confirm. Acceptance held: 0 Enter* subqueryDepth guards (2 sanctioned carve-outs), goldens 0-diff, TCK parity 3897 scenarios (3459 passed, 438 pending) zero failed, property+sentinel suites green, sink AST write-fence green, exported API unchanged. Polish follow-up: gqlc-kww.","external_ref":"https://github.com/areqag/gqlc/issues/288","dependencies":[{"issue_id":"gqlc-ls8.2","depends_on_id":"gqlc-ls8","type":"parent-child","created_at":"2026-07-12T23:14:52Z","created_by":"antranig-yeretzian","metadata":"{}"}],"comments":[{"id":"019f5ea1-a908-7929-8cd0-e3a1fc7f4085","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C commit 4 (EnterOC_With) halted on parity break: TestReadCoreAcceptance/[2]_Full_existential_subquery_with_aggregation — inner-WITH projection items leaked into outer part under EXISTS suppression. Root cause: collectProjection/collectReturnItem direct l.curPart.* writes (expr.go:36/42/209) bypass Phase-A sinks. linus-2 ruled Option A: expand commit-4 scope to migrate the callee in the same commit, per spec §5 Phase C line 837 ('Callees migrate in the same commit as the caller that reaches them'). carmack-2's original 'mechanical diff, no design decisions' claim for commit 4 was a mis-scoping on his part, not a spec gap. Option B (gate helper entry, keep direct writes) rejected — would fail Phase E forbidigo gate. Option C (Rev-4 spec revision) rejected — spec already covers it. Expanded commit subject: 'refactor(cypher): route EnterOC_With and collectProjection through collection sink'.","created_at":"2026-07-14T03:18:01Z"},{"id":"019f60a1-e5f4-7a29-a370-f0f614bcd5d2","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-5 (97bc044, EnterOC_Return guard-drop + 1 leak pin) PASSed by linus-2 with independent red-first re-verification. Return closes the collectProjection-family class. Stack on origin/cypher-collection-sink = 97bc044 (11 commits ahead of master). Remaining Phase C: Unwind (expr.go:124), Set/Delete/Remove/Create/Merge (expr.go:750/768/779/806/822), Call handlers; then Phases D/E/F. Side finding this cycle: master-guard hook false positive/negative filed as gqlc-2r4 with interim -C-form allowance.","created_at":"2026-07-14T12:37:31Z"},{"id":"019f60ad-26da-7c4a-9e59-41e5724e1008","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-6 (15a19a3, EnterOC_Unwind guard-drop + dual sink migration expr.go:125 refs / :139 unwindBindings + 1 dual-class pin) PASSed by linus-2 with independent dual-endpoint red-first re-verification. Stack on origin = 15a19a3 (12 ahead of master). Remaining Phase C: Set/Delete/Remove/Create/Merge (expr.go:750/768/779/806/822), then Call handlers; then Phases D/E/F. carmack-2 proceeding to commit-7 (Set) under standing order.","created_at":"2026-07-14T12:49:48Z"},{"id":"019f60bb-d008-75e6-b548-8e73a639c2f5","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-7 (d8e535c, EnterOC_Set guard-drop, three sink arms + writeSeen/appendEffect now in use, 1 pin, red-first three probe endpoints) PASSed by linus-2; commit-8 (Delete) authorized under the standing PASS-authorizes-next rule. Stack on origin = d8e535c (13 ahead of master). Remaining: Delete/Remove/Create/Merge + Call handlers, then D/E/F. Note: pair self-managed two message crossings this cycle without lead intervention.","created_at":"2026-07-14T13:05:49Z"},{"id":"019f60c2-5566-734d-a6a2-6f46ea940c54","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-8 (4fb8f58, EnterOC_Delete guard-drop + pin; listener.go-only migration) PASSed by linus-2; commit-9 (Remove) authorized under standing rule. Stack on origin = 4fb8f58 (14 ahead of master). Remaining: Remove/Create/Merge + Call handlers, then D/E/F checkpoint.","created_at":"2026-07-14T13:12:56Z"},{"id":"019f60c9-e770-7cc2-bd3b-738623175717","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-9 (9a26d95, EnterOC_Remove guard-drop + pin) PASSed by linus-2; commit-10 (Create) authorized. expr.go now has ZERO un-gated curPart.refs appends (verified by lead grep against pushed tip). Stack on origin = 9a26d95 (15 ahead of master). Remaining: Create/Merge + Call handlers, then D/E/F checkpoint.","created_at":"2026-07-14T13:21:13Z"},{"id":"019f60d0-ae53-782c-b574-1c898fc0f3d7","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-10 (bb0e5f4, EnterOC_Create guard-drop + pin, pattern subtree pre-gated) PASSed by linus-2; commit-11 (Merge) authorized. Stack on origin = bb0e5f4 (16 ahead of master). Remaining: Merge + InQueryCall/StandaloneCall, then D/E/F checkpoint.","created_at":"2026-07-14T13:28:37Z"},{"id":"019f60df-7318-7199-a6c2-efc2ce27fb19","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C progress: commit-11 (10a202c, EnterOC_Merge guard-drop + pin) PASSed by linus-2 — all write-clause handlers drained, sink classes at zero per carmack-2. commit-12 (InQueryCall) authorized. Stack on origin = 10a202c (17 ahead of master). Remaining: InQueryCall + StandaloneCall, then the Phase D/E/F lead checkpoint. 4 subqueryDepth occurrences left in listener.go at tip (2 Call guards + suppressed() seam).","created_at":"2026-07-14T13:44:45Z"},{"id":"019f60f1-1485-74e2-985a-771023fbecd3","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase C/D interleave: commit-12 (1182448, InQueryCall + callBindings sink) PASSed by linus-2 WITH PREREQUISITE — Phase D l.fail gate must land before commit-13. carmack-2 delivered commit-12.5 (7af1e6a, fail-gate + 8-entry §4.2 Test A parity table; body honestly owns the retroactive latent-window close). Lead ratified the hoist (B.5 precedent) and required a spec Rev 4 stanza documenting the reorder before/with commit-13. Stack on origin = 7af1e6a (19 ahead). Remaining: Rev 4 stanza + StandaloneCall, then lead checkpoint covering residual D verification + E/F.","created_at":"2026-07-14T14:04:00Z"},{"id":"019f60f1-5fd1-7620-9a3b-182d2e693a3d","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"commit-12.5 (7af1e6a, Phase D fail-gate) PASSed by linus-2 with RED baseline reproduced. commit-13 (StandaloneCall, final Phase C handler) authorized; Rev 4 spec stanza requirement outstanding (lands before/with commit-13). Lead checkpoint follows its PASS.","created_at":"2026-07-14T14:04:19Z"},{"id":"019f60fb-d4b7-7a29-b499-e089aeba48d8","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"commit-12.5 (7af1e6a) PASS details: linus-2 flipped the fail-gate and reproduced 8/8 parity-table REDs with named sentinels, goldens byte-identical across all 19 commits, SyntaxError pre-walk ordering invariant verified. Rev-4 gate HELD: spec at HEAD ends at Rev 3; commit-13 will not clear review until the Rev-4 stanza lands (own docs commit or folded). Non-blocking nit for later: 'path vs unwind kind conflict' pin comment attributes wrong sentinel (names ErrVariableKindConflict sites; RED actually surfaces 'path binding requires at least one member'). Watch item: carmack-2 sent two consecutive self-addressed messages (role-confusion signature); role-check nudge delivered 14:04.","created_at":"2026-07-14T14:15:45Z"},{"id":"019f6100-0c10-7336-b5d5-f0cb9be5cc35","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"PHASE C COMPLETE at 2f08d90 (21 ahead of master): Rev-4 stanza (1bcaa30) + commit-13 StandaloneCall (2f08d90) both PASSed by linus-2 with first-party verification (grammar-reachability vs Cypher.g4, RED-first sink stub, guard census = exactly 2 sanctioned carve-outs: suppressed() seam listener.go:224 + Exit balance decrement :828; zero Enter* guards). Lead checkpoint ruling: Option 1 — Phase E (forbidigo, commit-14) in-stack with RED-analogue receipts, two accepted nits (pin sentinel comment, spec §3.2 stale example) land before Phase F, then Phase F acceptance sweep + linus-2 whole-diff PASS = merge authority; lead opens single PR, squash+auto-merge.","created_at":"2026-07-14T14:20:21Z"},{"id":"019f6104-a9e9-7140-8f8c-54d3b502fa6b","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase E escalation + ruling: forbidigo is identifier-chain-based (flags reads AND writes) — spec-literal §4.3 would cost 45 nolints (25 on l.err read idiom), carmack-2 measured 45 hits / 7 writes / 38 reads across the six fields. Lead ruled Option 4 (mechanism swap): AST-based write-fence test in package cypher (AssignStmt/IncDecStmt on the six §4.3 fields must be inside the sink whitelist), zero nolints, no forbidigo config; intent preserved and enforcement stronger (l.err writes still fenced). RED-first both directions required; Rev-5 spec stanza documents the swap. Bounded fallback to narrowed forbidigo (his Option 2) without re-checkpoint if genuine blocker. Fence-test approach is house-consistent with the existing codegen-fence pattern.","created_at":"2026-07-14T14:25:23Z"},{"id":"019f6111-3969-722a-b9e4-6fd21b182b49","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"Phase E COMPLETE: commit-14 (8605ab7, AST write-fence test sink_fence_test.go 199 lines + Rev-5 stanza + both accepted nit fixes) PASSed by linus-2; Phase F (acceptance sweep + final whole-diff review) authorized. Stack on origin = 8605ab7 (22 ahead of master).","created_at":"2026-07-14T14:39:07Z"},{"id":"019f611d-4b2f-79ec-85e5-e92985bc474a","issue_id":"gqlc-ls8.2","author":"antranig-yeretzian","text":"WHOLE-DIFF PASS from linus-2 at 3a12686 (23 commits, true diff 295165e..3a12686: 8 files +2006/-112, scope = internal/query/cypher + owning spec only). All §7 fences re-verified first-party by reviewer. Merge authority granted. Lead ruling on the base-drift caveat: merge-update (origin/master ddc395d INTO branch), NOT rebase — force-push ban; squash-merge erases the merge commit. carmack-2 dispatched for merge + full gates + fences on merged tip; linus-2 one-fence confirm follows; then lead opens single squash+auto-merge PR. Polish items (listener.go:222 stale docstring, sink_fence_test.go:153 hard-coded prose list) filed as separate bead.","created_at":"2026-07-14T14:52:18Z"}],"dependency_count":0,"dependent_count":1,"comment_count":15}
{"_type":"issue","id":"gqlc-ls8.1","title":"resolver: deepen the carried scope — branchState becomes a scope module","description":"internal/resolver/resolve.go (1,977 lines). The carried scope — what a query part knows about each binding across a WITH — is one concept scattered over ten parallel maps in branchState (resolve.go:88: exportedNodeTypes, exportedEdgeTypes, exportedEdgeKeys, exportedEdgeCands, exportedEdgeBindings, exportedNullableBinding, exportedOptionalGroup, exportedResolvedTypes, exportedCallTypes, exportedOrder). Internal helper interfaces are as wide as the state they thread: exportScope (:524) takes 12 parameters; projectionType (:1181) and refProjectionType (:1206) take 10 each; demoteNullableInPlace (:1427) mutates the nullable map as a buried side effect. Deletion test fails: removing any one map requires edits at 20+ call sites. The filed ay9 close-out residual (cross-Part carry is name-granular, does not carry group ids) needs exactly a branchState extension — expensive under this shape, trivial after.","design":"Deepen the scope into its own module (likely its own file, possibly own type with methods): one type owning the per-variable resolved facts behind lookup / carry-forward / demote operations, so helpers take one scope value and the ten maps become private implementation. The demotion fixed-point becomes a method testable directly. Team decides the concrete interface in a docs/specs/ design doc, grilled by Linus BEFORE code. Pure internal refactor: resolver.New / Resolve / ValidatedQuery are pinned by ADR 0008 and must not move.","acceptance_criteria":"All 136 resolver golden pairs byte-identical (no -update). Sentinel reachability sweep green. just test / fmt-check / lint green. Resolve's exported API unchanged. New focused unit tests on the scope module (at minimum: carry-forward and demotion fixed-point). Consider closing or advancing the ay9 residual (group-id carry across Parts) as a stacked follow-up cycle if cheap after the refactor — do not gold-plate it in.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-13T03:14:37Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:15Z","started_at":"2026-07-13T03:18:47Z","closed_at":"2026-07-13T22:48:53Z","close_reason":"Shipped in PR #282 (squash-merged 2026-07-13). branchState deepened into scope module: 11 phase-owning methods, resolvePart reduced to orchestration; exportScope/projection param-shovels eliminated; 136 goldens byte-identical; Resolve API unchanged (ADR 0008). Round-1 REVISE (CloseEdges split) reworked to spec-pinned single method. Non-blocking observations deferred without beads (goldens pin behavior): Phase-E per-variant validators read receiver fields directly; BindEdge R5 §6.4 edge-parity not exhaustively verified outside the 136-fixture set.","external_ref":"https://github.com/areqag/gqlc/issues/289","dependencies":[{"issue_id":"gqlc-ls8.1","depends_on_id":"gqlc-ls8","type":"parent-child","created_at":"2026-07-12T23:14:37Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ls8","title":"Architecture deepening: five module-depth refactors from the 2026-07-12 review","description":"Epic for the five deepening candidates surfaced by the /improve-codebase-architecture review (2026-07-12). Each child is one candidate, run as its own gt branch + fresh Carmack/Linus team, spec-first (docs/specs/ design doc grilled by Linus before code), TDD, goldens as refactor fence. Children 1-4 run in parallel (disjoint packages); child 5 is blocked by child 2 (both touch internal/query/cypher). All are behavior-preserving refactors: existing golden suites must stay green (byte-identical where the brief says so). No ADR conflicts: ADR 0008 pins only Resolve's interface; ADR 0009/0010 leave stage internals free; ADR 0003 Stage-11 invariant preserved by child 2.","status":"closed","priority":2,"issue_type":"epic","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-13T03:14:01Z","created_by":"antranig-yeretzian","updated_at":"2026-07-17T04:01:41Z","closed_at":"2026-07-17T04:01:41Z","close_reason":"All five deepening candidates from the 2026-07-12 architecture review shipped: ls8.1 resolver scope module (#255), ls8.2 cypher collection sink (#272), ls8.3 codegen prepare/render split (#266), ls8.4 cli generate pipeline (#274 stack), ls8.5 golden-corpus test migration (#308). Each ran spec-first with fresh Carmack/Linus adversarial team, behavior-preserving, goldens green throughout. Umbrella GH #290 closed manually per pr-body-closes-gh-issue practice.","external_ref":"https://github.com/areqag/gqlc/issues/290","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-m1c.3","title":"CLI-2: gqlc init — interactive huh wizard for gqlc.yaml","description":"Implement gqlc init with charm.land/huh/v2 (ADR 0011; dep enters go.mod here). TTY-only: guard with term.IsTerminal, fail cleanly otherwise (no flag matrix in v1); wire huh accessible mode (ACCESSIBLE env). Flows: (1) no config at target path (default ./gqlc.yaml, -f/--file to override): fresh wizard from sensible defaults; (2) existing loadable config: prefill every field via Value(\u0026v) binding — validate stored enum values against the *Values() vocabularies before binding (huh Select silently lands on index 0 on mismatch); (3) existing broken config: print the loader's error (line info + vocabularies) and offer fresh-start overwrite or abort — no salvage decoder. Prompts derive from config.SchemaLangValues/QueryLangValues/DriverValues; path inputs and package name validate inline (non-empty; token.IsIdentifier). Config-file-only writes: soft-warn when schema/queries paths don't exist, never create them; print a next-steps epilogue. Final step: preview the exact canonical file Save will write and confirm; when the existing file contains comments, state they will not survive the rewrite. Old-version files migrate naturally (Load normalises, Save writes latest).","status":"closed","priority":2,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T21:55:45Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:17Z","started_at":"2026-07-12T23:20:02Z","closed_at":"2026-07-13T02:26:15Z","close_reason":"Shipped in PR #277 (spec #276). gqlc init: TTY-guarded three-flow huh/v2 wizard, config.Canonical() widening, generate missing-config reword, README install+workflow section. Linus PASS, all CI gates green, golden fence intact.","external_ref":"https://github.com/areqag/gqlc/issues/291","dependencies":[{"issue_id":"gqlc-m1c.3","depends_on_id":"gqlc-m1c","type":"parent-child","created_at":"2026-07-12T17:55:45Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-m1c.3","depends_on_id":"gqlc-m1c.2","type":"blocks","created_at":"2026-07-12T17:55:51Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-m1c.2","title":"CLI-1: gqlc generate — config-driven pipeline orchestration","description":"Implement gqlc generate: load config from ./gqlc.yaml or -f/--file (config.Load; version seam handles old versions; missing file = clear error pointing at gqlc init); resolve config-relative paths against the config file's directory (spec'd format semantics the CLI owns); parse schema (schema_language axis); load procsig registry when the procsig key is set — this supersedes gqlc-aqb's flag approach; split every query file in the queries dir via queryfile.Parse; per named query run parse -\u003e resolve, ACCUMULATING failures across queries (print all with file + query context, exit 1, write nothing); on full success call codegen.Generate (WithDriverVersion mapped from the config driver axis) and write the file set under ADR 0012 semantics: wipe + rewrite only when the output dir is absent, empty, or all files bear the generated-code marker; otherwise abort naming offenders — never delete an unmarked file. Spec must pin: query-file discovery rule (extension / ordering for determinism), exit codes, error text shapes, and the write-after-full-generation ordering (codegen purity makes a failed run wipe nothing).","status":"closed","priority":2,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T21:55:44Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:17Z","started_at":"2026-07-12T22:26:12Z","closed_at":"2026-07-12T23:19:34Z","close_reason":"Shipped in PR #274 (spec #273). gqlc generate: config-driven 9-stage pipeline, per-query error accumulation, ADR 0012 tripwire output protocol, codegen.WithPackageName widening, internal/logger deleted. Linus PASS, all CI gates green, golden fence intact.","external_ref":"https://github.com/areqag/gqlc/issues/292","dependencies":[{"issue_id":"gqlc-m1c.2","depends_on_id":"gqlc-m1c","type":"parent-child","created_at":"2026-07-12T17:55:44Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-m1c.2","depends_on_id":"gqlc-m1c.1","type":"blocks","created_at":"2026-07-12T17:55:50Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-m1c.1","title":"CLI-0: cmd/gqlc skeleton — cobra root + version command","description":"Create cmd/gqlc/main.go (thin main calling the cobra root), delete the root main.go dev harness, add spf13/cobra to go.mod (ADR 0011). Commands: bare root (help), version. Reconcile the version variable: internal/codegen carries var version = \"dev\" (-ldflags -X, C6) for the generated-file header; the CLI version command must print the same value — likely extract a shared internal package so one -X flag serves both (spec decides the seam). Update any just build/install recipes and CI touchpoints for the new entrypoint path (CI/just parity rule). Acceptance: go build ./... green with no root main.go; go install ./cmd/gqlc yields a gqlc binary; gqlc version prints the dev default and honours -ldflags -X override; gqlc --help lists version only (generate/init arrive in later stages); all CI checks green.","status":"closed","priority":2,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T21:55:43Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:18Z","started_at":"2026-07-12T21:56:12Z","closed_at":"2026-07-12T22:25:09Z","close_reason":"Shipped in PR #271 (spec #270). cmd/gqlc skeleton: cobra root + version command, internal/version seam (single -X target), root main.go dev harness deleted, golden fence intact. Linus PASS, all CI gates green.","external_ref":"https://github.com/areqag/gqlc/issues/293","dependencies":[{"issue_id":"gqlc-m1c.1","depends_on_id":"gqlc-m1c","type":"parent-child","created_at":"2026-07-12T17:55:43Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-m1c","title":"CLI epic: cmd/gqlc cobra surface — init / generate / version","description":"Build gqlc's real CLI, replacing the root-main.go dev scratch harness. Decisions grilled with owner 2026-07-12 and pinned in ADR 0011 (cobra + charm.land/huh/v2; rejections recorded) and ADR 0012 (output dir exclusively gqlc-owned; wipe-and-rewrite guarded by the all-marked tripwire); CONTEXT.md gained the Output directory term. Session decisions binding on all stages: entrypoint at cmd/gqlc/main.go and root main.go deleted (install path go install github.com/areqag/gqlc/cmd/gqlc@latest); config lookup = ./gqlc.yaml default + -f/--file on init and generate; generate accumulates errors across named queries, prints all, writes nothing on failure; init is TTY-only (clean failure, no flag matrix in v1), config-file-only writes with soft warnings for missing paths, broken existing config = report loader error + offer fresh-start overwrite (no salvage decoder), final wizard step previews exact output file and flags comment loss; config version differences handled by the existing version-probe seam (init on an older file is the migration; newer-than-binary refused). Three stages, each spec-first (spec PR -\u003e code PR -\u003e close-out PR, fresh Carmack+Linus, verified Linus PASS = merge authority): CLI-0 skeleton, CLI-1 generate, CLI-2 init.","notes":"Deps enter go.mod at the stage that uses them: cobra at CLI-0, huh/v2 at CLI-2. Release bead gqlc-23e (release-please) stays owner-driven, outside this epic.","status":"closed","priority":2,"issue_type":"epic","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T21:55:19Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:19Z","started_at":"2026-07-12T21:56:12Z","closed_at":"2026-07-13T02:26:17Z","close_reason":"Epic complete: CLI-0 (#270/#271), CLI-1 (#273/#274), CLI-2 (#276/#277). cmd/gqlc ships init / generate / version on cobra + huh/v2 per ADR 0011/0012. All stages Linus-PASSed, golden fence intact throughout.","external_ref":"https://github.com/areqag/gqlc/issues/294","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-suj","title":"internal/config: add neo4j-go-v6 to the Driver enum (follow-up to gqlc-lq3)","description":"PR #259 (gqlc-lq3) shipped codegen.WithDriverVersion(DriverV5|DriverV6). The config file's driver axis must now admit the v6 target.\n\nCHANGES:\n1. internal/config: DriverNeo4jGoV6 Driver = \"neo4j-go-v6\" constant + append to DriverValues(). Everything else (UnmarshalYAML validation, invalid-value and missing-field messages, future init prompts) derives from that slice — verify no other literal list exists.\n2. Tests: update every substring assertion pinning '(valid values: neo4j-go-v5)' to the two-member list; add v6 acceptance row; missing-driver message row updated. Golden fixture stays v5.\n3. docs/specs/config-file-format.md: §3 driver row valid values; every §6.3 catalogue row that spells the driver list; sweep the doc for 'single'/'only one'/'exactly one' claims about the driver axis; §7 rationale — note the driver axis grew to two members and the required-no-default posture meant zero migration/ambiguity (the anticipated payoff).\n4. CONTEXT.md glossary 'Config file' entry: 'closed vocabulary with exactly one member today' no longer holds for driver — reword (schema language and query language remain single-member).\n\nNON-GOALS: config must NOT import internal/codegen — the config.Driver → codegen.DriverVersion mapping is the future CLI's job (leaf decoupling). No default when omitted (driver stays required). gqlc-8gp NITs stay out of scope.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T13:53:14Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:20Z","started_at":"2026-07-12T13:53:15Z","closed_at":"2026-07-12T14:03:49Z","close_reason":"Shipped in PR #264 (squash-merged, CI green). Linus adversarial review round 1: verified PASS — DriverValues() sole authority confirmed by repo-wide grep, three-way spec/impl/test list consistency, both error paths render two-member list, v6 acceptance decodes, fixture stays v5, no codegen import, commit hygiene clean. 1 cosmetic NIT deferred to gqlc-8gp.","external_ref":"https://github.com/areqag/gqlc/issues/295","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lq3","title":"codegen: WithDriverVersion option — emit against neo4j-go-driver v5 or v6","description":"Add a driver-version knob to codegen (the Option surface at codegen.go:28-34 explicitly reserved 'a target-driver selection'). WithDriverVersion(DriverV5|DriverV6), default DriverV5 for existing consumers. v6 mode: (a) import paths .../v6/neo4j + .../v6/neo4j/dbtype; (b) v6-native improvements — emit neo4j.Driver / neo4j.Session instead of the DriverWithContext/SessionWithContext alias names (v6 renamed them back, aliases kept but non-alias names are the v6-native spelling); (c) version-aware comment text where generated comments cite the driver version. API surface is otherwise name-identical between v5 and v6 (verified against 6.x branch: DriverWithContext = Driver alias at neo4j/driver.go:111). Test strategy: representative v6 golden fixtures (v6 output differs beyond import lines due to native names, so substitution-assertion won't hold); nested module test/data/codegen/go.mod gains the v6 dependency alongside v5 (different module paths, can coexist); test-codegen-fence compiles both. TDD: failing v6 goldens/unit tests first. Cycle 2 of the stack — sits on the driverTarget seam from gqlc-p5b.","acceptance_criteria":"v5 output byte-identical to current goldens (default unchanged). v6 goldens compile under neo4j-go-driver/v6 in the nested module fence. Both driver deps coexist in test/data/codegen/go.mod. just test, lint, fmt-check, tidy-check, test-codegen-fence green.","notes":"Linus nits from cycle-1 review (b4ce606) to fold into this cycle: (1) cardinalityAnnotation belongs in prepare.go (only caller prepare.go:466); (2) header() belongs in the render layer, stranded in generate.go; (3) types.go name understates scope — it is shared text/type derivation (identifier mangling + type tables); acceptable, rename optional.","status":"closed","priority":2,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T04:01:40Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:20Z","started_at":"2026-07-12T04:16:16Z","closed_at":"2026-07-12T04:36:38Z","close_reason":"Shipped in PR #259. WithDriverVersion(DriverV5|DriverV6) option, default v5 byte-identical; driverV6 target emits /v6/ paths + native neo4j.Driver name; six representative _v6 golden fixtures via manifest.json driverVersion marker (unknown values fail loudly); nested module carries both driver majors through the fence. Linus PASS zero blockers. Follow-ups: gqlc-do1 (Vector/UUID column types), v6 live-smoke bead.","external_ref":"https://github.com/areqag/gqlc/issues/296","dependencies":[{"issue_id":"gqlc-lq3","depends_on_id":"gqlc-p5b","type":"blocks","created_at":"2026-07-12T00:01:40Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-p5b","title":"codegen: split generate.go into layer files + centralize driver target","description":"Mechanical refactor of internal/codegen/generate.go (2,461 lines, monolith) into files along the layer boundaries that already exist in the function ordering: prepare.go (phaseZAdmit/phaseAAdmit/phaseBDerive, validateQueries, sweepIdentifiers, entity prep), render_db.go / render_querier.go / render_models.go / render_queries.go (renderDB, renderQuerier, renderModels, renderCypherFile + write* decode helpers), types.go (goType/temporalGoType/scalarGoType/driverCarrier mapping tables), and a new driver.go holding a driverTarget value that centralizes the 5 hardcoded neo4j-go-driver/v5 import-path string literals (generate.go:950,1044,1197,1199,1459). Pure file reorganization; no behavior change. This is cycle 1 of the v5/v6 driver-option stack — driverTarget is the seam cycle 2 keys the version knob on.","acceptance_criteria":"Zero golden diffs: all 61 test/data/codegen/valid fixtures byte-identical without -update. just test, lint, fmt-check, tidy-check, test-codegen-fence all green. Exported codegen API unchanged.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T04:01:02Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:21Z","started_at":"2026-07-12T04:01:52Z","closed_at":"2026-07-12T04:36:37Z","close_reason":"Shipped in PR #257 (squash 7e82b8d→master). generate.go (2,461 lines) split into prepare/render_*/types/driver layer files; driverTarget seam centralizes the six v5 import literals. Linus PASS zero blockers (AST byte-diff: 70 decls identical, 5 changed = threading only). All gates green; 61 goldens byte-identical.","external_ref":"https://github.com/areqag/gqlc/issues/297","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-73h","title":"codegen: testcontainer-backed live smoke test for generated repositories","description":"Owner-flagged idea (2026-07-11): spin up a real neo4j v5-community via testcontainers-go and verify that the emitted repository code actually queries a live driver — not just type-checks against v5.28.4.\n\nScope suggestion (per C1 orchestrator brief):\n- internal/codegen/live_test.go with build-tag codegen_live so CI stays opt-in.\n- Testcontainer running neo4j:5-community.\n- Steps:\n  1. Generate a repo from a valid fixture (probably many_col_many for a decent surface).\n  2. Compile the generated package into a small test binary.\n  3. Seed the neo4j with a few known rows.\n  4. Run the test binary against the live db.\n  5. Assert :one and :many methods return the expected data (Row struct field values, ErrNoRows, ErrMultipleResults).\n\nRationale for deferring past C1:\n- The goldens and just test-codegen-fence already prove the emitted code compiles + lints under the pinned driver — the type-check gap is closed.\n- Adding testcontainers introduces a Docker dep and CI-time increase that changes the character of the internal/codegen test suite (pure Go today).\n- The gap this closes is 'compiles + lints' vs 'actually queries' — real, but not blocking C1's DoD. Better to land it as its own PR cycle with the Docker-CI wiring done deliberately.\n\nPriority: medium — worth doing before v1 GA so the emitted-code contract is battle-tested against a real driver.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T05:28:24Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T18:24:17Z","started_at":"2026-07-11T17:05:41Z","closed_at":"2026-07-11T18:24:17Z","close_reason":"Shipped in PR #247 (feat(codegen): live testcontainer smoke test for generated repositories). Testcontainer-backed neo4j:5-community harness with codegen_live build tag; verifies emitted repo code queries a live driver. GH #227 auto-closed via 'Closes #227' in PR body. Agent completed while CI pending, so bead close deferred to orchestrator housekeeping post-merge.","external_ref":"https://github.com/areqag/gqlc/issues/227","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-one","title":"Retire the internal API freeze concept from ADRs, specs, and code comments","description":"ADR-driven freeze / revision protocol / apidiff-gate machinery is being retired for internal-package types (query.Query, ValidatedQuery). internal/ is a hard Go-level barrier — no external consumers, no semver contract to protect. Codegen consumes a mutable internal type; that's normal engineering (sqlc, syn, etc). The five-cycle unfreeze campaign wasn't paying rent (most Linus rounds passed first try). Approach: implicit retirement — rewrite ADRs 0003/0004/0006/0007/0008/0009 in place to strip freeze framing while preserving still-valid decisions (Stage 1-14 scope, resolver API contract, query.Query surface inventory). Rename 0007/0008 filenames. Also scrub docs/specs/ (rename 5 unfreeze-*.md files to model-change-*.md), CONTEXT.md, and ~30 code comments in internal/. Split into 3 stacked PRs: (1) ADRs + CONTEXT.md, (2) docs/specs/ scrub + renames, (3) internal/ code-comment purge. Small chore(beads) PR at the end for gqlc-i7i close + bd remember.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-10T07:10:09Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T07:41:02Z","started_at":"2026-07-10T07:10:18Z","closed_at":"2026-07-10T07:41:02Z","close_reason":"Freeze concept retired. PR #205 (ADRs + CONTEXT.md), PR #207 (file renames + spec doc scrubs), PR #206 (internal/ code comments). internal/ has no external consumer to protect; refactor freely. See bd remember 'no-internal-api-freeze'.","external_ref":"https://github.com/areqag/gqlc/issues/204","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0ig","title":"Model unfreeze: CALL-arg attribution dropped on the wire — no argument-site type check possible","description":"OWNER DECISION REQUIRED (ADR 0008 revision protocol; freeze-not-a-wall policy). The frozen wire model discards CALL argument attribution entirely: internal/query/cypher/call.go:54-66 mines each argument expression's type then DISCARDS it (explicit bucket-3 comment), emitting parameter uses as untagged ExprUse{t, ExprInProjection} and argument refs as bare names into curPart.refs. query.CallBinding (query.go:843-910) carries no arg-type/arg-ref/CallArg fields. The Stage-14 parser spec §4.5 (docs/specs/cypher-query-parser-stage-14.md:971-990) skiplists the arg-vs-param type check as bucket-3 by design. Consequence: ADR 0009's R7 line 'argument assignability including NUMBER assignable-from INTEGER-or-FLOAT (ADR 0007 Stage-14 note)' has NO application site in the resolver — R7 shipped YIELD typing + gate lift + parser-authoritative unknown-procedure only, with the delta documented in docs/specs/resolver-stage-r7.md §7.1.1. Proposed widening: per-position CALL-arg records on CallBinding (arg query.Type + param name/token from the matched procsig.Signature) emitted at parse; the resolver's argument-site check incl. NUMBER assignable-from INTEGER-or-FLOAT then becomes a subset/assignability walk over those records. Same attribution family as gqlc-fvo (Use→Part) — consider deciding both together.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-06T06:09:59Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:13Z","started_at":"2026-07-07T08:05:49Z","closed_at":"2026-07-10T06:07:13Z","close_reason":"Delivered across PRs #121 (spec), #122 (model unfreeze: CallBinding.args axis, ADR 0008 amendment), #123 (resolver argument-site assignability walk incl. NUMBER assignable-from + TypeNull wildcard, sentinel ErrCallArgAssignability, 4 new fixtures), #125 (spec errata: fabricated witnesses corrected, census pinned, TypeNull E1), #124 (R7 §7.1.1 closed-out errata). Follow-up filed: gqlc-8en (R7 §1/§4.8 stale posture prose). Session note: cycle survived two teammate incidents (rogue merge, protection-deletion attack) — see bd memory unfreeze-loop-incident.","external_ref":"https://github.com/areqag/gqlc/issues/157","metadata":null,"dependencies":[{"issue_id":"gqlc-0ig","depends_on_id":"gqlc-0mx.9","type":"blocks","created_at":"2026-07-06T02:09:59Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-fvo","title":"Model unfreeze: Use→Part attribution — parameter Uses carry no Part field","description":"OWNER DECISION REQUIRED (ADR 0008 revision protocol; freeze-not-a-wall policy). The wire model carries no Use→Part attribution: PropertyUse/ExprUse/ClauseSlotUse in internal/query/query.go have no Part field (grep-verified during R5 code round 1). Consequence: the resolver cannot attribute a parameter PropertyUse to the Part it lexically occurs in, so R5 ships the any-valid-witness rule (resolve.go witnessAcrossScopes: witness in EVERY scope containing the Ref's variable, keep successes, unify via R2 lattice, error only when all containing scopes fail with ErrUnknownProperty). This is honest but has quantified residual unsoundness documented in docs/specs/resolver-stage-r5.md §4.2.4 (a Use valid only in a scope other than its lexical Part is admitted) and a §7 unfreeze row (commit 28cab79). Proposed: add Part index (or Part ref) to the Use records at emission. Would let the resolver witness exactly in the lexical scope and delete the any-valid-witness workaround.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-05T23:09:34Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:12Z","started_at":"2026-07-06T23:26:42Z","closed_at":"2026-07-10T06:07:12Z","close_reason":"Cycle 2 complete: spec PR #116 (81321f0), unfreeze PR #117 (9d5b565, additive part int on 3 Use variants + ADR 0008 amendment, zero-golden rebaseline), resolver-widening PR #118 (d68fc75, Part-exact witness retires R5 §4.2.4 any-valid-witness), errata PR #119 (448eb47). Residuals filed: gqlc-4w5 (semantic-scope attribution), gqlc-qcc (Use.Branch axis).","external_ref":"https://github.com/areqag/gqlc/issues/158","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-hk0","title":"Model unfreeze: ExprProjection.ContainsAggregate (Shape B) — nested aggregates invisible to resolver","description":"OWNER DECISION REQUIRED (ADR 0008 revision protocol; freeze-not-a-wall policy). Frozen model drops nested-aggregate structure at classification: classifyRichExpression (internal/query/cypher/typing.go:857-877) unconditionally returns NewExprProjection(refs, t), so RETURN count(n)+1 is indistinguishable from RETURN n.x+1. R5 handled this via the uniform-exclude posture (ExprProjection never a grouping key =\u003e under-grouping when mixed with AggregateProjection; spec docs/specs/resolver-stage-r5.md §7.1.5 + audit note). Proposed Shape B: add a ContainsAggregate bool to ExprProjection, set by classifyRichExpression during the walk it already performs (parser-side, no resolver re-parse). Shapes A (OriginalText) and C (TextSpan) were evaluated and retired: recovered text re-parses opaque. Deferral lock to flip: parser pin 'count in arithmetic' (parser_test.go:1319-1327). Unblocks exact grouping-key semantics for mixed Parts.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-05T23:09:23Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:13Z","started_at":"2026-07-06T13:49:58Z","closed_at":"2026-07-10T06:07:13Z","close_reason":"Delivered: spec PR #111 (17c4d18), unfreeze PR #112 (e0e2c83 — containsAggregate field + walker + ADR amendment + 20-golden rebaseline), resolver-widening PR #113 (0c0f076 — fillGroupingKeys gate+loop over ContainsAggregate, 2 fixtures, 116-golden byte-identity fence). Errata docs PR #114. Linus PASS on all three.","external_ref":"https://github.com/areqag/gqlc/issues/159","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.9","title":"R7: resolver CALL — YIELD column typing, NUMBER assignable-from via registry","description":"Type YIELD columns from the procsig.Registry signatures; argument-site assignability including NUMBER assignable-from INTEGER-or-FLOAT (the signature-only marker, ADR 0007 Stage-14 note); unknown procedure at generation time is an error by design.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:55:06Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:13Z","started_at":"2026-07-06T04:42:37Z","closed_at":"2026-07-10T06:07:13Z","close_reason":"Delivered via PR #107 (spec, master a4b497f) + PR #108 (code, master 038b597). CALL lands: Phase A1 CallBinding admission with shadow-delete + duplicate tripwire; YIELD column typing via callTypes lane (CallBinding.ResultType→ResolvedProperty{Type,Nullable}, NUMBER→ResolvedUnknown, registry source of truth); call-vs-entity re-bind guard at all three sites (labelled node, edge, inferUnlabelled commit) firing ErrPartBindingTypeConflict; unknown procedure parser-authoritative (ErrUnknownProcedure at parse, resolver trust posture — no re-check). Zero new sentinels; two message-set widenings. 14 valid + 3 invalid fixtures; byte-identity fence over all 102 pre-existing valid goldens held. Linus PASS: spec round 2, code round 1. NOTE honest delta vs ADR 0009 R7 line: argument-site assignability (NUMBER assignable-from INTEGER-or-FLOAT) has NO application site — frozen wire drops CALL-arg attribution (call.go:54-66 bucket-3, Stage-14 spec §4.5); documented in resolver-stage-r7.md §7.1.1; owner-decision unfreeze bead filed at this close-out.","external_ref":"https://github.com/areqag/gqlc/issues/160","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.9","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:55:05Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.9","depends_on_id":"gqlc-0mx.6","type":"blocks","created_at":"2026-07-03T16:55:05Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.9","depends_on_id":"gqlc-0mx.8","type":"blocks","created_at":"2026-07-03T16:55:18Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.8","title":"R6: resolver writes — effect validation against schema","description":"Validate Effects against the schema: SET/REMOVE property + label existence, CREATE/MERGE shape (labels form valid keys, endpoints resolvable), DELETE targets. Projection-less writes resolve to zero-column ValidatedQuery.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:55:05Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:13Z","started_at":"2026-07-05T23:09:51Z","closed_at":"2026-07-10T06:07:13Z","close_reason":"Delivered: spec PR #104 (364e9e1) + code PR #105 (c5b747b). Phase E effect validation for all 8 frozen Effect variants, admission gate lifted, ErrInvalidEffectTarget sentinel (10 total), ExprInSetValue/ExprInDeleteTarget consolidated into resolveType arm, zero-column ValidatedQuery for projection-less writes, 26 valid + 16 invalid fixtures (+1 retired), byte-identity fence over 76 pre-existing goldens held. Linus PASS round 1 (zero blockers) + delta PASSes on 92f2534/9043222. Zero new frozen-model deficiencies.","external_ref":"https://github.com/areqag/gqlc/issues/161","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.8","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:55:04Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.8","depends_on_id":"gqlc-0mx.6","type":"blocks","created_at":"2026-07-03T16:55:04Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.8","depends_on_id":"gqlc-0mx.7","type":"blocks","created_at":"2026-07-03T16:55:17Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.7","title":"R5: resolver multi-part/branch — WITH carry-forward, UNION column compatibility, RETURN * expansion, grouping keys","description":"WITH carry-forward checks (dropped names unbound downstream), UNION branch column compatibility, RETURN */WITH * expansion against in-scope bindings (resolver owns expansion per the model), implicit grouping-key semantics for aggregate projections — including the recorded gqlc-gyw contract: grouping keys for ExprProjection residuals discovered by resolver-side re-parse of the projection's original text span; ContainsAggregate axis only as the ADR-0008 escape hatch, never inferred from Type.","notes":"R4 spec §7.4 hand-off: cross-WITH regime (b) nullability demotion is R5 admission-gate scope — an OPTIONAL-introduced binding carried through WITH and re-matched in a later Part demotes via the later Part's own Bindings slice; NO model change needed (per-Part Bindings suffice). Same-Part regime (b) is NOT R5 scope — blocked on model unfreeze beads gqlc-ay9 (Class A) / gqlc-5xg (Class B), owner decision pending.\nR5 code cycle STARTED 2026-07-05 on branch resolver-r5-code (worktree /home/antranig/Developer/gqlc/resolver-r5, base origin/master 1ce6dba). Order: (1) add ValidatedQuery.Distinct + Column.GroupingKey wire fields, rebaseline 51 R4 goldens with -update; (2) add ErrUnionColumnMismatch sentinel + retire the 5 R4 invalid fixtures whose gates are being removed; (3) admission-gate widening + branch-and-part driver; (4) resolveBranch/carriedScope + UNION + RETURN * expansion + AggregateProjection + computeGroupingKeys + cross-WITH nullability; (5) 20 new valid + 5 new invalid fixtures. NOT pushed — team-lead owns push/PR.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:55:04Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:13Z","started_at":"2026-07-05T14:34:35Z","closed_at":"2026-07-10T06:07:13Z","close_reason":"R5 complete. Spec PR #101 (1ce6dba), code PR #102 (4f634d5). Delivered: WITH carry-forward via branchState (8 export axes), UNION strict index-wise column compatibility (ErrUnionColumnMismatch), RETURN */WITH * expansion (own-Part-first shadowing dedup), implicit grouping keys (Column.GroupingKey + ValidatedQuery.Distinct, 51-golden rebaseline), cross-WITH regime (b) nullability demotion (no model change), §6.4 cross-Part re-bind guard (ErrPartBindingTypeConflict, node+edge; carry-wins for unlabelled), any-valid-witness parameter rule (C1 fix). Fixtures now 76 valid + 29 invalid. Linus PASS on both spec (4 rounds, B1-B8) and code (2 rounds, C1+N1-N7) cycles. Follow-ups filed: Shape B ContainsAggregate unfreeze, Use→Part attribution unfreeze, R5 nits.","external_ref":"https://github.com/areqag/gqlc/issues/162","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.7","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:55:03Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.7","depends_on_id":"gqlc-0mx.6","type":"blocks","created_at":"2026-07-03T16:55:03Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.5","title":"R3: resolver edge semantics — undirected two-orientation trial, multi-type cross-product, var-length","description":"Undirected edges: try both orientations against the schema (textual-order endpoints per the directed marker), including the double-match decision recorded on gqlc-int. Multi-type edges: one candidate EdgeKey per type (cross-product). Var-length edges: EdgeKey lookup with hop range, list\u003cedge\u003e result typing.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:46Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:14Z","started_at":"2026-07-05T06:51:21Z","closed_at":"2026-07-10T06:07:14Z","close_reason":"R3 done: spec PR #95 + code PR #96 squash-merged. Undirected two-orientation trial, multi-type cross-product (ResolvedEdgeUnion, wire tag edgeUnion), var-length list\u003cedge\u003e typing, ErrAmbiguousEdgeOrientation (sentinel set 7), Phase B label×orientation union widening, 12 valid + 7 invalid new fixtures, 3 R1-era invalids retired as features. Linus PASS both cycles. Follow-up nits: gqlc-yyg.","external_ref":"https://github.com/areqag/gqlc/issues/163","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.5","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:54:46Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.5","depends_on_id":"gqlc-0mx.4","type":"blocks","created_at":"2026-07-03T16:54:46Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.4","title":"R2: resolver typing — property-type upgrade of TypeUnknown + parameter unification across Uses","description":"Upgrade the parser's honest TypeUnknown from the schema (property lookups, accessors); unify each parameter's Uses (PropertyUse/ClauseSlotUse/ExprUse) into a single resolved type, conflict sentinel on disagreement. Resolved types carry the schema's bit-width families (graph.PropertyType, ADR 0002) per ADR 0009's vocabulary mapping.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:26Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:14Z","started_at":"2026-07-04T15:11:52Z","closed_at":"2026-07-10T06:07:14Z","close_reason":"R2 done: spec PR #92 + code PR #93 squash-merged to master. Four ResolvedType variants (scalar/temporal/list/unknown), resolveType 17-arm mapping, parameter unification lattice with ErrParameterTypeConflict (sentinel set now 6), 12 valid + 8 invalid new fixtures, R0/R1 goldens byte-identical. Linus PASS both cycles.","external_ref":"https://github.com/areqag/gqlc/issues/164","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.4","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:54:26Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.4","depends_on_id":"gqlc-0mx.2","type":"blocks","created_at":"2026-07-03T16:54:26Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.4","depends_on_id":"gqlc-0mx.3","type":"blocks","created_at":"2026-07-03T16:54:45Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.3","title":"R1: resolver edges — directed EdgeKey formation, inline endpoints, unlabelled-binding inference","description":"Directed edge bindings resolve to schema.EdgeKey (source/label/target triple) via named-binding or inline endpoints; unlabelled node bindings inferred by walking the edges that touch them (the ADR 0003 consequence).","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:25Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:14Z","started_at":"2026-07-04T14:24:58Z","closed_at":"2026-07-10T06:07:14Z","close_reason":"R1 merged to master: spec PR #89 (8b3ee2b) + code PR #90 (squash 6296e72). Three-phase kernel (A1 labelled nodes / A2 directed single-hop single-type EdgeKey formation incl. anonymous edges and inline endpoints / B fixed-point unlabelled-binding inference / C deferred edge closure). ResolvedEdge{schema.EdgeKey} joins the sum; sentinels grow to closed 5 (adds ErrUnknownEdge, ErrAmbiguousBinding); 8 new valid + 8 new invalid fixtures, edge_binding.cypher retired to valid. Linus PASS on kernel + prescribed nit round + ratification for fdee623.","external_ref":"https://github.com/areqag/gqlc/issues/165","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.3","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:54:24Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.3","depends_on_id":"gqlc-0mx.2","type":"blocks","created_at":"2026-07-03T16:54:25Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-i7i","title":"apidiff CI gate on internal/query (frozen-API enforcement)","description":"ADR 0008's revision protocol names an apidiff gate: a just recipe + CI step (CI-parity convention — every CI check has a local recipe) running golang.org/x/exp/cmd/apidiff (or equivalent) over internal/query against the merge base, failing any PR whose exported-API change is not compatible. Until this lands, review carries the guarantee. Also consider covering the JSON wire shape via the existing golden suite (already enforced by tests). Noted originally on gqlc-cta (static-analysis follow-up item 3).","status":"closed","priority":2,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:04Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T07:41:02Z","closed_at":"2026-07-10T07:41:02Z","close_reason":"Freeze concept retired. PR #205 (ADRs + CONTEXT.md), PR #207 (file renames + spec doc scrubs), PR #206 (internal/ code comments). internal/ has no external consumer to protect; refactor freely. See bd remember 'no-internal-api-freeze'.","external_ref":"https://github.com/areqag/gqlc/issues/166","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-33k.2","title":"Model projection-list DISTINCT (RETURN DISTINCT / WITH DISTINCT) as Part-level axis","description":"Reviewer S-1 from Stage 10 (PR for gqlc-2av): MATCH (n) RETURN DISTINCT n lowers to the same model as MATCH (n) RETURN n — clause-level DISTINCT silently dropped. Stage 10's rationale for modelling aggregate-DISTINCT (cardinality-changing single-bit axis; two observably-different queries must not lower to indistinguishable models) applies verbatim. Fix direction: Part.Distinct bool with always-emit JSON, mirroring AggregateProjection.Distinct.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T23:16:55Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:14Z","started_at":"2026-07-03T15:12:23Z","closed_at":"2026-07-10T06:07:14Z","close_reason":"Landed in PR #76 (squash-merged; GH #75 auto-closed). Part gains exported Distinct bool axis (always-emit JSON \"distinct\"), NewPart grows to 5 args; parser reads oC_ProjectionBody DISTINCT in collectProjection, threaded via rawPart/buildPart; EXISTS-subquery suppression covers the axis via existing subqueryDepth gate. ErrEmptyPart invariant deliberately excludes Distinct. 3199 goldens rebaselined (+4711/-0, all lines the new distinct key); godog 3897/3459/438/0 unchanged; guard quartet + lint green. Linus PASS zero blockers, 3 mutants killed; spec docs/specs/cypher-query-parser-part-distinct-axis.md.","external_ref":"https://github.com/areqag/gqlc/issues/167","metadata":null,"dependencies":[{"issue_id":"gqlc-33k.2","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-02T19:16:55Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-33k.1","title":"Preserve AggregateFunc kind for aggregates over rich expressions","description":"Reviewer SF-3 from Stage 10 (PR for gqlc-2av): when an aggregate's argument is not a bare var/literal/star (sum(x+1), count(n.age+1), collect(a OR b), count(count(*)), min([1,2,3]), sum(range(1,3))), classifyProjection falls through to ExprProjection and the AggregateFunc kind is silently dropped — resolver cannot distinguish an aggregate column from a plain expression of the same type. Inherited from Stage 6; Stage 10's DISTINCT rationale (two observably-different queries must not lower to indistinguishable models) applies verbatim. Fix direction: classifyFunction recognizes aggregate invocations with expression arguments and keeps the outer kind, or a dedicated axis on ExprProjection. Spec stage-10 §8 documents the gap.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T23:16:44Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:14Z","started_at":"2026-07-03T15:55:19Z","closed_at":"2026-07-10T06:07:14Z","close_reason":"Landed in PR #79 (squash-merged; GH #78 auto-closed). classifyFunction gains aggregate-name-first dispatch into classifyAggregateCall: rich aggregate args (arithmetic, boolean composites, list literals, nested calls, parameters, count(*)) lower as AggregateProjection with kind/refs/DISTINCT preserved via typeExpressionMining; parameter Uses preserved verbatim per Stage 6 §4 (ExprUse{aggregateResultType(fn,operand), ExprInProjection} — count($p) keeps TypeInt). Nested aggregates (count(n)+1) stay ExprProjection as documented deferral; resolver contract recorded on gqlc-gyw (re-parse first, post-freeze additive ContainsAggregate axis as escape hatch). 75 goldens rebaselined (128 kind swaps, purity mechanically verified); suite 3897/3459/438/0 unchanged. Adversarial review: spec FAIL round 1 (4 blockers incl. sum($p) Use-regression catch), revision PASS round 2, full PASS round 3 with 4/4 mutants killed. Spec docs/specs/cypher-query-parser-aggregate-kind-rich-exprs.md.","external_ref":"https://github.com/areqag/gqlc/issues/168","metadata":null,"dependencies":[{"issue_id":"gqlc-33k.1","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-02T19:16:43Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-3r0","title":"Reject projection-position pattern predicates (Pattern1 [22]/[23])","description":"Stage 8 review SF3: patterns used as scalar expressions in RETURN/WITH projection position (Pattern1 [22]/[23], TCK SyntaxError:UnexpectedSyntax) currently parse-accept and sit as enumerated skiplist entries in acceptance_test.go. Per ADR 0007 this is bucket-1 (context-sensitive parse rule): pattern predicates are legal only in WHERE/EXISTS positions. Detect the position-specific misuse and reject (likely alongside Stage 11 predicate work, which touches pattern-in-expression contexts). Remove the two skiplist entries when done. Parent epic gqlc-33k.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T13:46:04Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:45:36Z","closed_at":"2026-07-03T00:23:28Z","close_reason":"Folded into Stage 11 (PR #65): fifth sentinel ErrPatternInProjection at collectReturnItem, covers RETURN+WITH via shared oC_ProjectionBody; Pattern1 [22]/[23] skiplist entries removed.","external_ref":"https://github.com/areqag/gqlc/issues/169","dependencies":[{"issue_id":"gqlc-3r0","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-02T09:48:15Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-x6u","title":"TCK corpus completion: wire remaining dirs + codify out-of-scope skiplist policy","description":"Final sweep after Stages 6-14: every TCK feature dir must be either wired into the godog suite or documented out-of-scope. Wire useCases/{countingSubgraphMatches,triadicSelection} as parse corpus. Give skiplist entries named policy categories (runtime-error, result-assertion-only) so pending unambiguously means work remaining and the suite is a true progress meter. Acceptance: zero unwired dirs, zero unexplained skiplist entries, godog green.","status":"closed","priority":2,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:54:37Z","created_by":"antranig-yeretzian","updated_at":"2026-07-03T14:23:55Z","started_at":"2026-07-03T12:29:34Z","closed_at":"2026-07-03T14:23:55Z","close_reason":"Stage 15 landed: PR #73 squash-merged as 4848c234, GH #51 closed. Corpus complete: 3897 scenarios (3459 passed, 438 pending), 16006 steps, 3199 goldens, 89-entry typed skiplist across 8 categories with TestSkiplistCategoryPolicy guard. Thermo-nuclear review: 1 blocker (regex enumeration) fixed in fafdd3f, delta re-review PASS.","external_ref":"https://github.com/areqag/gqlc/issues/51","dependencies":[{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-2av","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-33k","type":"parent-child","created_at":"2026-07-01T23:56:12Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-665","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-97z","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-9b4","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-9nl","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-cqh","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-im1","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-mkv","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-x6u","depends_on_id":"gqlc-ydk","type":"blocks","created_at":"2026-07-01T23:54:37Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":9,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-rsx","title":"Gate merges on actionlint (always) and govulncheck (on dep changes)","description":"lint/test/tidy are required checks; actionlint and vuln are advisory. Make actionlint an always-run required job in ci.yml, and make govulncheck required via the no-op twin pattern (same-named job on inverse path filter) so it blocks exactly the PRs that touch go.mod/go.sum.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T02:37:08Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:15Z","started_at":"2026-07-02T02:37:25Z","closed_at":"2026-07-10T06:07:15Z","close_reason":"actionlint always-run required job in ci.yml; govulncheck required via single conditional workflow (in-job dep-diff, not the twin pattern)","external_ref":"https://github.com/areqag/gqlc/issues/170","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jp9","title":"Rename module path to github.com/areqag/gqlc","description":"GitHub username changed from antranig-yeretzian to areqag. Update go.mod module directive and all import paths (49 refs across 24 .go files). Leave .beads/*.jsonl historical records untouched.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T00:41:15Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:15Z","started_at":"2026-07-02T00:41:22Z","closed_at":"2026-07-10T06:07:15Z","close_reason":"Module path renamed to github.com/areqag/gqlc; PR #31 opened, thermo-nuclear review passed, just test green","external_ref":"https://github.com/areqag/gqlc/issues/172","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-int","title":"Resolver: undirected edge two-orientation trial + multi-match semantics","description":"Post-freeze resolver contract handed off by Stage 5 (gqlc-7z6). An EdgeBinding with directed=false has NO authoritative orientation: the parser records endpoints in textual order only. The resolver MUST form TWO candidate schema.EdgeKeys (source-\u003etarget AND target-\u003esource) and accept the binding if EITHER resolves against the directed-only schema. OPEN QUESTION (spec cypher-query-parser-stage-5.md §9): the trial can match zero, one, or TWO distinct directed edge types (schema has both A-\u003eB and B-\u003eA with the queried label) — define whether a double match is ambiguous, a union, or an error, and whether the resolver must report WHICH orientation(s) matched (which may require widening the marker bool-\u003eenum, cheap pre-consumer). Lives downstream of resolver bead gqlc-0mx and the freeze gqlc-cta.","notes":"Linked to resolver stage R3 (gqlc-0mx.5) at the freeze (2026-07-03): the undirected two-orientation trial + double-match decision is R3's core design question; this bead carries the accumulated context.","status":"closed","priority":2,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-06-04T03:30:13Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:45:40Z","closed_at":"2026-07-05T07:48:05Z","close_reason":"Decided and implemented in R3 (spec PR #95 §3.5/§4.6, code PR #96): resolver forms both orientation candidates; single-type single-hop undirected double-match =\u003e ErrAmbiguousEdgeOrientation (author disambiguates with directed arrow — no | union opt-in was written); multi-type \u003e=2 matches =\u003e ResolvedEdgeUnion with canonically ordered EdgeKeys; matched orientation(s) inspectable from EdgeKey.Source/Target — no parser marker widening needed, query.Query freeze respected.","external_ref":"https://github.com/areqag/gqlc/issues/173","dependencies":[{"issue_id":"gqlc-int","depends_on_id":"gqlc-0mx.5","type":"parent-child","created_at":"2026-07-03T16:55:19Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-int","depends_on_id":"gqlc-cta","type":"blocks","created_at":"2026-06-03T23:30:24Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-npg","title":"Stage 3 — projections \u0026 aggregations","description":"Extend RETURN beyond bare-variable / single-property: expressions, aggregates (count/sum/collect/...), literals, function calls, RETURN *. ReturnItem becomes an expression sum. Per ADR 0004 §Stages.","notes":"Stage 3 on branch stage-3-projections (single branch, separated commits). DONE: (1) grill-with-docs -\u003e spec docs/specs/cypher-query-parser-stage-3.md committed (d652c11). (2) MODEL commit f053af8 PUSHED: ReturnItem.Value Projection sealed sum + AggregateFunc enum + Query.ReturnsAll + JSON + 77 goldens; Linus gate PASS; pure refactor (180 pending unchanged), just test green. NEXT sub-step = UNLOCK commit: listener classifies projections (Ref/Literal/Func/Aggregate) + accepts RETURN * (set Query.ReturnsAll), add skiplist group per spec §6 (NoVariablesInScope/UnknownFunction/ColumnNameConflict), Layer-2 swaps (+2 mustParse, -1 return-star mustReject, +1 authored residual ErrUnsupportedProjection), trim ErrUnsupportedProjection docstring, regen new goldens. Use /tdd red then /gated-swarm Carmack+Linus. Spec is source of truth. Verify just test green + pending \u003c 180 before close.","status":"closed","priority":2,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:07Z","created_by":"antranig-yeretzian","updated_at":"2026-06-04T02:01:31Z","started_at":"2026-06-04T01:17:28Z","closed_at":"2026-06-04T02:01:31Z","close_reason":"Stage 3 landed on branch stage-3-projections. Model commit f053af8 (Projection sealed sum + AggregateFunc + Query.ReturnsAll, Linus gate PASS, pure refactor). Unlock commit (this): listener classifies var/var.prop/scalar-literal/func/aggregate + count(*) + RETURN *-\u003eReturnsAll; 3 skiplist entries (NoVariablesInScope/UnknownFunction/ColumnNameConflict); +2 mustParse, -1 return-star mustReject, +1 authored residual; errors docstring trimmed; 10 new goldens; CONTEXT.md Projection/Aggregate + ADR 0003 note. TDD red-\u003egreen, Linus gate PASS. godog 329/180 -\u003e 336/173 (+7 passed, -7 pending). just test green. Follow-ups filed: gqlc-gyw (resolver grouping), gqlc-zir (unary-minus nit).","external_ref":"https://github.com/areqag/gqlc/issues/18","dependencies":[{"issue_id":"gqlc-npg","depends_on_id":"gqlc-4n2","type":"blocks","created_at":"2026-05-26T00:26:10Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":3,"comment_count":0}
{"_type":"issue","id":"gqlc-v23","title":"Stage 4 — multi-scope \u0026 UNION","description":"Support WITH chaining (intermediate scopes that project the values Stage 3 introduces) and UNION branches. query.Query gains part/branch structure. Per ADR 0004 §Stages; explicitly depends on Stage 3.","status":"closed","priority":2,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:07Z","created_by":"antranig-yeretzian","updated_at":"2026-06-04T02:51:44Z","started_at":"2026-06-04T02:06:22Z","closed_at":"2026-06-04T02:51:44Z","close_reason":"Stage 4 done: WITH chaining + UNION. Restructured query.Query into Branches(UNION)-\u003eParts(WITH) with UnionKind enum; per-part scope threading (exported names L-to-R, WITH * transitive, cross-branch isolation); parameters stay query-wide. godog read-core 371 passed / 179 pending (was 336/173 at Stage 3 end; +35 scenarios parse, clauses/with+clauses/union added to readCoreDirs). 7 WITH/UNION negatives skiplisted (all verified below the type-interface boundary by adversarial review). Linus gate APPROVED. Commits 4491fb8(model)+b6cd944(unlock)+5821e4c(prep) pushed to origin/stage-4-with-union. NO PR per loop discipline.","external_ref":"https://github.com/areqag/gqlc/issues/19","dependencies":[{"issue_id":"gqlc-v23","depends_on_id":"gqlc-npg","type":"blocks","created_at":"2026-05-26T00:26:10Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":4,"comment_count":0}
{"_type":"issue","id":"gqlc-s2t","title":"Stage 2 — nullability (OPTIONAL MATCH)","description":"Make bindings introduced by OPTIONAL MATCH nullable, and propagate that flag through the columns derived from them so codegen can emit pointer (or option) types post-freeze. Per ADR 0004 §Stages.","status":"closed","priority":2,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:06Z","created_by":"antranig-yeretzian","updated_at":"2026-06-04T01:08:39Z","closed_at":"2026-05-26T23:42:25Z","close_reason":"Stage 2 complete: nullable bindings, listener accepts OPTIONAL MATCH, Layer-2 + goldens updated. Spec doc at docs/specs/cypher-query-parser-stage-2.md. Stack: stage-2-prep → stage-2-model → stage-2-unlock → stage-2-docs. just test green, godog 329/0/180.","external_ref":"https://github.com/areqag/gqlc/issues/20","dependencies":[{"issue_id":"gqlc-s2t","depends_on_id":"gqlc-4n2","type":"blocks","created_at":"2026-05-26T00:26:09Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-ld2o","title":"codegen/age: the width walk is silent on an unannotated width with a computed value","description":"`declaredPropertyTypes` / `declaresAWidth` (internal/codegen/age/sidecar_test.go)\nderive the sweep's domain from `internal/graph`'s declaration by reading the\n`PropertyType` annotation off the AST. PR #848 round 2 widened that walk. What\nit now does and does not see, measured first-party at 20264ffd (screen is\n`go test -c -o /dev/null ./internal/codegen/age/`, rc=0 on every row below,\n`-run '^TestTheDecodeAndTheGateReadTheSameSidecarKeys$'` anchored, 0\n`no tests to run`):\n\nADMITTED to the domain\n- `TypeFoo PropertyType = \"FOO\"` (const)\n- `var TypeFoo PropertyType = \"FOO\"`\n\nREFUSED by name (the walk fails loudly)\n- `TypeFoo = \"FOO\"` (untyped const)\n- `TypeFoo = PropertyType(\"FOO\")`\n- `TypeFoo = PropertyType(\"F\" + \"OO\")` — only the call's Fun is read, so the\n  argument's shape does not matter\n- `var TypeFoo PropertyType` (annotated, no value)\n- `var TypeFoo PropertyType = ListOf(TypeInt, false)`\n- `var TypeFoo PropertyType = \"F\" + \"OO\"`\n\nSILENT — the residue this bead is for\n- `TypeFoo = \"F\" + \"OO\"` (untyped const, computed value): test rc=0, no FAILs,\n  domain comes back 31 while internal/graph declares 32.\n- `var TypeFoo = ListOf(TypeInt, false)` (unannotated var, computed value):\n  same, test rc=0, no FAILs.\n\nNOT LEGAL GO, so not a residue\n- `const TypeFoo = ListOf(TypeInt, false)` — `ListOf(TypeInt, false) (value of\n  string type PropertyType) is not constant`.\n\nSo the gap is exactly one shape: a declaration carrying no `PropertyType`\nannotation whose value is neither a string literal nor a `PropertyType(...)`\nconversion. `declaresAWidth` returns false for it, `declaredPropertyTypes`\nskips it, and the domain comes back one width short with the sweep still\nreporting agreement.\n\nThe four anchors in `declaredPropertyTypes` catch a walk that reached nothing\nor stopped part way through. They do not catch a 32nd width the walk cannot\nread.\n\nAlso unrecognised, not measured because there is nothing in internal/graph to\nmeasure it against: a declaration bearing neither mark `declaresAWidth` reads\n(a name prefixed `Type`, or a spec sharing a block with the annotated widths).\n\nClose condition: either the walk stops keying on spelling — type-check\ninternal/graph with `go/types` and read the constants' computed values, which\nsees every spelling — or a guard asserts the domain's size against a count\nderived some other way, so a silently-dropped width is named. Whichever is\nchosen, mutate it with the two SILENT rows above and show a named\n`--- FAIL:`.\n\nScreen note: `go build ./...` is rc=0 on a broken `sidecar_test.go`, and\n`go vet ./internal/codegen/age/...` is nondeterministic in this repo (677\npre-existing `unreachable code` findings from generated ANTLR, surfacing or\nnot with build-cache state at the same SHA). Use\n`go test -c -o /dev/null ./internal/codegen/age/`.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:54:24Z","created_by":"areqag","updated_at":"2026-08-17T08:01:05Z","external_ref":"https://github.com/areqag/gqlc/issues/884","dependencies":[{"issue_id":"gqlc-ld2o","depends_on_id":"gqlc-x5dq","type":"discovered-from","created_at":"2026-08-17T03:54:23Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-t802","title":"resolver: describeColumnType panics on a nil list element, the in-package twin of edze's prepare.go fault","description":"Found while auditing `internal/resolver`'s `ResolvedType` type switches for\n`gqlc-856f`. Recorded here rather than fixed because the nil posture belongs to\n`gqlc-edze`, which `gqlc-856f` was explicitly scoped out of deciding.\n\n`describeColumnType` (`internal/resolver/resolve.go:363`) has a default arm\nthat returns `t.String()`. That refuses gracefully for a pointer or embedded\nform — which is why `gqlc-856f`'s audit passed it — but its `ResolvedList` arm\nrecurses on `v.Element`, and a nil `Element` is a nil interface, which matches\nno arm and reaches `t.String()` on nil.\n\nMeasured at 52f8662 via `go test -overlay` (no file added to the tree):\n\n    describeColumnType(ResolvedList{Element: nil})\n      -\u003e PANIC: runtime error: invalid memory address or nil pointer dereference\n\n`resolvedTypeEqual` is safe on the same input — its `a == nil || b == nil`\nguard catches the recursion — and returns false:\n\n    resolvedTypeEqual(ResolvedList{Element: nil}, ResolvedList{Element: ResolvedNode{}}) = false\n\nThe two compose into the fault path: `compareBranchColumns`\n(`resolve.go:~2050`) calls `describeColumnType` only for columns\n`resolvedTypeEqual` reported unequal, so a nil-element list on one branch and a\npopulated list on the other reaches the panic while building the mismatch\nmessage.\n\n## Reachability: latent, not live\n\nNo originator was found. All three `ResolvedList{...}` constructions in\nnon-test code assign a non-nil `Element`:\n\n- `scope.go:1004` — `ResolvedEdge` or `ResolvedEdgeUnion`\n- `resolve.go:1294` — the result of `resolveType`\n- `resolve.go:1458` — the result of `unify`\n\n`unify` will *propagate* a nil: `unify(ResolvedUnknown{}, nil)` returns\n`(nil, true)`, so `ResolvedList{Element: nil}` is constructible at\n`resolve.go:1458` from an already-nil element. That is propagation, not\norigination. `describeColumnType` is unexported and reached only through the\nresolver's own pipeline, so this is not reachable by an out-of-package caller\nthe way `internal/codegen`'s sites are.\n\n## Relationship to gqlc-edze\n\n`gqlc-edze` already owns `resolver.ResolvedList{Element: nil}` faulting at\n`internal/codegen/prepare.go:1050`, plus the eight typed-nil pointer forms.\nThis is the same question one package earlier. If `edze` chooses normalisation\nat the boundary, this site may be covered for free; if it chooses refusal, this\ndefault arm needs a nil check of its own. Fold into `edze` if it is being taken\nwhole.\n\n## Acceptance\n\nEither `describeColumnType`'s default distinguishes a nil interface from a type\nit does not recognise, or `edze` records that the resolver's own pipeline\ncannot originate a nil element and this stays latent by argument rather than by\naccident. A regression pin either way.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:46:12Z","created_by":"areqag","updated_at":"2026-08-17T07:47:14Z","external_ref":"https://github.com/areqag/gqlc/issues/876","dependencies":[{"issue_id":"gqlc-t802","depends_on_id":"gqlc-856f","type":"discovered-from","created_at":"2026-08-17T03:46:12Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-dc6k","title":"docs: five prose restatements of ResolvedType as a sealed sum survive gqlc-856f","description":"`gqlc-856f` corrected the claim at its source (`internal/resolver/validated.go`)\nand `gqlc-h4ug` had already corrected it in\n`docs/specs/codegen-sentinel-taxonomy.md` (rows at lines 138 and 222 now state\nthe unbounded-inhabitant reading accurately). The prose restatements below are\nwhat is left. Each describes `ResolvedType` as sealed/closed:\n\n- `docs/specs/resolver-stage-r0.md:245` — \"`ResolvedType` is the sealed sum the\n  columns and parameters carry.\" §3.4's opening sentence.\n- `docs/specs/resolver-stage-r5.md:963` — \"the sealed sum\" in the type-equality\n  paragraph.\n- `docs/adr/0010-codegen-typed-repositories-from-validatedquery.md:16` — \"sealed\n  `ResolvedType` sum\".\n- `docs/specs/codegen-prepare-commits.md:385` — describes a fence row using \"a\n  test-file-local stub `ResolvedType` implementation that satisfies the sealed\n  interface via `isResolvedType()`\". The construction described only works\n  in-package; from outside, embedding is what obtains the marker.\n\n## A different sum, same reasoning — worth a separate decision\n\n`docs/specs/codegen-stage-c5.md:739` says of the **synthesised edge-union\ninterface in generated code** (not `resolver.ResolvedType`):\n\n    Consumers type-switch on the interface at the call site; the sealed\n    sum guarantees exhaustive coverage under `gochecksumtype`.\n\nTwo things to check rather than assume: (1) the same\nembedding/pointer argument applies to any Go interface sealed by an\nunexported marker, including the generated one, so \"guarantees exhaustive\ncoverage\" is the same overclaim one layer down; (2) `gochecksumtype` is not in\n`.golangci.yml`'s enabled linters, so whatever it would guarantee is not\ncurrently run. Neither is verified here — this bead records them as questions.\n\n## Why filed rather than fixed\n\n`docs/specs/codegen-stage-c5.md` is held by open PR #804\n(`docs/c1-bare-arg-spec-drift`). The other four files are unheld, but they are\nprose restatements rather than type switches, so they fall outside\n`gqlc-856f`'s acceptance criterion, and splitting them across territories\nmid-flight buys nothing.\n\n## Acceptance\n\nEach of the four `ResolvedType` restatements either drops \"sealed\" or says what\nthe marker does and does not cover, pointing at\n`internal/resolver/validated.go`'s corrected comment rather than repeating the\nargument. The c5 question is answered either way and recorded.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:45:53Z","created_by":"areqag","updated_at":"2026-08-17T07:47:15Z","external_ref":"https://github.com/areqag/gqlc/issues/878","dependencies":[{"issue_id":"gqlc-dc6k","depends_on_id":"gqlc-856f","type":"discovered-from","created_at":"2026-08-17T03:45:52Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-qv80","title":"codegen/age: unservedColumn's fall-through comment restates the sealed-sum claim gqlc-856f corrected","description":"`internal/codegen/age/errors.go:423-425`, at the end of `unservedColumn`,\njustifies its fall-through with:\n\n    // ResolvedType is a sealed interface, so the switch above is its\n    // whole membership; a variant added to the resolver lands here and is\n    // dropped rather than emitted through an arm chosen for some other\n    // shape.\n\nBoth halves are wrong in the way `gqlc-856f` corrected at the source\n(`internal/resolver/validated.go`). `ResolvedType`'s unexported marker is not\na seal: a pointer form of any variant satisfies it (value receivers, and a\npointer's method set contains its value methods), and a struct embedding a\nvariant satisfies it from any package (Go promotes an embedded type's\nunexported methods). So the switch above is NOT its whole membership, and the\ntype reaching the fall-through today is not \"a variant added to the resolver\"\nbut a pointer or embedded form of one of the existing eight.\n\n**The behaviour is correct** — the fall-through returns\n`\"projects \" + t.String()`, a non-empty reason, so an unrecognised type is\nrefused rather than served. Only the comment is wrong. It is the last live\nrestatement of the claim in Go source after `gqlc-856f`.\n\n## Second, smaller finding in the same file\n\n`internal/codegen/age/errors.go:251`, in `unservedReason`:\n\n    _, union := col.Type.(resolver.ResolvedEdgeUnion)\n\nFor `*resolver.ResolvedEdgeUnion` or a struct embedding it, `unservedColumn`\nreturns a non-empty reason (via the fall-through above) but this assertion\nyields `false`, so the `edgeUnion` flag is false for what is in substance an\nedge-union column. That flag drives the ranking `unservedReason` documents at\nlines 240-248 (\"an edge-union column outranks an unserved parameter\"). No\npanic; the reason text is still produced, only the ranking degrades.\n\n## Why this is filed rather than fixed\n\n`internal/codegen/age/errors.go` is held by open PR #848\n(`fix/age-offset-sidecar-hardening`). `gqlc-856f`'s acceptance criterion asks\nfor a verdict or a filed follow-up per audited site, not a cross-territory\nedit.\n\n## Acceptance\n\nThe comment says what the seal does and does not cover, and names the\nconstruction that actually reaches the fall-through. Decide whether the\n`edgeUnion` ranking flag at :251 should recognise the non-value forms or\nwhether the degradation is acceptable, and record which.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:45:34Z","created_by":"areqag","updated_at":"2026-08-17T07:47:17Z","external_ref":"https://github.com/areqag/gqlc/issues/880","dependencies":[{"issue_id":"gqlc-qv80","depends_on_id":"gqlc-856f","type":"discovered-from","created_at":"2026-08-17T03:45:34Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-6ynu","title":"codegen: three hand-maintained emitted-file lists, two polarities, one of them unheld","description":"Three hand-maintained lists of emitted-file basenames now live in the tree, two of them of opposite polarity:\n\n- `internal/codegen/conformance/conformance_test.go:638` `connectionSurface` — exclusion, `{db.go, graph.go}`\n- `internal/codegen/prepare_test.go` `fixedDeclarationFiles` — inclusion, `{db.go, graph.go, querier.go}`\n- `internal/codegen/prepare_test.go` `inputDerivedFiles` — exclusion, `{models.go}`\n\nPR #845 held the two `prepare_test.go` lists to the corpus in three ways: every emitted basename must be classified, every entry must match a golden, and a basename whose exported names every emitting fixture declares must be on the fixed side. `connectionSurface` is held by nothing of the sort. A reader also has to keep two polarities straight to answer \"which files does the sweep read\".\n\nThis was carried as a RELATED note inside gqlc-qx5p, whose acceptance PR #845 meets; splitting it out so it does not close with that bead.\n\nFIX DIRECTION: derive `connectionSurface` from the same corpus measurement, or state in one place which list is authoritative and have the others reference it.\n\nACCEPTANCE: adding a basename to any one of the three lists that no golden emits, or dropping one that every golden emits, turns a NAMED test red for all three lists.\n","status":"open","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:40:52Z","created_by":"areqag","updated_at":"2026-08-17T07:44:16Z","external_ref":"https://github.com/areqag/gqlc/issues/873","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-laoy","title":"codegen: the .cypher.go suffix arm still admits a file only one fixture emits","description":"`internal/codegen/prepare_test.go` (`TestFixedDeclarationSweepEqualsTheReservedSet`, PR #845) now measures which side of the emitted-file partition a basename sits on: a basename more than one fixture emits, and an exported name every one of those fixtures declares, is a name that does not follow from the batch, so its file must be in `fixedDeclarationFiles`. That closes the `helpers.cypher.go` escape — a new emitted file ending `.cypher.go` written into every golden — because `Helper` then reaches 83/83 fixtures and the guard names the file.\n\nTHE RESIDUAL HOLE IS THE ONE-FIXTURE BASENAME. The measurement needs two fixtures to have any evidence at all, so a basename exactly one fixture emits is skipped. Today those are `directory.cypher.go` and `people.cypher.go`, both from the `multi_source_files` fixture, both genuinely input-derived (`AllPersonIds`, `PersonNameById`). Measured out-of-tree over the 642 committed goldens:\n\n    db.go               fixtures=83  New/WithTx/Queries at 83/83     -\u003e fixed, measured\n    graph.go            fixtures=45  SessionInit/EnsureGraph/DropGraph at 45/45 -\u003e fixed, measured\n    querier.go          fixtures=83  Querier/ReadQuerier/WriteQuerier at 83/83  -\u003e fixed, measured\n    models.go           fixtures=83  top name Person 62/83           -\u003e no full name, correctly skipped\n    queries.cypher.go   fixtures=71  top name GetAction 5/71         -\u003e no full name, correctly skipped\n    directory.cypher.go fixtures=1   AllPersonIds 1/1                -\u003e SKIPPED, n\u003c2\n    people.cypher.go    fixtures=1   PersonNameById 1/1              -\u003e SKIPPED, n\u003c2\n\nSo an emitter that grew a fourth fixed file ending `.cypher.go` AND enrolled exactly one fixture for it would still leave the sweep silently. The suffix arm names no file, which is what makes it the only arm with this width; the two map arms are pinned name by name.\n\nFIX DIRECTION: either enumerate the query-source stems the corpus carries (a fourth hand-maintained list, which cuts against gqlc-qx5p's RELATED note), or read the per-source query file names from the fixtures' own query source files rather than from a suffix, so the arm admits exactly the files the batch's sources justify.\n\nACCEPTANCE: a fourth emitted file ending `.cypher.go`, written into the golden targets of exactly one fixture with a declaration no `reservedIdentifiers` row holds, turns a NAMED test red, with `go test -c -o /dev/null ./internal/codegen/` clean before the test runs.\n\nPROVENANCE: measured while fixing blocker 1 of the round-3 independent review of PR #845, 2026-08-17. The reviewer's F1b row (a `helpers.cypher.go` in every golden) is closed by that PR; this is what the same escape hatch still admits at n=1.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:40:40Z","created_by":"areqag","updated_at":"2026-08-17T07:44:17Z","external_ref":"https://github.com/areqag/gqlc/issues/874","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-x2sg","title":"codegen spec fence: an exhibit's exemption is taken by a claim spelled the same way","description":"The spec fence exempts a small written set of parenthesis-less parameter lists as EXHIBITS -- shapes a document quotes to show what the fence catches, rather than claims about the emitted surface. The census (`specBareListExhibits`, internal/codegen/conformance/specfence_test.go) names each exempted list verbatim, per document, and exempts it once: a second list spelled the same way in the same document is graded.\n\nWhat is still open: the exemption is claimed by the FIRST list matching that text, and the fence cannot tell an exhibit from a claim spelled identically. So replacing an exhibit IN PLACE with a sentence asserting that shape is what the emitter prints keeps the census satisfied and leaves the claim ungraded.\n\nReproduction (measured first-party 2026-08-17 on docs/c1-bare-arg-spec-drift, vet-screened, reverted clean). In docs/adr/0029-the-codegen-spec-fence.md Decision 10, replace\n\n    Measured: a bullet reading ``the parameter list is `ctx\n    context.Context, minAge int64` `` inserted into C1 5.3 -- ...\n    left both sweeps green.\n\nwith\n\n    The emitted parameter list is `ctx context.Context, minAge int64` at one\n    query parameter.\n\n-\u003e `go test ./internal/codegen/conformance/` exits 0. The document now states the capture vector gqlc-lhs3 removed as a fact about the emitter, and the fence says nothing.\n\nScope: this is what is LEFT of the earlier, much wider hole. Before this branch's round-4 change the census named DOCUMENTS, so every parenthesis-less list anywhere in a listed document was ungraded -- appending `ctx context.Context, minAge int64` to that ADR was green with nothing removed. That is now red; only the replace-an-exhibit-in-place spelling survives.\n\nWhy it was not closed here: distinguishing an exhibit from a claim needs something in the document the fence can read -- a marker on the line, or a section the exemption is scoped to. Both put fence syntax into prose that a reader sees, which is a design call rather than a fix, and neither was measured. A per-site verbatim census over ALL graded sites is separately argued against in gqlc-0rjn (it makes the test a copy of the documents); the census here is bounded to the three lists the fence declines to grade, which have to be written down somewhere to be declined at all.\n\nRelated: gqlc-0rjn (per-document floor of one graded site), gqlc-e143 (prose beside an intact graded span), gqlc-jfwo (roots the censuses never name).\n","notes":"Filed from PR #804 round-4. Branch docs/c1-bare-arg-spec-drift, head 840a74c6. The wide hole (any parenthesis-less list anywhere in a listed document went ungraded) is CLOSED there; this bead is the residue: the exemption is claimed by the first list matching the census text, so replacing an exhibit IN PLACE with a claim spelled the same way keeps the census satisfied and leaves the claim ungraded. Witnessed green 2026-08-17, vet-screened, reverted clean. Stated as a negative in four places: specfence_test.go header (floor of five), specBareListExhibits' own comment, ADR 0029 decision 10 + consequences, and c1.md 5.7's open list.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:32:37Z","created_by":"areqag","updated_at":"2026-08-17T08:01:45Z","external_ref":"https://github.com/areqag/gqlc/issues/872","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vh74","title":"ci: which live arm a live test belongs in is asserted nowhere","description":"internal/liverecipes asserts that the union of the live recipes' -run allowlists equals the set of top-level tests the codegen module declares (bd gqlc-df3d). It does not assert WHICH half a test belongs in.\n\nWhich containers a live test needs is a property of its body — it calls into the neo4j driver, or into the AGE dialect — and is declared nowhere the reader can see. So the following stays green:\n\n- moving TestAGESessionInit out of test-codegen-live-age's -run and into test-codegen-live-neo4j's, where it runs against a neo4j container and either fails for the wrong reason or passes vacuously;\n- dropping a test from the half that has the container it needs while the other half still claims it.\n\nBoth are union-preserving, and the union is all this guard reads.\n\nClosing it needs a declared property per test that the recipes can be checked against — a naming convention the reader enforces, a per-test tag, or a registry the tests themselves write to. Filed as residue from gqlc-df3d; not a regression of it.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T07:19:35Z","created_by":"areqag","updated_at":"2026-08-17T07:29:07Z","external_ref":"https://github.com/areqag/gqlc/issues/871","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-tisj","title":"codegen: the fixed-declaration sweep records a method whatever its receiver, so a non-*Queries method would be forced into reservedIdentifiers and over-refuse queries","description":"`TestEveryEmittedFixedDeclarationIsReserved` (`internal/codegen/prepare_test.go`, PR #845) walks the emitter-fixed golden files and requires a `reservedIdentifiers` row for every exported declaration it finds. Its `*ast.FuncDecl` arm records the name whatever the receiver is.\n\nThat is right for the corpus as it stands — probed independently on #845: the three fixed files declare exactly the twelve reserved names, and no method in them has a receiver other than `*Queries`. It is wrong in principle for a method the emitter might add on some other receiver. Such a name occupies no package block and collides with nothing, but the sweep would demand a row for it, and `internal/codegen/prepare.go:570` refuses a *query* on membership in `reservedIdentifiers` alone — at either scope. So satisfying the sweep would refuse a query name that generates fine.\n\nWITNESS (mutation M13, independent review of #845, reproduced first-party at `660fe68a`): appending `func (h qHelper) Validate() bool { return true }` to every golden `db.go` turns `TestEveryEmittedFixedDeclarationIsReserved` red demanding a reserved row for `Validate`. `go vet` clean before the test ran.\n\nToday the limit is stated in the test's doc comment and nothing enforces it. Two ways out, and the second is cheap:\n\n  1. Record a method only when its receiver is `*Queries`, and assert separately that no other receiver appears in those files — so a new receiver type is a decision someone makes rather than a row someone adds.\n  2. Leave the sweep as it is and add the receiver assertion alone: the sweep's demand is then correct because the antecedent is fenced.\n\nACCEPTANCE: the M13 witness above no longer demands a `reservedIdentifiers` row for a method on a non-`*Queries` receiver, and a golden carrying such a method still fails some NAMED test rather than passing silently.\n\nPROVENANCE: nit N1, independent review of PR #845, 2026-08-17. Not reachable at that commit — zero such methods exist in the corpus.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:44:28Z","created_by":"areqag","updated_at":"2026-08-17T06:48:05Z","external_ref":"https://github.com/areqag/gqlc/issues/863","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2rli","title":"a PR body edit now re-runs the whole of ci.yml for a one-line check","description":"gqlc-w4al added `edited` to `ci.yml`'s `pull_request` types so the\ncheck-PR-body gate re-evaluates when a body changes. `ci.yml` carries five\njobs (lint, test, tidy, codegen-fence, actionlint), so every body edit now\ncosts a full pipeline run — about 3.5 min of wall time on recent runs — for a\nstep that reads one file and one API response.\n\n## Why it was accepted rather than optimised\n\nThe cheap version is a job-level `if:` skipping the expensive jobs on\n`edited`, and it is a bypass rather than an optimisation. Every job in a\ntriggered workflow emits a check run; a skipped job's conclusion is `skipped`;\nbranch protection reads `skipped` as a pass; and the newest check run for a\ncontext is the one it reads. So a red `lint` on the head SHA would be erased\nby anyone editing the PR description. vuln.yml carries the same reasoning in\nits header for govulncheck.\n\nThe correct shape is a separate workflow holding just this check, triggered on\n`[opened, synchronize, reopened, edited]`, with its own concurrency group.\nThat was not done here because the new job id would have to be added to\nmaster's required status checks, and a required context that does not yet\nexist on the PRs already open reports as \"Expected — waiting for status to be\nreported\" and blocks every one of them until it is re-triggered. Doing that\nmid-flight would have stranded other work.\n\n## Mitigation already in place\n\nThe concurrency group is `${{ github.workflow }}-${{ github.ref }}` with\ncancel-in-progress off master, so an edit that lands alongside a push collapses\ninto a single run rather than doubling. The pathological case is an edit\narriving mid-run, which cancels the run in progress and restarts it.\n\n## Shape of a fix\n\n- Add `.github/workflows/pr-body.yml` with one job running\n  `.github/scripts/check-pr-closes.py`, on the four PR activity types.\n- Drop the step from `ci.yml`'s tidy job and drop `edited` from its types.\n- Add the new job id to both master's classic branch protection contexts and\n  the `protect-master` ruleset's required_status_checks. Both exist and their\n  lists currently differ (the ruleset omits live-smoke and codegen-fence).\n- Do it at a quiet moment, and re-trigger every open PR afterwards.\n- `internal/tools/ciguard` asserts the step lives in the `tidy` job because\n  `tidy` is a required context; those assertions move with the step.\n\n## Acceptance\n\n- A PR body edit runs one job, not five.\n- The new job is a required status check in both the ruleset and the classic\n  protection, verified by reading them back after the change.\n- No PR is left with a permanently pending required context.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:41:07Z","created_by":"areqag","updated_at":"2026-08-17T06:48:05Z","external_ref":"https://github.com/areqag/gqlc/issues/864","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ipx6","title":"spec fence: docFiles' docRoots existence guard is unwitnessed","description":"internal/codegen/conformance/specfence_test.go, docFiles(): require.NoErrorf(t, err, \"docRoots entry %q does not exist\", root) is structurally unable to fail on a clean tree, and no witness stands in for it.\n\nWitness (independent review of PR #804 at dbeabc3a, mutation m15): replacing that require with `continue` leaves the whole package green — SURVIVED, screened for compile breaks by go vet.\n\nADR 0029 decision 7 draws the line between witnessed plumbing and unwitnessed assertions and enumerates six lines in the first category. This is a seventh line of exactly that shape and is outside the enumeration, so it is neither witnessed nor declared.\n\nBlast radius is small: if a censused root vanished, the censuses still redden by the lost direction (measured — probe D3 moved README.md off disk and both fence tests reddened). Only a root no census names (README.md, CONTEXT.md today) would go silent.\n\nFix is either a witness in the same shape as TestSpecSweepsCarryUnreadableSites (docFiles taking its roots as a parameter) or an eighth item in decision 7's enumeration.","status":"open","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:25:26Z","created_by":"areqag","updated_at":"2026-08-17T06:28:28Z","external_ref":"https://github.com/areqag/gqlc/issues/853","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jqix","title":"resolver: an unsatisfiable endpoint refuses as ErrUnknownEdge inline and ErrUnknownLabel as a var","description":"On test/data/resolver/invalid/schemas/satisfy_plural_edges_inline_subtype.gql, the same endpoint written two ways refuses two different ways:\n\n  MATCH (p:Person)-[r:WORKS_AT]-\u003e(:Company:Desk) RETURN p\n    -\u003e unknown edge: Employee\u0026Person-[WORKS_AT]-\u003eCompany\u0026Desk, Person-[WORKS_AT]-\u003eCompany\u0026Desk\n\n  MATCH (p:Person)-[r:WORKS_AT]-\u003e(z:Company:Desk) RETURN p\n    -\u003e unknown label: no node type satisfies Company\u0026Desk; declared types carrying these labels: Company, Company\u0026Large, Desk\n\nNo declared node type carries both Company and Desk, so the honest diagnosis is the var spelling's: the LABEL SET is unsatisfiable, and there was never an edge to look for. The inline spelling reports a missing edge and invites the author to go declare one.\n\nThe cause is ordering. A var endpoint goes through resolveNodeLabels at Phase A1, which refuses an unsatisfiable label set before edges are closed. An inline endpoint has no binding and is only ever read by endpointLabels at edge-closure time, where the empty satisfying set falls through to the spelled key and closeEdge refuses the probe set.\n\ngqlc-3uof's branch pins the CURRENT wording (TestInlineEndpointCommitsOnTheTypesSatisfyingIt/a_conjunction_no_type_satisfies asserts the whole message) because the fallback is what makes the refusal name Company\u0026Desk at all rather than nothing. Any fix here should move that row, not delete it.\n\nFix is likely a satisfiability check over inline endpoint label sets at Phase A1, before CloseEdges runs. That also makes TestInlineEndpointsAgreeWithTheirVarSpelling assertable on the error message rather than on verdict class alone — today it compares err.Error() and passes only because these unsatisfiable spellings are not among its rows.","notes":"Pre-existing; unchanged by fix/resolver-inline-endpoint-satisfying-set, which deliberately preserves it so describeTriedEdges still names the labels the author wrote.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:19:45Z","created_by":"areqag","updated_at":"2026-08-17T06:28:29Z","external_ref":"https://github.com/areqag/gqlc/issues/854","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jjcu","title":"resolver conformance: invalidFixtureContains is opt-in and no new fixture opts in, so a refusal's message text is unpinned","description":"`invalidFixtureContains` is the manifest field that pins the TEXT of a refusal, as opposed to its sentinel. It is conditional — a fixture that omits the field is simply not message-checked — and the independent review of PR #806 measured that **0 of the 18 fixtures that PR added opt in**.\n\nThis is the 'conditional gate omission is invisible' shape this repo has been bitten by before: `if entry.X != \"\"` catches DISAGREEMENT but not ABSENCE. A fixture that says nothing about the message passes the message check by saying nothing. Silence should not read the same as agreement.\n\nWHY THIS IS P3 AND NOT HIGHER — the review deliberately downgraded it from a blocker, and the reasons are worth preserving so nobody re-escalates it without cause:\n  - the shape is PRE-EXISTING, not introduced by #806;\n  - `invalidFixtures` and the schema mapping are both totality-checked, so a fixture cannot silently drop out of the run entirely;\n  - eight dedicated tests already assert messages via `ErrorIs` + `Require().Equal`, so the most important messages are pinned outside the manifest mechanism.\n\nSo the exposure is: a refusal's wording can degrade for the 18 new fixtures without any test noticing, while the sentinel and the verdict stay pinned. Related to gqlc-yb0w, which is the same blind spot at corpus scale — there the sweep cannot see a sentinel change inside a FAIL-\u003eFAIL cell; here the manifest cannot see a message change at all.\n\nFIX: make the declaration positive on both shapes rather than conditional on one. Either require every invalid fixture to carry `invalidFixtureContains`, or require it to carry an explicit opt-out that says why the message is not worth pinning. Absence must be a decision someone wrote down, not a default.\n\nACCEPTANCE: add an invalid fixture with no `invalidFixtureContains` and no opt-out, and confirm a NAMED test goes red saying which fixture failed to declare. Today it passes.\n\nPROVENANCE: nit N3, independent Linus review of PR #806, 2026-08-17, /tmp/factory/verdict-806.md.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:01:42Z","created_by":"areqag","updated_at":"2026-08-17T06:02:01Z","external_ref":"https://github.com/areqag/gqlc/issues/842","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-grvy","title":"resolver: comments in resolve.go and scope.go narrate the review's history and defend rejected alternatives","description":"PR #806 (merged as `aade7a40`, bead gqlc-0tft) added 408 comment lines to 228 code lines in `internal/resolver/resolve.go` and `internal/resolver/scope.go`. An independent review measured the banned-shape rate with the same grep on both sides: 1.2% across the two files' 679 pre-existing comment lines vs 7.4% across the 408 added — six times the files' own rate.\n\nAGENTS.md: comments explain WHY, never WHAT; no narration of changes; no justifying a design by contrasting a rejected alternative (that belongs in an ADR, which a comment may cite but must not restate).\n\nVERIFIED VERBATIM ON MASTER (I read each, they are not paraphrases):\n\n  resolve.go:888  '...filed as gqlc-pv0u rather than narrowed here.'\n  resolve.go:891  'Package-level rather than closures inside orientationDisagreement because...'\n  resolve.go:1347 'This is a STRICTER question than qualifiedDemoter's, and deliberately not the'\n  resolve.go:1418 'The OPTIONAL arm is deliberately blunter than DemoteNullability's... but that\n                   is a further widening, and it is filed rather than taken here.'\n  scope.go:90     '...the alternative is an eleventh branchState lane, and no fixture pays\n                   the precision.'\n\n'deliberately not the' is an AGENTS.md ban-list phrase verbatim.\n\nA NOTE ON ONE UNVERIFIED COUNT: the review reported 'nine `changes no answer and no test can pin it` blocks, ~60 lines, freezing one instant of the test suite into source.' I could not reproduce nine — `git grep -c \"changes no answer\" origin/master -- internal/resolver/` returns 2 in resolve.go and 1 in scope.go. Either the phrasing varies across the blocks or the count is wrong. Whoever takes this should establish the real number first rather than inheriting nine. The underlying concern is sound regardless: a comment asserting that no test pins something is a claim about the suite at one instant, and it rots silently.\n\nPRECEDENT that this is a real class here and not taste: master already carries `c1558e9e` — 'codegen/age: the sweep's comments state the rule, not the review's history (#829)', the same cleanup applied to a different package.\n\nWHY THIS IS A FOLLOW-UP AND NOT A REVERT: the review returned ZERO correctness blockers. Independent corroboration ran 310 queries x 28 schemas = 8680 cells, branch vs base: 8 FAIL-\u003ePASS, 2616 PASS-\u003ePASS with all goldens byte-identical, and **zero accept-\u003erefuse, zero accept-\u003edifferent-answer**. No pre-existing .golden.json was modified. B1 questions no line of behaviour.\n\nFIX: rewrite each comment to state the contract the code must honour. Where the rationale is genuinely load-bearing, move it to an ADR and cite the ADR from the comment.\n\nACCEPTANCE: the two files' banned-shape rate is no higher than their pre-#806 baseline of 1.2%, measured with the same grep on both sides.\n\nPROVENANCE: blocker B1, independent Linus review of PR #806, 2026-08-17, /tmp/factory/verdict-806.md.","status":"open","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T06:00:55Z","created_by":"areqag","updated_at":"2026-08-17T06:02:02Z","external_ref":"https://github.com/areqag/gqlc/issues/843","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-o22k","title":"bd-gh-sync: neither side's sync timestamp is a description-edit time, so the prefix tiebreak has a residue in each direction","description":"gqlc-6mx3 added an edit-time tiebreak to the append-only eligibility rule in .githooks/bd-gh-sync: where the GH body properly extends the bead description, the bead is pulled only when gh.updatedAt is later than bd.updated_at by more than a 60s skew margin. Both inputs are proxies, measured first-party on 2026-08-17:\n\n* bd.updated_at bumps on any write to the bead, not only a description edit. Scratch workspace /tmp/bdscratch-6mx3: 'bd update --priority 1', 'bd update --status in_progress', 'bd note' and 'bd assign' each moved updated_at with the description byte-identical (05:47:22 -\u003e 36 -\u003e 38 -\u003e 43 -\u003e 48). 'bd tag add' and 'bd comment' did not.\n* gh.updatedAt bumps on a comment, not only a body edit. 'gh issue list --repo areqag/gqlc --state all --limit 200 --json number,updatedAt,comments': 17 of 17 commented issues have updatedAt within 2s of their newest comment's createdAt (#818 +0s, #770 +1s, #760 +1s, #756 +0s, #755 +0s, #745 +0s).\n\nConsequences, both pinned only as prose in the script header today:\n1. OVER-HOLD (fail-closed, visible): a legitimate GitHub append that any bd-side write followed is held, and the notice reprints on every merge until one side is edited again. Recoverable by hand.\n2. FAIL-OPEN (silent): a trailing block cut in bd, followed by a comment on the GH issue, makes GitHub the later side and the cut block is written back over the bead. This is the gqlc-6mx3 defect surviving in a narrower shape.\n\nClosing (2) needs an input neither bulk listing carries. Candidates not investigated: GitHub's REST issue payload does not expose a body-edit time in 'gh issue list --json' (the field list has no lastEditedAt; checked 2026-08-17), so it would need the GraphQL userContentEdits connection or a per-issue call, which costs one request per bead; or bd's own Dolt history for the description column, which is local and free but says nothing about GitHub. Decide which before widening the rule.\n\nNo fixture covers (2) today because the suite would have to assert the current wrong answer.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:57:01Z","created_by":"areqag","updated_at":"2026-08-17T06:02:02Z","external_ref":"https://github.com/areqag/gqlc/issues/844","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ccsm","title":"codegen/prepare: three 'X rather than Y' comments restate rejected alternatives, and scopeName's if/else mislabels a third scope","description":"Two small residues from PR #832 (`5718a935`, bead gqlc-e6mh), in `internal/codegen/prepare.go` and its test.\n\n(1) THREE COMMENTS JUSTIFY THE DESIGN BY CONTRAST. AGENTS.md forbids a comment that defends a choice by naming what was rejected — that belongs in an ADR, which a comment may cite but must not restate. Three comments added by #832 are in the \"X rather than Y\" shape. Locate them with:\n\n    git grep -n 'rather than' origin/master -- internal/codegen/prepare.go internal/codegen/prepare_test.go\n\nand judge each: keep the sentence if it states a non-obvious CONSTRAINT a reader cannot derive from the code, delete it if it is only defending the choice. Note that at least one of these ('a cost this test locks in rather than hides', prepare_test.go:548/:578) is doing real work — it is the only place the AGE-only over-breadth of DBTX/SessionInit is admitted, and gqlc-xv62 tracks moving that admission into the spec. Do not delete it until the spec carries it, or the information is lost.\n\n(2) `scopeName` WOULD MISLABEL A THIRD SCOPE. The `identifierScope` enum (`prepare.go:23-30`) has two members, `scopePackage` (iota, the zero value) and `scopeMethod`. `scopeName` renders them with an if/else rather than a switch with a default, so adding a third member silently renders it as whichever branch the else covers — a wrong diagnostic string, not a compile error.\n\nThis is fail-open in the diagnostic rather than in the gate, so it is genuinely minor: the refusal still fires, only its wording is wrong. Filing it because the same fail-quiet-on-extension shape is the substance of gqlc-fd06, and closing both together is cheaper than twice.\n\nFIX: switch on the scope with a default that panics or returns an explicit unknown-scope string naming the value. The zero value must remain `scopePackage` — #832 relies on that for fail-closed behaviour, since an unset scope reads as the stricter package scope.\n\nACCEPTANCE: adding a third `identifierScope` member without teaching `scopeName` about it is caught — by a compile error, a panic in test, or a named failing assertion. Not by a silently wrong string.\n\nPROVENANCE: nits N4 and N5, independent Linus review of PR #832, 2026-08-17. Both non-blocking; the review returned PASS with zero blockers.","status":"open","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:49:20Z","created_by":"areqag","updated_at":"2026-08-17T05:50:33Z","external_ref":"https://github.com/areqag/gqlc/issues/839","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-xv62","title":"codegen spec: sentinel taxonomy omits that two reserved identifiers are AGE-only, so the table overstates the refusal","description":"`docs/specs/codegen-sentinel-taxonomy.md:144` describes the C1 exported-identifier reserved set landed by PR #832 (`5718a935`, bead gqlc-e6mh) without the backend caveat the tests now spell out.\n\nMEASURED (independent reviewer, #832, first-party): emitting a package per (name x backend) for `(:N { name :: STRING NOT NULL })` plus one `:one` query, at a commit with no gate, then `go build` in `test/data/codegen`'s module, partitions the 12 reserved rows three ways:\n\n  - fail on BOTH backends (7):  Queries, New, Querier, ReadQuerier, WriteQuerier, ErrNoRows, ErrMultipleResults\n  - fail on apache-age-pgx-v5 ONLY (2):  DBTX, SessionInit  -- these COMPILE CLEAN on neo4j-go-v5\n  - compile on both (3):  WithTx, EnsureGraph, DropGraph  (method scope, no package-level clash)\n\nSo for `DBTX` and `SessionInit` the gate is OVER-BROAD on neo4j: it refuses a schema that would have generated a perfectly good package. That is a deliberate, defensible choice — a false refusal is the safe direction, and the alternative is a backend-conditional reserved set — but it must be STATED, not silently implied.\n\nCorroboration that the neo4j goldens really do not carry these names: `grep -rl 'DBTX\\|SessionInit' test/data/codegen/valid/*/golden/neo4j-go-v*/` returns 0 files.\n\nThe code side already says this plainly. `internal/codegen/prepare_test.go:548` and `:578` call it \"a false refusal on that target... a cost this test locks in rather than hides\", and PR #832's body repeats it under \"Where this is weakest\". Only the SPEC is silent, so a reader of the taxonomy alone would conclude all 12 names collide on every target.\n\nFIX: give the taxonomy row the same three-way partition, and say in the negative which target each name actually collides on. Per AGENTS.md, state the limit and enumerate it rather than asserting a universal the corpus falsifies.\n\nACCEPTANCE: a reader of `codegen-sentinel-taxonomy.md` alone can say, for each of the 12 reserved identifiers, which backends it would actually break and which it is refused on defensively.\n\nPROVENANCE: nit N2, independent Linus review of PR #832, 2026-08-17.","notes":"READY PATCH, not pushed — same patch as gqlc-fd06's note:\n\n  /tmp/factory/e6mh-nits-fd06-xv62.patch  (applies to 5718a935)\n  local branch fix/codegen-reserved-set-completeness, commit 660fe68a\n\nThe spec half is one line. docs/specs/codegen-sentinel-taxonomy.md:144\nkeeps its existing text and gains, after the scopeMethod sentence:\n\n  The seeded half is uniform across targets while the declarations behind\n  it are not - `DBTX` and `SessionInit` come from the Apache AGE emission\n  alone, so seeding them refuses a name a neo4j-only batch leaves free. A\n  false refusal, taken per D2 Resolved rather than admitting an input\n  under one target and refusing it under another.\n\nNames the two AGE-only rows and why the over-breadth is deliberate, which\nis what prepare_test.go's reservedIdentifierRows and\nTestReservedIdentifiersAreUniformAcrossBackends already say. Prose only;\nnothing measures it, so it rides with fd06's test rather than standing\nalone.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:49:04Z","created_by":"areqag","updated_at":"2026-08-17T05:51:32Z","external_ref":"https://github.com/areqag/gqlc/issues/840","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-iw9p","title":"elemLocal doc comment justifies its design by contrasting a rejected axis — AGENTS.md forbids that in a comment","description":"`internal/codegen/neo4j/render_queries.go:704-709` (on master, landed in PR #822 / `0a139b74`) carries a second paragraph on `elemLocal`'s doc comment that is an 'the axis is A, not B' contrast:\n\n    // The axis is depth, not the column position the row assembly's own\n    // accumulator numbers on: sibling list columns each open their own loop\n    // at the row assembly's indent, so their locals sit in disjoint blocks,\n    // while a nested list's loop is emitted inside the enclosing one's body\n    // and shares it. The outermost depth is unsuffixed, so a single-level\n    // list column emits the loop spec §5.5 spells out.\n\nAGENTS.md forbids both shapes present here:\n  1. It documents the CALLER's naming scheme (the row assembly's accumulator\n     numbering), not this function's contract. A reader of `elemLocal` does not\n     need to know how a different function numbers a different variable.\n  2. It justifies the chosen design by contrasting a rejected alternative. That\n     belongs in an ADR, which a comment may cite but must not restate.\n\nThe FIRST paragraph (:698-703) is fine and should stay: it states the non-obvious\nconstraint — that under a fixed name the inner declaration shadows the enclosing\none and the emitted package stops compiling at nesting depth three. That is a WHY\na reader cannot derive from the two-line body.\n\nSurfaced as an unfiled nit by the independent reviewer on PR #822 (linus-ye6g);\nfiled rather than folded in, per this repo's preference for a well-filed follow-up.\n\nFIX: delete the second paragraph. If the depth-vs-column-position distinction is\nload-bearing for a future reader, it belongs in the C5 spec section that describes\nthe row assembly, next to the accumulator numbering it is contrasting against.\n\nACCEPTANCE: `elemLocal`'s comment explains only why the depth suffix exists; no\nsentence in it describes what the row assembly does or what naming axis was\nrejected.","status":"closed","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:43:27Z","created_by":"areqag","updated_at":"2026-08-17T06:59:48Z","closed_at":"2026-08-17T06:59:48Z","external_ref":"https://github.com/areqag/gqlc/issues/836","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-s0pu","title":"codegen spec C3 §? claims list loops use 'for i, elem := range value' — false at depth\u003e=1 and for bare-element arms","description":"`docs/codegen-stage-c3.md:1250` (on master, verified at origin/master) states as a bolded invariant:\n\n    - **List loops use `for i, elem := range value`** — the emitted\n      loop variable pair is idiomatic; ...\n\nBoth halves of that variable pair are falsified by the emitter that landed in PR #822 (`0a139b74`).\n\nWITNESS (first-party, `git show origin/master:internal/codegen/neo4j/render_queries.go`):\n\n    :682 func walkListElemPlan(..., depth int) {\n    :685   indexVar := \"i\"\n    :686   if carriesElemBare(e) {\n    :687     indexVar = \"_\"\n    :688   }\n    :690   iterVar := elemLocal(\"elem\", depth)\n    :691   fmt.Fprintf(b, \"%sfor %s, %s := range %s {\\n\", indent, indexVar, iterVar, srcVar)\n\nand `elemLocal` (:710) returns `name + strconv.Itoa(depth)` for depth != 0.\n\nSo the emitted loop header is:\n  - depth 0, element asserted:  `for i, elem := range value`   \u003c- the only case the spec describes\n  - depth 0, bare element:      `for _, elem := range value`\n  - depth 1:                    `for i, elem1 := range inner1`\n  - depth 2:                    `for i, elem2 := range inner2`\n\nThis is pre-existing spec drift, not a regression introduced by #822 — the depth suffix is what #822 added, and the spec bullet was never updated. It was surfaced as an unfiled nit by the independent reviewer on #822 (linus-ye6g) and is filed here rather than folded in.\n\nFIX: state the negative and enumerate the limits per AGENTS.md — the loop header is `for \u003cindex\u003e, elem\u003cdepth\u003e := range \u003csrc\u003e`, where \u003cindex\u003e is `i` when the body's fail message names it and `_` otherwise, and where the depth suffix is empty at the outermost level. Do not restate a universal the emitter does not honour.\n\nACCEPTANCE: the C3 bullet describes all four emitted shapes above, and a reader can predict the loop header for a depth-2 nested list column from the spec alone.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:43:14Z","created_by":"areqag","updated_at":"2026-08-17T05:44:10Z","external_ref":"https://github.com/areqag/gqlc/issues/837","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-j0qj","title":"codegen/age: ADR 0028 says the five refusals are 'not yet' verified by CI — a dispatched run witnessed them","description":"docs/adr/0028-age-refuses-on-the-query-text-only-with-a-witness.md:442-447 currently reads:\n\n  '**The first run of TestAGERefusesTheFunctionsItDoesNotDefine.** The AGE live\n   half is nightly-and-manual (codegen-live.yml, job live-smoke-age, skipped on\n   pull requests), so these five refusals ship verified by a hand-run spike and\n   not yet by CI.'\n\nThe parenthetical is still TRUE — live-smoke-age is skipped on pull_request. But 'not yet by CI' is now FALSE, and the passage's whole point is provenance.\n\nWITNESS (verified first-party, not taken from the author's report):\n  gh run view 31996378339 --json workflowName,event,headBranch,status,conclusion\n    -\u003e wf=codegen-live event=workflow_dispatch branch=feat/age-dialect-gap-admission\n       status=completed conclusion=success\n  gh run view 31996378339 --json jobs -\u003e live-smoke: success / live-smoke-age: success\n\nThe recipe in that log names the test as a WHOLE -run alternative:\n  cd test/data/codegen \u0026\u0026 go test -count=1 -tags codegen_live -run 'TestLiveSmoke|TestAGESessionInit|TestAGERefusesRelationshipTypeAlternation|TestAGERefusesTheFunctionsItDoesNotDefine' -skip 'TestLiveSmoke/neo4j' ./...\nending 'ok github.com/areqag/gqlc/test/data/codegen 11.499s', with ZERO 'no tests to run' lines in the log. Both functions are declared (test/data/codegen/live_age_dialect_test.go:89 and :196).\n\nIMPORTANT CAVEAT TO CARRY INTO THE FIX: the recipe has no -v, so there is no per-test PASS line. That the two witnesses executed is an INFERENCE from (regex names them as whole alternatives) + (both declared) + (no '[no tests to run]' marker) + (ok). It is sound, but the ADR must state it as an inference, not as an observed pass — this repo has been bitten by go test -run's unanchored regex printing PASS over nothing.\n\nFIX: replace 'not yet by CI' with what actually happened, citing run 31996378339 and the branch it ran on, and keep the true part about pull_request skipping. Consider whether the wider claim in gqlc-osf1's orbit needs the same correction.\n\nThe broader capability this surfaced: codegen-live is workflow_dispatch-able, so 'Docker is not running locally' is not a hard limit on witnessing live behaviour. Worth stating once in the ADR so the next author reaches for it.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:36:29Z","created_by":"areqag","updated_at":"2026-08-17T05:39:34Z","external_ref":"https://github.com/areqag/gqlc/issues/835","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-w1xz","title":"codegen/neo4j: the nested-list arm's fixed 'ok' depends on 'inner' being depth-suffixed","description":"Found by the adversarial review of gqlc-ye6g (PR #822, merged 0a139b74).\n\n`walkListElemBody`'s ColumnList arm emits, at render_queries.go:809:\n\n    fmt.Fprintf(b, \"%s%s, ok := %s.([]any)\\n\", indent, inner, iterVar)\n\n`inner` is now `elemLocal(\"inner\", depth+1)` and so is a fresh identifier at\nevery depth. `ok` is not — it is a fixed name, as are `v`, `node` and `rel` in\nthe leaf arms above it.\n\nThat is legal today, and the reason is worth writing down because nothing in\nthe tree says it: Go's `:=` needs only ONE new variable on the left, and\n`inner_d` supplies it. Each nested level is also its own block, so the inner\n`ok` shadows rather than redeclares.\n\nThe dependency runs the wrong way round from how the code reads. The fix\ngqlc-ye6g landed is described as being about shadowing at depth; but if\n`inner` ever went back to a fixed name while the arms stayed in one block,\nthe failure would not be the shadowing this bead's comment describes — it\nwould be `no new variables on left side of :=`, the exact error shape ibg5\nproduced. The suffix on `inner` is load-bearing for two separate reasons and\nthe comment names only one.\n\nProposal: one sentence at the ColumnList arm recording that `ok` is\ndeliberately unsuffixed and what makes that safe. No emission change.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:28:59Z","created_by":"areqag","updated_at":"2026-08-17T05:29:22Z","external_ref":"https://github.com/areqag/gqlc/issues/828","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0ini","title":"githooks: bd-gh-sync's held-bead ids skip the charset gate the rest of the script relies on","description":"Found in adversarial review of fix/bd-gh-sync-pull-witness (PR #825), fixed at\nthe rendering layer there, not at the source.\n\nThe pull path has three python payloads. Two of them (pull selection, push\nselection) take ids that have been past the shell charset gate\n\n    case \"$_bid\" in \"\" | -* | *[!A-Za-z0-9._-]*) ... refuse\n\nThe third — the postcondition that compares the two `bd list` snapshots — does\nnot: its ids come straight off `bd list`, because the beads it is about are\nprecisely the ones no batch touched, so nothing filtered them.\n\nPR #825 closed the two observable consequences (a space rendering one bead as\ntwo names in the summary, a newline splitting a moved.txt record so one bead is\ncounted as two) by escaping at the point of rendering: `one_line()` in the\npostcondition plus an awk join that cuts the record tag by field. Four\nassertions pin it.\n\nWHAT IS STILL OPEN: the asymmetry itself. Any future reader of that snapshot —\na new field, a new record type, a new summary arm — starts from unvalidated\nbytes again and has to rediscover it. The two selections and the postcondition\nshould agree on what an id may contain, either by gating the postcondition's\nids too or by stating in one place that it deliberately does not and why.\n\nRelated: gqlc-p5gh (the same file's `_run_batch` stdin), gqlc-tvv7 (the census\nthat guards these assertions).","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:28:11Z","created_by":"areqag","updated_at":"2026-08-17T05:28:25Z","external_ref":"https://github.com/areqag/gqlc/issues/826","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-072v","title":"githooks: bd-gh-sync-test's empty-last-line arms are unexercised, so their loss is silent","description":"Commit 0c13ddcb (branch fix/bd-gh-sync-pull-witness, merged as PR #825) added\n`[ -z \"$_ll\" ] ||` to the assertion arm for `an exemption set that could not be\nwritten stops the held-bead check`, and the commit message says every other arm\nof that shape already had one. That is true — 20 `${_ll##...}` chains, zero\nunguarded heads.\n\nWhat no assertion covers is the arm itself. Measured at 703a7493 in a detached\nworktree:\n\n    # delete `[ -z \"$_ll\" ] || ` from the exemption-set block\n    bash .githooks/tests/bd-gh-sync-test.sh\n    -\u003e 145 passed, 0 failed, exit 0\n\nThe guard is structurally unable to fail, which is the defect this branch spent\n14 commits closing one level down. It matters because the arm exists to stop a\n`${_ll##*pattern*}` chain reading a *missing* last line as a match: with `$_ll`\nempty, `${_ll##*x*}` is empty, so `[ -n \"\" ]` is false and the block passes by\nfinding nothing. A run where the script died before printing a summary would\nread as a pass on every one of those 20 chains.\n\nWHY IT SURVIVES: no fixture makes `$TMP/err` empty. The script prints something\non stderr on every path the suite exercises, so `last_line` is never \"\".\n\nWHAT WOULD CLOSE IT: a fixture where the script emits no stderr at all — e.g. a\nstub that makes it exit before the first echo — asserted once against a chain\nthat would otherwise pass vacuously. One assertion covers the shape; the other\n19 arms are then protected by argument rather than by 19 fixtures.\n\nNot a live bug: every arm has the guard today. This is about whether their\nremoval would be noticed.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:27:57Z","created_by":"areqag","updated_at":"2026-08-17T05:28:26Z","external_ref":"https://github.com/areqag/gqlc/issues/827","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-kjjp","title":"codegen/age: ADR 0028 states a forward universal about AGE versions that do not exist yet","description":"ADR 0028 line ~501 reads: 'no projection of date() will ever parse on this server, whatever carrier AGE later grows'.\n\nThis is a forward-looking universal over unreleased Apache AGE versions. Nothing in this repo can witness it — the pinned image is apache/age@sha256:4241e2d8 (PG 18.1, AGE 1.7.0), and a claim quantified over 'whatever carrier AGE later grows' is unfalsifiable by any probe we can run. The repo's standing rule is that a 'cannot / never / every' in prose must be replaced by a stated negative with its limits enumerated.\n\nWITNESS: found by the adversarial review of PR #809 (feat/age-dialect-gap-admission), recorded as nit 3. The reviewer established that the clause governs MESSAGE ORDERING only — it does not gate anything, so this is prose accuracy, not a behavioural defect. That is why it is P3 and not a blocker on #809.\n\nFIX: restate as a measured negative bounded by the pinned image, e.g. what the 1.7.0 probe actually returned for a date() projection, and drop the quantification over future versions. If a forward statement is genuinely wanted, it belongs in the ADR's explicitly-speculative section alongside the 'Suspected and unverified' entries, not stated as fact.\n\nRelated: gqlc-kb2c (#794) is the sibling prose-rot bug in the same ADR (six cardinals in one sentence).","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:24:44Z","created_by":"areqag","updated_at":"2026-08-17T05:25:56Z","external_ref":"https://github.com/areqag/gqlc/issues/824","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-m3sj","title":"codegen: the committed golden corpus has no list column past nesting depth two","description":"`TestGoldenBuild` (internal/codegen/conformance) compiles the committed\n`test/data/codegen` module, so it is the gate that says the golden tree\nitself builds. Its deepest list column is `LIST\u003cLIST\u003c...\u003e\u003e`:\n`list_list_int`, `schema_list_any_property` and `schema_list_property_widths`\nare all depth two.\n\nDepth two does not reach the element loop's local collision. gqlc-ye6g's\nwitness: at `origin/master` before the fix, a depth-three column emitted\n`innerAcc = append(innerAcc, innerAcc)` and\n\n    valid/probe_depth3/golden/neo4j-go-v5/queries.cypher.go:55:32:\n    cannot use innerAcc (variable of type []int64) as int64 value in\n    argument to append\n\nwhile every committed depth-two golden compiled. So a re-broken emitter\nwhose goldens were regenerated would leave `TestGoldenBuild` green.\n\nAfter gqlc-ye6g the depth-three coverage lives only in\n`internal/codegen/neo4j/corpus_test.go`, whose corpus is assembled in a\ntemp dir. That harness is stronger (it compiles AND runs the emitted\npackage) but it is one harness, and deleting the query from\n`testdata/corpus_queries.cypher` together with its test from\n`corpus_test.go.txt` removes the coverage without failing anything —\n`declaredTests` reads the pass set from the same template.\n\nFix: add a `list_list_list_int` fixture under `test/data/codegen/valid/`\nmirroring `list_list_int` one level deeper, so the committed tree carries\na depth-three witness that `TestGoldenBuild` compiles.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:22:54Z","created_by":"areqag","updated_at":"2026-08-17T05:23:20Z","external_ref":"https://github.com/areqag/gqlc/issues/823","dependencies":[{"issue_id":"gqlc-m3sj","depends_on_id":"gqlc-ye6g","type":"discovered-from","created_at":"2026-08-17T01:22:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rxq9","title":"resolver: two resolvedCovers writes are unpinned today and become load-bearing the moment the lane is carried","description":"Mutation testing of `feat/resolver-narrow-plural-endpoints` at 40ce99a7 (28-row\nmatrix, out-of-tree copy, `go build` green before every run) found two writes to\nthe `resolvedCovers` lane that SURVIVE deletion with the whole suite green:\n\n- `internal/resolver/scope.go`, the collapse arm of `NarrowPluralEndpoints`:\n  deleting `s.resolvedCovers[v] = struct{}{}` while keeping\n  `delete(s.nodeCands, v)` changes no test.\n- `internal/resolver/resolve.go`, `candidateTypes`' Phase B commit: deleting the\n  `else { delete(t.resolvedCovers, n.Variable()) }` arm changes no test.\n\nBoth survivals are honest — the comments beside each already say \"changes no\nanswer today and no test can pin it\" and enumerate why. The collapse write is\nunobservable because `CloseEdges` returns immediately after the pass and the\nlane is not carried; the else arm is unobservable because an unlabelled pending\nbinding is never already in the lane and `newScope` does not seed it from the\ncarry.\n\n## Why this outlives the branch\n\nBoth reasons are stated as properties of the CURRENT call order and the CURRENT\ncarry, and both are exactly what `gqlc-etj6` proposes to change. Carrying the\nlane across a `WITH`, or moving the narrowing relative to the carry check, makes\nboth lines live — and whoever makes that change gets no red test if they get\neither one wrong. The collapse omission would silently mark a correctly-narrowed\nsingleton as uncovered (lost precision); the else-arm omission would leave a\nstale `covered` on a binding a later Phase B iteration demoted (a WRONG type\ncommitted, which is the unsound direction).\n\nSo the risk is not that the lines are wrong now. It is that the safety net for\nthe person who makes them matter does not exist, and the comments that carry the\nreasoning are not a gate.\n\n## Fix\n\nDo this as part of `gqlc-etj6` rather than before it — there is nothing to pin\nuntil the lane is observable. When the lane is carried, add one row per write:\na query whose answer differs with and without the write, asserted on the\nresolved column type, not on a golden.\n\n## Witness\n\nMutation ids G16-collapseCovers and G25-phaseB-elsearm, both SURVIVE; the\nremaining 20 guards in the same matrix were KILLED by named tests.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T05:01:07Z","created_by":"areqag","updated_at":"2026-08-17T05:17:33Z","external_ref":"https://github.com/areqag/gqlc/issues/821","dependencies":[{"issue_id":"gqlc-rxq9","depends_on_id":"gqlc-etj6","type":"blocks","created_at":"2026-08-17T01:01:13Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.17","title":"codegen/age: the witness sweep binds the server answer into the body, not into an assertion","description":"`TestEveryDialectGapCarriesItsWitness` (internal/codegen/age/dialect_test.go) renders the named live test's body from the AST and requires every probe text, every recorded server answer and every served control text to appear in it. That is what makes the witness-required property mechanical rather than aspirational, and it kills the mutations that matter (unwitnessed probe added, probe commented out, witness shared between gaps, table emptied, recipe selector broken four ways).\n\nWhat it does NOT bind is that the witness **uses** the answer. A live test whose body contains the answer string as a dead local, a log line, or an unasserted variable satisfies the sweep exactly as well as one that does `require.Contains(t, err.Error(), p.answer)`.\n\nMeasured, not supposed: mutation M18 in the #809 review moved a recorded `answer` out of its assertion and left it as a bare string in the same body. The sweep stayed green.\n\nThe branch shipped this disclosed rather than fixed — `dialect_test.go`'s header comment and ADR 0028 both say the binding is textual containment, not assertion reachability. This bead is to close or ratify it, not to re-litigate the disclosure.\n\n**Severity is low and the reason is specific.** The failure requires a human to write a live witness that carries the exact server answer as a string while not asserting on it — which is more work than writing the assertion. The realistic decay path is refactoring: someone rewrites the witness to assert on a sentinel/SQLSTATE instead of the message text, leaves the old message string in a comment-adjacent position or a table row, and the sweep does not notice the coverage left.\n\nFix shapes:\n\n1. Narrow the render. Instead of the whole body, render only the arguments of assertion calls (`require.*` / `assert.*` / `s.Require().*`) reachable in the body, and require the answer to appear there. Costs a small call-expression walk; kills M18 directly.\n2. Or invert it: have the live witness itself iterate `gap.refused` and assert per probe, and have the sweep require that the witness ranges over the table rather than restating it. That makes the binding structural and removes the string-matching entirely — but it is a bigger change to `test/data/codegen/live_age_dialect_test.go` and needs a live run to land safely (Docker/CI, per gqlc-0oau).\n\nOption 1 is the cheap one and is checkable without a container.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:58:45Z","created_by":"areqag","updated_at":"2026-08-17T05:00:36Z","external_ref":"https://github.com/areqag/gqlc/issues/811","dependencies":[{"issue_id":"gqlc-35yu.17","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-08-17T00:58:45Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.16","title":"codegen/age: the undefined-function diagnose ships two universals no witness carries","description":"`internal/codegen/age/dialect.go`'s `ErrUndefinedFunction` diagnose text tells the author two universal things:\n\n- \"Apache AGE 1.7.0 defines **no temporal constructor at all**\"\n- \"AGE's **whole** temporal surface is timestamp()\"\n\nNeither is carried by anything on the branch that shipped them (PR #809, ADR 0028). The live witness `TestAGERefusesTheFunctionsItDoesNotDefine` measures five refusals and two served texts; five \"does not exist\" answers and one working `timestamp()` do not establish a closed set over AGE's function catalogue.\n\nThe provenance is a hand-run spike, recorded at ADR 0028:29 — \"of the 348 functions in `ag_catalog` exactly one has a temporal name\". That 348 is itself an unwitnessed cardinal: no live test counts `ag_catalog`, so a version bump of the pinned image (`apache/age@sha256:4241e2d8...`) that adds a temporal function silently falsifies the ADR line AND the author-facing message, with nothing red.\n\n**Why this was not a blocker on #809.** It errs toward under-refusing: the claim only affects the remediation advice, never the refusal decision, so no author is refused because of it. The `find` path is driven entirely by `undefinedFunctionProbes`.\n\n**Why it is still worth closing.** It is advice with a universal quantifier in it, in the one place an author actually reads, and the branch's own posture is that the gap table is short on purpose (bd gqlc-osf1). \"No temporal constructor at all\" reads as the opposite of \"short on purpose\" — the message asserts closure over exactly the surface the ADR says is unmeasured.\n\nFix shapes, in the ratified order (a claim not made \u003e a claim a test carries \u003e a number \u003e a hedge):\n\n1. Drop the universals. The message works as \"AGE 1.7.0 defines no temporal constructor this project has measured, and `timestamp()` is the one that answered\" — same advice, no closure claim.\n2. If closure is wanted, make it a live assertion: query `ag_catalog`'s function names from the pinned image inside the existing `ageDialectHarness` and pin the temporal-named set. That would also give the 348 a witness and redden it on an image bump.\n\nRelated: gqlc-osf1 (the table is known short), gqlc-kb2c (the ADR's cardinals rot together — the 348 is a seventh of the same family, on the provenance axis rather than the table-size axis).","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:58:27Z","created_by":"areqag","updated_at":"2026-08-17T05:00:37Z","external_ref":"https://github.com/areqag/gqlc/issues/812","dependencies":[{"issue_id":"gqlc-35yu.16","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-08-17T00:58:27Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rqu8","title":"review: a counterfactual comment ('X rather than Y') is two claims; add the fourth prose grep","description":"Round 7 of fix/emitted-decoder-reachability found a false claim that had passed a lead review, including my own.\n\nThe sentence: \"It is \\`!=\\` rather than \\`\u003e\\` so that the other direction - more names than values - binds nothing either.\"\n\nI graded it TRUE-but-unpinned. Only the first half was true. Truth table, built out of tree with the operator as a parameter (/tmp/probe-05tl-op/main.go):\n\n| shape | != shipped | \u003c | \u003e |\n|---|---|---|---|\n| more names than values | binds nothing | BINDS | binds nothing |\n| fewer names than values | binds nothing | binds nothing | PANIC |\n\n`\u003e` rejects the more-names direction too; it differs from `!=` only by dropping the bounds guard. The operator that breaks the property is `\u003c`. Round 6 named the wrong counterfactual.\n\nTwo process consequences worth adopting repo-wide:\n\n1. A fourth prose grep for review briefs: `rather than|instead of|as opposed to`. The existing three (universals / distributive-cardinal / detector-direction) all structurally miss this shape. A counterfactual asserts a property of the road not taken, which nothing runs.\n\n2. A KILLED mutation row is evidence for your claim only if the failure it produces is the one your claim predicts. My own `\u003e` row was recorded KILLED, but its kill was a panic on an unrelated fixture, not a naming change - the disproof was already in my matrix and I misread it. Match the named failure to the mechanism, not just the sign.\n\nBoth are already written into agent memory. This bead is for propagating them into CLAUDE.md / the standing reviewer brief if we keep one in-repo.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:55:45Z","created_by":"areqag","updated_at":"2026-08-17T04:56:05Z","external_ref":"https://github.com/areqag/gqlc/issues/807","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-mp9n","title":"codegen/conformance: the length guard's bounds direction is pinned only by a panic","description":"`boundLiteralNames`'s length guard `len(lhs) != len(rhs)` (internal/codegen/conformance/decoder_reachability_test.go) is pinned in both directions, but asymmetrically.\n\n- more-names-than-values: pinned by a fixture. Mutating to `\u003c` reddens TestSweepMarksEachYieldedFunctionByItsSpelling with a clean list diff (`the function literal at line 15` -\u003e `unpaired`). Landed in 6912d310.\n- fewer-names-than-values (the bounds hazard): pinned only because mutating to `\u003e` PANICS with index out of range inside TestSweepReadsAFunctionLiteralAtAnyDepthOfADeclaration/a_value_of_a_declaration_binding_fewer_names_than_values. The run aborts, so `=== RUN` drops from 2311 to 860.\n\nA panic-abort is a real kill but a poor pin: it takes the binary down rather than producing a named assertion about the guard, and the RUN drift makes it noisy in any matrix. The comment above the guard also names only the more-names test, not this one.\n\nResidue from the round-7 review of fix/emitted-decoder-reachability. Not blocking - the direction IS covered. See /tmp/lead/verdict-05tlr7.md.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:55:29Z","created_by":"areqag","updated_at":"2026-08-17T04:56:06Z","external_ref":"https://github.com/areqag/gqlc/issues/808","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-gmz6","title":"codegen: two gates are called the fence, and the codegen-fence CI context runs the other one","description":"Two different things in this repo are called \"the fence\", and the CI context\nnamed after one of them runs the other.\n\n- `just test-codegen-fence` is the **nested-module quality gate**: `cd\n  test/data/codegen \u0026\u0026 go build ./... \u0026\u0026 go vet -tags codegen_live ./...`, `go\n  mod tidy -diff`, `golangci-lint run`. `docs/specs/codegen-stage-c1.md` §8\n  calls it \"the compile fence\".\n- `internal/codegen/conformance/specfence_test.go` is the **spec fence**, added\n  by `gqlc-rz0l`. `docs/specs/codegen-stage-c1.md` §5.3 says the argument name\n  and the binding values \"are fenced\" and names those tests.\n\nThe required CI context `codegen-fence` (`.github/workflows/ci.yml:104-113`)\nruns the first, not the second. The spec fence runs under `just test` — `go test\n-shuffle=on ./...` — and so is PR-blocking via the `test` context.\n\nSo the gate is real; the naming is the hazard. A reader who wants to know\nwhether the spec fence blocks a PR will look at the context whose name matches\nand find a recipe that never loads `internal/codegen/conformance`. The reverse\nmistake is worse: someone narrowing `just test-codegen-fence` would believe they\nwere touching the spec fence.\n\nNot a defect in either gate. Both halves of PR-blocking hold for the spec fence:\nthe check runs (`test`) and it is required.\n\n## Fix, if taken\n\nRename one of them. `codegen-fence` -\u003e `codegen-goldens` for the nested-module\njob and its recipe is the smaller edit and the more accurate name; it touches\n`justfile`, `.github/workflows/ci.yml`, the branch-protection required-contexts\nlist and the §8 prose. Renaming a required context needs the protection setting\nupdated in the same change or merges block.\n\nAlternatively leave the names and add one line to §8 and to the fence file's\nheader saying which gate runs which, so the collision is at least documented.\n\n## Acceptance\n\nReading either name leads to the right gate without opening the justfile.\n\nFiled from the pre-merge adversarial review of `docs/c1-bare-arg-spec-drift`.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:44:30Z","created_by":"areqag","updated_at":"2026-08-17T04:46:46Z","external_ref":"https://github.com/areqag/gqlc/issues/792","dependencies":[{"issue_id":"gqlc-gmz6","depends_on_id":"gqlc-rz0l","type":"discovered-from","created_at":"2026-08-17T00:44:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jnsk","title":"spec fence: a graded site inside an HTML comment is invisible to the reader but not to the census","description":"The spec fence reads raw markdown bytes, so a `\u003c!-- ... --\u003e` HTML comment is\ngraded exactly as visible text is. Measured on both sides at `2d1b603e`, in a\ndetached worktree:\n\n**A commented site reddens the fence.** Appending\n\n    \u003c!--\n    func (q *Queries) Ghost(ctx context.Context, id int64) error\n    --\u003e\n\nto `docs/specs/codegen-stage-c4.md` fails `TestSpecMethodArgIsGeneratorOwned`\nwith \"documented method argument is not generator-owned\". That direction fails\nclosed — a maintenance annoyance, the same shape as `gqlc-173n`'s second half.\n\n**A commented site also satisfies a census.** A new document\n`docs/notes/ghostdoc.md` whose only `ctx context.Context` occurrence sits inside\n`\u003c!-- --\u003e` is reported as \"the sweep produced an entry specSigDocs does not\ndeclare: docs/notes/ghostdoc.md\". So `sweep.sigDocs[file]` is set by an invisible\nsite, and a listed document can keep its census entry on a signature no reader\nof the rendered document ever sees.\n\nThat second direction is the one that matters, and it is not the same claim as\n`gqlc-0rjn`. `gqlc-0rjn` is a floor of one *graded* site per listed document.\nThis says a graded site need not be a *printed* site — which is the word the\nfence uses for itself throughout (`specSigDocs` are \"the documents that print an\nemitted query method signature whole\"; the census exists to catch \"a document\nthat still prints the surface and no longer contributes to the sweep\"). Comment\ncarriage lets a document stop printing the surface while still contributing.\n\nNot currently exploited: no `\u003c!-- --\u003e` in any swept document contains\n`ctx context.Context` or `map[string]any{` (verified by grep at `2d1b603e`).\n\n## Fix, if taken\n\nStrip HTML comments from a document's text before scanning it, in `readDoc` or\nin a wrapper around it. Cheap, and it moves both directions the right way at\nonce. The thing to keep is a witness: a row asserting that a signature inside\n`\u003c!-- --\u003e` is neither graded nor exempted, so the stripping cannot quietly stop\nhappening. Do not strip fenced code blocks or inline code along with it — those\nare where nearly every graded site lives.\n\n## Acceptance\n\nA drifted signature inside an HTML comment in a swept document is not graded,\nand a document whose only graded site is inside an HTML comment does not satisfy\nits census entry.\n\nFiled from the pre-merge adversarial review of `docs/c1-bare-arg-spec-drift`.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T04:44:13Z","created_by":"areqag","updated_at":"2026-08-17T04:46:46Z","external_ref":"https://github.com/areqag/gqlc/issues/793","dependencies":[{"issue_id":"gqlc-jnsk","depends_on_id":"gqlc-rz0l","type":"discovered-from","created_at":"2026-08-17T00:44:13Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-kb2c","title":"codegen/age: ADR 0028's probe cardinals are six numbers in one sentence that rot together","description":"ADR 0028 'What is refused today' states: 'Two gaps carrying eight refused probes and four served texts — twelve query texts, counted off dialectGaps itself: three refused and two served for the alternation, five refused and two served for the undefined function.'\n\nAll six cardinals are TRUE as of cc4df90e — verified by counting dialectGaps in internal/codegen/age/dialect.go: gap 1 (ErrRelationshipTypeAlternation) has 3 refused probes and 2 served texts; gap 2 (ErrUndefinedFunction) has 5 (undefinedFunctionProbes) and 2. Totals 2/8/4/12 check out.\n\nThe defect is structural, not factual: adding one probe to undefinedFunctionProbes — which the ADR explicitly invites, since that slice is the ONLY source of the refused name set and grows as live measurements land — silently falsifies six cardinals in one sentence, and nothing fails. Same rot-together class as the recipe-count cardinal that gqlc-35yu.12 removed at cc4df90e (\"the recipe count is written at the pin and nowhere else\"), on a different axis, in the same document.\n\nDisclosed by the 35yu.12 implementer as deliberately out of scope: it stopped at the axis the lead ruled on rather than expanding unasked, and expected the sweep to find live ones. It did not — they are all currently true. Hence P3 rot-risk rather than a defect.\n\nFix shape, per the ratified ordering (a claim not made \u003e a claim a test carries \u003e a number \u003e a hedge): prefer deleting the cardinals in favour of the length-invariant mechanism they illustrate. If a number is genuinely needed, derive it in a test that reddens when dialectGaps grows and the prose does not follow.\n\n---\n\n**Amendment (2026-08-17, review of PR #809 at 3ce7798a): a seventh cardinal, outside the ADR.**\n\nThe site list above is short by one, and the extra site is not in `docs/`. `test/data/codegen/invalid/unrepresentable_temporal_localtime_column/queries.cypher` reads:\n\n\u003e AGE's dialect gate (internal/codegen/age/dialect.go) refuses the **five** constructor names a live session was measured on — date() among them — on the query TEXT\n\nSame mechanism, same trigger: append one probe to `undefinedFunctionProbes` and \"five\" goes false, silently. It matters slightly more than the ADR copies, because that comment is the fixture's stated reason for existing — it explains why `localtime()` reaches the carrier refusal rather than the dialect gate — so a reader who trusts the \"five\" will mis-reason about which arm is under test.\n\nDeliberately NOT swept on PR #809. A partial fix would have left this issue's own site list wrong while reading as addressed. The whole set should move together, under the same ratified ordering: prefer deleting the cardinal in favour of the mechanism (\"the constructor names a live session was measured on\", no number), and if a number is wanted, derive it where `undefinedFunctionProbes` can redden it.\n\nSites now known: ADR 0028 \"What is refused today\" (six cardinals in one sentence) and this fixture comment (one).","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T03:41:20Z","created_by":"areqag","updated_at":"2026-08-17T04:57:44Z","external_ref":"https://github.com/areqag/gqlc/issues/794","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-xzgt","title":"resolver: Phase B's covered=false conjunct is coarser than the evidence requires","description":"Round-4 review of feat/resolver-narrow-plural-endpoints (PASS, /tmp/lead/verdict-0tftr4.md) left this as precision residue.\n\ncandidateTypes sets covered=false for any folded edge failing witnessesItsEndpoints. That is sound and was the correct fix for F3 — the edge is not evidence about every returned row, so its far-end enumeration cannot license narrowing. But it is coarser than strictly needed: it discards the edge's contribution to COVERAGE wholesale, where a finer rule could still credit the rows the edge does witness.\n\nDeliberately not skipping the edge entirely: skipping widens the intersection and turns a unique inference into ErrAmbiguousBinding, a refusal master does not make. The reviewer built that alternative and measured 4 rows where it regresses against both master and the fix.\n\nAlso here: mutation L05 (the narrowing collapse's resolvedCovers write) SURVIVES and is annotated in-situ as unpinnable. If the conjunct is refined, check whether L05 becomes reachable.\n\nNon-breaking widening under ADR 0006, so this is precision-only.","status":"open","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T01:35:37Z","created_by":"areqag","updated_at":"2026-08-17T04:46:48Z","external_ref":"https://github.com/areqag/gqlc/issues/795","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-f2h7","title":"resolver: writtenBindings over-approximates anonymous CREATE bindings, and Phase B now reads it","description":"Round-4 review of feat/resolver-narrow-plural-endpoints (PASS, /tmp/lead/verdict-0tftr4.md) left this as residue.\n\ncandidateTypes now takes written map[string]struct{} from scope.writtenBindings() so a folded edge failing witnessesItsEndpoints marks covered=false. writtenBindings over-approximates for ANONYMOUS bindings in a CREATE/MERGE clause. The reviewer confirmed this is observable — naming the CREATE's bindings changes the answer — and that NO test pins any of it.\n\nBoth the implementer and the reviewer measured the shapes they could reach as landing on master's answer, i.e. an unrealised widening rather than a regression. That is why it is residue and not a defect in the change.\n\nWanted: a test row that pins the anonymous case, so a future edit to writtenBindings cannot silently move Phase B's answer.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T01:35:24Z","created_by":"areqag","updated_at":"2026-08-17T04:46:48Z","external_ref":"https://github.com/areqag/gqlc/issues/796","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-dhm3","title":"codegen spec fence: prose residue and a listPlaceholderRe widening the fence tolerates","description":"Non-blocking residue from the round-2 adversarial review of docs/c1-bare-arg-spec-drift at 3dd322dc (PASS). None of these falsify a stated claim; they are precision debt.\n\n1. specfence_test.go:54-56 -- loose anaphora. 'that shrinkage' has no unambiguous antecedent in the preceding sentence. Name the thing.\n\n2. docs/c1.md:823 -- 'the rest of every other signature' is vague about what 'the rest' ranges over. Say which components are covered and which are not.\n\n3. listPlaceholderRe tolerates a lowercase-only widening. The reviewer measured this and was explicit that it does NOT falsify the fence's stated claim, because the named risk \u003cbareParam\u003e is pinned and still dies. So this is a note about the regex being broader than strictly needed, not a hole. Worth deciding whether to tighten it or to state the tolerance, rather than leaving a reader to rediscover it.\n\nContext worth keeping: the round-2 reviewer went looking for a fifth false universal in this file on the theory that four is a pattern, and did NOT find one. The sharpest claim in the file (specfence_test.go:1051-1053, that an arm reporting another arm's headline is invisible to the sweeps) is exactly true. So this file is in good shape; the above is polish.\n\nRelated: gqlc-0rjn (the per-document census floor, documented not closed).","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T00:31:34Z","created_by":"areqag","updated_at":"2026-08-17T04:46:49Z","external_ref":"https://github.com/areqag/gqlc/issues/797","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-tvv7","title":"githooks: the bd-gh-sync assertion census cannot notice its own deletion","description":"The bd-gh-sync test suite keeps a census of its own assertions (added ca5e70cb, branch fix/bd-gh-sync-pull-witness). Adversarial review of 0c13ddcb found the census guards MEMBERSHIP but not its own EXISTENCE or the STRENGTH of what it counts.\n\nThree measured holes:\n\n1. Deleting the whole census block -\u003e exit 0, '140 passed, 0 failed'. Truncating the file at the census (test:2164) -\u003e exit 0 with no '--- N passed' summary line at all. The census cannot detect that it was removed.\n\n2. Weakening an assertion in place survives. Replacing test:2158's condition with a comparison against an impossible sentinel, while leaving the assertion NAME and its ok/bad calls untouched, exits 0 at 141/141. The census checks that an assertion by that name ran, not that it still tests anything.\n\n3. Prose at test:2166 says the suite 'still makes every assertion it made yesterday'. That overstates what is enforced: the census pins the SET of assertion names, not their content. State the negative instead.\n\nReviewer priced the existence half at roughly three lines, external to the suite (something outside the script must assert the summary line appears and the count is \u003e= a floor sourced from outside the run). Holes 2 and 3 may be acceptable-and-documented rather than closed; a census that pins assertion strength is a much larger design.\n\nNote the general shape: this is a gate that cannot observe its own absence, the same class as a conditional check that is skipped silently. A pass count sourced from the run itself cannot detect that the run was empty.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-17T00:26:28Z","created_by":"areqag","updated_at":"2026-08-17T04:46:50Z","external_ref":"https://github.com/areqag/gqlc/issues/798","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-pv0u","title":"resolver: a directed close still collects the right-to-left endpoint reading","description":"endpointContribution (internal/resolver/resolve.go) applies readsRightToLeft to every candidate regardless of e.Directed(). When srcs and tgts overlap, a directed candidate can satisfy both readings, and the pattern's left end then also collects k.Target -- a type the arrow rules out.\n\nReproduction (test/data/resolver/invalid/schemas/satisfy_plural_edges_chain.gql):\n\n  MATCH (p:Node)-[r:X]-\u003e(q:Node) RETURN p\n    -\u003e ambiguous label: A\u0026Node, B\u0026Node, C\u0026Node\n\nC\u0026Node has no outgoing X, so the arrow proves p is in {A\u0026Node, B\u0026Node}. The extra C\u0026Node comes from readsRightToLeft firing on the B-\u003eC declaration because srcs and tgts overlap.\n\nPrecision only, never unsound: the contribution stays a superset of the truth, so this is missed widening in the ADR 0006 non-breaking direction. Filed rather than fixed under gqlc-0tft because the fix changes which queries resolve.\n\nNote readsLeftToRight's doc comment ends 'on a directed close only the left-to-right reading was ever probed', which reads as if the other reading were irrelevant on a directed edge. The code does not act on that. Either act on it or say the code deliberately does not.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-16T23:45:13Z","created_by":"areqag","updated_at":"2026-08-17T04:46:50Z","external_ref":"https://github.com/areqag/gqlc/issues/799","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-jfwo","title":"spec: the spec fence's docRoots can fail to grow, and no census notices","description":"`docRoots` in `internal/codegen/conformance/specfence_test.go` enumerates the trees the spec fence sweeps: `docs`, `README.md`, `CONTEXT.md`.\n\nIt cannot shrink quietly. A root that stops existing fails `docFiles` by name (`docRoots entry \"docs\" does not exist`), and dropping `docs` loses every document `specSigDocs` / `specBindDocs` / `specListRuleDocs` declare, so the census reports each by name.\n\nIt can fail to grow, and nothing objects. The censuses name **documents**, not roots, so they are silent about markdown `docRoots` never reaches. `AGENTS.md`, `CLAUDE.md` and `CONTRIBUTING.md` sit beside the two files named in `docRoots` and are unswept; a new document added at the repository root joins them with no edit to the fence at all.\n\nThat is strictly weaker than the floor the fence sets for itself everywhere else. Dropping a declared document is a visible two-part edit that deletes a named document from a list in a test file. Adding an unswept one is a zero-part edit.\n\nNot currently exploited: none of the three prints a query method signature or a `map[string]any` driver-binding literal (verified by grep for `ctx context.Context` and `map[string]any{`).\n\nFiled from gqlc-rz0l, which corrected the two prose claims that overstated the floor — the fence comment said \"One thing no check that reads only these documents can reach\" and C1 §5.3 said \"What is left open is what no check reading only these documents can close\". Both now enumerate this second limit and cite this bead.\n\nOptions, not decided here:\n- A census over the repository-root markdown set, so a new root document is red until declared swept or declared out of scope. Mirrors the existing censuses exactly; costs one line per new root document.\n- Sweep every `.md` in the tree with an exclusion list (`test/data/`, `.beads/`, vendored trees) instead of an inclusion list, so the default for a new document is swept.\n\nAcceptance: adding a repository-root markdown document that prints a single-parameter query method signature reddens the fence, or the fence records why that is out of its scope.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-16T23:22:14Z","created_by":"areqag","updated_at":"2026-08-17T04:46:51Z","external_ref":"https://github.com/areqag/gqlc/issues/800","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-etj6","title":"resolver: plural endpoint narrowing does not reach across a WITH carry","description":"`gqlc-0tft` made a plural node binding narrow from an edge that every returned\nrow provably has. It does not reach across a `WITH`:\n\n    MATCH (p:Person) WITH p MATCH (p)-[w:WORKS_AT]-\u003e(c:Company) RETURN p.id\n\nstays refused. The plural carry is rejected before Part 1's edge gets the chance\nto narrow `p`, so the query is refused for a reason that the second part would\nhave answered.\n\n## Why this is a widening and not a bug\n\nNothing here is unsound: the refusal is conservative and no wrong column is\nemitted. It is a query a user can reasonably write and reasonably expect to\nresolve, refused because of the *order* two mechanisms run in rather than\nbecause the schema cannot answer it.\n\nConfirmed by `linus-0tft-r2` during the round-2 review, who separately\nestablished that a plural binding cannot cross a `WITH` at all on master\n(`MATCH (p:Person) WITH p RETURN p.id` is `ErrAmbiguousLabel` there), so this is\nnot a regression introduced by the narrowing — it is the narrowing not yet\nreaching a place that was already refused.\n\n## Fix\n\nWork out whether the narrowing can run before the carry check, or whether the\ncarry check can consult the narrowed set. Ordering changes in the resolver have\nbeen load-bearing on this epic — `gqlc-0tft` itself had to move its narrowing\n*below* the deferred-edge close, pinned by\n`TestDeferredEdgesCloseBeforeTheNarrowing` — so whatever order is chosen needs a\ntest that fails when it is reversed.\n\n## Acceptance\n\nThe query above resolves, with `p` committed to the single type `WORKS_AT`'s\ndeclared source endpoint names, and a test that reddens if the two mechanisms\nare reordered.\n\nFiled by the coordinator from `linus-0tft-r2`'s non-blocking findings (NB-4),\nwho described it as \"missed widening, filed-shaped\".\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T22:51:04Z","created_by":"areqag","updated_at":"2026-08-17T04:46:52Z","external_ref":"https://github.com/areqag/gqlc/issues/801","dependencies":[{"issue_id":"gqlc-etj6","depends_on_id":"gqlc-0tft","type":"discovered-from","created_at":"2026-08-04T18:51:04Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-p5gh","title":"bd-gh-sync: _run_batch's loop stdin is the allowlist, so a bd that reads stdin truncates the batch","description":"`_run_batch` runs `bd github sync` inside\n\n    while read … done \u003c\"$_tmp/allow.txt\"\n\nso the loop's stdin is the allowlist file. A `bd` that reads stdin — for a\nprompt, a confirmation, or anything else — consumes the remainder of that file,\nand the batch silently stops partway through the ids it was given.\n\n## Why it is filed rather than fixed\n\n**Pre-existing**, not introduced by `gqlc-nvjz` or `gqlc-b7lt`, and it fails in\nthe **safe** direction: the ids that get eaten are ids that never reach `bd`,\nso they are never pulled, never counted as pulled, and — since `gqlc-b7lt` — \nnever exempted from the deletion detector either. The run is short, not wrong.\n\nThat is exactly why it is worth a bead rather than a silent fix in an unrelated\nbranch: today's `bd` does not read stdin, so nothing exercises it, and the day\none subcommand grows a prompt this becomes a truncation nobody can see. The\ncurrent safety depends on a property of a *different program*.\n\n## Fix\n\nRedirect the loop's stdin away from the allowlist, or feed the ids by a\nmechanism that does not occupy fd 0 — `\u003c /dev/null` on the inner command is the\nusual spelling, reading the file on a non-standard descriptor is the other.\n\n## Acceptance\n\nA test in `.githooks/tests/bd-gh-sync-test.sh` whose `bd` stub reads a line from\nstdin on `github sync`, asserting every id in the allowlist still reaches it.\nIt must fail against the current script for the reason it names.\n\nFiled by the coordinator from `linus-ghsync-r5`'s non-blocking findings (F5),\nwho flagged it as latent and pre-existing so it would not be mistaken for a\nregression on this branch.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T22:42:20Z","created_by":"areqag","updated_at":"2026-08-17T04:46:53Z","external_ref":"https://github.com/areqag/gqlc/issues/802","dependencies":[{"issue_id":"gqlc-p5gh","depends_on_id":"gqlc-b7lt","type":"discovered-from","created_at":"2026-08-04T18:42:20Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7rd9","title":"bd-gh-sync: a listing that exits 0 with garbage refuses without naming which listing","description":"`bd-gh-sync` now names the exit status behind a refused pull (`gqlc-nvjz`), but\nonly when the status is **non-zero**. A listing command that exits **0** and\nwrites garbage still refuses with a bare\n\n    JSONDecodeError: Expecting value: line 1 column 1\n\nand no line naming which of `bd list` or `gh issue list` produced it.\n\nThe script's own comment says the two \"send the operator to different places\".\nOnly half of that is delivered: the operator learns the input was unparseable\nbut not whose input it was.\n\n## Scope\n\nSymmetric with the push side, which has the same gap, so this is **not a\nregression** introduced by `gqlc-nvjz` — it is the remaining half of the same\nclaim, on both paths.\n\n## Why it is filed rather than folded in\n\n`gqlc-nvjz` asked for the discarded exit status to be captured and named. It\nwas, and that fix is sound. This is a different failure mode — a command that\n*succeeds* and lies — and the fix is a different one: the parse has to be\nattributed to its source rather than the status to its command. Doing it on\none path only would leave the asymmetry the pull-path work exists to remove.\n\n## Fix\n\nAttribute the decode failure to the listing it came from, on both the pull and\nthe push path. The status-naming lines added by `gqlc-nvjz` are the model for\nwhere the message belongs.\n\n## Acceptance\n\nA test in `.githooks/tests/bd-gh-sync-test.sh` for each of the four cases —\n`bd list` and `gh issue list`, on the pull and the push path — where the\ncommand exits 0 and writes unparseable bytes, asserting the refusal names the\ncommand. Each must fail against the current script for the reason it names.\n\nFiled by the coordinator from `linus-ghsync-p4`'s non-blocking findings (NB-4),\nwho flagged it as symmetric with the push side so it would not be mistaken for\na regression on the pull path alone.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T22:07:14Z","created_by":"areqag","updated_at":"2026-08-17T04:46:53Z","external_ref":"https://github.com/areqag/gqlc/issues/803","dependencies":[{"issue_id":"gqlc-7rd9","depends_on_id":"gqlc-nvjz","type":"discovered-from","created_at":"2026-08-04T18:07:14Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-bst4","title":"spec: C5 prints GetAction with two different parameter types in one document","description":"`docs/specs/codegen-stage-c5.md` prints one method under two different\nparameter types:\n\n- `:418` — `GetAction(ctx context.Context, arg GetActionParams)`\n- `:996` — `GetAction(ctx context.Context, arg int64)`\n\nOne method name, two arities, in one document. At most one of them is what the\nemitter produces for that method.\n\nConfirmed present at `77a8cc12`, so it is **pre-existing** and not introduced by\n`gqlc-rz0l`.\n\n## Why the fence does not catch it\n\n`gqlc-rz0l`'s spec fence grades the argument **name** position and explicitly\ndisclaims the type position. The name is `arg` in both lines, which is correct,\nso both pass. The divergence is entirely on the axis the fence says it does not\nread — this is the fence being honest about its boundary rather than failing.\n\n## Fix\n\nWork out which arity `GetAction` actually has by reading the emitter and the\ngoldens rather than by picking the line that looks more recent, then correct the\nother site. If both are intended — two different examples that happen to share a\nmethod name — rename one, because a reader who greps the document for\n`GetAction` currently gets two incompatible answers with nothing to choose\nbetween them.\n\n## Acceptance\n\nThe two sites agree, and agree with what the emitter emits.\n\nWorth considering while in there: whether the type position can be graded at all\nfor these signatures. If it can, this is the fixture for it. If it cannot —\n`gqlc-vu7z` records why multi-parameter signatures are hard to grade without a\nhand-maintained exclusion list — then say so and close this on the correction\nalone rather than leaving an implied promise of a fence that is not coming.\n\nFiled by the coordinator from `linus-rz0l-r2`'s non-blocking findings (NB-6),\nwho flagged it as pre-existing and off-axis so it would not be lost.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T10:01:11Z","created_by":"areqag","updated_at":"2026-08-04T21:44:37Z","external_ref":"https://github.com/areqag/gqlc/issues/781","dependencies":[{"issue_id":"gqlc-bst4","depends_on_id":"gqlc-rz0l","type":"discovered-from","created_at":"2026-08-04T06:01:11Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-e143","title":"spec: prose can redefine the argument name under an intact code span the fence grades","description":"The spec fence grades **identifiers** — the argument name a signature, a\n`\u003cparam-list\u003e` bullet or a binding map literal spells. It does not read the\nprose around them, and prose can contradict the code span it sits under.\n\nReviewer's mutation, green against the fence at `163d1873`:\n\nLeave `` `, arg \u003cT\u003e` `` intact in C1 §5.3 and rewrite the sentence beneath it to\n\n\u003e the argument name is `arg` where the query parameter is named `arg`;\n\u003e otherwise it is the parameter field-name mangle\n\nThe graded span still says `arg`, so every assertion passes — and the document\nnow specifies the capture vector `gqlc-rz0l` removed, in the section that is\nthis repository's statement of the rule.\n\n## Why it is filed rather than fixed\n\nOut of the fence's declared scope, and the fence says so rather than\nover-claiming. Closing it means grading natural language against an emitter,\nwhich is not a thing a Go test can do honestly. Every cheap approximation —\nkeyword blacklists, \"no sentence near this span may contain 'derive'\" — is a\nguard that passes over the rewordings nobody predicted and fails over innocent\nprose, which is worse than the honest gap.\n\nNoted rather than closed so no reader mistakes the fence for a claim about the\nwhole document.\n\n## If it is taken\n\nThe tractable half is narrowing what prose *can* say by giving the rule one\ncanonical statement and having every other mention point at it rather than\nrestate it. A document with one place to change is one a reviewer can watch.\nThat is an editorial change to `docs/specs/`, not a test.\n\nDo not close this by adding a fence that greps prose for suspicious words. The\nrepo's recurring defect class is a guard green over nothing, and a\nkeyword-matcher over English is that shape by construction.\n\n## Related\n\n`gqlc-vu7z` — the fence grades only one-parameter signatures.\n`gqlc-173n` — the bind sweep grades every map literal under `docs/` and cannot\nsee a deref.\n\nFiled by the coordinator from `linus-rz0l-r2`'s non-blocking findings (NB-3).\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T10:01:09Z","created_by":"areqag","updated_at":"2026-08-04T21:44:38Z","external_ref":"https://github.com/areqag/gqlc/issues/782","dependencies":[{"issue_id":"gqlc-e143","depends_on_id":"gqlc-rz0l","type":"discovered-from","created_at":"2026-08-04T06:01:09Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-9xy0","title":"codegen/age: the edge-union dispatch is emitted in a query method, so 05tl's gate cannot see it","description":"`gqlc-05tl` gates emitted label guards on reachability: a `switch label { case\n\"X\": ... }` whose case no value can satisfy is dead code the generator emitted\nat exit 0, which ADR 0025 already treats as a defect (\"it emits a column no\ndecoder can fill at exit 0\").\n\nThe gate sweeps `decode\u003cT\u003e` function bodies. AGE's edge-union dispatch is not\nin one. `internal/codegen/age/render_queries.go:477` emits the same\n`switch label { case ... }` shape **inside a query method**, so the sweep does\nnot read it and the gate's reachability claim does not cover it.\n\nSame defect class the gate exists to close, one emission site over.\n\n## Check first whether this is already moot\n\n`gqlc-35yu.14` **deletes** AGE's edge-union emission — `writeEdgeUnionDecode`,\n`columnName`, `labelName` and `edgeKeyToEntityName` all go — because AGE 1.7.0\nrejects relationship-type alternation outright (SQLSTATE 42601), so the decoder\nwas for a query the backend can never run.\n\nIf `.14` has merged, `render_queries.go:477` may no longer exist. **Do not\nclose this bead on that alone.** The finding is that the sweep is keyed on\n`decode*` bodies while emission is not confined to them; if the one site that\ndemonstrated it is gone, the gate is still keyed the same way and the next\nin-method emission is still invisible. Confirm whether any in-method `switch\nlabel` remains on any backend before deciding which of the two this is.\n\n## Fix\n\nEither widen the sweep to every emitted `switch` on a label regardless of the\nfunction it sits in, or state the boundary in the gate's own docstring so no\nreader takes it for a whole-tree claim. The first is preferable; the second is\nacceptable only if the first is genuinely out of reach, and then the docstring\nmust say what escapes rather than what is covered.\n\n## Acceptance\n\nA label guard emitted inside a query method, unsatisfiable by any value on its\naxis, fails the gate.\n\nThe regression must redden when the widening is reverted — a gate that passes\nbefore and after is the exact shape `gqlc-05tl` was filed against, and would be\nself-undermining here.\n\nNote the axis rule `gqlc-05tl` round 2 established: a node decoder's guard is\nsatisfiable by a node label, an edge decoder's by an edge label, and the two\nalphabets are **not** unioned. Whatever reads this site must be held to the same\nper-axis rule.\n\nFiled by the coordinator from `linus-05tl-r1`'s findings.\n","notes":"MEASURED 2026-08-16 on fix/emitted-decoder-reachability @85e5749d, answering this bead's own 'check first whether this is already moot'.\n\nAGE's site is GONE. gqlc-35yu.14 merged; internal/codegen/age/render_queries.go emits no 'switch label' at any width, and AGE refuses an edge-union column whole on codegen.ErrUnrepresentableEdgeUnion, so no AGE emission carries one.\n\nneo4j's site REMAINS, and it is the live instance of this bead. Both walkListElemBody (internal/codegen/neo4j/render_queries.go:729, emits at :775) and writeEdgeUnionDispatchBody (:857, emits at :861) write 'switch rel.Type { case \"\u003crelationship type\u003e\": ... }' into a query METHOD. packageLevelFuncs skips a receiver form, so the sweep does not read it. Confirmed in the golden: test/data/codegen/valid/edge_union_many_two_candidates/golden/neo4j-go-v5/queries.cypher.go.\n\nSo this is your second case, not the first: the gate is still keyed the same way and the next in-method emission is still invisible.\n\nThe branch takes fix option 2 (state the boundary in the gate's own docstring, naming what escapes) at commit d1542b3a. Option 1 -- widen the sweep to every emitted switch on a label regardless of enclosing function -- is NOT done and this bead stays open for it. Note the branch's docstring previously named AGE's render_queries.go:477 as the escaping site; that citation was stale and is corrected to neo4j's two.\n\nUnder 'just test' the only thing beneath an unsatisfiable case label there is a golden byte diff, which go test -update blesses away. The run that would catch it is edgeUnionDispatch in test/data/codegen/live_test.go, behind the codegen_live build tag and outside the default suite.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:45:56Z","created_by":"areqag","updated_at":"2026-08-16T23:39:20Z","external_ref":"https://github.com/areqag/gqlc/issues/783","dependencies":[{"issue_id":"gqlc-9xy0","depends_on_id":"gqlc-05tl","type":"discovered-from","created_at":"2026-08-04T05:45:55Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-o8oc","title":"resolver: an OPTIONAL edge in a proven group is a witness NarrowPluralEndpoints throws away","description":"`NarrowPluralEndpoints` skips every edge binding that is nullable, so an\nOPTIONAL edge never constrains its endpoints. That is the conservative fix\n`gqlc-0tft` shipped, and it is correct: an edge that may not exist on a\nsurviving row is not evidence about that row's node type.\n\nBut it is *more* conservative than the truth. `DemoteNullability` at\n`internal/resolver/scope.go:616` walks the identical shape and guards it with\n\n    (e.Nullable() \u0026\u0026 !demotedGroups[...]) || !qualifiedDemoter(e)\n\n— the extra clause being the point. An OPTIONAL edge **inside a proven group**\ngenuinely is a witness: the group's demotion is exactly the finding that every\nsurviving row carries that edge. `NarrowPluralEndpoints` throws that away.\n\n## Why it was not taken with the fix\n\nRuled by the coordinator during `gqlc-0tft`'s review round. The refinement is a\n*further widening* of what narrows, and a widening shipped without coverage is\nprecisely how the original defect arrived — `NarrowPluralEndpoints` was green\nwith the bug and green with the fix, which is to say the branch had zero\ncoverage for the distinction it got wrong. Fixing an unwitnessed widening with a\nsecond unwitnessed widening is not a fix.\n\nSo the plain guard shipped, and the precision is filed here to be taken with\nits own fixtures.\n\n## Fix\n\nAdopt `DemoteNullability`'s guard verbatim rather than paraphrasing it — the two\nsites are answering the same question about the same shape, and a guard that\ndrifts from its sibling is how the next one of these gets written.\n\nThe `demotedGroups` set has to reach `NarrowPluralEndpoints`; check whether the\ncall order already computes it before the narrow pass runs, and say so\nexplicitly if it does not, because reordering the two passes is a larger change\nthan this bead's headline.\n\n## Acceptance\n\nThe load-bearing half is the fixtures, not the guard. Required, in both\ndirections:\n\n- An OPTIONAL edge in a **proven** group narrows its endpoints — the column that\n  is now correctly NOT NULL is null-typed without the refinement.\n- An OPTIONAL edge in an **unproven** group still does not narrow — this is the\n  `gqlc-0tft` case and must not regress.\n\nEach fixture must redden when the corresponding half of the guard is deleted.\nA fixture that passes with and without the refinement is not coverage for it,\nand this bead exists because that is what happened last time.\n\nFiled by the coordinator from `linus-0tft-r1`'s findings.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:45:54Z","created_by":"areqag","updated_at":"2026-08-04T21:44:39Z","external_ref":"https://github.com/areqag/gqlc/issues/784","dependencies":[{"issue_id":"gqlc-o8oc","depends_on_id":"gqlc-0tft","type":"discovered-from","created_at":"2026-08-04T05:45:53Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7he2","title":"resolver: a Temporal kind declared through an alias or as a bare untyped const escapes the enum sweep","description":"`declaredTemporals` (`internal/codegen/conformance/assembled_input_test.go`)\nderives `resolver.Temporal`'s membership by parsing source, because\ncompilation erases the fact it needs: a constant the block names and\n`Temporal.String` has no arm for is, to anything running, identical to a\nvalue nobody declared. Membership is decided syntactically — a const spec\nwhose written type is the identifier `Temporal`, or which inherits that\ntype from the `iota` anchor's declaration.\n\nTwo shapes satisfy the compiler and escape the sweep. Both were reproduced\nlive in the `linus-h4ug-r9` round: each compiles, each declares a seventh\nkind with no `String()` arm, and `just test` stays green over both. In each,\nthe kind renders as `Temporal(6)`, `firstUndeclaredTemporal` hands the\n`list-elem-temporal` case a value that IS declared, and nothing says so.\n\n## Through a type alias\n\n    type temporalAlias = Temporal\n    const TemporalWeek temporalAlias = 6\n\nThe written type is `temporalAlias`, so the sweep does not recognise it;\nthe alias makes it the same type to the compiler, so `case TemporalWeek:`\ncompiles in a switch over the enum.\n\n## As a bare untyped declaration\n\n    const ( TemporalWeek = 6 )\n\nas its own declaration, not sharing the `iota` anchor's. Untyped integer\nconstants are assignable to `Temporal`, so `case TemporalWeek:` compiles.\nThe existing untyped guard refuses an untyped line only WHERE IT SHARES THE\nANCHOR'S DECLARATION — a standalone one has no anchor to inherit from and\nno anchor to be refused against.\n\n## Why filed rather than fixed\n\nCoordinator ruling. The third escape the reviewer found — a\n`TemporalWeek Temporal = 6` in another file of package `resolver` — WAS\npromoted and fixed, because the sibling fence on the same branch\n(`scanSources`, `internal/codegen/errors_test.go:789`) already sweeps every\nnon-test file in its package and its docstring states the reason in terms\nthat transfer word for word.\n\nThese two do not transfer. Resolving an alias syntactically means chasing\nalias declarations and getting it wrong in the general case; doing it\nproperly means type information, which is a different kind of test than a\nsource sweep. And fencing the bare untyped form in general means refusing\nevery untyped integer constant in the package, since any of them is\nassignable to `Temporal` — unbounded, and it would fire on unrelated code.\n\nThe code states both boundaries honestly rather than over-claiming, which\nis the reason these are P3 and not blocking: `temporalsOf`'s docstring says\nthe untyped refusal applies \"wherever it shares the anchor's declaration\".\nA reader is not misled about what is fenced.\n\n## If taken\n\n`go/types` over the loaded package rather than `go/parser` over the files\nanswers both at once, and would also subsume the file-level widening. Weigh\nthat against the cost: the sweep currently needs no build, which is part of\nwhy it can run inside the conformance suite.\n\nFiled by the coordinator from `linus-h4ug-r9`'s non-blocking finding (a),\nescapes 2 and 3.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:31:31Z","created_by":"areqag","updated_at":"2026-08-04T09:34:39Z","external_ref":"https://github.com/areqag/gqlc/issues/771","dependencies":[{"issue_id":"gqlc-7he2","depends_on_id":"gqlc-h4ug","type":"discovered-from","created_at":"2026-08-04T05:31:39Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-173n","title":"spec: the fence's bind sweep grades every map literal under docs/, and cannot see a deref","description":"Two reach limits in `scanSpecBinds`, `internal/codegen/conformance/specfence_test.go`.\nBoth were measured in the `linus-rz0l-r1` round; neither is live today.\n\n## `unwrapConversions` strips `*`, so a deref is invisible (his #17)\n\nThe sweep reduces a binding expression to the identifier it ultimately\nreads, peeling carrier conversions: `float64(arg)` binds `arg`. It also\npeels `*` and `\u0026`, so\n\n    map[string]any{\"x\": float64(*arg)}\n\nreduces to `arg` and passes.\n\nThis is DECLARED rather than concealed — C1 §5.3 scopes the fence to the\nargument NAME, and `*arg` does carry the right name. But it is worth\nrecording because of what sits next to it. The `gqlc-rz0l` branch corrected\nC3 §5.7 out of scope, precisely because a mechanical rename there would have\nproduced `float64(*arg)`, which is both wrong (a nullable parameter binds\nbare — `paramBindExpr` at `internal/codegen/neo4j/render_queries.go:428`\nreturns before `driverCarrier` is reached) and a nil panic at runtime. So the\none correction on that branch whose whole point was the deref is unfenced\nagainst its own failure mode.\n\nFix, if taken: grade the binding SHAPE for nullable parameters as well as\nthe name — a documented `map[string]any` entry whose expression derefs is\ndrift against `paramBindExpr`'s nullable-first ordering, regardless of what\nidentifier it names.\n\n## The sweep grades every `map[string]any{` under `docs/` (his #39)\n\n`docRoots` is the whole of `docs/` plus `README.md` and `CONTEXT.md`, and\n`mapAnchor` is the bare literal `map[string]any{`. Any such literal in any\nmarkdown file is graded as a documented parameter binding. A future ADR or\ndesign note showing\n\n    opts := map[string]any{\"debug\": true}\n\nfails `TestSpecParamsMapBindsGeneratorOwnedValue` and is told its\n\"documented parameter binding is not generator-owned.\"\n\nThis FAILS CLOSED, so it is a maintenance annoyance rather than a hole — the\ndanger is that whoever hits it responds by narrowing `docRoots`, and the\nbreadth of `docRoots` is load-bearing (the original drift reached C1, C3, C4\nand C5, and an ADR prints the same signatures). Verified as a live mutation:\nan unrelated literal planted in a doc reddens the sweep.\n\nFix, if taken: require the literal to sit in a binding context — adjacent to\na graded signature, or inside a section the spec marks normative — rather\nthan widening the identifier allowlist, which would weaken the sweep in the\ndirection that matters.\n\nFiled by the coordinator from `linus-rz0l-r1`'s non-blocking findings #17\nand #39.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:29:10Z","created_by":"areqag","updated_at":"2026-08-04T09:34:40Z","external_ref":"https://github.com/areqag/gqlc/issues/772","dependencies":[{"issue_id":"gqlc-173n","depends_on_id":"gqlc-rz0l","type":"discovered-from","created_at":"2026-08-04T05:29:19Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vu7z","title":"spec: the spec fence grades only one-parameter signatures, so multi-parameter drift is unswept","description":"`internal/codegen/conformance/specfence_test.go` pins the documented method\nargument name against `codegen.ParamArg`, closing the capture vector\n`gqlc-lhs3` removed from the emitter. It grades a signature only when the\nparameter list splits into exactly two:\n\n    params := splitTopLevel(list)\n    if len(params) != 2 {\n        continue\n    }\n\nSo a documented signature carrying the query author's parameter names as\nseparate bare arguments —\n\n    PeopleOverAge(ctx context.Context, minAge int64, locale string)\n\n— is skipped. The emitter renders two-or-more parameters as a single\n`arg \u003cMethodName\u003eParams` struct, so that signature is drift, and it is the\nnaive sqlc-shaped form a reader would plausibly write. Verified as a live\nmutation (#31 in the `linus-rz0l-r1` round): the suite stays green over it.\n\n## Why it is filed rather than fixed\n\nClosing it means telling an emitted query method apart from the other\ndocumented functions that also open `(ctx context.Context`. There are at\nleast two classes in `docs/` today: the 4-parameter `driverOrTx.run` seam,\nand the godog step handlers. Neither is an emitted query method and both\nwould false-positive.\n\nThe only cheap discriminator is a hand-maintained exclusion list. That is\nthe same defect class this epic has already filed four beads against — a\nhand-maintained set that silently shrinks, turning the gate off with no\ndiff to the gate (`gqlc-df3d`'s `-run` allowlist is the closest sibling).\nA fence that false-positives also gets neutered by whoever it annoys first,\nwhich is strictly worse than an honest gap.\n\n## Fix\n\nNeeds a real discriminator, not a list. Options worth weighing: anchor on\nthe receiver (`func (q *Queries)`) plus the `WriteQuerier`/`Querier`\ninterface blocks, which is where every emitted query method is declared and\nwhere nothing else is; or grade only signatures inside sections the spec\nalready marks normative. Whichever is chosen, the property to keep is the\none the fence already has: it must redden when it stops matching, rather\nthan grading zero sites and passing.\n\nNote the arity rule itself is simple once the discriminator exists — for an\nemitted query method, ANY parameter list longer than `(ctx, arg T)` is\ndrift, because the emitter has no such form.\n\nFiled by the coordinator from `linus-rz0l-r1`'s non-blocking finding #31.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:28:52Z","created_by":"areqag","updated_at":"2026-08-04T09:34:40Z","external_ref":"https://github.com/areqag/gqlc/issues/773","dependencies":[{"issue_id":"gqlc-vu7z","depends_on_id":"gqlc-rz0l","type":"discovered-from","created_at":"2026-08-04T05:29:18Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lvmr","title":"ci: the lint-hooks reachability walk accepts prose as a CI entry point, and cannot see a .yaml workflow","description":"`.githooks/tests/lint-hooks-test.sh` proves that some recipe CI actually runs\nreaches `lint-hooks` transitively, so shellcheck is not merely present in the\njustfile but wired to a job. It derives the entry-point set by grepping\n`just \u003crecipe\u003e` out of `.github/workflows/*.yml` after stripping comments.\n\nTwo ways the derived set is wrong. Neither is live today — I enumerated every\n`just` occurrence across all four workflow files — and the real regression\npath (deleting the `- run: just lint` step) is caught.\n\n## Comment stripping is whole-line only\n\nThe strip is `grep -hvE '^[[:space:]]*#'`. It correctly removes the two prose\n`just lint` mentions at `ci.yml:40-41`, which is the hazard it was built for.\nIt does not remove a trailing comment on an otherwise-live line, so\n\n    - run: just tidy   # just lint\n\nis accepted as an entry point that CI does not run. Nor does it distinguish a\nYAML key from a command, so\n\n    - name: just lint\n\nis accepted too. Both verified as live mutations (M11c, M11d in the\n`linus-ghsync-r2` round): the walk starts from a recipe CI never invokes and\nthe guard goes green.\n\nThe direction matters — a FALSE entry point makes the guard pass over a\nsevered edge. That is fail-open. Contrast the `.yaml` case below.\n\n## The glob is `*.yml`\n\nA workflow added as `.yaml` is invisible to the walk. This direction fails\nSAFE: fewer entry points means the walk is more likely to report UNREACHED\nand redden. Worth fixing at the same time because it is the same line, not\nbecause it is dangerous.\n\n## Fix\n\nMatch a command position rather than a substring — the entry points live in\n`run:` scalars, so read those rather than grepping the file flat. Whatever\nshape it takes, the property to keep is the one M12 already pins: deleting\nthe real `- run: just lint` step while leaving the prose at `ci.yml:40-41`\nmust still redden.\n\nWiden the glob to `*.yml` and `*.yaml` in the same change.\n\n## Why filed rather than fixed\n\nCoordinator ruling on `fix/bd-gh-sync-listing-and-shellcheck`. The guard is\nnew on that branch and holds under every mutation of the thing it protects\n(M9 severs the sole CI-to-shellcheck edge and it reddens, naming all twelve\nentry points). These are latent reach limits in the derivation, not a defect\nin the guard, and closing them properly means parsing YAML structure rather\nthan tightening a regex.\n\nFiled by the coordinator from `linus-ghsync-r2`'s non-blocking finding NB-6.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:23:09Z","created_by":"areqag","updated_at":"2026-08-04T09:34:41Z","external_ref":"https://github.com/areqag/gqlc/issues/774","dependencies":[{"issue_id":"gqlc-lvmr","depends_on_id":"gqlc-jhi2","type":"discovered-from","created_at":"2026-08-04T05:23:15Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ybzk","title":"ci: the bd-gh-sync remedy guard reads only stderr and matches two tokens, behind a failure message that claims semantics","description":"`.githooks/tests/bd-gh-sync-test.sh`'s guard over the push-path remedy text\nexists to stop a specific false instruction coming back — advice telling the\nreader to wait for the bd database to unlock, when the database is not the\nreason the push refused to count. It has two blind spots, neither live today,\nboth narrower than the failure message it prints.\n\n## It matches two tokens, not a meaning\n\nThe negative arm is `grep -qiE 're-run|the database is free'`. Its failure\nmessage reads \"the remedy sends the reader to wait on a database that is not\nthe reason\" — a claim about meaning, checked by a two-token pattern.\n\n    Wait for the bd database to unlock, then push again\n\nis the identical false advice and contains neither token. Verified as a live\nmutation (M5 in the `linus-ghsync-r2` round): the suite reports 124 passed,\n0 failed over it.\n\nNo grep can pin a paraphrase, so this is not straightforwardly fixable and\nmay not be worth fixing. What IS fixable is the mismatch between what the\nmessage asserts and what the pattern can support. The realistic regression —\na revert, or a verbatim re-add — is caught twice over (M4 and M8 both kill),\nand reflowing the original wording at different word boundaries leaves\n`re-run` intact (M6 kills).\n\n## It reads only `$TMP/err`\n\nBoth arms grep `$TMP/err`. The same false advice emitted verbatim without\n`\u003e\u00262` lands on stdout and is invisible to the guard (M7: 124 passed, 0\nfailed). A pre-push hook shows stdout to the operator just as plainly as\nstderr, so the reader is misled either way.\n\nThis is the uniform convention of the whole file — every assertion in it\ngreps `$TMP/err` — so changing it at this one site alone would be worse than\nleaving it. If it is worth closing, close it file-wide or not at all. It also\nneeds a compound regression to trip: re-add the line AND drop the redirection.\n\n## Why filed rather than fixed\n\nCoordinator ruling on `fix/bd-gh-sync-listing-and-shellcheck`. The branch's\nthree guards all hold under 34 mutations; these are reach limits, not\ndefects, and both need a regression that does not resemble the one the guard\nwas built for. Promoting them would have meant either an unfixable ask (the\nparaphrase) or a file-wide convention change smuggled into a bug-fix branch.\n\nFiled by the coordinator from `linus-ghsync-r2`'s non-blocking findings NB-1\nand NB-2.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:22:50Z","created_by":"areqag","updated_at":"2026-08-04T09:34:42Z","external_ref":"https://github.com/areqag/gqlc/issues/775","dependencies":[{"issue_id":"gqlc-ybzk","depends_on_id":"gqlc-mbe0","type":"discovered-from","created_at":"2026-08-04T05:23:15Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-eo74","title":"codegen: the decoder reachability gate checks each label guard alone, so a conjunction of two can be dead","description":"`gqlc-05tl`'s `TestEmittedDecodersGuardOnlyOnStampableLabels` asserts that every\nstring literal an emitted `decode\u003cT\u003e` compares for equality is a label the\nfixture's schema declares. That is a **per-literal** check, and its doc states\nit as such honestly.\n\nFound by `linus-05tl-r1b` as mutation M19b.\n\n## The hole\n\nTwo guards can each name a declared label and still be jointly unsatisfiable:\n\n    func decodePerson(raw []byte) (Person, error) {\n        label, props, err := agtypeEntity(raw, \"::vertex\")\n        ...\n        if label != \"Person\" { return Person{}, ... }\n        if label != \"Post\"   { return Person{}, ... }\n\n`label` is one string. It cannot be both. Every row takes one of the two early\nreturns, so `decodePerson` is dead on arrival -- the exact condition the gate\nexists to refuse -- and the gate is **green**, because \"Person\" and \"Post\" are\nboth declared.\n\nThe gate reasons about each literal against the alphabet. It does not reason\nabout the guards as a conjunction.\n\n## Why P3\n\nThe emitter does not currently produce this shape, so it is latent rather than\nlive. The reviewer classified it non-blocking on those grounds and I agree.\n\nWhat makes it worth filing anyway is that it is the SAME defect one level up: a\nguard that cannot fail, in the gate built to find guards that cannot succeed. The\ngate's power is bounded by the granularity of its reasoning, and nothing in the\ntree records that bound where a future reader would look before trusting it.\n\n## Fix\n\nEither widen the gate to decide satisfiability over the conjunction of guards in\na decoder body rather than each literal alone, or -- if that is disproportionate\n-- state the bound explicitly in the gate's 'What it does not decide' section,\nwhich is where its other four exclusions already live.\n\n**Prefer widening.** That section is already carrying one falsehood this epic had\nto correct (`gqlc-05tl` round 2 fixed a claim there that was the opposite of what\nthe code did), and adding a fifth exclusion to a list that has proven hard to keep\ntrue is the weaker move.\n\n## Acceptance\n\nEmit the two-guard shape and confirm the gate FAILS. Today it passes.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T09:10:11Z","created_by":"areqag","updated_at":"2026-08-04T09:34:43Z","external_ref":"https://github.com/areqag/gqlc/issues/776","dependencies":[{"issue_id":"gqlc-eo74","depends_on_id":"gqlc-05tl","type":"discovered-from","created_at":"2026-08-04T05:10:25Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-owlk","title":"ci: ensure-shellcheck hardcodes linux.$(uname -m), so a non-Linux host silently gets no linter","description":"The `ensure-shellcheck` recipe fetches a release archive whose name it builds as\n`linux.$(uname -m)`. The architecture is derived; the OS is a literal.\n\nOn any non-Linux host the fetch names an archive that does not exist. Depending\non how the recipe handles that, the developer either gets a confusing download\nerror or -- the case that matters -- gets no shellcheck and a lint step that\npasses over unlinted shell.\n\n## Why it is P3 and not higher\n\nCI runs Linux, so the enforcing path is unaffected. This is a local-development\ndefect: the person most likely to hit it is someone on macOS editing\n`.githooks/`, who would then be shipping shell that only the CI run lints.\n\n## Why it is worth filing anyway\n\n`gqlc-jhi2` was filed on the premise that 'a disable directive in a tree with no\nlinter is a comment', and the fix wired shellcheck into CI through\n`justfile`'s `lint: ensure-golangci lint-hooks` edge. That closed the CI half.\nThis is the same sentence about a developer's working copy.\n\nThe failure is also silent in the direction that matters. A developer who never\nhad shellcheck does not see a warning that they are unlinted; they see a green\n`just lint`.\n\n## Fix\n\nDerive the OS the way the architecture is already derived, and -- more\nimportantly than supporting every platform -- **fail loudly on a platform the\nrecipe cannot serve** rather than proceeding without a linter. Refusing to lint\nis an acceptable outcome; silently not linting is not.\n\nCheck `ensure-golangci` in the same pass. If it has the same shape, it has the\nsame defect, and the point is the rule rather than the one site.\n\n## Acceptance\n\nRun the recipe with `uname` stubbed to report a non-Linux OS and confirm it\nexits non-zero with a message naming the platform, rather than exiting 0 with no\nshellcheck present.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T08:51:30Z","created_by":"areqag","updated_at":"2026-08-04T09:34:43Z","external_ref":"https://github.com/areqag/gqlc/issues/777","dependencies":[{"issue_id":"gqlc-owlk","depends_on_id":"gqlc-jhi2","type":"discovered-from","created_at":"2026-08-04T04:51:37Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2ug9","title":".claude/settings.json pipes bd-gh-sync pull through tail, reporting tail's exit status","description":"`.claude/settings.json` invokes:\n\n    bd-gh-sync pull 2\u003e\u00261 | tail -1\n\nA pipeline reports the **last** command's exit status, so this reports `tail`'s,\nwhich is 0 essentially always. If `bd-gh-sync pull` exits non-zero, the hook does\nnot find out.\n\nThis is the exact class this repo has been bitten by three times now — most\nrecently `gqlc-nsq4`, where `(...) 2\u003e\u00261 | tee \"${log}\" || rc=$?` in the `vuln`\nrecipe was correct only by virtue of a `set -o pipefail` fifty lines away, and\nwas converted to `out=\"$(cmd 2\u003e\u00261)\" || rc=$?` for that reason.\n\n## Why it is P3\n\nHarmless *today*, and harmless by design rather than by luck: `bd-gh-sync pull`\nalways exits 0 on purpose, because a pull that cannot reach GitHub must not break\nthe developer's `git pull`. So there is currently no status for `tail` to\nswallow.\n\nThat is a property of the script, asserted nowhere, sitting behind a pipeline\nthat would silently discard the status the moment it changed. `gqlc-nvjz` is\nopen against that same pull path and one of its candidate fixes is a BLIND exit\n— which is precisely a non-zero status this pipeline would eat.\n\nSo the ordering matters: if `gqlc-nvjz` lands a non-zero pull exit first, this\nstops being latent.\n\n## Shape of the fix\n\n`out=\"$(bd-gh-sync pull 2\u003e\u00261)\"; rc=$?; printf '%s\\n' \"${out}\" | tail -1; exit \"${rc}\"`\n— or whatever the settings schema permits. Whichever form, the status that\nreaches the caller must be the script's.\n\nAlso worth checking the other hook invocations in that file for the same shape\nrather than fixing this one site.\n\n## Acceptance\n\nMake `bd-gh-sync pull` exit non-zero under a stub and confirm the hook sees it.\nA fix with no such demonstration is a change nothing measures — and this repo's\nown history is that pipeline-status bugs are found by executing them, not by\nreading them.\n\nFiled by the coordinator from `carmack-ghsync-p3b`'s out-of-scope list.\nPre-existing; not introduced by that branch.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:49:13Z","created_by":"areqag","updated_at":"2026-08-04T08:40:48Z","external_ref":"https://github.com/areqag/gqlc/issues/763","dependencies":[{"issue_id":"gqlc-2ug9","depends_on_id":"gqlc-mbe0","type":"discovered-from","created_at":"2026-08-03T20:49:12Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ye6g","title":"walkListElemBody's innerAcc is a fixed name, one nesting level below the bug ibg5 fixed","description":"`gqlc-ibg5` fixed `writeListColumnDecodeIndent`, which hardcoded the\naccumulator variable name `acc`, so two non-nullable list columns in one row\nemitted `acc := ...` twice and the generated file did not compile:\n\n    ./corpus_queries.cypher.go:124:7: no new variables on left side of :=\n\nThe fix derives the name: `\"acc\" + strings.TrimPrefix(varName, \"value\")`.\n\n`walkListElemBody`'s `innerAcc` is the same shape one level down and was left\nalone.\n\n## Why it is currently safe, and why that is thin\n\n`innerAcc` sits inside a per-iteration loop body, so a deeper nesting opens a\nnew scope and *shadows* rather than redeclares. That is a real argument and it\nis why this is P3 rather than P2.\n\nIt is thin because the property it rests on is lexical scoping of the emitted\ncode, not anything the emitter checks. Two **sibling** nested lists rendered\ninto the same block — as opposed to one inside the other — would collide the\nsame way `acc` did. Whether the emitter can currently produce that shape is the\nquestion this bead has to answer, and \"I could not construct one\" is a\ndifferent finding from \"the emitter cannot\".\n\n## Shape of the fix\n\nIf the sibling shape is reachable: derive `innerAcc` the same way `acc` is now\nderived, and add a fixture with two sibling nested list columns in one row.\n\nIf it is not reachable: say so in a comment at the declaration, naming the\ninvariant that makes it unreachable and what would have to change for it to\nbecome reachable. A fixed name that is safe by accident and a fixed name that\nis safe by argument look identical in the source, which is why the argument has\nto be written down.\n\nDo not fix it by renaming and stopping. `gqlc-ibg5` was found by a fixture that\ncompiled the emitted output; this needs the same. A rename with no fixture that\nwould have caught the collision is a change nothing measures.\n\n## Acceptance\n\nA fixture with two sibling nested lists in one row, and a demonstration that\nreverting the derivation makes it fail to compile — or a recorded argument that\nthe shape is unreachable, with the invariant named.\n\nFiled by the coordinator from `carmack-dcap-ibg5`'s out-of-scope list.\n","notes":"CORRECTION (2026-08-04, coordinator). This bead's own 'why it is currently safe' section is WRONG, and the reviewer proved it. It is not latent. It is broken today.\n\nlinus-dcap-ibg5-r1 constructed the shape and got a real compile failure at nesting depth \u003e= 3:\n\n    ./corpus_queries.cypher.go:302:33: cannot use innerAcc (variable of type []int64) as int64 value in argument to append\n\nThe shadowing argument is the defect, not the mitigation. The inner 'innerAcc :=' shadows the outer one, so 'append(accVar, innerAcc)' resolves BOTH operands to the inner variable. The outer accumulator is not merely hidden -- it becomes unreferenceable at exactly the point the emitted code needs to append to it. So the scoping property the description leaned on is what breaks it.\n\nRaised P3 -\u003e P2 accordingly.\n\nThe fix already exists in the tree, one axis over: writeSliceNarrow on the entity path suffixes its locals 'acc%d' BY DEPTH and works correctly. walkListElemBody should do the same. Note that gqlc-ibg5's own fix (deriving from varName via strings.TrimPrefix) is the COLUMN-position axis and does not generalise to depth -- the two need different suffixes because they number different things.\n\nAcceptance must include a fixture at depth \u003e= 3, since depth 2 does not reach it.","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:48:14Z","created_by":"areqag","updated_at":"2026-08-17T05:27:54Z","closed_at":"2026-08-17T05:27:54Z","external_ref":"https://github.com/areqag/gqlc/issues/764","metadata":null,"dependencies":[{"issue_id":"gqlc-ye6g","depends_on_id":"gqlc-ibg5","type":"discovered-from","created_at":"2026-08-03T20:48:13Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4int","title":"ci: test-codegen-live-neo4j omits -count=1, so a live green can be a cache hit","description":"`justfile:186`, the `test-codegen-live-neo4j` recipe, runs `go test` without\n`-count=1`. The other live recipes pass it.\n\nThe comment justifying the omission argues from Go's test cache key, and that\nargument is sound **as far as it goes**: the key covers the test binary and its\ninputs, so an unchanged tree legitimately serves a cached result.\n\nThe gap is the one the comment itself concedes: **the cache key does not\ninclude the container runtime.** A live suite's real input is a testcontainers\nPostgres or neo4j instance — its image, its extension version, its state. None\nof that is in the key. So a green here can be a result computed against a\ncontainer that no longer exists, or against a different image than the one\ncurrently pinned.\n\nThis repo has already been bitten by exactly this: a live suite reported\n`ok ... 0.2s`, which is not a duration in which a container starts. That is how\nthe missing `-count=1` was found elsewhere.\n\n## Why it is filed rather than fixed\n\nPre-existing on master. The `gqlc-35yu.14` branch only rewrites the comment\nthat justifies it, and rewriting a justification is not the same as accepting\nthe thing justified — but changing recipe behaviour was out of that branch's\nscope.\n\n## Fix\n\nAdd `-count=1`, and delete the cache-key argument rather than amending it. The\nargument is not wrong about Go; it is answering a question about the wrong\ninput set, and a comment that reasons correctly to a wrong conclusion is worse\nthan none.\n\nCheck the other live recipes at the same time — the point is the rule, not the\none site.\n\n## Acceptance\n\nEvery live recipe passes `-count=1`. Verify by running one twice in a row and\nconfirming the second run is not instant. A live suite reporting a sub-second\n`ok` has served a cache hit and measured nothing.\n\nFiled by the coordinator from `linus-35yu14-r5`'s non-blocking findings.\nAdjacent to `gqlc-df3d` (live recipes select tests by a hardcoded `-run`\nallowlist) — same recipes, different defect. Check whether they should be taken\ntogether.","notes":"ADJUDICATED on branch fix/live-recipe-structural-selection (PR #882, bd gqlc-df3d).\nDecision: do NOT add -count=1 to test-codegen-live-neo4j. No `Closes #765` on the\nPR — the coordinator should close this as adjudicated.\n\nWHAT WAS MEASURED (go1.26.6, `go version` in the repo):\n\n1. Env vars read during a run ARE part of the test cache key. A probe module\n   whose test calls os.Getenv(\"PROBE_SKIP\") was run 7 times:\n     1 unset      -\u003e cold (ran)\n     2 unset      -\u003e (cached)\n     3 PROBE_SKIP=1 -\u003e miss, ran\n     4 PROBE_SKIP=1 -\u003e (cached)\n     5 PROBE_SKIP=2 -\u003e miss, ran\n     6 unset      -\u003e (cached)  [distinct entry from 3 and 5]\n     7 PROBE_SKIP=2 -\u003e (cached)\n   Three distinct values produced three distinct cache entries. So\n   GQLC_SKIP_LIVE=1 cannot poison the entry a real (unset) run serves: the\n   skipped run and the live run key differently.\n   (An earlier probe measured go1.26.5 because the /tmp module's go.mod said\n   `go 1.26`; re-pinned to 1.26.6 and re-run — same result.)\n\n2. Both container images are digest-pinned as compiled-in constants, so an\n   image bump changes the test binary and therefore the key:\n     test/data/codegen/live_neo4j_test.go:40\n     test/data/codegen/live_age_test.go:34\n\n3. What remains outside the key is the container RUNTIME (daemon present,\n   image locally pullable) — which the in-tree comment already conceded.\n\nWHAT WAS ACTUALLY WRONG: not the recipe, the comment. It claimed \"every input\n... is inside the test binary\", which is false in the direction that matters\n(the env vars are inputs, and they are outside the binary but inside the key;\nthe runtime is outside both). The comment on this branch is corrected to name\nthe env-var/cache-key half explicitly rather than overclaiming.\n\nWHY NOT -count=1 ANYWAY: the bead's evidence for a stale green is a live suite\nreporting `ok ... 0.2s`. That shape is a cache hit, and a cache hit here is only\nreachable when the binary, the args and every env var read are identical — i.e.\nwhen nothing about the run changed except the clock. The cost of -count=1 is\npaid on every PR that touches nothing in the live tree. Adding it would trade a\nmeasured, keyed cache for wall time on the argument that the runtime is\nunkeyed; the runtime being unkeyed is a reason to distrust a green whose\nCONTAINER changed, which -count=1 does not detect either (it re-runs against\nwhatever runtime is there).\n\nThe AGE half keeps its -count=1: it is not being removed, and this is a\ndecision about not adding a third.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:34:05Z","created_by":"areqag","updated_at":"2026-08-17T08:02:33Z","external_ref":"https://github.com/areqag/gqlc/issues/765","dependencies":[{"issue_id":"gqlc-4int","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-03T20:34:09Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-4int","depends_on_id":"gqlc-df3d","type":"related","created_at":"2026-08-03T20:34:10Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-etz8","title":"resolver/codegen: the enum-growth fence lives across a package boundary, and Cardinality has none","description":"`gqlc-h4ug` round 6 built a guard that catches a `resolver.Temporal` member\ndeclared without a `String()` arm. It works — three mutations redden it — but it\nlives in `internal/codegen/conformance` and reaches across a package boundary\nthrough a **hardcoded relative path**:\n\n    \"../../resolver/validated.go\"\n\nTwo problems, one of them latent:\n\n## The fence is owned by the wrong package\n\n`resolver` owns the enum. A guard on the enum's shape belongs beside it. As\nbuilt, reorganising the codegen tree — moving the conformance package, renaming\na directory — silently breaks the path, and what breaks is a *derivation*: the\nsuite stops finding the constant block it is supposed to compare against.\n\nRound 6's implementer hard-`Require`s the block shape, so this should fail\nloudly rather than pass vacuously. **Verify that before assuming it.** A parse\nthat yields an empty set and an assertion that compares an empty set to an\nempty set is this project's most-found defect, and a missing file is exactly\nhow it arises.\n\n## `codegen.Cardinality` has no such guard at all\n\nThe same fail-open shape — a named integer enum whose `String()` can be grown\nout of step with its constant block — applies to `codegen.Cardinality`, which\n`docs/specs/codegen-sentinel-taxonomy.md` §2 already carries a row for. Nothing\nguards it.\n\n## Shape of the fix\n\nMove the derivation into `internal/resolver` as a test on its own package, so\nthe path is local and a file move breaks compilation rather than a string. Then\napply the same treatment to `codegen.Cardinality` in `internal/codegen`.\n\nIf a shared helper falls out of doing both, good — but do not build the\nabstraction first. Two similar tests in two packages is the right amount of\nduplication for two enums.\n\n## Acceptance\n\nFor each enum, all three mutations red: a const with no `String` arm; a `String`\narm with no const; well-formed growth of both. And: deleting or moving the file\nthe derivation reads must fail loudly, naming what it could not find — verify by\nmutation, do not assume the `Require` covers it.\n\nFiled by the coordinator from `carmack-h4ug-r6`'s out-of-scope list.\nDiscovered-from `gqlc-h4ug`.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:23:17Z","created_by":"areqag","updated_at":"2026-08-04T08:40:50Z","external_ref":"https://github.com/areqag/gqlc/issues/766","dependencies":[{"issue_id":"gqlc-etz8","depends_on_id":"gqlc-h4ug","type":"discovered-from","created_at":"2026-08-03T20:23:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4lkv","title":"codegen/age: propertyWidths claims to derive from the property table and is a literal","description":"`internal/codegen/age/render_models_test.go:16-22` declares `propertyWidths` and\nsays of it:\n\n    Taken from the property table rather than written out here, so a width\n    the table gains is a row these sweeps gain.\n\nIt is not taken from anything. It is a hand-written 7-element literal with no\ncross-check against `typeMap.Property`. `grep -rn propertyWidths internal/`\nreturns the declaration and two `range`s over it — nothing that compares the\nset to the table it claims to mirror.\n\nSo a Go carrier width the property table gains is a row the two sweeps **do\nnot** gain, and the comment says the opposite.\n\n## Severity\n\nLower than the epic's usual instance of this shape. The sweeps still assert\nsomething over the seven widths they do hold, so this is not a gate that is\ngreen on nothing — it is a gate whose coverage silently fails to grow. The\nblast radius is one width of coverage per new width, not the whole assertion.\n\nIt is filed because the comment makes a **derivation claim** that is false, and\na reader who trusts it will add a width to the table and believe the sweeps\npicked it up.\n\n## Shape of the fix\n\nTwo options, and the second is better:\n\n1. Delete the sentence. Honest, cheap, and leaves the coverage gap.\n2. Make the claim true: derive the row set from the distinct Go carriers\n   `typeMap.Property` admits. Not a `require.Len` — a length pin is the same\n   family of defect this epic keeps finding, and `gqlc-35yu.11`'s own\n   `require.Len(temporalKinds, int(TemporalDuration)+1)` is the cautionary\n   case, where both sides of the assertion moved together so it could never\n   fire.\n\nPrefer 2. Compare the swept set against the derived set as a **set**, so a\nwidth added to the table with no sweep row reddens by name.\n\n## Acceptance\n\nAdd a width to `typeMap.Property` without touching the test, and watch the\nsweep redden naming the new width. Revert.\n\nFiled by the coordinator from `linus-35yu11-r6`'s non-blocking findings.\nDiscovered-from `gqlc-35yu.11`.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:21:05Z","created_by":"areqag","updated_at":"2026-08-04T00:22:07Z","external_ref":"https://github.com/areqag/gqlc/issues/757","dependencies":[{"issue_id":"gqlc-4lkv","depends_on_id":"gqlc-35yu.11","type":"discovered-from","created_at":"2026-08-03T20:21:19Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nuk0","title":"ci: just --fmt --check fails at HEAD and is gated in no job","description":"`just --fmt --check --unstable` exits 1 at HEAD. Measured on `7005805`, so this\npredates the vuln-gate branch and is not something it introduced.\n\nNo CI job runs it. The justfile is therefore unformatted, has been for some\ntime, and nothing in the tree says so — which is the same shape as every other\nfinding in this epic: a property that is supposed to hold, does not, and no\ngate is looking.\n\n## The two halves\n\n**Formatting.** Run the formatter and commit the result. Mechanical, and the\ndiff should be inspected once rather than trusted, because `--unstable` means\nthe formatter's output is not covenanted across `just` releases.\n\n**Gating.** Adding the check to CI pins a formatter whose behaviour is\nexplicitly unstable, so a `just` upgrade on the runner can redden a branch that\nchanged nothing. That is a real cost and it is why this is filed rather than\nfolded into the vuln-gate branch.\n\nThe trade to weigh: an unstable-but-pinned check that occasionally needs a\nreformat commit, versus a justfile that drifts with nothing noticing. The first\nfailure mode is noisy and visible; the second is silent. Recommend gating, with\nthe `just` version pinned in the workflow so an upgrade is a deliberate edit.\n\n## Constraint\n\nDo **not** rename, split, add or remove any job in `.github/workflows/` —\nGitHub derives required check contexts from job ids, and branch protection\nnames them. A new check goes inside an existing job.\n\n## Acceptance\n\n`just --fmt --check --unstable` exits 0 at HEAD, and a deliberate\nmisformatting reddens CI.\n\nFiled by the coordinator from `carmack-vulngate-r2`'s out-of-scope list.\nDiscovered-from `gqlc-rohp`.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:16:13Z","created_by":"areqag","updated_at":"2026-08-04T00:22:08Z","external_ref":"https://github.com/areqag/gqlc/issues/758","dependencies":[{"issue_id":"gqlc-nuk0","depends_on_id":"gqlc-rohp","type":"discovered-from","created_at":"2026-08-03T20:16:26Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-bo16","title":"codegen/age: three arms of the multi-label diagnostic are unpinned","description":"Found by the adversarial review of gqlc-35yu.15, all non-blocking, all the same\nshape: a live branch or a sweep with no test behind it.\n\n## 1. The offender count is unpinned\n\nThe diagnostic says \"This schema declares %d of them\". Hardcoding that %d to 1\nwhile the message goes on to list two offenders leaves the whole suite green.\nThe *per-offender* label count is pinned — dropping labels or truncating a label\nset reddens `multilabel_test.go:146` — but the top-level count is not.\n\nOne line in TestMultiLabelDiagnosticNamesEveryOffenderAndItsLabels closes it:\nassert the printed count equals the number of offenders the test found, not a\nliteral.\n\n## 2. The singular/plural query branch is unpinned\n\nThe assertion is `Contains(msg, strconv.Itoa(queries)+\" quer\")`, which matches\nboth \"query\" and \"queries\". So deleting the `if queries == 1 { queryNoun =\n\"query\" }` branch entirely survives. Cosmetic in effect, but it is a live\nbranch with no test.\n\n## 3. The fixture list in the sweep is a literal, not a glob\n\n`requireLabelsDistinguishable` — the guard that stops a fixture's own type name\nfrom satisfying an assertion about its labels — runs over a hardcoded slice of\nfixture names. A third multi-label fixture dropped into\n`internal/codegen/age/testdata/` is not swept until someone edits the slice.\n\nThis one is defensible as written: that testdata directory holds fixtures for\nother tests too, so a bare glob would sweep files the guard does not apply to.\nBut it is the one place in this test file that is not self-extending, and the\nepic's recurring defect is exactly a hand-maintained list that nobody remembers\nto update. If a glob is wrong, the fix is a naming convention the glob can key\non (e.g. `multi_label_*.gql`) plus a check that the convention is exhaustive.\n\n## Not in scope\n\nThe review also found that `keyLabels` and `entityKindNoun`'s edge arms are dead\nand untested — both mutate to nonsense with a fully green `./...`. That is\ndeliberate: multi-label EDGES are unreachable across three syntaxes (verified at\nthe grammar: `no viable alternative at input ':WORKED_FOR\u0026FOR'`, `mismatched\ninput ':' expecting ']-\u003e'`, `no viable alternative at input 'LABELS\nWORKED_FOR\u0026FOR'`), and ADR 0027 documents the retention. Nothing would flag the\narm rotting if the grammar ever opened, but that is a different bead if anyone\nwants it.\n\n## Acceptance\n\nEach of the three mutations above turns a gate red, naming what it caught.\n\nFiled by the coordinator from the gqlc-35yu.15 review's non-blocking findings.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:08:56Z","created_by":"areqag","updated_at":"2026-08-04T00:10:19Z","external_ref":"https://github.com/areqag/gqlc/issues/753","dependencies":[{"issue_id":"gqlc-bo16","depends_on_id":"gqlc-35yu.15","type":"discovered-from","created_at":"2026-08-03T20:09:03Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5et5","title":"codegen/age: the corpus template is unpinned against the emission it drives","description":"The AGE corpus harness (internal/codegen/age/corpus_test.go) now proves, via a\n`go test -json` pass-set comparison, that every test the corpus template\ndeclares actually ran. That closes the 'harness green on an empty test body'\nhole.\n\nIt does not close the adjacent one. Nothing requires the template to **exercise\neach emitted `agtype*` helper**. A helper could gain an arm that no corpus test\nreaches, and the harness would stay green — it only checks that the tests the\ntemplate declares all ran, not that the declared set covers the emission.\n\n## Shape of the fix\n\nThe emission side is enumerable: the helpers are rendered from a known set, and\n`TestEmittedHelpersAreClosedOverWhatTheyCall` (added on gqlc-35yu.11) already\nwalks the rendered output for `agtype*` identifiers. The missing claim is the\nconverse — every `agtype*` helper the emission **declares** is **called** by at\nleast one corpus test.\n\nSet equality between the declared helper set and the exercised helper set, not a\ncount. Deriving the exercised set is the harder half: either instrument via the\ncoverage profile the sentinel fence already collects, or have the template's\nassertions name the helper they exercise and check that naming against the\ndeclared set.\n\n## Acceptance\n\nAdd an `agtype*` helper arm that no corpus test reaches and watch a gate go red\nnaming it. Then delete it and watch green.\n\nSuggested by the gqlc-35yu.11 round-5 implementer; filed by the coordinator.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-04T00:01:38Z","created_by":"areqag","updated_at":"2026-08-04T00:07:14Z","external_ref":"https://github.com/areqag/gqlc/issues/750","dependencies":[{"issue_id":"gqlc-5et5","depends_on_id":"gqlc-35yu.11","type":"discovered-from","created_at":"2026-08-03T20:09:04Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rmzg","title":"codegen/age: the alternation refusal reports source text but no position","description":"`rejectRelationshipTypeAlternation` (`internal/codegen/age/errors.go`) refuses a\nquery whose relationship detail names two or more types, and its message names\nthe query and quotes the alternation's source text:\n\n    write each relationship type as its own query: PostIDs (\":AUTHORED|LIKES\")\n\nThat is enough to find the offending construct in a short query. It is not\nenough in a long one, and it is not enough at all when the same alternation\ntext appears twice in one file.\n\n`cypher.RelationshipTypeAlternations` (`internal/query/cypher/alternation.go`)\nwalks ANTLR's `oC_RelationshipTypes` nodes, so the position is already in hand\nat the point the finding is made — every ANTLR context carries a start token\nwith a line and column. The walk currently discards it and reports source text\nonly.\n\n## Scope\n\nCarry the line/column off the `oC_RelationshipTypes` start token through to the\nmessage. Match whatever positional convention the rest of gqlc's diagnostics\nalready use rather than inventing a second one — check what\n`internal/queryfile` and the resolver's errors spell before choosing.\n\n## Acceptance\n\nA fixture with two identical alternations in one file produces two findings\ndistinguishable by position, and a test asserts on that distinction rather\nthan on the count.\n\nFiled by the coordinator from a candidate raised by the `gqlc-35yu.14` round-3\nimplementer.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T23:47:58Z","created_by":"areqag","updated_at":"2026-08-04T00:07:15Z","external_ref":"https://github.com/areqag/gqlc/issues/751","dependencies":[{"issue_id":"gqlc-rmzg","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-03T19:48:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2fz1","title":"codegen/age: the DATE encoding owes a year-range check and the collation ordering test the spike flagged","description":"The settled encoding table on gqlc-35yu.11 stores DATE as the\nzero-padded ISO 'YYYY-MM-DD' string, on the argument that its lexical\norder is its chronological order. The argument holds across\n[0001-01-01, 9999-12-31] and nowhere else, and the claim as originally\nwritten in internal/codegen/age/types.go carried no such bound. The doc\nnow states it; the check that would enforce it does not exist, because\nDATE is still refused for want of a backend-invariant carrier. This\nbead is the debt whoever admits the width inherits.\n\nTwo ways out of the range, both mis-sorting rather than failing:\n\n  - Year 10000 and beyond needs a fifth digit. '10000-01-01' sorts\n    under '2024-01-01' because '1' \u003c '2', so every date past year 9999\n    files at the front.\n  - A proleptic year before 1 CE needs a sign. '-0500-01-01' sorts\n    under every unsigned date for the same reason, and worse, the whole\n    BCE era then sorts ascending where chronology wants it descending.\n\nWHAT TO DO when DATE is admitted: range-check in the emitted encoder\nand fail the value naming the property, rather than storing a string\nthe database will silently order wrongly. The precedent is CALENDAR\nDURATION — an encoding that cannot be made faithful is a refusal, not a\nbest effort.\n\nSECOND, SEPARATE DEBT ON THE SAME WIDTH, from the spike notes on\ngqlc-35yu.11: agtype string comparison uses DB COLLATION, not byte\norder. DATE stayed a string by owner decision, on readability in the\nstored graph, with the note \"if a collation is ever found where ISO\ndates misorder, that reopens this - worth one explicit test\". That test\ndoes not exist either. Both are owed by the same change, so they are\none bead.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:58:53Z","created_by":"areqag","updated_at":"2026-08-03T17:00:14Z","external_ref":"https://github.com/areqag/gqlc/issues/738","dependencies":[{"issue_id":"gqlc-2fz1","depends_on_id":"gqlc-35yu.11","type":"discovered-from","created_at":"2026-08-03T12:58:53Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-oaxm","title":"bd-gh-sync: six diagnostic lines are untested, and the gh stub cannot fail at all","description":"Adversarial review of `gqlc-w4q9` applied 34 mutations to `.githooks/bd-gh-sync`\nand killed 28. All six survivors are in diagnostic-refinement code: each one is\ndeletable, or invertible, with no assertion going red. None changes a verdict\nline's pass/fail or the exit status — they change only the wording of a detail\nline — which is why this was returned non-blocking.\n\nThe sites:\n\n- `:661` — `[ -n \"$_blind\" ] \u0026\u0026 [ \"$_post_rc\" -ne 0 ] \u0026\u0026 _blind=\"'bd list' exited …\"`.\n  The **pull** side has the equivalent line *and* an assertion for it (\"the\n  blind notice names the exit status it saw\"). Push has the line and no\n  assertion.\n- `:662` — the `_gh_rc` twin, and **the worst of the six**: `_gh_rc != 0` is not\n  exercised at all, because the test harness's `gh` stub always exits 0. The\n  reviewer proved the path correct by hand with a throwaway fixture (exits 1,\n  prints `'gh issue list' exited 4`, verdict last), but **the suite cannot see\n  it**. A stub that can only succeed cannot test a failure path.\n- `:486-488` — the `_list_rc` diagnostic on push. New on the `gqlc-w4q9` branch,\n  untested.\n- `:628` — `bid = one_line(b.get(\"id\") or \"\")` in the close comment. New,\n  untested.\n- `:399` — pull's `*) _blind=\"the check itself did not run\"`. Pre-existing on\n  master, untested there too.\n- `:625-627` — the `close_reason` first-line extraction and 300-char clamp.\n  Pre-existing on master.\n\n## Why this is worth closing\n\nThese lines exist to tell a human *why* a run went blind. A blind run that\nreports the wrong reason is worse than one that reports none, because it sends\nthe reader to the wrong place. And the `_gh_rc` case is not merely untested —\nit is untestable with the current stub, which is a gap in the harness rather\nthan in the script.\n\n## Fix\n\nTeach the `gh` and `bd` stubs to fail on demand — an env var naming the exit\nstatus is enough — then assert each diagnostic names the status it saw. The\npull side's existing assertion is the model to copy.\n\n## Acceptance\n\nEach of the six lines must be individually deletable-with-a-red-test. Verify\neach currently survives deletion first, so the new assertions are known to be\nwatching something.\n\nFound during the adversarial review of `gqlc-w4q9`, returned non-blocking.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:53:54Z","created_by":"areqag","updated_at":"2026-08-04T09:42:14Z","closed_at":"2026-08-04T09:42:14Z","close_reason":"Merged in PR #778 (77a8cc12)","external_ref":"https://github.com/areqag/gqlc/issues/739","dependencies":[{"issue_id":"gqlc-oaxm","depends_on_id":"gqlc-w4q9","type":"discovered-from","created_at":"2026-08-03T12:53:59Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vcvh","title":"codegen/neo4j: driverSliceCarriers' []byte arm is unreached by the corpus it sweeps","description":"`driverSliceCarriers` contains a `\"[]byte\"` entry. No golden in the committed\ncorpus contains a `[]byte` assertion — an independent AST sweep of every neo4j\ngolden yields `[]any`×37, `bool`×4, `dbtype.Date`×2, `dbtype.Relationship`×13,\n`float64`×6, `int64`×17, `string`×17, `time.Time`×5, and nothing else.\n\nSo the census's `[]byte` arm is dead over the corpus it sweeps. `[]byte` is\nexercised at runtime by `internal/codegen/neo4j/corpus_test.go` (`Listy.Blobs`),\nwhich is real coverage of the *decode path* — but it is not coverage of the\n*census arm*, and those are different claims. The arm could be deleted, or\nmistyped, and no test would notice.\n\n`[]byte` is a genuine member of the driver's `PropertyValue` set\n(`neo4j-go-driver/v5@v5.28.4/neo4j/graph.go:25-38`), so the entry is correct.\nThe gap is that its correctness is unwitnessed.\n\n## Fix\n\nAdd a golden fixture whose entity carries a `BYTES`-width property, so the\ncensus's `[]byte` arm is reached by the corpus it sweeps.\n\n## Acceptance\n\nWith the fixture in place, deleting `\"[]byte\"` from `driverSliceCarriers` must\nredden the census, naming the fixture. Verify it does **not** redden today\nfirst, so the test is known to be watching something.\n\nFound during the adversarial review of `gqlc-35yu.13`, returned non-blocking.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:47:51Z","created_by":"areqag","updated_at":"2026-08-04T00:15:18Z","closed_at":"2026-08-04T00:15:18Z","close_reason":"Merged: PR #752 (3kds/6rh3/vcvh) and PR #754 (.15).","external_ref":"https://github.com/areqag/gqlc/issues/731","dependencies":[{"issue_id":"gqlc-vcvh","depends_on_id":"gqlc-35yu.13","type":"discovered-from","created_at":"2026-08-03T12:47:59Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-6rh3","title":"codegen/neo4j: the driver-carrier census's scalar axis cannot go empty, so it guards nothing","description":"`gqlc-35yu.13` gave the neo4j driver-carrier census two axes so that \"one going\nempty behind the other fails rather than hiding\". That claim holds in one\ndirection only.\n\nThe **slice** axis does the anti-vacuity work: deleting every list-bearing\ngolden drives its count to zero and the sweep goes red naming the vacuity. This\nwas verified by mutation.\n\nThe **scalar** axis cannot do the same job. Its `NotZero` counts every non-slice\nassertion in every neo4j golden — 64 of them at the time of writing, including\n`v.(string)` from nullable scalars that have nothing to do with the carrier rule.\nRemoving every list fixture leaves it comfortably non-zero. There is no plausible\ncorpus edit that empties it, so it can never fire, and it contributes no\nevidence to the claim it is written to support.\n\nThis is the same shape the epic keeps closing: a count that is satisfiable by\nthe wrong population. `NotZero` was already the wrong assertion in\n`TestStageSpecsReadAsHistory`, where it stood in for \"twelve\".\n\n## Fix\n\nEither give the scalar axis a population it actually gates — the assertions that\nride the carrier rule, not every scalar assertion in the tree — or state\nplainly in the test that only the slice axis carries the anti-vacuity claim and\ndrop the scalar `NotZero`, which is currently decoration.\n\nPrefer the first. A census with one live axis and one decorative axis reads as\nthough it has two.\n\n## Acceptance\n\nWhatever population the scalar axis ends up over, there must exist a corpus edit\nthat empties it and reddens the axis. Demonstrate that edit. If no such edit\nexists, the axis is not a guard.\n\nFound during the adversarial review of `gqlc-35yu.13`, returned non-blocking.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T16:47:50Z","created_by":"areqag","updated_at":"2026-08-04T00:15:18Z","closed_at":"2026-08-04T00:15:18Z","close_reason":"Merged: PR #752 (3kds/6rh3/vcvh) and PR #754 (.15).","external_ref":"https://github.com/areqag/gqlc/issues/732","dependencies":[{"issue_id":"gqlc-6rh3","depends_on_id":"gqlc-35yu.13","type":"discovered-from","created_at":"2026-08-03T12:47:59Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nsq4","title":"vuln: the blind-package ratchet's own-module filter terminates instead of recursing","description":"The blind-package ratchet added by `gqlc-rohp` (`justfile:307-315`) decides\nwhether a package's in-package test pulls third-party code into a region\ngovulncheck cannot see. It tests the *direct* imports of the in-package test\nagainst a third-party predicate, having first filtered out own-module imports.\n\nThat filter is the hole. A package whose in-package test imports **only an\nown-module test-only helper**, where that helper itself imports third-party\ncode, is filtered out before the third-party test ever runs. The package is not\ncounted as blind. But govulncheck still loses the same call edges, because the\nloss is caused by the `p [p.test]` path collision in\n`x/vuln@v1.6.0 internal/vulncheck/packages.go:144`, not by who the direct\nimporter is.\n\nSo the ratchet would report a residual smaller than the real one, and — worse\nthan being merely wrong — it would report it in the reassuring direction.\n\n## Not currently live\n\nProbed at the time of filing: `comm -13` of the non-test closure against\nown-module `.TestImports` is **empty**, so no package in the tree has this\nshape today. The finding understates the residual only prospectively. That is\nwhy it was returned non-blocking rather than promoted, and why it is a bead\nrather than a fix in `gqlc-rohp`'s branch.\n\nBut \"empty today\" is exactly the state that makes it invisible when it stops\nbeing empty — nothing announces the first package to acquire this shape.\n\n## Fix\n\nMake the third-party test transitive rather than direct. The own-module filter\nshould not be a *termination*; it should be a *recursion*. An own-module import\nof an in-package test is a reason to look at that package's own imports, not a\nreason to stop looking.\n\nTake the transitive closure of the in-package test's imports through own-module\nedges, then apply the third-party predicate to the frontier. `go list` already\ngives both `.Imports` and `.TestImports`, so this needs no new tooling.\n\n## Acceptance\n\nPlant a package whose in-package test imports only an own-module helper that\nitself imports a third-party module, and whose non-test closure does not reach\nthat module. The ratchet must name that package as blind. Under the current\npredicate it does not — verify that it does not, first, so the test is known to\nbe watching something.\n\nFound during the adversarial review of `gqlc-rohp` and returned as non-blocking.\nRelated to `gqlc-m5rc`, which is the residual this ratchet measures.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T10:15:19Z","created_by":"areqag","updated_at":"2026-08-04T08:52:02Z","closed_at":"2026-08-04T08:52:02Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/724","dependencies":[{"issue_id":"gqlc-nsq4","depends_on_id":"gqlc-m5rc","type":"related","created_at":"2026-08-03T06:15:27Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-nsq4","depends_on_id":"gqlc-rohp","type":"discovered-from","created_at":"2026-08-03T06:15:27Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-coa5","title":"codegen/age: the qualification sweep's witness table is keyed on a file only the test skeleton emits","description":"`sweptWitnesses` in `internal/codegen/age/age_test.go` is the table that keeps\nthe schema-qualification sweep honest: for each emitted file it names the\nqualified spellings that must be present, so a sweep which silently stopped\nlooking at anything would be caught by the witness count going to zero.\n\nIt is keyed on three files, one of which is `q.cypher.go`. **Only the test's own\nskeleton `emitReadBatch()` emits a file by that name.** The 30-plus corpus\nfixtures emit their query files under their own per-source names, so none of them\nis matched by a witness key.\n\nSo the witness half proves the sweep fires on a hand-built skeleton. It does not\nprove the sweep fires on the corpus. The guard's comment claims more than the\nguard does.\n\n## Why this is worth fixing rather than shrugging at\n\nThe positive-count assertion still runs over the corpus, so this is not a case of\nnothing being checked at all. But the count and the witness table answer\ndifferent questions. The count says \"the sweep looked at something\". The witness\ntable says \"the sweep looked at the *right* thing, and here is the specific\nqualified spelling it had to find\". Only the second survives a refactor that\nchanges which region is swept — and the sweep's region is exactly what\n`gqlc-du0k` moved, by cutting the embedded query text and the author's parameter\nkeys out of it.\n\nThat is the shape this epic keeps turning up: a check that is green because of\nwhat it is *not* looking at. Here the blind spot is per-file rather than total,\nwhich makes it harder to see, not less real.\n\n## Reproduction\n\nEmit any corpus fixture and list the file names the AGE backend produces. None of\nthem is `q.cypher.go`. Then confirm the consequence directly: unqualify an\n`ag_catalog` spelling in the emitted query file of a corpus fixture — the witness\ntable has no key that would name it, so only the generic count and the bare-\nspelling scan can react, not the per-file witness assertion the table exists to\nprovide.\n\n## Fix\n\nKey the witness table on something the corpus actually emits. The natural axis is\nthe *kind* of emitted file rather than its literal name — the statement-composer\nfile, the models file, the db file — since the per-source query file names vary\nby fixture while their required qualified spellings do not. Deriving the key from\nthe emission rather than from a hand-written literal also removes the chance of\nthis drifting again the next time a file is renamed.\n\nDo not solve it by adding `q.cypher.go`'s per-fixture equivalents as more literal\nkeys. A hand-maintained list of 30 names is the same defect with more rows, and\nit goes stale the first time a fixture is added.\n\nFound during the adversarial review of `gqlc-du0k` and returned as non-blocking:\nit is a pre-existing question about the test's corpus reach, not a defect that\nbead introduced. Confirmed present on `master` after `gqlc-du0k` merged.\n\n## Acceptance\n\nUnqualifying an `ag_catalog`-owned spelling in the query file of a **corpus**\nfixture reddens the witness assertion by name — the failure message must name the\nmissing qualified spelling and the file, not merely report a count that moved.\nAnd the witness keys must be derived from what the emission produces rather than\nhand-written, so that adding a fixture cannot leave it unguarded.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T10:03:29Z","created_by":"areqag","updated_at":"2026-08-03T10:12:31Z","external_ref":"https://github.com/areqag/gqlc/issues/723","dependencies":[{"issue_id":"gqlc-coa5","depends_on_id":"gqlc-du0k","type":"discovered-from","created_at":"2026-08-03T06:03:34Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-oltq","title":"resolver: describeColumnType panics on a nil type that resolvedTypeEqual guards against","description":"`describeColumnType` panics on a nil `ResolvedType`, while `resolvedTypeEqual`\n— the function it exists to explain — explicitly guards nil. Both are called on\nthe same value, `Column.Type`, from the same two lines of\n`compareBranchColumns` (`internal/resolver/resolve.go`). One of the two is\nwrong about what it can be handed.\n\nExecuted:\n\n    describeColumnType(nil)                     -\u003e panic (nil interface, t.String())\n    describeColumnType(ResolvedList{Element: nil}) -\u003e panic (recurses to the same)\n\n    resolvedTypeEqual(a, b) opens with:\n        if a == nil || b == nil { return a == nil \u0026\u0026 b == nil }\n\n`describeColumnType`'s `default` arm and its `ResolvedList` recursion both call\n`.String()` on the interface without a nil check.\n\n**Pre-existing, not a regression.** The message this arm was built from\n(pre-`gqlc-nkjt`) called `.String()` on the same value and panicked identically.\n`gqlc-nkjt` moved the call into a helper and added a variant switch; it did not\nintroduce the reachability, and it did not remove it.\n\nRaised as an unverified suspicion by the `gqlc-nkjt` round-4 adversarial\nreviewer, who could execute the panic but could not settle whether anything can\nproduce the input.\n\n## The question this bead exists to answer\n\n**Is a nil `Column.Type` constructible from user input?**\n\n`Column.Type` is written at exactly one place — `scope.go:548`,\n`Column{Name: item.Name, Type: colType}` — from `projectionType`'s return. So\nthe question reduces to: does any arm of `projectionType`, or of the\n`resolveType` / `unify` / edge- and node-projection helpers it delegates to,\nreturn `(nil, nil)`? The nested case is the same question one level down: can a\n`ResolvedList` be built with a nil `Element`? There are three construction sites\n(`resolve.go:1058`, `resolve.go:1135`, `scope.go:731`), and each takes its\nelement from a call that also returns an error or an ok flag.\n\nTwo branches, and exactly one of them is true:\n\n**If yes** — some arm returns a nil type with a nil error — this is a **live\npanic on user input**. A `UNION` whose branches disagree on a column reaching\nthat arm crashes the compiler instead of refusing the query. Fix:\n`describeColumnType` renders nil as something honest, and a fixture reaches it.\n\n**If no** — nothing can produce a nil `Column.Type` — then `resolvedTypeEqual`'s\nnil guard is **dead code claiming a reachability that does not exist**, which is\nworse than absent: the next reader takes it as evidence that nil is expected\nhere and writes more nil handling around it. Fix: delete the guard and say in a\ncomment why nil is not constructible, or make the impossibility structural.\n\nDo not resolve this by adding a nil check to `describeColumnType` and moving on.\nThat satisfies both branches without deciding either, and leaves the same\ncontradiction one function over.\n\n## Acceptance\n\nThe answer is stated with the evidence that settles it, and one of the two arms\nis removed:\n\n- **Live-panic branch:** an invalid-corpus fixture reaches the type arm with a\n  nil (or nil-element) column type and refuses with a message rather than\n  panicking. Mutation: deleting the new nil handling in `describeColumnType`\n  reddens that fixture — a panic, not a wrong string.\n- **Dead-guard branch:** `resolvedTypeEqual`'s nil guard is gone. Mutation:\n  reinstating it as `if a == nil || b == nil { return true }` — a compile-valid\n  change that inverts what the guard said — reddens nothing, which is the proof\n  that nothing reached it. Record that negative in the commit message alongside\n  the reachability argument, because a \"reddens nothing\" result is only evidence\n  when the sweep that produced it is named.\n\nEither way, `describeColumnType` and `resolvedTypeEqual` must agree about\nwhether nil is a value they can be handed, and say so in one place.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T09:59:14Z","created_by":"areqag","updated_at":"2026-08-03T10:00:32Z","external_ref":"https://github.com/areqag/gqlc/issues/722","dependencies":[{"issue_id":"gqlc-oltq","depends_on_id":"gqlc-nkjt","type":"discovered-from","created_at":"2026-08-03T05:59:14Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-95b8","title":"errors: AllSentinels' defensive copy is unwitnessed in codegen, cypher and queryfile","description":"Three packages export an `AllSentinels()` that hands out a defensive copy of\ntheir canonical sentinel slice, and all three carry a doc comment asserting a\nsafety property no test checks:\n\n- `internal/codegen/errors.go:204` — `slices.Clone(allSentinels)`\n- `internal/query/cypher/errors.go:140` — `slices.Clone(allSentinels)`\n- `internal/queryfile/errors.go:69` — `make` + `copy`\n\nEach is documented \"the slice is copy-returned so a mutation cannot leak into\nthe canonical set.\"\n\n**Reproduction (MUT-O, run against `internal/query/cypher`).** Replace the body\nwith `return allSentinels` — no clone, the caller now holds the package's own\nbacking array — and run `go test -count=1 ./...`. The full suite stays green.\nThe same mutation on the other two packages behaves identically: every caller\nin the tree (the conformance harness's `sentinelByName` builder, the\nqueryfile and codegen reachability sweeps) only ranges over the result, so\nnothing in the corpus ever writes through the returned slice, and the copy is\ntherefore load-bearing for no live caller.\n\nThat is the whole problem. The defence exists for a *future* caller, which is a\nreasonable thing to build — but the doc comment states it as a property that\nholds, and nothing holds it. An untested doc comment claiming a safety property\nis worse than no comment: a reader who wants to sort the result in place, or a\nmaintainer weighing whether the clone is worth its allocation, will trust the\nsentence rather than check it, and the day someone deletes the clone as dead\nweight the sentence keeps asserting a guarantee the code no longer provides.\nAbsent the comment, that reader would at least look.\n\nThe three are a **house pattern**, not a deviation any one package introduced,\nso the fix belongs across all three at once rather than in whichever package a\nreviewer happens to be reading. One shared shape of test, three packages.\n\nDiscovered by the `gqlc-rrtl` adversarial reviewer, who ruled it non-blocking\nfor that branch precisely because the sibling packages match: nothing was\nregressed, the hole is pre-existing and uniform.\n\n**Not in scope:** changing what `AllSentinels` returns, or whether ordering is\nguaranteed. The contract stands as documented; this bead only asks that the\ndocumented part be witnessed.\n\n## Acceptance\n\nFor each of `internal/codegen`, `internal/query/cypher` and\n`internal/queryfile`, a test exists such that mutating that package's\n`AllSentinels` to return the canonical slice directly — `return allSentinels`\nin codegen and cypher, `return allSentinels` in place of the `make`/`copy` in\nqueryfile — turns `go test -count=1 ./\u003cpkg\u003e/` **red**, with a failure message\nthat names the leak rather than merely reporting a length or ordering\nmismatch.\n\nEach mutation must be compile-valid: a mutant that fails to build proves the\ncode is load-bearing, not that a test is watching it.\n\nA single shared helper exercised from three package-local tests satisfies\nthis; three near-identical tests also satisfy it. What does not satisfy it is\na test in only one package, since the finding is that all three share the\nunwitnessed claim.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T09:51:10Z","created_by":"areqag","updated_at":"2026-08-03T09:53:25Z","external_ref":"https://github.com/areqag/gqlc/issues/718","dependencies":[{"issue_id":"gqlc-95b8","depends_on_id":"gqlc-rrtl","type":"discovered-from","created_at":"2026-08-03T05:51:10Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ny7o","title":"resolver: valid/ fixtures mean 'valid at R3', and nothing in the harness says so","description":"`test/data/resolver/valid/` means \"valid **at R3**\", not \"valid\". Nothing in the\nharness says so, and nothing stops a fixture in that directory from encoding a\nquery the codegen stage refuses one stage later.\n\nSo the directory name asserts something stronger than the tests behind it check.\nA reader adding a fixture there reasonably believes they are declaring the query\nwell-formed end to end; they are declaring only that the resolver accepts it.\n\n## Why it matters rather than being a naming quibble\n\nThe two stages disagree in both directions and the disagreement is invisible:\n\n- A `valid/` fixture that codegen refuses is not a test failure anywhere. It sits\n  in the corpus asserting a claim no test makes.\n- Worse for coverage: a reviewer reasoning about what the corpus proves will\n  count `valid/` fixtures as end-to-end evidence. They are not. This is the same\n  shape as the defect class this epic keeps turning up — a check that reads as\n  stronger than it is, so the gap is invisible from the green build.\n\nFound while implementing the `gqlc-nkjt` / `gqlc-ejn0` / `gqlc-9lew` /\n`gqlc-ipdx` bundle, where several new fixtures had to be reasoned about\nstage-by-stage precisely because the directory name does not carry the stage.\nDeliberately left alone there — out of scope for those beads. Confirmed present.\n\n## Fix\n\nTwo shapes are plausible and the bead should pick one rather than doing both:\n\n1. **Say it.** Rename or document the directory so the stage is in the name or in\n   a `README` the harness points at, and state which stage each `valid/` subtree\n   is valid at. Cheapest, and honest.\n2. **Check it.** Run every resolver `valid/` fixture through codegen and require\n   either that it succeeds, or that it carries an explicit marker recording that\n   codegen refuses it and why. That converts an unstated assumption into an\n   assertion, and the marker file becomes the place the stage disagreement is\n   written down.\n\nOption 2 is the stronger answer if it is tractable — it turns the directory name\ninto something enforced rather than something believed. If it is not tractable\n(for instance because codegen needs a target backend the resolver corpus does not\nname), say so explicitly in the fix so the next reader does not re-derive the\nquestion.\n\nDo not do a third thing: do not silently delete or relocate fixtures that codegen\nrefuses. Some of them exist to pin resolver behaviour on shapes codegen will\nnever see, and that is legitimate.\n\n## Acceptance\n\nEither (a) every `valid/` fixture is run through codegen and a fixture that\ncodegen refuses without a recorded marker reddens a test — verified by adding\nsuch a fixture and watching it go red; or (b) the corpus carries prose stating\nwhich stage `valid/` is valid at and why the stronger check was not taken, and a\ntest or lint pins that prose to the directory it describes so it cannot silently\ndrift away from the fixtures.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-03T09:38:16Z","created_by":"areqag","updated_at":"2026-08-03T09:53:25Z","external_ref":"https://github.com/areqag/gqlc/issues/719","dependencies":[{"issue_id":"gqlc-ny7o","depends_on_id":"gqlc-nkjt","type":"discovered-from","created_at":"2026-08-03T05:38:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-eb5j","title":"resolver: ErrUnionColumnMismatch's name arm prints the same column index twice","description":"## CORRECTION (2026-08-03) — the original diagnosis was wrong\n\nThis bead used to claim the name arm \"prints a value that is never what it\nsays\". That is **false**, and it was disproved by execution in the `gqlc-nkjt`\nround-4 adversarial review. Do not act on the old wording.\n\nBoth `%d` column arguments are `i`. But `compareBranchColumns` compares the two\nbranches **index-wise** — `for i := range base`, `other[i]` against `base[i]` —\nso `i` *is* branch 0's column index. The message states a true fact twice under\ntwo labels. The defect is **redundancy, not falsehood**.\n\nConfirmed by output:\n\n    union_column_name_mismatch.cypher\n      -\u003e branch 1 column 0 named \"name2\"; branch 0 column 0 named \"a.name\"\n    union_column_name_only_mismatch.cypher\n      -\u003e branch 1 column 0 named \"y\"; branch 0 column 0 named \"x\"\n\n## What is actually left to do\n\n### 1. Delete the second index\n\n`internal/resolver/resolve.go:316`:\n\n    \"branch %d column %d named %q; branch 0 column %d named %q\"\n\nThe third `%d` cannot differ from the second while the comparison is positional,\nso printing it invites the reader to check two numbers against each other and\nlearn nothing. One index, named once:\n\n    \"branch %d column %d named %q; branch 0 named %q\"\n\nor some equivalent that does not restate `i`. If the comparison ever stops being\npositional — a named-column join across branches, say — the second index becomes\na real fact and comes back with the code that makes it one.\n\n### 2. The arm-direction question, now that the type arm has settled\n\nThe old wording said the name arm \"leads with the failing branch, not branch 0\"\nand that this made two arms of one error read in opposite directions. That split\nno longer exists: PR #717 reverted the type arm to lead with the failing branch,\nso **all three arms of `ErrUnionColumnMismatch` now lead with the failing branch\nand name branch 0 second**. There is nothing inconsistent to fix.\n\nWhat is left is a single design decision, and this bead is the one place to make\nit: **is branch-0-first the better convention?** The argument for it is that\nbranch 0 names the result columns (R5 §3.1), so it is the reference the other\nbranches are measured against and reads naturally first. The argument against is\nthat the reader is looking for the culprit, and the culprit is branch N.\n\nWhichever wins, all three arms move together. Do not change one arm.\n\n### 3. Neither name-arm fixture observes anything about it\n\n`union_column_name_mismatch.cypher` and `union_column_name_only_mismatch.cypher`\nboth reach this arm, and on both the mismatch sits at column index **0** — so\nthe duplicated index prints `0` twice and is invisible, and no assertion looks at\nthe branch order at all. `invalidFixtureContains` has no entry for either. This\nis the same pattern the rest of the epic keeps finding: the fixtures reach the\ncode and observe nothing about it.\n\nA fixture whose mismatch sits at a **non-zero** column index is what makes a\nduplicated index visibly wrong rather than coincidentally right — for example\ntwo branches agreeing on column 0 and disagreeing on the name of column 1.\n\n## Acceptance\n\n- The name arm prints each column index once.\n- All three arms of `ErrUnionColumnMismatch` state their branches in the same\n  order, and that order is a decision recorded here rather than an accident.\n- A pinned fixture has its name mismatch at a non-zero column index, so the\n  index in the message is a fact about that fixture and not the constant 0.\n- Mutation: swapping the name arm's branch order, or replacing either printed\n  index with a literal `0`, reddens a test. Both mutations must be compile-valid\n  and neither may be blessable by `-update` — invalid fixtures have no golden,\n  so the pin belongs in `invalidFixtureContains` or an equivalent hand-written\n  assertion.\n","notes":"2026-08-03: body REWRITTEN. The original diagnosis — that the second index 'prints a value that is never what it says' — was disproved by execution in the gqlc-nkjt round-4 review. The comparison is index-wise, so i IS branch 0's column index: the message is redundant, not false. The 'two arms read in opposite directions' half is also gone; PR #717 reverted the type arm to lead with the failing branch, so all three arms now agree. What remains: delete the duplicated index, decide the branch-order convention for all three arms in one place, and fixture a mismatch at a non-zero column index.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T13:04:43Z","created_by":"areqag","updated_at":"2026-08-03T09:58:24Z","external_ref":"https://github.com/areqag/gqlc/issues/716","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-6308","title":"captured agtype corpus text is never checked against a live AGE 1.7.0","description":"The AGE decode tests assert against **captured** agtype text — literal strings\ncommitted to the corpus as the representative of what a live server returns.\nNothing checks that capture against a live AGE 1.7.0.\n\nSo the decode path is tested against the author's belief about agtype's wire\nform rather than against agtype. If a capture is wrong — a nested list rendered\none bracket-depth off, a float spelled without its `::float` annotation, a null\nelement spelled `null` where the server sends something else — the unit tests\nare green, the live tests are green (they exercise a different, real path), and\nthe divergence surfaces only on a shape the live corpus happens not to cover.\n\nThis matters more after `gqlc-35yu.13` than before it. Scalars are easy to get\nright by inspection; nested lists (`[][]float32`) and heterogeneous ANY lists are\nnot, and those are exactly the shapes `.13` added. The nested case is where a\ncapture is most likely to be wrong and least likely to be noticed.\n\nThe obvious fix — assert the captures against a live server — has a scheduling\nproblem: AGE live tests run only nightly on master (`live-smoke-age` is\n`if: github.event_name != 'pull_request'`), so a wrong capture would not redden\na PR. That is acceptable for a drift detector, which is what this is: the\ncaptures are correct today or they are not, and a nightly job that reddens\ntells us within a day.\n\nFix: a live test behind the `codegen_live` tag that, for each captured agtype\nliteral in the corpus, issues the query that produced it against the pinned AGE\nimage and asserts the server's bytes equal the capture. Keep the captures — the\npoint is not to stop using them but to know when they stop being true. Where a\ncapture cannot be reproduced by a single query, either drop it or record in the\ntest why it is synthetic.\n\nAcceptance: every captured agtype literal used by a decode test is either\nreproduced from the live pinned image (`apache/age@sha256:4241e2d8bb86a6b2ea44e9ad06c73856e12b209de295124603a599dd7feb70eb`,\nPostgreSQL 18.1 / AGE 1.7.0) or is explicitly marked synthetic with a reason; and\ncorrupting any one capture reddens the live suite.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T12:25:28Z","created_by":"areqag","updated_at":"2026-08-02T12:36:12Z","external_ref":"https://github.com/areqag/gqlc/issues/712","dependencies":[{"issue_id":"gqlc-6308","depends_on_id":"gqlc-35yu.13","type":"discovered-from","created_at":"2026-08-02T08:25:28Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-igs4","title":"decoder/QueryText-const namespaces can collide; generate exits 0 on non-compiling Go","description":"A schema label ending in `QueryText` collides with the query-text constant of a\nlike-named method, and `gqlc generate` exits 0 while emitting Go that does not\ncompile.\n\nSchema label `FooQueryText` plus `// name: DecodeFoo` emits both\n\n    func decodeFooQueryText(...)\n    const decodeFooQueryText = `...`\n\ninto one package, and `go build` then fails:\n\n    decodeFooQueryText redeclared in this block\n\nThis is the **loud** half of the capture class `gqlc-lhs3` closed the silent half\nof. It fails at build time rather than shipping a wrong runtime value, so it is\nnot urgent — but it falsifies two statements the codebase now asserts, and those\nare what make it worth fixing rather than documenting:\n\n- the `QueryTextConst` doc comment says the method name is \"already unique\n  across the package\". It is not unique against the **decoder** namespace.\n- `sweepIdentifiers`' comment says `\u003cmethodName\u003eQueryText` consts \"stay off the\n  sweep … a QueryText collision is caught by the method-name axis first\". It is\n  not caught: the collision is between a generator-owned const and a\n  generator-owned func, so neither the method-name axis nor the parameter-name\n  axis sees it.\n\nSo the emitted package has two generator-owned namespaces — decoders derived\nfrom schema labels, and query-text consts derived from method names — that can\ncollide with each other. Both are generator-owned, which is why the capture\nguards (which police *author*-chosen identifiers against generator-owned ones)\nare structurally blind to it.\n\nConfirmed identical at `9b87846`, so `gqlc-lhs3` neither causes nor worsens it.\nThe two comments above were corrected on that branch to state what is true; this\nbead is the collision itself.\n\nFix: the two namespaces need to be disjoint by construction, the same way\n`gqlc-ak39` made the generator-owned scope a closed set. A suffix or prefix that\ncannot appear in a derived decoder name, or a single collision check across all\ngenerator-owned package-level names, either would do. Prefer the closure\napproach — a guard asserting the generator-owned package-level names are\npairwise distinct is an equality assertion, not a disjointness one, and cannot\nbe blessed away.\n\nAcceptance: `FooQueryText` + `// name: DecodeFoo` either generates a compiling\npackage or is refused at generate time with a message naming both colliding\nconstructs; a fixture holds it; and the guard goes red if the disambiguation is\nremoved.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T12:25:26Z","created_by":"areqag","updated_at":"2026-08-02T12:36:12Z","external_ref":"https://github.com/areqag/gqlc/issues/713","dependencies":[{"issue_id":"gqlc-igs4","depends_on_id":"gqlc-lhs3","type":"discovered-from","created_at":"2026-08-02T08:25:25Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nkjt","title":"resolver: ErrUnionColumnMismatch names neither arm nor what it projected","description":"`ErrUnionColumnMismatch` fires when the two arms of a Cypher `UNION` project\ncolumns that do not agree. Its message prints `edge` for **both** branches, so\nthe author is told the arms mismatch without being told which side is which, or\nwhat either side actually projected.\n\nFound by the `gqlc-35yu.14` implementer while probing whether `UNION ALL` could\nreach a multi-candidate edge column on AGE (it cannot — the arms are refused\nbefore a union is formed, which is what made the message visible).\n\nFix: name each arm and what it projected, in the order the author wrote them.\nThe refusal is already correct; only the diagnostic is uninformative.\n\nAcceptance: a mismatched `UNION` refuses with a message that distinguishes the\ntwo arms, and an invalid-corpus fixture holds it.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:40:18Z","created_by":"areqag","updated_at":"2026-08-03T16:32:06Z","closed_at":"2026-08-03T16:32:06Z","close_reason":"Landed in PR #728","external_ref":"https://github.com/areqag/gqlc/issues/705","dependencies":[{"issue_id":"gqlc-nkjt","depends_on_id":"gqlc-35yu.14","type":"discovered-from","created_at":"2026-08-02T00:40:17Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-du0k","title":"codegen/age: the schema-qualification guard substring-matches, so a user parameter named $agtypeArgs fails it","description":"`TestEveryAgeIdentifierIsSchemaQualified` scans emitted output for `agtype`\nwithout a word boundary, so it matches inside longer tokens — including tokens\nthat came from the **user's own query text**, which the emitter copies verbatim\nper ADR 0005.\n\nReproduced by naming a query parameter `$agtypeArgs`:\n\n    \"agtype\" at offset 14760 is not schema-qualified\n\nThe offset lands in the embedded query string, not in generated code. So the\nguard fails on input it has no business judging, and an author's parameter name\ncan make a green build red.\n\nFound on the `gqlc-lhs3`/`gqlc-ni66` branch and left alone there because\n`age_test.go` was being rewritten concurrently by `gqlc-gjqo`. That rewrite has\nsince landed, so this is now safe to take.\n\nThe guard itself is worth keeping — AGE requires `ag_catalog.agtype` rather than\nbare `agtype`, and an unqualified spelling is a real defect. It just needs to\nscan generated identifiers rather than raw bytes: match on a word boundary at\nminimum, and preferably exclude the embedded query-text literal from the scan\nregion entirely, since nothing in it is the generator's to qualify.\n\nAcceptance: a query parameter named `$agtypeArgs` generates cleanly, and a\ngenuinely unqualified `agtype` in emitted code still fails the guard.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:40:17Z","created_by":"areqag","updated_at":"2026-08-03T10:02:37Z","closed_at":"2026-08-03T10:02:37Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/706","dependencies":[{"issue_id":"gqlc-du0k","depends_on_id":"gqlc-lhs3","type":"discovered-from","created_at":"2026-08-02T00:40:16Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2m2v","title":"codegen: a multi-parameter query using $_ emits Go that does not parse","description":"A multi-parameter query using `$_` or `$__` emits Go that does not parse:\n\n    format failure: queries.cypher.go: 25:50: expected selector or type assertion, found ','\n\non all three backends. Cause: `paramFieldName(\"_\")` returns the empty string, so\nthe generated `*Params` struct gets a field with no name and the bind expression\ncomes out as `arg.,`.\n\nFound by the `gqlc-lhs3`/`gqlc-ni66` implementer while sweeping parameter names\nfor the capture class. **Deliberately left out of that branch**: the defect is in\nfield-name derivation, not in scope capture, and it fails loudly at generate time\nrather than at exit 0. The single-parameter `$_` case *is* closed there and\ncovered, because the single-parameter form no longer derives a Go identifier from\nthe parameter name at all.\n\nFix: `paramFieldName` must either produce a usable identifier for every parameter\nname the parser admits, or refuse the query by name. Refusing is probably right —\n`$_` has no natural Go field spelling and silently renaming it would surprise the\nauthor — but either is defensible as long as the outcome is a diagnostic rather\nthan unparseable output.\n\nAcceptance: a two-parameter query using `$_` either generates Go that compiles or\nis refused with a message naming the parameter, and a fixture holds whichever was\nchosen.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:40:15Z","created_by":"areqag","updated_at":"2026-08-02T11:59:20Z","external_ref":"https://github.com/areqag/gqlc/issues/707","dependencies":[{"issue_id":"gqlc-2m2v","depends_on_id":"gqlc-lhs3","type":"discovered-from","created_at":"2026-08-02T00:40:15Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ipdx","title":"resolver: R3 §4.6 does not name the mixed-symmetry shape, where the arrow narrows without disagreeing","description":"R3 §4.6 classifies each candidate of an undirected edge binding as reading\nboth ways (skipped) or one way (a witness). `gqlc-kph4` fixtured the two pure\nshapes: every candidate symmetric (accept, arrow inert) and asymmetric\ncandidates that disagree (refuse, arrow is the remedy).\n\nThe **mixed** shape is unnamed: one candidate reads both ways, one reads a\nsingle way, and no two candidates *disagree*. Demonstrated by the kph4 round-3\nreviewer with a self-edge plus a rev-only candidate, and generalised without\nthe self-edge (`L\u0026M\u0026X -\u003e L\u0026M\u0026Y` reading both ways, plus `M -\u003e L\u0026M\u0026X` rev-only).\n\nAccepting is correct — refusing would demand an arrow to resolve a\ndisagreement that does not exist. But the arrow is **not** inert here: the\nundirected form closes to a 2-key edge union where the directed form closes to\n1. So the author's choice of marker silently changes the result type, which is\nthe property §4.6's two named shapes each avoid in their own way.\n\nConsequences are backstopped rather than silent: a whole-entity `RETURN r`\nrefuses with `ErrUnrepresentableEdgeUnion`, and a property projection emits the\nhonestly intersected type. So this is a spec-completeness and coverage gap, not\na live defect.\n\nAdjacent to `gqlc-9lew`.\n\nFix: name the mixed shape in §4.6 with its own row (what is skipped, what\nsurvives, why acceptance is right), and add a corpus fixture on each side of\nthe marker so the 2-key/1-key difference is pinned rather than incidental.\n\nAcceptance: §4.6 names three shapes rather than two, and a valid-corpus fixture\npair holds the undirected 2-key union and its directed 1-key twin.\n","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:30:30Z","created_by":"areqag","updated_at":"2026-08-03T16:32:08Z","closed_at":"2026-08-03T16:32:08Z","close_reason":"Landed in PR #728","external_ref":"https://github.com/areqag/gqlc/issues/700","dependencies":[{"issue_id":"gqlc-ipdx","depends_on_id":"gqlc-kph4","type":"discovered-from","created_at":"2026-08-02T00:30:30Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ejn0","title":"resolver: orientation-ambiguity refusal names an empty edge on an anonymous binding","description":"The orientation-ambiguity refusal names the edge binding, so on an anonymous\nedge it names the empty string:\n\n    MATCH (a:Employee)-[:REVIEWED]-(b:Person)\n    -\u003e edge \"\" matches Employee\u0026Person-[REVIEWED]-\u003ePerson left-to-right and ...\n\nFound by the `gqlc-kph4` round-3 reviewer while probing shapes the implementer\nhad not built. Pre-existing — `f1fd981` used the same `edge %q, v` — and\nunfixtured, so no golden pins the degenerate spelling.\n\nThe message is otherwise good: both witnesses are formatted, and the prescribed\nremedy (add the arrow) is still actionable. The defect is purely that `edge \"\"`\nreads as a bug in gqlc rather than as a description of the author's pattern.\n\nFix: when the binding name is empty, describe the edge by its position in the\npattern rather than by a quoted empty name — e.g. `the edge between a and b`,\nor the type where one is written. Add an invalid-corpus fixture with an\nanonymous edge so the spelling is pinned; there is currently none.\n\nAcceptance: an anonymous undirected edge with disagreeing orientation refuses\nwith a message that names something the author wrote, and a corpus fixture\nholds that message byte-for-byte.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:30:29Z","created_by":"areqag","updated_at":"2026-08-03T16:32:06Z","closed_at":"2026-08-03T16:32:06Z","close_reason":"Landed in PR #728","external_ref":"https://github.com/areqag/gqlc/issues/701","dependencies":[{"issue_id":"gqlc-ejn0","depends_on_id":"gqlc-kph4","type":"discovered-from","created_at":"2026-08-02T00:30:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-icsd","title":"[bug] bd-gh-sync: a newline in a bead id splits the plan record, bypassing the id gate","description":"`.githooks/bd-gh-sync`'s pull path refuses bead ids that would re-parse — quotes,\nspaces, leading dashes — and the in-code rationale says so: *\"Taking it whole\nlets the gate below see it and refuse it.\"*\n\nThat is true for a space. It is **false for a newline**, because the plan file the\ngate reads is line-delimited: an `ALLOW \u003cid\u003e` record carrying an embedded newline\nsplits into two records *before* `sed 's/^ALLOW //'` runs, so the charset gate\nnever sees the corruption at all.\n\nThe consequence is not a crash — it is the script silently contradicting itself\ninside a single run. Demonstrated by the round-4 reviewer with two beads,\n`b-held` (`in_progress`, must be held out of the pull) and `b-held\\ntrash`\n(eligible):\n\n    bd-gh-sync: holding b-held (GH #1) out of the pull — status-in_progress-unrepresentable-on-gh\n    bd github sync --pull-only --prefer-github --issues b-held\n    bd-gh-sync: pulled 1 bead(s), held 1, left 0 unmirrored GH issue(s) alone.\n\nIt announces the hold, then pulls the held bead, then reports success. The\npostcondition is blinded too: `b-held` lands in `allow.txt`, so the moved-bead\ncheck skips it.\n\n**Reachability is nil today.** All 270 live ids match `[A-Za-z0-9._-]+`, and ids\nare machine-minted by `bd`. This is the same corruption-only threat model the\npull-hardening already chose to defend, so it is an *incomplete* fix rather than\na regression — the base was strictly worse, refusing nothing.\n\nNot folded into `gqlc-63y` because that branch was already on its fourth review\nround and the finding is non-blocking with nil reachability. Filed so the stated\ninvariant becomes literally true rather than true-for-most-inputs.\n\nFix: about two lines in the selection Python — emit a corrupt id as a\nnon-`ALLOW` record rather than letting it split the line, so the existing charset\ngate sees and refuses it by name like every other hostile shape.\n\nAcceptance: a bead id containing an embedded newline is refused by name, and no\n`--issues` batch is issued that names a bead the same run declared held.\n","status":"closed","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:24:20Z","created_by":"areqag","updated_at":"2026-08-03T16:57:25Z","closed_at":"2026-08-03T16:57:25Z","close_reason":"Landed in PR #733","external_ref":"https://github.com/areqag/gqlc/issues/697","dependencies":[{"issue_id":"gqlc-icsd","depends_on_id":"gqlc-63y","type":"discovered-from","created_at":"2026-08-02T00:24:19Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-pig9","title":"vuln: the scanned-module list is hardcoded in two places, so a third module is silently unscanned","description":"The set of Go modules to scan is written out literally in two places:\n`.github/workflows/vuln.yml`'s change detection paths, and the `vuln` recipe in\nthe `justfile`. Both name `test/data/codegen` by hand.\n\nA third module added later is therefore both unwatched (the workflow will not\ntrigger on its dependency changes) and unscanned (the recipe will not visit it)\n— silently, with no diagnostic and no failing gate.\n\nThat is the same defect class `gqlc-rohp` exists to fix: a required security\ngate that is green because it is looking at nothing. rohp closed the instance;\nthis closes the mechanism that produced it.\n\nConfirmed by the adversarial reviewer at `0475cdb`: the repo currently has\nexactly two `go.mod` files and exactly one build tag (`codegen_live`, 4 files),\nso there is **no third blind spot today**. This is prophylactic, which is why it\nwas ruled out of scope for rohp rather than folded in.\n\nFix: derive the module list rather than declaring it — discover `go.mod` files\nunder the tree and iterate, in both the recipe and the workflow's path filter.\nIf the workflow's path filter cannot be made dynamic, then at minimum add a\ncheck that fails when the discovered module set differs from the declared one,\nso adding a module forces the lists to be updated rather than silently\nbypassing them.\n\nAcceptance: add a third throwaway module with a known-vulnerable dependency and\nconfirm `just vuln` reports it without anyone editing a path list.\n","notes":"Second fail-open axis, from the gqlc-rohp round-3 review: vuln.yml runs the scan only when a go.mod/go.sum changes, so a PR that newly *imports* an already-required module skips the scan entirely. Sharper than it sounds — the nested module currently carries GO-2026-5841 (klauspost/compress) in exactly that 'required but not imported' state, so the very next import of it lands unscanned. Same class as the hardcoded module list this bead already tracks: the trigger, like the list, is a proxy for the real condition rather than the condition itself.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:02:18Z","created_by":"areqag","updated_at":"2026-08-04T08:52:02Z","closed_at":"2026-08-04T08:52:02Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/692","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7qr5","title":"codegen/neo4j: positional decoder locals leave index gaps at nullable properties","description":"`gqlc-ak39` replaced neo4j's user-derived decoder locals with positional\ngenerator-owned ones (`value0`, `value1`, …), which closed the whole capture\nclass rather than special-casing a reserved list.\n\n`valueName(i)` indexes over **all** fields, including nullable ones — but the\nnullable arm emits its own block with generator-fixed locals and never consumes\na `value\u003ci\u003e`. So a decoder with interleaved nullable properties emits\n\n    value0, value1, [nullable block], value3, [nullable block], value6, value8…\n\nSee `test/data/codegen/valid/entity_property_shadows_decoder_local/golden/neo4j-go-v5/models.go`,\nwhich jumps `value1` → `value3`.\n\nEntirely harmless — the indices are unique by construction, which is the only\nproperty the scheme needs, and Go's unused-variable rule makes any positional\nmis-wiring a compile error regardless of density. Purely a legibility point:\nthe gaps read like something was deleted, and a reader of generated code who\nnotices will go looking for a bug that is not there.\n\nFix if anyone touches this code for another reason: increment the counter only\non the required arm, so the emitted sequence is dense `value0..valueN`. Not\nworth a standalone change and a full neo4j golden churn (~80 files) on its own.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T04:00:31Z","created_by":"areqag","updated_at":"2026-08-02T04:04:13Z","external_ref":"https://github.com/areqag/gqlc/issues/693","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-llkx","title":"bd-gh-sync: an empty bead description is an unconditional ALLOW, and nothing pins it","description":"`.githooks/bd-gh-sync` decides pull eligibility with a prefix test: GitHub's\nbody must start with the bead description. When the bead description is the\nempty string, `\"\".startswith(\"\")` — and indeed `anything.startswith(\"\")` — is\nuniversally true, so the bead is unconditionally ALLOWed and GitHub's body is\npulled over it whatever it contains.\n\nFound in the round-3 adversarial review of `fix/bd-gh-sync-state-fidelity` at\n`d25d947`.\n\nRuled out of scope for that branch for a specific reason worth preserving: the\nbehaviour is **identical under the old subset rule**, so it is not a regression\nthat branch introduced. It is the degenerate endpoint of a bd-side deletion —\nif a description has been emptied on the bd side, there is nothing to protect,\nand pulling GitHub's version is arguably the right answer.\n\nWhat makes it worth a bead is not the behaviour but the absence of a pin. The\nsibling case (trailing-block deletion, [[gqlc-6mx3]]) IS covered by a fixture;\nthis one is not. So nobody would notice if a future change made the empty-desc\npath do something else, and this script has now failed review three times for\nexactly the reason that its fixture set could not express the input in\nquestion.\n\nMinimum work: add a test asserting the current behaviour, so the decision\nbecomes deliberate rather than incidental. Then decide separately whether\nunconditional ALLOW on an empty description is actually what we want, or\nwhether an empty description should HOLD on the grounds that it is more likely\nto be an accident than an intent.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:46:59Z","created_by":"areqag","updated_at":"2026-08-02T03:48:57Z","external_ref":"https://github.com/areqag/gqlc/issues/687","dependencies":[{"issue_id":"gqlc-llkx","depends_on_id":"gqlc-63y","type":"blocks","created_at":"2026-08-01T23:47:09Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-gdlv","title":"bd-gh-sync: the moved-bead list truncates by id sort, so the interesting id can be the one dropped","description":"`.githooks/bd-gh-sync:267-268` truncates the moved-bead list with `head -n 3`\nover an **id-sorted** list, appending `+N more`.\n\nFound in the round-3 adversarial review of `fix/bd-gh-sync-state-fidelity` at\n`d25d947`. The reviewer constructed a run with 5 moved beads where the\ninteresting one sorts last, and got `(m-a, m-b, m-c, +2 more)` — the id of the\nbead that actually mattered appears only in output lines that `tail -1`\ndiscards.\n\nRuled non-blocking and out of scope for that branch because the **count**\nsurvives into the last line, and the count is the actionable trigger: an\noperator who sees `+2 more` knows to go look. Nothing is silently lost, only\ninconveniently placed.\n\nWorth doing anyway because the ordering is arbitrary — id sort has no relation\nto interestingness. Options: sort by recency of change, or drop the truncation\nentirely (a moved-bead list is already an exceptional event, so its length is\nnaturally bounded), or keep the truncation but make the omitted ids reachable\nwithout re-running.\n\nRelated: [[bd-gh-sync-tail-1-contract]] — several findings on this script share\nthe same underlying constraint, that the session-start consumer reads only\n`tail -1`, so anything actionable has to be on the last line.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-02T03:46:58Z","created_by":"areqag","updated_at":"2026-08-02T03:48:57Z","external_ref":"https://github.com/areqag/gqlc/issues/688","dependencies":[{"issue_id":"gqlc-gdlv","depends_on_id":"gqlc-63y","type":"blocks","created_at":"2026-08-01T23:47:09Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-fc1v","title":"bd-gh-sync: no circuit breaker on the ALLOW count","description":"Nothing caps how many beads a single run can hand to --prefer-github. The 129-vs-4 near-miss was caught by eye, not by the script. The ALLOW count does surface in the summary line, but that line prints on total failure too (the fail-open-in-reporting finding, promoted to blocking on gqlc-63y).\n\nOnce the eligibility rule narrows to append-only (gqlc-63y), the blast radius drops sharply, which is why this is P3 rather than a blocker there.\n\nPROVENANCE: non-blocking finding 6 in the bd-gh-sync adversarial review (2026-08-01).\n\nFIX: refuse and report when ALLOW exceeds a threshold (or a fraction of the mirrored corpus) rather than proceeding.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T21:23:54Z","created_by":"areqag","updated_at":"2026-08-02T03:41:47Z","external_ref":"https://github.com/areqag/gqlc/issues/682","dependencies":[{"issue_id":"gqlc-fc1v","depends_on_id":"gqlc-63y","type":"blocks","created_at":"2026-08-01T17:24:16Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-9lew","title":"resolver: two distinct swapped pairs in one candidate set are fixture-uncovered","description":"A candidate set can contain TWO distinct swapped pairs, reachable via a 2x2 endpoint cross-product with all four EdgeKeys declared. The orientation-ambiguity refusal resolves first-in-candidate-order, and that order is deterministic (satisfyingNodeTypes sorts at internal/resolver/resolve.go:1391), so the behaviour is well-defined — but no fixture covers it, so the determinism is unasserted.\n\nPROVENANCE: raised as a non-blocking finding in the gqlc-kph4 adversarial review (2026-08-01). I ruled it out of scope there: kph4 already carries three blockers plus three promoted findings, and this one asserts a property that holds today rather than fixing a defect.\n\nFIX: add an invalid fixture with four declared EdgeKeys over a 2x2 endpoint cross-product; assert the refusal message names a stable pair.\n","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T21:23:40Z","created_by":"areqag","updated_at":"2026-08-03T16:32:07Z","closed_at":"2026-08-03T16:32:07Z","close_reason":"Landed in PR #728","external_ref":"https://github.com/areqag/gqlc/issues/683","dependencies":[{"issue_id":"gqlc-9lew","depends_on_id":"gqlc-kph4","type":"blocks","created_at":"2026-08-01T17:24:15Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-k22l","title":"vuln: clear the three uncalled advisories the widened scan now surfaces","description":"Widening the scan in gqlc-rohp made the nested module test/data/codegen report three advisories that govulncheck classifies as not-called, so the gate exits 0 and does not block:\n\n- GO-2026-5158 — go.opentelemetry.io/otel v1.41.0, baggage parsing no longer caps raw header length. Imported, not called. Fixed in v1.42.0.\n- GO-2026-5841 — github.com/klauspost/compress v1.18.5, OOB read in .../s2. Required, not imported. Fixed in v1.18.7.\n- GO-2026-5932 — golang.org/x/crypto/openpgp unmaintained and unsafe by design. Required, not imported. No fix available; nothing in this tree imports openpgp, so this one is permanent noise unless x/crypto drops the package.\n\nBoth fixable ones are indirect deps of testcontainers-go v0.43.0. Not bumped in gqlc-rohp because there is no reachable exposure and pinning indirect deps ahead of the module that requires them is churn that go mod tidy can undo. Bump them when testcontainers-go next moves, or pin deliberately if the classification changes to called.\n\nAlso worth reporting upstream: govulncheck v1.6.0 silently drops the imports of an in-package test variant, because it keys loaded packages by import path and `p [p.test]` collides with `p`. Reproduced minimally — identical module, identical deps, identical flags, only `package v` vs `package v_test` differing: the in-package form reported nothing, the external form reported the vulnerability with a call trace. gqlc-rohp works around it by keeping the live battery an external test package, held by test-codegen-fence.","status":"closed","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T21:09:10Z","created_by":"areqag","updated_at":"2026-08-04T08:52:03Z","closed_at":"2026-08-04T08:52:03Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/684","dependencies":[{"issue_id":"gqlc-k22l","depends_on_id":"gqlc-rohp","type":"blocks","created_at":"2026-08-01T17:24:44Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-monh","title":"resolver/codegen: rationale text from the edge-set work is incomplete or reaches across layers","description":"Three comment/citation sites left by gqlc-35yu.9. None change behaviour; each\nis a place where the text a future reader is asked to trust says less than the\ncode guarantees, or justifies itself from the wrong layer.\n\n1. internal/resolver/resolve.go, edgeProbes' doc comment, and\n   docs/specs/resolver-stage-r3.md §4.4. Both name `src == tgt` as THE reason\n   two probes can name one key. Under ADR 0022 plural endpoints there is a\n   second: overlapping srcs/tgts give (A,B) twice with A != B. The\n   postcondition (the result is a set) is unaffected — the enumeration dedupes\n   on the whole EdgeKey, not on the self-loop condition — but the stated\n   rationale is not exhaustive, so a reader checking the code against the\n   reason would conclude the dedupe is dead on the plural path. See gqlc-kph4,\n   which is the coverage side of the same observation.\n\n2. internal/resolver/resolve_test.go, TestEdgeUnionKeysAreASet. Its comment\n   justifies the assertion by what codegen does with a duplicate (emits the\n   switch case twice, package fails to compile). AGENTS.md discourages\n   cross-layer appeals: the resolver's own reason to return a set is R3 §4.6\n   dispatching on the candidate count, which is true whether or not codegen\n   exists. Restate it from the resolver's contract and let the codegen\n   consequence be at most a closing aside.\n\n3. internal/codegen/prepare.go, admitEdgeUnionCandidates. The first gate (fewer\n   than two candidates) cites \"R3 spec §4.4\" for the invariant it is asserting.\n   The third gate — two candidates sharing an edge label — likewise depends on\n   the resolver having returned a set rather than a bag, since a bag would\n   reach it as a false duplicate-label refusal. It carries no citation. Give it\n   the same one.","notes":"\nMANAGER SEQUENCING NOTE (2026-08-01). Deliberately NOT staffed in parallel with\ngqlc-kph4, which is in flight on feat/resolver-plural-undirected-coverage.\n\nThis bead's item 1 is the edgeProbes doc comment and R3 spec §4.4 — the exact\ntext kph4 will be rewriting, because kph4's whole subject is the plural-endpoint\npath those two currently describe incompletely. Two agents editing the same\nparagraphs concurrently produces a conflict whose resolution is a judgement\ncall neither of them is placed to make.\n\nItems 2 and 3 (TestEdgeUnionKeysAreASet's cross-layer justification, and the\nmissing R3 §4.4 citation on admitEdgeUnionCandidates' third gate) are genuinely\nindependent of kph4 and could have gone now. Kept together with item 1 anyway:\nsplitting one small comment-accuracy bead into two branches and two PRs costs\nmore review attention than it saves wall-clock, and item 1 is the one with real\ncontent.\n\nStaff immediately once kph4 lands, and re-read item 1 first — kph4 may have\nalready made it true, in which case this bead shrinks to items 2 and 3.","status":"open","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T19:51:30Z","created_by":"areqag","updated_at":"2026-08-01T20:43:27Z","external_ref":"https://github.com/areqag/gqlc/issues/672","dependencies":[{"issue_id":"gqlc-monh","depends_on_id":"gqlc-35yu.9","type":"discovered-from","created_at":"2026-08-01T15:51:30Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-monh","depends_on_id":"gqlc-kph4","type":"blocks","created_at":"2026-08-01T17:24:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h4ug","title":"docs: sentinel taxonomy tables in docs/specs drift from codegen.allSentinels, with no fence","description":"docs/specs/codegen-stage-c3.md, c4.md, c5.md and c6.md carry taxonomy tables mapping\nconstructs to error sentinels, plus a mirror of codegen.allSentinels annotated with the\nstage that introduced each. Nothing checks those tables against the Go slice, so they\ndrift.\n\nKnown drift as of gqlc-35yu.9:\n  - the LIST / ANY property refusal added for apache-age-pgx-v5 (round 2) routes\n    through ErrUnrepresentableWidth and is not in the construct tables\n  - ErrUnrepresentableEdgeUnion (round 3, shared Phase A gate) is in neither the\n    construct tables nor the allSentinels mirror\n\nRaised by Carmack during gqlc-35yu.9 and deliberately not fixed there: the branch's own\nprecedent left the tables alone, and a drive-by two-row edit on an AGE-owned branch is\nthe wrong unit of work for drift that is already several entries deep.\n\nDo this as one sweep: reconcile every table against allSentinels, and decide whether\nthe stage specs are historical records of what stage Cn delivered (in which case a\nsentinel added by a later epic does not belong in them, and the mirror needs a new home)\nor a live index (in which case add a fence that fails when the two disagree, the way\nTestSentinelReachability already forces fixture coverage).","notes":"Fence landed on chore/sentinel-taxonomy-fence. Forks resolved:\n\n(1) Historical record vs live index — HISTORICAL, decided on evidence rather than\ntaste. C2/C3/C4/C5 taxonomy tables name ErrOutOfC2Scope..ErrOutOfC5Scope, sentinels\nretired by the per-stage rename; a fence over them would demand falsifying what each\nstage shipped. C6 §7 also declares itself the last stage, so a sentinel arriving after\nit has no stage spec left to be recorded in — the mirror genuinely has nowhere to go\ninside the series. So the stage specs freeze and the mirror gets a new home:\ndocs/specs/codegen-sentinel-taxonomy.md, a live index the fence owns. One pointer line\nadded to C6 §7 saying the live index moved; C0–C5 untouched, since a reader following\nthe series arrives at C6 and the new doc explains the relationship.\n\n(2) Fence shape — plain Go test, TestSentinelTaxonomy in internal/codegen/errors_test.go,\ninside package codegen so it reads allSentinels directly. No new just recipe and no new\nCI job: the required-context list on master is exact, and splitting a job silently\ndeletes a context. The existing `test` job runs it.\n\n(3) Naming the slice members — the test parses errors.go with go/parser and binds\nident-\u003evalue through the errors.New message literal, rather than a hand-written\nname switch. conformance_test.go sentinelIdent is already such a switch and is a third\nmirror of the same set; adding a fourth to fence the second would be the disease. The\nfence asserts message uniqueness so the binding stays sound.\n\n(4) Three directions, not two. index\u003c-\u003eallSentinels; taxonomy rows\u003c-\u003eindex; and\ndeclared-in-errors.go \u003c-\u003e documented (index union excluded). The third is the one the\nfirst two cannot see: a sentinel declared and left out of allSentinels leaves both other\nsets agreeing on a membership that never grew.\n\n(5) Rejected: asserting document row order matches slice order. Buys tidy diffs, costs a\nred suite on a legitimate reorder.\n\n(6) Rejected: naming age.ErrUnsupportedQuery / ErrUnsupportedSchema in the doc. They are\noutside allSentinels and have no exported set to fence, so naming them would add exactly\nthe kind of unfenced claim this bead exists to remove. The preamble says backend packages\ncarry their own sentinels without naming symbols.\n\nDrift found, all of it fixed on the same branch so the fence lands green:\n  - ErrUnrepresentableEdgeUnion is in allSentinels and has two invalid fixtures, and\n    appears in zero files under docs/. C6 §7 says fifteen sentinels; the slice has sixteen.\n  - ErrUnrepresentableWidth catchment is stale in kind, not just in rows. C3/C4/C5 tables\n    enumerate \"the eight unrepresentable widths\" as a property of the language. Since the\n    AGE backend it is the target TypeMap answer: AGE also refuses BYTES, the five\n    temporals, LIST and ANY, and unrepresentable_width_list_schema /\n    unrepresentable_width_any_schema pin widths no table names. The live doc states the\n    backend-relative rule instead of enumerating.\n  - The tables carry a stage-relative \"Retires at\" column that stopped meaning anything at\n    C6. The live doc drops it and keeps an \"Introduced\" column that can hold a bead id.\n\nVerified by mutation, not by assertion: six drift modes each fail with a message naming\nthe doc, the section and the edit — missing index row (the real drift), index naming a\nretired sentinel, missing construct row, missing exclusion row, misspelt sentinel, and a\nconstruct routed to a non-reachable sentinel. Gates: just test, just lint, just\nactionlint, just test-codegen-fence all green.\nREVIEW: VERDICT FAIL. Manager confirms two of the three blockers by direct test; the third by reading. Returned for a fix round.\n\nThe fence itself is sound. The reviewer broke it six ways and it named the document and the section every time; I had already broken it a seventh (an undeclared sentinel) with the same result. Every one of the author's five claims verified. The problem is not the fence — it is what the fence is fencing, and a hole in the one axis the fence was built for.\n\nB1 (BLOCKING). docs/specs/codegen-sentinel-taxonomy.md:81 states a phase at which the refusal cannot fire:\n\n  | ErrUnrepresentableWidth | A projected column, or a query parameter, whose property width the target's type table has no carrier for. | Phase A |\n\nPrepare runs phaseZAdmit before phaseAAdmit (prepare.go:238), and phaseZAdmit walks every property of every node and edge type, so any width the target refuses always loses at Phase Z. The code says so itself, in phaseAAdmit's own doc comment (prepare.go:544-547): \"Phase Z already caught schema-side offenders so a column projecting an unrepresentable-width property is unreachable unless the query declares an unrepresentable width on a parameter.\"\n\nI read that comment directly rather than take the report's word. Note it is the COLUMN half of the row that is unreachable; the parameter half is fine. So the row is half-wrong rather than wholly wrong, which is if anything worse — it reads as authoritative and a contributor cannot tell which half to trust. Corroborating: all five unrepresentable_width_* fixtures are *_schema, and §4 step 3 tells a contributor to add a fixture per construct, which for the column half is impossible.\n\nThe reviewer flags the same defect at :90 (ErrAliasRequired at Phase B, where prepare.go:717 calls rowFieldName(col.Name) on a column prepare.go:579 already called it on in Phase A — identical predicate, identical input, byte-identical error strings). I have not verified that second instance myself; the implementer should, and should say which way it went.\n\nThis is precisely the failure the fence cannot see. A fence holds a document against the code for CONSISTENCY. Nothing holds its prose against the code for CORRECTNESS, so the prose has to be right at commit time. Shipping a wrong phase inside a document whose header calls itself \"the live index\", in a bead whose entire premise is that documentation drifted from code, is not acceptable.\n\nB2 (BLOCKING). errors_test.go:176-177 has a silent `continue` on the axis the AST parse exists for:\n\n  if !ok || len(value.Names) != 1 || len(value.Values) != 1 { continue }\n\nI reproduced this myself. Adding to errors.go's own var block:\n\n  ErrMultiA, ErrMultiB = errors.New(\"multi a\"), errors.New(\"multi b\")\n\n— two sentinels, one ValueSpec, absent from allSentinels, documented nowhere — leaves `go test -run TestSentinelTaxonomy ./internal/codegen/` GREEN. Direction (c) is bypassed by a comma.\n\nWhat makes this blocking rather than a gap: twelve lines below, at :184, the author writes a LOUD Require for the adjacent failure mode (declared as something other than errors.New), and it fires correctly. The same author, in the same function, treated one neighbour as a hard error and the other as a skip. That asymmetry is not a scoping decision, and it is one line.\n\nSecond bypass of the same axis: errors_test.go:165 parses the single file \"errors.go\", so a sentinel declared in any other file of package codegen is invisible to (c). parser.ParseDir over the package closes it. Nothing in the repo pins sentinels to errors.go.\n\nB3 (BLOCKING, accepted on the reviewer's evidence). §2 omits the list-leaf construct for ErrUnrepresentableEdgeUnion. admitEdgeUnionCandidates has two call sites (prepare.go:597 column, prepare.go:1013 list-element chain), and invalid/unrepresentable_edge_union_shared_label_list has its own committed fixture for the second. §2's preamble asserts a sentinel carries EVERY construct in its catchment, and §2 does carry list-leaf rows for ErrUnrepresentableWidth and ErrOutOfC6Scope. So the document breaks its own stated contract, on the exact sentinel whose absence from the docs motivated this bead.\n\nNON-BLOCKING, and I am ruling on each rather than leaving them to the implementer:\n\nN1 — §3 is an unaudited silencing valve. The reviewer dropped ErrUnrepresentableEdgeUnion from allSentinels, moved its row to §3 with the text \"Silenced by fiat.\", deleted both fixtures, and the whole codegen tree stayed green while prepare.go:671 still returns that sentinel. That is real, but it is NOT a regression — the pre-commit state was no better, and the escape needs three coordinated edits. The repo already has the counter-measure in another package (internal/schema/gql/sentinel_coverage_test.go, TestAllSentinelsAreReachable, which reads errors off the wire for exactly this reason). FILE AS A FOLLOW-UP, do not fix here. The bead is about drift between doc and code; reachability is a different axis and deserves its own bead rather than being smuggled into this one.\n\nN2 — three rows present defensive branches (prepare.go:589, :650-655, whose own comments say a synthetic seam is required to reach them) as ordinary user-refusable constructs. FIX HERE. It is the same class as B1 — a contributor will try to write an impossible fixture — and the document already has a section for the distinction.\n\nN3 — \":13 says three renames ago; the chain C2-\u003eC3-\u003eC4-\u003eC5-\u003eC6 is four.\" FIX HERE. It is one word, and the sentence is load-bearing for the design fork the author used to justify the whole approach.\n\nN4 — \":29-30 says allSentinels is the closed set Generate may return on user input\", falsified by age.ErrUnsupportedQuery, which is not in allSentinels. FIX HERE, as the reviewer suggests: \"the shared front end may return\", not \"Generate\".\n\nN5, N6, N7 — leave. N5 and N6 are diagnosable in seconds, N7 is inherited wording from the repo's own sweepIdentifiers comment and changing it here widens the diff for no gain.\nReview FAIL addressed on the same branch.\n\nB1b, the ErrAliasRequired-at-Phase-B question the manager left open: the flag is\nCORRECT, the branch is unreachable. Structurally, prepare.go:579 (Phase A) and :717\n(Phase B) both call rowFieldName(col.Name) over the same q.Validated.Columns of the\nsame q, rowFieldName is a pure function of its argument, and Prepare runs phaseAAdmit\nto completion before phaseBDerive, first-offender-wins. So Phase A always returns\nfirst, with a byte-identical message.\n\nRather than argue it, I instrumented all eighteen sentinel fail-sites in prepare.go\nwith a file-sink counter and ran the whole suite. Only five are reached:\nEU_shared_label (x5), A_alias (x4), A_nonproperty_param (x2), L_width (x1),\nL_unknown_type (x1). The Phase B alias site is not among them, nor is the Phase A\nwidth-column site. First attempt at this measurement used println and read as\n\"nothing is reachable\" — go test hides passing packages stderr. Worth remembering.\n\nThe same sweep corrected two rows the review did not flag, in both directions:\n  - the list-leaf width row (L_width) and the list-leaf no-arm row (L_unknown_type)\n    ARE reached, so they stay in the reachable table. I had been about to demote the\n    first on the same shadowed-by-Phase-Z reasoning as the column row; the data says\n    no.\n  - A_backtick and A_width_param are reachable in principle but reached by no\n    fixture. Not a doc defect, a corpus gap; not fixed here.\n\nDocument restructured rather than patched: §2 now carries reachable constructs only,\nnew §3 \"Branches no input reaches\" carries the six shadowed/defensive branches with\nthe reason named per row (shadowed = an earlier phase applied the same predicate to\nthe same input; defensive = an upstream invariant would have to break). §4 is the\nold §3, §5 the old §4. This is what B1 and N2 have in common, and splitting the table\nis what makes §5 step 3 (\"a fixture per construct\") true instead of impossible.\n\nB2: declaredSentinels now walks every non-test .go file in the package rather than\nerrors.go alone, and a multi-name var spec is handled by pairing names with values\npositionally, with a hard Require when the two do not pair one to one. The managers\nown repro (ErrMultiA, ErrMultiB = ...) now fails, as does a sentinel declared in a\nsecond file, as does `ErrPairA, ErrPairB error`.\n\nB3: added the list-element-chain row for ErrUnrepresentableEdgeUnion, which\ninvalid/unrepresentable_edge_union_shared_label_list covers.\n\nN3 fixed (four renames, C2-\u003eC3-\u003eC4-\u003eC5-\u003eC6, not three). N4 fixed (§1 now says the\nshared front end, and says a Generate call can fail outside the set). N1 left alone\nper ruling.\n\nFence gained a fourth assertion, TestUnreachedBranchesNameRealSentinels: a §3 row\nmust name a sentinel allSentinels still holds. Six drift modes re-verified against\nthe renumbered document, including renaming a heading out from under the parser.\nGates: just test, just lint, just actionlint, just test-codegen-fence all green.\nHANDOFF 2026-08-01. Refix committed at 097cc79 on chore/sentinel-taxonomy-fence, tree clean. Adversarial review of the refix was dispatched and DIED ON SESSION QUOTA before a verdict — NO VERDICT EXISTS, do not merge. Re-dispatch against 097cc79.\n\nHighest-value attacks for the next reviewer: (a) the new section 3 'Branches no input reaches' documents SIX dead branches — for each, is it unreachable by a structural argument (an earlier phase tests a strictly weaker predicate on the same data) or merely un-hit by today's fixtures? A coverage gap filed as documented intent is worse than the original bug; (b) does ANYTHING fail when a section 3 branch becomes reachable again, or does the new fourth assertion only check that the names are real sentinels? (c) is the PHASE column fenced at all, or only the name — if phase is unfenced the entire B1 class returns silently, and B1 was a phase error; (d) re-run the ErrMultiA/ErrMultiB repro then push past it: an explicit-typed var, one built by fmt.Errorf(%w), one assigned from a non-errors.New call, one in a var(...) block mixed with non-error vars, one in a build-tagged file, an Err-named non-error, and an error not named Err — every silent skip is B2 again in a new coat.\n=== NEW SENTINEL LANDING ON fix/typemap-temporal-rejection (gqlc-9n6t, 2026-08-02) ===\n\ncodegen.ErrUnrepresentableTemporal (\"unrepresentable temporal kind\") joins\nallSentinels on that branch, making seventeen. The live index this bead moves to\ndocs/specs/codegen-sentinel-taxonomy.md does not exist on master yet, so the\nbranch could not add its rows without duplicating the whole file — recorded here\ninstead so whichever branch lands second adds them rather than the fence going\nred on a set it never saw.\n\nRows the index needs:\n\n  §1 Index | ErrUnrepresentableTemporal | A temporal-expression kind the\n  target's type table has no faithful Go carrier for. Sibling of\n  ErrUnrepresentableWidth rather than a reuse: the family splits on the\n  construct refused, and a temporal expression carries no property width, so\n  the edit it asks for is to the query's RETURN clause and not to the schema.\n  | gqlc-9n6t, after C6\n\n  §2 Refusal taxonomy | ErrUnrepresentableTemporal | A projected column whose\n  temporal kind the target's type table has no carrier for. | Phase B\n\n  §3 Branches no input reaches | ErrUnrepresentableTemporal | A list element\n  leaf whose temporal kind the target's type table has no carrier for.\n  Shadowed by backend capability, not by an earlier phase: the only target\n  refusing a temporal kind is Apache AGE, and its unserved-query gate drops any\n  list column before Prepare sees it, while the neo4j targets carry every kind.\n  Becomes reachable if a backend refuses a kind and serves list columns. |\n  Phase A\n\nNote §2 is Phase B, not Phase A — Phase A does not ask the type table about\ntemporal kinds; the refusal is at the row-field derivation site in\nphaseBDerive. Negative fixture:\ntest/data/codegen/invalid/unrepresentable_temporal_date_column (apache-age-pgx-v5).\n\nAlso worth knowing for the fence's third direction: the branch leaves the C0-C6\nstage specs untouched, per this bead's ruling (1) that they are historical\nrecords. C6 §7 therefore still says fifteen.\nIN-FLIGHT WORK PRESERVED (2026-08-02): worktree /home/antranig/Developer/gqlc/gqlc-h4ug-fence, branch chore/sentinel-taxonomy-fence, base 097cc79 (the commit that FAILED review), UNCOMMITTED refix on top. Agent killed mid-task by a quota limit, not by a failure. Dirty set: all eight docs/specs/codegen-stage-c*.md plus the taxonomy spec, and internal/codegen/{errors,errors_test,prepare}.go — consistent with the B1 reclassification of the three misclassified sites. Do not discard; read the diff before resuming.\nAGENT KILLED BY QUOTA LIMIT 2026-08-03 ~12:50 EDT, mid-refix. NOT a failure. Do not discard.\n\nWorktree /home/antranig/Developer/gqlc/gqlc-h4ug-fence, branch chore/sentinel-taxonomy-fence, HEAD 9d46684 (advanced from daff8ed, so work WAS committed):\n9d46684 test(codegen): measure the unreachable claim instead of asserting it\n4aae1ba test(codegen): separate unreachable branches from the taxonomy, widen the parse\n3096a9e test(codegen): fence the sentinel taxonomy against allSentinels (gqlc-h4ug)\nc198b3d codegen: TypeMap.Temporal gains a rejection channel (#714)\n1bc5a84 chore(bd): close the bdsync four, kph4 and gjqo (#708)\n474c238 codegen/age: the capture guard sweeps the corpus, not three hand-built shapes (#703)\neeb6f3f resolver: undirected orientation ambiguity is a disagreement, not a count (#702)\n1479d2e githooks: the bd-gh-sync pull path reports what it actually did (#698)\ndc89506 chore(bd): close gqlc-ak39 and record the beads it turned up (#696)\n375594e codegen/neo4j: decoder locals become positional, closing the shadowing class (#694)\n6cb1b24 chore(bd): close gqlc-35yu.10 and record the beads its review turned up (#689)\na912e3f codegen/age: the write path, and emitted names stop being capturable (#685)\n\nUncommitted survivors: modified docs/specs/codegen-sentinel-taxonomy.md, plus four NEW untracked invalid fixture directories:\n  test/data/codegen/invalid/invalid_entity_name_edge_label_mangle/\n  test/data/codegen/invalid/invalid_entity_name_node_label_mangle/\n  test/data/codegen/invalid/out_of_c6_scope_backtick_text/\n  test/data/codegen/invalid/unnamed_ambiguous_edge_label/\n\nThose four fixtures look like the answer to B1 — they give corpus reach to taxonomy rows that had none, which is exactly what 'assert NotZero for every section 2 row' requires. Read them before deciding anything.\n\nOutstanding from the failed round-2 review: B1 section 2 is unmeasured and ships a false row (ErrInvalidCardinality at prepare.go:554, coverage block 553.110,555.4 count 0 across all four binaries) — assert NotZero per section-2 fail-site with a NAMED exception list, then move or delete the false row. B2 the doc-anchor test asserts NotZero as a proxy for twelve; enumerate the twelve anchors as a literal expected set (file, heading, kind) and assert set EQUALITY. Promoted: N1 blockFor's tie-break depends on map iteration order; N2 isSentinelIdent has a hole; N3 comment volume; N4 downgrade section 2's preamble if measurement does not support it.\n\nAlso: master moved to c198b3d, which added sentinel ErrUnrepresentableTemporal. After rebase the fence should demand a taxonomy row for it — if it does not, that is itself a finding.\nQUOTA KILL 2026-08-03 ~19:00 EDT — state preserved. Worktree gqlc-h4ug-fence, branch chore/sentinel-taxonomy-fence, HEAD d5eff03, 137 uncommitted insertions across docs/specs/codegen-sentinel-taxonomy.md, internal/codegen/conformance/assembled_input_test.go, internal/codegen/errors.go and internal/codegen/prepare.go. This is round 5's fix for the pointer-inhabitant blocker: *resolver.ResolvedNode satisfies ResolvedType (all eight variants declare isResolvedType() and String() with VALUE receivers, validated.go:97-364) but 'case resolver.ResolvedNode:' does not match it, so prepare.go:620 column-unknown-variant and prepare.go:1051 list-elem-unknown-variant are both REACHABLE by an assembled Input and both return their sentinel cleanly with no panic. The prepare.go deletions are the two //gqlc:unreachable tags coming off. Resume from this working tree.\nQUOTA KILL RESCUE (2026-08-04). Agent carmack-h4ug-r8 was killed mid-round by a quota limit (resets 12:30am ET).\n\nState preserved, NOT lost:\n- Worktree: /home/antranig/Developer/gqlc/gqlc-h4ug-fence\n- HEAD: 284c40e334d019ef864edfaad0121537451d4c42 (advanced from 9697a05)\n- Three files modified, uncommitted: docs/specs/codegen-sentinel-taxonomy.md,\n  internal/codegen/conformance/assembled_input_test.go, internal/codegen/errors_test.go\n- Diffstat: 243 insertions, 108 deletions\n- CRITICAL: with those uncommitted changes, 'go test -count=1 ./...' returns RC=0.\n  The rebase fix WORKS; the agent died before committing it.\n- Backup: /tmp/quota-rescue/h4ug/uncommitted.patch (26578 bytes) plus file copies.\n\nRound-8 brief covered:\n1. Teach the go/parser derivation to exclude the int-typed successor sentinel\n   TemporalCount (added to internal/resolver/validated.go by the .11 merge) while\n   still reddening a Temporal-typed const with no String arm.\n2. prepare.go:448 (len(parts)==0 arm) became uncovered after the rebase; taxonomy\n   doc s2 row 114 already describes reaching it via Schema.Nodes keyed by the empty\n   LabelSetKey.\n3. The outside-iota-block hole in the same derivation.\n4. corpusCoverage discards stdout (_, s.corpusStderr, s.corpusErr = s.goCmd(args...)),\n   so TestCorpusRunsGreen reports only 'exit status 1' with empty stderr. This cost a\n   real diagnostic round. Commit 58920da already tried to close this and reproduced a\n   weaker form of it.\n\nResume: reapply nothing; the worktree already holds the work. Commit it, then verify\nthe remaining fold-ins.","status":"closed","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T19:03:22Z","created_by":"areqag","updated_at":"2026-08-04T09:50:08Z","started_at":"2026-08-01T20:43:27Z","closed_at":"2026-08-04T09:50:08Z","close_reason":"Merged in PR #779 (64ad1c4c)","external_ref":"https://github.com/areqag/gqlc/issues/667","dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-awtb","title":"codegen: narrow-width property conversions wrap silently instead of erroring","description":"A property whose schema width is narrower than the carrier the driver hands back is\nconverted at the field with a plain Go conversion, so a value that does not fit wraps\nsilently rather than failing. Found by Linus during review of gqlc-35yu.9.\n\nPre-existing and cross-backend: AGE narrows agtype's int64/float64 carriers to the\ndeclared INT32/UINT64/FLOAT32 width, and the neo4j targets do the same from their own\ncarriers. Neither reports the loss.\n\nThis is a correctness question, not a codegen-mechanics one: a store can hold a value\noutside the declared width (nothing in AGE or neo4j enforces a GQL property type at\nwrite time unless the writer went through gqlc), so the read path is where it surfaces.\n\nDecide the posture before implementing. Options: range-check at the field and fail the\ndecode; range-check and saturate; document that the schema's declared width is trusted.\nWhichever is chosen must be the same on every backend, since the emitted surface is\nbackend-invariant, and it needs corpus coverage at the boundary values.","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T19:03:21Z","created_by":"areqag","updated_at":"2026-08-01T19:49:59Z","external_ref":"https://github.com/areqag/gqlc/issues/668","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.15","title":"codegen/age: a multi-label node or edge type refuses the whole schema","description":"age.rejectMultiLabelSchema fails generation when the schema declares a node or\nedge type keyed on more than one label. Verified against apache/age 1.7.0:\n`CREATE (x:A:B {n: 1})` is a syntax error — AGE stamps exactly one label on a\nvertex or an edge and its parser has no syntax for a second — so a graph\nmatching such a type cannot be written through this backend and no value\nmatching it can arrive to be decoded.\n\nThe gate is on the schema's whole entity table rather than on the columns a\nbatch projects, because Phase Z names an entity for every type in the schema\nand the emitted Go surface is backend-invariant: a struct no query could fill\nis still a struct the package would have to declare, with a label check\nnothing could satisfy.\n\nThe cost is the same shape as gqlc-35yu.13's: a schema carrying one multi-label\ntype refuses AGE generation for the whole batch, including queries that never\ntouch it. test/data/codegen/valid/entity_multi_label_named is the fixture; it\nis enrolled for neo4j-go-v5 only.","acceptance_criteria":"Either:\n- a per-entity admission lands in the shared prepare phase, so a backend can\n  refuse one entity without refusing the schema, and the AGE gate narrows to\n  the queries that actually project the refused type; or\n- the bead closes recording that whole-schema refusal is the right posture,\n  with the reason stated where an author hitting it will find it.","notes":"SEQUENCING (2026-08-03): held behind gqlc-35yu.13 rather than parallelised, on a genuine collision — not on conflict risk as a blanket excuse.\n\nTwo reasons, and the second is the load-bearing one.\n\nTerritory: .15's answer lands in internal/codegen/age/prepare.go's gate and, if the per-entity route is taken, in the shared internal/codegen/prepare.go admission phase. .13 is in flight in exactly that gate — it is the LIST/ANY-width arm of the same unservedColumn machinery. Two agents editing one gate is a merge conflict for no gain.\n\nDesign: .15's two acceptance branches are not independent of .13's outcome. Both beads describe the SAME cost shape verbatim — 'a schema carrying one refused type refuses AGE generation for the whole batch, including queries that never touch it'. If .13 builds a per-entity admission mechanism, .15 consumes it and becomes a one-line enrolment. If .13 keeps whole-schema refusal, then .15 is no longer a per-bead choice at all: it is the same posture decision for both, and deciding it twice in two branches is how the two arms end up disagreeing.\n\nSo .15 starts after .13 merges, and its first act is to read what .13 actually did rather than re-deriving the posture blind. If .13 lands per-entity admission, .15 should be small. If it does not, .15 owns the posture decision for both arms and should say so in its own acceptance.\n\nOUTCOME (2026-08-03, branch feat/age-multi-label-posture): second branch taken — whole-schema refusal recorded as the posture, in docs/adr/0027-age-refuses-a-multi-label-schema-whole.md. Not on principle; both alternatives were built and measured.\n\nShape (a), omit the struct on AGE: enrolled valid/entity_multi_label_named in both targets, dropped the entity on AGE, regenerated goldens. TestBackendInvariantSurface RED — \"entity_multi_label_named declares a different Go surface under neo4j-go-v5 than under apache-age-pgx-v5\", the AGE surface holding 3 declarations to neo4j's 4.\n\nShape (b/c), emit the struct and narrow the refusal to projecting queries: TestBackendInvariantSurface stays GREEN, because the comparison is over declarations and decodePersonEmployee is an unexported receiver-less func it skips. It emits, at exit 0, a decoder reading `if label != \"Employee\u0026Person\"` — gqlc's own LabelSetKey join spelling, which no AGE vertex carries and no AGE statement can stamp. That is the disposition ADR 0025 rejected for temporal kinds.\n\nThe ADR 0025 channel shape does not transfer: the temporal enum splits, so its answer has to vary per member. Label-set arity does not — one label or unrepresentable, permanently, because the constraint is in AGE's grammar. A refusal channel whose answer can never vary reads as a capability.\n\nThe diagnostic is rewritten to carry the reasoning: names each refused type and its labels, gives `CREATE (x:A:B)` as the witness that does not parse, counts the queries the schema refusal takes with it, names both exits.\n\nNO invalid/ FIXTURE, and none is possible today: manifest sentinels resolve through sentinelByName's three lanes (codegen., queryfile., cypher.) and conformance imports no single backend, so age.ErrUnsupportedSchema is unnameable there. Filed as gqlc-rv0h. The corpus is instead pinned from the AGE side — TestRejectsMultiLabelSchema asserts both that the manifest omits apache-age-pgx-v5 and that this backend refuses the schema.\n\nINCIDENTAL FINDING: the GQL grammar admits no `\u0026` in an edge's key label set — `-[:WORKED_FOR\u0026FOR]-\u003e` fails with \"no viable alternative at input ':WORKED_FOR\u0026FOR'\". So the EntityEdge arm of the multi-label check is unreachable from real input. Kept, because codegen.Entity can carry one and a grammar change would reach it silently.\n\nUNVERIFIED: Docker is inaccessible on this machine, so nothing behind codegen_live ran. The AGE 1.7.0 parser facts are taken from this bead's own provenance, not re-verified live.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T17:25:36Z","created_by":"areqag","updated_at":"2026-08-04T00:15:18Z","closed_at":"2026-08-04T00:15:18Z","close_reason":"Merged: PR #752 (3kds/6rh3/vcvh) and PR #754 (.15).","external_ref":"https://github.com/areqag/gqlc/issues/669","dependencies":[{"issue_id":"gqlc-35yu.15","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-08-01T13:25:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-p6mh","title":"codegen/age: fence that every AGE target emits an identical SessionInit","description":"All 12 apache-age-pgx-v5 golden targets carry their own copy of SessionInit.\nNothing asserts directly that the copies agree.\n\nToday they agree by construction -- SessionInit is emitted from one template\nwith no per-target variation -- and the codegen fence proves each golden\nmatches what the emitter produces. So the property holds transitively. What\nis unpinned is the template gaining a per-target conditional: the fence would\nstay green while the copies diverged.\n\nThis matters because live_age_session_test.go binds ONE package's SessionInit\nto pools that hand out handles generated into others. That binding is only\nsound while the copies issue the same statements. The live arm checks the two\npackages it mixes (rightly -- that is its own precondition), but the general\nclaim is a pure-text property of the emitted tree and does not belong behind a\ntestcontainer.\n\nAssert it offline: read SessionInit's body out of every\ntest/data/codegen/valid/*/golden/apache-age-pgx-v5/graph.go and require all 12\nbyte-identical. No container, runs in the default build, fails the moment the\ntemplate grows a per-target branch.\n\nFound by Linus as NB2 on gqlc-35yu.8, where the finding was ruled a naming\ndefect in that bead's own test and the exhaustive claim was split out to here.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T15:53:58Z","created_by":"areqag","updated_at":"2026-08-01T15:58:01Z","external_ref":"https://github.com/areqag/gqlc/issues/659","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7f9a","title":"codegen: dollarTag is computed on bytes the emitted raw string does not carry","description":"Both backends emit a query's text as a Go RAW string literal:\n\n    internal/codegen/age/render_queries.go:89\n    internal/codegen/neo4j/render_queries.go:179\n    fmt.Fprintf(\u0026b, \"const %sQueryText = `%s`\\n\\n\", p.Bare, p.SourceText)\n\nThose two lines are byte-identical, so this is epic-wide, not an AGE gap.\n\n## The invariant that breaks\n\nGo discards carriage returns inside raw string literals -- it is in the spec,\nnot an implementation detail. So the VALUE of the emitted constant is\np.SourceText with every \\r removed, while everything computed at generate time\nwas computed on p.SourceText WITH them. I checked internal/queryfile for\nnormalisation and found none, so a CRLF-authored .cypher file carries \\r into\nSourceText and the two byte strings genuinely differ.\n\nFor neo4j the consequence is cosmetic: \\r\\n and \\n are the same query to the\nserver.\n\nFor AGE it is not, because dollarTag (age/render_queries.go:52) chooses the\ndollar-quote delimiter by scanning the text, and gqlc-35yu.7 established that\nthe delimiter must be chosen against exactly the bytes the parser will scan --\nthat was B1, where a tag occurrence straddling the text/tag boundary produced\ngenerated code that could not run. This is the same invariant failing for a\ndifferent reason: dollarTag scans the pre-emission bytes, and the parser sees\nthe post-emission ones. Removing \\r can create a $gqlc-shaped occurrence that\ndollarTag never saw.\n\n## Reachability is narrow -- confirm before investing\n\nOrdinary CRLF puts \\r at line ends, which cannot forge $gqlc$: a trailing\n`$gqlc\\r\\n` still has \\n after the token, not $. Breaking it needs a bare \\r\nINSIDE the token, e.g. `$gql\\rc` at the very end of the text, which no normal\nauthoring produces. That is why the .7 reviewer filed it non-blocking and why I\ndid not hold the merge for it.\n\nDo NOT start by writing the fix. Start by deciding whether the reachable set is\nempty. If it is, the right change may just be to normalise line endings at\nqueryfile read time so generate-time and emission-time bytes are the same by\nconstruction, which kills the class rather than the instance -- and which is\nprobably worth doing anyway for golden stability across checkouts with\ndifferent autocrlf settings.\n\n## Whichever way it goes, leave a fence\n\n.7's TestDollarTagClosesOnlyAtTheEnds (age/render_queries_test.go) covers the\npredicate but feeds it strings directly, so it cannot see this: the gap is\nbetween what dollarTag is HANDED and what is EMITTED. A fence has to compare\nthe emitted constant's value against the bytes the tag was chosen on.\n","status":"open","priority":3,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T14:28:06Z","created_by":"areqag","updated_at":"2026-08-01T14:28:42Z","external_ref":"https://github.com/areqag/gqlc/issues/656","dependencies":[{"issue_id":"gqlc-7f9a","depends_on_id":"gqlc-35yu.7","type":"discovered-from","created_at":"2026-08-01T10:28:06Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-fb4a","title":"codegen tests: sweep the type-map tables against their domain, not a hand-counted length","description":"internal/codegen/age/types_test.go and internal/codegen/neo4j/types_test.go (:45, :60, :96, :118, :155) drive their type-map assertions from hand-written literal tables. Both carried require.Len(table, \u003cliteral count\u003e) assertions, which assert that a literal sitting directly above has the length someone counted by hand: they fire only if an editor changes the table and forgets the number, and when they fire they name nothing about what was lost. The AGE ones were removed in gqlc-35yu.6; the neo4j ones remain.\n\nRemoving them leaves a real gap: deleting a row from a table now fails nothing, so a type silently stops being tested.\n\nFix: assert domain COVERAGE, not length — the union of the representable and unrepresentable tables must ElementsMatch the full domain, the way TestRunDriverAxis sweeps config.DriverValues(). That bites on both growth and shrink and is not tautological, because the expected side is derived rather than counted.\n\nBlocker: the enumerators do not exist. There is no graph.PropertyTypeValues(), no resolver.TemporalValues(), no resolver.ScalarValues() — config.DriverValues() exists only because the YAML loader needed one. This bead adds them to internal/graph and internal/resolver and then sweeps both backends. Note graph.PropertyType is an OPEN string type, so its enumerator is a maintained list rather than a closed set, and the enumerator itself needs a fence.\n\nDo both backends in one change: fixing one leaves the same hole plus an inconsistency.\n\nOut of scope: typeMap.Temporal returning \"any\" for every kind with no rejection channel is gqlc-9n6t. Temporal has no switch at all, so it has neither growth nor shrink coverage today — but that fence and 9n6t's rejection channel are the same edit, so 9n6t owns it.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T06:02:59Z","created_by":"areqag","updated_at":"2026-08-01T12:44:24Z","external_ref":"https://github.com/areqag/gqlc/issues/653","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ued2","title":"spec: the :many empty-result contract (empty slice, not nil) is emitted but unspecified","description":"Generated `:many` methods emit `out := make([]RowT, 0, len(records))`, so an empty result set returns a non-nil zero-length slice rather than nil. That is a deliberate emitter choice, but `docs/specs/codegen-stage-c1.md` never states it — the only nearby line is §'Columns is a non-empty slice', which is about something else.\n\ngqlc-35yu.4 strengthened the live battery from `require.Empty` (which accepts nil) to `require.NotNil`, so the behaviour is now pinned by a test while remaining absent from the spec. Test and spec disagree about what is normative.\n\nWorth settling before gqlc-35yu.8 brings the AGE arm into the same battery: if AGE's emitter reaches for `var out []RowT` the arm fails, and whether that failure is a bug in AGE or an over-tight test is currently unanswerable from the documents. The uniform-generated-interface promise argues for making it normative rather than relaxing the test.","acceptance_criteria":"codegen-stage-c1.md states the :many empty-result contract explicitly and distinguishes it from :one's ErrNoRows; the live battery's assertion cites it.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T03:58:13Z","created_by":"areqag","updated_at":"2026-08-01T04:12:22Z","external_ref":"https://github.com/areqag/gqlc/issues/646","dependencies":[{"issue_id":"gqlc-ued2","depends_on_id":"gqlc-rohp","type":"blocks","created_at":"2026-08-01T17:24:37Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-hhd0","title":"codegen tests: narrow-width list-element fixture (int32/float32 in a list) — golden-uncovered conversion branch","description":"Found by adversarial review of gqlc-35yu.1 (2026-07-31), deliberately deferred out of that bead.\n\nNo fixture exercises a NARROW-WIDTH LIST ELEMENT (int32 or float32 inside a list), so the driver-carrier conversion branch at internal/codegen/neo4j/render_queries.go:654 is not covered by any golden.\n\nWhy it was deferred rather than fixed in gqlc-35yu.1: that bead is a pure refactor whose entire gate is \"existing golden trees byte-identical\". Adding a fixture there would have introduced NEW golden files into a diff whose whole point is that goldens did not move, muddying the one signal the gate exists to produce.\n\nWhy it still matters: gqlc-35yu.1 justified deleting ListElem.Carrier / ListElem.UsesConvert and recomputing them via driverCarrier(e.GoType) on the grounds that the derivation is total. Review confirmed that by substitution over master's code — but for THIS branch specifically, \"the goldens prove it\" is weaker than it sounds, because no golden reaches it. The substitution argument stands; the empirical backstop does not exist yet.\n\nAdd a fixture (suggested name: list_int32) under test/data/codegen/valid with a query projecting a list of a narrow-width numeric type, plus its golden subtree. Note that gqlc-35yu.3 restructures fixtures into one dir with per-target golden subtrees — if that has landed by the time this is picked up, follow the new layout and the manifest targets list rather than the old one.\n","acceptance_criteria":"A fixture exercising a narrow-width list element exists under test/data/codegen/valid with its golden subtree; the conversion branch in the neo4j render path is exercised by the golden suite rather than only by substitution argument.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T00:18:08Z","created_by":"areqag","updated_at":"2026-08-01T00:19:58Z","external_ref":"https://github.com/areqag/gqlc/issues/636","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-modl","title":"TCK conformance ledger: execute the openCypher TCK against neo4j + AGE and track per-backend coverage","description":"Owner idea, 2026-07-31. Not yet scoped — this bead exists to hold the idea and the design tension, not to authorise a harness.\n\nProposal: run the whole vendored openCypher TCK generically against BOTH backends (neo4j and Apache AGE), analyse coverage, and incrementally drive it up.\n\nCURRENT STATE\n- The TCK is vendored at test/data/query/cypher/tck (tag 2024.3, via `just fetch-tck`).\n- It is consumed PARSE-ONLY, by internal/query/cypher/acceptance_test.go, over a SUBSET of feature dirs (match, return, match-where, return-skip-limit, union, with, with-where, return-orderby, with-orderBy, with-skip-limit). Nothing executes TCK scenarios against a live backend.\n- gqlc-35yu.4/.8 build a shared live scenario battery with per-backend adapters against live containers. That harness is the natural substrate for this work, which is why this bead depends on the epic.\n\nTHE DESIGN TENSION — RESOLVE BEFORE BUILDING\ngqlc is a code generator, not a query engine. The TCK tests ENGINE semantics. So \"TCK coverage\" is ambiguous across three quite different levels, with very different cost and value:\n  L1 PARSE      — does gqlc parse the scenario's query? (partially covered today)\n  L2 GENERATE   — can gqlc emit code for it? Most TCK scenarios carry no schema and no typed params, so this needs a synthesised schema per scenario.\n  L3 EXECUTE    — does the GENERATED code, run against backend X, produce the TCK's expected result table? This is the real prize and by far the most expensive: each scenario ships its own graph fixture as Cypher plus an expected-results table, so the harness must set up, generate, compile, run, and compare.\n\n\"100% coverage\" is probably the WRONG TARGET. A large fraction of the TCK is out of scope for gqlc BY DESIGN — dynamic queries, and everything gqlc deliberately rejects (C6 scope limits, alias-required expressions, and similar). Chasing 100% would mean either weakening those rejections or permanently carrying a pile of unexplained failures.\n\nThe more valuable and actually achievable goal: a CLASSIFIED COVERAGE LEDGER in which every TCK scenario is either passing on a given backend or carries a recorded, reviewed reason for not being — out-of-scope-by-design, backend dialect gap, or genuine gqlc gap. Then the number to drive to zero is \"genuine gqlc gaps\", not \"non-passing scenarios\", and the ledger doubles as a per-backend dialect-gap map (which is adjacent to gqlc-35yu.12).\n\nOPEN QUESTIONS\n- Which level (L1/L2/L3) is the target? L3 per-backend is what \"validate against both implementations\" implies, but it is a large harness.\n- Is the ledger a committed artefact (reviewable diff when coverage shifts) or a generated report?\n- Where does it run? L3 against two live containers is nightly-job shaped, not PR-CI shaped.\n","acceptance_criteria":"Framing agreed with the owner (which of the three coverage levels this targets, and how deliberately-out-of-scope scenarios are classified) before any harness work starts.","notes":"\n=== FRAMING RESOLVED 2026-08-01 — acceptance criterion met, harness work authorised ===\nDecided under standing delegated authority (owner directive 2026-08-01: decide, record,\nreport). Reversible: nothing is built yet.\n\n1. LEVEL — staged L1 -\u003e L2 -\u003e L3, one ledger spanning all three.\n   The three levels are not alternatives; they are columns. A scenario that does not\n   parse cannot generate, and one that does not generate cannot execute, so the ledger\n   carries a per-level status and the levels gate each other naturally.\n   L1 (parse) is backend-INDEPENDENT — it tests gqlc's Cypher front end, not a backend.\n   It needs no container and no part of the 35yu epic, so it is separable and starts\n   first. Split out as its own bead (see below); gqlc-modl retains L2/L3.\n   L3 is the real prize and stays gated on 35yu.4/.8's live harness.\n\n2. \"100% COVERAGE\" IS THE WRONG TARGET — this reverses the shape of the owner's original\n   ask deliberately. A large fraction of the TCK exercises dynamic queries and constructs\n   gqlc rejects BY DESIGN (C6 scope limits, alias-required expressions). Driving\n   non-passing to zero would mean weakening those rejections. The number driven to zero\n   is GENUINE GQLC GAPS.\n\n3. CLASSIFICATION IS A CLOSED ENUM, and unclassified is a hard failure:\n     pass | out-of-scope-by-design (must cite the ADR or spec clause) |\n     dialect-gap (backend cannot; gqlc could) | gqlc-gap (genuine)\n   A scenario present in the vendored TCK with no ledger row, or a row with no reason on\n   a non-pass, fails the build. Per AGENTS.md \"make illegal states unrepresentable\": an\n   unexplained failure must not be representable, because that is precisely how a ledger\n   decays into a pile of ignored red.\n\n4. THE LEDGER IS A COMMITTED ARTEFACT, not a generated report.\n   Why: the value is the reviewable DIFF when coverage shifts. A generated report shows\n   state; a committed ledger makes a silent regression — a scenario that stops parsing —\n   a red line in a PR diff a human must approve. Direct precedent in this repo: the godog\n   skiplist and the mustReject pins are committed for exactly this reason, and AGENTS.md\n   already requires skiplist updates to travel in the same branch as the behaviour change\n   that causes them. The ledger inherits that rule.\n\n5. WHERE IT RUNS — L1 and L2 in PR CI (pure parse/generate, no containers, no meaningful\n   wall-time). L3 in the nightly codegen_live job alongside 35yu.8. PR CI wall-time stays\n   untouched, consistent with the epic's locked constraint.\n\n6. SEQUENCING — L1 does not depend on the 35yu epic and may start whenever; L2/L3 stay\n   blocked on 35yu.4/.8. gqlc-modl is NOT to be started while the 35yu epic is in flight.\n\n=== CORRECTION 2026-08-01 — the CURRENT STATE above is STALE, and L1 IS ALREADY DONE ===\nRead the code before acting on this bead; the description was written from a stale\nsnapshot. Measured on master at 536931a:\n\n  - internal/query/cypher/acceptance_test.go enrols ALL 37 TCK feature leaf dirs, not the\n    10-dir subset the description claims. 220 .feature files, 1615 scenarios.\n  - The skiplist carries 140 entries, every one classified into one of EIGHT declared\n    skipCategory constants (catRuntimeError, catResultAssertionOnly, catValueBelowBoundary,\n    catGroupingKeySemantic, catBindingKindConflict, catWriteShapeConstraint,\n    catClauseComposition, catSignatureArgCheck).\n  - TestSkiplistCategoryPolicy already enforces both halves of the invariant I specified\n    in the framing note: an entry with no valid category fails, AND a declared category\n    with zero entries fails as a dead policy slot.\n  - The corpus is never edited; skipped scenarios are reported and counted, never deleted.\n\nCONSEQUENCE 1 — do NOT file or build an \"L1 ledger\" bead. L1 coverage is complete and the\nclassified-ledger mechanism already exists, in production, with its own policy test. My\nframing note above independently specified this design; the repo got there first. That is\na strong signal the pattern is right.\n\nCONSEQUENCE 2 — this bead's job is narrower and cheaper than the description implies:\nEXTEND the existing skiplist/skipCategory mechanism to L2 (generate) and L3 (execute per\nbackend), rather than designing a new ledger format. Same committed-artefact rule, same\n\"uncategorised is a build failure\" rule, same \"never delete from the corpus\" rule. Reuse\nskipCategory's shape; the L3 taxonomy needs at least one new family the L1 set lacks —\nbackend dialect gap — which is where this bead meets gqlc-35yu.12.\n\nCONSEQUENCE 3 — the honest headline number today is L1 = 1615 scenarios enrolled, 140\ndeliberately accepted with a recorded policy reason, 0 unexplained. That is the format the\nL2/L3 columns should report in.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T00:13:04Z","created_by":"areqag","updated_at":"2026-08-01T03:27:08Z","external_ref":"https://github.com/areqag/gqlc/issues/637","metadata":null,"dependencies":[{"issue_id":"gqlc-modl","depends_on_id":"gqlc-35yu.4","type":"blocks","created_at":"2026-07-31T20:13:12Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-modl","depends_on_id":"gqlc-35yu.8","type":"blocks","created_at":"2026-07-31T20:13:13Z","created_by":"areqag","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-35yu.12","title":"codegen/age: dialect-gap posture for neo4j-only Cypher in user query text","description":"User query text executes verbatim (ADR 0005), so a query calling neo4j-only functions (datetime(), duration(), point(), etc.) or unsupported openCypher constructs generates fine for the AGE target and fails at RUNTIME with an AGE error. Decide and implement the posture: document-only (list known gaps, let runtime errors surface them) vs a backend-specific admission hook in shared phase A that flags known-unsupported constructs at generate time. Evidence from the live battery and early AGE usage should inform which gaps are actually hit before building detection machinery. Post-epic backlog: intentionally unblocked, not on the epic critical path.","acceptance_criteria":"A decision is recorded (document-only or admission hook) with rationale; if document-only, the generated package or docs name the known gaps; if admission hook, at least the verified-unsupported construct list errors at generate time with invalid fixtures.","notes":"LANDED 2026-08-17. PR #809, squash-merged as 82503c9f. GH issue #634 closed by the PR (verified via closingIssuesReferences: [634] only — umbrella #633 untouched and still OPEN). The ledger no longer lies.\n\nBranch history: 21 commits, rebased onto master f1f23cb6 (the go1.26.6 toolchain patch from PR #810) with zero content drift — `git diff 3ce7798a 32c1bc45` touched only the two go.mod `go` directives. Final head 32c1bc45.\n\nLIVE EVIDENCE, recorded because it corrects a standing assumption. The AGE live arm IS workflow_dispatch-able (codegen-live.yml, `if: github.event_name != 'pull_request'`), so it was run on the branch rather than waiting for the nightly: run 31996378339, job live-smoke-age, conclusion SUCCESS.\n\nThe job log carries the recipe verbatim:\n  cd test/data/codegen \u0026\u0026 go test -count=1 -tags codegen_live -run 'TestLiveSmoke|TestAGESessionInit|TestAGERefusesRelationshipTypeAlternation|TestAGERefusesTheFunctionsItDoesNotDefine' -skip 'TestLiveSmoke/neo4j' ./...\n  ok  github.com/areqag/gqlc/test/data/codegen  11.499s\nwith zero `no tests to run` lines anywhere in the log.\n\nStated precisely, because the difference matters: the recipe carries no -v, so no per-test PASS line is printed. What the log proves directly is that the package passed against real containers under a -run naming both witnesses as whole alternatives, and that nothing was skipped for want of a match. That both witnesses executed follows from the regex plus their declarations at test/data/codegen/live_age_dialect_test.go:89 and :196 — an inference from three checked facts, not a printed line. Anyone wanting the printed line needs -v in the recipe.\n\nThat run was at 3ce7798a, which differs from the merged content only in the go.mod `go` directive.\n\nCI on #809 at 32c1bc45: lint, test, tidy, codegen-fence, live-smoke, govulncheck, actionlint all SUCCESS. live-smoke-age SKIPPED, as designed for pull_request (bd gqlc-0oau).\n\nReview residue filed, not fixed: gqlc-35yu.16 (the diagnose message ships two universals — \"no temporal constructor at all\", \"AGE's whole temporal surface is timestamp()\" — that no witness carries; provenance is the unwitnessed 348-function figure at ADR 0028:29), gqlc-35yu.17 (the witness sweep binds the server answer INTO the body but not into an assertion; mutation M18 survived). gqlc-kb2c amended with a seventh cardinal site outside docs/: the \"five constructor names\" in test/data/codegen/invalid/unrepresentable_temporal_localtime_column/queries.cypher. gqlc-osf1 left open and untouched — the table is still known short.\n\nFollow-up PR #829 strips eight round-N review-narration clauses from dialect_test.go (AGENTS.md: comments do not narrate changes); the ~48 `review mutation X` citations were kept deliberately.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-31T23:12:37Z","created_by":"areqag","updated_at":"2026-08-17T05:30:16Z","closed_at":"2026-08-17T03:41:34Z","close_reason":"PASS after six adversarial rounds. Branch feat/age-dialect-gap-admission @ cc4df90e, 20 commits, unpushed pending user go-ahead.\n\nPosture implemented per the 2026-08-04 RULING: admission hook populated only by verified constructs, extending .14's existing text gate rather than building a parallel one. Two gaps carry live-measured probes (ErrRelationshipTypeAlternation, ErrUndefinedFunction).\n\nLead first-party verification (verdicts at /tmp/lead/verdict-35yu12r{5,6,7}fix.md). Final matrix at cc4df90e, go vet as compile precondition, RUN=6385 on every row, files md5-restored: R1 KILLED, D1 KILLED (2 named tests), D2CTRL KILLED, D2CONSEQ KILLED (2 named tests), DELDUP KILLED naming its own row. Every row identical to decef24c, so the prose-only final commit moved no behaviour.\n\nFindings closed across rounds: the round-5 tables held claims nothing re-stated (both lists now pinned where the claim is made); the round-6 count pin caught a name that LEFT but not one DUPLICATED, restoring the R1 hole while the pin held (closed by a complaint in recipeBodies, the choke point both justfile readers share, shown failing by DELDUP rather than assumed); the round-7 pass removed the recipe cardinal from 15 prose sites so it is written only at the pin.\n\nAlso corrected: 'both AGE recipes already carry a -skip' was false since round 3 — test-codegen-live carries none. Verified against the justfile by the lead.\n\nResidue filed: gqlc-kb2c (ADR probe cardinals rot together). Declined: R1PIN (a two-point edit defeating a pin's own literal is generic to every require.Len and carries no branch-specific information). Left open and stated in the ADR: the construct list does not catch a row that is wrong.","external_ref":"https://github.com/areqag/gqlc/issues/634","dependencies":[{"issue_id":"gqlc-35yu.12","depends_on_id":"gqlc-35yu","type":"parent-child","created_at":"2026-07-31T19:12:37Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-828","title":"schema/gql: labelSets' defensive clone is unpinned — dropping it aliases the caller's label slice and the suite stays green","description":"`labelSets` (internal/schema/gql/resolve.go:140-150) builds a declaration's complete\nlabel set by appending the implied labels to the key ones:\n\n    return keyKey, append(slices.Clone(key), implied...).Key(), true\n\nDropping the clone — `append(key, implied...)` — leaves the entire suite green:\n\n    go build ./...            ok\n    go test ./internal/...    ok\n\nThat is an unpinned aliasing guard, not an unnecessary one. `append` on a slice with\nspare capacity writes into the caller's backing array, so without the clone\nlabelSets would scribble the implied labels over whatever follows the key labels in\nthe caller's storage. The caller is resolve()'s node and edge loops, and the slice\nis the rawNode/rawEdge's keyLabels — live data that is read again later in the same\npass (resolve.go:57 nodeKeyLabels, :113 edgeKeyLabels).\n\nWHY IT CANNOT BITE TODAY, AND WHY THAT IS NOT REASSURING. Every LabelSet that\nreaches labelSets is produced by labelSet() at nodetype.go:36-53, which returns\neither `graph.LabelSet{name.GetText()}` (len == cap == 1) or\n`make(graph.LabelSet, len(names))` (len == cap). With no spare capacity, append\nalways reallocates and the clone is redundant. So the clone's correctness depends\non a capacity invariant that\n\n  - is established in a different file from the code that relies on it,\n  - is stated nowhere, in a comment or a test, and\n  - would be silently undone by any future collector that accumulates labels with\n    append rather than sizing a make() up front — which is the obvious way to write\n    one, and is how impliedLabels is already threaded through rawNode/rawEdge.\n\nFOUND BY the 29-case mutation sweep of resolve.go (2026-07-28). The file is\notherwise in good shape: 26 of 29 mutations were caught outright, including every\nsentinel swap and both duplicate-type checks. This was the only survivor that\nrepresents a real guard. The two others are provably dead writes, recorded in bd\nmemory and deliberately not filed:\n\n  :61  `if n.alias != \"\"` gating idx.aliases[n.alias]\n  :64  `if n.name != \"\"` gating idx.declared[n.name]\n\nBoth widen to write under the \"\" key, and neither map is ever read with an empty\nkey: the only two read sites, resolve.go:209 and :211, sit lexically inside\n`if ref.alias != \"\"` in rawEndpoint.resolve. Equivalent mutants.\n\nTHE FIX is a unit test rather than a code change, because the code is already\ncorrect — what is missing is the assertion that keeps it correct.\nTestLabelSetsDoesNotWriteThroughItsInput hands labelSets a key slice with spare\ncapacity and asserts the byte beyond len is untouched. It has to assert on the\nbacking array rather than on the slice: appending past len leaves the caller's\nslice header alone, so `key` reads as one element with or without the clone, and\nonly the sentinel behind it moves. Verified 1:1 — the test fails with the clone\ndropped and passes with it restored, and no other test in the suite moves either\nway.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:58:03Z","created_by":"areqag","updated_at":"2026-07-28T16:01:02Z","started_at":"2026-07-28T15:58:15Z","closed_at":"2026-07-28T16:01:02Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/579","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-nu2","title":"test(resolver): pin the group-zero contract stated by three guards in scope.go","description":"The 120-case mutation sweep of internal/resolver/scope.go found three guards\nthat each state the same contract — group id 0 means \"not in an OPTIONAL\ngroup\", not \"in the group numbered zero\" — and no test anywhere pinned any of\nthem.\n\n    scope.go:403  addMember's    `if v == \"\" || g \u003c= 0`\n    scope.go:427  demoteGroup's  `if g == 0 || demotedGroups[g]`\n    scope.go:837  Export's       `if g \u003e 0`\n\n## Why the sweep could not tell them apart\n\nEach survives alone, so on a first read all three look inert. They are not: the\nfirst two mask each other exactly. addMember refuses to enter a group-0 name in\nthe membership table, and demoteGroup refuses to demote group 0 even if a name\ngot in. Drop either alone and the other still blocks the cascade. Only the\nCOMBINED mutation discriminates — with both dropped, a proven non-nullable\nbinding demotes \"group 0\", which sweeps up every other groupless name in the\nPart.\n\nThis is the class-5 mutually-masking pair from the survivor triage, measured\nrather than argued:\n\n    SURVIVED   addMember g\u003c=0 alone\n    SURVIVED   demoteGroup g==0 alone\n    CAUGHT     PAIR addMember + demoteGroup\n    CAUGHT     Export g\u003e0 alone\n\nExport's `g \u003e 0` is the third statement of the same contract and a chained\nlink: relaxed, it writes a zero id into the carry, where addMember rejects it on\narrival. It is only reachable as a defect if addMember is also relaxed, so it\nneeds its own direct assertion rather than a behavioural one.\n\n## Parser reachability\n\ncypher/listener.go:159-162 records the invariant nullable \u003c=\u003e optionalGroup \u003e= 1,\nand build.go:280-290 reaches the nullable constructors only when\noptionalGroup \u003e 0, so the violating shape is parser-unreachable today. The\ntest-only constructor query.NewNullableNodeBinding (query.go:332, no group\nargument) can express it at the scope layer, which is where the three guards\nlive. Same posture as the shadow-cascade tests merged in #569: pin the\ninvariant where it is stated, and treat parser-reachability as a separate\nquestion.\n\n## Fixed here\n\ninternal/resolver/scope_test.go only; no production change.\nTestScopeGroupZeroIsNotAnOptionalGroup drives one nullable groupless binding\nand one required groupless binding through SeedLocalNullability +\nDemoteNullability, asserts the nullable one is not demoted by the other's\nwitness, and asserts directly that neither name reaches exportedOptionalGroup.\n\nVerified 1:1 — the pair mutation and Export's half are each caught by this test\nand by nothing else.\n\nRelated: gqlc-bef item 1.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:18:30Z","created_by":"areqag","updated_at":"2026-07-28T15:21:54Z","started_at":"2026-07-28T15:18:35Z","closed_at":"2026-07-28T15:21:54Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/571","dependencies":[{"issue_id":"gqlc-nu2","depends_on_id":"gqlc-bef","type":"related","created_at":"2026-07-28T11:18:35Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-bef","title":"resolver: scope.go sweep residue — 8 of 10 survivors triaged and closed, the last 2 blocked on gqlc-ka1's ADR","description":"The residue of the 120-case mutation sweep of internal/resolver/scope.go that gqlc-efe did not fix. Filed so the next sweep of this file does not re-derive the triage from scratch, and so the genuinely-open items are not lost among the equivalent mutants.\n\nSTATUS: eight of the ten items are now closed. The two that remain (3 and 4) are\nboth blocked on the same ADR — gqlc-ka1's choice between per-fixture message\nassertions and split sentinels. Nothing further in this bead is actionable\nwithout that decision.\n\n## Argued inert — do not re-file\n\n  :606  virtualProjection's Hops() TypeList/TypeEdge stamp. Already gqlc-2y7's dead code.\n  :298  InferUnlabelled's r3EdgeAdmissible re-check. The code comment says the check already ran at BindEdge, so every edge reaching here has passed it.\n  :677  refProjectionType's carried-alias `ref.Property == \"\"` half. The doc comment says a property lookup on a carried alias is parser-unreachable — the scope check rejects Ref{\"c\",\"p\"} unless c is a binding name.\n  :387  DemoteNullability's 5xg EdgeBinding arm. The comment says it outright: an edge is never bare, so the parser passes a constant false (cypher/pattern.go:314).\n  :380, :406, :432  the three \"is the name already in the table\" present-guards. Dropping them writes false where the entry was absent, and false is also the zero value every reader gets from a missing key, so the mutants are equivalent.\n  :583  materialiseReturns' empty-scopeOrder bail. Returns nil rather than an empty non-nil slice; len is 0 either way and nothing compares against nil.\n  :481  the endpoint loop's `v == \"\"`. Unreachable: NewVarEndpoint rejects the empty variable (query.go:1158-1161), and an unnamed endpoint is an InlineEndpoint, a different sum member. Only the zero value VarEndpoint{} could carry \"\", and no constructor produces one. (Resolved by gqlc-dg3.)\n  :759  Export's localGroup `v == \"\"`. A dead write: localGroup is read at :836 only for names in exportedNames, and buildScopeOrder excludes \"\" at :540. (Resolved by gqlc-dg3.)\n  :486  DemoteNullability's `changed = true` after the node write. Provably redundant rather than untested: nothing reads nullableBinding to decide edge admissibility, so any state change that could enable a new edge also sets changed via demoteGroupsOf at :488. Equivalent mutant.\n  :292, :303  InferUnlabelled's two pending gates. See item 7 below.\n  :540  buildScopeOrder's `seen[v]` dedup. See item 8 below.\n  :232  BindCall's slot nullable bit. Dead field, not a missing test — see item 5 below; it belongs to gqlc-2y7.\n  :977-980  WitnessUse's ExprUse error propagation. Unreachable by construction — see item 10 below.\n\n## Closed\n\n1. ~~The group-zero triple~~ — RESOLVED by gqlc-nu2 (PR #572). The premise in the original filing was wrong: the three do not all survive together. addMember's `g \u003c= 0` (:403) and demoteGroup's `g == 0` (:427) are a mutually-masking pair (each survives alone, the PAIR is caught), and Export's `g \u003e 0` (:837) is a chained link, inert behind addMember's rejection of a zero id on arrival, so it needs a direct assertion. The answer to \"can a binding be Nullable() with OptionalGroup() == 0\" is: not from the parser (cypher/listener.go:159-162 records nullable \u003c=\u003e optionalGroup \u003e= 1), but the test-only nullable constructors can express it at the scope layer, which is where the guards live.\n\n2. ~~The anonymous-binding guards~~ — RESOLVED by gqlc-dg3 (PR #574), and one of them was a real defect. \"Does anything read the \"\" key\" has different answers per site, so they do not triage as a class. Only an EdgeBinding may be anonymous (query.go:86), so two anonymous edges in one Part both write nullableBinding[\"\"] and the last wins — making SeedLocalNullability's :340 and addMember's :403 a load-bearing masking pair that, dropped together, reports OPTIONAL-matched bindings as non-nullable. The other two (:481, :759) moved to the inert list above. See bd memory sweep-shared-sentinel-key-collision.\n\n5. ~~BindCall's slot nullable bit (:232)~~ — CLOSED, and the premise was wrong twice over. The filing said \"no fixture yields a nullable CALL YIELD scalar\"; valid/call_standalone_yield_star_string_nullable.cypher does exactly that and its golden pins `\"nullable\": true`. The real reason the bit is unpinned is that it is never read: hard-wiring it to false SURVIVES, and hard-wiring it to true ALSO SURVIVES. Projection nullability comes from a different lane — callProjectionType (resolve.go:719-722) reads `nullableBinding[ref.Variable]`, not `slot.nullable`. Proven by deletion rather than by grep: removing the field from callBindingSlot and its write at :232 leaves `go build ./...`, `go vet` and the full `go test ./internal/...` all green. This is dead code, not a test gap, and gqlc-2y7's 13:36 comment already owns the field family. Moved there; do not re-file here.\n\n6. ~~CloseEdges' deferred-close error (:275-277)~~ — RESOLVED by gqlc-0ul (PR #576). Such an input does exist: an edge deferred for an unlabelled endpoint that still fails closure after inference, reached via orientation rather than labels. All six pre-existing negative fixtures spell both endpoints, so none ever defers.\n\n7. ~~InferUnlabelled's two pending gates~~ — CLOSED as inert, measured. Widening `len(bb.Labels()) == 0` (:292) to admit every node SURVIVES, and so does dropping `len(pending) == 0` (:303) and its duplicate at resolve.go:570. The reason :292 does not matter is that inferUnlabelled filters the candidates again at resolve.go:582 (`if len(resolved) \u003e 0`), and THAT filter is pinned — dropping it is CAUGHT by valid/carry_wins_over_unlabelled_rebind.cypher. The two empty-pending bails are pure short-circuits standing immediately ahead of `for len(pending) \u003e 0` (resolve.go:592), which is already a no-op on an empty slice. Equivalent mutants.\n\n8. ~~buildScopeOrder's `seen[v]` dedup (:540)~~ — CLOSED as an equivalent mutant; the premise is impossible, not merely untested. The filing asked for \"a Part whose bindings name the same variable twice\". No such Part can exist: cypher/pattern.go:398-425's mergeBinding keys on `part.byVar[variable]` and folds a re-reference into the existing binding record, so Part.Bindings is variable-unique by construction. Probed directly — `MATCH (a:Person), (a:Person)`, `MATCH (a:Person) MATCH (a)` and `(p)-[r:KNOWS]-\u003e(p)` each yield exactly one binding per variable. A fixture was written for this, measured as non-pinning, and deleted rather than shipped. Agrees with gqlc-2y7's independent TRIPWIRE-S result.\n\n10. ~~WitnessUse's ExprUse error propagation (:977-980)~~ — CLOSED as a defensive tripwire, unreachable by construction. Swallowing the error SURVIVES. resolveType (resolve.go:762-810) returns a non-nil error from exactly two arms: list-of-nodes (:791) and list-of-edges (:793); every other arm returns a value or panics. So the propagation fires only if an ExprUse's EnclosingType is LIST\u003cNODE\u003e or LIST\u003cEDGE\u003e — and it cannot be, because of the thing that creates the ExprUse in the first place. An ExprUse exists only because a parameter sits inside the enclosing expression; a parameter types as TypeUnknown; and listLiteralType (cypher/typing.go:558-568) takes commonType over the elements, which collapses to TypeUnknown the moment two element types differ. So any list containing a parameter has element TypeUnknown. The only other producer of LIST\u003cEDGE\u003e, refType at cypher/expr.go:294, fires on a bare var-length Ref, which is a RefProjection and bypasses resolveType entirely (gqlc-2y7 §2). Probed 22 shapes — list literals, nested lists, concatenation, IN, CASE, coalesce, indexing, SET value, both node and edge — and not one produced a list-of-nodes/edges enclosing type. The two list arms ARE pinned, via the projection path, by invalid/list_of_nodes_projection.cypher and invalid/list_of_edges_projection.cypher.\n\n## Genuinely open — both blocked on gqlc-ka1's ADR\n\n3. PropertyUseWitness's out-of-scope arm (:929-931) is unreachable because WitnessUse gates on Contains() first (:963). Confirmed independently by the gqlc-9gz emitter audit. A guard chain of the listener.go :67 -\u003e :92 -\u003e :113 kind: relax the gate and the downstream arm starts mattering, with no test on it. See bd memory listener-sweep-survivors.\n\n4. refProjectionType's out-of-scope arm (:680) — DIAGNOSED by gqlc-9gz (PR #578), still open to fix. The full ErrOutOfR0Scope audit found eighteen emitters against five fixtures, with the five mapping 1:1 onto the five proven sites. :680 is unreached because resolve.go:200 rejects path/unwind bindings first; relax :200 and :680 covers for it, raising the identical sentinel from another file. Pinning :680 on its own therefore needs either a per-fixture message assertion or a split sentinel — that is gqlc-ka1's ADR, so it was deliberately not done. The ten defensive emitters are enumerated in gqlc-9gz and should not be re-filed.\n\n## Method note\n\nTwo of the three items closed first were mutually-masking pairs whose members each read as inert alone. The COMBINED mutation is what discriminates, and it should be run on any group of guards that state the same contract *before* concluding they are equivalent.\n\nMore generally: in six of the eight closed items the original filing's stated premise turned out to be wrong or incomplete — not the verdict, the *reason*. Item 5 was filed as \"no fixture covers this\" when a fixture does cover it and the field is simply unread. Item 8 asked for an input the parser cannot construct. Item 1 asserted the three guards survive together when the middle pair does not. Measure the premise, do not argue it; and when a mutation survives, find out *which* downstream site is covering for it before deciding what the survival means.\n\n## Sentinel-coverage note (from gqlc-9gz)\n\nPer-sentinel coverage is not per-emitter coverage. ErrOutOfR0Scope had five\nfixtures and eighteen emitters, and TestSentinelReachability was green\nthroughout. Worse, a fixture can prove a sentinel reachable without proving\n*which site* raises it: if removing the guard under test leaves the query\nfailing with the same sentinel from a downstream site, the mutation survives.\nWrite the fixture so that removing the guard makes it PASS. See bd memory\nsweep-negative-fixture-must-not-name-rejected-binding.\n\n## Two ways a survivor can be \"covered for\" (generalised from items 7 and 10)\n\nBoth closed-as-inert items this round were survivors whose contract was held up\nsomewhere else, and the two mechanisms are worth naming because they look\nidentical from the sweep output:\n\n  - A LATER FILTER restates the guard. :292 admits too much, but resolve.go:582\n    narrows it again before anything observes the difference. The later filter is\n    the one worth pinning; the earlier one is an optimisation.\n  - THE INPUT CANNOT BE BUILT. :977-980 needs an ExprUse over a list of nodes,\n    and the parameter that creates the ExprUse is exactly what forces the list's\n    element type to UNKNOWN. The guard is not untested, it is unreachable, and\n    the proof lives in the producer (typing.go's commonType), not in the consumer.\n\nThe first deserves a test somewhere else in the chain. The second deserves a\ncomment in situ and nothing more.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T15:12:19Z","created_by":"areqag","updated_at":"2026-07-30T03:01:59Z","started_at":"2026-07-30T02:29:35Z","closed_at":"2026-07-30T03:01:59Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/570","dependencies":[{"issue_id":"gqlc-bef","depends_on_id":"gqlc-2y7","type":"related","created_at":"2026-07-28T11:50:07Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-bef","depends_on_id":"gqlc-efe","type":"related","created_at":"2026-07-28T11:12:24Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-bef","depends_on_id":"gqlc-ka1","type":"related","created_at":"2026-07-28T11:50:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-825","title":"schema/gql: canonicalSpelling's comma-space collapse is dead code until gqlc-5md lands, and nothing pins it","description":"Found by a 31-case mutation sweep of internal/schema/gql/propertytype.go (24/31 caught). Both comma-collapsing lines in collapseParenthetical can be deleted with the full suite green:\n\n    propertytype.go:289    inner = strings.ReplaceAll(inner, \" ,\", \",\")\n    propertytype.go:290    inner = strings.ReplaceAll(inner, \", \", \",\")\n\n## This is not a missing fixture\n\nThe obvious diagnosis — nothing feeds a comma-bearing spelling through — is wrong, and checking it first is what makes the rest of this bead worth writing. The suite already drives all four comma-bearing grammar arms end to end:\n\n  - propertytype_test.go:239   FLOAT(10, 2)\n  - propertytype_test.go:244   DECIMAL(10, 2)\n  - propertytype_test.go:273   DECIMAL(10, 2) NOT NULL\n  - corpus_area_d1_test.go:267 STRING(2, 5)\n  - corpus_area_d1_test.go:291 BYTES(1, 10)\n\nThey exercise the lines and still cannot observe them.\n\n## Why — provable for every input, not just the ones in the suite\n\ncanonicalSpelling's result is consumed at exactly two sites, both in normaliseType:\n\n  :253   typeSpellings[full]\n  :256   typeSpellings[truncateParenthetical(full)]\n\nAt :253, no key in typeSpellings contains a comma (every parenthesised row is a single-argument binary width: INT(8) ... UINT(256)). So a `full` holding a comma misses whether or not the collapse ran.\n\nAt :256, truncateParenthetical returns s[:i] for i = index of the first '('. The collapse only rewrites `inner`, which lies strictly after that first '(' — head is built from s[:open] and is untouched. So the truncated key is byte-identical either way. This holds even for nested parentheticals, where `inner` itself contains a '(': the first '(' of the result still sits at len(head).\n\nTherefore the two lines cannot change normaliseType's output for any input at all. This is dead code, not untested code, and no end-to-end fixture can ever pin it.\n\n## Do not delete it\n\nThe lines become load-bearing the moment a comma-bearing row exists, and gqlc-5md (length/precision/scale field on PropertyType) is precisely the bead that adds one — DECIMAL(10,2) would start hitting :253, and a DECIMAL(10, 2) spelling that had not been collapsed would miss the new row and silently fall back to bare DECIMAL. That is the failure gqlc-5md's implementer would have to debug, having never seen this note.\n\n## Proposed fix — pin the equivalence, not the key\n\nAssert directly on canonicalSpelling that comma spacing does not change the lookup key:\n\n    canonicalSpelling(\"DECIMAL(10, 2)\") == canonicalSpelling(\"DECIMAL(10,2)\")\n    canonicalSpelling(\"DECIMAL(10 , 2)\") == canonicalSpelling(\"DECIMAL(10,2)\")\n\nAsserting the equivalence rather than the literal string keeps the test tolerant of a future re-spelling of the canonical form, and it stays green after gqlc-5md lands rather than going red like gqlc-h9n.21 deliberately does. Testing the unexported helper directly has precedent: typeSpellingsRowFor (propertytype_test.go:440) already calls canonicalSpelling and truncateParenthetical.\n\n## Also\n\ncollapseParenthetical's doc comment (propertytype.go:273-276) offers `FLOAT(10, 2)` as a motivating example of two spellings sharing a lookup key. They do share one — but that shared key is not a row, so the sharing buys nothing today. The comment should say which half of its claim is currently inert, the same comments-are-claims-not-facts problem found at resolve.go:66 in gqlc-zm2.\n\n## Sweep survivors deliberately NOT filed\n\nThe other five survivors are all provably unable to change output and want no fixture:\n\n  :279 / :283  the unbalanced-paren early returns. The grammar's LEFT_PAREN ... RIGHT_PAREN arms are always balanced, so neither branch is reachable; the :279 mutant is additionally masked by the :283 guard.\n  :292         `+ s[end+1:]`. notNull is the only grammar element that can follow ')' (GQL.g4:1755, :1761, :1832, :1846, :1889), and canonicalSpelling:269 strips it one line before the collapse runs, so the tail is always empty.\n  :297         TrimRight on the truncated head. collapseParenthetical:287 already trimmed that space before truncateParenthetical ever sees the string.\n  :296         i \u003e= 0 vs i \u003e 0. No value-type spelling begins with '('.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T14:29:34Z","created_by":"areqag","updated_at":"2026-07-28T14:35:24Z","started_at":"2026-07-28T14:29:46Z","closed_at":"2026-07-28T14:35:24Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/564","dependencies":[{"issue_id":"gqlc-825","depends_on_id":"gqlc-5md","type":"related","created_at":"2026-07-28T10:29:46Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-3rj","title":"resolver: inferUnlabelled's multi-round loop is uncovered production code","description":"TRIPWIRE-L, planted during the gqlc-oou inference sweep (2026-07-28), put a panic at the top of the SECOND iteration of inferUnlabelled's outer 'for len(pending) \u003e 0' loop. It never fired across go test ./... . No fixture in the resolver corpus needs more than one inference round.\n\nThis is NOT a fail-open defect and NOT provably unreachable -- it is genuinely uncovered production code. The corresponding mutation ('inference stops iterating past the first round', which caps the loop at one pass) SURVIVED the sweep for exactly this reason.\n\nWHAT A FIXTURE WOULD NEED: a binding that is ambiguous when it is first evaluated but becomes unambiguous once a LATER binding in the same round commits its type. That requires a purpose-built schema -- none of social.gql / social_r2 / social_r5 / social_r6 / social_r7 / social_edgeunion has the right shape, because in all of them each unlabelled binding either resolves on its first look or never resolves at all.\n\nSketch of the shape needed: two unlabelled bindings b1 and b2 joined by an edge, where b1's candidate set is narrowed only by b2's committed type, and b2 is itself only resolvable after some third labelled anchor. Ordering matters -- the fixture must present b1 BEFORE b2 in whatever order inferUnlabelled walks pending, or the multi-round path is skipped again.\n\nUntil such a schema exists, the loop's termination behaviour, the carry-wins skip on already-carried bindings, and any interaction between rounds are all unexercised. Low priority: the single-round path is well pinned (25/37 caught in that sweep), and no known query shape reaches the second round.\n\nFiled from gqlc-oou / PR #553.","status":"closed","priority":3,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T13:11:20Z","created_by":"areqag","updated_at":"2026-07-28T14:18:45Z","started_at":"2026-07-28T14:00:08Z","closed_at":"2026-07-28T14:18:45Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/554","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2y7","title":"resolver: two pieces of dead code in scope.go — the Has* trio has no production callers, and virtualProjection's TypeList/TypeEdge stamp is never read","description":"Fell out of the gqlc-76y mutation sweep. Both are unreachable-by-construction, so\nmutation testing can never cover them and no fixture would help. They need a\ndelete-or-keep decision instead.\n\n## 1. scope.HasNode / HasEdge / HasCall have zero production callers\n\n    grep -rn \"\\.HasNode(\\|\\.HasEdge(\\|\\.HasCall(\" internal/ --include=*.go | grep -v _test.go\n    -\u003e nothing\n\nThey are only called from scope_test.go, as assertions about scope contents. The\nHasEdge assertions there are all negative (require.False), which is why deleting an\narm of HasEdge leaves the suite green.\n\nOptions: delete them and have scope_test.go read the maps directly, or keep them as\ndeliberate test affordances and accept that they are uncovered by construction. If\nkept, a comment saying so would stop the next sweep re-flagging them.\n\n## 2. virtualProjection's variable-length arm is inert\n\nscope.go:603-608 stamps query.TypeList{} on a wildcard-expanded RefProjection when\nthe edge binding is variable-length, and query.TypeEdge{} otherwise. That stamp is\nnever read:\n\n- projectionType routes query.RefProjection to refProjectionType(pp.Ref(), sch),\n  discarding pp.Type(). refProjectionType re-derives varLength from\n  s.edgeBindings[...].Hops() itself (scope.go:682) and builds the ResolvedList there.\n- Nothing downstream of the resolver reads it either. The only readers of\n  query.TypeList are internal/query/cypher/typing.go and expr.go, both in the parser,\n  which runs before the resolver; and resolve.go's resolveType, which RefProjection\n  never reaches.\n\nProven rather than argued: collapsing the branch to always stamp TypeEdge survives\nthe full `go test ./...` on the gqlc-76y branch, where\nvalid/returns_all_var_length_edge.cypher's golden pins r as a list of edges. If the\nstamp were observable that golden would have moved.\n\nOptions: delete the branch and always stamp TypeEdge (or TypeUnknown, since no value\nis meaningful), or keep it as documentation of intent with a comment saying the\nresolved type comes from refProjectionType.\n\nDeliberately not doing either autonomously — these are production deletions and the\ncode may be scaffolding for a later stage.","status":"closed","priority":3,"issue_type":"chore","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T11:11:42Z","created_by":"areqag","updated_at":"2026-07-30T03:01:58Z","started_at":"2026-07-30T02:29:35Z","closed_at":"2026-07-30T03:01:58Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/529","comments":[{"id":"019fa8a6-8f3e-77fa-98a4-a9e9e968482b","issue_id":"gqlc-2y7","author":"areqag","text":"Extended by a 33-mutation sweep of scope.go's projection lane (buildScopeOrder, materialiseReturns, virtualProjection, projectionType, refProjectionType, ResolveProjections), run 2026-07-28 at 9e743a3. 25/33 caught, no BUILD-FAILs.\n\nTHE STAMP FINDING IS WIDER THAN THIS BEAD RECORDS. gqlc-2y7 says virtualProjection's TypeList/TypeEdge stamp is never read. The sweep confirms that and adds the third:\n\n    virtualProjection stamps a wildcard node as unknown        SURVIVED\n    virtualProjection stops distinguishing a var-length edge   SURVIVED\n\nThe first replaces query.TypeNode{} with query.TypeUnknown{} in the node arm; nothing observes it. So ALL THREE of virtualProjection's Projection type stamps are inert, not just the edge pair — projectionType routes a RefProjection to refProjectionType, which re-derives the type from the binding tables and never consults the stamp it was handed. Whatever this bead does about the edge stamps should do the same for the node one, or the asymmetry becomes the next survivor.\n\nNO NEW FAIL-OPENS IN THE PROJECTION LANE. The other six survivors were each probed or tripwired and are not test gaps, which is worth recording so the lane is not re-swept from scratch:\n\n- buildScopeOrder's `v == \"\" || seen[v]` guard -- EQUIVALENT MUTANT, and this overturned my first reading. I expected removing it to let a re-referenced name into scopeOrder twice and emit a duplicate RETURN * column. It does not: the cypher parser's mergeBinding folds a re-reference into one binding record, so seen[v] never fires for a name in the tables, and an anonymous binding (v == \"\") is filtered by the three lookups below it anyway. Proven with a tripwire that panics when the guard fires AND the name would have passed a lookup: never fired across the whole suite.\n- materialiseReturns' empty-scopeOrder short-circuit -- tripwire never fired; returnsAll with an empty scope is unreached.\n- virtualProjection's out-of-scope error -- tripwire never fired. Documented in situ as a resolver-side-bug tripwire.\n- projectionType's unknown-variant default -- defensive tripwire over a closed variant set.\n- refProjectionType's final ErrOutOfR0Scope return -- tripwire never fired.\n- the carried-alias bypass's `ref.Property == \"\"` condition -- documented unreachable in situ: the parser scope check rejects Ref{\"c\", \"p\"} unless c is a binding name in scope.\n\nSweep artefacts: /tmp/mkproj.py, /tmp/proj_cases.json, /tmp/proj_sweep.py.","created_at":"2026-07-28T12:15:16Z"},{"id":"019fa8f0-9967-70a0-8a50-bee1dce818eb","issue_id":"gqlc-2y7","author":"areqag","text":"Mutation-sweep evidence (from gqlc-l7v):\n\n1. virtualProjection's TypeList stamp is now CONFIRMED unread, not\n   suspected. The mutation 'a wildcard var-length edge stops projecting\n   as a list' — deleting the TypeList wrapper entirely — SURVIVED the\n   full suite. Nothing downstream consumes it.\n\n2. New instance in the same family: callBindingSlot (resolve.go:104)\n   declares resultType, nullable, procedure and sourceField. Only\n   resultType is ever read (scope.go:612, resolve.go:724). The other\n   three are write-only.\n\n3. Third instance: buildScopeOrder's local-loop '|| seen[v]' dedup is\n   unreachable. TRIPWIRE-S (panic on a duplicate name arriving in the\n   local loop) never fired across the whole suite — the cypher parser\n   already dedups binding records before the resolver sees them.","created_at":"2026-07-28T13:36:08Z"},{"id":"019fa96a-c1dc-76ab-976a-549d06f7e1a3","issue_id":"gqlc-2y7","author":"areqag","text":"callBindingSlot's write-only fields, now proven by deletion rather than by grep\n(follow-up to the 13:36 comment's finding 2, arriving from gqlc-bef item 5).\n\nDeleting a field together with its write at scope.go:230-235 and rebuilding is a\nsharper instrument than grepping for readers, because it also catches reads\nthrough struct literals, embedding and reflection. Measured one field at a time\nand all three together:\n\n    nullable only      UNREAD    go build ./... = 0, go vet = 0, go test ./internal/... = 0\n    sourceField only   UNREAD    go build ./... = 0, go vet = 0, go test ./internal/... = 0\n    procedure only     build ./... = 0, TEST BUILD FAILS\n    all three          build ./... = 0, TEST BUILD FAILS\n\nSo the 13:36 claim holds, with one correction worth writing down: `procedure`\nhas no reader either. It appears exactly once outside the write, at\nscope_test.go:140, as a field NAME in the struct literal\n`map[string]callBindingSlot{\"c\": {procedure: \"test.proc\"}}` — a value the test\nconstructs and never reads back. That is a construction reference, not a read,\nbut it does mean deleting `procedure` is a two-file change while deleting\n`nullable` or `sourceField` is a one-file change.\n\nThe full reader set for the struct is two sites, both on resultType:\n\n    grep -rn \"\\.procedure\\|\\.sourceField\\|\\.resultType\" internal/resolver/\n    resolve.go:724   switch slot.resultType.(type) {\n    scope.go:612     return query.NewRefProjection(query.Ref{Variable: name}, slot.resultType), nil\n\nWHY THE nullable FIELD IS THE INTERESTING ONE. gqlc-bef filed it as a test gap —\n\"no fixture yields a nullable CALL YIELD scalar, so the bit could be hard-wired\nfalse\". Both halves of that were wrong. A fixture does exist\n(valid/call_standalone_yield_star_string_nullable.cypher, whose golden pins\n\"nullable\": true), and the bit survives being hard-wired in EITHER direction:\n\n    nullable: false   SURVIVED\n    nullable: true    SURVIVED\n\nA mutation surviving in both directions is the signature of a dead write, and it\nis worth treating as a distinct sweep verdict — a live field can survive being\npinned to its common value, but it cannot survive being pinned to the wrong one.\nProjection nullability reaches the golden by a different lane entirely:\ncallProjectionType (resolve.go:719-722) reads nullableBinding[ref.Variable].\n\nThe doc comment at resolve.go:104-106 is what made this look load-bearing — it\nsays the slot carries \"bridged Stage-6 type, nullability, and the two identity\nstrings codegen may consult on the query.Query side (spec §4.1)\". Codegen never\nsees them: exportedCallTypes is resolver-internal, and scope.go:866 says so\noutright (\"carry-only lanes and callTypes are NOT observable through...\"). If\nthe delete-or-keep decision lands on keep, that comment is the thing to fix,\nbecause it is the reason two separate sweeps have now flagged these fields.\n\nStill not deleting anything autonomously — same posture as the rest of this bead.\n","created_at":"2026-07-28T15:49:34Z"}],"dependency_count":0,"dependent_count":0,"comment_count":3}
{"_type":"issue","id":"gqlc-p9v","title":"schema/gql: nothing pins that a syntax error names the offending token","description":"listener.SyntaxError has two branches, and the interesting one is unasserted. Deleting it -- so every syntax error falls through to the bare \\\"syntax error at %d:%d: %s\\\" form and never names the offending token -- passes the whole suite.\n\nMeasured at 61a6cd1 as part of a 19-mutation sweep of listener.go; 17 were caught and this was one of the two that were not.\n\nlistener.go states the reason for the branch: \\\"Naming the offending token alongside line:column makes the location concrete for a schema author scanning their source.\\\" Two places could have pinned it and neither does:\n\n  invalidFixtures maps syntax_error.gql to nil, which TestInvalid documents as \\\"any non-nil error from the syntax error listener satisfies it\\\" -- so the message is not read at all.\n  TestPropertyBareDurationRejectedAtParse asserts strings.HasPrefix(err.Error(), \\\"syntax error at \\\"), which is the half both branches share.\n\nThis is the same class as ErrEndpointNotAlias, a branch that exists purely to sharpen a diagnostic and decides no outcome -- and that one IS pinned (removing it is caught). The inconsistency is the finding: one diagnostic-only branch is evidence, the other is decoration.\n\nFIX: assert on syntax_error.gql that the message names the token, not merely that it starts with \\\"syntax error at \\\". A `near %q` substring is the load-bearing half; asserting the exact token keeps it honest about which token ANTLR reports.","status":"closed","priority":3,"issue_type":"bug","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T09:55:09Z","created_by":"areqag","updated_at":"2026-07-28T10:02:31Z","started_at":"2026-07-28T09:55:18Z","closed_at":"2026-07-28T10:02:31Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/504","dependencies":[{"issue_id":"gqlc-p9v","depends_on_id":"gqlc-fon","type":"discovered-from","created_at":"2026-07-28T05:55:18Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-1uf","title":"Token-to-section attribution in obligationSections is unpinned","description":"obligationSections in internal/schema/gql/corpus_carriers_test.go attributes each grammar token to the section of the FIRST rule that references it. That choice is undefended: rewriting it to take the LAST reference instead moves 9 tokens to a different section (TYPE from 12.6 to 18.2, COMMA from 18.9 to 18.5, plus LEFT_BRACE, RIGHT_BRACE, TO, AS, NOT, IMPLIES, PERIOD) and every gate stays green.\n\nFound during gqlc-ii8's mutation sweep: this was mutation C8, the one survivor of ten.\n\nWhy it does not bite today: attribution only matters for a token that one corpus area carries and another does not. All 9 movers are reached from both areas, so no area-ownership verdict changes. The rule is a choice no test currently defends, not a live defect.\n\nWhat to do: either pin the first-reference rule with a test that fails when attribution moves, or establish that attribution is genuinely arbitrary for the property the carriers file measures and say so in the doc comment instead of implying a rule.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T08:32:44Z","created_by":"areqag","updated_at":"2026-07-28T08:50:42Z","started_at":"2026-07-28T08:36:50Z","closed_at":"2026-07-28T08:50:42Z","close_reason":"Landed as PR #488 (squash 2026-07-28). Resolved by removing the choice rather than pinning it: a token is now attributed to every section naming it, and each section's areas are checked separately. Measurement behind it — 12 tokens had a smaller owner set than the grammar warrants, so first-reference was lossy, not merely arbitrary. Both carrier gates now route through unmetGroups. Strengthening is free: 34 parse-side and 30 resolving-side section groups, 0 gaps. Sweep: 10 mutations, 8 caught; the 2 survivors are the same measured fact (the corpus contains no case distinguishing old rule from new), which is why the witnesses are synthetic.","external_ref":"https://github.com/areqag/gqlc/issues/486","dependencies":[{"issue_id":"gqlc-1uf","depends_on_id":"gqlc-ii8","type":"discovered-from","created_at":"2026-07-28T04:33:04Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.34","title":"schema/gql: ANY VALUE and ANY PROPERTY VALUE need only a decision, not a model change","description":"Split out of `gqlc-h9n.6`. Unlike its sibling `gqlc-h9n.33`, nothing structural\nblocks this one, which is the whole reason it is filed separately.\n\n`ANY VALUE` (`openDynamicUnionType`, valueType alt 7) and `ANY? PROPERTY VALUE`\n(`dynamicPropertyValueType`, alt 8) are atomic: they take no parameter, so a\nsingle `graph.TypeAny` constant would hold them in the flat enum today. They are\ndeclined with `ErrDynamicUnionType`, which they share with the closed unions —\nand that sharing is deliberate (ADR 0019 keeps the sentinel taxonomy ISO's\nrather than gqlc's), but it does hide that the two halves need different work.\n\nWhat is actually missing is a decision about the generated Go, and the ground is\nalready broken: ADR 0010's mapping table emits `any` for `scalar null` and\n`map[string]any` for `scalar map`, so \"a generated repository may hand back an\nuntyped value\" is not a new position for gqlc to take.\n\nThe open question is narrower than it looks. `ANY VALUE` includes paths and\nelement references — things `gqlc-h9n.6` decided are not property values at all —\nwhereas `ANY PROPERTY VALUE` is by construction the union of the storable ones.\nWhether both map to `any`, or only the second one is accepted, is the decision.\n","status":"closed","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T06:44:08Z","created_by":"areqag","updated_at":"2026-07-28T17:36:56Z","started_at":"2026-07-28T17:16:54Z","closed_at":"2026-07-28T17:36:56Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/471","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.34","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-28T02:44:08Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.33","title":"schema/gql: RECORD and closed dynamic unions need a property type that carries members","description":"Split out of `gqlc-h9n.6`, which decided *why* these are declined and gave them\nsentinels; this is the work that would lift the decline.\n\n`graph.PropertyType` is `type PropertyType string` over a flat set of constants\n(internal/graph/propertytype.go). Three declined families need it to carry\nsomething more:\n\n- `recordType` (GQL.g4:1977-1980) — `RECORD { f :: STRING }` needs a field list.\n  Declined today with `ErrRecordValueType`.\n- `closedDynamicUnionType` (valueType alts 9 and 10) — `ANY VALUE\u003cSTRING | INT\u003e`\n  and the bare `STRING | INT` need a member list. Declined today with\n  `ErrDynamicUnionType`, which they share with the *open* unions — those have a\n  different blocker and are `gqlc-h9n.34`.\n- `LIST` / `ARRAY` — `gqlc-h9n.5`, which is already open and has the fuller\n  design note. **Read that note before starting here**: it establishes that\n  encoding the parameter in the string is not viable, because `goType`'s bare\n  `return \"\", false` (internal/codegen/types.go:163) already means \"permanently\n  unrepresentable\", and a structured string would be reported to users as a\n  permanent boundary rather than as an unimplemented feature.\n\nSo this is the same missing thing three times, and `gqlc-h9n.5` is the one with\na design note. Whoever lands the parameterised model should land all three or\nsay why not.\n\nADR 0019 records that these are unimplemented rather than declined, and that the\n`yet` in their error messages is what says so.\n","status":"open","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T06:43:55Z","created_by":"areqag","updated_at":"2026-07-28T20:54:31Z","started_at":"2026-07-28T20:29:00Z","external_ref":"https://github.com/areqag/gqlc/issues/472","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.33","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-28T02:43:55Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.32","title":"schema/gql: seventeen optionality classes the corpus exercises on one branch only","description":"gqlc-h9n.13 partitioned the grammar's 137 optionality points into 49 classes, keyed by (ISO clause, optional element), and gated on them: every class owes a corpus file taking its branch and one eliding it. 32 classes were already discharged by the corpus. The other 17 are registered in `owedOptionality` in internal/schema/gql/corpus_branch_test.go, each naming the exact spelling no file has ever parsed.\n\nThis bead is those 17 files. The register is a pin to repin: `TestOptionalityCoverage` fails once a class the corpus now parses both ways is still listed, so each file lands with its entry deleted, and the list reaching empty is the bead closing.\n\nThe spellings, by clause:\n\n- 17.1 `../../gt` — a COPY OF source climbing more than one level\n- 17.6 a dotted object path qualifying a COPY OF source name\n- 18.2 a node pattern or phrase carrying no filler; a node type filler that is a key label set and nothing else\n- 18.3 an edge type phrase that names the type and stops; an edge type filler that is a key label set and nothing else; an endpoint reference spelling `()`\n- 18.5 the empty property block `{}`\n- 18.6 propertyType's `::`-elided spelling, `{ id INT NOT NULL }`\n- 18.8 `TABLE { ... }` without BINDING\n- 18.9 a record type with more than one field; the empty `RECORD {}`; the RECORD-elided record type; `GRAPH` without PROPERTY; `ANY` without VALUE; a closed dynamic union with more than one member\n- 18.10 fieldType's `::`-elided spelling\n\nSeveral are in territory the resolver declines with ErrUnsupportedType (18.8, 18.10, and most of the 18.9 rows), so those files record a decline rather than a resolve and need a sentinel plus an Annex D feature classification. The classification is the expensive half and is per-construct research against ISO 39075 — it cannot be inferred from the grammar, and a fabricated Annex D code is rejected by isValidFeature but a real code cited on the wrong construct is not.\n\nFiles are split across corpus areas A, B, C, D1 and D2, so this can be taken clause by clause rather than in one PR.\n","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T06:07:29Z","created_by":"areqag","updated_at":"2026-07-28T07:28:02Z","started_at":"2026-07-28T06:54:45Z","closed_at":"2026-07-28T07:28:02Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/467","dependencies":[{"issue_id":"gqlc-h9n.32","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-28T02:07:29Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.31","title":"schema/gql: declared bit width on PropertyType — an ADR 0002/0017 reversal, blocked on a named consumer","description":"Third sibling to gqlc-5md (magnitude qualifiers) and gqlc-8pe (duration\nqualifier). Same reversal shape — ADR 0002 keeps a fixed enum of widths, this\nbead argues carrying the declared one — applied to the residue ADR 0017 decided\nto accept and lose.\n\n## What is lost\n\n`INT(p)` / `UINT(p)` / `INTEGER(p)` under `binaryExactNumericType` take an\narbitrary bit width. `PropertyType` enumerates eight of them (8, 16, 32, 64,\n128, 256 and the two machine words). gqlc-h9n.16 folds the ones with an exact\nconstant; every other p falls through `normaliseType`'s truncating fallback to\nthe unqualified type. `INT(7)`, `INT(10)` and bare `INT` are byte-identical in\nthe model.\n\nADR 0017 decides that this is accepted rather than rejected, and states the\nfidelity limit. This bead is the fidelity limit's fix.\n\n## Why sibling, not part of gqlc-5md\n\ngqlc-8pe's three tests, applied here:\n\n1. **Different missing thing.** gqlc-5md and gqlc-8pe are both \"the model has no\n   field for this kind of information\". Here the model *does* have a width axis\n   — it quantises it. The fix is not a new field of a new kind; it is replacing\n   an enumerated axis with an integer, or accepting that the axis is enumerated\n   and rejecting off-lattice widths. That is a different edit and a different\n   argument.\n\n2. **Different consumer trigger.** gqlc-5md unblocks on a length-validating\n   setter or a `math/big` DECIMAL carrier; gqlc-8pe on a write-time selector\n   over `dbtype.Duration`'s fields. This one unblocks on something that can act\n   on a width Go has no type for — a range-checking setter, or a driver that\n   round-trips a declared width. Go has `int8`, so `INT(8)` already emits\n   something; `INT(7)` has no Go carrier at all, which is why the consumer\n   question is sharper here than for either sibling.\n\n3. **Different unify decision.** gqlc-5md warns that a length field makes\n   `CHAR(4)` and `CHAR(8)` stop unifying at `unionProperty`\n   (internal/resolver/resolve.go:744) and `unify` (:844). The analogous question\n   here is not the same question: integer widths are ordered and nested, so\n   \"unify to the wider\" is an available answer that has no counterpart for\n   lengths or for disjoint duration fields. Deciding it inside gqlc-5md would\n   smuggle a numeric-widening rule in under a string-length one.\n\n## Prerequisite is already satisfied\n\ngqlc-h9n.21 landed (PR #461), so the collision assertions exist. The three\ncorpus rows below already carry this bead and are asserted by\n`TestSemanticCaseCollisions`; when a width field lands they go red, which is the\nred-green signal that the blind spot closed.\n\n## What's discarded, per corpus\n\n- `corpus/18.9-value-type/scalar_signed_integer.gql`: `i_prec :: INT(10)` and\n  `v_intprec :: INTEGER(10)` — 10 is not on the enum's lattice.\n- `corpus/18.9-value-type/scalar_unsigned_integer.gql`: `u_prec :: UINT(10)`.\n- `corpus/18.9-value-type/scalar_float.gql`: `f_prec :: FLOAT(10) NOT NULL` and\n  `f_prec_scale :: FLOAT(10, 2)` — but see below.\n\n## FLOAT(p) is carried here only provisionally\n\n`FLOAT(p)` is not folded at all (ADR 0002), so its loss is total rather than\njagged. It is listed here because the missing slot looks the same from the\nmodel's side, but which sibling owns it depends on gqlc-h9n.28: if `p` counts\nmantissa bits this bead owns it, and if it counts decimal digits gqlc-5md does.\ngqlc-h9n.28 is permanently blocked (no ISO arbiter, gqlc-lir), so the row stays\nhere and this paragraph is why.\n\n## Acceptance criterion\n\nName the consumer first. Same bar as gqlc-5md and gqlc-8pe: a field nothing\nreads is a model change with no observable effect and a live regression risk at\n`unionProperty`.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T05:11:50Z","created_by":"areqag","updated_at":"2026-07-28T05:17:58Z","external_ref":"https://github.com/areqag/gqlc/issues/462","dependencies":[{"issue_id":"gqlc-h9n.31","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-28T01:11:50Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-8pe","title":"schema/gql: duration-qualifier field on PropertyType — an ADR 0002 reversal, blocked on a named consumer","description":"Sibling to gqlc-5md. Same reversal shape (ADR 0002 says qualifiers are dropped; this bead argues preserving them), applied to a different qualifier kind: field-selector rather than magnitude.\n\ngqlc-h9n.4 (PR #439) collapses DURATION(YEAR TO MONTH) and DURATION(DAY TO SECOND) onto a single TypeDuration. The ADR is amended in place to say so, and states the fidelity limit explicitly: after normalisation, the two spellings are indistinguishable at every downstream stage, exactly as CHAR(4) and CHAR(8) are today. The ADR points at gqlc-5md as the model for the fix — \"the same way as\", not \"part of\" — hence this sibling bead rather than a widening.\n\n## Why sibling, not part of gqlc-5md\n\nThe taxonomic argument (different information, so a different field) is weak on its own: widening gqlc-5md's title from \"length/precision/scale\" to \"length/precision/scale/qualifier\" is a two-word edit. The reasons that actually make this a sibling are operational, and each one is a decision that cannot be taken jointly.\n\n1. **Different consumer trigger.** gqlc-5md unblocks on a length-validating setter or a `math/big` DECIMAL carrier. This bead unblocks on a write-time field-selector over `dbtype.Duration.Months` vs `.Days`/`.Seconds`/`.Nanos`. Both are blocked on a named consumer, but not the same one — either can arrive without the other.\n\n2. **Different `unionProperty`/`unify` decision.** gqlc-5md warns that adding a length field silently changes `unionProperty`: `CHAR(4)` and `CHAR(8)` start failing to unify, a large behaviour change that has to be decided explicitly. The analogous question here — should `DURATION(YEAR TO MONTH)` and `DURATION(DAY TO SECOND)` unify? — is a separate answer over two disjoint-field qualifier states with no numeric range. One PR cannot resolve both without smuggling one decision inside the other.\n\n3. **Different collision prereq.** gqlc-5md is blocked by gqlc-h9n.21, which asserts the model collision for the five length/precision spellings against a bare carrier. The DURATION pair cannot be expressed that way — bare `DURATION` is a parser error, pinned by `TestPropertyBareDurationRejectedAtParse` — so it forces the `siblings []string` generalisation of h9n.21's design. That requirement is invisible if buried inside gqlc-5md.\n\n## Prerequisite\n\nBlocked by gqlc-h9n.21. The two DURATION corpus rows are declared as a semantic case there (row 10 of the collision table), and the equality assertion has to exist before this bead can be shown to have closed anything: when the qualifier field lands, the assertion goes red and the semantic case is deleted. Without the prereq there is no red-green signal that the blind spot closed.\n\n## What's discarded, per corpus\n\n- corpus/18.9-value-type/scalar_duration_year_month.gql: DURATION(YEAR TO MONTH) — the qualifier selects Months.\n- corpus/18.9-value-type/scalar_duration_day_second.gql: DURATION(DAY TO SECOND) — the qualifier selects Days/Seconds/Nanos.\n\nBoth entries carry this bead as their `bead` field, with matching semanticCases rows, so the corpus and the model agree on which spellings are lossy.\n\n## This is a reversal of a recorded decision\n\ndocs/adr/0002-bit-width-preserving-value-type-model.md as of PR #439 says: \"the two ISO qualifiers correspond to populating disjoint subsets of those fields at write time. There is no distinct YearMonthDuration type to project onto.\" That is the design justification for the collapse. This bead argues that the write-time constraint (which fields of the carrier a value may populate) is worth preserving on the schema side.\n\n## Blocked on a named consumer\n\nSame discipline as gqlc-5md: implement when a downstream consumer (schema validator, write-time check, driver-level constraint) is named and wants the qualifier. Without one, growing the model to carry it is speculative surface.\n","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T23:41:30Z","created_by":"areqag","updated_at":"2026-07-27T00:27:21Z","external_ref":"https://github.com/areqag/gqlc/issues/441","metadata":null,"dependencies":[{"issue_id":"gqlc-8pe","depends_on_id":"gqlc-h9n.21","type":"blocks","created_at":"2026-07-26T20:03:33Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5md","title":"schema/gql: length/precision/scale field on PropertyType — an ADR 0002 reversal, blocked on a named consumer","description":"Split out of gqlc-h9n.16 (Problem B) after a design pass measured the blast radius. gqlc-h9n.16 keeps Problem A, the width fold.\n\nSTRING(10), CHAR(4), VARCHAR(5), DECIMAL(10,2) carry information the model has no constant for. Honouring them needs a new field on PropertyType.\n\n## This is a reversal of a recorded decision, not a bug fix\n\ndocs/adr/0002-bit-width-preserving-value-type-model.md, verbatim: 'GQL type spellings are normalised into this enum (e.g. SMALLINT~Int16, ...); length and precision qualifiers (VARCHAR(255), DECIMAL(p,s)) are dropped.'\n\nThe tree also has a passing test named TestPropertyLengthQualifiersDropped (internal/schema/gql/propertytype_test.go:108) whose doc comment cites ADR 0002 and whose five rows are exactly VARCHAR(255), CHAR(8), STRING(100), DECIMAL(10, 2), FLOAT(10). Four of the five die under this bead. Deleting a test called ...QualifiersDropped is the honest signal that a decision is being reversed, and it should be reviewed as an ADR amendment rather than as a defect fix.\n\n## Why it is blocked rather than merely deferred\n\nNothing would read the field. Measured: codegen.goType (internal/codegen/types.go:157) dispatches on PropertyType alone; CHAR(4) and bare CHAR both emit 'Amount string' and Go has no fixed-length string type to emit into. DECIMAL(10,2) and bare DECIMAL both die at ErrUnrepresentableWidth (internal/codegen/prepare.go:489) — precision and scale change nothing about there being no Go carrier. There is no plausible consumer without either a math/big DECIMAL carrier (declined in ADR 0002's Consequences, deferred to codegen) or a length-validating setter, which no bead proposes.\n\nSo the acceptance criterion is: name the consumer first.\n\n## Cost when it does land — measured, and low\n\nThe designer ran the migration in a scratch worktree; patch kept at /tmp/h9n16/struct-experiment.patch. Changing 'type PropertyType string' to a struct with Base+Len, converting the const block to var, and adding String() plus MarshalJSON produced exactly TWO compile errors in the whole tree (internal/resolver/validated.go:123 and internal/codegen/prepare_test.go:306, both mechanical string(x) -\u003e x.String()). After those, go build and the full go test suite are green with ZERO golden-file changes — MarshalJSON preserves the \"type\":\"STRING\" wire form.\n\nOne real cost: Go cannot const a struct, so 26 TypeXxx constants become mutable package-level vars. Mitigation if that matters: nest, with 'type BaseType string' keeping the const block and PropertyType wrapping it.\n\n## The trap that must not be missed\n\nunionProperty (internal/resolver/resolve.go:744) and unify (:844) compare property types with !=. If the length lands inside PropertyType, then CHAR(4) vs CHAR(8), STRING(5) vs STRING(10), and CHAR(4) vs bare CHAR all START FAILING — a large silent behaviour change across every union, WITH carry and UNION column, in exchange for a field codegen does not read. Either the length lives outside the compared value, or unionProperty/unify must compare base-only. The struct comparison will silently do the wrong thing by default, so this has to be decided explicitly.\n\n## Ordering\n\ngqlc-h9n.21 MUST land before this bead, in its own PR, green. h9n.21 asserts require.Equal(model(CHAR(4)), model(CHAR)), which is true today and false after this lands — so it goes red exactly when this bead closes the blind spot, which is the entire point of h9n.21. If this lands first, h9n.21 can never be written as specified and becomes dead work.\n\n## Half-closure to be aware of\n\nEven with a length field, CHAR(4) and VARCHAR(4) still resolve identically, because typeSpellings maps CHAR, VARCHAR and STRING all to graph.TypeString. Fixed-versus-variable is a SECOND dropped bit this bead does not recover. h9n.21's scalar_char_fixed_length case must therefore be paired against bare CHAR, as h9n.21 specifies; pairing it against VARCHAR(4) would never go red.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T07:08:08Z","created_by":"areqag","updated_at":"2026-07-26T16:40:05Z","external_ref":"https://github.com/areqag/gqlc/issues/413","dependencies":[{"issue_id":"gqlc-5md","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T03:11:10Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-5md","depends_on_id":"gqlc-h9n.16","type":"related","created_at":"2026-07-26T03:11:09Z","created_by":"areqag","metadata":"{}"},{"issue_id":"gqlc-5md","depends_on_id":"gqlc-h9n.21","type":"blocks","created_at":"2026-07-26T03:11:18Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-b63","title":"ci: nothing enforces the GOLANGCI_LINT_CACHE / ci.yml path coupling","description":"PR #396 made the golangci-lint cache per-worktree by changing two things that must agree:\n\n- `justfile`: `export GOLANGCI_LINT_CACHE := justfile_directory() + \"/.bin/golangci-cache\"`\n- `.github/workflows/ci.yml`: `actions/cache` `path: .bin/golangci-cache`\n\nNothing in the tree fails if a future edit drops or diverges either half.\n\n## Why that is worse than a normal untested invariant\n\nThe failure is silent and has no error signal. Reviewer verified the\ncounterfactual against `ci.yml:36-43`: with `path:` back at\n`~/.cache/golangci-lint` while the justfile still exports `.bin/golangci-cache`,\nthe restore step populates a directory `just lint` never reads, lint runs cold,\nand the post-job save writes a stale cache under the `run_id`-unique primary key.\nCI stays green. The only symptom is the lint job quietly costing ~78s instead of\n~0.6s, forever, until someone notices the wall-time.\n\nDivergence in the other direction reintroduces gqlc-6rv itself.\n\n## Possible shape\n\nA CI step or just recipe that asserts `.bin/golangci-cache` is non-empty after\n`just lint`. Cheap (sub-second) and it fails loudly at the point of divergence.\nPer the house preference for just recipes over inline workflow steps, this\nprobably belongs in the justfile with CI invoking it.\n\nNot obviously worth its own PR — good candidate to fold into the next CI change.\n\nSource: rev-6rv review of PR #396, nit N2 (non-blocking). Measured costs in that\nreview: cold 78.4s, warm 0.6s, 20 MiB per warm worktree.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T06:44:39Z","created_by":"areqag","updated_at":"2026-07-28T12:58:20Z","started_at":"2026-07-28T12:43:42Z","closed_at":"2026-07-28T12:58:20Z","close_reason":"lint-cache-check recipe + CI step; PR #548 merged","external_ref":"https://github.com/areqag/gqlc/issues/414","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.20","title":"schema/gql: four corpusAreas prefixes match no file — fill or drop","description":"`corpusAreas` (`internal/schema/gql/corpus_test.go:148`) declares 13 path prefixes\nacross the five areas. Four of them match no corpus file at `7217b18`:\n\n| prefix | area | files |\n|---|---|---|\n| `18.5-property-types/` | B | 0 |\n| `18.6-property-type/` | B | 0 |\n| `18.7-property-value-type/` | B | 0 |\n| `18.10-field-type/` | D2 | 0 |\n\nThe other nine carry all 86 files. The four directories do not exist on disk.\n\nA prefix that matches nothing is inert — `requireOwnedByArea` only ever consults\nprefixes to *admit* a file — so this is not a live fault. It is a claim the tree\nmakes and does not keep, and `corpusArea`'s own doc comment says prefixes are \"the\nonly place a clause directory is named, so a typo in an entry's path fails instead\nof quietly creating a directory\". An unbacked prefix weakens exactly that.\n\n## The choice, and why it is not cosmetic\n\nThe clause-18.5 / 18.6 / 18.7 / 18.10 productions **are** covered — the gate reads\n`rules 133/133`. They are covered by files filed under *other* clause directories:\nany node with a property reaches `propertyTypes` / `propertyType` /\n`propertyValueType`, and record types reach `fieldType`. So the empty prefixes are\na symptom of the incidental-carrier class, not a separate problem — the names are\ngreen, but no file whose *subject* is those clauses reaches them.\n\nThat argues for filling rather than deleting: four small files, one per clause,\nwhose header comment says what production they are the deliberate carrier of.\nDeleting the prefixes is the cheaper option and is defensible if the view is that\nthese productions have no surface syntax of their own worth a dedicated file —\nbut that should be an explicit decision recorded in the comment, not a silent\ntidy-up.\n\nDecide one way or the other; do not leave four prefixes asserting a partition that\ndoes not exist.\n\nRelated: the incidental-carrier gating bead — if that detector lands and is\nprefix-driven, these four empty prefixes will need a disposition anyway.\n\nDiscovered while reviewing PR #392.\n","status":"closed","priority":3,"issue_type":"chore","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T05:35:25Z","created_by":"areqag","updated_at":"2026-07-28T05:56:15Z","started_at":"2026-07-28T05:47:09Z","closed_at":"2026-07-28T05:56:15Z","close_reason":"Kept the four prefixes and registered them as ownership-only. Dropping them was proven strictly weakening by mutation: TestCorpusAreaParseCarriers reports fieldType, propertyType, propertyTypeList, propertyTypesSpecification and propertyValueType as unowned, because areaPrefixNumbers/areaOwners read prefixes as claims on ISO sections, not only on directories. Filling them is ceremony on the grammar: 18.7 is a pure alias rule, 18.5/18.6 are the braced property block 18.2's files already spell, and 18.10's fieldType is reachable only through constructs declined with ErrUnsupportedType. ownershipOnlyPrefixes records the per-prefix reason and TestEveryAreaPrefixIsBacked makes it expire: every prefix backs a file or is registered, and every registered prefix backs none. PR #466, merged.","external_ref":"https://github.com/areqag/gqlc/issues/415","labels":["gql","schema","test"],"dependencies":[{"issue_id":"gqlc-h9n.20","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T01:35:25Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.13","title":"schema/gql: promote parse-shape recording from report to gate","description":"The alternative-level obligation set landed in gqlc-h9n.2 counts *labelled alternatives* (`rule#N`). linus established during that bead that this undercounts the real branching surface, and team-lead ruled the wider measure lands as a **report** rather than a gate so that five parallel corpus authors were not re-briefed mid-flight (amendment A10).\n\nMeasured surface, 2026-07-25: **319 branch points** — 174 top-level alternatives, 8 nested-group, 137 optionality. Of the 137 optionality points, **129 have neither branch demanded** by any obligation.\n\nWorked example of why `rule#N` cannot see it: `NODE TYPE LABEL A` and `NODE LABEL A` produce the **identical** `nodeTypePhraseFiller` shape `[nodeTypeFiller]`, because the spelling difference turns on `TYPE?` in the *parent* rule. So that distinction is invisible to alternative-counting *and* to per-rule shape recording of `nodeTypePhraseFiller` itself. Any promotion has to record shapes in a way that attributes a parent-rule optionality point to the place an author can act on.\n\nWhat exists after gqlc-h9n.2: distinct ordered-child shapes are accumulated per rule and the totals are logged. There is deliberately no pinned count and no checked-in golden — a golden that moves whenever any author adds a corpus file is a merge-conflict generator, so the artifact is written only behind an opt-in flag.\n\nThis bead decides whether the 319 is an obligation, and if so how much of it. It is not a foregone conclusion: a large fraction of the 129 may be uninteresting optionality (whitespace-equivalent keyword elision) rather than distinct semantics, and gating on all of it would be the uncloseable-gate mistake that A1§3 and A9 both had to correct. Read the number first, partition it, then gate on the part that is worth files.","acceptance_criteria":"The 319 branch points are partitioned into semantically distinct spellings versus uninteresting optionality, with the criterion written down rather than judged case by case.\nA decision is recorded on whether the distinct partition becomes a gate, with reasoning, including the option of not gating.\nIf gated: the golden artifact is generated once and the gate is fail-closed, so a scanner regression yielding an empty shape set cannot pass.\nParent-rule optionality points are attributed to a rule an author can act on, not to the rule whose shape is unchanged by them.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T11:50:48Z","created_by":"areqag","updated_at":"2026-07-28T06:12:36Z","started_at":"2026-07-28T05:56:20Z","closed_at":"2026-07-28T06:12:36Z","close_reason":"Decided: the 319 does not become an obligation as measured, and the pinned artefact is the grammar side rather than the corpus side. Reproduced 174 top-level alts + 8 nested-group branches + 137 optionality = 319, and 129 undemanded. Criterion (mechanical): class each optionality point by (ISO clause, optional element) — the element because notNull after INT and after FLOAT reach the same sub-rule, collapsing ~40 points to one; the clause because the same element elsewhere is another construct's handling, keeping 18.2's and 18.3's nodeTypeFiller apart. 137 points to 49 classes, 32 already discharged. The class set is derived from GQL.g4 and the reachable closure alone, so no corpus file can move it and A10's merge-conflict objection does not reach it: pinned as membership, with the point count pinned separately so a scan that stops descending fails even when classes coincidentally match. owedOptionality registers the 17 remaining as a pin to repin; gqlc-h9n.32 writes those files. Known under-demand (two rules in one clause with distinct listener methods) recorded rather than hidden — tightening to per-position raises the debt to 95. TestCorpusShapes stays an opt-in report. PR #468, merged.","external_ref":"https://github.com/areqag/gqlc/issues/379","dependencies":[{"issue_id":"gqlc-h9n.13","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-25T07:50:48Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.11","title":"docs: correct the GQL.g4 provenance claim — community ANTLR adaptation, not a WG3 artifact","description":"`internal/grammar/gql/GQL.g4` is described in beads, code comments and triage\nnotes as \"a faithful ISO GQL grammar\". That overstates its provenance.\n\nEstablished 2026-07-25: the file is **byte-identical** to\n`antlr/grammars-v4/gql/GQL.g4` — a community-contributed grammar (author \"Miotto\nNicola\"), not a WG3 or standards-editor artifact. Visible ANTLR adaptations and\ntranscription artifacts:\n\n- `procedureSpecification` alternatives commented out\n- `connectorUndirected : TO | TILDE` overlapping `connectorPointingRight : TO |\n  RIGHT_ARROW`, an ambiguity ISO resolves with syntax rules (see gqlc-h9n.9 item 4)\n- a misplaced `// 19.9 \u003clabled predicate\u003e` section comment (also misspelled)\n\nIts graph-type DDL section does match, rule-for-rule, TuGraph's independently\nmaintained ISO GQL grammar — which is real corroboration, and worth stating\n*instead* of the faithfulness claim.\n\n## Why this is worth a bead rather than a silent edit\n\nThe claim shaped a wrong conclusion during triage. \"The grammar is faithful and\nis not the problem\" was used to move attention to the listener, which happened to\nbe correct that time. But the same reasoning would have suppressed investigation\nof a genuine grammar defect, and two candidates already exist: the `TO` ambiguity\nabove, and the fact that a full check of all 3774 lines against Annex A was not\npossible because the standard is paywalled.\n\n## Deliverable\n\nReplace the faithfulness claim wherever it appears with an accurate provenance\nnote: upstream source, the fact that it is a community adaptation, what was\nverified (graph-type DDL vs TuGraph, rule-for-rule) and what was not (the other\n~3700 lines, unverifiable against a paywalled Annex A). Anywhere the grammar is\ntreated as authoritative for a semantic question, say that it cannot answer prose\nquestions — the endpoint-alias investigation in gqlc-uhb is the worked example.\n","acceptance_criteria":"Every 'faithful ISO GQL grammar' claim is replaced with an accurate provenance note naming the upstream antlr/grammars-v4 source and its community authorship.\nThe note states what was verified (graph-type DDL matches TuGraph's independent grammar rule-for-rule) and what was not (the remaining lines, unverifiable against a paywalled Annex A).\nThe known ANTLR adaptations and transcription artifacts are listed.\nAnywhere the grammar is used to settle a semantic question, a note records that it cannot answer prose questions.","status":"closed","priority":3,"issue_type":"chore","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:37:29Z","created_by":"areqag","updated_at":"2026-07-28T06:36:55Z","started_at":"2026-07-28T06:15:08Z","closed_at":"2026-07-28T06:36:55Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/371","dependencies":[{"issue_id":"gqlc-h9n.11","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:37:28Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.10","title":"schema/gql: four undefined-or-unrecorded graph-type DDL behaviours","description":"Four small graph-type DDL behaviours that are individually not worth a bead but\ncollectively leave the accepted subset undefined. Each needs a decision and a\ntest, most of them one line.\n\n## 1. A node type with properties but no labels is rejected\n\n    nodeTypeImpliedContent : nodeTypeLabelSet | nodeTypePropertyTypes | nodeTypeLabelSet nodeTypePropertyTypes    # GQL.g4:1528-1532\n\nThe middle alternative is properties with no label set. Verified 2026-07-25:\n`NODE TYPE P (p {id STRING})` fails with `ErrUnnamedNodeType` (\"node type has no\nlabel\"). Grammar-legal, so decide: is an unlabelled node type meaningful for a\ngenerator that names Go types after labels? Probably not — but say so, and make\nthe error explain *that*, rather than implying the author forgot something.\n\n## 2. A qualified graph type name keeps only its last component\n\n`EnterCreateGraphTypeStatement` (listener.go:92) reads\n`c.CatalogGraphTypeParentAndName().GraphTypeName().Identifier().GetText()` and\ndiscards the parent path, so `CREATE GRAPH TYPE /a/b/G AS { ... }` resolves as if\nit were `G` (verified 2026-07-25).\n\nThis one is **already decided and already pinned** — `TestGraphTypeName`\n(parser_test.go:71-90) has a \"catalog path keeps last component\" case, and the\ndocstring explains the reasoning. So it is not a silent reinterpretation and needs\nno code change.\n\nWhat it needs is a line in the ADR, and a note that it becomes load-bearing if\n`COPY OF` is ever supported (gqlc-h9n.1), since that form references a graph type\n*by catalog path* — at which point discarding the parent stops being harmless.\nListed here only so the ADR does not omit it.\n\n## 3. IF NOT EXISTS and OR REPLACE parse and are ignored\n\nBoth are in `createGraphTypeStatement` (GQL.g4:344). Ignoring them is correct for\na static generator with no catalog to check against or replace in — there is only\never one graph type and it is always \"created\".\n\nLike item 2 this is **already pinned** by `TestGraphTypeName` (parser_test.go:79-80,\nthe \"or replace ignored\" and \"if not exists ignored\" cases), so no code or test\nchange is needed. It needs one ADR line each so a future reader does not mistake\nthe silence for an oversight.\n\n## 4. `(a TO b)` is grammatically ambiguous and resolved only by rule order\n\n    connectorPointingRight : TO | RIGHT_ARROW     # GQL.g4:1659-1662\n    connectorUndirected    : TO | TILDE           # GQL.g4:1664-1667\n\n`TO` appears in both, and `endpointPair : endpointPairDirected |\nendpointPairUndirected` lists directed first, so ANTLR resolves `CONNECTING\n(a TO b)` as pointing-right. That is almost certainly the intended reading — ISO\ndisambiguates with syntax rules that an ANTLR grammar cannot express — but gqlc\ndepends on ANTLR's first-alternative-wins behaviour for it.\n\nThis matters directly to the phrase-form work (gqlc-uhb): if `TO` ever resolved\nas undirected, every `CONNECTING (X TO Y)` would hit the undirected rejection.\nPin it with a test so a future grammar regeneration or rule reordering cannot\nflip it silently.\n","acceptance_criteria":"A node type with properties and no labels has a decided outcome and an error that explains the reason rather than implying an omission.\nA test pins CONNECTING (a TO b) as pointing-right, so the connector ambiguity cannot flip silently on grammar regeneration.\nUNDIRECTED ... CONNECTING (a TO b) has a golden pinning today's directed-endpoint-pair misparse, as the phrase-form twin of the gqlc-h9n.3 arc-form case.\nThe already-pinned behaviours (qualified name truncation, IF NOT EXISTS, OR REPLACE) each get an ADR line; no code change is made to them.\nThe ADR notes that qualified-name truncation becomes load-bearing if COPY OF is ever supported.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:37:28Z","created_by":"areqag","updated_at":"2026-07-28T06:49:32Z","started_at":"2026-07-28T06:25:43Z","closed_at":"2026-07-28T06:49:32Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/372","dependencies":[{"issue_id":"gqlc-h9n.10","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:37:27Z","created_by":"areqag","metadata":"{}"}],"comments":[{"id":"019f979e-8eac-734c-8a68-f94172aa34ab","issue_id":"gqlc-h9n.10","author":"areqag","text":"This bead is why the gqlc-h9n.2 flagged-alternative criterion had to be rewritten, and it is worth recording because the near-miss was invisible.\n\nconnectorPointingRight : TO | RIGHT_ARROW  and  connectorUndirected : TO | TILDE both accept a bare TO, so which one wins is decided by ANTLR alternative order today. The original coverage criterion flagged an alternative only when its symbols were a subset of its SIBLINGS' symbols — and TO is unique among each rule's siblings, so neither TO alternative was flagged. But TO is also a temporal duration qualifier: '{ (:A { p :: DURATION(YEAR TO MONTH) }) }' covers the TO token while entering connectorPointingRight and connectorUndirected zero times, and 'CONNECTING (a -\u003e b)' / 'CONNECTING (a ~ b)' enter both rules without either taking its TO alternative.\n\nSo the coverage gate would have gone fully green without ever compelling the file that exposes this bead. Verified on real parses by linus, reproduced independently by team-lead (/tmp/lead-invisible.py).\n\nThe criterion is now forbid-and-recompute: delete an alternative, rebuild the closure, and flag it if all 133 rules and 133 tokens are still reached. Under that criterion connectorPointingRight#1 and connectorUndirected#1 are both flagged, so the corpus is REQUIRED to contain all four connector spellings — bare TO for each rule plus -\u003e and ~. Area C owns them.\n\nDONE for this bead means the resolution no longer depends on ANTLR ordering, with those four files distinguishing the two rules.","created_at":"2026-07-25T04:52:59Z"},{"id":"019f991c-b205-793d-b296-874057d38587","issue_id":"gqlc-h9n.10","author":"areqag","text":"Item 4 is sharper and worse than written. Verified 2026-07-25 (team-lead, /tmp/h9n2probe/leadconn), two corrections:\n\n1. It is not just 'resolved by rule order' — edgeKind is never consulted at all. Prediction for endpointPair sees only the parenthesised token run, because endpointPairPhrase is a sibling of edgeTypePhraseFiller under edgeTypePhrase. So the DIRECTED/UNDIRECTED keyword is INERT at this decision. Measured, under both SLL and full LL:\n\n    UNDIRECTED ... CONNECTING (a TO b)\n      -\u003e endpointPair \u003e endpointPairDirected \u003e endpointPairPointingRight \u003e connectorPointingRight(TO)\n         AMBIG rule=endpointPair alts={1,2} exact=true\n\n2. The bead says the pointing-right reading is 'almost certainly the intended reading'. That holds for DIRECTED. It does NOT hold for UNDIRECTED: an undirected edge type whose endpoints are written (a TO b) gets a DIRECTED endpoint pair in the tree. That is a class-2 silent reinterpretation — the phrase-form twin of the arc-form case already tracked as gqlc-h9n.3 — and the phrase form is the spelling a human is more likely to write. It needs a golden pinning today's wrong output, not just a test pinning pointing-right on the directed case.\n\nConsequence for gqlc-h9n.2: connectorUndirected#1 is UNREACHABLE, not merely invisible. No corpus file can take it, so it is exempt from the alternative obligation rather than demanded (amendment A9). If this bead ever makes edgeKind load-bearing for prediction, that exemption must be deleted and the alternative covered — the harness asserts the exemption bidirectionally, so it will go red on its own and tell you.","created_at":"2026-07-25T11:50:22Z"}],"dependency_count":0,"dependent_count":0,"comment_count":2}
{"_type":"issue","id":"gqlc-h9n.6","title":"schema/gql: ADR the rejected value-type families — PATH, RECORD, dynamic unions, references, immaterial","description":"Six value-type families in GQL.g4 §18.9 that a static code generator targeting\nproperty-graph storage plausibly *should* refuse — but which today share the\nsingle generic `ErrUnsupportedType` sentinel and have no ADR recording the\nreasoning.\n\n    pathValueType    : PATH notNull?                                        # 1964\n    recordType       : ANY? RECORD notNull? | RECORD? fieldTypesSpecification notNull?   # 1977-1980\n    ANY VALUE? notNull?                                                     # 1728  open dynamic union\n    ANY? PROPERTY VALUE notNull?                                            # 1729  dynamic property value\n    ANY VALUE? \u003cA|B\u003e  /  A | B                                              # 1731-1732  closed dynamic union\n    referenceValueType : graph | bindingTable | node | edge references      # 1900-1904\n    immaterialValueType : nullType | emptyType   (NULL, NULL NOT NULL, NOTHING)  # 1907-1919\n\n## Why this is a decision bead, not a feature bead\n\nSeveral of these are not property value types in any storable sense. `PATH`, the\nreference value types and the binding-table type describe *query result* values,\nnot stored properties — a node property cannot hold a path. `NOTHING` and `NULL\nNOT NULL` are the empty type, inhabited by nothing, which cannot be a property\ntype at all. Under the no-dialect principle, refusing them is legitimate; what\nis missing is the written reason.\n\n`ANY PROPERTY VALUE` and the closed dynamic unions are the genuinely arguable\nones: they *are* property value types by construction, and a generator could map\nthem to `any` or to a generated sum type. Whether that produces code worth\nhaving is the real question.\n\n`RECORD` sits between the two — it is a structured value type, and\n`fieldTypesSpecification` (1982) has its own `fieldType` rule (1996), so the\ngrammar fully describes it, but neo4j has no native record-valued property.\n\n## Deliverable\n\nAn ADR per group, or one ADR with a section per group, each stating the concrete\nreason for rejection — and, where the reason is \"not representable in the\ntargeted store\" rather than \"not a property type\", saying which store and why,\nso the decision can be revisited when a different backend is targeted.\n\nReplace the single generic `ErrUnsupportedType` for these with errors precise\nenough that a schema author knows whether they hit a permanent boundary or an\nunimplemented feature. That distinction is invisible today.\n","acceptance_criteria":"An ADR records a concrete reason for each rejected value-type family, distinguishing 'not a property value type' from 'not representable in the targeted store'.\nThe generic ErrUnsupportedType is replaced for these families by errors that say whether the boundary is permanent or unimplemented.\nEach family has a test pinning its rejection to the specific error.","status":"closed","priority":3,"issue_type":"decision","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T03:34:30Z","created_by":"areqag","updated_at":"2026-07-28T06:57:20Z","started_at":"2026-07-28T06:37:21Z","closed_at":"2026-07-28T06:57:20Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/373","dependencies":[{"issue_id":"gqlc-h9n.6","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-24T23:34:30Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5yj","title":"ci: cancel sibling jobs when any required blocking check fails","description":"Right now if one required blocking check in ci.yml fails (e.g. actionlint hits a transient network error on setup-just), the other required checks (lint/test/tidy/govulncheck/codegen-fence/live-smoke) all continue to completion. Wastes CI minutes and delays the failure signal on the PR. Especially painful on flaky network failures where a rerun of just the failed check would suffice.\n\nOptions to explore:\n1. **Dependency chain**: put fast checks first (actionlint, tidy) as `needs:` for the slower ones (test, live-smoke). Slow jobs skip when fast fails. Simple, no third-party actions.\n2. **Gate job pattern**: one lightweight 'gate' job that runs all fast validators; each expensive job `needs: [gate]`. Cleaner structure but changes the check-name topology (may affect required-status-checks ruleset).\n3. **Runtime cancellation via API**: use `gh api` in a step to cancel sibling in-progress jobs on failure. More complex and racy.\n4. **Concurrency groups per (workflow, PR)**: cancels prior runs on new pushes to same PR, but does NOT cancel sibling jobs within the same run.\n\nPrefer option 1 for simplicity. Requires updating the ruleset's required_status_checks list if job names change.\n\nDiscovered 2026-07-19 during gqlc-23e recovery: PR #343 had one flaky actionlint failure while everything else passed; had to rerun just the failed job. If checks were chained, we'd have re-run only actionlint and skipped the passed ones automatically.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-19T03:52:06Z","created_by":"antranig-yeretzian","updated_at":"2026-07-30T02:09:09Z","started_at":"2026-07-30T01:55:05Z","closed_at":"2026-07-30T02:09:09Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/344","labels":["ci","followup"],"dependencies":[{"issue_id":"gqlc-5yj","depends_on_id":"gqlc-rvw","type":"blocks","created_at":"2026-07-18T23:52:06Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2fi","title":"workflow: concurrent-session cwd coordination for shared repo","description":"Multiple agent sessions can operate on the same repo cwd (\\`/home/antranig/Developer/gqlc/gqlc\\`) simultaneously, running on different branches. Observed 2026-07-18 during gqlc-71n:\n\n1. Session A on \\`chore/gqlc-23e-release-pipeline-impl\\` making bd metadata edits.\n2. Session B (mine) working on gqlc-71n. Carmack on arrival found the tree checked out on session A's branch, not their own. Correctly switched.\n3. Session A's staged \\`.beads/issues.jsonl\\` change persisted through the switch — no visible conflict, but a coordination hazard.\n4. Later carmack switched back to their branch to complete work; sometime after Session A checked out its branch again, potentially with different HEAD.\n\nProblems:\n- No signaling between sessions about \"I'm here, don't switch branches\".\n- Session A's staged changes travelled to session B's branch until carmack noticed.\n- Team-lead reviews (running \\`git status\\`, \\`git log\\`) may inspect the wrong branch's state if branch drifts under them.\n\nMitigation used this session: sibling git worktree via \\`git worktree add ../gqlc-71n-review \u003cbranch\u003e\\`. Kept my review isolated from session A's cwd state.\n\nLonger-term options:\n1. Institutionalize \\`git worktree add\\` per session (each session takes its own sibling worktree at spawn).\n2. Session leader checks \\`git branch --show-current\\` before every branch-modifying op; if unexpected, use a worktree.\n3. Documentation-level guidance in CLAUDE.md's session-completion checklist.\n\nFiled for policy conversation. Non-blocking.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T14:23:40Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T14:31:12Z","closed_at":"2026-07-18T14:29:37Z","close_reason":"Policy stated by user 2026-07-18: every modification-intent session runs in its own sibling worktree; shared repo cwd is read-only research only. No open policy question remaining. Documentation half being shipped via a small CLAUDE.md addition PR (this session); enforcement half is session discipline + memory (workflow-sibling-worktree-per-session).","external_ref":"https://github.com/areqag/gqlc/issues/331","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-1dq","title":"release: add Homebrew tap (areqag/homebrew-gqlc) once release pipeline is stable","description":"Follow-up to [[gqlc-23e]] release automation. Once the tagpr + GoReleaser pipeline is proven stable (a few clean release cycles), enable first-class Homebrew distribution for the gqlc CLI.\n\nSteps:\n1. Create empty companion repo areqag/homebrew-gqlc (Homebrew tap convention: repo name must start with \"homebrew-\").\n2. Uncomment the `brews:` block in .goreleaser.yaml (added as commented placeholder in gqlc-23e implementation, referencing this bead).\n3. Create a fine-grained GitHub PAT with contents:write on areqag/homebrew-gqlc.\n4. Add the PAT as GH repo secret HOMEBREW_TAP_GITHUB_TOKEN in areqag/gqlc.\n5. Cut a test release and verify formula lands in the tap repo.\n6. Document `brew tap areqag/gqlc \u0026\u0026 brew install gqlc` in README.\n\nNot urgent: `go install github.com/areqag/gqlc/cmd/gqlc@latest` covers Homebrew's audience today. Deferred while the CLI is 0.0.x experimental — no point building distribution ergonomics until the surface is stable enough that users would actually pin a version.\n","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T14:11:37Z","created_by":"antranig-yeretzian","updated_at":"2026-07-19T04:55:45Z","closed_at":"2026-07-19T04:55:45Z","external_ref":"https://github.com/areqag/gqlc/issues/327","metadata":null,"labels":["followup","release"],"dependencies":[{"issue_id":"gqlc-1dq","depends_on_id":"gqlc-23e","type":"blocks","created_at":"2026-07-18T10:11:36Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-71n","title":"chore(hooks): reject Co-Authored-By claude trailers + CLAUDE.md AI-attribution rule","description":"Hard-enforce the \"no AI attribution in commits/PRs\" style rule (see\n[[feedback-no-ai-attribution-in-prs-or-commits]]) via three layers, of\nwhich two require code changes:\n\n1. **Local commit-msg hook** (\\`.githooks/commit-msg\\`) — reject any\n   commit whose message contains a Co-Authored-By trailer matching\n   Claude/Anthropic patterns (case-insensitive on the trailer key;\n   match \\`claude\\` in the value OR \\`@anthropic\\.com\\` in the email).\n   Exit non-zero with a helpful message pointing at the CLAUDE.md\n   section. Silent success on clean commits. Same wiring as other\n   \\`.githooks/*\\`: activated by \\`core.hooksPath=.githooks\\` (already\n   set via \\`just init\\`).\n\n2. **CLAUDE.md AI-attribution section** — document the rule, its\n   rationale (AI use is a given; explicit disclosure is noise; bot\n   contributor pollutes GH authorship signal), and reference the\n   commit-msg hook enforcement.\n\n3. **PR-body footer** (the \\`🤖 Generated with Claude Code\\` line) —\n   deliberately NOT hook-enforceable because PR bodies bypass local\n   git. Rely on the CLAUDE.md rule + memory for this half. Escalate\n   to a CI check on PR body if it recurs.\n\nAlso add tests: a \\`.githooks/tests/commit-msg-test.sh\\` in the same\nshape as the existing \\`.githooks/tests/claude-pre-bash-test.sh\\`,\nwired into \\`just test-hooks\\`. Should cover: rejects mixed-case\ntrailer keys, rejects claude in value, rejects anthropic.com email,\naccepts clean commit, accepts human co-author.\n\nImpact: the only commit in the recent session that surfaced Claude\nas a GH contributor was fdddba39 (chore beads close-out) via its\n\\`Co-authored-by: Claude Opus 4.7 (1M context) \u003cnoreply@anthropic.com\u003e\\`\ntrailer. With this hook in place, that commit would have been\nrejected at commit time.","status":"closed","priority":3,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T14:07:10Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T14:25:00Z","started_at":"2026-07-18T14:07:32Z","closed_at":"2026-07-18T14:25:00Z","close_reason":"Shipped in PR #330. Three-layer enforcement: (1) .githooks/commit-msg rejects Co-Authored-By trailers matching claude/anthropic patterns, skipping merge commits via MERGE_HEAD; (2) .githooks/tests/commit-msg-test.sh covers 7 cases wired into just test-hooks; (3) CLAUDE.md 'AI attribution' section documents the rule and hook-enforcement scope. Live-hook meta-check confirmed core.hooksPath wiring. From next commit forward, no Co-Authored-By: Claude trailers can land.","external_ref":"https://github.com/areqag/gqlc/issues/328","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-rvw","title":"codegen-live: promote nightly to PR-blocking merge gate","description":"Promote .github/workflows/codegen-live.yml from workflow_dispatch +\nnightly cron to a PR-blocking check on the merge gate. The codegen -\u003e\ndriver contract is currently only exercised against a real neo4j once\na day; a regression that ships in a codegen refactor sits on master\nfor up to 24h before nightly surfaces it. Blocking on PR closes that\nwindow.\n\nPrecondition: gqlc-9u5 (Go test cache enabled) must be merged first.\nWithout cache, this promotion imposes ~30s on every PR — violates the\nci-speed-parity invariant. With cache, a typical PR pays \u003c1s\n(cached PASS); only PRs that touch codegen-relevant paths pay ~30s.\n\nChanges:\n1. Add on.pull_request to codegen-live.yml (already has push:master +\n   nightly cron; add pull_request).\n2. Update the workflow docstring to reflect its new role — no longer\n   an opt-in satellite, now a merge-gate.\n3. Consider a paths: filter (belt + suspenders alongside Go's cache).\n   Path candidates: internal/codegen/**, internal/resolver/**,\n   internal/schema/**, internal/queryfile/**, test/data/codegen/**,\n   .github/workflows/codegen-live.yml, justfile. Miss one and we'd\n   falsely skip; skip if the cache is trusted enough.\n\nDepends on: gqlc-9u5.\nDescends from: gqlc-5gc scope discussion.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T04:05:01Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T13:51:16Z","started_at":"2026-07-18T05:28:49Z","closed_at":"2026-07-18T13:51:16Z","close_reason":"Code half shipped in PR #323 (on.pull_request trigger). Config half shipped via gh api PATCH (gqlc-oe0) — required_status_checks.contexts now includes live-smoke. Promotion fully enforced.","external_ref":"https://github.com/areqag/gqlc/issues/313","metadata":null,"dependencies":[{"issue_id":"gqlc-rvw","depends_on_id":"gqlc-9u5","type":"blocks","created_at":"2026-07-18T00:05:04Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-9u5","title":"codegen-live: enable Go test cache in CI","description":"Wire the codegen-live workflow so Go's test cache actually works across\nCI runs. Today \\`just test-codegen-live\\` (justfile:98) passes \\`-count=1\\`,\nthe canonical opt-out from Go's test result cache. Combined with\nactions/setup-go@v5 defaulting cache-dependency-path to root go.sum\n(missing the nested test/data/codegen/go.sum), we get zero cache hits\non the live-smoke workflow even when nothing that could affect the\ngenerated code has changed.\n\nChanges:\n1. Remove \\`-count=1\\` from the test-codegen-live recipe. Go's cache\n   invalidates automatically when any input file (source, test binary,\n   env vars the test reads via os.Getenv) changes.\n2. Add cache-dependency-path: |\\n     go.sum\\n     test/data/codegen/go.sum\n   to actions/setup-go@v5 in .github/workflows/codegen-live.yml so\n   nested-module dep bumps invalidate the cache correctly.\n3. Pin neo4jImage in test/data/codegen/live_test.go from\n   \"neo4j:5-community\" to a digest (\"neo4j@sha256:...\"). Mutable tags\n   would allow neo4j to push a rebuild under the same string, and Go's\n   cache — which trusts the string constant as the input — would serve\n   a stale PASS. Digests are immutable, so any digest bump naturally\n   invalidates the cache.\n\nResult: cache-miss = full ~30s run (v5 + v6 arms in parallel).\nCache-hit = \u003c1s \"(cached)\" without touching docker. Sets up the\nfollow-up bead promoting the workflow to PR-blocking.\n\nPrecedes: codegen-live PR-blocking promotion bead.\nDescends from: gqlc-5gc scope discussion.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T04:04:48Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T05:37:01Z","started_at":"2026-07-18T05:14:59Z","closed_at":"2026-07-18T05:37:01Z","close_reason":"Shipped in PR #322. Removed -count=1 from just test-codegen-live, added cache-dependency-path with both root + nested go.sum, pinned neo4jImage to multi-arch manifest-list digest sha256:362542416.... Cache now serves (cached) PASS on unchanged inputs; sets up gqlc-rvw's PR-blocking promotion.","external_ref":"https://github.com/areqag/gqlc/issues/314","metadata":null,"dependency_count":0,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-bz8","title":"codegen: investigate SingleT/CollectT shape for :one and :many emission","description":"Explore whether the generated :one and :many methods should route through\nneo4j.SingleT[T]/CollectT[T] instead of the current \\`records, err := q.db.run(...)\\`\n+ manual \\`len(records)\\` sentinel-branching shape (see generated\ntest/data/codegen/valid/*/golden/queries.cypher.go and db.go).\n\nTrade-offs to evaluate:\n  * Less emitted boilerplate per :one method\n  * SingleT folds \"zero rows\" and \"multiple rows\" into one error — losing\n    the current ErrNoRows / ErrMultipleResults sentinel granularity that\n    is the semantic contract of :one. Any shape change must preserve\n    (or deliberately drop, with owner sign-off) that contract.\n  * :many currently materialises \\`[]*neo4j.Record\\` then decodes; a\n    CollectT[T]-based path could inline the mapper.\n  * Interacts with gqlc-1a5 (:iter streaming cardinality, post-v1).\n\nDeferred from the gqlc-5gc scope discussion: verified v5 and v6 both\nexpose SingleT/CollectT, so this is a codegen-quality question, not a\ndriver-version question. Do the investigation before committing to a\nshape.","status":"closed","priority":3,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T04:04:35Z","created_by":"antranig-yeretzian","updated_at":"2026-07-30T02:34:04Z","started_at":"2026-07-30T02:29:37Z","closed_at":"2026-07-30T02:34:04Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/315","metadata":null,"comments":[{"id":"019fb0df-1a54-781b-be34-80ef81e92d75","issue_id":"gqlc-bz8","author":"areqag","text":"Investigation complete (2026-07-30). Recommendation: **DON'T ADOPT** either SingleT or CollectT.\n\n**SingleT**: irreversibly folds ErrNoRows and ErrMultipleResults into a single untyped *UsageError. The driver exposes no exported sentinels to distinguish them — the only way to tell them apart is message-string parsing. Adopting SingleT destroys the two-sentinel public contract that callers use via errors.Is. Fatal.\n\n**CollectT**: the driverOrTx.run seam returns []*neo4j.Record; CollectT requires a streaming Result cursor. Making them compatible requires either passing a mapper callback through the seam (inverting control, making the interface generic) or exposing the raw cursor past transaction close (unsound). Runtime cost is identical — both paths call Collect then iterate. Boilerplate reduction is 0–1 lines per :many method. Also conflicts with gqlc-1a5 direction, which needs the raw cursor for streaming :iter.\n\nNo code changes. No ADR needed — the decision follows directly from the driver API shape and the existing sentinel contract. Close as investigated / no action.","created_at":"2026-07-30T02:33:59Z"}],"dependency_count":0,"dependent_count":0,"comment_count":1}
{"_type":"issue","id":"gqlc-exl","title":"cypher parser tests: rebaseline cost of the ~112 surviving authored mustParse pins (post-ls8.5 residual)","description":"gqlc-ls8.5's mechanical census showed only 43 of 159 hand-built mustParse pins are shape-mirrors of existing goldens; ~112 are authored inputs the mustParse doctrine (parser_test.go preamble) and TestGoldenOrphans rightly forbid converting to goldens (evidentiary circularity: round-tripping launders current parser behavior into 'expected', and -update silently rebases thereafter). So the original 'maintenance outlier' complaint — every additive model axis costs manual edits across parser_test.go — is only PARTIALLY closed by ls8.5; the authored pins retain that cost. This bead evaluates what, if anything, reduces it without weakening evidence: candidates include (a) contributing authored scenarios upstream to the TCK corpus so they earn goldens legitimately, (b) helper/table consolidation so additive zero-valued axes are absorbed without per-case edits, (c) a narrowly-safeguarded doctrine amendment (would need a first-golden review protocol that answers the silent -update objection concretely). Deliberately open-ended; a 'no action, cost is the price of evidence' close is acceptable if analysis supports it.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-15T00:31:37Z","created_by":"antranig-yeretzian","updated_at":"2026-07-30T02:29:23Z","started_at":"2026-07-18T04:03:11Z","closed_at":"2026-07-30T02:29:23Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/307","metadata":null,"dependencies":[{"issue_id":"gqlc-exl","depends_on_id":"gqlc-ls8.5","type":"blocks","created_at":"2026-07-14T20:31:50Z","created_by":"antranig-yeretzian","metadata":"{}"}],"comments":[{"id":"019f6e3c-0532-7e1c-a8a6-1532194adfdd","issue_id":"gqlc-exl","author":"antranig-yeretzian","text":"gqlc-ls8.5 §9 Q3 by-stage consolidation detail (queued at Phase-3 close;\nreleased post-verdict per team-lead ratification of PR #308 at f33296a).\n\nRecording the concrete shape of candidate (b) — helper/table consolidation —\nas it surfaced during the ls8.5 Phase-1/2/3 grill so the option is\nactionable when this bead moves. Not a proposal; a factual snapshot of the\nresidual pin population by stage, which the ls8.5 census incidentally\nproduced.\n\nBy-stage rebaseline surface (from the residual 117 mustParse entries at\nPhase-3 tip 3,528, per §2 outturn table):\n\n- Stage 4 (UNION-nesting): every mustParse pin that exercises multi-branch\n  structure carries the same one-branch-per-part scaffolding through\n  `oneBranch` / `oneWriteBranch`. An additive Stage-4 axis change (e.g. a\n  new BranchState carrier field) forces per-pin manual edits across every\n  authored UNION-nested pin. Contiguous rebaseline section: group these\n  pins into a Stage-4 subsection of the mustParse map so an axis-change\n  touches N helper-lines instead of N-pins.\n\n- Stage 12 (write-side statements): every pin using `oneWriteBranch`\n  asserts on the StatementKind axis end-to-end. An additive write-side\n  axis (statement-scope, per-clause write metadata, etc.) forces the same\n  pattern of per-pin manual edits. Contiguous rebaseline section: group\n  Stage-12 write pins together so the helper carries the axis burden.\n\n- Stage 14 (CALL / procsig-carrying pins): the residual sig-divergent\n  keeper + any future Class-A sig-carrying pins whose sigs diverge from\n  Background sigs. An additive procsig axis (new `procsig.Signature`\n  field, per-CallBinding metadata) forces manual edits across every\n  Stage-14 pin. Contiguous rebaseline section: group Stage-14 CALL pins\n  together with a shared authored-sigs helper so the axis-change\n  touches the helper, not per-pin bodies.\n\nRationale: the ~20.7 gross lines/pin cost observed in the ls8.5 Phase-2\nsweep (871 lines across 42 deletions; see spec §2 actuals table) is\ndominated by the helper-invocation lines, not the pin body itself. A\nconsolidation that lifts the axis-touching lines into shared helpers\nconverts per-pin manual edits into per-helper manual edits without\nweakening the independent-evidence property (the hand-derived Query\nshape stays hand-derived; only the constructor scaffolding is shared).\n\nConstraint the consolidation must preserve: the reviewer diffing an\naxis-rebaseline PR must still be able to read each pin's expected\nshape independently — the helper cannot become so terse that the pin's\ntarget shape is inferred rather than stated. A helper that returns a\npre-shaped Query is a step too far; a helper that takes the axis values\nas parameters and returns the composed Query is the ceiling.\n\nCross-refs: cypher-golden-test-migration.md §6 (per-pin cost analysis),\n§9 Q3 (the parent question this comment operationalises). PR #308 sha\nf33296a shipped the ls8.5 residual figures this consolidation would\ntarget.","created_at":"2026-07-17T04:00:55Z"},{"id":"019f736e-d8ce-7c3d-9088-28d1b50990f5","issue_id":"gqlc-exl","author":"antranig-yeretzian","text":"Analysis (autonomous, 2026-07-18):\n\nVerified census. `parser_test.go` at HEAD is 3,528 lines; `mustParse` runs L125-2610 and holds **117 pins** (matches ls8.5 Q1 outturn: 43+4+112 pre-migration, then 42 Class-A shape-mirrors deleted; residual = 117). Helper concentration inside the map:\n\n- `oneBranch(query.Part{...})` — 74 top-level uses (60 pre-Stage-12, 1 Stage-13 \"MERGE inside EXISTS does not flip\", 14 Stage-14).\n- `oneWriteBranch(query.Part{...})` — 12 top-level uses (9 Stage-12 write pins, 3 Stage-13 MERGE-write pins).\n- 30 inline `query.Query{Branches: []query.Branch{{Parts: []query.Part{...}}}}` builds + 1 `func() query.Query {...}()` composer, all pre-Stage-12. Of the 30, 3 are true UNION shape (multi-branch: \"union two branches\", \"union param branch attribution\", \"union all two branches\"), ~7 are true WITH-chain shape (multi-part in one branch: \"optional group ids span parts without reset\", \"with chain whole entity\", \"with chain value projection\", \"unwind list of lists\", \"return order by param\", \"with distinct\", \"exists body param via with-where\", \"unwind of list-of-nodes reused as node match\"), and ~20 are single-branch/single-Part pins written raw — mostly Stage-6/7 temporal-arithmetic pins (\"return date constructor\", \"return duration between\", the six duration/date arithmetic pins around L637-682) plus a few Parameter-carrying pins that predate `oneBranch`'s variadic Parameters signature. These 20 could migrate to `oneBranch` today with no design change.\n\nStage bucketing of the 117 by file-position: Stages 0-11 hold 90 pins (concentrated in Stage 6 rich-projection/temporal, Stage 8 pattern shapes, and Stage 11 EXISTS-body pins — 15+ EXISTS-body pins in the L1397-1938 range dominate this segment); Stage 12 write pins hold 9; Stage 13 MERGE holds 4; Stage 14 CALL holds 14. Sig-carrying pins total 12 (11 with populated `sigs`, the sole `sigDivergentKeepers` member the survival case). The 3-way clustering the beads comment names (Stage 4 UNION-nesting, Stage 12 write-side, Stage 14 CALL/procsig) is only partly correct: Stage-14 CALL is a real 14-pin cluster with uniform `oneBranch`+`sigs` scaffolding; Stage-12 write is a real 9-pin cluster on `oneWriteBranch`; but Stage-4 UNION-nesting is only 3 pins — the comment's framing of \"every authored UNION-nested pin\" over-counts by conflating \"multi-Part WITH chains\" (~7 pins) with UNION (3 pins).\n\nEmpirical axis-cost check against the shipped model-change specs, which is the load-bearing calibration the bead is missing: **ay9** flipped **3 mustParse pins** (spec §4.3, L84-91: \"optional match simple\", \"optional match reuses prior binding\", \"count distinct\" — 4 `NewNullable*` invocations become `*InGroup(..., 1)`); the other 149 Binding-constructor invocations in the file stayed verbatim under `require.Equal`. **hk0** flipped **1 pin** (\"count in arithmetic\"); the other 24 ExprProjection assertions stayed verbatim (Shape B's zero-valued `ContainsAggregate` is bit-identical). **0ig** touched **11 pins** carrying 15 CallBinding invocations, of which only those with ≥1 argument rebaseline (spec §4.3). **5xg** flipped **0 pins** — Go zero-value equality holds. Ledger: 3 / 1 / 11 / 0. The bead description's \"every additive model axis costs manual edits across ~112 pins\" is empirically wrong; the actual cost per shipped axis has been 0-11 pins, and each cluster was the exact subset whose src exercised the changed axis.\n\nCandidate evaluation. **(a) Upstream to TCK.** The 112 Class-C pins are authored precisely because the TCK is silent on the sentinel taxonomy they encode: 5xg's required-bare-ref flag, fvo's Use-part attribution, ay9's per-clause OptionalGroup — none of these are surface Cypher semantics the openCypher project would accept as scenarios. A handful (some MERGE and CALL kill-probes) plausibly map to legitimate skeptical scenarios, but even a maximally-successful contribution cycle would land ~5-10 scenarios upstream over several TCK bumps, converting \u003c10% of the Class-C set at best, on a multi-quarter cadence outside gqlc's control. Cost/removal ratio is poor.\n\n**(b) Helper/table consolidation.** The relevant axis cost per shipped spec is 0-11 pins per axis, i.e. it is not concentrated across all Class-C pins — the axis touches only pins whose src exercises it. Under `require.Equal` a helper that hides an axis inside its scaffolding gives you nothing you don't already have from Go zero-value equality: hk0's 24 non-flipping ExprProjection pins pay ZERO edit cost today. What consolidation could buy: for the 14 Stage-14 CALL pins, a helper `oneCallBranch(part query.Part, sigs []procsig.Signature, params ...query.Parameter) query.Query` co-locates the `sigs` field with the branch so any procsig axis change edits the helper; for the 3 UNION pins, `twoBranchUnion(a query.Branch, b query.Branch, kind query.UnionKind, params ...query.Parameter)` closes the raw-Branches inlining. Both preserve the \"reviewer reads each pin's expected shape independently\" constraint IF the helper returns a composed `query.Query` from parameterized inputs, not a pre-shaped Query. The real gain is measured in per-cluster line counts, not per-axis edits — which is a code-hygiene win, not a maintenance-outlier fix.\n\n**(c) Doctrine amendment.** The `-update` laundering objection is concrete: minting a golden from an authored src runs the parser once under `-update`, records that output as expected, and every subsequent `-update` silently rebaselines through the same parser. A first-golden review protocol would need to (1) prohibit `-update` from ever touching an authored-origin golden except through a two-reviewer human sign-off that inspects the diff against the model contract, and (2) tag each authored golden at mint time with the ADR/spec §-ref that ratified its shape. Both are hard to enforce in code — `-update` is one flag on `go test`, not a per-file gate. Any protocol short of a file-level `//go:build authored-golden` guard that fails `-update` unless overridden by an environment variable will drift under time pressure. The safeguard exists as an idea but doesn't answer the objection concretely — it just adds a review layer over the same silent-rebase mechanism.\n\nRecommendation. **Adopt a narrow (b) targeted at Stage-14, with 20-pin `oneBranch` migration cleanup as a separable follow-on.** Not (a) — the upstream conversion rate is too low. Not (c) — the doctrine amendment does not answer the `-update` objection concretely enough to override the lead ruling that stands from ls8.5 §1.4/§8. Not \"no action\" — the Stage-14 CALL cluster is a genuine 14-pin uniform-scaffold surface where an axis addition would touch every pin; consolidation here is a real win, not a code-hygiene wash.\n\nConcrete helper signatures for (b):\n\n```go\n// oneCallBranch composes a Stage-14 one-branch/one-part CALL pin: same\n// shape as oneBranch, plus authored procedure sigs the parser needs for\n// registry-backed CALL resolution. Every Stage-14 mustParse pin uses\n// this so a procsig axis change touches the helper, not per-pin bodies.\nfunc oneCallBranch(\n    part query.Part,\n    sigs []procsig.Signature,\n    params ...query.Parameter,\n) (query.Query, []procsig.Signature)\n```\n\nReturning both the Query and the `sigs` slice lets the pin body destructure into `want, sigs` at the call site — the pin still names its `sigs` in-place, but any future field addition on `procsig.Signature` (a per-CallBinding metadata axis, an authored-vs-Background provenance bit, etc.) edits `oneCallBranch` once. Applies to all 14 Stage-14 pins.\n\n```go\n// twoBranchUnion composes a Stage-4 two-branch UNION pin: two branches\n// (each supplied as an already-built Branch), a single combinator, and\n// query-level Parameters. Every UNION pin in mustParse uses this so a\n// combinator or per-branch metadata axis touches the helper, not the pin.\nfunc twoBranchUnion(\n    a query.Branch,\n    b query.Branch,\n    kind query.UnionKind,\n    params ...query.Parameter,\n) query.Query\n```\n\nApplies to 3 pins (\"union two branches\", \"union param branch attribution\", \"union all two branches\").\n\nThe 20 raw-inlined single-branch/single-Part pins (\"return date constructor\" through \"return duration between\", plus the temporal/arithmetic follow-ons at L637-682, plus the parameter-carrying pins that predate `oneBranch`'s variadic signature) should be migrated to `oneBranch(query.Part{...}, params...)` in a separable stylistic PR — no design change, no axis cost, just eliminating one class of divergent scaffolding a future reviewer would trip over. Explicitly NOT part of this bead's decision; log it as a separate low-priority code-hygiene bead. Not scoping to Stage 12 either: `oneWriteBranch` already exists and every Stage-12 write pin uses it. Not adding a \"WithChain\" helper — the 7 multi-Part WITH-chain pins each have a unique Part composition (part 1's Returns are the WITH projection; part 2's Bindings are what the MATCH-after-WITH introduces; the shapes diverge structurally); a helper here would either return a pre-shaped Query (violates the reviewer-reads-pin-shape constraint) or take so many parameters it's less legible than the inline form.\n\nConstraints for future work. A bad first attempt at (b) would introduce a helper that materializes the pin's target shape from an axis argument the pin never names — e.g. `stage14CallPin(name string) query.Query` that hard-codes each pin's expected Query by key. That launders the same evidentiary circularity the mustParse doctrine forbids (the \"expected\" shape is now hidden in Go code the reviewer must dereference to read). The doctrine cliff: any helper whose return value contains a Bindings/Returns/Effects slice the caller did not pass in is over the line — the helper must be a pure composer of caller-supplied parts. A second cliff to avoid: coupling `oneCallBranch` to `sigs` provenance (Background-equal vs authored-divergent) — the coupling belongs at pin declaration site (as the current `sigDivergentKeepers` allowlist encodes it), not inside the helper. Signal of a bad attempt: the helper signature grows past ~4 parameters, or the pin body loses the ability to be read as \"this src produces this shape\" without stepping into the helper.\n\nThe bead's original headline — \"every additive axis costs manual edits across ~112 authored pins\" — should be closed as OVERSTATED at ls8.5 close. The load-bearing 112 Class-C pins are cheap under `require.Equal` for every axis whose zero value coincides with the pre-axis shape; the actual per-axis cost has been 0-11 pins, and each of those was the exact subset whose src exercised the axis, which is the cost of evidence, not of scaffolding. (b) removes a real 14-pin surface at Stage 14 without weakening evidence; the residual across Stages 0-11 stays authored, per doctrine, and per the empirical 0-3 pin-touch cost the shipped model-change record proves.\n\nIndependent verification: 117-pin count reproduced (main-checkout grep against L125-2610); 74/12/30/1 helper decomposition inspected; agent-attributed axis-flip counts (ay9=3, hk0=1, 0ig=11, 5xg=0) NOT independently re-verified against each PR's diff — best-effort agent claim. Owner call: pick between narrow (b) at Stage-14, \"no action pending a real axis demand\", or defer to a specific follow-up bead for the 14-pin oneCallBranch cutover.\n","created_at":"2026-07-18T04:14:32Z"}],"dependency_count":1,"dependent_count":0,"comment_count":2}
{"_type":"issue","id":"gqlc-kww","title":"cypher collection-sink polish: stale sink-lift docstring + fence-message sink list","description":"Two non-blocking polish items deferred from the gqlc-ls8.2 whole-diff PASS (linus-2, 2026-07-14): (1) listener.go:222 doc-string still says 'The per-method //nolint:unused directives lift as callers land' — Phase C complete, all directives lifted, sentence is anachronistic. (2) sink_fence_test.go:153 hard-codes the sanctioned-sinks list as prose in the failure message instead of deriving from the sinkFuncs map keys; enforcement is map-authoritative so soundness is fine, but the message can go stale. Both are two-line edits; bundle in any future cypher touch.","status":"closed","priority":3,"issue_type":"chore","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-14T14:51:18Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T04:14:57Z","started_at":"2026-07-18T04:03:09Z","closed_at":"2026-07-18T04:14:57Z","close_reason":"Shipped in PR #316: two-line polish landed — listener.go Phase A/C/E lifted to past tense (nolint-lift sentence dropped) + sink_fence_test.go fence message derived from sinkFuncs map keys (sorted).","external_ref":"https://github.com/areqag/gqlc/issues/304","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5gc","title":"codegen: v6 arm for the live-smoke testcontainer (TestLiveSmoke)","description":"just test-codegen-live / TestLiveSmoke proves generated repositories against a real neo4j testcontainer, but only for the v5 driver: it hard-imports two v5 golden fixture packages and constructs a v5 driver. Now that codegen emits v6 (gqlc-lq3, PR #259), the live smoke should gain a v6 arm: v6 twins of the harness fixtures plus a second driver-construction path (github.com/neo4j/neo4j-go-driver/v6). Carmack evaluated during gqlc-lq3 and confirmed non-trivial — needs its own cycle. Also from Linus's gqlc-lq3 review: if a fixture-authoring doc materialises in docs/specs, mirror the manifest.json driverVersion marker documentation there.","notes":"2026-07-12: not started this session — no docker on this host (test-codegen-live cannot be validated first-party), and worktree agent-ae6c9d3f61c7afa48 holds a locked branch feat/gqlc-73h-codegen-live-smoke, so the live-smoke lane may have concurrent work. Coordinate before claiming.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T04:32:28Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T05:15:58Z","started_at":"2026-07-18T04:05:05Z","closed_at":"2026-07-18T05:15:58Z","close_reason":"Shipped in PR #320. Live-smoke v6 arm alongside v5 with both t.Parallel(); v6 fixture twins added; nightly codegen-live workflow will exercise the actual container run.","external_ref":"https://github.com/areqag/gqlc/issues/298","metadata":null,"dependencies":[{"issue_id":"gqlc-5gc","depends_on_id":"gqlc-lq3","type":"blocks","created_at":"2026-07-12T00:32:27Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-do1","title":"codegen: v6-only column types — Vector + UUID (schema→resolver→codegen)","description":"neo4j-go-driver v6 adds dbtype.Vector (Bolt 6.0) and native UUID (Bolt 6.1, v6.2.0). Mapping these as gqlc column types requires the full pipeline: schema property types, graph.PropertyType, resolver ResolvedType, codegen type tables (gated on DriverV6 — reject or fall back under DriverV5). Deliberately out of scope for the v6-option stack: no vector/uuid anywhere in internal/graph or internal/schema today, so this is schema-layer design work first. Also evaluated and deferred: v6.2 object-mapping preview API (unstable, preview-labelled — do not codegen against it); session.Run one-shot retry ADR-038 (generated code uses ExecuteRead/ExecuteWrite closures, no win).","design":"Design research banked 2026-07-12 (verified against driver 6.x branch + Neo4j docs):\n\nDRIVER SHAPES: dbtype.UUID = [16]byte (RFC 9562, ParseUUID/String/TextMarshaler). dbtype.Vector[T VectorElement], VectorElement = float64|float32|int8|int16|int32|int64. neo4j.PropertyValue constraint (graph.go:25-30) already includes UUID and ALL SIX Vector instantiations -\u003e generated decode via neo4j.GetProperty/GetRecordValue works unchanged; no new decode machinery.\n\nSCHEMA SPELLING: adopt Cypher 25 official syntax VECTOR\u003cFLOAT32\u003e(1024) (element + dimension; used in IS :: constraints, Neo4j 2025.10+, requires v6 drivers). UUID: no GQL-standard spelling exists; propose bare UUID keyword.\n\nGRAMMAR DELTA (the hard part): GQL.g4 valueType/predefinedType has NEITHER — UUID needs a predefinedType alternative + keyword token; VECTOR needs a parameterized rule (angle-bracket element + parenthesized dimension). Regen = just build-grammar = docker + ANTLR 4.13.1.\n\nPIPELINE MAP: spellings table internal/schema/gql/propertytype.go:53-108 (canonicalSpelling normalization); enum internal/graph/propertytype.go:9-39 — NB flat enum cannot carry Vector's element+dimension: main modeling decision is PropertyType growing structure (or parallel fields) — deserves grilling/ADR. Resolver: ResolvedProperty passes graph.PropertyType through (resolve.go:1300,1345) — no resolver enum change for property-sourced columns. Codegen v5-rejection precedent: goType ok=false -\u003e ErrUnrepresentableWidth at prepare.go:410/498/560 + types.go:254; UUID/Vector under DriverV5 should mirror it with a new error (requires-driver-v6), which means threading driverTarget into the prepare phases (today target only reaches render).\n\nSTAGING: (1) UUID first — no parameterization: keyword + TypeUUID + goType v6 arm + v5 rejection + _v6 goldens. (2) Vector second — grammar param type + PropertyType shape change, grill the modeling with the user first. Dimension: store for future constraint emission; Go carrier ignores it (Vector.Elems is a plain slice).","notes":"2026-07-12: claim released — implementation blocked on this host: grammar regen needs docker (none) or java (none). Design fully banked in DESIGN field; UUID stage is mechanical once grammar tooling is available; Vector stage needs a PropertyType-shape grill with the user first.","status":"open","priority":3,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T04:01:41Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:23Z","started_at":"2026-07-12T06:21:31Z","external_ref":"https://github.com/areqag/gqlc/issues/299","dependencies":[{"issue_id":"gqlc-do1","depends_on_id":"gqlc-lq3","type":"blocks","created_at":"2026-07-12T00:01:50Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-aqb","title":"Wire procsig.Load into codegen CLI as -procsig flag","description":"Follow-up to gqlc-6rz. The procsig on-disk file format and loader (Load/Decode/MarshalJSON/Save) shipped in PR #251 but the CLI entry point that consumes them is out of scope for that bead — no real CLI exists today; main.go is a dev-driver that parses a sample schema. Once a codegen CLI ships (or main.go grows a proper subcommand), add a -procsig \u003cpath\u003e flag that resolves to procsig.Load(path) and threads the returned Registry into the parser via cypher.WithRegistry. Explicit in gqlc-6rz spec §7.","notes":"2026-07-12: config file format (gqlc-3w2) now carries an optional 'procsig:' path field — when the CLI ships, read Config.ProcsigPath (flag may override) -\u003e procsig.Load -\u003e cypher.WithRegistry. This bead stays open until that threading exists.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T18:18:59Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:23Z","closed_at":"2026-07-12T21:55:52Z","external_ref":"https://github.com/areqag/gqlc/issues/300","dependencies":[{"issue_id":"gqlc-aqb","depends_on_id":"gqlc-6rz","type":"blocks","created_at":"2026-07-11T14:19:03Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-aqb","depends_on_id":"gqlc-m1c.2","type":"supersedes","created_at":"2026-07-12T17:55:52Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-2ud","title":"Codegen C4 spec §4.9: mark truth-table cells as pipeline-unreachable","description":"Two truth-table cells at spec §4.9 (docs/specs/codegen-stage-c4.md) are unreachable via the current test pipeline: (a) (:exec, StatementRead, len(Columns)==0) requires CALL-no-YIELD which needs a procsig registry entry (harness uses empty registry); (b) (:one/:many, StatementRead, len(Columns)==0) is grammatically rejected — oC_SinglePartQuery requires oC_Return when no updating clause is present. Both sentinel paths are covered by write-side symmetric fixtures (per Linus PASS on PR #237). Amend the spec to explicitly mark these cells as pipeline-unreachable with rationale. Docs-only follow-up.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T15:07:15Z","created_by":"antranig-yeretzian","updated_at":"2026-07-12T06:52:19Z","started_at":"2026-07-11T17:05:59Z","closed_at":"2026-07-12T06:52:19Z","close_reason":"Shipped in PR #246 (docs(specs): mark codegen-stage-c4 §4.9 unreachable truth-table cells). Dagger footnotes + pipeline-unreachable note per cell, pointing at write-side symmetric fixtures covering the sentinel paths (C4 Linus PASS on PR #237). Bead was left IN_PROGRESS after merge — closed during gqlc-3w2 loop housekeeping 2026-07-12.","external_ref":"https://github.com/areqag/gqlc/issues/238","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-v5t","title":"resolver: builtin scalar-function signature table (elementId → string)","description":"ADR 0010 D3 resolved entity structs as schema-shaped only; driver identity is accessed via explicit projection: RETURN p, elementId(p) AS id. The resolver currently types function invocations as unknown, so the alias renders as 'any' in generated code. Add a small builtin scalar-function signature table to the resolver — elementId(node|edge) -\u003e string is the motivating entry; the same mechanism serves other builtins (id(), count(), etc.) later. When it lands, the recommended identity pattern types as string with zero codegen changes. Not blocking any C-stage.","status":"closed","priority":3,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T02:50:20Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T17:25:09Z","started_at":"2026-07-11T17:09:34Z","closed_at":"2026-07-11T17:25:09Z","close_reason":"Shipped in PR #248. Parser-side builtin scalar-function signature table wired into classifyFunction + typeAtom; motivating entry elementId(node|edge) -\u003e string plus id(node|edge) -\u003e int. Codegen fixture identity_element_id/ pins the ADR 0010 D3 identity pattern rendering Id string (was any).","external_ref":"https://github.com/areqag/gqlc/issues/217","dependencies":[{"issue_id":"gqlc-v5t","depends_on_id":"gqlc-8i0","type":"discovered-from","created_at":"2026-07-10T22:50:20Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-1a5","title":"Codegen: :iter streaming cardinality (ADR 0010 D8, post-v1)","description":"Fourth cardinality :iter generating streaming methods — func (q *Queries) X(ctx, ...) iter.Seq2[Row, error] — per ADR 0010 D8. Rationale: avoid sqlc's structural weakness where []Row materialisation makes full-result loads easy and streaming unretrofittable; neo4j driver streams natively and range-over-func scopes session cleanup to loop exit (early break included). v1 reserves the space only: cardinality is an open enum, method shapes assume no materialisation. Open design questions to settle when picked up: author-declared :iter vs auto-generated XIter sibling for every :many; interplay with SKIP/LIMIT pagination params; requires Go \u003e= 1.23 in the user's module.","notes":"Design opens transferred from ADR 0010 D8 (grill close-out, 2026-07-11): (1) :iter as author-declared cardinality vs an auto-generated XIter sibling for every :many; (2) interaction with SKIP/LIMIT pagination parameters (orthogonal — document both). v1 committed exactly two enablers: cardinality is an open enum (annotation grammar admits :iter without a break) and nothing in the D2 method shape assumes materialisation. Session-lifetime hazard owns the design: iter.Seq2 scopes cleanup to loop exit incl. early break.\n\nDesign opens resolved (grill, 2026-07-11): ADR 0010 §D8 amended in PR #250 with seven Resolved-grill markers. Summary:\n- Author-declared :iter opt-in (auto-XIter siblings rejected)\n- :iter read-only; ErrIterOnWrite sentinel rejects writes\n- Cardinality×shape validation extends symmetrically\n- Method naming inherits §D2 verbatim\n- Retry envelope: pre-first-yield only (post-yield errors yielded to caller, no double-yield)\n- SKIP/LIMIT orthogonal\n- Testing: goldens + gqlc-73h live smoke extension\nUniversal :iter (kill :many) considered and tabled for later revisit.\nImplementation trigger: gated on user demand or benchmarked need. Grill markers in ADR are the paved runway.","status":"open","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-11T02:00:30Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T17:58:27Z","external_ref":"https://github.com/areqag/gqlc/issues/218","dependencies":[{"issue_id":"gqlc-1a5","depends_on_id":"gqlc-8i0","type":"blocks","created_at":"2026-07-10T22:00:37Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-vl9","title":"GH↔bd sync: bd→GH close/update propagation for already-mirrored beads","description":"The GH↔bd sync hooks (PR #201) cover three directions:\n- Post-merge pull: GH → bd (creates, closes, comments) — works.\n- Session-start pull: GH → bd — works.\n- Pre-push push: bd → GH — but ONLY for beads without external_ref (i.e., NEW beads).\n\nUncovered: bd → GH status/comment updates on ALREADY-MIRRORED beads. When an agent runs 'bd close gqlc-abc' on a bead that already has a GH mirror, the pre-push hook's filter (external_ref == null) skips it, and the GH mirror stays open until the human closes it manually.\n\nRoot cause: the fast filter needed to avoid the ~30s cost of 'bd github sync --push-only' (which scans every bead against GH) doesn't cover updates on existing beads. Session memory kv.memory.bd-github-sync-quirks notes that even --push-only may not reliably propagate close status (bd bug).\n\nOptions to close the gap (design decision pending):\n1. Accept it — treat GH as the canonical close-decision surface; agent closes on bd are recommendations, human closes on GH are authoritative. Requires human touch on every close.\n2. Track dirty beads via a marker file that bd updates on close/update, and pre-push consumes and clears.\n3. Wait for a bd feature that emits close-since-last-sync events (upstream bd change).\n4. Fall back to 'bd github sync --push-only' (30s) only when a fast preflight detects a locally-closed bead that has an external_ref. Requires a per-bead updated_at vs GH-updated_at compare that bd currently only exposes via full sync.\n\nNo consumer is blocked; the workflow degrades gracefully (human sees stale-open GH issue, closes manually, next post-merge propagates the close back into bd).","status":"closed","priority":3,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-10T06:24:10Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T04:47:57Z","started_at":"2026-07-18T04:39:51Z","closed_at":"2026-07-18T04:47:57Z","close_reason":"Shipped in PR #318 (squash 81e5bec). Extended .githooks/bd-gh-sync push path: bulk gh issue list --state open intersected with closed-bd beads bearing external_ref; each mismatch gets gh issue close with a comment derived from close_reason. Verified live on push: 13 stale mismatches accumulated since PR #201 all closed cleanly (GH open 26→13, matching real open-bead count). Silent, idempotent, non-fatal. All CI green (tidy/lint/test/actionlint/codegen-fence/govulncheck).","external_ref":"https://github.com/areqag/gqlc/issues/202","labels":["priority::low","type::feature"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0kq","title":"Model / resolver: edge-side non-bare OPTIONAL-edge re-reference witness (post-5xg residual)","description":"Post-5xg residual, filed at unfreeze cycle 5 close-out. The 5xg widening (PR #134) closes same-Part Class B same-Part bare-pattern demotion for both NodeBinding and EdgeBinding. However, the parser's bare-predicate (bare == chain-head with empty chain) is grammatically unreachable for edge variables — every edge sits between two nodes in a chain link. Consequently, an OPTIONAL-introduced edge re-referenced by name in a required chain link (e.g. OPTIONAL MATCH (a)-[r:R]-\u003e(b) MATCH (c)-[r]-\u003e(d) RETURN r) has ReferencedInRequiredBarePattern()==false and stays nullable under both 5xg and ay9 (its group has no proven member; itself is the OPTIONAL binding, not a witness). Neither Class A nor Class B closes this case. Candidate closures: (1) widen the 5xg parser predicate to admit edges in required chain links (would set flag on r); (2) add a distinct axis (e.g. ReferencedInRequiredChain []ClauseRef or bool); (3) treat this as an ay9 refinement (a same-name required chain-edge witnesses the OPTIONAL edge's group). Design decision pending. This is a legitimate under-demotion (lattice-safe under R4 §4.2) with a small blast radius (only OPTIONAL edges re-referenced by name in required chains).","status":"closed","priority":3,"issue_type":"feature","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-10T02:47:50Z","created_by":"antranig-yeretzian","updated_at":"2026-07-30T03:24:03Z","started_at":"2026-07-30T03:16:43Z","closed_at":"2026-07-30T03:24:03Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/174","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-984","title":"Resolver: cross-Part OPTIONAL-group carry via branchState (post-ay9 residual)","description":"Post-ay9 residual, filed at unfreeze cycle 4 close-out. The ay9 widening (PR #129) closes same-Part Class A OPTIONAL-clause-sibling demotion via a group-closure fixed point in demoteNullableInPlace. Cross-Part carry across WITH is unchanged: the resolver-internal branchState.exportedNullableBinding (internal/resolver/resolve.go:357-361) is name to bool and does not carry OPTIONAL-group ids. A binding carried across WITH keeps its own Nullable bit but loses its ay9-widened group closure — the downstream Part's demoter cannot see the source Part's OPTIONAL-group membership, so a WITH-carried OPTIONAL binding subsequently proven demotes only itself, not its group-siblings from the source Part. Extend branchState to carry OPTIONAL-group ids alongside the nullable bit (resolver-internal — no model change; ADR 0008 unaffected). Detail in docs/specs/unfreeze-ay9-optional-group.md sections 2.5 and 7 (residual note). This is a normal resolver refinement, not a model unfreeze.","status":"closed","priority":3,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-10T01:17:30Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T18:49:18Z","started_at":"2026-07-11T18:04:12Z","closed_at":"2026-07-11T18:49:18Z","close_reason":"Shipped in PR #253 (feat(resolver): cross-Part OPTIONAL-group carry via branchState). Extended branchState.exportedOptionalGroup (name → int) so WITH-carried OPTIONAL bindings preserve group-id membership across Part boundaries. Group closure in demoteNullableInPlace unions carried and local memberships. Resolver-internal, no model change, ADR 0008 unaffected. New fixture demote_cross_with_optional_group.cypher; existing golden demote_bare_reference_composes_with_group flipped from Class A under-demote to tight demote (bonus 5xg §8.4 fixture 4 boundary lift). Spec ay9 §2.5+§7 amended. Recovered from stalled agent by orchestrator; force-push --lease after gci fix.","external_ref":"https://github.com/areqag/gqlc/issues/175","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-8en","title":"R7 spec §1 + §4.8 still assert pre-0ig deferred posture (follow-up errata)","description":"PR #124 re-review PASS carried two non-blocking NITs in pre-existing R7-era prose untouched by the 0ig errata commits: (1) §1 'Honest scope-vs-ADR delta' (docs/specs/resolver-stage-r7.md:44-54) asserts argument assignability has 'no resolver-side application site' unstruck — it is 'at R7'-scoped but is the doc's headline scope statement far from any errata marker; (2) §4.8 (:1560-1567) 'The NUMBER assignable-from axis has NO R7 application site… the parser does not emit it' is present-tense and false post-#122/#123. Fix: strike+annotation in the 0ig house pattern, or a doc-top errata pointer. Docs-only, small.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-07T17:53:24Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:15Z","started_at":"2026-07-10T05:24:50Z","closed_at":"2026-07-10T06:07:15Z","close_reason":"PRs #139/#140/#143 merged; Linus PASS on each.","external_ref":"https://github.com/areqag/gqlc/issues/176","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-qcc","title":"Use.Branch axis: UNION cross-branch attribution residuals (post-fvo)","description":"Follow-up from unfreeze cycle 2 (gqlc-fvo, PR #118). Use records carry Part but no BRANCH index; the frozen model cannot derive per-branch Use counts (WHERE-body Uses leave no footprint on Part.Bindings/Returns/Effects — fvo spec erratum 2, §7.2.2 countUsesInBranch unimplementable). Shipped recovery: position cursor with Part-index-drop boundary detection + witnessInBranch cross-branch fallback at the SAME Part index on ErrUnknownProperty. Two verified residuals (linus, PR #118 review; no corpus fixture triggers either): (a) same-Part UNION boundary ambiguity — a Use invalid in its true branch but valid in a sibling branch at the same Part is admitted via fallback (any-valid-witness survives in the cross-branch direction only); (b) UNION-later-Part spurious rejection — branch 0 with fewer Parts than the Use's Part index hits ErrOutOfR0Scope on the primary try and the fallback does NOT recover from non-ErrUnknownProperty errors, so a Use genuinely in branch 1 Part 1 is spuriously rejected. Fix: additive 'branch int' axis on Use records under ADR 0008 revision protocol (or equivalent attribution), then delete the cursor+fallback recovery. See fvo spec §7.2.2 erratum and R5 §4.2.4 cross-refs.","notes":"Linus PASS (2026-07-12, PR #262). Two non-blocking NITs recorded for posterity: (1) resolve.go ~407-409 retains stale pre-fvo comment prose describing the old cursor recovery narrative — candidate for a drive-by prose fix next time that region is touched; (2) ADR 0008 amendment claim 'no golden carries a Use outside branch 0' is vacuously true (no golden serializes Use records at all) — wording could be tightened. Neither affects correctness.","status":"closed","priority":3,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-07T07:50:02Z","created_by":"antranig-yeretzian","updated_at":"2026-07-12T07:03:50Z","started_at":"2026-07-12T06:29:02Z","closed_at":"2026-07-12T07:03:50Z","close_reason":"Shipped in PR #262 (squash-merged 2026-07-12). Added Branch attribution axis to all three Use variants, stamped once at the addParameterUse emission chokepoint; resolver routes Uses directly via tables[u.Branch()] (range-checked to ErrOutOfR0Scope); deleted witnessInBranch/usePart/cursor+Part-drop recovery entirely. Both verified residuals pinned: unknown_property_union_sibling_branch.cypher (residual a, now invalid) and parameter_union_later_part.cypher (residual b, now valid, golden STRING NOT NULL). Spec closure in fvo §7.2.2/§7.2.1/§12, R5 §4.2.4, ADR 0008 dated amendment. Linus PASS; two non-blocking NITs in notes.","external_ref":"https://github.com/areqag/gqlc/issues/177","metadata":null,"labels":["model","resolver","unfreeze-followup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-4w5","title":"Semantic-scope attribution: WITH…WHERE aliased-shadow residual (post-fvo)","description":"Follow-up from unfreeze cycle 2 (gqlc-fvo, PRs #116-#118). Part attribution on Use records is EMISSION-TIME, which diverges from Cypher's post-projection semantics in two documented shapes: (1) FINDING-9 (fvo spec §7.6.1): WITH-trailing WHERE PropertyUses mine PRE-swap (mineWhere at listener.go:283-284 runs before the Part swap at :292-293), so 'MATCH (a:Post) WITH a.title AS a WHERE a.x = $p RETURN a' attributes Ref{a,x} to Part 0 (a=Post) and the resolver ADMITS if Post has x, though Cypher evaluates the WHERE post-projection (a=STRING). No regression vs pre-fvo any-valid-witness, but not full Cypher semantics. (2) Intra-clause asymmetry (found in PR #117 review): a direct param in 'WITH n WHERE $x' lands Part 0 (pre-swap mineWhere) while an EXISTS-body param in the same syntactic WHERE lands Part 1 (natural walk post-swap, listener.go:627-638; parser pin 1712). Closing this needs a SEMANTIC-scope attribution axis (which scope evaluates the predicate), not the lexical Part axis — a distinct model decision, likely another ADR 0008 additive revision. See fvo spec §7.6.1/§9 and errata.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-07T07:50:00Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:45:46Z","external_ref":"https://github.com/areqag/gqlc/issues/178","labels":["model","resolver","unfreeze-followup"],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lta","title":"Doc: Effect docstring says 'seven' variants, actual is eight (query.go:1633-1636)","description":"Linus R6 spec-review NIT-1: the frozen Effect interface docstring at internal/query/query.go:1633-1636 says a clause effect is 'exactly one of the seven' variants, but there are EIGHT — MergeEffect (query.go:2101) also implements isEffect(). Doc-only cleanup; no API/behaviour change, does NOT require the ADR 0008 unfreeze protocol (comment text is not frozen surface). Fix the count or reword to avoid a hardcoded number.","status":"closed","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-06T04:42:36Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:15Z","started_at":"2026-07-10T05:24:02Z","closed_at":"2026-07-10T06:07:15Z","close_reason":"PR #137 merged: Effect docstring 'seven' → 'eight'. Linus PASS. NIT: enumeration list still names only 7 variants — deferred (not in scope for one-word fix).","external_ref":"https://github.com/areqag/gqlc/issues/179","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7w5","title":"R5 nits: spec wording + edge re-bind coverage fixture (Linus code-round N-1/N-2/N-3)","description":"Non-blocking follow-ups from Linus's R5 code review PASS (PR #102). N-1: tighten §4.2.4 wording in docs/specs/resolver-stage-r5.md — the any-valid-witness prose can be read as trying scopes in order rather than all scopes independently. N-2: add a coverage fixture for var-length × fixed-length edge re-bind across WITH (the §6.4 edge guard keys on Labels().Key(); a var-length vs fixed-length same-key re-bind is admitted — pin that this is intended). N-3: copy-nit — §4.2.4:828 and §7:2333 describe the same residual unsoundness with slightly divergent phrasing; align them. All three are doc/test-only; no resolver behavior change.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-05T23:09:44Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:16Z","started_at":"2026-07-10T05:26:23Z","closed_at":"2026-07-10T06:07:16Z","close_reason":"PR #149 merged (R5 nits: §4.2.4/§7 wording 'any-scope-admits' alignment + var-length×fixed edge re-bind fixture). Linus PASS.","external_ref":"https://github.com/areqag/gqlc/issues/180","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5xg","title":"Model unfreeze: preserve same-Part second-reference facts on Binding (ADR 0008 additive) — R4 flow-typing Class B","description":"Frozen-model gap Class B (R4 spec §7.5.3 item 2): mergeBinding (internal/query/cypher/pattern.go:373-401) DISCARDS the second occurrence of an already-bound variable, so a required bare-pattern re-reference of an OPTIONAL-introduced binding in the same Part (e.g. OPTIONAL MATCH (a)-[:R]-\u003e(b) MATCH (b) RETURN b) leaves no witness in part.Bindings — the demoter cannot see it. NOT closable by the Class A group axis: the witness itself is destroyed, not merely ungrouped. Proposal per spec §7.5.4 Axis 2, two candidate shapes: (a) RequiredReferences []ClauseRef or (b) ReferencedInRequiredBarePattern bool on Binding — additive, zero-value-safe, in-protocol under ADR 0008 post-freeze revision. Capability recovered: same-Part bare-pattern demotion (regime (b) within a single Part; cross-WITH regime (b) is R5 scope and needs NO model change). PROTOCOL: model change lands in its own unfreeze PR (ADR 0008 revision), never rides a resolver-stage PR. No R4 fixture exercises this shape — the widening PR adds one. Independent of the Class A bead — owner may accept one, both, or neither. Owner decision pending.","notes":"Filed at R4 close-out per merged spec §7.5.5 / §9 DoD item 9. Design detail: docs/specs/resolver-stage-r4.md §7.5.","status":"closed","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-05T14:34:22Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:16Z","started_at":"2026-07-10T01:22:54Z","closed_at":"2026-07-10T06:07:16Z","close_reason":"Cycle 5 complete: spec PR #132 (b0d8869) + model unfreeze PR #133 (ef9b0c4, additive ReferencedInRequiredBarePattern axis on Node/Edge bindings + ADR 0008 amendment + exported package-level mutator wrappers for cross-package call sites) + resolver-widening PR #134 (edbbb9d, demoteNullableInPlace bare-ref pre-pass; conservative composition — does NOT seed demotedGroups per spec §8.1; 0 golden flips + 4 new fixtures per §8.3-§8.4; zero new sentinels per §8.2) + errata PR #135 (150c373, 24 stage-spec successor rows retired + DoD grep line-number refresh). Linus PASS on all four PRs (spec required 1 round, resolver was PASS zero-blocker/zero-nit). Residual filed as gqlc-0kq (edge-side non-bare OPTIONAL-edge re-reference witness — grammatically-unreachable bare predicate on edges, resolver-internal or additive-model design pending). CAMPAIGN COMPLETE: the model-unfreeze campaign (hk0 / fvo / 0ig / ay9 / 5xg) has discharged every planned bead. Only residuals gqlc-984 (post-ay9 cross-Part carry) and gqlc-0kq (post-5xg edge-side) remain open. No further planned unfreeze work.","external_ref":"https://github.com/areqag/gqlc/issues/181","metadata":null,"dependencies":[{"issue_id":"gqlc-5xg","depends_on_id":"gqlc-0mx.6","type":"blocks","created_at":"2026-07-05T10:34:21Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-ay9","title":"Model unfreeze: OPTIONAL-clause-group axis on Binding (ADR 0008 additive) — R4 flow-typing Class A","description":"Frozen-model gap Class A (R4 spec §7.5.3 items 1+3): the model does not record which bindings were introduced by the same OPTIONAL MATCH clause, so a required-edge witness inside an OPTIONAL group cannot propagate demotion to its OPTIONAL siblings, and the resolver under-demotes (e.g. demote_chained_from_required: p/r1 stay nullable although post demotes). Proposal per spec §7.5.4 Axis 1: additive OptionalGroup axis on Binding (zero-value-safe wire default) — in-protocol under ADR 0008 post-freeze revision, additive only. Capability recovered: OPTIONAL-sibling demotion — any query starting with an OPTIONAL of arity \u003e= 2 chained through one of its members. PROTOCOL: model change lands in its own unfreeze PR (ADR 0008 revision), never rides a resolver-stage PR. On landing, a follow-up refinement widens the R4 demotion rule and updates the demote_chained_from_required golden on that widening PR. Independent of the Class B bead — owner may accept one, both, or neither. Owner decision pending.","notes":"Filed at R4 close-out per merged spec §7.5.5 / §9 DoD item 9. Design detail: docs/specs/resolver-stage-r4.md §7.5.","status":"closed","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-05T14:34:02Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:16Z","started_at":"2026-07-07T18:44:52Z","closed_at":"2026-07-10T06:07:16Z","close_reason":"Cycle 4 complete: spec PR #127 (3540710) + model unfreeze PR #128 (062b840, additive OptionalGroup axis on Node/Edge bindings + ADR 0008 amendment) + resolver-widening PR #129 (c7d2647, demoteNullableInPlace fixed-point group closure per spec §8.1; 2 golden flips + 4 new fixtures per §8.3-§8.4; zero new sentinels per §8.2) + errata PR #130 (11 stage-spec successor rows per §8.6). Linus PASS on both PR #129 and #130. Residual filed as gqlc-984 (resolver-internal cross-Part carry via branchState). Class B (gqlc-5xg) is the sole remaining unfreeze bead.","external_ref":"https://github.com/areqag/gqlc/issues/182","metadata":null,"dependencies":[{"issue_id":"gqlc-ay9","depends_on_id":"gqlc-0mx.6","type":"blocks","created_at":"2026-07-05T10:34:02Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-0mx.6","title":"R4: resolver nullability — flow-typing regimes a+b (ADR 0006)","description":"Regime a: cases flow-typable from the model alone (non-nullable edge implies non-nullable endpoints). Regime b: per-clause structure cases (bare MATCH reusing an OPTIONAL-introduced binding). Design context on gqlc-lqm. Conservative default stays: visibly non-null still emitted nullable until this lands (non-breaking to relax later).","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T20:54:48Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:16Z","started_at":"2026-07-05T07:48:06Z","closed_at":"2026-07-10T06:07:16Z","close_reason":"R4 merged: spec PR #98 (c81954a) + code PR #99 (3b9d7f9). Delivered regime (a) edge-chain demotion (demoteNullable + qualifiedDemoter single-pass lattice), additive always-emit Nullable wire axis on ResolvedNode/ResolvedEdge/ResolvedEdgeUnion, ResolvedProperty disjunction semantics, 14 new valid + 1 new invalid fixtures, 19 rebaselined goldens (exact spec §3.6 list), zero new sentinels, zero changes under internal/query (ADR 0008 preserved). Linus PASS both cycles. Regime (b): cross-WITH → R5; same-Part → two unfreeze follow-up beads per spec §7.5.5 (owner decision, separate PRs).","external_ref":"https://github.com/areqag/gqlc/issues/183","metadata":null,"dependencies":[{"issue_id":"gqlc-0mx.6","depends_on_id":"gqlc-0mx","type":"parent-child","created_at":"2026-07-03T16:54:47Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.6","depends_on_id":"gqlc-0mx.4","type":"blocks","created_at":"2026-07-03T16:54:47Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-0mx.6","depends_on_id":"gqlc-0mx.5","type":"blocks","created_at":"2026-07-03T16:55:02Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":5,"comment_count":0}
{"_type":"issue","id":"gqlc-kux","title":"Stage 14 review follow-ups: dead intra-YIELD check, double-recorded arg refs, stale listener comment","description":"Three non-blocking findings from the Stage 14 thermo-nuclear review (all mutant-test-proven dead or stale; none affect wire or correctness):\n\n1. call.go:139-143 — collectYieldItems intra-YIELD collision check is fully shadowed by the buildPart callByVar backstop (build.go:145-147). Disabling it fails ZERO tests. Pick one authority: delete the collectCall check, or narrow the backstop.\n2. call.go:54-59 — outer ref-append loop double-records every var ref in CALL args; typeExpressionMining -\u003e typeAtom (typing.go:324) already pushes var atoms onto curPart.refs. Disabling it fails ZERO tests (unbound-arg kill-probe still rejects). Delete the outer loop; it contradicts the save-restore convention at expr.go:159-161 / 400-402.\n3. listener.go:82-83 — stale pre-Stage-13 future-tense comment ('Stage 13 will also flip it on EnterOC_Merge once MERGE lands; today MERGE stays behind ErrUnsupportedClause'). MERGE landed (Stage 13) and ErrUnsupportedClause is retired (Stage 14). Rewrite as history.\n\nWhen fixing 1/2, add the missing discriminating pins if a single authority is chosen (so the survivor is mutant-detectable).","status":"closed","priority":3,"issue_type":"chore","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T12:22:45Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:16Z","started_at":"2026-07-10T05:26:16Z","closed_at":"2026-07-10T06:07:16Z","close_reason":"PR #148 merged (Stage 14 dead-code removal: intra-YIELD check + double-recorded arg refs; listener.go comment rewritten as history). Linus PASS with independent kill-probes on both surviving authorities (buildPart callByVar + typeAtom refs).","external_ref":"https://github.com/areqag/gqlc/issues/184","metadata":null,"dependencies":[{"issue_id":"gqlc-kux","depends_on_id":"gqlc-im1","type":"discovered-from","created_at":"2026-07-03T08:22:45Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-6rz","title":"On-disk procsig registry file format","description":"Stage 14 defers the CLI-consumable on-disk format for procedure signatures. Today procsig.Registry is populated in-memory (godog step parses per-scenario declarations; real CLI callers would need to construct signatures from Go code). Design and implement a text format (YAML / JSON / .procsig) that mirrors the TCK's declaration syntax, plus a loader that constructs procsig.Registry from a file path. Follow-up to gqlc-im1.","status":"closed","priority":3,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T11:53:48Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T18:20:04Z","started_at":"2026-07-11T18:03:40Z","closed_at":"2026-07-11T18:20:04Z","close_reason":"Shipped in PR #251. JSON on-disk registry format with strict-decoded loader (Load/Decode), canonical MarshalJSON, and Save round-trip helper. Format defensibly JSON — stdlib only, matches existing schema/query/validated precedent, extensible via omit-if-nil version field. Rejects bare null / null signatures explicitly (would otherwise silently produce the empty registry). Loader delegates semantic checks (unqualified name, duplicate column, unknown token) to existing NewRegistry so file surface and in-memory API stay lockstep. Follow-up gqlc-aqb filed for wiring into a real CLI.","external_ref":"https://github.com/areqag/gqlc/issues/185","dependencies":[{"issue_id":"gqlc-6rz","depends_on_id":"gqlc-im1","type":"blocks","created_at":"2026-07-03T07:53:51Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-jzz","title":"Stage 13 spec §4.3: correct list-nested param prediction (typeAtom approves; PropertyUse recorded)","description":"Reviewer SF2 from Stage 13 thermo-nuclear review: spec §4.3 lines ~903-907 predict MERGE (a {tags: [$t, $u]}) is caught by requireAllParametersApproved as ErrUnsupportedParameter. Reality: typeAtom inside the list literal marks each param approved (typing.go:335), so the widening records PropertyUse{Ref{a, tags}} on both params. Wire behavior is defensible (Use points at the right property slot, wrong nesting level; resolver reconciles) but the spec prediction is wrong. Fix: rewrite §4.3's prediction to document actual behavior and the nesting-level caveat.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T09:21:58Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:16Z","started_at":"2026-07-10T05:26:18Z","closed_at":"2026-07-10T06:07:16Z","close_reason":"PR #142 merged (Stage 13 §4.3 corrected: list-nested params approved by typeAtom, PropertyUse{Ref{var,key}} recorded). Linus PASS with 1 NIT (expr.go:610-611 comment drift — filed as follow-up).","external_ref":"https://github.com/areqag/gqlc/issues/186","metadata":null,"dependencies":[{"issue_id":"gqlc-jzz","depends_on_id":"gqlc-cqh","type":"discovered-from","created_at":"2026-07-03T05:21:58Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-11c","title":"Stage 13 spec §8: retire stale posture-1 paragraph (contradicts §4.3 and shipped behavior)","description":"Reviewer SF1 from Stage 13 thermo-nuclear review: spec §4.3 (lines ~875-900) describes posture 2 (record PropertyUse for params in rich inline-map values), and the implementation ships posture 2. But §8 (~line 1118) still closes with 'The current draft chooses posture 1' (defer to ExprUse via Stage 6 typer). Doc-vs-doc contradiction; §4.3 and the code win. Fix: rewrite the §8 paragraph to match shipped posture 2.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T09:21:46Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:17Z","started_at":"2026-07-10T05:26:19Z","closed_at":"2026-07-10T06:07:17Z","close_reason":"PRs #139/#140/#143 merged; Linus PASS on each.","external_ref":"https://github.com/areqag/gqlc/issues/187","metadata":null,"dependencies":[{"issue_id":"gqlc-11c","depends_on_id":"gqlc-cqh","type":"discovered-from","created_at":"2026-07-03T05:21:46Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-mpn","title":"docs(spec): Stage 12 spec doc-parity follow-ups from reviewer confirmation pass","description":"Two doc-only wire-shape gaps in docs/specs/cypher-query-parser-stage-12.md found during the SF-delta confirmation pass (PR #67, reviewer CONFIRM verdict). Code truth is correct in both; the spec lags.\n\nGap A — §1.3 anonymous-node semantics: add one sentence naming the emergent `variables: null` outcome for anonymous-only CREATE clauses (e.g. CREATE ({prop: \u003cliteral\u003e}) — anonymous nodes never enter curPart.bindings, so the [before..len(bindings)] delta walk yields an empty slice, marshalled as null). Goldens Temporal4_06cc612fd9c1 and Create1_2edce65e88dc confirm. Without the sentence, future readers may misread variables:null as a silent-drop bug.\n\nGap B — §8 SF6 disclosure golden-count phrasing: correct at 2aacf3c (~136 UNLOCK + ~24 re-keyed ≈ ~160) but SF3 (756da6e) later minted +71 more (53 create/delete + 18 Temporal4). One-line update: total Stage-12 new goldens ≈ 231; 3061 on disk at branch HEAD.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T05:02:43Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:17Z","started_at":"2026-07-10T05:26:17Z","closed_at":"2026-07-10T06:07:17Z","close_reason":"PR #141 merged (Stage 12 doc-parity: §1.3 anon-only CREATE + §8 SF6 golden-count). Linus PASS.","external_ref":"https://github.com/areqag/gqlc/issues/188","metadata":null,"dependencies":[{"issue_id":"gqlc-mpn","depends_on_id":"gqlc-mkv","type":"discovered-from","created_at":"2026-07-03T01:02:43Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-33k.4","title":"Enrich CreateEffect to distinguish created vs pre-bound pattern variables","description":"Stage 12 round-1 reviewer finding SF4: MATCH (n) CREATE (n) parses via mergeBinding, and CreateEffect.Variables comes out empty because the bindings slice doesn't grow (walk-order before..after delta is zero). Spec corrected to code truth (records only newly-created bindings). If a consumer ever needs 'which variables did this CREATE clause touch' including pre-bound ones (e.g. for typed-Create codegen over mixed MATCH+CREATE), CreateEffect needs a pre-merge variable list or a per-element created/reused axis. Blocked conceptually on a real consumer need — do not build speculatively.","notes":"Re-parented from closed epic gqlc-33k to the resolver/codegen umbrella at the freeze (2026-07-03): explicitly consumer-driven (do not build speculatively) — pick up only when a consumer demonstrates the need. ADR 0008 names it a known deferred addition under the additive revision protocol.","status":"open","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T04:37:40Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:45:53Z","external_ref":"https://github.com/areqag/gqlc/issues/189","dependencies":[{"issue_id":"gqlc-33k.4","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T01:39:38Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-33k.3","title":"Type params in EXISTS RegularQuery LIMIT/SKIP as clause-slot Uses","description":"Stage 11 round-2 reviewer caveat (S5): a $param inside an EXISTS { RegularQuery-form } LIMIT or SKIP records the blanket ExprUse{TypeBool, ExprInPredicate} minted by EnterOC_ExistentialSubquery's parameter sweep, rather than the precise clause-slot Use (int, Limit/Skip). Recorded, not dropped — sound but imprecise. Cause: inner LIMIT/SKIP handlers early-return under subqueryDepth\u003e0, so the subquery sweep is the only mining path. Fix direction: let the sweep classify params by enclosing slot, or narrowly re-enable slot handlers inside subqueries. Add spec §8 note when addressed.","notes":"Re-parented from closed epic gqlc-33k to the resolver/codegen umbrella at the freeze (2026-07-03): post-freeze Use-precision refinement, consumer-facing. ADR 0008 names it a known deferred addition under the additive revision protocol.\n\nAutonomous agent (2026-07-11 orchestrator) claimed and stalled during golangci-lint verify with no committed work — worktree reset --hard wiped any WIP. Unclaimed for future pickup. When picking up: agent didn't leave artefacts, so start fresh from the bead brief. Grep entry points: EnterOC_ExistentialSubquery, subqueryDepth, LIMIT/SKIP handlers in internal/resolver/ and parser. Recommended fix direction (per prior orchestrator's Carmack call): approach (1) — classify params by enclosing slot inside the sweep — centralises Use-classification.","status":"closed","priority":3,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-03T00:21:46Z","created_by":"antranig-yeretzian","updated_at":"2026-07-11T18:43:51Z","started_at":"2026-07-11T18:04:06Z","closed_at":"2026-07-11T18:43:51Z","close_reason":"Shipped in PR #254 (feat(resolver): precise clause-slot Uses for params in EXISTS LIMIT/SKIP). Approach 1 from bead brief: classify params by enclosing slot inside the sweep, centralising Use-classification. Post-freeze Use-precision refinement under ADR 0008 additive revision protocol. Fixtures parameter_exists_body_clause_slot_{limit,skip}.cypher pin (int, Limit)/(int, Skip) emission. Spec §8 addendum + ADR 0008 update. Recovered from stalled agent by orchestrator.","external_ref":"https://github.com/areqag/gqlc/issues/190","dependencies":[{"issue_id":"gqlc-33k.3","depends_on_id":"gqlc-8i0","type":"parent-child","created_at":"2026-07-10T01:39:37Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-e34","title":"Split dev setup out of README.md into CONTRIBUTING.md","description":"README.md currently mixes project description with dev/contributor setup instructions (just init, hook explanation, just recipe list). Move the 'Setup after cloning' and 'Development' sections into a new CONTRIBUTING.md, leaving README.md focused on what the project is, with a pointer link to CONTRIBUTING.md for contributors.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T23:23:12Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:17Z","started_at":"2026-07-02T23:23:17Z","closed_at":"2026-07-10T06:07:17Z","close_reason":"Merged via PR #63 (docs-contributing-md -\u003e master), squash commit 8352903","external_ref":"https://github.com/areqag/gqlc/issues/191","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7ur","title":"ErrVariableKindConflict message text stale for path/unwind conflicts","description":"errors.go:41 message string is 'variable used as both node and edge', but Stage 9 reuses the sentinel for path-vs-unwind and unwind-vs-unwind kind conflicts (and Stage 8 for path conflicts). errors.Is classification is correct; only the human-readable text misleads. Reword to cover all kind pairs or parameterize with the two kinds. Found in Stage 9 re-review (non-blocking note).","notes":"Detached from epic gqlc-33k at its close (2026-07-03): small standalone parser polish (stale sentinel message text), not freeze-blocking — the message wording is not part of the frozen contract (sentinel identity is, text is not).","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T22:36:57Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:17Z","started_at":"2026-07-10T05:24:04Z","closed_at":"2026-07-10T06:07:17Z","close_reason":"PR #138 merged: ErrVariableKindConflict message updated for path/unwind/CALL cases. Linus PASS. Symbol identity preserved (ADR 0008).","external_ref":"https://github.com/areqag/gqlc/issues/192","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-c5f","title":"Dialect extension: shortestPath()/allShortestPaths() (Neo4j, outside openCypher)","description":"Stage 8 review finding: MATCH p = shortestPath((a)-[*]-\u003e(b)) hits an ANTLR 'no viable alternative' parse error — the vendored openCypher grammar (internal/grammar/cypher) appears not to implement shortestPath/allShortestPaths, though openCypher considers them valid. Below the model layer entirely. Needed before the freeze claim of full pattern support; likely grammar regeneration or upstream grammar version bump, then model decision (path-typed function-like pattern). Parent epic gqlc-33k.","notes":"PREMISE CORRECTED at freeze (2026-07-03): this is NOT a grammar bug. shortestPath/allShortestPaths are Neo4j dialect extensions — zero rules in the official openCypher grammar (verified against internal/grammar/cypher/Cypher.g4) and zero occurrences in the entire vendored TCK. The freeze (ADR 0008) records the posture: out of frozen scope, post-freeze additive feature under the revision protocol — expected shape is a selector axis on PathBinding, the same move EdgeHops made on EdgeBinding. Requires: grammar extension (dialect layer decision), model axis, parser lowering. De-prioritized to P3, detached from closed epic gqlc-33k.","status":"closed","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T13:04:00Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:45:56Z","closed_at":"2026-07-10T05:19:39Z","close_reason":"Won't do (owner decision, 2026-07-10). shortestPath()/allShortestPaths() are Neo4j dialect extensions with zero rules in the official openCypher grammar (verified against internal/grammar/cypher/Cypher.g4) and zero occurrences in the vendored TCK. The freeze (ADR 0008) already recorded them as out-of-scope; the owner has elected not to add the dialect layer / model axis / parser lowering that would be needed. Not superseded — dropped entirely. Reopen if a future consumer requires Neo4j-dialect parity.","external_ref":"https://github.com/areqag/gqlc/issues/193","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-cmp","title":"Add Layer-2 mustParse pin for WITH-aggregate-WHERE scope snapshot (foaf shape)","description":"Reviewer suggestion (Stage 6, non-blocking): With7 [2] scope-leak regression (mineWhere refs snapshot/restore at expr.go) is guarded only by the godog corpus. Add a mustParse pin for 'MATCH (a) WITH a, count(*) AS c WHERE c \u003e 1 RETURN a' to lock the snapshot/restore against future refactors at Layer 2.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T10:27:36Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:18Z","started_at":"2026-07-10T05:26:20Z","closed_at":"2026-07-10T06:07:18Z","close_reason":"PR #147 merged (Layer-2 mustParse pin: WITH-aggregate-WHERE 'foaf' scope-snapshot from With7[2]). Linus PASS + kill-probe verified (removing expr.go:452 restore triggers ErrUnboundVariable).","external_ref":"https://github.com/areqag/gqlc/issues/194","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-j6c","title":"Narrow bucket-3 whitelist for InvalidNumberLiteral/InvalidUnicode* details","description":"Reviewer finding (Stage 6, non-blocking): InvalidNumberLiteral / InvalidUnicodeCharacter / InvalidUnicodeLiteral in the acceptance_test.go bucket-3 detail whitelist cover a mixed set — some TCK scenarios are value-level (out-of-range ints, \\uH in lexed string), others parse-shape (0x with no digits, em-dash operator). Safe today (parser rejects the parse-shape subset; 0 fails) but reduces regression sensitivity to a future lexer bug labeled with those details. Narrow to the value-out-of-range subset.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T10:27:35Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:17Z","started_at":"2026-07-10T05:26:21Z","closed_at":"2026-07-10T06:07:17Z","close_reason":"PR #145 merged (bucket-3 whitelist narrowed: removed InvalidNumberLiteral/InvalidUnicode*). Linus PASS. Zero TCK count shift; strict tightening of accept-and-defer surface.","external_ref":"https://github.com/areqag/gqlc/issues/195","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-5z8","title":"Detect dead git-hook activation (core.hooksPath drift)","description":"Discovered 2026-07-02: core.hooksPath in .git/config pointed at the empty .git/hooks (absolute path), silently disabling the .githooks pre-commit/pre-push gates for an unknown period — every local commit/push since ran ungated; CI caught nothing because it is the authoritative gate, but the local budget-split design (README) was dead. Origin of the override unknown (bd doctor silent on hooks; global config clean). Fixed by re-running 'just init' and verified via the hook's BRANCH_OVERRIDE test seam. Needs a cheap drift detector that runs where developers already are, since CI cannot see local git config — e.g., a warning in a frequently-run just recipe (test/lint) when core.hooksPath != .githooks, or a 'just doctor' recipe. Keep it sub-second per the hook budget philosophy.","status":"closed","priority":3,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T03:44:51Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:18Z","started_at":"2026-07-10T05:26:22Z","closed_at":"2026-07-10T06:07:18Z","close_reason":"PRs #139/#140/#143 merged; Linus PASS on each.","external_ref":"https://github.com/areqag/gqlc/issues/196","metadata":null,"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-gyw","title":"Resolver: implicit grouping-key semantics for aggregate projections","description":"Stage 3 (gqlc-npg) carries AggregateProjection with only its []Ref payload + the aggregate kind, deliberately NOT modelling implicit grouping (which non-aggregate RETURN columns form the group key). Per spec §9 (cypher-query-parser-stage-3.md) this is a resolver/Stage-4 concern entangled with WITH, and there is a real risk the []Ref-only payload proves insufficient and AggregateProjection must be reopened. This issue tracks adding grouping-key resolution once the model is frozen (gqlc-cta) and WITH lands (Stage 4, gqlc-v23).","notes":"Linked to resolver stage R5 (gqlc-0mx.7) at the freeze (2026-07-03): grouping-key semantics are R5's scope; this bead carries the recorded contract (resolver-side re-parse; ContainsAggregate only as ADR-0008 escape hatch).","status":"closed","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-06-04T02:00:56Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:45:59Z","closed_at":"2026-07-05T23:09:09Z","close_reason":"Superseded by R5 spec B7 resolution (PR #101). The contract recorded here (resolver-side re-parse of ExprProjection text to detect nested aggregates) was retracted: the frozen model drops nested-aggregate structure at classification (classifyRichExpression returns plain ExprProjection; recovered text re-parses opaque, so re-parse cannot recover the aggregate bit). R5 landed the uniform-exclude posture instead: ExprProjection items are NEVER grouping keys; result-set effect is under-grouping when a Part mixes AggregateProjection with ExprProjection. Parser pin 'count in arithmetic' (parser_test.go:1319) is the explicit deferral lock. Escape hatch: Shape B ContainsAggregate model-unfreeze follow-up bead (parser-side bit set by classifyRichExpression during its own walk) filed for owner decision per ADR 0008 revision protocol.","external_ref":"https://github.com/areqag/gqlc/issues/197","dependencies":[{"issue_id":"gqlc-gyw","depends_on_id":"gqlc-0mx.7","type":"parent-child","created_at":"2026-07-03T16:55:21Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-gyw","depends_on_id":"gqlc-cta","type":"blocks","created_at":"2026-06-03T22:01:03Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-gyw","depends_on_id":"gqlc-v23","type":"blocks","created_at":"2026-06-03T22:01:04Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":2,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-lqm","title":"Resolver: flow-type nullable bindings across clause structure","description":"Demote a binding's effective Nullable flag in the validated query when downstream clauses prove the binding must exist on every surviving row. Two regimes with different model dependencies:\n\n(a) Edge-chain demotion (no new model structure beyond Stage 2 needed) — a non-nullable edge implies its endpoints exist, so chain 'non-nullable edge → non-nullable endpoints → demote those endpoints' nullability'. Handles the common case: OPTIONAL MATCH (a)-[:R]-\u003e(b) MATCH (b)-[:S]-\u003e(c) RETURN a, b, c — both a and b demote.\n\n(b) Bare-pattern demotion (requires Stage 4 clause structure) — a required clause that reuses an OPTIONAL-introduced binding as a bare node/edge pattern filters NULL-rows. E.g. OPTIONAL MATCH (a)-[:R]-\u003e(b) MATCH (b) RETURN b. The model needs per-clause structure to record the second reference; Stage 4 (WITH/UNION) introduces that structure.\n\nPer ADR 0006: the parser does not do this; the resolver does. (a) is shippable on its own after the freeze + resolver exists; (b) additionally waits on Stage 4.","notes":"Linked to resolver stage R4 (gqlc-0mx.6) at the freeze (2026-07-03): flow-typing regimes a+b are R4's scope; this bead carries the ADR 0006 design context.","status":"closed","priority":3,"issue_type":"feature","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T22:51:44Z","created_by":"antranig-yeretzian","updated_at":"2026-07-05T14:33:44Z","closed_at":"2026-07-05T14:33:44Z","close_reason":"Design captured and superseded by merged R4 spec (docs/specs/resolver-stage-r4.md, PR #98). Regime (a) shipped in R4 code (PR #99, 3b9d7f9). Regime (b) split per spec §7.4: cross-WITH → R5 (gqlc-0mx.7) admission-gate scope, no model change needed; same-Part → blocked on two independent frozen-model gaps (spec §7.5.3 Class A / Class B), tracked by the two unfreeze beads filed at R4 close-out. GH #21 stays open for the regime-(b) remainder. Forced past the open umbrella epic gqlc-0mx (open until R7 by design).","external_ref":"https://github.com/areqag/gqlc/issues/21","dependencies":[{"issue_id":"gqlc-lqm","depends_on_id":"gqlc-0mx","type":"blocks","created_at":"2026-05-26T18:51:52Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-lqm","depends_on_id":"gqlc-0mx.6","type":"parent-child","created_at":"2026-07-03T16:55:20Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-lqm","depends_on_id":"gqlc-cta","type":"blocks","created_at":"2026-05-26T18:51:50Z","created_by":"antranig-yeretzian","metadata":"{}"},{"issue_id":"gqlc-lqm","depends_on_id":"gqlc-v23","type":"blocks","created_at":"2026-05-26T18:51:54Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":3,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-f1d","title":"docs: Stage 1 spec §6 — make 5→3 arithmetic explicit (Metz cycle-5 non-blocking note)","description":"In docs/specs/cypher-query-parser-stage-1.md §6 Layer-2 coverage gaps, the opening claim says ErrUnsupportedParameter is reachable by 5 fail-sites and the list shows 3 remaining uncovered. The intermediate isn't shown: 5 = 1 (cycle-introduced AUTHORED pin) + 1 (whole-map, corpus-covered by Match1[6] in mustReject) + 3 (uncovered). A reader counting 5-1=4 vs 3 bullets pauses unnecessarily. Insert one sentence between 'five distinct fail-sites' and 'the remaining three' that names Match1[6]'s coverage explicitly. Metz's exact suggested phrasing is in her cycle-5 diff review.","status":"closed","priority":3,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T06:31:12Z","created_by":"antranig-yeretzian","updated_at":"2026-07-02T03:29:45Z","started_at":"2026-07-02T03:16:54Z","closed_at":"2026-07-02T03:29:45Z","close_reason":"PR #35 merged; GH #22 auto-closed","external_ref":"https://github.com/areqag/gqlc/issues/22","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-7z6","title":"Stage 5 — undirected patterns","description":"Support (a)-[r]-(b) patterns matching in either direction. Edge binding gains a direction-agnostic marker; resolution tries both orientations against the directed schema. May end up N/A if the schema stays directed-only — re-evaluate when reached. Per ADR 0004 §Stages.","status":"closed","priority":3,"issue_type":"feature","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-05-26T04:26:08Z","created_by":"antranig-yeretzian","updated_at":"2026-06-04T03:30:00Z","started_at":"2026-06-04T02:54:46Z","closed_at":"2026-06-04T03:30:00Z","close_reason":"Stage 5 complete: undirected relationship patterns (a)-[r]-(b) and (a)\u003c-[r]-\u003e(b) now parse. EdgeBinding gains a positive 'directed' marker (field+Directed() accessor+trailing constructor param+always-emitted JSON key); listener uses an inverted zero-value-safe 'undirected' raw field, polarity flip at toBinding. collectEdge stops rejecting left==right; directed edges canonicalise, undirected keep textual order (no flip; \u003c--\u003e guarded). godog read-core 371/179 -\u003e 380/170 (+9 passed). Linus gate APPROVED. Branch stage-5-undirected pushed to origin (commits dae7fb3 prep, b474fbe model, d4fbedd unlock). Not N/A: parser is schema-agnostic, marker is load-bearing (undirected yields no single EdgeKey, resolver forms two candidate keys).","external_ref":"https://github.com/areqag/gqlc/issues/23","dependencies":[{"issue_id":"gqlc-7z6","depends_on_id":"gqlc-4n2","type":"blocks","created_at":"2026-05-26T00:26:11Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":2,"comment_count":0}
{"_type":"issue","id":"gqlc-o7s3","title":"test/data/codegen: two package doc comments for package fixtures","description":"`include.go` and `live_test.go` both carry a doc comment on `package fixtures` in the nested `test/data/codegen` module. godoc picks one arbitrarily, so which one documents the package is undefined. Pre-existing; surfaced during gqlc-35yu.4. Pick the one that belongs and demote the other to a plain comment.","status":"open","priority":4,"issue_type":"bug","owner":"antranig.yeretzian@proton.me","created_at":"2026-08-01T03:37:56Z","created_by":"areqag","updated_at":"2026-08-01T04:12:23Z","external_ref":"https://github.com/areqag/gqlc/issues/647","dependencies":[{"issue_id":"gqlc-o7s3","depends_on_id":"gqlc-rohp","type":"blocks","created_at":"2026-08-01T17:24:36Z","created_by":"areqag","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.35","title":"schema/gql: the empty edge endpoint () has two readings and one diagnostic","description":"`(:Person) -[:AUTHORED]-\u003e ()` reaches sourceNodeTypeReference /\ndestinationNodeTypeReference alternative 2 with the nodeTypeFiller elided\n(GQL.g4:1620, 1625). It is pinned by\n18.3-edge-type/endpoint_no_filler.gql, which reports ErrUnknownEndpoint —\n\"edge endpoint references an undeclared node type\".\n\nThat message assumes one of two readings, and gqlc has never chosen\nbetween them in writing:\n\n1. `()` names the node type whose key label set is empty. resolve()\n   (resolve.go:217-223) takes `ref.labels.Key()`, gets the empty key, and\n   misses idx.types. Under this reading the message is true but weak: no\n   such type can be declared at all, ADR 0018 rejecting every declaration\n   of one with ErrUnnamedNodeType, so the endpoint is *undeclarable*\n   rather than undeclared and an author told to declare it will fail.\n\n2. `()` is an unconstrained endpoint — \"any node type\". Under this\n   reading the message is simply wrong: the endpoint references no type,\n   let alone an undeclared one.\n\nThe decline is not in question either way. Schema.Edges is keyed on a\nsource/label/target triple (schema.go) with no wildcard for either end,\nso reading 2 is unrepresentable in gqlc's model just as reading 1 is\nundeclarable in it. What turns on the answer is only which sentinel and\nmessage the author gets.\n\nSettling reading 1 vs 2 needs the Syntax Rules for \u003cedge type\nspecification\u003e, which are in the paid ISO/IEC 39075 PDF that gqlc-lir\ndeclined to buy — so this may end up recorded as unsettled rather than\nanswered, in the form ADR 0018 uses for the identity question.\n\nScope if taken: either keep ErrUnknownEndpoint and say in an ADR why it\nis the right report under both readings, or add a sentinel naming the\nempty endpoint the way gqlc-h9n.12's and gqlc-h9n.6's leaves name what\nthey decline. Not blocked.","status":"closed","priority":4,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-28T07:10:50Z","created_by":"areqag","updated_at":"2026-07-28T17:36:31Z","started_at":"2026-07-28T17:30:16Z","closed_at":"2026-07-28T17:36:31Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/476","dependencies":[{"issue_id":"gqlc-h9n.35","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-28T03:10:50Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.27","title":"grammar: graphTypeLikeGraph and LIKE are declared under // 12.4 but belong to 12.6","description":"Found 2026-07-26 while building the gqlc-h9n.17 orphan detector, which derives a grammar name's owning corpus area from the ISO clause heading it is declared under in GQL.g4.\n\n`graphTypeLikeGraph` and the `LIKE` token sit under the `// 12.4 \u003cinsert graph statement\u003e` heading, apparently for typesetting reasons — they are semantically part of 12.6. Section 12.4 has no corpus directory, so the detector computes an owning-area set that no file can ever satisfy, and both names have to be carried as exemptions.\n\nh9n.17 landed option (b): keep the declarations where they are and carry an exemption with a `why:` reason. That is correct and low-risk — moving declarations in a generated-adjacent grammar file to satisfy a test is the tail wagging the dog.\n\nOption (a) is to move the two declarations under the 12.6 heading, which would delete two exemptions and make the ownership map exactly reflect semantics. Worth doing only if GQL.g4 is being edited in that region anyway. The risk is nonzero: the file is a community ANTLR adaptation (see gqlc-h9n.11) and reordering declarations can perturb ANTLR alternative ordering, which this project has already been bitten by — see the bd memory `antlr-alt-order-unreachable-alternatives`. Any move must be accompanied by a diff of the generated parser, not just a green test run.\n\nP4: pure ergonomics, two exemption lines. Do not do it standalone.","notes":"SCOPE BROADENED 2026-07-26 by team-lead, prompted by rev-h9n17's review of #419.\n\nOriginally titled for graphTypeLikeGraph/LIKE, which are declared under a // 12.4 comment but belong to clause 18. PR #419's exemption list then used this bead to excuse three further names — isOrColon, IS, COLON — which are the same defect but for clause 16.7, not 12.4.\n\nLeft as-is, closing this bead by moving only the 12.4 declarations would strand those three exemption entries pointing at a closed bead, and a stale exemption reference is exactly the rot the exemption metadata guards exist to prevent. So the bead is now the general class: any rule whose enclosing section comment attributes it to a clause that does not own it, which matters because the corpus ownership map is derived FROM those section comments.\n\nClosing this bead requires sweeping GQL.g4 for the whole class, not just the two names originally cited, and retiring every exemption that cites gqlc-h9n.27 (currently 5 in corpus_carriers_test.go: graphTypeLikeGraph, LIKE, isOrColon, IS, COLON).","status":"open","priority":4,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-26T07:17:55Z","created_by":"areqag","updated_at":"2026-07-26T17:04:45Z","external_ref":"https://github.com/areqag/gqlc/issues/416","metadata":null,"dependencies":[{"issue_id":"gqlc-h9n.27","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-26T03:17:54Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-h9n.14","title":"schema/gql: mechanically derive the unreachable-alternative partition","description":"gqlc-h9n.2 pins 48 grammar alternatives that rule and token coverage provably cannot compel, and discharges `48 minus a checked-in exemption list`. Exemptions exist because some alternatives are not merely invisible but **unreachable** — ALL(*) hands every input that would take them to a lower-numbered sibling, so no valid GQL file can reach them. First confirmed member: `connectorUndirected#1`, stolen by `connectorPointingRight#1` (see gqlc-h9n.10 item 4).\n\nThe list is populated by hand today. linus proposed deriving it from `PredictionModeLLExactAmbigDetection` plus a `ReportAmbiguity` listener. team-lead **rejected that for gqlc-h9n.2 on soundness, not cost** (amendment A10), and the reasoning is the substance of this bead:\n\nThe mechanism does detect the case — the report fires under SLL with `exact=false` and under full LL with `exact=true`. But an AMBIG report establishes only *\"these alternatives were ambiguous on THIS input at THIS position.\"* An exemption asserts *\"EVERY input that would take alternative N is ambiguous with a lower-numbered alternative.\"* The gap between the two is input enumeration. A mechanism that resembles a proof of unreachability without being one is worse than a hand-list with a bead attached, because the next reader will trust it.\n\nSo this bead is not \"wire up ReportAmbiguity\". It is: is there a **sound** derivation, and is it worth having? Candidates worth evaluating before writing code — direct ATN reachability analysis over the generated serialised ATN (which is static and does not need input enumeration); grammar-level analysis with an external tool; or accepting that the hand-list plus its `stolenBy` witness is the right engineering answer and closing this as wont-fix with that reasoning recorded.\n\nNote the current design already fails closed without any derivation: the exemption list is asserted bidirectionally against the actually-uncovered set, so an exemption that becomes covered turns the gate red and must be deleted. This bead buys confidence that the list is *complete*, not that it is *honest* — it is already honest.","acceptance_criteria":"A decision is recorded on whether a sound mechanical derivation of unreachability exists, with the ATN-reachability option evaluated explicitly rather than assumed impossible.\nClosing this as wont-fix is an acceptable outcome provided the reasoning is written down, since the hand-list already fails closed.\nIf a derivation lands, it must not weaken the existing bidirectional exemption assertion, and must distinguish \"unreachable\" from \"ambiguous on some input\".","notes":"DECISION 2026-07-28: wont-fix. No sound and complete derivation exists, and the two candidate mechanisms sit on the wrong side of the bound rather than near it. Reasoning is now in the alternativeExemption doc comment (internal/schema/gql/corpus_grammar_test.go), where the next person to read the register will meet it, with a cross-reference from optionalityExemption in corpus_branch_test.go.\n\n1. UNDECIDABILITY. An exemption is a universal claim: every input that would take alternative N is taken by another alternative. Deciding it reduces to CFL inclusion. Take two grammars in Greibach normal form -- GNF is left-recursion-free, so ANTLR accepts it, which closes the obvious objection that arbitrary CFGs have indirect left recursion ANTLR rejects -- and make them alternatives 1 and 2 of a rule anchored at EOF. Full-context prediction must simulate to EOF to disambiguate, so it selects alternative 2 on exactly the inputs in L2 \\ L1, and alternative 2 is dead iff L2 is contained in L1. CFL inclusion is undecidable, so this is.\n\n2. ATN REACHABILITY, evaluated explicitly as the acceptance criteria require. It is vacuous, not weak. In the ATN an alternative's entry state is an epsilon successor of its decision state, so it is reachable exactly when its rule is; connectorUndirected is in wantObligationRules, so a graph walk reports connectorUndirected#1 reachable. The analysis answers \"is this alternative in the grammar\", which was never the question. Secondary cost finding, recorded for whoever revisits: the Go runtime does not export the graph anyway -- Transition.getTarget, ATN.states and RuleTransition.followState are all unexported in antlr4-go v4.13.1 -- so even the vacuous version needs reflection or a fork.\n\n3. ReportAmbiguity under PredictionModeLLExactAmbigDetection. Amendment A10 rejected it on the input-enumeration gap. Item 1 is why that gap cannot be closed: closing it IS deciding CFL inclusion, so this is not a mechanism that needs more engineering.\n\n4. WHAT IS AVAILABLE is sound-but-incomplete: prove deadness for particular shapes. That is the wrong half. The bead itself notes the list is already honest and that what a derivation would buy is completeness -- which is precisely the undecidable direction.\n\n5. THE PROCEDURE CONVERGES WITHOUT ONE, and there is now evidence rather than an argument. Over-claiming already fails closed (an exemption the corpus covers turns TestAlternativeExemptions red). Under-claiming announces itself as a coverage demand no file satisfies, and authoringGuidance routes the author who hits it to read the grammar instead of widening the harness. gqlc-h9n.32 found the second dead branch by exactly that route, after this bead was filed: an author wrote ANY VALUE\u003cA|B\u003e, watched it keep landing on the branch already covered, and the grammar argument followed. Two findings, both from authoring, none missed.\n\nNote for a future reader: the two registers rest on DIFFERENT ALL(*) behaviours. alternativeExemptions' entry is ordered choice (a lower-numbered alternative wins); optionalityExemptions' is left-recursion rewriting (the higher-numbered valueType#10 wins, because ANTLR turns direct left recursion into a loop that consumes the operator first). A derivation would have to cover both, which is a further reason to doubt the one-mechanism framing the original proposal had.","status":"closed","priority":4,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-25T11:51:07Z","created_by":"areqag","updated_at":"2026-07-28T07:35:40Z","started_at":"2026-07-28T07:28:10Z","closed_at":"2026-07-28T07:35:40Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/380","dependencies":[{"issue_id":"gqlc-h9n.14","depends_on_id":"gqlc-h9n","type":"parent-child","created_at":"2026-07-25T07:51:06Z","created_by":"areqag","metadata":"{}"}],"dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-we8","title":"chore(lint): golangci-lint cache clean recipe or auto-clean for phantom deleted-worktree issues","description":"When a git worktree is deleted (e.g., \\`../wt/gqlc-kww\\`), golangci-lint's on-disk cache retains issues indexed by the deleted worktree path. Subsequent \\`just lint\\` invocations report phantom issues in nonexistent paths — 5 phantoms were observed during gqlc-71n implementation. Manual \\`golangci-lint cache clean\\` fixes it.\n\nOptions:\n1. Add \\`just lint-clean\\` recipe that runs \\`golangci-lint cache clean\\` (simple, opt-in).\n2. Detect stale cache entries in \\`just lint\\` and auto-clean when detected (harder — requires diffing cache index against existing worktrees).\n3. Add a note to \\`just doctor\\` warning about stale cache when \\`ls ../wt/\\` shows deleted paths that appear in the cache.\n\nLandmine flagged by linus during gqlc-71n review, 2026-07-18. Not blocking that PR.","status":"closed","priority":4,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T14:23:28Z","created_by":"antranig-yeretzian","updated_at":"2026-07-26T06:50:25Z","closed_at":"2026-07-26T06:50:25Z","close_reason":"Subsumed by PR #396 and closed with gqlc-6rv. we8 proposed one of {a lint-clean recipe, auto-detect in lint, a doctor warning} — all three are reactions to a cache poisoned by a removed worktree. The per-worktree cache eliminates the poisoning source, so there is nothing left to detect, clean or warn about. Reviewer independently confirmed the subsumption is genuine rather than a convenient closure.","external_ref":"https://github.com/areqag/gqlc/issues/332","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-buk","title":"chore(docs): update codegen-live doc strings for two-arm reality","description":"After gqlc-5gc lands, two doc strings still describe the codegen-live\nharness as single-driver (v5 only):\n\n1. justfile:91 — \"runs the codegen live-smoke test against a real neo4j\n   **v5** testcontainer\" — stale singular claim. Should mention both v5\n   and v6 arms.\n2. .github/workflows/codegen-live.yml:1-4 header — \"boots a neo4j\n   v5-community testcontainer and drives the generated repositories\n   against it (bd gqlc-73h)\" — same singular claim. Update to reflect\n   both v5 and v6 arms.\n\nBoth are string-only edits, no logic changes. Kept out of the gqlc-5gc\nPR to preserve its \"test-code only, no CI/config drift\" hygiene.\n\nFlagged by linus during the gqlc-5gc combined final review as\nout-of-scope-but-worth-fixing. Can be folded opportunistically into\ngqlc-9u5 (cache PR touches justfile:98 + .github/workflows/codegen-\nlive.yml) or gqlc-rvw (blocking PR also touches the workflow file), or\naddressed as its own tiny PR.","status":"closed","priority":4,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-18T04:47:05Z","created_by":"antranig-yeretzian","updated_at":"2026-07-18T05:37:04Z","closed_at":"2026-07-18T05:37:04Z","close_reason":"Shipped in PR #322 (folded with gqlc-9u5). Stale singular-v5 claims in justfile:91 docstring and .github/workflows/codegen-live.yml header rewritten to name both v5+v6 arms; historical bd gqlc-73h reference preserved, bd gqlc-5gc credited for v6 arm addition.","external_ref":"https://github.com/areqag/gqlc/issues/319","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-243","title":"config spec §6.3: catalogue the explicitly-tagged garbage-int variant or hedge completeness","description":"Residual from the Linus PASS on PR #266 (gqlc-8gp): 'version: !!int abc' reaches the typed decode and yields 'config: \u003csrc\u003e: field \"version\": yaml: cannot decode !!str `abc` as a !!int' — the overflow row's wrapping but yaml's 'cannot decode' text, no 'unmarshal errors:' block and no line info. One variant still sits outside §6.3's claim to list every loader message. Fix options: generalize the overflow row's title to cover both yaml-wrapped int-decode failures (add the second example + a pinned test), or hedge the completeness claim with 'yaml.v3's message'. Ultra-pathological input; pure doc/test polish, no code change expected.","status":"closed","priority":4,"issue_type":"task","assignee":"areqag","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T14:19:11Z","created_by":"antranig-yeretzian","updated_at":"2026-07-30T02:45:49Z","started_at":"2026-07-30T02:29:37Z","closed_at":"2026-07-30T02:45:49Z","close_reason":"Closed","external_ref":"https://github.com/areqag/gqlc/issues/301","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-8gp","title":"internal/config polish: line info on version-probe errors + §6.3 catalogue completeness","description":"Two non-blocking NITs from the Linus PASS verdict on PR #258 (gqlc-3w2):\n\n1. strictInt messages omit line info (internal/config/config.go ~199-204): 'field \"version\" must be a YAML integer (got !!float \"1.5\")' carries no 'line N:' even though value.Line is available in the unmarshaler; sibling enum errors do carry it. The spec catalogue documents the line-less shape so there is no drift — pure polish. If fixed, update the §6.3 row + test substrings together (three-way consistency).\n\n2. docs/specs/config-file-format.md §6.3 completeness micro-gap: a genuine !!int that overflows Go int (version: 9223372036854775808) produces a sane, prefixed, line-carrying yaml unmarshal error whose shape is absent from a catalogue that claims to list every loader message. Either add the row or soften the completeness claim.\n\nNoted and deliberately NOT findings: 'version: !!int \"1\"' and 'version: 0x1' load — genuine !!int spellings of 1, consistent with §3.","notes":"3rd NIT (from Linus PASS on PR #264 / gqlc-suj): docs/specs/config-file-format.md §8 rationale bullet is still titled 'Required single-value enums' while its body now narrates the driver axis growing to two members — the title reads historically but is false for one of three axes. Retitle (e.g. 'Required enums, no defaults') next time the doc is touched. Cosmetic only.","status":"closed","priority":4,"issue_type":"task","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-12T04:40:29Z","created_by":"antranig-yeretzian","updated_at":"2026-07-14T03:42:25Z","started_at":"2026-07-12T14:08:23Z","closed_at":"2026-07-12T14:19:06Z","close_reason":"Shipped in PR #266 (squash-merged, CI green). Linus round 1: verified PASS — line-anchored version-probe errors proven value-anchored via reviewer scratch probes, overflow row deterministic under pinned yaml.v3, three-way impl/spec/test consistency verbatim, §8 retitled. Residual NIT (explicitly-tagged garbage int uncatalogued) filed as its own P4 bead.","external_ref":"https://github.com/areqag/gqlc/issues/302","dependency_count":0,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-3i3","title":"R7 follow-up nits: spec scalar-casing gloss + two stale doc comments","description":"Three nit-class follow-ups from the R7 cycles (PR #107 spec Linus PASS round 2; PR #108 code Linus PASS round 1). (1) docs/specs/resolver-stage-r7.md:465 quotes R2 literal goldens as {kind: \"scalar\", scalar: \"INT\"} but the goldens pin lowercase \"int\" (the property-arm \"type\": \"INT\" prose is correct) — one-word casing fix. (2) internal/resolver/errors.go ErrOutOfR0Scope doc comment still lists 'call bindings' among the unsupported constructs; spec §5.2 designated this comment for update when the BindingCall gate lifted (behaviorally retired in f2abd31) — remove 'call' from the 'path / unwind / call bindings' clause. (3) internal/resolver/resolve.go:121-123 comment still says the registry 'is reserved for R7. When R7 lands the CALL/YIELD handler here, drop this discard and route the registry into that handler' — contradicts the shipped §4.4 trust posture (discard STAYS; registry consulted only at parse). Rewrite to state the trust posture, matching resolver.go's updated WithRegistry doc.","status":"closed","priority":4,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-06T06:10:00Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:18Z","started_at":"2026-07-10T05:24:03Z","closed_at":"2026-07-10T06:07:18Z","close_reason":"PR #144 merged (R7 nits: scalar-casing INT→int, ErrOutOfR0Scope doc drop 'call', WithRegistry doc post-0ig trust posture). Linus PASS.","external_ref":"https://github.com/areqag/gqlc/issues/198","metadata":null,"dependencies":[{"issue_id":"gqlc-3i3","depends_on_id":"gqlc-0mx.9","type":"blocks","created_at":"2026-07-06T02:10:00Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-yyg","title":"R3 follow-up nits: same-binding self-loop fixture + spec count→weight gloss","description":"Two nit-class follow-ups from the R3 code review (PR #96, Linus PASS). (1) Add a same-binding self-loop fixture — (p:Person)-[r:KNOWS]-\u003e(p) — to lock touchingSide/candidateTypes behavior; existing self_loop_directed.cypher only covers the schema-level self-loop (Source==Target label), not a single VarEndpoint on both sides. (2) docs/specs/resolver-stage-r3.md §6.4 names the type-differs property 'count' but the implementation uses 'weight' because COUNT is a GQL reserved word (GQL.g4:3319) — amend the spec line with a one-line gloss.","status":"closed","priority":4,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-05T07:46:49Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T06:07:18Z","started_at":"2026-07-10T05:24:04Z","closed_at":"2026-07-10T06:07:18Z","close_reason":"PR #146 merged (R3 nits: self-loop fixture + §6.4 count→weight gloss + COUNT reserved-word gloss). Linus PASS.","external_ref":"https://github.com/areqag/gqlc/issues/199","metadata":null,"dependencies":[{"issue_id":"gqlc-yyg","depends_on_id":"gqlc-0mx.5","type":"blocks","created_at":"2026-07-05T03:47:04Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":0,"comment_count":0}
{"_type":"issue","id":"gqlc-23e","title":"Release automation: DEFERRED (workflow-file guard blocks all in-workflow identities)","description":"## Repurposed 2026-07-18\n\nOriginal scope (release-please, no binaries) has been superseded. See bead `gqlc-23e` for the full current design.\n\n**Now**: automated release tagging + Go CLI binary artifacts on every release cut from master.\n\n### Driver\n\n- **tagpr** (Songmu/tagpr, v1.20.1) + **GoReleaser**, single-workflow pattern.\n- No manifest JSON, no Node runtime, standard `GITHUB_TOKEN`. Rolling Release-PR UX identical to release-please.\n\n### Why not release-please anymore?\n\nResearch (2026-07-18) surfaced three concrete blockers with the original 2025 plan:\n\n- `google-github-actions/release-please-action` is archived. Live path is `googleapis/release-please-action@v5`.\n- Seeding the release-please manifest at `0.0.0` is broken ([release-please#2087](https://github.com/googleapis/release-please/issues/2087)) — first `feat:` proposes `1.0.0`.\n- `GITHUB_TOKEN` cannot trigger required status checks on the Release PR — would need a GitHub App token dance.\n\ntagpr sidesteps all three and matches this project's CI-speed priority.\n\n### Scope\n\n- **Semver**: `0.0.x` while experimental. VERSION file seeded at `0.0.0`; first release is `v0.0.1`.\n- **Binaries**: linux + darwin, amd64 + arm64. Four tar.gz archives + SHA256SUMS per release. Windows deferred.\n- **Homebrew tap**: deferred to a follow-up bead (`gqlc-1dq`) once the pipeline is proven stable.\n- **Cadence**: `on: push: branches: [master]`. Never a required status check; cannot block merges. Feature PRs see zero release-related jobs.\n\n### Acceptance\n\nSquash-merging feature PRs updates a rolling Release PR (tagpr-managed). Merging the Release PR cuts a `v*` tag + GitHub Release with the four binaries + checksums. First release is `v0.0.1`. Manual tag pushes blocked by a tag ruleset. CODEOWNERS present.\n\n_Full design, notes, and acceptance criteria live on bead `gqlc-23e`._\n","design":"1) .github/workflows/release.yml — on: push: branches: [master]. Single job. Steps:\n   - actions/checkout@v4 with fetch-depth: 0\n   - actions/setup-go@v5 (goreleaser needs a Go toolchain)\n   - Songmu/tagpr@v1 → outputs.tag (opens/updates a Release PR; on merge of Release PR, creates the tag + GitHub Release)\n   - goreleaser/goreleaser-action@v6 gated on `steps.tagpr.outputs.tag != ''`, args: release --clean\n\n2) .tagpr config (repo-root .tagpr file):\n   - version_file: VERSION\n   - release_branch: master\n   - major_labels: (none while pre-1.0)\n\n3) VERSION seeded at 0.0.0 (tagpr convention; first bump becomes v0.0.1). Also read by cmd/gqlc version command via linker flags.\n\n4) .goreleaser.yaml:\n   - builds: cmd/gqlc, GOOS=[linux, darwin], GOARCH=[amd64, arm64], CGO_ENABLED=0\n   - ldflags: -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}\n   - archives: tar.gz name_template = \"{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}\"\n   - checksum: sha256 (SHA256SUMS file)\n   - brews: block present but commented, referencing follow-up bead\n\n5) .github/CODEOWNERS (from original 2026-07-01 design, still valid — last matching pattern wins):\n   *                       @antranig-yeretzian\n   /.github/               @antranig-yeretzian\n   /.goreleaser.yaml       @antranig-yeretzian\n   /.tagpr                 @antranig-yeretzian\n   /VERSION                @antranig-yeretzian\n\n6) Tag ruleset (GitHub UI, manual post-merge step) restricting tag creation to bots — prevents accidental manual v* tag pushes.\n\n7) No just recipe: tagpr is GitHub-side automation, not a local check — nothing to run locally, so the CI-parity rule doesn't apply.\n","acceptance_criteria":"Squash-merging feature PRs to master updates a rolling Release PR (tagpr-managed) with an incremented VERSION and CHANGELOG.md entry. Merging the Release PR creates a v* tag + GitHub Release with linux/darwin × amd64/arm64 tar.gz binaries + SHA256SUMS. Manual tag pushes blocked by tag ruleset. CODEOWNERS present. First release is v0.0.1. release.yml is push-triggered only — never a required status check, cannot block merges. cmd/gqlc version command reports the tagpr-injected VERSION at runtime.","notes":"Research (2026-07-18) driving the driver change from release-please to tagpr:\n\n- release-please + GoReleaser remains valid (used by k8sgpt-ai/k8sgpt, cloudquery/cloudquery, lindell/multi-gitter). But three concrete problems with the 2025 design:\n  * google-github-actions/release-please-action is now ARCHIVED. Live path is googleapis/release-please-action@v5 (v5.0.0 Node 24 bump 2026-04-22).\n  * Seeding manifest at 0.0.0 is BROKEN (release-please#2087) — first feat: proposes 1.0.0. Would need to seed at 0.0.1 with bump-patch-for-minor-pre-major=true.\n  * GITHUB_TOKEN cannot trigger required status checks on the Release PR (release-please-action#1179) — would need actions/create-github-app-token@v1.\n\n- tagpr sidesteps all three: no manifest JSON, standard GITHUB_TOKEN works, no separate action-path/version bookkeeping. Same rolling Release-PR UX. Used by kazeburo/chocon, pyama86/pachanger, babarot/enhancd, ccusage/ccusage, youki-dev/youki.\n\n- Binary storage cost: GitHub Releases are free and unmetered. Only cost is CI minutes (~3 min per release for 4-arch build + upload). Public repo → free.\n\n- CODEOWNERS interaction: while solo, the catch-all makes explicit entries redundant; value is documenting intent for when maintainers arrive. tagpr's Release PR touches VERSION + CHANGELOG.md — once 'Require review from Code Owners' is enabled, owner must approve every Release PR (acceptable/desirable: releases become an explicit owner decision).\n\n- Homebrew tap deferred: goreleaser has native brews: support; empty companion repo areqag/homebrew-gqlc + a HOMEBREW_TAP_GITHUB_TOKEN secret enables `brew tap areqag/gqlc \u0026\u0026 brew install gqlc`. Tracked in follow-up bead.\n\nCORRECTION (2026-07-18, from Linus review): the description/design's claim that \"Conventional-Commit PR titles drive bumps (feat: → patch, feat!: → minor)\" is inaccurate for tagpr. tagpr bumps are LABEL-driven, not commit-message-driven — apply `tagpr:minor` or `tagpr:major` labels on the merged Release PR (or on the individual feature PR before it merges) to control the bump. Default is patch. Conventional Commits shape is still valuable for changelog readability, but does not automate the bump.\n\nFOLLOW-UP RISK for gqlc-1dq: goreleaser v2 has moved the Homebrew key. The commented `brews:` stub in .goreleaser.yaml uses a key soft-deprecated in v2.10 and hard-deprecated in v2.16. When gqlc-1dq is picked up, run `goreleaser check` before uncommenting; expect to replace with the current key (verify against goreleaser docs at that time — the surface may have shifted again).\nDEFERRED 2026-07-19 (PR #349): After ~20 hours of iteration, GitHub's workflow-file guard reproducibly blocks every documented mechanism for cutting release tags via GitHub Actions on this repo. All release-pipeline infrastructure has been deleted; bead stays OPEN as deferred/blocked. Users install via `go install github.com/areqag/gqlc/cmd/gqlc@latest`.\n\nINVESTIGATION SUMMARY:\n\nThe workflow-file guard is a hidden GitHub behavior that blocks any token-based identity used inside a GitHub-hosted Actions runner from creating tag refs when any commit in the tag's history touches .github/workflows/**. Since every release-pipeline modification is inherently a workflow-file change, the guard triggers on every attempted tag cut.\n\nAttempts and outcomes (all reproducibly reject with GH013 \"Cannot create ref due to creations being restricted\"):\n\n  1. GitHub App via git push --tags — PRs #334, #336, #337 (tagpr scheme = release-please-alternative)\n  2. Immutable-releases + goreleaser mode=replace — PR #338 (dead end: goreleaser cannot delete published-immutable)\n  3. App with tagpr release=draft + goreleaser use_existing_draft — same PR #338 (draft-then-publish worked around immutability but tag creation still blocked)\n  4. Fine-grained PAT (Contents/PRs/Issues R/W) — PR #340 → run 29670537576\n  5. Fine-grained PAT + Workflows R/W — same secret rotated (Linus's non-blocking note said this might be the missing scope; empirically it was not)\n  6. GitHub App via REST API POST /git/refs (bypassing git wire protocol) — PR #342, diagnostic step\n  7. Classic PAT (`repo` + `workflow` scopes) — PR #345 → run 29672955808\n  8. SSH deploy key with write access + webfactory/ssh-agent — PR #347 → run 29673254598\n\nCommunity references consulted (all gave contradictory guidance):\n  - github/community#116660 — \"PAT is the only solution\" (Apr 2024, referred to classic PATs)\n  - github/community#109715 — App tag push fails on workflow-touching history\n  - github/community#141159 — Fine-grained PAT with workflow scope still fails (2026, unresolved)\n  - github/community#68419 — Deploy key + ruleset bypass (2022, pre-guard-tightening)\n\nEmpirical evidence that the guard is context-specific to GitHub-hosted runners:\n  - My user identity from local dev machine via SSH keys: pushed test tags to same SHAs successfully\n  - My user identity from local dev machine via HTTPS + gho_ OAuth token: pushed test tags successfully\n  - My user identity from GitHub CLI: created tags via REST API successfully\n  - Every in-workflow identity type (App, all PAT types, deploy key): failed with GH013\n\nHypothesis: GitHub tightened the workflow-file guard sometime in 2025-2026 to cover all in-workflow identities, including previously-bypassing deploy keys. This is not documented in official GitHub docs (verified 2026-07-19).\n\nREVISIT TRIGGERS:\n  - GitHub publishes documentation of this behavior with an official escape hatch\n  - A new identity type or workflow-context switch appears that empirically bypasses\n  - We accept manual tag pushes and shift the automation to trigger goreleaser on push:tags:v*\n  - We migrate to self-hosted runners (speculative — unclear if the guard is IP-scoped or identity-scoped)\n\nFOLLOW-UP TASKS:\n  - Delete stale repo secrets TAGPR_APP_ID, TAGPR_APP_PRIVATE_KEY, TAGPR_DEPLOY_KEY, TAGPR_PAT\n  - Optional: uninstall gqlc-release-bot App from areqag/gqlc\n  - Optional: delete gqlc-release-tagpr deploy key from repo settings\n  - Follow-up bead gqlc-1dq (Homebrew tap) becomes moot until release automation returns; leave as-is","status":"blocked","priority":4,"issue_type":"task","owner":"antranig.yeretzian@proton.me","created_at":"2026-07-02T01:33:10Z","created_by":"antranig-yeretzian","updated_at":"2026-07-19T04:33:30Z","started_at":"2026-07-02T01:42:30Z","external_ref":"https://github.com/areqag/gqlc/issues/171","metadata":null,"dependencies":[{"issue_id":"gqlc-23e","depends_on_id":"gqlc-nmg","type":"blocks","created_at":"2026-07-01T21:33:26Z","created_by":"antranig-yeretzian","metadata":"{}"}],"dependency_count":1,"dependent_count":1,"comment_count":0}
{"_type":"issue","id":"gqlc-zir","title":"Parser: unary-minus over a projection (RETURN -n.num) accepts as a plain ref","description":"Linus review of Stage 3 unlock flagged that RETURN -n.num parses as a RefProjection to n.num rather than residual ErrUnsupportedProjection. Cause: the shared precedence-tower collapse (shape.go nonArithmeticFromAddSub) reads OC_UnaryAddOrSubtractExpression(0) without rejecting a leading '-'. Pre-existing behaviour governing Stage 0-2 too, not introduced by Stage 3, and no TCK read-core scenario exercises it — hence non-blocking. Decide whether a unary sign is arithmetic that should be residual (value lives below the type-interface boundary either way, ADR 0005) and tighten the helper if so.","status":"closed","priority":4,"issue_type":"bug","assignee":"antranig-yeretzian","owner":"antranig.yeretzian@proton.me","created_at":"2026-06-04T02:00:57Z","created_by":"antranig-yeretzian","updated_at":"2026-07-10T05:46:03Z","started_at":"2026-07-02T03:16:55Z","closed_at":"2026-07-02T03:29:46Z","close_reason":"PR #36 merged: unary-signed operands now residual via GetChildCount guard in nonArithmeticFromAddSub","external_ref":"https://github.com/areqag/gqlc/issues/200","dependency_count":0,"dependent_count":0,"comment_count":0}
