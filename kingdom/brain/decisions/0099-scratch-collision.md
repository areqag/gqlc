# Scratch collision, not a decision

This file exists to be refused. It claims ordinal 0001, which
`0001-founding.md` already holds on master, so it is the acceptance probe for
bd gqlc-4plwf: a PR that collides with a number the base already carries.

It is deleted with its branch as soon as the probe has been read. If you are
reading it on master, the probe leaked and the file should be removed.
